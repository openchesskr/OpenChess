# OpenChess

체스 오프닝 학습·연습 애플리케이션.

## 개발 워크플로우 규칙 (모든 세션 필독)

- **PR을 임의로 생성·머지하지 말 것.** 어떤 이유로든(작업이 끝났다고 판단되더라도, 이전에 "PR 만들자"는 지시가 있었더라도) 새로 PR을 열거나 머지하기 전에는 반드시 사용자에게 먼저 물어보고 명시적 승인을 받는다. 문서 수정·버전 라벨 정정처럼 사소해 보이는 변경도 예외 없음 — 커밋/푸시까지만 하고 PR 생성 여부는 승인 후 결정한다.
- **PR 번호를 버전 번호에 맞춘다.** 이 프로젝트는 PR 번호와 배포 버전 번호를 의도적으로 맞춰 나간다. 예: v0.1.0(비교적 큰 단위 업데이트)은 PR #10에 맞출 예정. 새 PR을 만들기 전 다음 PR 번호가 계획한 버전과 맞는지 확인하고, 안 맞으면 사용자에게 확인받는다.
- **"버전 기록"은 PR 생성 전에 자동으로 채워 넣는다.** 아래 "개발자 기록"(채팅 전용, 머지 후 1회)과는 별개로, README `## 버전 기록` 섹션과 `src/App.jsx`의 CHANGELOG 배열(접속 시 뜨는 공지 모달의 내용이자 APP_VERSION이 파생되는 곳), 그리고 `src/AboutPage.jsx`의 `VERSION_HISTORY` 배열(`/about` 소개 페이지의 버전 기록 탭)까지 **세 곳 모두** 매 세션·매 버전마다 PR을 만들기 전에 그 버전 커밋에 포함해 둔다 — 사용자에게 물어보지 않고 항상 진행하며, 세 곳 중 하나라도 빠뜨린 채 넘어가지 않는다(과거 v0.1.2·v0.1.3이 `VERSION_HISTORY`에서만 누락된 채 배포된 적이 있었음 — 재발 방지). README 버전 기록은 기존 항목들과 같은 형식(`### OpenChess vX.Y.Z — 날짜` 헤더 + 보안/성능/기능/버그 수정/UI/UX/시스템 등 실제 해당하는 카테고리만 굵게, 개발자 관점의 상세 서술)으로 적고, CHANGELOG 배열에는 그 내용을 개발 용어 없이 사용자 입장에서 쉬운 말로 다시 정리해 새 항목을 배열 맨 앞에 추가한다. `AboutPage.jsx`의 `VERSION_HISTORY`에는 같은 내용을 CHANGELOG와 같은 쉬운 말로, 다만 카테고리(`feature`/`ui`/`ux`/`perf`/`fix`/`security`)로 나누고 `summary` 한 줄 요약·마스코트 도입부/마무리 멘트(`mascot.intro`/`outro`)·대표 기능 하이라이트(`highlight`)까지 갖춰 배열 맨 앞에 추가한다(버전 번호는 세 곳이 항상 일치해야 함).
- **매 버전(모든 세션)마다 "개발자 기록"을 남긴다.** 이건 사용자가 머지 후에 체크해야 할 할 일(TODO) 목록이 아니라, 그 버전에서 실제로 무엇을 개발했고 무엇이 어떻게 적용됐는지에 대한 기술적 기록이다 — 원인 분석, 선택한 해결 방식, 실제로 바뀐 동작 위주로 서술한다. PR 본문이나 README 버전 기록에 적지 않고, 채팅으로 사용자에게 텍스트로 직접 작성해 보여준다. 문장은 사용자가 개발 중 쓰는 말투를 살려 "~할 것" 형식의 간결한 항목체로 적는다.
  - **시점**: 세션 중 커밋할 때마다 바로바로 남기지 않는다. PR을 생성하고 사용자가 머지한 뒤, 그 PR(버전)에 실제로 들어간 변경사항 전체를 한 번에 모아 요약해서 남긴다 — 같은 세션에서 여러 번 커밋했어도 기록은 머지 후 한 번만.
  - **형식**: 날짜 → "개발 방향 : ..." 한 줄 요약 → 카테고리별(보안/성능/기능/UI/UX/시스템/의존성 등 실제 해당하는 것만) 번호 매긴 목록. 예시:
    ```
    2026/7/14

    개발 방향 : 보안 강화 및 성능 개선

    보안
    1. puzzles 테이블 RLS를 손볼 것 — update가 using(true) with check(true)로 전체 개방돼 있어 로그인 없이도 임의 퍼즐의 solves/likes 카운터를 직접 덮어쓸 수 있었음. 컬럼 단위 grant로 좁혀 두 카운터는 puzzle_solve/puzzle_like_toggle RPC로만 바뀌게 할 것.
    2. notifications insert 정책을 강화할 것 — auth.uid() is not null만 검사해 로그인만 하면 아무 유저에게나 아무 종류의 알림을 위조해 보낼 수 있었음. kind별로 실제 발생 가능한 상황인지 서버에서 검증할 것.

    성능
    1. 알림·친구요청 배지·채팅창·안읽은 채팅 배지 폴링을 Supabase Realtime 구독으로 전환할 것 — 소켓이 조용히 끊기는 경우를 대비해 느슨한 안전망 폴링만 남길 것.
    ```
- **용어: "국면"이 아니라 "포지션"을 쓴다.** 웹사이트에서 사용자에게 보이는 텍스트(테마 설명, 힌트 문구 등)에는 체스 위치를 가리킬 때 "국면" 대신 "포지션"이라는 용어를 쓴다. 새로 추가하는 화면 텍스트도 이 규칙을 따른다.
- **SQL은 MIGRATION_vX.Y.Z.sql 같은 증분 파일을 만들지 않는다.** DB에 반영해야 할 SQL 변경이 있는 세션은, `supabase-setup.sql`(항상 처음부터 다시 실행해도 안전한, 지금까지의 모든 스키마·함수·정책을 담은 통합 파일)을 그 변경까지 반영해 최신 상태로 갱신해 두고, 세션이 끝날 때(커밋 이후, PR 생성 여부와 무관하게) 그 `supabase-setup.sql` 전체를 SendUserFile로 사용자에게 첨부한다 — 사용자는 그 파일 전체를 Supabase SQL Editor에 그대로 붙여넣고 실행하면 된다. 이번 버전에서 바뀐 부분만 따로 떼어 별도 파일로 만들지 않는다(과거 `MIGRATION_v0.4.x.sql` 파일들은 이 규칙 도입 이전 방식이었다 — 새로 만들지 않는다).

## 버전 기록

### OpenChess v0.4.5 — 2026/9/3

**버그 수정 — 맞친구 요청(교차 요청) 자동 수락 시 알림 누락**
`friend_request`/`friend_request_by_mid`는 내가 요청을 보낼 때 상대가 이미 나에게 pending 요청을 보내둔 상태면 그 자리에서 자동으로 `accepted`로 승격시키고 그 상태를 그대로 반환한다(`supabase-setup.sql:196-245`). 그런데 클라이언트의 세 호출부(`UserProfilePage`의 자동 초대·수동 버튼, 친구 모달 `doRequestByName`)는 모두 `status === "pending"`일 때만 `notifyCreate(..., "friend_request", ...)`를 불렀고, `"accepted"` 분기에서는 아무 알림도 만들지 않았다 — 원래 요청을 먼저 보냈던 쪽(B)은 상대(A)가 뒤늦게 같은 요청을 보내 자동으로 친구가 맺어졌다는 사실을 알림으로도 화면으로도 전혀 알 수 없었다(A만 "~님과 친구가 되었어요!" 팝업을 봤다). 이제 `"accepted"` 분기에서도 `doAccept`와 동일하게 상대에게 `friend_accepted` 알림을 보내고, 내 알림함에 남아 있던 그 사람의 원래 `friend_request` 알림도 `notifyResolveFriendRequest`로 "수락함" 처리한다.

**버그 수정 — PvP 재접속 시 정상 진행 중인 대국을 좀비로 오판해 강제 종료**
`/play` 진입 시 서버에 남아 있는 내 `active` 대국을 이어받는 effect가, `updated_at`이 2분 이상 오래됐으면 무조건 "죽은 대국"으로 판정해 `pvp_finish(..., "aborted")`로 곧장 중단 처리했다. 그런데 `updated_at`은 실제로 수를 둘 때만(`pvp_move`) 갱신되고 별도 하트비트가 없어, 10|0보다 긴 시간제어(예: 60|30)에서 상대가 2분 넘게 정상적으로 생각 중일 때 내가 새로고침하거나 다른 화면에 있다 돌아오기만 해도 아직 아무도 떠나지 않은 대국을 내 클라이언트가 혼자 기권 처리해 버렸다. `pvp_queue_join`(대기열 합류 RPC)에도 같은 2분 기준의 좀비 판정이 이미 있어 실제 방치된 대국은 "대국 상대 찾기"를 누르는 시점에 정리되므로, 이 페이지 진입 시 수동 확인에서는 서버에 중단을 보고하지 않고 그냥 이어받지 않기만 하도록 좁혔다 — 대국은 `active`로 그대로 남아 상대가 있다면 계속 이어갈 수 있고, 진짜 좀비 정리는 기존 경로(`pvp_queue_join`)에 맡긴다.

**시스템 — package.json 버전 표기 정정**
`package.json`의 `version`이 오래전 `0.3.8`에 멈춰 있었다(앱 내 표시 버전은 `AboutPage.jsx`의 `VERSION_HISTORY[0].version`에서 파생돼 실제로는 영향 없었음). 배포 버전에 맞춰 최신화했다.

**시도했다가 되돌림 — "대국 상대 찾기"를 누르면 이미 끝난 대국으로 되돌아가는 문제**
체크메이트를 당한 쪽의 클라이언트가 결과 보고 전에 탭을 닫아버리면 `pvp_games` 행이 `status='active'`로 계속 남아, 승자가 몇 분 안에 "대국 상대 찾기"를 다시 누르면 `pvp_queue_join`이 새 매칭 대신 그 이미 끝난 대국으로 되돌려주는(재접속 처리) 문제가 있다. 처음엔 `pvp_finish`에 `p_objective` 플래그를 추가해, 체크메이트처럼 서버가 신뢰하는 `sans`만으로 객관적으로 계산되는 결과라면 승자 쪽도 보고할 수 있게 하려 했다 — 하지만 `pvp_move`가 SAN 합법성 자체를 검증하지 않는 구조라, 이 플래그는 결국 클라이언트가 "이 결과는 객관적이다"라고 스스로 주장하는 값일 뿐이었다. 악의적인 클라이언트가 조작된 수순으로 가짜 체크메이트를 만든 뒤 이 플래그로 자기 승리 선언 금지를 우회해 즉시 승리를 우겨 넣을 수 있는, 그 가드가 애초에 막으려던 것과 정확히 같은 종류의 권한 우회여서(보안 검토에서 지적) 되돌렸다. 서버가 `sans`를 직접 재생해 체크메이트 여부를 검증하지 않는 한 안전하게 해결할 방법이 없어, 근본 수정(승자 쪽이 즉시 결과를 확정하는 것)은 다음 버전 이후로 미룬다.

**시도했다가 되돌림 — 위 문제의 완화 시도: "대국 상대 찾기"가 좀비 대국을 붙잡고 있는 시간을 2분에서 10초로 단축**
사용자 제보(화면 녹화)로 실제 재현을 확인한 뒤, `pvp_queue_join`이 "최근" 대국이라 판단해 그대로 돌려주던(재접속 처리) 기준을 2분에서 10초로 줄여 체감 지연을 최소화하려 했다. 하지만 이 기준은 대칭적이라는 걸 놓쳤다 — "내 대국이 오래 조용하면 좀비로 보고 정리한다"는 규칙은 지금 지고 있는 쪽에게도 그대로 적용된다(보안 검토에서 지적). 시간제어가 있는 대국에서는 상대 차례일 때 몇 초쯤 조용한 게 정상인데, 10초는 지고 있는 쪽이 그 몇 초를 그냥 기다렸다가 "대국 상대 찾기"를 눌러 자기 대국을 스스로 `aborted`로 만들어 패배를 회피하기에 충분히 짧은 시간이었다(원래 2분은 그렇게 하기엔 대국 시계상 손해가 너무 커서 사실상 쓸모없는 회피 수단이었다). 서버가 `sans`를 직접 재생해 "정말 조용한 것"과 "정말 끝난 것"을 구분하지 않는 한 안전한 기준을 고를 수 없어, 원래의 2분으로 되돌린다 — 이 문제의 근본 수정은 위 항목과 마찬가지로 다음 버전 이후로 미룬다.

**버그 수정 — 대국 결과 팝업의 "대국 리뷰 보기" 버튼이 화면을 넘어가지 않던 문제**
버튼을 누르면 `openReview`가 `reviewGame`을 정상적으로 세팅하고 주소도 `/review/(식별자)`로 정확히 바뀌는데도, 화면은 계속 대국 화면에 머물러 있는 것처럼 보였다. 실제로는 `ReviewPage`도 함께 마운트돼 있었지만 `PlayPage` 뒤에 완전히 가려져 있었을 뿐이다 — App 루트가 `{reviewGame && <ReviewPage/>}`를 `{playGame && <PlayPage/>}`보다 먼저(더 위쪽 JSX 순서로) 렌더링하는데, 두 오버레이의 최상위 `position:fixed` wrap이 하필 같은 `zIndex:300`을 쓰고 있어(같은 z-index끼리는 CSS 스택 순서가 DOM 순서로 정해짐) 나중에 그려지는 `PlayPage`가 항상 `ReviewPage` 위를 덮어버렸다 — 리뷰를 여는 경로 자체가 항상 `PlayPage` 위에서 시작되므로 100% 재현됐다. `ReviewPage`의 `zIndex`를 310으로 올려 항상 위에 뜨도록 고쳤다.

**UI — "대국 리뷰 보기" 버튼 리디자인**
예전엔 재대결·닫기 버튼들과 마찬가지로 금색(`T.brass`) 계열이라, 이 버튼을 누르면 다른 화면(리뷰)으로 넘어간다는 신호가 색만 봐서는 드러나지 않았다. 리뷰 기능 자체를 상징하는 색(`T.best`, "최선의 수" 등급에 쓰이는 연두색)과 흰색 별 아이콘(`Star`)을 가져와 시각적으로 구분되게 했다.

### OpenChess v0.4.4 — 2026/9/2

**버그 수정 — 퍼즐 만들기가 무작위로 "이미 존재함"으로 거부되던 핵심 버그**
`puzzles` 테이블의 행 번호(`no`)는 퍼즐 id 문자열의 FNV-1a 해시값(`puzzleNo`, 6자리·90만 개 공간)이다. 퍼즐 만들기 마법사의 중복 검사(`checkPcDuplicate`)는 그 번호에 아무 행이나 있으면 내용을 비교하지 않고 곧장 "이미 같은 퍼즐이 존재하여 퍼즐 만들기가 취소됩니다"로 막았다 — 사이트에 만들어진 퍼즐이 쌓일수록 서로 완전히 다른 포지션인데 번호만 우연히 겹치는 경우가 늘어나, 실제로는 새로운 PGN·FEN으로 퍼즐을 만들려는 시도까지 무작위로 거부되는 게 핵심 원인이었다. 이제 그 번호의 행이 있어도 실제 내용(`data.id`)이 지금 만들려는 id와 같을 때만 진짜 중복으로 판정한다. 같은 이유로 `puzzleShare`(서버 저장)도 번호만 우연히 겹친 무관한 퍼즐의 데이터를 덮어쓰지 않도록, 저장 전 서버 행의 `id`가 다르면 그 저장만 조용히 건너뛰게 방어했다(이 세션의 로컬 상태에는 이미 반영돼 있어 풀이는 계속 가능하고, 서버 공유만 못 하게 된다 — 매우 드문 충돌 한정).

**버그 수정 — 퍼즐에서 언더프로모션이 불가능하던 문제**
퍼즐 풀이 화면(`PuzzleSolver`)에는 승격 기물을 고르는 UI가 아예 없어, 폰이 마지막 랭크에 닿으면 `buildSan`이 인자 없이 호출돼 항상 퀸으로만 승격했다 — 언더프로모션(룩/비숍/나이트)으로만 통과되는 라인은 구조적으로 풀 수 없었다. `ReviewPage`·`PlayPage`가 이미 쓰던 `promoPrompt`(대기 중인 승격 좌표) 상태 + `ReviewPromoPrompt` 컴포넌트 패턴을 그대로 들여와, 폰이 마지막 랭크로 이동하는 수는 곧장 두지 않고 먼저 승격 기물을 고르게 한다.

**버그 수정 — 채팅 `/play` 명령어가 안 되던 원인**
`pvp_invite_friend()`는 상대에게 이미 보낸 pending 초대가 있으면 "중복 방지"로 아무것도 하지 않고 그 초대를 그대로 돌려주기만 했다 — 채팅 메시지도 새로 남기지 않고 받는 쪽 화면도 갱신되지 않았다. 응답 없이 방치된 초대 하나만 있어도, 그 뒤로는 채팅에서 "/play 3"을 몇 번을 입력해도(입력창은 비워지니 성공한 것처럼 보이지만) 아무 일도 일어나지 않아 "명령어가 안 된다"는 것과 정확히 같은 증상으로 재현됐다. 이제 이미 pending 초대가 있어도 `time_control`·`updated_at`을 새 요청으로 갱신하고 채팅에도 매번 새 카드를 남긴다 — 받는 쪽 `GlobalPvpInviteBanner`는 이 테이블을 `event:"*"`로 구독하므로 update만으로도 다시 뜬다. 아울러 "/play " 뒤에 유효하지 않은 타임컨트롤이 와도(예: 그냥 "/play 같이 하자" 같은 평범한 문장) 무조건 명령어로 인식해 "사용법: ..." 오류로 전송을 막던 것을, 실제로 유효한 타임컨트롤일 때만 명령어로 인식하도록 바꿨다 — 그 외에는 애초에 명령어 취급을 하지 않고 평범한 텍스트로 그대로 전송된다.

**버그 수정 — 실시간 PvP 여러 건**
① 대국 도중 새로고침하거나(언마운트 cleanup이 걸리지 않는 하드 리로드) 잠시 다른 화면에 있다가 `/play`로 돌아오면, 서버에는 여전히 `active`로 남아 있는 내 대국을 이어받을 방법이 없어 상대는 계속 내 응수를 기다리고 나는 새로 시작할 수밖에 없었다 — `/play` 진입 시(도전장 수락으로 이어받는 경우가 아니라면) 서버에서 내가 참가자인 진행 중 대국을 한 번 확인해 있으면 곧장 이어받도록 했다. ② 내가 수를 둘 때 `pvp_move` RPC가 실패해도(네트워크 오류, 아직 서버에 반영 안 된 상태에서의 "내 차례 아님" 등) 조용히 무시해, 실제로는 서버에 반영되지 않은 수가 내 화면에만 영원히 남아 완전히 어긋난 상태로 굳는 문제가 있었다 — 실패하면 서버가 마지막으로 확정한 수순으로 다시 맞춰 되돌린다.

**기능 — MID 형식 고정(앞 영문 5자리 + 뒤 숫자 4자리)**
계정 센터의 MID가 9자리 중 아무 자리에나 영문/숫자가 무작위로 섞여 있어 사람이 부르거나 받아 적기 불편했다 — `gen_mid()`가 앞 5자리는 영문 대문자만, 뒤 4자리는 숫자만 뽑도록 바꿨다(예: `ABCDE1234`). 기존에 옛 형식으로 이미 발급된 MID는 새 형식으로 다시 발급했다.

**UI — 퍼즐 카드 레이아웃 재정비**
사용자 스케치 요청대로, 오프닝 배지를 PGN 배지 바로 옆에 붙이고(예전엔 서로 다른 줄에 떨어져 있었다), 국면·FEN 배지는 그 아래 줄에 이어 붙였다. 6자리 퍼즐 번호는 왼쪽, 퍼즐 레이팅은 같은 줄 오른쪽으로 다시 짝지었다(예전엔 번호가 오른쪽에, 레이팅은 배지 줄 한가운데 섞여 있었다). 좋아요·리포스트·공유 수는 왼쪽에 묶고, 실제 공유하기 버튼만 그 줄의 오른쪽 끝(카드 우하단)으로 분리했다.

**버그 수정 — 방금 만든 퍼즐에 생성자가 표시되지 않던 문제**
퍼즐 만들기(`submitPuzzleCreate`)는 저장과 동시에 그 퍼즐을 곧장 연다(`setActive`) — 실제 생성자 기록은 `onSavePuzzle` 안에서 fire-and-forget으로 도는 `puzzleShare`(행 upsert → `puzzle_claim_creator` RPC, 순차 네트워크 요청 2번)가 서버에 반영해야 조회된다. `PuzzleSolver`의 `creatorInfo` 조회는 마운트 시 딱 한 번만 시도해, 그 두 요청이 끝나기 전에 항상 먼저 도착해 매번 "생성자 없음"으로 확정되고 재시도가 없어 이후에도 영원히 반영되지 않았다 — 방금 만든 퍼즐마다 100% 재현됐다. 못 찾으면 짧은 간격(1·2·3·4초)으로 몇 번 더 재시도하도록 고쳤다.

**기능 — MID 기반 유저 검색 + 친구 초대 링크 + 프로필 전용 페이지**
사용자 요청 — "MID로 친구 추가·검색, 초대 링크로 자동 친구 추가, 프로필을 별도 페이지로". 유저 검색창(`userSearch`)이 `#ABCDE1234` 형식(정규식 `MID_RE`)을 인식하면 `username ilike` 검색 대신 `userProfileByMid`로 mid를 직접 조회한다 — `profiles` 테이블은 이미 `select using (true)`(전체 공개)라 별도 RLS 없이 REST로 바로 조회된다. 서버에 `friend_request_by_mid(text)` RPC를 새로 추가했다 — 기존 `friend_request(username)`과 완전히 같은 로직이되 `mid = upper(p_mid)`로 상대를 찾는다.

계정 센터에 "친구 초대 링크"(`{origin}/user/{MID}?invite=friend`) 복사 버튼을 추가했다. 이 URL을 처음 여는 사람 입장에서 두 가지가 한 번에 일어난다 — ① 그 MID의 프로필 페이지가 열리고, ② 로그인한 상태면 `friendRequestByMid`가 한 번 자동 호출된다(같은 mid로 재방문해도 새로고침마다 중복 전송되지 않도록 `autoInviteTriedRef`로 세션당 1회만 시도). 요청이 실제로 나가면(자동이든 카드 안의 "친구 요청" 버튼이든) 화면 위쪽에 "~님에게 친구 요청을 보냈습니다!"(이미 친구면 "~님과 친구가 되었어요!") 팝업이 2.6초간 떴다 사라진다 — 카드 안쪽 작은 문구(`inviteMsg`)만으로는 눈에 잘 안 띈다는 피드백을 반영했다.

프로필 보기 자체를 검색·채팅·`/play` 친구 목록마다 각자 오버레이 모달로 띄우던 것(`ChatUserProfileModal`, `UserSearchModal`의 내부 `sel` 서브뷰)을 걷어내고, `openchess.kr/user/<MID>` 고유 URL을 갖는 하나의 실제 페이지(`UserProfilePage`)로 통합했다 — `/review`·`/puzzle/(번호)-(라인)`과 같은 패턴으로 여는 곳에서 `pushState`, 뒤로가기 `popstate` 핸들러와 마운트 시 딥링크 리졸버 effect 양쪽에서 경로를 해석한다. `ChatPanel`(채팅창의 "프로필 보기"·멘션 탭)과 `PlayPage`("친구와 플레이하기" 목록 프로필 클릭)는 이제 App 루트가 내려주는 `onOpenUserProfile`(username → mid 조회 → 페이지 이동)을 호출한다. 다만 `FriendsModal` 자체의 친구 목록 프로필 서브뷰(수락/거절/삭제 같은 인라인 액션이 함께 있는 화면)는 그대로 남겨 뒀다 — 그 액션들을 페이지 이동 없이 바로 처리하는 게 더 나은 흐름이라고 판단했다.

`UserProfilePage`는 `useNarrow(880)` 기준으로 넓은 화면에서는 신원·MID·친구 요청 버튼을 왼쪽 고정 폭(300px) 열에, 통계·활동(`ProfileStatsPanel`)을 오른쪽 나머지 폭 열에 나란히 배치한다(사용자 요청 — "데스크톱은 비율이 남아도니까 한 화면에 더 많이 보이게"). 좁은 화면에서는 기존 모달과 같은 순서로 세로로 쌓인다.

**문서 — v0.4.4 증분 SQL 마이그레이션**
`MIGRATION_v0.4.4.sql`에 `pvp_invite_friend`·`gen_mid`/MID 형식 변경분에 이어, `friend_request_by_mid(text)` 신규 함수를 추가했다.

**UI/UX — 프로필 페이지·`/play` 리디자인 (frontend-design 스킬)**
사용자 요청 — "방금 만든 프로필 페이지와 play 페이지 쪽을 리디자인해줘, 매칭 대기 상태의 화면을 별도로, 친구 목록은 고정으로, framer-motion으로 더 생동감 있게". 기존 브라스/에보니 팔레트(`T`)와 `SITE_FONT` 한 벌을 그대로 쓰되(새 브랜드를 만드는 브리프가 아니라 기존 제품에 손을 더하는 브리프이므로), "지금 이 순간에도 뭔가 움직이고 있다"는 감각을 이 두 화면에만 집중해서 넣었다.

① `/play` 매칭 대기 — 예전엔 설정 카드 안에 작게 얹힌 레이더 링 하나였다. `MatchmakingWait`를 걷어내고 `MatchmakingScreen`을 새로 만들어, 매칭 중(`pvpWaiting`)이거나 친구 응답 대기 중(`myInvite`)이면 설정 카드 자체를 통째로 이 화면으로 바꿔치기한다(사용자 요청 "매칭 대기 상태의 화면을 별도로 만들어줘"). 무대 중앙엔 랜덤 매칭이면 보드 기물 스킨 그대로의 나이트(`PieceGlyph`, "상대를 찾아 나선다"는 모티프)를, 친구 초대 응답 대기면 그 친구의 아바타를 두고, 3개의 팽창하는 링 + 서로 다른 공전 주기(7.5/9.5/11.5초)로 도는 3개의 궤도 입자 + 상하로 유영하는 중앙 원을 framer-motion으로 계속 움직인다 — 궤도 입자 중 하나만 브릴리언트 색(`T.brilliant`, 체스 등급표에서 이미 "탁월한 수"에 쓰던 청록)을 써서 브라스 일색에 작은 리듬을 준다.

② 친구 로스터 상시 노출 — "친구와 플레이하기" 버튼을 누르면 펼쳐지던 드롭다운(`friendDropdownOpen`)을 없애고, 로그인만 돼 있으면 항상 펼쳐진 목록으로 바꿨다(사용자 요청 "친구 목록이 그냥 고정으로 보이게"). 목록 로딩 effect도 드롭다운을 여는 시점이 아니라 `myUid` 유무만 보고 곧장 실행하도록 바꿨다. 각 행은 처음 한 번만(행마다 고정된 `key`라 실시간 접속 갱신으로 재정렬돼도 다시 재생되지 않는다) 왼쪽에서 살짝 미끄러지며 스태거 등장하고, 온라인 상태면 초록 도트 옆에 브릴리언트색 펄스가 계속 번진다.

③ 프로필 페이지 — 신원 열은 왼쪽에서, 통계 열은 오른쪽에서(약간의 시차를 두고) 미끄러지며 들어와 "누구인지 먼저, 그다음 무엇을 했는지" 순서로 시선이 가게 했다. 지금 접속 중이면 아바타 테두리를 따라 브릴리언트색 링이 계속 은은하게 퍼진다(매칭 화면의 궤도 입자와 같은 색으로, "지금 여기 있다"는 신호를 사이트 전체에서 하나의 색으로 통일). OC/chess.com 통계 토글은 뚝 바뀌는 대신 `AnimatePresence`로 부드럽게 겹쳐 바뀌고, 친구 요청 팝업도 CSS keyframe 대신 framer-motion spring으로 들어오고 나간다.

`npm run build` 통과를 매 커밋마다 확인했다. 이 세션 환경에는 인증·Supabase 연동이 필요한 화면을 실제로 띄워 볼 수 있는 브라우저가 없어, 코드 리뷰와 빌드 통과로만 검증했다 — 배포 후 실제 화면에서 애니메이션 타이밍·간격을 한 번 확인해 보길 권한다.

**버그 수정 — 퍼즐 카드 PGN 배지 위치, 오래된 실시간 대국이 계속 잡히던 문제**
사용자 제보(스크린샷) — 퍼즐 카드 리디자인에서 PGN 배지를 오프닝 이름 옆(제목·별점과 같은 줄)에 붙였더니, 긴 오프닝 이름의 말줄임(`...`) 바로 옆에 떠 자리가 어색해 보였다. PGN은 애초에 국면·FEN과 같은 "이 퍼즐의 시작 위치 정보" 배지이므로, 다시 그 둘과 한 무리로 묶어 제목 아래 배지 줄에 표시하도록 되돌렸다.

`/play`의 재접속 로직(이전 버전에서 추가한, `/play` 진입 시 서버의 active `pvp_games` 행을 곧장 이어받는 effect)이 대국 종료 처리 없이 방치된(양쪽 다 떠난) 오래된 대국까지 무조건 이어받고 있었다 — 클럭이 진작 0을 지나 곧장 "패배" 화면으로 떨어지고, `/play`를 열 때마다(심지어 "대국 상대 찾기"를 누르기도 전에) 이 죽은 대국을 계속 다시 붙잡아 정작 새 매칭 화면에는 영영 들어갈 수 없게 만드는 원인이었다. `pvp_queue_join`이 이미 쓰고 있던 "2분 이상 갱신 없으면 좀비 대국"이라는 같은 기준을 클라이언트 재접속 로직에도 적용해, `updated_at`이 2분보다 오래됐으면 이어받는 대신 `pvp_finish(..., 'aborted')`로 정리한 뒤 평소처럼 설정 화면부터 새로 시작하게 했다.

**기능 — 대국 종료 팝업 (chess.com 벤치마킹)**
사용자 요청 — "대국이 끝났을 때 팝업 창을 띄워줘, chess.com을 벤치마킹하자". chess.com 종료 화면의 구조(체커보드 헤더 배너, 큼직한 결과 헤드라인 + 사유, 두 선수 VS 대면, 호스트 코멘터리, 리뷰 CTA, 재대결/닫기)를 참고하되 색은 그대로 베끼지 않고 OpenChess 자체 보드 색(`T.boardLight`/`T.boardDark`)으로 체커보드 배너를 그렸다. chess.com은 레이팅·랭킹 변동(+8, #10 등)과 Best/Good/Miss 집계를 보여주지만, OpenChess에는 PLAY 대국에 대한 그런 실시간 레이팅·수 등급 집계가 없다 — 없는 숫자를 지어내는 대신, 실제로 갖고 있는 정보(결과·사유·상대·타임 컨트롤)만으로 채우고, chess.com의 호스트 코멘터리 자리는 OpenChess의 코치 마스코트 말풍선(`MascotBubble`)이 결과·사유별 맞춤 코멘트로 대신한다. `result`가 null→값으로 바뀌는 순간에만 자동으로 열리고(문자열로 요약한 결과 키로 "이미 보여준 결과인지" 비교 — `result` 자체는 클럭이 매초 리렌더시켜도 항상 새 객체라 참조 비교로는 매초 다시 열려버린다), 사용자가 닫으면 재대결·다시 설정 전까지는 다시 뜨지 않는다.

**기능 — 집중 분석에서 퍼즐 만들기가 퍼즐 카드로 바로 들어가지 않고 마법사를 거치도록 변경**
사용자 요청 — "집중 분석 모드에서 퍼즐을 만들 때도, 바로 퍼즐 카드로 들어가지지 말고, 퍼즐 페이지의 퍼즐 마법사 화면으로 이동하도록 하고, 만일 같은 포지션에서 이미 누군가 퍼즐을 만든 게 존재한다면, ~님이 이미 이 퍼즐을 만들었어요! 로 표시하고, 이때는 '퍼즐 만들기'가 아니라 '퍼즐 풀기'라고 표시해줘". 예전엔 `useFocusAnalysis`가 실수/블런더/부정확한 수/탁월한 수를 감지하면 그 자리에서 곧장 엔진으로 전술 트리를 만들어 조용히 서버에 저장했다(`auto:true`) — 다 되면 뜨는 "퍼즐 풀기" 버튼은 그렇게 미리 만들어진 퍼즐을 곧장 여는 것이었다. 이 자동 생성·저장을 걷어내고, 대신 퍼즐 탭의 만들기 마법사가 그대로 이어받을 수 있는 시드(`{ pgn, targetPly, theme }`)만 계산해 두었다가, 버튼을 누르면 `onOpenPuzzleWizard`가 퍼즐 탭으로 이동시키고 그 시드를 그대로 마법사에 흘려보낸다. 마법사는 시드의 PGN을 1단계 입력창에 채워 자동으로 파싱·채점한 뒤, 지정된 수를 골라 2단계까지 자동 진행한다 — 이 시점에 부딪히는 중복 검사(`checkPcDuplicate`)는 퍼즐 탭에서 직접 만들 때와 완전히 같은 코드 경로라, 이미 같은 위치에 누군가 만들어 둔 퍼즐이 있으면 "@아무개님이 이미 이 퍼즐을 만들었어요!" 배너가 뜨고 3·4단계 대신 곧장 "퍼즐 풀기" 버튼만 나타난다(눌러 그 퍼즐을 바로 연다). 없으면 평소와 똑같이 미리보기·공개 설정을 거쳐 "퍼즐 만들기"로 이어진다. 두 진입 경로(집중 분석에서 진입/퍼즐 탭에서 직접 만들기)가 이제 완전히 같은 화면·같은 코드를 탄다.

### OpenChess v0.4.3 — 2026/8/31

**기능 — Apple/Facebook 로그인 + 계정 센터(연결/해제·로그아웃·계정 탈퇴)**
`authGoogleStart`를 `authOAuthStart(provider)`로 일반화하고 로그인 창에 Apple/Facebook 버튼을 각 브랜드 가이드라인 색상으로 추가했다. 이메일 중복·로그인 실패 시 "이미 다른 방식으로 가입됨" 힌트도 Google 전용에서 세 제공자 공통(`hintProvider`)으로 일반화했다. 계정 하나에 대응하는 고유 UID는 이미 `profiles.id`(=`auth.users.id`)가 맡고 있어, 여러 로그인 수단을 같은 계정에 연결하는 쪽은 새 SQL 없이 Supabase Auth의 identity linking(GoTrue `/user/identities/authorize`·`DELETE /user/identities/{id}`)을 그대로 사용했다(`getUserIdentities`/`linkIdentityRedirect`/`unlinkIdentity`). 헤더 프로필 메뉴·설정 탭 양쪽에서 진입 가능한 계정 센터(`AccountCenterModal`)를 새로 만들어 로그인 수단(이메일/Google/Apple/Facebook) 연결·해제, 로그아웃, 계정 탈퇴(아이디 입력 확인 후 영구 삭제)를 한 곳에서 처리하게 했다 — 다른 오버레이와 같은 `screens`/`pushScreen` 패턴이라 뒤로가기와 자연스럽게 맞물린다. 계정 탈퇴는 새 SQL 함수 `delete_own_account()`(SECURITY DEFINER, `auth.users`에서 본인 행 삭제)로 처리하며, 기존 스키마가 사용자 데이터 테이블 대부분을 `auth.users`에 `on delete cascade`로 걸어 둬서 관련 데이터가 함께 정리된다. Apple/Facebook 발급부터 Supabase Providers 설정, Manual/Automatic Linking 옵션, 콜백 URL까지 단계별로 정리한 `SETUP_OAUTH.md`도 추가했다.

계정 센터에는 계정마다 고유한 9자리 영문 대문자+숫자 회원 번호(MID)와 복사 버튼도 함께 추가했다 — `profiles.mid` 컬럼(고유, 서버가 `gen_mid()`로 자동 생성)을 신설해 신규 가입은 컬럼 default로 자동 발급되고, 기존 계정은 1회 backfill했다.

**기능 — `/play` 설정 화면을 스케치대로 재설계 + 매칭·초대 흐름 다듬기**
사용자가 제공한 스케치대로 "봇/실시간 대국"·"랜덤 매칭/친구와 플레이" 두 탭 전환을 없애고, 타임 컨트롤을 불렛·블리츠·래피드·스탠다드 4개 카테고리 × 3개(불렛: 1분/2분/2분+1초, 블리츠: 3분/5분/5분+2초, 래피드: 10분/15분/15분+10초, 스탠다드: 30분/1시간/1시간+30초) 그리드에서 고른 뒤 [선택된 타임 컨트롤로 대국 상대 찾기]·[봇과 플레이하기]·[친구와 플레이하기] 세 액션 중 하나를 바로 누르는 한 화면 구성으로 바꿨다. "봇과 플레이하기"만 진영·봇 등급을 더 골라야 해서 그때만 뒤로가기가 있는 하위 화면(`setupPhase "bot"`)으로 넘어간다.

매칭 대기 화면은 레이더 펄스 애니메이션 + 경과 시간을 보여주는 전용 컴포넌트(`MatchmakingWait`)로 바꿔 랜덤 매칭 대기·친구 도전 응답 대기 양쪽에서 공용으로 쓴다. `/play` 페이지를 벗어나면(언마운트) `leaveCleanupRef`로 언마운트 시점의 최신 상태를 읽어, 대기열에 있었으면 매칭 취소로 대기열에서 빼고 결과 없이 진행 중이던 실시간 대국이 있었으면 그 자리에서 기권으로 서버에 보고하도록 했다. "친구와 플레이하기"는 뜨는 드롭다운 대신 카드 자체를 아래로 넓히는 인라인 목록으로 바꿔, 각 행의 프로필(사진·이름)을 누르면 그 친구 프로필로 이동하고(뒤로가기 시 `/play`로 복귀) 별도 "도전" 버튼으로 대국 신청을 분리했다. 친구 대국 신청(`pvp_invites`) 전역 알람 박스(`GlobalPvpInviteBanner`)를 App 루트에 추가해 사이트 어느 화면에 있어도 상단에 뜨고, 수락·거절하거나 상대가 취소하기 전까지 사라지지 않는다. 같은 신청은 채팅 메시지로도 동시에 전송해(`chat_messages.pvp_invite_id`, `pvp_invite_friend` RPC) 같은 디자인의 카드(`PvpInviteChatCard`)로 대화방에도 남고, 채팅에서 `/play 3`·`/play 15+10`처럼 시간을 지정해 바로 신청할 수도 있게 했다(프리셋에 없는 분도 `timeControlFromKey` 헬퍼로 `PlayPage`가 올바른 라벨·클럭으로 이어받는다).

**기능 — `/faq` 페이지 신설**
`/about`과 같은 패턴(App의 무거운 초기화를 거치지 않는 별도 정적 페이지)으로 `/faq`를 새로 만들었다(`src/FaqPage.jsx`, `main.jsx` 경로 분기 추가). `/about`의 Reveal(스크롤 진입 애니메이션)·KineticWord/KineticTagline·SectionDivider·색 팔레트를 그대로 재사용해 같은 톤을 유지했고, 카테고리별 아코디언 FAQ와 하단 이메일 문의 링크(설정 탭과 같은 `openchesskr@gmail.com` mailto)를 담았다. 설정 탭의 "문의 / FAQ" 카드에 "FAQ 보기"(`/faq`로 이동) 버튼을 추가하고, 기존 "문의하기"(이메일)는 그대로 뒀다. 채팅 명령어 미리보기도 "/"만 입력해도 항상 뜨던 것을 정확히 "/help"를 입력했을 때만 뜨도록 바꾸고 `CHAT_COMMANDS` 목록에 `/play`·`/help` 항목을 추가했다.

**UX — 유휴 자동 로그아웃 제거**
사용자 요청으로 30분 유휴 자동 로그아웃을 없앴다 — 같은 기기에서는 더 이상 자동으로 로그아웃되지 않는다. 대신 액세스 토큰이 오래 열어 둔 탭에서 조용히 만료되는 일(사실상의 숨은 로그아웃)이 없도록, 50분마다 refresh_token으로 미리 갱신하는 타이머(`refreshAccessToken`)로 대체했다.

**UI — 퍼즐 카드 높이 고정**
"n명이 풀었습니다" 줄과 PGN/국면/FEN/레이팅 배지 줄이 있고 없고에 따라 카드 높이가 들쭉날쭉하던 문제를, 두 영역 모두 내용이 없어도 안전 높이를 항상 예약(`minHeight`)하도록 고쳐 같은 행의 카드 높이가 항상 고정되게 했다.

**버그 수정 — 실시간 대국 매칭, 이미지 스캔, 퍼즐 생성**
pvp 대국 진입 시(대기열 매칭·친구 초대 수락·재접속) `mode`가 `"pvp"`로 바뀌지 않던 버그를 고쳤다 — 친구 초대를 수락하는 쪽은 모드 토글을 직접 누른 적이 없어 기본값 `"bot"`에 머물러 실제 상대 대신 로컬 봇과 대국하고 내 수도 서버로 전송되지 않았다. `applyPvpGame`에서 한 번에 처리해 모든 진입 경로에서 재발하지 않게 했다. `pvp_queue_join`이 오래 방치된(2분 이상 업데이트 없는) `"active"` 대국을 무조건 재접속으로 보고 즉시 돌려주던 것도, 중단 처리 후 정상 매칭시키도록 고쳤다 — 좀비 대국 때문에 랜덤 매칭이 대기 화면 없이 곧장 응답 없는 상대에 물리던 문제의 근본 원인이었다. `ImageSourceMenu`(이미지 스캔 소스 선택 메뉴)의 z-index가 학습 탭 보드 편집기 모달보다 낮아 메뉴가 모달 뒤에 가려 클릭해도 반응이 없어 보이던 버그를, 앱에서 가장 높은 기존 z-index보다 위로 올려 고쳤다. "내 파일에서 선택"의 accept에 `image/*` MIME이 여전히 남아 있어(v0.4.2에서 확장자를 추가했음에도) Android가 계속 이미지 전용 인텐트(주로 Google Photo)로 열던 문제도, `image/*`를 완전히 빼 진짜 파일 탐색기가 열리도록 마저 고쳤다.

퍼즐 만들기 마법사의 "기물 희생하기" 테마는 저장 시 트리 자체의 첫 수가 이미 `mistakeSan`(선택한 수)이라, 검증(`isPuzzleSequenceValid`)이 그 수를 두 번 두려다 매번 불법 수로 걸려 저장이 조용히 거부되고 있었다(화면엔 `setActive`로 곧장 보여줘 성공한 것처럼 보였을 뿐) — 희생 테마 + 트리 형식일 때만 `setup`에서 `mistakeSan`을 빼도록 고쳤다. 이어서 "선택한 수의 직전 수를 컴퓨터의 응수로, 선택한 수 자체를 사용자가 찾을 첫 수로" 만들어 달라는 요청에 맞춰, `submitPuzzleCreate`가 저장 시 `mistakeSan`을 선택한 수 대신 그 직전 수로 바꾸고 `setupSans`를 그만큼 줄이도록 데이터 형식을 바꿨다 — `mistakeSan`이 다른 테마와 같은 뜻("이미 두어진, 방금 응수된 수")이 되어 퍼즐 솔버·검증 등 `setup=[...setupSans, mistakeSan]`을 쓰는 모든 곳이 예외 없이 정확히 동작한다(솔버 인트로가 직전 수를 컴퓨터 응수로 자동 재생하고, 트리의 첫 수 = 선택한 수 자체를 사용자가 찾는다). `isPuzzleSequenceValid`는 새 형식으로 먼저 검증하고 실패하면 구버전 형식으로 한 번 더 시도해 하위 호환을 유지한다. 그 밖에 앱 로드 시 한 번만 채워지던 `creatorUsernames`(퍼즐 탭 "내가 만든 퍼즐" 필터용 생성자 맵)를 `onSavePuzzle`이 새 퍼즐 저장 시 로컬에도 낙관적으로 반영하도록 해, 방금 만든 퍼즐이 그 세션 안에서 바로 필터에 잡히지 않던 문제도 고쳤다. 지난 커밋에서 `incomingInvite` state를 전역으로 옮기며 지웠는데 호출부(`applyPvpGame`, `rematch`) 2곳에 `setIncomingInvite(null)` 호출이 남아 있어, 실시간 대국 적용·재대국 시마다 `ReferenceError`로 죽던 문제도 함께 고쳤다.

**문서 — v0.4.3 증분 SQL 마이그레이션**
이미 v0.4.2까지 적용된 프로젝트에 이번 버전에서 바뀐 SQL만 다시 실행할 수 있도록 `supabase-setup.sql`에서 관련 부분(`chat_messages.pvp_invite_id`, 좀비 대국 정리, `pvp_invite_friend` 채팅 알림, `delete_own_account`, `gen_mid`/`profiles.mid`)만 뽑아 `MIGRATION_v0.4.3.sql`로 정리했다.

`npm run build` 통과를 매 커밋마다 확인했다.

### OpenChess v0.4.2 — 2026/8/30

**보안 — pvp_finish 자기 승리 선언 차단**
자동 보안 리뷰 지적(커밋 리뷰)을 반영했다. `pvp_finish(p_game_id, p_status)`는 참가자면 백/흑 어느 쪽 결과든 자유롭게 넣을 수 있었다 — 예를 들어 지고 있는 쪽이 자기가 백이라는 이유만으로 `pvp_finish(id, 'white_won')`을 그대로 호출해 실제로 이기지 않고도 서버에 "내가 이겼다"고 우겨 넣을 수 있었다(권한 우회). 체크메이트·스테일메이트·3회 동형 반복은 양쪽 클라이언트가 같은 `sans` 배열로 완전히 동일하게 계산하므로 협상 대상이 아니다 — 그래서 "자기 자신을 승자로 선언"하는 경우만 막았다: 호출자가 자신을 패자(또는 무승부·중단)로 보고하는 것만 허용하고, 승자 쪽 클라이언트가 뒤이어 같은 이벤트에 반응해 중복 호출해도 조용히 거부되게 했다(패자 쪽 호출 하나만 성공하면 결과 반영에는 충분하고, 클라이언트는 이미 그 실패를 무시하도록 되어 있었다). 무승부는 "제안"이 아니라 실제 수순에서 객관적으로 계산되는 상태라 양쪽 모두 계속 허용한다.

**기능 — `/play` 실시간 대국(PvP): 랜덤 매칭 + 친구 초대 + 타임 컨트롤**
`/play`가 봇 전용 페이지에서 OpenChess 사용자 간 실시간 대국까지 지원하도록 확장됐다. Supabase에 `pvp_queue`(대기열)·`pvp_games`(대국 기록)·`pvp_invites`(친구 도전장) 세 테이블과 `pvp_queue_join`/`pvp_queue_leave`/`pvp_move`/`pvp_finish`/`pvp_invite_friend`/`pvp_invite_respond`/`pvp_invite_cancel` RPC를 SECURITY DEFINER로 추가했다 — 클라이언트는 이 테이블들에 직접 쓰기 권한이 없고 오직 RPC로만 드나든다. 대국 진행 중에는 `pvp_games`에 대한 Realtime(postgres_changes) 구독으로 상대의 수·기권·종료 상태를 즉시 받는다(내 수는 낙관적으로 먼저 반영한 뒤 서버에도 보낸다).

실시간 대국은 진영을 고르지 않는다(서버가 항상 무작위 배정) — 대신 "랜덤 매칭"과 "친구와 플레이"로 나뉜다. 랜덤 매칭은 같은 타임 컨트롤을 고른 사람끼리만 대기열에서 짝짓도록 `pvp_queue`·`pvp_queue_join`에 `time_control`(초기시간-증가시간 형식 문자열) 인자를 추가했다. 친구와 플레이는 친구 목록 드롭다운(정렬: 최근 접속순, 동시 접속 중인 친구가 여럿이면 퍼즐 티어→XP 순)에서 상대를 골라 도전장을 보내고, 받은 쪽은 `/play` 화면에 뜬 배너에서 수락·거절할 수 있다 — 수락하면 `pvp_invite_respond`가 대국을 만들어 초대에 연결한다.

타임 컨트롤(불릿/블리츠/래피드/클래식/무제한)은 설정 화면 맨 위에서 봇 대국·실시간 대국 공통으로 고른다. 클럭은 서버가 시간을 재는 것이 아니라 각 클라이언트가 로컬로 계산한다(권위 있는 서버 클럭이 아니므로 밀리초 단위로 정확히 일치하진 않지만 실사용에는 충분하다고 판단) — 0에 닿으면 시간 초과 패로 처리하고, pvp 모드에서는 체크메이트와 같은 가드(1회만 보고)로 서버에도 알린다.

**기능 — 퍼즐 공개/비공개 설정**
퍼즐 만들기 마법사에 4단계(공개 설정, 기본값 공개)를 추가하고, 퍼즐 풀이 화면 2페이지의 생성자 권한 박스에서도 나중에 바꿀 수 있게 했다. `puzzles.is_public` 컬럼은 `data`(jsonb, 누구나 update 가능)가 아니라 별도 보호 컬럼으로 분리했다 — `data`에 넣으면 기존 "puzzles update(data)" grant가 열려 있어 아무나 남의 퍼즐을 비공개로 바꿀 수 있게 되기 때문이다. 이 컬럼은 REST 직접 update grant를 주지 않고 `puzzle_set_visibility` RPC(생성자 본인 또는 개발자/공동개발자만, 1시간 편집 주기와 무관하게 언제든 가능)로만 바뀐다. 읽기 정책도 비공개 퍼즐은 소유자·편집자에게만 보이도록 다시 작성했다.

**기능 — 오프닝/미들게임/엔드게임 포지션 단계 분류**
퍼즐의 시작 포지션을 기물 점수(폰1·나이트3·비숍3·룩5·퀸9 합) + 남은 기물 수 + 폰들이 시작 랭크에서 얼마나 전진했는지, 세 지표를 각각 0~1로 정규화해 평균 낸 점수로 오프닝/미들게임/엔드게임 3단계로 분류한다(`gamePhaseOf`). PGN/FEN 배지와 완전히 같은 크기·색의 배지로 퍼즐 카드·퍼즐 풀이 카드에 표시하고, 정렬·필터 드롭다운에도 다중 선택 가능한 구분을 추가해 실제 피드 필터링에 반영했다.

**기능 — 실시간 접속 상태(Discord 스타일 온라인 표시) + chess.com 최근 접속**
Supabase에 `presence` 테이블(`uid`, `last_seen`)과 `touch_presence()` RPC를 추가했다. 로그인 중에는 앱 최상단에서 25초 간격으로(백그라운드 탭에서는 멈춤) 자신의 `last_seen`을 갱신하고, `last_seen`이 90초 이내면 "온라인"으로 판단한다. 친구 목록·프로필 카드에 초록 점(`OnlineDot`)과 "n분 전 접속" 텍스트(`presenceLabel`)를 표시하며, chess.com 프로필 헤더에는 chess.com API가 제공하는 `last_online`을 이용해 "chess.com n분 전 접속"도 함께 보여준다. 처음엔 `usePresenceMap`이 20초 폴링만 썼는데, `presence` 테이블을 supabase_realtime publication에 등록하지 않아 실제로는 폴링에만 의존하고 있었다 — 이후 이 테이블을 publication에 등록하고 `usePresenceMap`이 필터 없이(관심 uid는 클라이언트에서 걸러냄) postgres_changes를 구독하도록 고쳐, 친구가 `/play`가 아니라 어떤 화면에 있어도 접속 여부가 즉시 반영되게 했다.

**기능 — chess.com 프로필 통합 표시 + `/about` 히어로 타이포그래피**
프로필 카드에서 chess.com 통계 보기를 고르면 카드 상단의 사진·아이디·소개를 OpenChess 것 대신 chess.com 것(아바타·아이디·실명)으로 같은 위치·크기에 보여준다. 이어서 `AccountChessStats`가 통계 본문 위에 따로 그리던 작은 사진+아이디 줄이 이 헤더와 중복된다는 지적을 받아, 헤더가 이미 있는 화면(내 프로필/유저 검색/친구 프로필)에서는 `compact` 옵션으로 그 줄을 생략하고, 국적 배지·시간 규정별 레이팅을 헤더 쪽(`ChesscomHeaderIdentity`, 세 화면이 공유)으로 옮겼다. `/about` 첫 페이지의 배너와 히어로 카드 사이 비어 보이던 공간에는, 스크롤로 지나갈 때마다 단어별로 다른 방향·크기로 튀어오르는 타이포그래피 태그라인을 추가했다(Pager가 내부 스크롤 컨테이너를 쓰는 레이아웃이라 window 기준 `useScroll` 대신, 이미 검증된 `whileInView` 패턴을 재사용).

**UI — 소셜 버튼 정렬, 모바일 안전 영역, 학습 탭 PLAY 버튼**
퍼즐 카드·퍼즐 풀이 카드 모두 좋아요 → 리포스트 → 공유 순서로 통일하고, 이 아이콘 줄을 카드 폭 전체를 차지하는 새 줄로 분리해(`flex-wrap` 컨테이너에서 `width:100%`로 강제 줄바꿈) 왼쪽 정렬했다(기존엔 `marginLeft:auto`로 오른쪽에 몰려 있었다). v0.4.1에서 새로 추가된 `MoveLongPressPreview`(수 미리보기)와 퍼즐 탭 정렬·필터 드롭다운이 `position:fixed`만 쓰고 있어, transform이 걸린 조상(탭 전환 애니메이션 등) 안에서는 뷰포트가 아니라 그 조상 기준으로 계산되어 모바일 화면 가장자리에서 잘려 보이던 문제를 `document.body` 포털로 옮겨 해결했다(`ClickInfoBadge`·`ImageSourceMenu`와 동일한 패턴으로 통일). 학습 탭 PLAY 버튼의 그림자 색을 같은 줄의 다른 버튼(뒤집기·초기화 등)과 통일했다(`#8A6A2F` → `#000`).

**버그 수정 — 이미지 스캔, 파일 선택, 프로필 칭호, 퍼즐 마법사**
`api/scan-board.js`의 `GEMINI_MODEL`이 실재하지 않는 `gemini-3.6-flash`로 설정돼 있어 이미지 스캔이 항상 404로 실패하고 있었다 — 실제로 존재하는 안정 버전 `gemini-2.5-flash`로 되돌렸다. `ImageSourceMenu`의 "내 파일에서 선택"이 갤러리·Google Photo와 똑같이 `accept="image/*"`만 써서, 이 값만 보고 특수 사진 선택기를 곧장 띄우는 브라우저에서는 갤러리·Google Photo를 눌렀을 때와 구분되지 않고 늘 같은 화면(주로 Google Photo)으로 연결되던 문제를, accept에 확장자 목록을 함께 넣어(이미지 전용 값에서 벗어나면 특수 선택기 대신 진짜 파일 탐색기가 열림) 해결했다. 프로필 카드(내 프로필/다른 사용자/헤더 메뉴/친구·검색 목록)에서 칭호 배지 표시를 모두 없앴다(칭호 획득 팝업은 유지). 퍼즐 마법사 3단계 미리보기에서 기물 희생하기 테마가 선택한 수까지 이미 둔 위치를 시작 포지션으로 보여주던 것을, 그 직전 위치로 바로잡았다(이 테마는 선택한 수 자체를 풀이자가 스스로 찾아야 하므로). `genPuzzleTree` 호출에 `onProgress` 콜백을 연결해, 전술 라인 생성 중에도 채점 단계와 동일하게 박스 우상단에 실시간 진행률(%)을 표시하도록 했다.

`npm run build` 통과를 매 커밋마다 확인했다.

### OpenChess v0.4.1 — 2026/8/28

**버그 수정 — 버전 표시가 v0.3.9에 멈춰 있던 문제**
헤더·`/about`에 표시되는 버전(`APP_VERSION`)이 `CHANGELOG[0].version`에서 파생되는데, v0.4.0(PR #40)이 병합될 때 `CHANGELOG` 배열에 새 항목이 추가되지 않아 실제로는 v0.4.0이 배포된 뒤에도 화면엔 계속 v0.3.9로 표시되고 있었다. `src/App.jsx`의 `CHANGELOG`와 `src/AboutPage.jsx`의 `VERSION_HISTORY`에 누락돼 있던 v0.3.9·v0.4.0 두 버전의 기록을 실제 커밋 내역(PR #39·#40에 포함된 개별 커밋 메시지·diff)을 근거로 소급 작성해 채워 넣고, 이 README `## 버전 기록`에도 같은 내용으로 두 버전의 절을 추가했다 — 이제 세 곳 모두 v0.4.1을 최신으로 가리킨다.

**UI — `/about` 소개 페이지 히어로 리디자인 + 인터랙티브 퀵 내비게이션**
v0.2.2에서 "최신 내용·스크린샷 갱신, 버전 기록 사이 상호작용 요소 추가"로 미뤄 뒀던 항목을 단순 갱신이 아니라 리디자인으로 마무리했다. 히어로 섹션을 은은한 글래스 패널(브라스 톤 그러데이션 배경 + 얇은 테두리)로 감싸고, 제목 크기를 `clamp()`로 화면 폭에 반응하게 키우며 "퍼즐" 한 단어에 강조색을 줬다. 기존 "무료로 시작하기" 버튼 옆에 "기능 둘러보기" 보조 버튼을 추가했고, 그 아래 "학습·도감·퍼즐"/"티어"/"정확도 체계" 세 개의 퀵 내비게이션 칩을 새로 달아 각각 해당 섹션(`#features`/`#tiers`/`#accuracy`, 각 섹션에 `scrollMarginTop`을 줘 헤더에 가리지 않게 함)으로 부드럽게 스크롤 이동하도록 했다 — v0.2.2에서 요청했던 "상호작용 요소"에 해당한다. 아울러 v0.4.0에서 메인 퀘스트가 챕터 퀴즈 방식에서 로드맵형 "레슨" 시스템으로 바뀐 것을 반영해, "학습" 기능 소개 문구도 최신 내용으로 갱신했다. 로컬 dev 서버 + Playwright로 히어로가 의도한 레이아웃으로 렌더링되는 것과 "티어" 칩 클릭 시 실제로 티어 섹션까지 스크롤되는 것을 스크린샷으로 확인했다.

**기능 — FEN 리뷰/분석 등급·코치 설명 완전 동등화 (item 4)**
사용자 요청 — "분석/리뷰 기능에서의 FEN 모드 호환성 완료(PGN과 차이 X)". 그동안 FEN 자유 탐색·평가치 바·엔진 라인·무승부 판정은 표준 대국 리뷰와 동등했지만, 실제 수 등급(최선의 수·탁월한 수·유일한 수 등)과 코치 카드가 참조하는 MEC(수 설명) 로직만은 `recaptureFact`를 비롯한 20개 넘는 하위 "Fact" 함수가 전부 `boardFromSans`(항상 표준 시작 위치를 재생)를 내부적으로 다시 호출하고 있어, 이 부분만 FEN 모드에서 부정확하거나(ReviewPage의 자유 탐색 수는 등급 자체가 아예 비어 있었음, LearnTab의 마지막 수 배지도 FEN 모드에서는 통째로 꺼져 있었음) 미묘하게 어긋날 수 있었다(analyzeGame 경로는 try/catch로 죽지는 않되 "유일한 수" 강등 같은 부가 보정이 가끔 안 걸리는 정도).

`castleEpFacts`·`controlFacts`·`pieceMoveState`(피스 전개 이력 추적 — 시작 보드를 `fenRoot.board`로, 색상 판정을 `plyIsWhite`로 일반화)를 포함해 mecFacts가 의존하는 함수 전부에 `fenRoot`를 threading해, 내부의 모든 `boardFromSans`/`sansToFen` 호출을 이미 있던 fenRoot-aware 헬퍼(`boardOfRoot`/`fenOfRoot`)로 교체했다. `ownPriorMoveWasSacrifice`도 동일하게 고쳤다. 이어서 세 호출부를 실제로 fenRoot를 넘기도록 수정했다 — ① `ReviewPage`의 자유 탐색 채점 effect(더 이상 `fenRoot`일 때 `setExploreMove(null)`로 비워 버리지 않는다), ② `ReviewPage`의 코치 카드 `mecNotes`, ③ `LearnTab`의 "마지막 수 재평가" effect(`evalMoveKind`에도 fenRoot 파라미터를 추가) — 세 곳 모두 이제 표준 리뷰와 완전히 같은 코드 경로로 등급·코치 설명을 계산한다. `analyzeGame`(게임 리뷰 본편)의 `recaptureFact` 호출에도 fenRoot를 연결해, 부가 보정이 항상 정확히 걸리도록 했다.

이론 수 판정(`isBookMoveAt`)만은 fenRoot일 때 건너뛴다 — 표준 시작 위치를 전제하는 이론 DB 자체가 임의의 FEN 포지션에는 대응되는 데이터가 없으므로(오프닝 이름·마스터 대국 통계와 같은 성격), 값을 지어내는 대신 자연스럽게 비워 두는 쪽이 맞다는 판단이다. 퍼즐 풀이 화면의 등급 판정(`classifyMoveKindDetailed` 등)은 손대지 않았다 — 퍼즐은 아직 FEN 시작 위치를 지원하지 않아(item 5, 다음 작업) 이번 범위에 해당하지 않는다.

`npm run build` 통과를 확인했고, Playwright로 앱을 실제로 띄워 콘솔에 새 런타임 오류가 없는 것을 확인했다.

**문서 — 해결된 "남은 작업" 기록 정리**
과거 버전에서 다음 버전으로 미뤄 뒀다가 이후 실제로 해결된 항목들의 기록을 정리했다. v0.2.4의 "퍼즐 라인 길이 검증 보강"과 v0.2.3의 "정확도 계산 보정 체계(레이팅대별 회귀 보정)"는 이미 해결되었다는 확인을 받아 해당 남은 작업 메모를 지웠다. v0.2.9의 "이미지 3장을 활용한 리뷰 로딩 3분할 애니메이션 — 이미지 미수신으로 보류" 항목도, 바로 다음 커밋에서 실제 이미지가 반영되며 완전히 해결된 상태라 중복되는 보류 메모를 지웠다(해결 내용은 바로 다음 절에 그대로 남아 있음). v0.2.2의 "`/about` 최신화" 남은 작업은 위 히어로 리디자인으로 해결되어 함께 지웠다.

**기능 — 모든 퍼즐 FEN 호환성 + FEN 코드를 통한 사용자 임의 퍼즐 생성 (item 5)**
사용자 요청 — "현재의 모든 퍼즐의 시작 포지션을 FEN으로 구현, FEN 코드를 통한 사용자의 임의 퍼즐 생성 구현(PGN→FEN은 자동, FEN→PGN은 생성자 및 개발자의 수동 입력)". 퍼즐은 지금까지 `setupSans`(표준 시작 위치부터의 SAN 수순)만으로 시작 위치를 표현했다 — 이번에 모든 퍼즐이 `fen` 필드도 함께 갖도록 확장했다.

① **PGN→FEN 자동 변환**: `onSavePuzzle`(모든 퍼즐 저장의 단일 창구)이 `setupSans`가 있는 대국 기반 퍼즐엔 `sansToFen(setupSans)`로 계산한 FEN을 자동으로 채워 넣는다 — 별도 마이그레이션 스크립트 없이, 앞으로 저장되는 모든 퍼즐(과 재공유되는 기존 퍼즐)이 자연스럽게 FEN을 갖게 된다.

② **FEN 기반 퍼즐 생성**: 학습 탭 보드 편집기(`BoardEditorModal`)에 "이 포지션으로 퍼즐 만들기" 버튼을 추가했다 — 대국 기록 없이 지금 보드에 올려 둔 배치를 그대로 시작 위치(fenRoot)로 삼아 `genPuzzleTree`를 돌려 전술 트리를 만든다. `setupSans`는 비워 두고(=FEN→PGN, 이 포지션 이전의 실제 수순은 FEN 자체가 담지 못하는 정보라 자동 복원이 불가능하다), 나중에 생성자·개발자가 원하면 수동으로 채워 넣을 수 있게 열어 뒀다.

③ **퍼즐 생성·풀이 파이프라인 전체를 fenRoot 인식하게 확장**: `genPuzzleTree`·`puzzleCandidatesAt`·`evalPositionMulti`·`classifyMoveKindDetailed`·`posEvalToWhite`·`ownPriorMoveWasSacrifice`에 `fenRoot`를 threading해(item 4의 mecFacts 계열과 같은 패턴 — `boardFromSans`/`sansToFen` 대신 `boardOfRoot`/`fenOfRoot`) FEN 기반 퍼즐도 대국 기반 퍼즐과 똑같은 방식으로 전술 트리가 만들어지고 채점된다. `PuzzleSolver`(풀이 화면)도 `puzzle.setupSans`가 없고 `puzzle.fen`만 있으면 그 FEN을 루트로 재생하도록 고쳤다 — 보드·캐슬링 판정(`fenLegalDests`)·평가치·코치 카드(`mecFacts`)·수 체계 아이콘·레이팅 계산(`puzzleLineBaseRating`)까지 전부 fenRoot를 반영한다. `isPuzzleSequenceValid`(저장 시 서버 검증)도 `setup`이 비어 있어도 `fen`이 있으면(=FEN 기반 퍼즐) 통과시키도록 함께 고쳤다 — 이게 빠져 있었다면 FEN 기반 퍼즐은 저장 검증에서 항상 거부됐을 것이다.

오프닝 채택률(리체스 Explorer)처럼 표준 시작 위치 전제 데이터가 필요한 보조 기능은 fenRoot일 때 건너뛴다(item 4와 같은 원칙). 퍼즐 카드 미리보기·오프닝 필터·공유 딥링크처럼 `setupSans` 전체 목록에 의존하는 나머지 주변 기능들은 기존 "레거시 퍼즐(setupSans 없음)" 처리 관례를 그대로 따라 FEN 전용 퍼즐에서 자연스럽게 비워진다 — 필요하면 다음 라운드에서 이어서 다듬는다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 홈·퍼즐 탭에서 새 런타임 오류가 없는 것을 확인했다.

**기능 — 공개 퍼즐 레이팅 (item 3 사전 작업)**
사용자 요청 — 퍼즐 탭 개편(item 3)을 "체감 레이팅" 대신 실제로 오르내리는 공개 레이팅 기반으로 하자는 방향에 따라, 먼저 그 레이팅 시스템 자체를 만들었다. 표준 Elo(`puzzleEloUpdate`, K=24) — 라인을 끝까지 풀면 그 라인의 `puzzleLineBaseRating`을 상대로 승리(1)로, 틀린 수를 둘 때마다 그 즉시 패배(0)로 반영한다(한 시도 안에서 여러 번 틀리면 그만큼 여러 번 깎이고, 다음 패배의 기댓값 계산엔 방금 깎인 값이 그대로 쓰인다). 이미 푼 라인을 다시 풀거나 틀려도 매번 반영한다(XP처럼 "처음 한 번만" 보상하는 지표가 아니라 실력을 계속 추적하는 지표이므로 — 기존 추천 랭킹 이벤트와 같은 원칙).

`totalXp`와 완전히 같은 방식(로컬 캐시·계정 진행 상황·공개 프로필 세 곳)으로 영속화하고, `MyProfileCard`·`HeaderProfileMenu`가 공유하는 `PublicProfileStats`의 티어 배지 옆에 배지로 노출해(`publishProfile`이 이미 공개 프로필 블롭에 실어 보내므로, 친구·검색으로 보는 다른 사람의 프로필에도 자동으로 함께 뜬다) "공개 레이팅으로 프로필 카드에 노출"이라는 요청을 그대로 만족시켰다. 시작값은 `puzzleLineBaseRating`과 같은 100~3000 척도의 최하단인 800으로 뒀다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 새 런타임 오류가 없는 것을 확인했다. 퍼즐 탭 자체의 레이아웃 개편(추천/미해결/해결 완료 3분할 → 난이도·약점·테마 3요소를 이 레이팅으로 점수화하는 단일 피드)은 이 레이팅 값을 입력으로 쓰는 다음 작업으로 이어간다.

**기능 — 퍼즐 탭 개편: 단일 노출 점수 피드 (item 3)**
사용자 요청 — 기존 추천/미해결/해결 완료 3분할과 오프닝별 가로 스크롤 거치대가 복잡하고, "풀었는지 여부"가 목록을 가르는 주 기준인 게 이상하며, '추천 퍼즐' 알고리즘이 불투명하고, 세 구획이 서로 중복된다는 지적에 더해 "내가 관심 있고 풀고 있었던 퍼즐"과 "내가 직접 만든 퍼즐"을 직관적으로 찾을 수 있었으면 한다는 요청을 받아, 세 구획을 없애고 단일 정렬 피드로 통합했다.

새 기본 정렬("추천순")은 `puzzleExposureScore` — ① **난이도 적합도**(퍼즐 레이팅이 방금 만든 공개 퍼즐 레이팅에 가까울수록 높은 점수, 400점 차이에서 0으로 선형 감쇠), ② **약점 보완도**(테마별 정답률이 낮을수록, 그 테마 퍼즐일수록 높은 점수 — 아직 재 볼 데이터가 없는 테마는 중립 50점), ③ **테마 적합도**(테마 칩을 선택했을 때만 의미 있음, 선택 없으면 중립 50점)를 0.4·0.35·0.25로 가중합하고 이미 푼 퍼즐엔 8점의 약한 감점만 준다 — "해결 여부"가 더는 목록을 가르는 주 기준이 아니라 점수 안의 한 요소로만 반영된다. 예전 "🔥 추천 퍼즐" 섹션이 하던 발견 기능(주간 인기 퍼즐, 내가 리포스트한 퍼즐)은 별도 섹션 대신 이 피드의 후보 풀에 합류시켰다(일간/주간/월간 전환과 재셔플 UI는 더 필요 없어 없앴다, 주간 하나로 고정).

"내가 관심 있고 풀고 있었던 퍼즐"과 "내가 직접 만든 퍼즐"을 직관적으로 찾도록, 테마 칩과 별개로 빠른 접근 칩(전체/내가 만든 퍼즐/풀고 있는 중)과 "해결 완료 숨기기" 토글을 새로 뒀다 — "풀고 있는 중"은 라인을 하나 이상 풀었지만 아직 전체 완주는 아닌 퍼즐로 판정한다. 정렬 박스는 추천순(기본)·최신순·레이팅순 세 가지를 그대로 수동으로도 고를 수 있게 남겨 뒀다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 퍼즐 탭에서 새 필터·정렬 UI가 의도한 대로 렌더링되고 새 런타임 오류가 없는 것을 확인했다.

**버그 수정 — 퍼즐 탭에서 퍼즐을 누르면 먹통 되는 문제**
사용자 신고 — "퍼즐 누르면 웹사이트 먹통 되는 문제". 바로 앞 커밋(단일 노출 점수 피드 개편)에서 추가한 `themeRates`(`themeSolveRates`) `useMemo`가, 이 컴포넌트가 이미 여러 번 겪었던 것과 똑같은 React Rules of Hooks 위반 — "모든 훅은 조기 반환(`if (active) return <PuzzleSolver .../>`)보다 앞에서 호출돼야 한다" — 을 어기고 그 조기 반환 뒤(`matchesOpeningFilter` 근처)에 끼워 넣어져 있었다. 목록 화면(active=null)에서는 이 훅이 호출되다가, 퍼즐을 하나라도 열면(active가 생기는 순간) 그 즉시 호출이 통째로 건너뛰어져 이전 렌더보다 적은 수의 훅이 호출된다 — React가 "Rendered fewer hooks than expected"로 감지해 컴포넌트 트리 전체를 무너뜨렸다(이 앱엔 ErrorBoundary가 없어 새로고침 전까지 화면이 하얗게 멈춘 채 굳는다). `themeRates` useMemo를 다른 훅들과 같이 조기 반환보다 앞(`puzzleRatingMap` 옆)으로 옮겨 고쳤다.

**UI — 퍼즐 레이팅 노출 확대**
사용자 요청 — 프로필 카드의 퍼즐 레이팅 배지를 누르면 "퍼즐 레이팅 : n" 말풍선이 뜨도록, 그리고 모바일에서 화면 밖으로 잘리지 않게. `CircleBadge`(수 체계 아이콘 설명 말풍선)가 쓰던 안전 영역 배치 로직(`position:fixed` + 화면 가장자리 margin 클램프 + 화면 위/아래 절반에 따라 열리는 방향을 바꿔 항상 화면 중앙 쪽을 향함)을 범용 컴포넌트 `ClickInfoBadge`로 뽑아내, `PublicProfileStats`의 퍼즐 레이팅 배지와 `TierProgressStrip`(퍼즐 탭 상단 티어 스트립) 양쪽에 재사용했다. 퍼즐 탭에서는 이 레이팅을 요청대로 "티어를 표시하는 도형"(`TierLogoDisc`) 우측에도 새로 노출했다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워(모바일 폭 뷰포트 포함) 퍼즐 레이팅 배지·말풍선·티어 스트립이 의도한 대로 렌더링되는 것을 스크린샷으로 확인했다.

**버그 수정(롤백) — 퍼즐 카드 노출 점수 요소별 표시**
사용자 피드백 — 바로 앞 커밋에서 추가한 카드 하단의 노출 점수 배지 줄(난이도·약점·테마·합계)이 카드 비율을 이상하게 만든다는 신고를 받아, `PuzzleCard`의 해당 UI와 `PuzzleTab`의 `exposureScoresFor`/`exposureScores` 전달 로직을 모두 롤백했다. 노출 점수 자체(추천순 정렬 알고리즘)는 그대로 유지된다 — 카드에 요소별 수치를 보여주는 표시만 없앴다.

**버그 수정 — FEN 기반 퍼즐 클릭 먹통 + 카드 미리보기 누락**
사용자 신고 — "FEN 퍼즐 누르면 웹사이트 먹통 되는 문제, FEN 퍼즐에는 체스보드 미리보기가 표시되지 않는 문제". 두 문제 모두 근본 원인은 하나 — item 5(FEN 기반 퍼즐)로 만들어진 퍼즐은 `mistakeSan`(직전 실수 수)이 아예 없는데(대국 기록 없이 지금 포지션 자체가 시작점이므로), 두 곳에서 여전히 그 값이 항상 있다고 가정하고 있었다.

① **클릭 먹통**: `PuzzleSolver`의 진입 애니메이션(`intro`)이 항상 `<AnimatedMove sans={puzzle.setupSans || []} san={puzzle.mistakeSan} .../>`를 그렸다 — `san`이 `undefined`면 `AnimatedMove` 내부의 `sanSrc(before, undefined, color)`가 합법 수를 못 찾고, 그 대체 경로(`boardFromSans([...sans, undefined])`)가 존재하지 않는 수를 보드에 적용하려다 예외를 던져 컴포넌트 트리 전체가 무너졌다(이 앱엔 ErrorBoundary가 없어 화면이 그대로 하얗게 멈춘 채 굳는다). `mistakeSan`이 없는 퍼즐은 재생할 "직전 수" 자체가 없으므로, intro 애니메이션을 건너뛰고 바로 (fenRoot를 인식하는) 정상 보드를 보여주도록 조건을 `intro && puzzle.mistakeSan`으로 고쳤다.

② **카드 미리보기 누락**: `PuzzleCard`의 `hasPreview = p.setupSans && p.mistakeSan`가 FEN 퍼즐에서는 항상 거짓이 되어 미리보기 영역 자체가 비어 있었다. FEN 퍼즐은 "직전 수"를 애니메이션할 수 없으므로, `mistakeSan`이 없고 `p.fen`이 있으면 그 FEN을 `parseFenFull`로 파싱한 정적 보드(`<Board .../>`, 애니메이션 없음)를 대신 보여주도록 했다 — 보드 방향(flip)도 setupSans 홀짝 대신 FEN에 실제로 담긴 차례(`turn`)로 판정한다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 새 런타임 오류가 없는 것을 확인했다.

**UI — 퍼즐 카드 비율·보드 크기 + 정렬 UI 정리**
사용자 요청 — 퍼즐 카드의 미리보기 체스보드가 카드 폭에 딱 맞는 정사각형이 되도록 카드를 세로로 늘리고 보드를 더 크게. `DailyPuzzleNoticeModal`이 쓰던 것과 같은 패턴(ResizeObserver로 실제 렌더 폭을 재서 그 값을 그대로 보드 size로 씀)을 `PuzzleCard`에도 적용해, 고정 190px 대신 카드 폭을 꽉 채우는 정사각형 보드로 바꿨다. 이 미리보기 자리 자체를 `aspectRatio: "1/1"`로 항상 예약해 둬(미리보기가 없는 레거시 퍼즐도 빈 정사각형을 차지) 카드 비율이 콘텐츠에 따라 흔들리지 않게 했고, 카드 하단 패딩도 늘렸다.

사용자 요청 — 오프닝/생성자 검색창과 정렬 3분할 세그먼트 박스가 나란히 있어 산만하다는 피드백에 따라, 검색창 폭을 줄이고 그 옆에 깔때기(Filter) 아이콘 버튼 하나만 뒀다 — 누르면 `CircleBadge`와 같은 안전 영역 클램프 패턴의 드롭다운으로 추천순/최신순/레이팅순을 고른다.

사용자 요청 — 퍼즐 탭 상단 퍼즐 레이팅 배지가 티어 도형에 바로 붙어 있던 것을, 가운데 정보 칼럼에 `flex:1`을 줘 배지가 스트립 우측 끝에 여백을 두고 자리하도록 했다.

**기능 — 퍼즐 만들기를 퍼즐 탭으로 이동 (item 3 후속)**
사용자 요청 — 퍼즐 만들기 기능을 분석 탭의 보드 편집기(연필 아이콘) 대신 퍼즐 탭 자체에서 할 수 있게. 분석 탭 `BoardEditorModal`에서 `onCreatePuzzle` prop과 관련 UI(버튼·진행 메시지·state)를 모두 걷어내 순수 포지션 편집 용도로만 남겼다.

퍼즐 탭의 "번호로 풀기" 검색 줄 바로 위에 "퍼즐 풀기/퍼즐 만들기" 선택 박스를 새로 뒀다. "퍼즐 만들기"를 고르면 기존 목록·검색·필터 UI 전체가 3단계 생성 마법사로 바뀐다 — ① **PGN/FEN 코드 입력**: `NotationTools`가 쓰던 것과 같은 검증 로직(`looksLikeFen`으로 FEN 여부 판정, 아니면 `parsePgnMoves`로 파싱 후 SAN 하나하나 합법성 검사)을 재사용해, FEN이면 그 자체를 시작 포지션으로, PGN이면 마지막 수를 "다루는 수"로 그 앞까지를 setupSans로 삼는다. ② **퍼즐 유형 선택**: 같은 PGN 한 수가 기물 희생·우위 점하기·실수 응징하기 여러 기준을 동시에 만족할 수 있어, 자동 판정 대신 세 유형 중 직접 고르게 했다 — 고르는 즉시 그 유형에 맞는 방식으로 `genPuzzleTree`를 돌린다(기존 자동 생성 규칙과 동일: "기물 희생하기"는 마지막 수 직전 위치에서 그 수를 `firstSan`으로 강제, 나머지 두 유형은 마지막 수가 이미 두어진 뒤 위치에서 트리 생성). ③ **퍼즐 라인 편집(생성자 권한 박스)**: 생성된 트리를 기존 `PuzzleSchematic` 컴포넌트로 미리보기하고, 만든 뒤에도 그 퍼즐을 열어 라인을 추가·삭제할 수 있다는 안내와 "다시 생성" 버튼을 뒀다. 세 단계가 모두 끝나야(포지션 확인 + 유형 선택 + 트리 생성 성공) 하단 "퍼즐 만들기" 버튼이 활성화되고, "취소" 버튼은 언제든 마법사를 초기화하고 풀기 모드로 되돌아간다. 저장과 동시에 방금 만든 퍼즐을 바로 열어(`setActive`) 결과를 확인할 수 있게 했다.

id·이름 규칙은 기존 자동 생성 경로(집중 분석 화면의 실수 응징하기/우위 점하기/기물 희생하기)와 완전히 동일하게 맞춰(`setupSans.join(" ")+"|"+mistakeSan`, `puzzleName`) 우연히 같은 포지션이면 같은 퍼즐로 수렴하도록 했다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워(모바일 폭 뷰포트) 퍼즐 카드 비율·정렬 드롭다운·퍼즐 만들기 마법사 1·2단계(PGN 입력→유형 선택 박스 등장)까지 실제 클릭으로 확인했다 — 이 샌드박스는 엔진(Stockfish) 워커 초기화가 막혀 있어 실제 트리 생성(3단계)까지는 재현하지 못했다.

**기능 — 퍼즐 만들기 1단계에 chess.com 대국 선택 추가 + 정렬 드롭다운에 필터 통합**
사용자 요청 — 퍼즐 만들기의 PGN/FEN 입력 단계에서도 유산 만들기와 똑같이 내 chess.com 대국을 골라 바로 채워 넣을 수 있게. `LegacyManageModal`이 쓰는 패턴(별도 창 없이 그 자리 아래에 `AccountChessStats`를 펼치고, 각 대국 줄에 `onSelectGame`으로 "선택" 버튼만 놓임)을 그대로 재사용했다 — App 최상단의 `chesscom`(`useChessCom(profile.chesscom)`)과 `profile.chesscom`(연동된 사용자명)을 `PuzzleTab`에 새 prop으로 내려보내고, 1단계 박스에 "chess.com 대국에서 선택" 토글 버튼을 추가했다(연동 안 돼 있으면 유산 만들기와 동일한 안내 문구와 함께 비활성화). 대국을 고르면 `sansToPgnText(g.moves, "w")`로 그 대국의 기보를 PGN 텍스트로 변환해 입력창에 채워 넣는다 — 그대로 "확인"을 누르면 마지막 수(대개 체크메이트·기권 직전 수)를 다루는 퍼즐이 되고, 원하는 수까지만 남기고 뒷부분을 지우면 그 수를 다루는 퍼즐로 만들 수 있다(직접 붙여넣은 PGN과 완전히 같은 방식으로 동작).

사용자 요청 — 정렬 드롭다운(깔때기 버튼)에 정렬 기준뿐 아니라 그 아래 별도 줄에 있던 빠른 접근 칩("내가 만든 퍼즐"/"풀고 있는 중")과 테마 칩("기물 희생하기"/"우위 점하기"/"실수 응징하기")도 함께 넣어 달라는 요청에 따라, 그 두 칩 행을 완전히 없애고 드롭다운 하나에 "정렬"·"빠른 필터"·"테마" 세 구획(각 구획 제목 + 선택된 항목에 체크 표시)과 "해결 완료 숨기기" 토글까지 전부 모았다 — 검색 줄 아래가 훨씬 정돈됐다. 테마 항목에는 기존처럼 개수도 함께 보여준다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 정렬 드롭다운에 네 구획이 모두 표시되는 것과, 퍼즐 만들기 1단계에 "chess.com 대국에서 선택" 버튼(비연동 상태의 비활성화 안내 포함)이 뜨는 것을 스크린샷으로 확인했다.

**UI — 정렬·필터 드롭다운을 개발자 편집 박스 스타일로**
사용자 요청 — 통합된 정렬 드롭다운을 개발자의 "이름·키워드 편집" 박스(집중 분석 화면, `KW_PAIRS`/`KW_SINGLES` 토글 칩)와 같은 레이아웃으로. 밝은 양피지색 리스트(세로로 나열된 행 + 체크마크) 대신, 어두운 브라스 톤 카드(`linear-gradient(180deg,#3A2516,#241509)` 배경 + 브라스 테두리) 안에 구획마다 작은 라벨을 두고 그 아래 `KW_SINGLES`와 같은 줄바꿈 칩 그룹(선택 시 반투명 브라스/그린 배경으로 채워지는 작은 알약 버튼)을 배치하도록 다시 그렸다. "해결 완료 숨기기"는 카드 하단에 "저장/취소" 버튼 행과 같은 자리에 폭 100%짜리 토글 버튼으로 뒀다. 드롭다운 폭을 220→240px로 넓히고, 화면 밖으로 잘리지 않도록 안전 영역 클램프 계산(`toggleSortMenu`)의 `menuW`도 함께 맞췄다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 새 레이아웃이 화면 안에 온전히 들어오는 것을 스크린샷으로 확인했다.

**보안/UX — 퍼즐 만들기 로그인 게이트 + PGN/FEN 필터·배지 + 레이팅 말풍선 + PGN 수 직접 선택**
사용자 요청 — 로그인하지 않은 상태에서는 퍼즐 만들기 기능 자체를 쓸 수 없게. `PuzzleTab`에 `myUid`가 없으면 "퍼즐 만들기" 토글 버튼을 비활성화하고 "로그인 후 이용할 수 있어요" 안내를 옆에 띄운다 — 만든 퍼즐은 제작자(myUid)를 기록해 공유되므로 로그인 없이는 애초에 의미가 없다. 만들기 화면을 보던 도중 로그아웃되는 경우까지 대비해 `useEffect`로 항상 풀기 모드로 되돌린다.

사용자 요청 — 정렬·필터 드롭다운에 전체/PGN/FEN 구분 추가. `sourceFilter` state를 새로 두고, 카드의 PGN/FEN 배지와 같은 기준(`setupSans` 유무 = PGN, `fen` 필드 유무 = FEN)으로 목록을 좁힌다.

사용자 요청 — PGN·FEN 정보가 있는 퍼즐은 카드의 레이팅 배지 왼쪽에 같은 크기로 PGN·FEN 배지를 표시(둘 다 있으면 둘 다). `onSavePuzzle`이 대국 기반 퍼즐에도 자동으로 `fen`을 채워 넣게 된 뒤로는(item 5) 대부분의 퍼즐이 두 배지를 함께 갖게 된다.

사용자 요청 — 퍼즐 카드의 레이팅 배지를 누르면 "이 퍼즐의 레이팅 : a", "내 레이팅 : b(b−a)", "난이도 : 매우 쉬움~매우 어려움" 세 줄을 보여주는 말풍선이 뜨도록(모바일 안전 영역은 `ClickInfoBadge`가 담당, 다중 줄 콘텐츠를 받을 수 있게 `content`/`align` prop을 새로 추가했다). 난이도는 새 헬퍼 `puzzleDifficultyTier(diff)`(diff = 퍼즐 레이팅 − 내 레이팅)로 5단계(−300 이하 매우 쉬움 → +300 이상 매우 어려움)를 매기고, 초록(쉬움)→브라스(적정)→빨강(어려움)으로 이어지는 신호등 색을 준다. b−a 델타도 부호에 따라 같은 색 계열로 강조한다.

사용자 요청 — 퍼즐 만들기에서 PGN의 마지막 수를 무조건 "다루는 수"로 쓰던 것을, 유산 만들기(`LegacyManageModal`)와 같은 방식으로 바꿨다. 2단계에서 유형(기물 희생하기=탁월한 수, 우위 점하기=부정확한 수, 실수 응징하기=실수·블런더)을 고르면 그 유형에 실제로 해당하는 chess.com 등급 아이콘을 유형 버튼에 함께 보여주고(실수 응징하기는 실수·블런더 두 아이콘을 살짝 겹쳐서), PGN이면 전체 기보를 `analyzeGame`으로 한 번 채점해(테마를 바꿔도 재채점하지 않고 캐시 재사용) 그 등급에 해당하는 수들을 유산 만들기의 후보 목록과 같은 UI로 나열한다 — 사용자가 그중 하나를 고르면 그 수를 기준으로 `setupSans`/`mistakeSan`이 정해지고 전술 트리를 생성한다. 표시 요소가 늘어난 만큼 2단계 박스 기본 높이(`minHeight`)도 넉넉히 키웠다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워(비로그인 상태 포함) 퍼즐 만들기 버튼 비활성화·정렬 드롭다운의 새 "시작 포지션" 구획이 의도한 대로 렌더링되는 것을 스크린샷으로 확인했다. PGN 수 목록·레이팅 말풍선은 이 샌드박스에서 로그인·엔진(Stockfish) 워커를 쓸 수 없어 코드 경로 검토로 검증했다.

**버그 수정 — PGN/FEN 배지·레이팅 말풍선이 퍼즐 카드에만 있던 문제**
사용자 신고 — 바로 앞 항목의 PGN/FEN 배지와 레이팅 말풍선이 목록의 `PuzzleCard`(퍼즐 블록)에만 적용돼 있고, 실제로 퍼즐을 푸는 화면인 `PuzzleSolver`("퍼즐 풀이 카드")에는 빠져 있었다. `PuzzleSolver`가 이미 갖고 있던 우측 상단 레이팅 배지(닫기 버튼 바로 아래) 왼쪽에 같은 PGN/FEN 배지를, 그 레이팅 배지 자체에는 `PuzzleCard`와 동일한 `ClickInfoBadge` 기반 말풍선(이 퍼즐의 레이팅·내 레이팅·난이도, 색 강조 포함)을 그대로 적용했다. 이를 위해 `PuzzleSolver`에 `myPuzzleRating` prop을 새로 추가하고, `PuzzleTab`의 `<PuzzleSolver>` 호출부에서 `puzzleRating || 800`을 넘긴다(같은 이름의 지역 변수 `myPuzzleRating`은 이 호출부보다 뒤에 선언돼 있어 참조할 수 없으므로, 계산식을 그 자리에 그대로 인라인했다). 배지가 늘어난 만큼 제목 영역이 겹치지 않도록 그 옆 헤더 행의 오른쪽 여백(`paddingRight`)도 38→130으로 넉넉히 늘렸다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 퍼즐 탭에 새 런타임 오류가 없는 것을 확인했다(이 샌드박스는 로그인·로컬 퍼즐 데이터가 없어 풀이 화면 자체를 직접 열어보지는 못했다).

**UI/기능 — 퍼즐 카드 다듬기 + 퍼즐 만들기 마법사 대폭 개편**
사용자 요청 — 자잘한 다듬기 네 가지. ① 퍼즐 레이팅 배지의 아이콘(lucide `Puzzle`)을 하단 탭 바 "퍼즐" 탭이 쓰는 `MaterialIcon name="extension"`으로 통일 — 더 이상 쓰이지 않는 `Puzzle` import도 정리했다. ② 퍼즐 카드의 PGN/FEN 배지가 브라스 톤 하나로 카드 전체(레이팅·테마 색 등)에 묻혀 잘 안 보인다는 피드백 — 이 카드에서 유일하게 쓰는 파란 계열(`T.only`, 수 체계 "유일한 수" 색)로 바꿔 눈에 띄게 했다. ③ 좋아요·리포스트·공유 버튼의 아이콘·글자 크기를 한 단계씩 키웠다.

퍼즐 만들기 마법사도 크게 손봤다. **1단계** — "chess.com 대국에서 선택" 토글 버튼을 없애고, 연동돼 있으면 입력 박스 아래에 `AccountChessStats`를 항상 펼쳐 둔다(유산 만들기와 달리 토글 없이 기본 노출). "확인" 버튼은 박스 우하단으로 옮겼다.

**2단계** — 유형 버튼을 먼저 눌러야만 후보 수 목록이 뜨던 것을 없애고, PGN이 확인되는 즉시(`useEffect`) `analyzeGame`으로 전체 기보를 채점해 세 유형(기물 희생하기·우위 점하기·실수 응징하기)의 후보를 한꺼번에 각 유형 아래에 나열한다. 박스 우상단에 `analyzeGame`의 `onProgress`(0~1)를 그대로 받아 `%`로 보여주는 실시간 진행률을 추가했다.

**3단계** — 미리보기용 `PuzzleSchematic`이 일반 풀이 화면과 같은 "아직 두지 않은 갈래는 고스트로 가린다" 규칙을 그대로 물려받아 라인이 다 안 보이던 문제 — 생성자는 이미 자신이 만든 트리를 볼 권한이 있으므로, `PuzzleSchematic`에 `canEdit`과 분리된 `revealAll` prop을 새로 추가해(편집 버튼은 여전히 없이) 고스트 처리만 건너뛰고 전체 라인을 그대로 보여주게 했다.

**중복 검증** — 같은 PGN(수순+수)이나 FEN으로 이미 만들어진 퍼즐이 있으면 2단계(후보 수·유형을 고르는 순간)에서 막는다. 로컬(`puzzles`·`archivedPuzzles`)과 서버(`puzzleFetch`) 양쪽을 확인하는 `checkPcDuplicate(id)`를 추가했고, 겹치면 "이미 같은 퍼즐이 존재하여 퍼즐 만들기가 취소됩니다." 배너를 잠깐 띄운 뒤 `resetPuzzleCreate()`로 진행 상황을 초기화해 1단계로 되돌린다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 퍼즐 탭·레이팅 배지 아이콘 변경에 새 런타임 오류가 없는 것을 확인했다. 이 샌드박스는 로그인·엔진(Stockfish) 워커를 쓸 수 없어 퍼즐 만들기 마법사의 실제 동작(진행률·중복 검증 등)은 코드 경로 검토로 검증했다.

**UI — 퍼즐 탭 폼 요소 높이 통일 · 필터 드롭다운·말풍선 클리핑 수정 · PGN/FEN 배지 색 통일**
사용자 피드백 라운드. ① "번호로 풀기" 검색창·풀기 버튼·오프닝/생성자 검색창·필터(깔때기) 버튼 네 요소의 높이가 제각각이던 것을 `height:36, boxSizing:"border-box"`로 통일했다. ② 퍼즐 풀이 화면(`PuzzleSolver`)의 PGN/FEN 배지도 지난 라운드에 카드에서만 파란색(`T.only`)으로 바꾸고 빠뜨렸던 것을 마저 통일했다. ③ 정렬·필터 드롭다운 맨 위에 "전체·좋아요·리포스트" 구획을 추가하고, "빠른 필터"(내가 만든 퍼즐/풀고 있는 중)를 제외한 테마·시작 포지션·좋아요/리포스트 세 구획을 단일 선택에서 다중 선택(배열 토글)으로 바꿨다 — `puzzleThemeFitScore`도 배열을 받도록 확장. ④ 이번 버전에 새로 추가한 드롭다운·말풍선(`ClickInfoBadge`, `CircleBadge`, 정렬·필터 드롭다운)이 화면 가장자리에서 잘리거나 열어 둔 채 스크롤하면 엉뚱한 자리에 잔상처럼 남던 문제 — 열릴 때 위/아래 실제 남은 공간을 재서 더 넉넉한 쪽으로 열고(`top`/`bottom` 중 하나만 지정), 정렬·필터 드롭다운은 열리는 방향의 남은 공간에 맞춰 `maxHeight`도 함께 계산해 절대 화면 밖으로 넘치지 않게 했다. 세 컴포넌트 모두 열려 있는 동안 스크롤이 감지되면(중첩된 스크롤 컨테이너까지 capture 단계에서 감지) 즉시 닫히도록 `useEffect` + `scroll` 리스너를 추가했다.

`npm run build` 통과, Playwright로 필터 드롭다운을 실제로 열어 새 "전체·좋아요·리포스트" 구획과 정상적인 위치·크기로 렌더링되는 것을 스크린샷으로 확인했다.

**기능 — chess.com 대국 선택 토글 상태 표시 + 안내 문구 갱신**
사용자 요청 — 퍼즐 만들기 마법사 1단계에서 chess.com 대국을 선택하면 "선택됨"으로 표시하고 버튼 색을 바꾸며, 다시 누르면 선택이 풀리게 했다. `AccountChessStats`에 `selectedGameId` prop을 추가해 각 대국 줄의 "선택" 버튼이 현재 선택된 대국(`g.id` 또는 `g.endTime`으로 식별)이면 파란색 "선택됨"으로 바뀌게 했고, 같은 대국을 다시 누르면 `onSelectGame`이 그 사실(`gid`)을 함께 넘겨받아 입력을 비우고 선택을 해제한다. 입력 박스를 직접 타이핑해도 선택 상태는 함께 풀린다. 입력 박스 예시(placeholder) 문구도 "마지막 수가 퍼즐이 다루는 수가 돼요" 대신 "다음 단계에서 유형에 맞는 수를 골라요"로 갱신했다 — 2단계가 더는 PGN의 마지막 수만 쓰지 않고 유형에 맞는 후보 수 목록에서 직접 고르는 방식으로 바뀐 지 오래라, 예시 문구가 실제 동작과 어긋나 있었다.

**기능 — 유산 만들기 UI를 퍼즐 만들기 마법사와 통일 + 수 미리보기(꾹 누르기·마우스 오버 애니메이션)**
사용자 요청 — 유산(Legacy) 만들기 UI(`LegacyManageModal`)의 레이아웃·디자인을 이번 버전에 새로 만든 퍼즐 만들기 마법사와 통일하고, 두 마법사(퍼즐·유산) 모두 2단계에서 후보 수를 꾹 누르면(모바일) 또는 마우스를 올려두면(데스크톱) 미니 체스보드로 그 수를 애니메이션 재생하도록 했다.

① **레이아웃 통일**: `LegacyManageModal`의 창 배경을 단색 `T.paper`에서 다른 전체 화면 마법사들과 같은 어두운 라디얼 그라데이션으로 바꾸고, 각 단계(대국 선택/수 선택/재생 범위 설정)의 본문을 퍼즐 만들기 마법사와 같은 형태 — 밝은 `T.paper` 카드(테두리 `#DCCBA8`, `borderRadius:12`) 안에 "1. 대국 선택"처럼 번호가 붙은 제목 — 로 감쌌다. 헤더 텍스트 색도 밝은 바탕에 맞춰 조정했다.

② **수 미리보기**: 재사용 가능한 `MoveLongPressPreview` 컴포넌트를 새로 만들었다 — 자식(후보 수 버튼)을 감싸고, 데스크톱에서는 `onMouseEnter`/`onMouseLeave`로, 모바일에서는 400ms 롱프레스(`onTouchStart` 타이머 + `onTouchEnd`/`onTouchCancel`로 해제)로 안전 영역을 고려한 `position:fixed` 팝업을 띄운다. 팝업 안에는 그 수의 등급 아이콘(수 체계, `badgeIcon`)과 SAN 표기, 그리고 기존 `AnimatedMove`(반복 재생 간격 `loopMs`를 갖고 있어 마운트돼 있는 동안 스스로 계속 반복 재생함)를 그대로 재사용한 미니 보드를 넣었다 — 마우스를 올려 두거나 손가락을 떼지 않는 동안만 이 컴포넌트가 마운트돼 있으므로 "그동안 반복 재생"이 별도 로직 없이 자연히 만족된다. 퍼즐 만들기 마법사 2단계의 후보 수 버튼과 유산 만들기 2단계("수 선택")의 후보 수 버튼 양쪽 모두에 이 컴포넌트를 적용했다.

`npm run build` 통과를 확인했다.

**기능 — 퍼즐 생성자 본인의 퍼즐 삭제 (확인 다이얼로그 포함)**
사용자 요청 — 퍼즐 생성자에 한해, 퍼즐 풀이 화면의 2페이지(모식도 페이지)에서 자신이 만든 퍼즐을 삭제할 수 있게 하고, 삭제 전 확인 다이얼로그를 반드시 한 번 더 띄운다. `PuzzleSolver`에 `onDeletePuzzle` prop을 추가하고(부모 `PuzzleTab`이 삭제 후 곧바로 풀이 화면을 닫도록 연결), 모식도 페이지의 생성자 전용 구획(`isMyPuzzle`)에 "이 퍼즐 삭제" 버튼을 추가했다. 버튼을 누르면 곧바로 지우지 않고 대화·친구 삭제와 같은 패턴의 확인 다이얼로그(`confirmDeletePuzzle` 상태)를 띄운 뒤, "삭제"를 다시 눌러야 실제로 지워진다.

`npm run build` 통과를 확인했다.

**기능 — 이미지 스캔이 PGN/FEN 텍스트도 인식 + 이미지 소스 선택 메뉴**
사용자 요청 — 이미지 스캔이 체스판 실물 사진뿐 아니라 PGN 기보·FEN 코드가 텍스트로 적힌 사진(책 지면, 스크린샷, 메모)도 인식하게 하고, 모바일에서 이미지 선택 시 Google Photos 앱으로만 곧장 열리던 것 대신 카메라/갤러리/내 파일/Google Photo 중 먼저 고를 수 있게 했다.

① **서버(`api/scan-board.js`)**: Gemini에 이미지를 보내기 전에 먼저 "체스판 사진(board)"인지 "PGN/FEN 텍스트 사진(text)"인지 "둘 다 아님(none)"인지 스스로 판단하게(`kind` 필드) `responseSchema`와 프롬프트를 확장했다. board면 기존처럼 64칸을 하나씩 읽어 `ranksToFenBoard`로 조립하고, text면 이미지 속 텍스트를 한 글자도 지어내지 않고 그대로 옮겨 적게 해 `recognized_text`로 돌려준다. 응답에 `type: "board" | "text"` 판별 필드를 추가해 클라이언트가 분기할 수 있게 했다.

② **클라이언트**: 두 호출부(보드 편집기·퍼즐 만들기 마법사 1단계)가 함께 쓰는 공용 `scanImageFile(file)` 함수를 새로 만들었다. 보드 편집기는 `type:"text"`가 오면 FEN이면 그대로, PGN이면 `parsePgnMoves`로 수순을 끝까지 재생해 최종 포지션을 보드에 적용한다(편집기는 배치만 다루므로 수순 자체는 저장하지 않는다). 퍼즐 만들기 마법사는 인식된 텍스트(또는 board 스캔 시 기본 캐슬링·차례를 붙인 FEN)를 그대로 입력 박스에 채워 넣어, 기존처럼 "확인" 버튼으로 검증하는 흐름을 그대로 따른다.

③ **이미지 소스 선택 메뉴**: 재사용 컴포넌트 `ImageSourceMenu`를 새로 만들어, 스캔 버튼을 누르면 곧장 OS 파일 선택창을 띄우는 대신 "카메라로 촬영/갤러리에서 선택/내 파일에서 선택/Google Photo에서 선택" 네 옵션을 먼저 보여준다. 카메라는 `capture="environment"`로 확실히 분리되고, 나머지 세 옵션은 각각 독립된 `<input type=file>`로 연결했다(웹 표준상 OS 선택창이 실제로 무엇을 보여줄지는 웹 코드가 강제할 수 없어, 사용자가 원하는 경로를 먼저 명시적으로 고른 뒤 그 선택창이 뜨게 하는 것이 최선이다). 보드 편집기·퍼즐 만들기 마법사 두 곳의 스캔 버튼 모두 이 메뉴로 교체했다.

`npm run build` 통과를 확인했다. Gemini API 키·엔진 워커가 필요한 실제 인식 동작은 이 샌드박스에서 검증할 수 없어 코드 경로 검토로 확인했다.

**UI — 퍼즐 카드·퍼즐 풀이 카드 레이아웃 다듬기**
사용자 피드백 — 퍼즐 카드(그리드 블록)에서 제목 글자 크기를 조금 키우고(`PUZZLE_NAME_BASE_FS` 11→12), 퍼즐 유형·라인 개수를 알려주던 텍스트를 지웠다(손상된 퍼즐 경고는 유용한 오류 표시라 그대로 뒀다). 좋아요·리포스트·공유 아이콘과 수치 텍스트, 공유 버튼을 한 단계씩 더 키웠다.

퍼즐 풀이 카드(실제로 푸는 화면)에서는 제작자 표시의 "제작 · " 접두사를 없애 아이디만 보여주고, 그동안 닫기 버튼 아래 별도 줄(절대 위치)에 떠 있던 PGN/FEN 배지·퍼즐 레이팅 박스를 제작자 표시와 같은 줄로 내렸다 — 그 자리를 피해 잡아 뒀던 제목 영역의 넓은 오른쪽 여백(130px)이 필요 없어져 제목을 한 줄에 훨씬 길게 보여줄 수 있게 됐다.

**버그 수정 — 퍼즐 레이팅 말풍선 오배치·클리핑**
퍼즐 카드에서 퍼즐 레이팅 말풍선(내 레이팅 포함)이 잘못된 위치에 잠깐 나타났다가 제자리로 돌아오고, 퍼즐 풀이 카드에서는 그 말풍선이 화면 경계에 잘리는 문제 — 두 곳 모두 이 말풍선을 담당하는 `ClickInfoBadge`가 framer-motion으로 애니메이션되는 조상(퍼즐 카드를 감싸는 `FadeIn`의 `motion.div` 등, 애니메이션이 끝나도 `transform: translateY(0)`처럼 값이 남아 있어 CSS containing block을 만든다) 안에 있으면 `position:fixed`가 뷰포트가 아니라 그 조상 기준으로 계산돼 벌어지는 문제였다. `ClickInfoBadge`의 팝업을 `createPortal`로 `document.body`에 직접 띄워, 어떤 조상의 transform·overflow와도 완전히 무관하게 항상 실제 뷰포트 기준으로 정확히 그려지도록 고쳤다.

**기능 — 잘못된 PGN/FEN 입력 안내 통일**
퍼즐 만들기 마법사 1단계에서 PGN/FEN으로 인식되지 않는 입력(빈 값 제외)에 대해 사유별로 다르던 안내 문구(FEN 형식 오류·인식 불가·불법적인 수 등)를 "잘못된 기보 형식입니다."로 통일했다. `pcParsed`가 세워지지 않으면 2단계(`{pcParsed && (...)}`)로 넘어갈 수 없는 기존 구조를 그대로 활용해, 잘못된 입력으로는 2단계 진입이 계속 막힌다.

`npm run build` 통과를 확인했다.

**성능 — 이미지 스캔 인식 시간 단축**
사용자 요청. 휴대폰 카메라 사진은 흔히 3000px 이상·수 MB인데 체스판 배치나 PGN/FEN 텍스트 인식에는 그럴 필요가 없다 — 서버로 보내기 전에 클라이언트(`downscaleImageFile`, `createImageBitmap` + canvas)에서 먼저 최대 변 1280px·JPEG 품질 0.85로 축소·재압축해, 업로드 전송량과 비전 모델 처리 시간(이미지가 클수록 더 많은 타일/토큰으로 처리됨)을 함께 줄인다. 이미 충분히 작거나 축소에 실패하면(구형 브라우저 등) 원본을 그대로 쓴다.

**기능 — 기물 희생 화살표 정리: 금색 화살표 제거 + 퍼즐에도 빨간색 경고 화살표 적용**
사용자 요청. 집중 분석 모드에서 탁월한 수(희생)를 둘 때 방금 옮긴 기물이 "무엇을 공격하는지" 보여주던 금색(idea) 화살표를 없애고, "그 기물이 공격받는다"는 빨간색(danger) 화살표만 남겼다(`brilliantArrows`). 리뷰 기능에서만 보이던 이 빨간색 경고 화살표를 퍼즐 풀이 화면에도 그대로 적용해, 탁월한 수를 두면 방금 희생된(안전하게 잡힐 수 있는) 기물에 똑같이 빨간 화살표가 뜨도록 `PuzzleSolver`에 `hangingPieceArrows(board, oppColor)` 계산을 연결했다.

**버그 수정 — 리뷰의 희생 경고 화살표: 폰 방향 오류 + 수비자/공격자 비교 로직 추가**
사용자 제보 — 폰이 자신의 전진 방향과 반대쪽에서 잡을 수 있다고 표시되는 오류가 있었다. `hangingPieceArrows`의 폰 공격자 판정에서 방향 부호가 뒤집혀 있었던 것을 이 파일의 다른 올바른 폰 방향 계산(예: `seeSquare`가 쓰는 `lva`, `dir = color==="w" ? -1 : 1`)과 같은 공식으로 통일했다. 아울러 여러 기물이 동시에 희생됐을 때 각각에 화살표가 뜨는 것은 기존 구조로 이미 되고 있었지만, "수비자 수 < 공격자 수(다 막아도 결국 하나는 뚫림 → 진짜 위험)"면 공격하는 모든 기물에서 화살표를, 그렇지 않으면(수비자가 충분함) 희생한 기물보다 값이 낮은 공격자에서만 화살표를 그리도록 판정 로직을 추가했다(`countLegalCapturesOnSquare`로 수비자 수를 센다).

**버그 수정 — 퍼즐 삭제 기능**
사용자 제보 — 퍼즐 삭제가 실제로 동작하지 않던 문제. ① 이전 `onDeletePuzzle`은 로컬 상태만 지우고 서버(crowdsourced `puzzles` 테이블)에는 아무 요청도 보내지 않아, 새로고침하거나 다른 유저가 열면 삭제된 퍼즐이 그대로 다시 나타났다 — `public.puzzles`는 이 파일의 다른 생성자 전용 변경들과 같은 이유로 `authenticated`에 delete grant 자체가 없어서(SECURITY DEFINER RPC로만 허용) 직접 DELETE를 시도했어도 항상 거부됐을 것이다. 새 RPC `puzzle_delete`(`supabase-setup.sql`)를 추가해 서버에서 호출자가 그 퍼즐의 생성자 본인이거나 개발자/공동개발자인지 다시 검사한 뒤 실제로 행을 지우도록 했고, 클라이언트 `onDeletePuzzle`을 그 결과를 기다려 성공했을 때만 로컬 상태를 지우는 비동기 함수로 바꿨다(실패하면 오류를 보여주고 창을 닫지 않는다). ② 퍼즐 탭 피드의 모든 카드에 소유자 확인·확인 다이얼로그 없이 "✕" 삭제 버튼이 그대로 노출돼 있어, 로그인한 아무 유저나 남의 퍼즐을 즉시 지울 수 있었다 — 이 버튼을 제거하고, 퍼즐 삭제는 퍼즐 풀이 화면 2페이지의 생성자 전용·확인 다이얼로그 경로로만 하도록 정리했다.

**기능 — 집중 분석 수 설명: SAN 링크에 수 번호 필수 + 대괄호 구문은 글자 수 제외**
사용자 요청. 수 설명(해설) 본문에서 SAN처럼 생긴 낱말이 앞에 수 번호(예: "12." · "12...") 없이도 링크로 잡혀 우연히 SAN처럼 보이는 낱말까지 오탐되던 문제 — `SAN_TOKEN_RE`에 lookbehind로 수 번호 접두사를 요구하도록 바꿨고, `[[...]]` 대괄호 구문도 첫 수 앞에 수 번호가 있어야만(`[[12.e5 Nf3 Nc6 Bb5]]`) 인식하도록 `renderMoveNoteBody`를 고쳤다. 아울러 대괄호 안(인식용 수순)은 실제 설명 내용이 아니므로 150자 제한(`moveNoteEffectiveLen`)에 포함하지 않는다 — 입력 중 글자 수 계산과 저장 시 검증 양쪽 모두 이 함수를 쓴다.

**버그 수정 — 집중 분석 모드 뒤로가기가 경로를 기억하지 않던 문제**
사용자 요청. 집중 분석 중 다른 수의 집중 분석으로(수 설명 안의 SAN 링크로) 이동했다가 "←"를 누르면 홈(보드)으로 곧장 나가 버려, 원래 보던 집중 분석으로 못 돌아왔다 — `LearnTab`에 `focusStack` 상태를 추가해, `enterFocusAt`이 이미 집중 분석 중일 때 호출되면(다른 집중 분석에서 옮겨온 것) 현재 `focus`를 스택에 쌓고, `exitFocus`(뒤로가기)는 스택에 남은 게 있으면 그 자리로 한 단계 되돌아가고 없을 때만 완전히 나간다. 보드/PGN을 새로 불러오는 등 집중 분석 밖으로 완전히 벗어나는 다른 경로들(`onLoadFen`·`onOpenMasterGame`·`onOpenMyGame`·`onLoadPgn`)도 스택을 함께 비운다.

**UI — 수 블록 키워드 칩을 한 줄 자동 스크롤로**
사용자 요청. 학습 탭(집중 분석 모드 포함) 수 블록의 키워드 칩들이 두 줄 이상으로 줄바꿈되던 것을, 항상 한 줄에 담고 다 안 들어가면 가로 스크롤(줄바꿈 없음)되도록 바꿨다. 새 공용 컴포넌트 `KeywordScroll`이 내용이 넘칠 때만(`ResizeObserver`로 감지) 자동으로 천천히 오른쪽으로 스크롤됐다가 끝에 닿으면 잠깐 멈춘 뒤 처음으로 돌아가는 것을 반복한다 — `MoveTile`과 집중 분석 화면의 현재 수 키워드 표시 두 곳에 적용했다.

**UI — 퍼즐 카드·퍼즐 풀이 카드 레이아웃 다듬기 (2차)**
사용자 피드백. 퍼즐 카드 제목 글자 크기를 키우고(`PUZZLE_NAME_BASE_FS` 11→12), 퍼즐 유형·라인 개수를 알려주던 텍스트를 지웠다(손상된 퍼즐 경고는 유지). 좋아요·리포스트·공유 아이콘과 수치, 공유 버튼을 한 단계씩 더 키웠다. 퍼즐 풀이 카드는 제작자 표시의 "제작 · " 접두사를 없애고, PGN/FEN 배지·퍼즐 레이팅 박스를 제작자 표시와 같은 줄로 내려 제목을 한 줄에 더 길게 보이게 했다.

`npm run build` 통과, Playwright로 앱을 실제로 띄워 퍼즐·분석·학습 탭에 새 런타임 오류가 없는 것을 확인했다. 서버(Gemini API 키·Supabase RPC)가 필요한 실제 동작(이미지 인식 속도·퍼즐 삭제 RPC)은 이 샌드박스에서 직접 검증할 수 없어 코드 경로 검토로 확인했다 — `puzzle_delete` RPC는 `supabase-setup.sql`에 추가돼 있으나 실제 Supabase 프로젝트에 별도로 적용(마이그레이션 실행)해야 한다.

**기능 — PLAY: 레이팅별 봇과 직접 대국 (신규 `/play` 페이지)**
사용자 요청. 학습 탭 메인 체스보드 바로 아래 정중앙에 라운딩된 재생 아이콘 + "PLAY" 버튼을 추가했다 — 누르면 지금 보드에 입력돼 있는 포지션(진행된 수순 `sans` 또는 FEN 모드의 `fenRoot`)을 시작점으로 삼아 전용 페이지 `openchess.kr/play`가 새 히스토리 항목으로 열린다. 이 앱은 react-router 없이 `window.history.pushState`/`popstate`로 직접 라우팅하는 구조라(`/review`, `/puzzle/(번호)-(라인)`과 같은 기존 패턴), `/play`도 같은 방식(딥링크 resolver에 분기 추가, `playGame` 상태 + `openPlay`/`closePlay`)으로 통합했다 — 주소창에 직접 `/play`를 입력해도(표준 시작 위치로) 열린다.

① **설정 화면**: 진입하면 먼저 어느 진영(백/흑/랜덤)으로 둘지, 어떤 봇(400~2800, 7단계)과 대국할지 고르는 화면이 뜨고, "대국 시작"을 누르면 실제 대국이 시작된다.

② **레이팅별 봇 로직**: Stockfish 자체의 `UCI_LimitStrength`는 표현 가능한 최저 Elo가 1300대 안팎으로 높아 그보다 낮은 레이팅을 낼 수 없다 — 대신 `evaluateMulti`(MultiPV 5)로 후보 수 목록을 받아, 온도(temperature) 기반 가중 무작위로 하나를 고르는 자체 로직(`pickBotMove`)을 만들었다. 레이팅이 높을수록 온도가 낮아져 최선의 수를 거의 항상 고르고, 낮을수록 2~5순위 후보 수도 자주 섞여 나온다 — UCI의 score(cp)는 항상 "지금 둘 차례" 관점이라 후보끼리 값을 그대로 비교해 소프트맥스 가중치를 계산할 수 있었다. 레이팅에 따라 탐색 depth·movetime도 함께 늘려, 높은 레이팅일수록 후보 목록 자체의 질도 좋아지게 했다.

③ **대국 진행**: 보드 조작(클릭·드래그·프로모션)은 학습 탭 메인 보드와 동일한 패턴(`tryMove`/`onSquareClick`/`onPieceDrag`/`onDrop`)을 그대로 재구현했고, 게임 종료 판정은 기존 `gameEndState`(체크메이트·스테일메이트·3회 동형 반복)를 그대로 재사용해 새 판정 로직을 따로 만들지 않았다. 기권 버튼, 대국 종료 후 "대국 리뷰"(기존 `/review`로 그대로 넘김)·"다시 설정" 버튼을 뒀다.

**버그 수정 — PLAY 페이지에서 봇이 응답하지 않던 문제(공유 엔진 큐 기아)**
사용자 제보로 발견 — PlayPage(전체 화면 오버레이)를 열어도 그 밑의 학습 탭(`LearnTab`)은 언마운트되지 않고 계속 살아 있는데, `liveOn`(실시간 평가) 조건에 `!reviewGame`만 있고 `!playGame`이 빠져 있어 LearnTab이 계속 `useEngine`의 단일 공유 워커 큐에 자기 실시간 평가 작업을 채워 넣었다 — PlayPage의 봇 수 요청(`engine.evaluateMulti`)이 차례를 영영 받지 못해 "생각하는 중..."에 무한정 멈춰 있었다. 리뷰 페이지가 이미 쓰던 것과 같은 패턴으로 `liveOn={liveOn && !reviewGame && !playGame}`을 학습 탭·퍼즐 탭 양쪽에 적용해 고쳤다.

`npm run build` 통과, Playwright로 실제 대국 한 수(1.e4)를 두고 봇이 실제로 응수(1...e6)하는 것까지 엔드투엔드로 확인했다. 이 샌드박스(리소스 제한 컨테이너)에서는 WASM 엔진의 첫 탐색이 실제 기기보다 훨씬 느려(관찰된 응답 시간 약 10~15초) 실제 사용자 체감 속도와는 차이가 있을 수 있다.

**UI — PLAY 버튼 축소·배치 변경 + 대국 화면을 chess.com 스타일로 재구성**
사용자 피드백. 학습 탭 PLAY 버튼이 보드 아래 별도 줄에 크게 있던 것을, 뒤집기·초기화·뒤로·앞으로 네 버튼과 같은 줄 가운데에 그 버튼들과 같은 높이(40px)로 작게 배치했다.

PLAY 페이지의 대국 화면을 chess.com 대국 화면 구조(맨 위 수순 스트립, 상대 정보 줄, 보드, 내 정보 줄, 하단 컨트롤)를 참고해 다시 짰다. ① 맨 위에 "1. e4 c6"처럼 지금까지 둔 수를 가로 스크롤 스트립으로 보여주고, 각 수를 누르면 그 시점 포지션으로 되돌아볼 수 있다(`viewPly` 상태 — 새 수를 두면 자동으로 라이브로 복귀). ② 보드 위·아래에 상대(봇, 아이콘+레이팅 숫자, 생각 중일 때는 "생각하는 중..."으로 바뀜)와 나("나") 정보 줄을 뒀다. ③ 하단 컨트롤을 톱니 아이콘(기권·대국 리뷰·다시 설정을 담은 작은 팝업 메뉴) + 뒤로/앞으로(수순 되돌아보기) 세 버튼으로 정리했다 — 기존 텍스트 버튼 3개를 나란히 두던 줄은 없앴다.

`npm run build` 통과, Playwright로 학습 탭 PLAY 버튼 배치와 대국 화면(수순 스트립·상대/내 정보 줄·보드·하단 컨트롤)이 의도대로 렌더링되는 것을 스크린샷으로 확인했다.

**UI — PLAY 페이지 브랜딩 + 봇 강도·응수 로직 개선**
사용자 요청. PLAY 페이지 상단 헤더의 "PLAY" 아이콘·텍스트를 다른 페이지 헤더와 같은 오픈체스 로고(`/OpenChessLogo.png`)로 교체했다. 보드 아래 내 정보 줄도 하드코딩된 "나" 텍스트+체스 말 아이콘 대신, 실제 오픈체스 프로필 사진·아이디(`profile.photo`, `profile.nickname || profile.displayId`)를 보여주도록 고쳤다 — 로그인하지 않았거나 프로필 정보가 없으면 기본 프로필 아이콘과 "Unnamed"로 대체한다.

사용자 신고 — "레이팅 2800 봇이 너무 안 좋은 수를 둔다". 후보 수를 고르는 소프트맥스 온도(`botTemperature`)가 레이팅에 선형으로만 비례해 줄어들어(elo 400에서 T≈224, elo 2800에서도 T=8) 최상위 레이팅에서도 평가치 손실이 30~40cp 나는 후보가 꽤 자주 섞여 나왔다. 400(T=220)~2800(T=5) 구간을 지수적으로 보간하는 곡선으로 바꿔, 레이팅이 높아질수록 "약간 나쁜 수"의 가중치가 훨씬 가파르게 줄어들도록(사실상 배제되도록) 고쳤다 — 그랜드마스터 등급대에서는 사실상 최선 수 또는 그와 동등한 후보만 남는다. 탐색 depth·movetime 상한도 레이팅대별로 소폭 올렸다.

사용자 요청 — "봇도 응수 시간을 좀 가졌으면 한다". 기존엔 엔진 계산이 끝나는 즉시(짧으면 수백ms 이하) 바로 수를 뒀는데, `botThinkDelayMs`(레이팅에 비례해 600ms~2200ms)를 추가해 `pickBotMove` 계산과 `Promise.all`로 묶어, 계산이 그보다 빨리 끝나도 최소한 그 시간만큼은 "생각하는 중..."을 유지한 뒤 수를 두도록 했다.

사용자 요청 — "특정 포지션에서 같은 수만 반복해서 두지 않도록". 대국 하나마다 `botMoveMemoryRef`(`Map<FEN, Map<수, 선택 횟수>>`)를 두어, `pickBotMove`가 같은 포지션에서 이미 골랐던 적 있는 수의 소프트맥스 가중치를 선택 횟수만큼 지수적으로(0.12^n) 깎도록 했다 — 반복 포지션이 나와도 매번 같은 수만 두지 않고 다른 합리적인 후보로 넘어갈 확률이 커진다. 대국을 새로 시작하거나 재대국(rematch)할 때마다 이 기억은 초기화된다.

`npm run build` 통과, Playwright로 PLAY 설정/대국 화면에서 로고와 기본 프로필+"Unnamed" 정보 줄이 의도대로 렌더링되는 것, 그리고 실제로 1.e4를 두고 봇이 "생각하는 중..." 상태를 거쳐 응수하는 것까지 확인했다(이 샌드박스는 WASM 엔진이 느려 관찰된 응답 시간이 약 14초였다 — 실제 기기에서는 봇 사고 지연(0.6~2.2초)이 지배적일 것이다).

### OpenChess v0.4.0 — 2026/8/28

**기능 — 탭 이름 개편 + 메인 퀘스트 전면 개편("레슨" 시스템)**
탭 이름을 재편했다 — 기존 "학습" 탭은 "분석"으로, 기존 "퀘스트" 탭은 "학습"으로 이름을 바꿨다(메인 퀘스트 탭 강화 1단계). 이름 변경에 맞춰 내부 주석과 `/about` 소개 페이지 문구도 전체적으로 정리했다.

메인 퀘스트를 챕터 아코디언 목록 대신, 갈래를 갖는 모식도(나무) 형태로 순차 해금되는 "레슨" 시스템으로 전면 개편했다. 레슨은 페이지·스크립트(beats: 설명/객관식/주관식/원칙 목록/보드 연출 등) 기반 엔진으로 구현했고, 학습 탭·레슨 창은 크림 톤 디자인으로 새로 만들었다. 레슨 로드맵은 원형 노드에서 가로 카드형으로, 좌상단→우하단 대각선 트리 배치로 바꿨고, 노드 모양도 라운딩된 사각형으로 정리했다. 레슨 하나가 여러 선행 레슨(parents)을 가질 수 있도록(DAG) 지원을 추가했고, 레슨 보드 연출에 공격자/방어자 화살표 표시를 추가했다. 레슨 편집은 개발자 전용 권한으로 제한하고, 레슨의 제목·설명·보상·선행 레슨과 페이지·beat를 직접 편집할 수 있는 전용 폼 편집기를 새로 만들었다. 레슨 보드 연출의 좌표 정렬 버그를 고치고, 코치 말풍선이 상호작용 영역을 채우도록 개선했다.

**기능 — 퍼즐/도감 오프닝 검색 개편**
퍼즐 탭의 오프닝 검색과 퍼즐 생성자 검색을 하나의 순위가 매겨진 통합 박스로 합치고 정렬 옵션을 추가했다. 검색 범위를 모든 `SNAP.tree` 변형까지 남김없이 포함하도록 확장했다. 도감 오프닝 트리 검색창은 퍼즐 탭과 같은 디자인으로 통일하고 캔버스 밖 헤더로 분리했다. 검색 드롭다운의 "오프닝" 라벨 색상 대비를 수정했다.

**기능 — 집중 분석 하이퍼링크 + 뒤로가기 전반 정비**
수 설명 본문에서 실제로 그 위치에서 둘 수 있는 SAN을 언급하면 자동으로 그 수까지의 수순으로 이동하는 링크가 걸리도록 했다(설명 최대 길이도 150자로 늘림). 퍼즐 카드·오늘의 퍼즐 팝업의 기보에서 수를 클릭하면 집중 분석 모드로 바로 진입하도록 했고, 뒤로가기 동작을 전반적으로 정비했다 — 자체 URL 경로가 없는 오버레이(검색/친구/채팅/프로필창/티어맵/집중 분석/레슨/도감 카드 등)도 브라우저 뒤로가기 한 번으로 닫히게 했다.

**버그 수정 — 수 등급 판정 통일**
cp(센티폰) 기반 기초 등급 산정(승부가 이미 기운 위치에서의 판정 완화, 언더프로모션 승격 처리)을 여러 판정 경로(`classifyMoveKindDetailed`·`analyzeGame`·분석 탭·자유 탐색)에 걸쳐 하나로 통일했다. 탁월한 수 판정 중 포크 규칙을 수정하고, chess.com 연동 퀘스트·칭호가 참조하는 오프닝 기준도 함께 바로잡았다. 보드 스캔이 과도하게 인식하던 문제를 고쳤다.

**성능/버그 수정 — 퍼즐 탭 버벅임/먹통**
퍼즐 탭 오프닝 필터가 `useMemo` 없이 매 렌더마다 다시 계산되던 성능 버그를 고쳐, 일일 퍼즐 클릭 시 버벅이거나 먹통이 되던 문제를 해결했다. 정렬 박스(최신순/레이팅순) 관련 `useMemo` 두 개가 조기 반환 뒤에 있어 퍼즐 카드 클릭 시 흰 화면 먹통이 재발하던 버그도 함께 고쳤다.

**UI**
퍼즐 카드/일일 퍼즐 팝업 기보의 밑줄을 제거했다. 프로필 유산 칸이 3개 미만일 때 왼쪽 정렬되도록 하고, 분석 탭 아이콘을 Analytics 아이콘으로 교체했다.

> 참고: 이 버전은 실제로는 PR #40으로 병합되어 배포됐지만, 병합 시 README·CHANGELOG·`VERSION_HISTORY` 갱신이 누락돼 버전 표시가 v0.3.9에 멈춰 있었다. 이번(v0.4.1) 세션에서 실제 커밋 내역을 근거로 세 곳을 소급 정리했다.

### OpenChess v0.3.9 — 2026/8/21

**기능 — 데스크톱 전용 게임 리뷰 레이아웃 개편**
데스크톱에서는 화면 전체를 넓게 쓰는 전용 레이아웃으로 리뷰 화면을 다시 짰다 — 체스보드를 확대하고, 진입 애니메이션의 체스보드·정확도 그래프도 큰 화면에 맞게 키웠다. 이어서 평가치 그래프는 오히려 더 작게 줄이고 코치 설명을 보드 옆이 아니라 그 그래프 위쪽으로 옮겨 균형을 다시 맞췄다. 진입 시 함께 보여주는 평가치·정확도 그래프 세 개가 서로 비슷한 크기로 보이도록 조정했다.

**기능 — 리뷰 정확도 그래프 전면 재설계**
계산 중임을 알리는 안내와 계산이 끝났을 때의 확정 토스트, 구간별 등급 표시를 새로 설계했다. 그래프를 고정 간격 + 스크롤링 렌더링 방식으로 바꾸고, 수 번호 표기와 즉시 반영 페이싱을 다듬었다. 리빌 속도를 탄력적으로(계산이 밀리면 더 빠르게 따라잡도록) 재설계하고, 애니메이션이 정체되거나 중첩되는 버그와 정확도 증감 토스트가 밀리는 버그를 고쳤으며 `MiniAccCurve` 렌더링을 최적화했다. 체스보드 진입 애니메이션이 재생되는 동안은 그래프를 뒤로 숨기고 속도를 0.8배로 늦춰 두 애니메이션이 겹쳐 보이지 않게 했다. 부정확한 수부터는 정확도가 반드시 감소하도록 계산 로직도 다시 손봤다.

**기능/버그 수정 — 드래그 무브 판정 재설계**
방향 후보 칸 + 속도 기반 허용치 방식으로 드래그 착수 판정을 다시 짰다. 시작 문턱과 종료 지점 판정을 여러 차례 재조정해 실제 조작감에 맞췄고, 드래그 고스트를 ref 기반 명령형 갱신으로 바꿔 렌더링 병목을 없앴다. 탐색 박스가 빠른 드래그에서 왜곡되던 버그도 고쳤다.

**성능/버그 수정 — 리뷰 엔진 안정화**
Stockfish 18(정식, 대형 신경망) 분석 엔진을 추가하고 목록 맨 위로 올렸다. 리뷰 풀 부팅 경합으로 계산이 정체되던 문제를 고치고, 탁월한 수/유일한 수 판정 기준(2점)을 재조정했다.

**버그 수정 — 그 외**
오프닝 이름을 수정하면 그 오프닝에서 만들어진 퍼즐 이름에도 즉시 반영되도록 고쳤다. 도감 오프닝 트리에 이론 수를 추가하면 화면에 곧바로 반영되게 했다. 게임 리뷰를 닫았다가 같은 수순으로 다시 열어도 진입 애니메이션이 항상 처음부터 다시 재생되도록 고쳤다. 체스판 사진 스캔 기능 백엔드를 Gemini로 재전환해 다시 정상 동작하게 했다. 퍼즐 레이팅을 상향 조정하고 리뷰 필터의 레이팅 변동 버그, 탐색 박스가 모바일에서 잘리던 문제, 일일 퀘스트의 chess.com 링크가 앱을 안 여는 문제를 고쳤다.

**시스템**
사이트 전반 폰트를 IBM Plex Sans KR로 통일했다. 설정 탭에 로그인 상태일 때 프로필 미리보기를 표시한다.

### OpenChess v0.3.8 — 2026/8/19

**기능 — 설정 탭 "퍼즐 설정" 카드(LINE/PUZZLE CLEAR 애니메이션·코치 말풍선 on/off, 계정 영구 저장)**
"설정 탭에 '퍼즐 설정' 블록을 추가하고, 라인 클리어 애니메이션, 퍼즐 클리어 애니메이션, 코치 말풍선 표시 온오프 토글을 만들어 줘, 기본값은 모두 켜져 있는 걸로 하고, 세팅이 계정에 영구 저장되도록 해줘" — 설정 탭 "리뷰 설정" 카드 바로 아래에 새 카드를 추가해 세 토글(`lineClearOn`/`puzzleClearOn`/`coachBubbleOn`, 모두 기본 `true`)을 둔다. LINE/PUZZLE CLEAR 토글은 `PuzzleSolver`가 두 배너(`<LineClearBanner>`/`<PuzzleClearBanner>`)를 그릴지 말지를 그대로 가른다(꺼도 라인 자동 전환 타이머 등 내부 로직은 배너 렌더링과 무관하게 그대로 동작). 코치 말풍선 토글은 `PuzzleSolver`에 이미 있던 "코치 숨기기"(눈 아이콘, 세션 동안만 유지되는 수동 토글)의 초기값을 정한다 — 꺼져 있으면 그 퍼즐을 열 때부터 기본으로 숨겨진 채 시작하고, 그 세션 동안만은 눈 아이콘으로 다시 켜서 볼 수도 있다.

"계정에 영구 저장"이 핵심 요구였다 — 앞선 라운드의 리뷰 속도/포지션 변동성 보정처럼 이 기기의 localStorage에만 남는 얕은 저장(`loadReviewSpeedPref` 패턴)이 아니라, 보드/기물 스킨과 같은 경로(로그인 시 Supabase `user_progress.progress`(JSONB, 스키마 변경 없이 새 키만 추가) + 그 로컬 캐시 사본(`store.set`))로 실어 다른 기기에서 로그인해도 그대로 따라오게 했다. 두 저장 지점(로컬 캐시 restore·Supabase `progress` restore)에서 각각 안전하게 복원한다 — `if (d.xxx)`(참일 때만) 같은 흔한 패턴은 불리언에 그대로 쓰면 `false`로 저장된 값을 "저장 안 됨"과 구분하지 못해 기본값(true)으로 되돌아가 버리므로, `=== false`로만 판정해 명시적으로 꺼둔 경우에만 기본값(true)을 뒤집도록 했다. 로그아웃 시에는(다음 게스트/계정이 이전 계정 설정을 잠깐이라도 물려받지 않도록) 세 값 모두 다시 기본값(true)으로 리셋한다.

로컬 dev 서버로 `npm run build` 통과와 Playwright로 설정 탭에 새 카드·세 토글 문구가 모두 표시되는 것, 토글 클릭이 실제로 상태를 바꾸는 것을 확인했다.

**기능 — 독립 정확도 체계 재보정: 조화평균의 "한 수 쏠림" 완화(NEW_ACC_HARMONIC_FLOOR)**
"블런더 하나가 깎는 정확도가 너무 커, 보정해줘" — 실제 대국(openchesskr vs HaunthHenge, 상대편이 9수 중 무난한 수 8개(이론/최선) + 블런더 1개)을 chess.com 공식 리뷰와 나란히 비교해 준 스크린샷으로 검증했다. chess.com은 이 상대편 정확도를 72.1로 매기는데, 이 사이트는 40.2로 훨씬 낮게 나왔다 — 두 서비스의 수 등급 분류(이론 4~6·최선 2~3·블런더 1)는 사실상 일치했으므로, 등급 분류가 아니라 등급→정확도 변환 이후 단계(누적 방식)의 문제였다.

원인은 조화평균(harmonicMean = n / Σ(1/acc_i)) 자체의 구조다 — acc_i가 아주 작은 항 하나가 분모 합을 지배해, 극단적인 블런더 한 수의 개별 정확도(이 사례는 역산하면 약 7점)가 나머지 8개 만점 수를 거의 다 덮어써 버린다. `NEW_ACC_DECAY`(손실→정확도 감쇠 자체)를 완화하는 방향은 부정확·실수 같은 가벼운 감점까지 전부 함께 관대해져 버려 선택하지 않았다 — 대신 "한 수가 조화평균 합계(Σ(1/acc_i))에 기여할 수 있는 몫의 하한"만 새로 뒀다(`NEW_ACC_HARMONIC_FLOOR = 22`, 기존엔 발산 방지용으로만 쓰던 0.5). 개별 정확도가 이 값보다 낮은 수는 조화평균 계산에서 이 값으로 취급되어, 아무리 심한 블런더라도 그 수 하나가 나머지 좋은 수들을 압도하지 못한다 — 반대로 이미 22보다 높은(=덜 심각한) 부정확·실수 등급 수의 감점 강도는 전혀 달라지지 않는다. 이 값(22)은 위 실제 대국(8×100 + 1×22 → 조화평균 71.7)이 chess.com의 72.1과 거의 일치하도록 역산해 골랐다. `/about` 페이지의 정확도 체계 소개 섹션(별도 번들이라 공식을 그대로 복사해 둔 사본)에도 같은 하한(`AS_HARMONIC_FLOOR`)을 반영했다 — 다만 그 섹션의 손 계산 예시(블런더 손실 25%p 하나)는 개별 정확도가 22.9로 이미 새 하한보다 높아 표시되는 수치(86.7/74.8)에는 변화가 없다.

로컬 dev 서버 + Playwright로 ① 실제 게임 리뷰를 실행해 새 계산이 오류 없이 동작하는 것, ② 위 실제 대국과 같은 구조(8×100 + 1×22)로 손 계산했을 때 71.7이 나와 chess.com의 72.1과 0.4점 차이로 근접하는 것, ③ `/about` 페이지 예시 수치(86.7/74.8)가 하한 변경 전후로 그대로 유지되는 것을 확인했다.

**UI/기능 — 리뷰 진입 화면 백·흑 정확도 그래프 개편(하한선 완화 곡선·굵기·수별 등급 아이콘 마커·구간별 색상)**
사용자 요청 다섯 가지를 한 번에 반영했다. 대상은 `ReviewAccuracyRevealAnim`(게임 리뷰에 들어갈 때 정확도를 계산하며 보여주는 화면)의 `MiniAccCurve`(백/흑 정확도 스파크라인) 하나다.

① "하한선을 더 좁게 설정(초기 95)하고, 정확도가 하한선보다 낮아질 경우 하한선을 조금씩 낮추는 방향으로 하자" — 기존엔 세로 격자 하한선이 60/0 두 값 사이에서 즉시 스냅됐다(60 밑으로 떨어지는 순간 곧장 0으로). 이제 하한선을 95에서 시작해, 정확도가 95 밑으로 떨어지면 그 값을 5 단위로 여유 있게(3점 버퍼) 담을 수 있는 만큼만 목표를 낮추고, `requestAnimationFrame` 루프로 그 목표에 매 프레임 시간비례(초당 약 99.9%)로 서서히 다가가는 감쇠(ease)를 적용했다 — 회복되면 같은 방식으로 다시 95를 향해 서서히 좁혀진다. (버그 수정) 처음엔 "지금 하한선이 이미 그 값을 담고 있는가"로 목표를 판단했는데, 하한선 자신이 낮아지는 도중에 정확도값을 추월해버려 목표가 95↔낮은값 사이를 진동하다 정확도 바로 밑에 딱 붙어버리는(여유 없음) 버그가 있어, 항상 고정된 기준(95)만 보고 목표를 정하도록 고쳤다.

② "그래프 굵기를 조금 더 굵게" — 선 굵기를 1.8/2.4px(일반/확대 모드)에서 2.6/3.4px로 키웠다.

③④ "그래프 사이사이에 수에 해당하는 부분에 원을 표시하고, 그 원에는 그 수의 수 체계 아이콘이 채워지는 애니메이션을 넣어줘, 그리고 그래프의 각 부분의 색을 각 부분의 수 아이콘 색으로 바꿔줘" — `buildRevealData`가 이제 진영별 누적 정확도 곡선(wCurve/bCurve)과 정확히 1:1로 대응하는 move 레퍼런스 배열(wMoves/bMoves)도 함께 만들어 `MiniAccCurve`에 넘긴다. 그래프 선을 하나의 `<path>` 대신 수마다 나눈 `<line>` 세그먼트로 그려, 각 구간이 그 수의 등급 색(기존 `QCOLOR`, 리뷰 요약·수순 목록 등 앱 전역에서 이미 쓰는 배색)을 그대로 띠게 했다. 각 수 지점에는 원 마커(색은 QCOLOR와 동일)를 찍고 그 안에 그 수의 등급 아이콘(`badgeIcon`, 이미 앱 전역에서 쓰는 것과 같은 PNG)을 넣어, 수가 새로 공개될 때(shownCount 증가) 마커가 처음 마운트되는 순간 자연스럽게 pop-in 애니메이션(`miniAccDotPop`, 0→커짐→제자리로 안착)이 한 번 재생되게 했다 — 별도의 타이머 관리 없이 typoLetters 등 이 세션에서 써온 패턴과 같은 "마운트 시 자동 재생" 방식이다. 원 안 아이콘은 SVG 안에 raster 이미지를 끼워 넣는 대신(비율이 왜곡되기 쉬움) 같은 박스 위에 겹친 일반 HTML 오버레이로 그려 항상 또렷하게 보이게 했다. (버그 수정) 처음엔 원 지름을 8~11px로 작게 잡았는데, 그 크기에서는 PNG 아이콘이 뭉개져 흰 얼룩처럼만 보이고 무슨 등급인지 전혀 알아볼 수 없어서, 이 앱 다른 곳에서 등급 아이콘을 쓰는 최소 크기(11~15px)에 맞춰 원을 14~18px로 키웠다.

로컬 dev 서버로 `npm run build` 통과를 확인했고, Playwright로 실제 PGN(모차르트의 오페라 게임)을 붙여넣어 게임 리뷰를 실행해 분석 진행 중 화면을 단계별로 캡처, 하한선이 95에서 시작해 정확도가 떨어진 흑 쪽만 필요한 만큼(80대) 부드럽게 넓어지는 것, 굵어진 선, 수마다 찍힌 원 마커와 그 안의 등급 아이콘(책=이론 수, 별=최선/우수한 수, 체크=좋은 수, "?!"=부정확, "?"=실수 등), 구간별 색상이 모두 스크린샷으로 실제 확인됐다.

**UI — 통계 분리 토글을 프로필 카드 최상단에서 우상단 여백으로 이동**
"통계 분리 토글은 프로필 카드의 우상단 여백으로 이동시키자" — OpenChess/chess.com 통계를 고르는 토글을 카드 최상단의 큰 세그먼트 바(아이콘 42×34px)로 두던 것을 그만두고, "@아이디" 헤더 줄 오른쪽(MyProfileCard는 "프로필 편집" 버튼 옆, 친구·검색 프로필 상세는 채팅 버튼 옆)에 들어가는 작은 아이콘 두 개(30×26px)로 줄여 그 자리로 옮겼다 — 카드 세로 공간을 토글이 먼저 차지해 아이디·이름이 아래로 밀리던 것을 없앴다. 토글 버튼 자체를 그리는 부분을 `statsViewToggle` 공용 헬퍼로 뽑아 `MyProfileCard`·친구·검색 프로필 상세가 공유하게 했고, 프로필 상세 쪽의 통계 내용 컴포넌트는 이제 토글을 직접 그리지 않고 `statsView` 값만 받아 보여주는 역할로 바뀌어(`ProfileStatsToggle` → `ProfileStatsPanel`로 이름도 정리) 부모(`UserSearchModal`·`FriendsModal`)가 헤더 줄에서 토글을, 그 아래서 패널을 각각 그린다. 로컬 dev 서버로 `npm run build` 통과와 헤더 줄 레이아웃(정적 목업 스크린샷)을 확인했다.

**UI/기능 — LINE CLEAR·PUZZLE CLEAR 배너 마무리 다듬기(자간·정렬·순차 재생) + 친구·검색 프로필 상세에도 OpenChess/chess.com 통계 분리 적용**
사용자 요청 네 가지.

① "LINE CLEAR 텍스트의 자간을 조금 늘리고" — `typoLetters` 헬퍼에 `spacing`(기본값 0) 매개변수를 추가해 글자마다 `marginRight`를 줄 수 있게 하고, `LineClearBanner`의 LINE·CLEAR 두 줄에만 4px을 줘 자간을 살짝 넓혔다(`PuzzleClearBanner`는 그대로 0이라 기존 자간 그대로).

② "LINE CLEAR와 PUZZLE CLEAR의 두 텍스트의 y좌표를 각각 통일해줘" — 두 배너 모두 `flexDirection:column`+`justify-content:center`로 화면 정중앙에 콘텐츠를 배치하는데, 아이콘 영역의 높이(체크 표시 SVG 60px vs 별 3개 약 30px)와 바깥 `gap` 값(12 vs 14)이 서로 달라 전체 콘텐츠 블록의 높이가 미묘하게 갈리면서 정중앙에 오는 텍스트 줄의 y좌표도 따라 어긋나 있었다. 두 배너의 아이콘 영역을 똑같이 `height:60px`인 flex 슬롯으로 감싸고 바깥 `gap`도 14로 통일해, 두 텍스트 블록이 항상 같은 y좌표에 오도록 고쳤다.

③ "퍼즐의 마지막 라인을 해결했을 때, LINE CLEAR 애니메이션이 완전히 재생되고 사라진 뒤에 PUZZLE CLEAR 애니메이션이 나타나도록 해줘" — `PuzzleClearBanner`의 시작 지연을 LINE CLEAR의 총 재생 시간과 같은 1.1s에서 4.2s로 늘리고, 내부 모든 요소의 절대 지연 값도 함께 +3.1s만큼 밀었다(글로우 1.3→4.4s, 별 3개 `1.3+i*0.22`→`4.4+i*0.22`s, PUZZLE·CLEAR 글자 2.0/2.35→5.1/5.45s) — 배너가 나타난 시점 기준으로 본 내부 등장 순서(글로우→별→글자)는 그대로 유지된다. 전체 재생 길이가 8.4초(지연 4.2s + 재생 4.2s)로 늘어난 만큼, `PuzzleSolver`의 celebrate 자동 종료 타이머(전체 클리어 케이스)도 6.2초에서 9.3초로 늘려 글자가 다 나오기 전에 배너가 잘리지 않게 했다.

④ "친구, 검색 창 경로로 확인하는 자신이나 남의 프로필 카드에도 이번 세션에서 변경된 내용을 적용해줘" — `UserSearchModal`·`FriendsModal`의 프로필 상세 화면에도, 지난 라운드에 `MyProfileCard`에 추가한 최상단 OpenChess/chess.com 통계 분리 토글을 그대로 적용했다. 두 모달에 중복 구현하는 대신 읽기 전용 버전의 공용 컴포넌트 `ProfileStatsToggle`을 새로 만들어(편집·유산 관리 버튼 없이 토글 + `PublicProfileStats(hideChesscom)`/`AccountChessStats`만 전환) 두 모달 모두 이 컴포넌트를 쓰도록 바꿨다. 아울러 이름 아래 줄에 있던 "@아이디" 표시도 `MyProfileCard`·`HeaderProfileMenu`와 같은 자리(상단 헤더 라벨)로 옮겨 세 곳의 프로필 표시 방식을 완전히 통일했다(`FriendsModal`의 채팅 버튼도 이 헤더 라벨 줄로 함께 옮겨졌다).

로컬 dev 서버로 `npm run build` 통과를 확인했다. 이 세션의 샌드박스는 실제 유저 데이터가 있는 Supabase 프로젝트에 접근하지 못해(검색·리더보드·친구 추천 모두 빈 결과) 두 모달의 프로필 상세 화면을 실제로 열어보는 종단 간 테스트는 하지 못했고, 대신 코드 리뷰로 각 컴포넌트가 참조하는 prop·상태가 실제로 올바르게 전달되는지(특히 `ownerUid`/`viewerUid`, `chesscom` 훅 호출 위치) 재확인했다.

**UI/기능 — LINE CLEAR 배너 완전 통일(초록 체크 애니메이션 추가) + 프로필 카드를 헤더 드롭다운의 별도 "프로필 창"으로 이전 + OpenChess/chess.com 통계 분리**
사용자 요청 세 가지.

① "LINE CLEAR 애니메이션도 글자 크기를 똑같이 하고, 재생 시간도 똑같이 해주고, 초록색 원과 체크 표시가 되는 애니메이션을 먼저 삽입해줘" — 지난 라운드에서 이미 폰트·연출 방식은 통일했지만 라인 클리어 쪽만 글자를 32px로 줄이고 재생 시간도 2.2초로 짧게 잡아 뒀었다. 이번엔 글자 크기(46px)·재생 시간(4.2초) 모두 PuzzleClearBanner와 완전히 같게 맞추고, 별 3개가 있던 자리에 초록색 원이 먼저 그려진 뒤(스트로크 `stroke-dashoffset` 애니메이션으로 원 둘레를 0.45초에 걸쳐 그림) 그 안에 체크 표시가 이어서 그려지는(0.25초) "성공" 아이콘을 추가해 텍스트보다 먼저 재생되게 했다(`checkDraw` 키프레임 신규 추가). 재생 시간이 늘어난 만큼 `PuzzleSolver`의 celebrate 자동 종료 타이머도 라인 단독 클리어 케이스를 2.7초→5.1초로 늘렸다.

② "프로필 창에서 보이는 프로필 카드와 우상단 아이디를 눌러 나오는 카드도 이번 세션에서 변경된 사항을 똑같이 적용해 주고, 이제 설정 탭에서는 프로필 카드를 표시하지 말고, 대신 우상단 아이디를 눌러 나오는 카드의 우상단에 화살표 버튼을 눌러 자세히 볼 수 있게 하자" — `MyProfileCard`(내 프로필 카드)를 설정 탭(`SettingsTab`)에 상시 표시하던 것을 그만두고, 헤더 드롭다운(`HeaderProfileMenu`)의 새 화살표(`ChevronRight`) 버튼으로만 여는 별도 모달 `ProfileWindow`로 옮겼다. `MyProfileCard`가 필요로 하던 chess.com 계정 연동 플로우(아이디 입력·검증·확인 모달 — `cc`/`ccState`/`pending`/`verifyChesscom`/`confirmLink`/`changeChesscom`/`chesscomDaysLeft` 상태와 관련 UI)는 원래 `SettingsTab`이 들고 있었는데, `MyProfileCard` 없이는 그 자리에 남아 있을 이유가 없어 `ProfileWindow`로 통째로 옮겼다(z-index는 `ProfileWindow` 자체 배경 80 < `MyProfileCard`의 "프로필 편집" 모달 85 < chess.com 계정 확인 모달 90 순으로, 세 겹이 항상 이 순서로 쌓이도록 유지했다). `HeaderProfileMenu` 드롭다운 카드에도 `MyProfileCard`와 같은 헤더 구성(상단에 "@아이디" 라벨, 이름·소개 사이 중복 @아이디 줄 제거)을 적용해 두 곳의 프로필 표시가 완전히 일치하게 했다.

③ "프로필 카드에서 이제 최상단에 선택 박스를 만들고, OpenChess 아이콘(검은색 나이트), Chess.com 아이콘(초록색 폰)만을 각각 표시하여 두 부분의 통계를 분리하여 볼 수 있게 하자" — `MyProfileCard` 최상단에 아이콘 두 개(OpenChess 파비콘 `/favicon.png`, chess.com 로고 `/chess.com_Icon.png`)만 있는 세그먼트 토글을 추가하고, `statsView`("oc"|"cc") 상태에 따라 OpenChess 자체 통계(퀘스트 진척도·유산·푼 퍼즐, `PublicProfileStats`)와 chess.com 통계(`AccountChessStats`)를 완전히 분리해서 한 번에 한쪽만 보여준다 — 예전엔 이 카드 하나에 OC 통계 먼저, chess.com 통계는 점선 아래로 순서대로 쌓아 함께 보여줬다. chess.com 미연동 상태에서 그 탭을 고르면 통계 대신 연동 안내와 "연동하기" 버튼(편집 모달로 바로 이어짐)을 보여준다.

로컬 dev 서버로 `npm run build` 통과를 확인했고, Playwright로 게스트(비로그인) 상태에서 설정 탭에 더 이상 프로필 카드가 표시되지 않고 "리뷰 설정" 카드가 정상적으로 보이는 것, 콘솔 에러가 없는 것을 확인했다. 로그인이 필요한 부분(헤더 드롭다운의 화살표 버튼 클릭 → 프로필 창 열기, OpenChess/chess.com 통계 토글 전환)은 이 샌드박스에서 실제 Supabase 인증 없이는 종단 간 테스트를 하지 못해, 프롭 전달 경로(어떤 상태가 어느 컴포넌트로 흘러가는지)와 각 컴포넌트에서 참조하는 모든 식별자가 실제로 정의돼 있는지를 코드 리뷰로 재확인했다(예전 `SettingsTab`에서 쓰던 chess.com 연동 상태·함수가 새 `ProfileWindow`로 옮겨진 뒤 남은 곳 어디에서도 더는 참조되지 않는 것을 grep으로 확인).

**UI/기능 — LINE CLEAR·PUZZLE CLEAR 배너 통일 + 설정 탭 문구 수정 + /about 정확도 체계 소개 섹션**
사용자 요청 세 가지.

① "LINE CLEAR 애니메이션도 PUZZLE CLEAR 애니메이션과 똑같은 디자인과 애니메이션을 적용해 주고, 대신 두 애니메이션 모두 텍스트가 기울어지지는 않게 해줘" — 지난 라운드에서 만든 글자별 팝인 헬퍼(`typoLetters`, Alfa Slab One 폰트 + 방사형 글로우)를 `LineClearBanner`도 그대로 쓰도록 바꿨다(폰트 상수를 `PUZZLE_CLEAR_FONT`에서 배너 공용 이름 `CLEAR_TYPO_FONT`로 정리하고, 더는 안 쓰는 `CLEAR_FONT`(Kotta One)·`lineClearFade`/`lineWordInLeft`/`lineWordInRight` 키프레임은 죽은 코드로 남기지 않고 제거했다). 라인 하나 클리어는 퍼즐 전체 클리어보다 훨씬 자주 일어나는 가벼운 이벤트라 별 아이콘 없이 글자 크기만 줄이고(46→32px) 재생 시간도 짧게(4.2s→2.2s) 잡았다 — 그만큼 `PuzzleSolver`의 celebrate 자동 종료 타이머도 늘어난 길이에 맞춰 2.4s→2.7s로 늘려, 이전 라운드와 같은 이유(애니메이션이 끝나기도 전에 배너가 잘리는 문제)가 재발하지 않게 했다. "기울어지지 않게" 요청에는 `PuzzleClearBanner`가 단어 전체에 걸어 두던 정적 `rotate(-2.5deg)`/`rotate(2deg)`를 제거해 대응했다 — 글자가 등장하는 도중에만(`--tr` 커스텀 프로퍼티) 살짝 회전했다가 각 글자의 keyframe이 끝나는 시점엔 항상 `rotate(0deg)`로 되돌아오므로, 다 자리 잡은 뒤에는 어떤 글자도 기울어져 있지 않다.

② "'국면 변동성'은 '포지션 변동성'으로 용어를 수정하고, 더 빠르게와 더 정확하게 모두 설명으로 depth=n만 명시해 줘" — 설정 탭 리뷰 설정 카드와 관련 코드 주석 전체에서 "국면 변동성" 표기를 "포지션 변동성"으로 통일했다. 리뷰 속도 두 옵션의 부가 설명도 "기존과 동일한 속도"/"depth 25, 더 오래 걸려요" 같은 문장 대신 각각 `depth=20`(`REVIEW_DEPTH` 상수를 그대로 문자열에 꽂아 실제 값과 항상 일치하게 함)·`depth=25`로 값만 간결하게 표시한다.

③ "이번 세션에서 만든 정확도 체계를 /about 첫 페이지에 내 사고 과정을 그대로 시각화해서 보여줘, 구체적인 계산 수식과 그래프를 적극 활용해서" — `/about` 페이지(`AboutPage.jsx`의 `IntroPage`, ChessCom 연동 섹션 바로 다음)에 이번 세션 동안 실제로 설계했던 독립 정확도 체계를 4단계로 재구성한 `AccuracySystemSection`을 새로 만들어 넣었다. 이 페이지는 App.jsx와 별개 번들이라(파일 상단 T 팔레트 주석과 같은 이유) 공식(`winPctFromCp`·`newAccuracyFromAvgLoss`·`newCumulativeAccuracy`·`sharpLossMultiplier`)을 import하지 않고 그대로 복사해 두고, App.jsx 쪽 계수가 바뀌면 이 사본도 함께 맞춰야 한다는 주석을 남겼다. 네 단계 각각 직접 그린 SVG 그래프(범용 `AsLineChart` — 격자·축 눈금·강조 점을 갖춘 라인 차트, 외부 차트 라이브러리 없이 좌표를 직접 스케일링)로 시각화한다: ⓐ cp→win% 로지스틱 변환(S자 곡선, 0→50%·+300cp→75% 지점 강조), ⓑ 손실→정확도 지수 감쇠 곡선(0→100·손실 25%p→22.9점 지점 강조), ⓒ "손실을 먼저 평균 낸 뒤 변환"과 "각 수를 먼저 변환한 뒤 조화평균"의 차이를 옌센 부등식(convex 함수)으로 설명하고, 무난한 수 9개+블런더 1개짜리 손 계산 예시(막대그래프 `AsJensenBars`)로 전자는 86.7점, 후자는 74.8점이 나온다는 걸 직접 보여준다, ⓓ 포지션 날카로움(정규분포 누적분포함수로 0.85~1.15배 매핑)에 따라 같은 손실도 다르게 반영되는 곡선(8σ→×1.0·20σ→×1.13 지점 강조). 로컬 dev 서버로 `npm run build` 통과를 확인했고, Playwright로 (스크린샷 기반 스크롤 검증이 다중 스크롤 컨테이너 구조 때문에 신뢰할 수 없어서) DOM에서 네 단계 제목·SVG 텍스트 노드를 직접 읽어(`page.locator('svg text').allTextContents()`) 손 계산 값(0→50%, +300→75%, 0→100, 25→22.9, 86.7/74.8, 0.85/1/1.15, 8→×1.0, 20→×1.13)과 정확히 일치하는 것을 확인했다.

**기능 — 설정 탭 "리뷰 설정" 카드(리뷰 속도 선택 + 국면 변동성 보정 on/off)**
사용자 요청 세 가지를 하나의 카드 개편으로 반영했다. 실제 기보(위 재현성 수정 검증에 쓴 openchesskr vs GJScalzo 게임, chess.com 정확도 58.8/72.8)로 측정해 보니, 재현성 버그가 없는 상태에서도 이 체계의 정확도 차이가 이미 chess.com 자체 격차(14점)보다 컸다(약 26~30점) — 그래서 배율 상수를 또 조정하는 대신, 사용자가 원하는 만큼 조절할 수 있게 설정으로 노출하는 방향으로 바꿨다.

기존 "분석 엔진" 카드를 "리뷰 설정" 카드로 확장한다(엔진 선택은 그대로 최상단에 유지). ① **리뷰 속도** — "더 빠르게"(기존과 동일, `REVIEW_DEPTH=20`)와 "더 정확하게"(포지션당 depth 상한을 25로) 중 고를 수 있다. `ReviewPage`가 이 값을 prop으로 받아 `reviewDepth = reviewSpeed === "accurate" ? 25 : REVIEW_DEPTH`로 `analyzeGame`에 그대로 넘긴다 — movetime은 그대로 두었는데, depth 상한만 올려도 이미 예전엔 depth 20에서 일찍 멈추던 쉬운 포지션들이 남는 시간을 마저 써서 더 깊이 파고들게 되어(주석 참고) 그 자체로 "더 오래 걸리지만 더 정확한" 리뷰가 된다. ② **국면 변동성 보정 on/off** — 이전 라운드에서 추가한 "날카로움"(`sharpLossMultiplier`, 후보 수끼리 평가가 얼마나 팽팽했는지에 따라 그 수의 손실을 가중하는 chess.com식 보정)을 끌 수 있게 했다. 핵심 설계는 이 토글이 **재분석을 요구하지 않는다는** 것 — `sharpLossMultiplier(sharp, on)`·`newCumulativeAccuracy(losses, sharps, n, sharpOn)`·`reviewPhaseAccuracy(..., sharpOn)`·`buildRevealData(result, sharpOn)`에 모두 `sharpOn` 매개변수를 추가해, `analyzeGame`이 저장해 둔 원본 손실(`lossWinPct`)·날카로움(`sharp`) 값은 토글과 무관하게 항상 그대로 캐시되고, 최종 정확도로 변환하는 마지막 단계에서만 이 배율을 적용하거나(켜짐) 건너뛴다(꺼짐, 배율 1 고정). `ReviewPage`는 `result.whiteAcc`/`blackAcc`(분석 시점 값, 항상 보정 켜짐으로 계산됨)를 직접 표시하는 대신 `liveWhiteAcc`/`liveBlackAcc`(`useMemo`로 현재 `sharpOn` 값을 반영해 그 자리에서 재계산)를 모든 표시 지점(정확도 박스 2곳, `ReviewSummary`의 단계별 정확도, `ReviewAccuracyRevealAnim`의 진입 애니메이션 곡선)에 일관되게 사용해, 토글을 바꿔도 그 즉시(엔진 재호출 없이) 화면 전체가 같은 값으로 갱신된다.

리뷰 속도(depth)는 실제로 분석 결과 자체가 달라지므로, 캐시(위 재현성 수정에서 만든 로컬/Supabase 캐시)에 depth를 함께 저장해(`d` 필드) 지금 선택된 속도와 다른 depth로 캐시된 결과는 재사용하지 않고 새로 분석하도록 했다 — 변동성 보정 토글은 캐시된 원본 데이터를 그대로 재사용해도 되므로(변환 단계만 다름) 캐시 키에 영향을 주지 않는다.

로컬 dev 서버 + Playwright로 ① 리뷰 속도를 "더 정확하게"로 바꾸면 실제로 `reviewDepth=25`가 `analyzeGame`에 전달되고 정상적으로 완료되는 것, ② 국면 변동성 보정을 껐다 켰다 하면 같은 대국의 정확도(44.5/74.1 → 39.1/71.9)가 실제로 달라지는 것, ③ 토글만 바꿔 같은 대국을 다시 열었을 때 캐시를 그대로 재사용해(첫 분석 25.3초 → 토글 변경 후 재오픈 1.0초) 재분석 없이 즉시 새 값을 보여주는 것을 확인했다. UI(설정 탭의 새 "리뷰 설정" 카드 레이아웃, 라디오·토글 상태, 새로고침 후 두 설정 모두 유지되는 것)도 화면으로 확인했다.

**성능 — 포지션 평가 실패(타임아웃) 재시도 강화**
위 재현성 수정을 실제 대국(사용자가 제공한 PGN, chess.com 정확도 58.8/72.8)으로 검증하는 과정에서, 한 포지션이 엔진 타임아웃으로 실패하면 재시도를 1회만 하고도 실패하는 경우가 있어 그 수가 `pending`(정확도 집계 제외)으로 영구히 빠지는 걸 발견했다 — 표본(n)이 줄어들수록 조화평균이 남은 값에 더 민감해져, 이 탈락 자체가 최종 정확도를 왜곡시켰다(같은 대국을 여러 번 측정했을 때 pending이 적을수록 정확도가 더 높게 나오는 상관관계를 직접 확인). 실제로 계산 불가능한 포지션은 거의 없고 대부분 그 순간의 워커 부하 때문이므로, 재시도 횟수를 1회→2회로 늘렸다(그래도 실패하면 여전히 `pending`으로 남는다 — 존재하지 않는 값을 지어내진 않는다).

**버그 수정 — 같은 대국을 리뷰로 다시 열 때마다 정확도가 달라지던 비결정성 문제**
사용자 신고 — "같은 게임인데 리뷰를 다시 열 때마다 정확도 차이가 난다." `analyzeGame`은 리뷰 페이지를 열 때마다 처음부터 다시 돌았는데, 문제는 이 분석이 `REVIEW_MOVETIME_MS`(포지션당 엔진 탐색 "상한", 벽시계 시간 기준)를 쓴다는 점이었다 — 같은 포지션이라도 실행할 때마다 서버·기기 부하에 따라 실제로 도달하는 depth가 미세하게 달라질 수 있고, 후보 수끼리 평가가 팽팽한 포지션에서는 이 depth 차이가 최선수·평가치 자체를 바꿔(engine의 `posEval[i].best`가 실행마다 다른 SAN으로 나옴) 그 수의 등급(`kind`)·손실(`lossWinPct`)까지 바뀌었다 — 결국 같은 대국을 다시 분석할 때마다 정확도%가 미세하게(때로는 눈에 띄게) 흔들렸다.

이전엔 "재분석 결과를 캐시하지 않는다 — 분석 정확도는 절대 타협하지 않는다"는 명시적 원칙이 있었는데, 이 원칙은 "다시 계산하면 더 정확해진다"는 잘못된 전제 위에 있었다 — movetime 기반 분석을 다시 돌린다고 더 정확해지는 게 아니라, 그냥 결과가 무작위로 달라질 뿐이었다. 원칙을 "한 번 완결된(`resultDone`) 분석 결과는 그대로 보존해 항상 같은 값을 보여준다"로 바꿨다 — 캐시는 정확도를 낮추는 지름길이 아니라, 이미 낸 최선의 결과가 실행마다 흔들리지 않게 붙잡아 두는 것뿐이다. 두 단계 캐시를 뒀다: ① 이 기기(브라우저)의 `localStorage`에 완결된 `result`를 저장해(`reviewResultStorageKey` — `reviewStorageKey`와 같은 방식으로 대국을 식별) 같은 기기에서는 즉시(엔진 부팅조차 기다리지 않고) 재사용한다. ② chess.com 대국(`game.id` 있음)은 기존 `reviewed_games`(딥링크 캐시) 테이블에 새 `analysis` 컬럼을 추가해 함께 올려 둬, 다른 기기·다른 유저가 같은 대국을 열어도 항상 같은 값을 보게 했다(크라우드소싱 캐시 — 기존 `reviewedGameShare`/`Fetch`와 같은 패턴). 두 캐시 모두 `REVIEW_RESULT_CACHE_VERSION` 상수로 버전을 매겨 저장한다 — 앞으로 채점 로직(`gradeOne`)이나 정확도 공식(`NEW_ACC_*`)을 고칠 때마다 이 버전을 올려야, 이미 캐시된 옛 결과가 새 코드로 바뀐 뒤에도 그대로 남아 있는(공식을 고쳐도 예전 리뷰엔 반영 안 되는) 사고를 막는다.

로컬 dev 서버 + Playwright(개발 전용 `window.__debugOpenReview`/`window.__debugReviewResult` 임시 훅, 검증 후 완전히 제거)로 13수짜리 대국을 두 번 연달아 열어(같은 브라우저 컨텍스트, `localStorage` 캐시만 검증 — `game.id`가 없는 테스트 대국이라 Supabase 캐시는 건드리지 않음) 첫 번째는 약 7.4초(실제 분석), 두 번째는 0.5초 만에(캐시 즉시 반환) 완료되는 것과, 두 결과의 `JSON.stringify` 값이 완전히 동일한 것을 확인했다.

**버그 수정 — 체크메이트로 끝나는 대국의 마지막 수 아이콘이 영원히 "분석 중"에 멈추는 문제 / 프로필 카드 라벨 정리 / 퍼즐 클리어 애니메이션 폰트·연출 개편**
사용자 재피드백 세 가지.

① "리뷰 페이지에서 마지막 수의 수 체계 아이콘이 표시되지 않는다" — `gradeOne(i)`을 추적해 원인을 찾았다. 이 함수는 수 `i`를 채점하려면 원칙적으로 그 수를 두기 직전 포지션(`posEval[i]`)과 두고 난 뒤 포지션(`posEval[i+1]`) 둘 다 엔진 평가가 필요한데, 예외적으로 `matched`(둔 수가 그 포지션에서 엔진의 1순위 추천수와 SAN이 정확히 일치)일 때만 `posEval[i+1]`이 실패해도(`!posEval[i+1].ok`) 채점을 진행한다(엔진 자신의 최선수를 그대로 뒀다면 다음 포지션 평가는 이번 포지션 값의 부호만 뒤집으면 되기 때문). 문제는 대국의 마지막 수가 체크메이트면 "둔 뒤 포지션"은 합법수가 하나도 없는 종국이라 엔진이 원천적으로 평가를 낼 수 없다(`posEval[N].ok`가 영원히 `false`) — 그런데 실제로 둔 메이트 수가 그 포지션에서 엔진이 내부적으로 고른 특정 1순위 수(외통이 여러 갈래인 국면이면 SAN이 다를 수 있음)와 정확히 일치하지 않으면 `matched`도 `false`가 되어, 이 수는 `kind:"pending"`으로 등록된 채 다시 채점을 재시도할 경로가 전혀 없어(대기 중인 이후 포지션이 없으므로) 영구히 "분석 중" 아이콘에 멈춰 있었다. 체크메이트로 게임을 끝내는 수는 그 자체로 이 포지션에서 나올 수 있는 최선의 결과(즉시 승리)이므로, `matched` 판정에 `/#/.test(fullSans[i])`(둔 수의 SAN에 체크메이트 기호가 있는지)를 `||`로 더해 이런 수를 `matched`와 동일하게 취급하도록 한 줄만 고쳤다 — 이후 로직(손실 0, `best` 등급, "더 나은 수" 제안 생략)이 모두 `matched` 하나로 이미 자연스럽게 갈라지도록 짜여 있어 별도 분기 추가 없이 이 한 줄만으로 충분했다. 로컬 dev 서버 + Playwright(개발 전용 `window.__debugOpenReview`/`window.__debugReviewResult` 임시 훅으로 로그인 없이 체크메이트로 끝나는 세 실전 기보 — Fool's mate·Scholar's mate·Legal's mate —를 각각 새 페이지에서 열어 분석 완료까지 기다린 뒤, 검증 후 두 훅 모두 완전히 제거)로 세 대국 모두 마지막 수가 더 이상 `pending`에 멈추지 않고 실제 등급(`best`)으로 채점 완료되는 것을 확인했다.

② "프로필 카드의 '내 프로필' 텍스트에 '@아이디'를 대신 표시하고, 이름과 소개 사이의 아이디 표시는 삭제하고 여백을 늘릴 것" — 설정 탭 `MyProfileCard` 헤더의 고정 라벨 `"내 프로필"`을 `@{displayId}`(+ `roleIcon`)로 교체하고, 원래 닉네임과 소개(`bio`) 사이에 별도로 떠 있던 `@아이디` 줄(모노스페이스 폰트)은 완전히 제거했다 — 같은 정보(아이디)가 카드 안에 두 번 중복돼 있던 걸 헤더 라벨 하나로 합친 것이다. 그 줄이 빠지며 비게 된 이름-소개 사이 간격은 `bio` 블록의 `marginTop`을 1px→5px로 늘려 보정했다.

③ "퍼즐 클리어 애니메이션 폰트를 Alfa Slab One으로, 더 역동적인 타이포그래피 연출로, 재생 시간도 늘리고 끝난 뒤 창이 닫히기까지 지연도 추가" — `PuzzleClearBanner`("PUZZLE"·"CLEAR" 두 단어) 전용 폰트 상수 `PUZZLE_CLEAR_FONT`(Alfa Slab One, `index.html`의 Google Fonts 링크에 추가)를 새로 만들고(라인 클리어 배너의 `CLEAR_FONT`는 Kotta One 그대로 유지 — 이번 요청은 퍼즐 전체 클리어 배너 한정), 단어를 통째로 좌우에서 슬라이드시키던 기존 방식(`typoLetters` 도입 전) 대신 글자 하나하나가 제각기 다른 회전각(`--tr` CSS 커스텀 프로퍼티, 홀짝 인덱스로 부호를 번갈아 준다)으로 튕겨 들어와 자리를 잡는 `puzzleLetterPop` 키프레임 + `typoLetters` 헬퍼로 바꿔 포스터 문구 같은 느낌을 냈다("PUZZLE"은 -2.5°, "CLEAR"는 +2°로 단어 전체도 살짝 반대 방향으로 기울여 둠). "너무 짧게 재생되고 끝나버린다"는 지적의 실제 원인은 두 가지가 겹쳐 있었다 — 배너 자체의 `puzzleClearFade` 애니메이션(1.1초 지연 + 2.6초 재생 = 총 3.7초)보다, 같은 `celebrate` 상태를 라인 클리어와 공유해 이를 자동으로 꺼 버리는 `PuzzleSolver`의 타이머가 항상 2.4초로 고정돼 있어 글자가 다 나오기도 전에(글자 스태거 특성상 마지막 글자는 3초 안팎에 끝남) 배너 전체가 중간에 잘려 사라졌다. 배너 재생 시간을 2.6초→4.2초로 늘리고(지연 포함 총 5.3초), `celebrate` 자동 종료 타이머도 `fullyComplete`(퍼즐 전체 클리어, 곧 이 배너가 뜨는 경우)일 때만 6.2초로 늘려(라인 하나만 클리어한 경우의 2.4초는 그대로 — 그쪽 배너는 1.5초짜리라 문제 없었음) 배너가 다 재생되고도 "조금 뒤"(0.9초 여유) 닫히도록 했다. 별 3개 뒤에 은은한 방사형 글로우(`radial-gradient` 원)도 하나 더해 화면 전체가 더 극적으로 보이게 했다.

로컬 dev 서버로 `npm run build` 통과를 확인했고, ①은 위와 같이 Playwright로 직접 검증했다. ②·③은 실제 로그인(Supabase 인증)·퍼즐 풀이 흐름이 필요해 이 샌드박스에서 종단 간 재생 확인은 못 했고, 스타일·타이머 값·CSS 애니메이션 정의를 코드 리뷰로 재확인했다.

**기능 — 이론 수·최선의 수 무감점 유지 + 그 외 수 감점 확대(5~7%p 목표) / 정확도 팝업 크기·속도 재조정**
사용자 재피드백 두 가지.

① "이론 수와 최선 수는 정확도 손실이 없게 하고, 대신 다른 수는 손실을 조금 늘려 평균 5~7%p 정도 낮아지게" — `noPenalty`(best/only/brilliant/book) 판정 자체는 이미 그대로 두고(요청과 일치), `gradeOne`에서 `lossWinPct`를 계산하는 지점에 새 배율 상수 `NEW_ACC_PENALTY_MULT`를 곱해 그 외 모든 감점 대상 수(우수·좋음·부정확·실수·놓친 수·블런더)의 손실만 키웠다. 실제 배율을 정할 때 조화평균의 비선형성을 실측으로 확인했다 — 배율 1.35로 시작해 실제 기보(클린한 기보·블런더 있는 기보)에 적용해 보니, 손실이 거의 없는 클린한 쪽은 겨우 2~4%p만 낮아지는데 블런더가 있는 쪽은 24%p나 폭락했다(harmonic mean이 이미 낮은 값에 극도로 민감해, 그 값을 더 낮추면 역수 합이 걷잡을 수 없이 커짐). 배율을 1.1, 1.2로 낮춰 가며 같은 두 기보로 재측정해, 배율 1.2에서 실수가 거의 없는 쪽은 1%p 안팎(원래 감점 대상 수 자체가 적어 배율을 올려도 영향이 작음), 블런더가 있는 쪽은 7.2%p 낮아지는 것을 확인해 이 값으로 정했다 — "5~7%p"라는 요청을 정확히 균일하게 맞추는 건 조화평균 구조상 원천적으로 불가능하지만(실수가 많을수록 배율의 영향이 커지는 게 자연스러운 방향이기도 하다), 실수가 있는 일반적인 대국에서는 목표 범위에 들어오고 클린한 대국은 원래도 감점 여지가 적어 소폭만 낮아지는 균형점을 골랐다.

② "그래프 위 +n% 텍스트가 여전히 겹친다" — 텍스트 크기를 줄이고(그래프 위 `7.5`→`6`, 정확도 박스 옆 `12`→`10`) 사라지는 속도를 더 높였다(전체 표시 시간 `REVEAL_POPUP_MS` 850ms→550ms, 페이드아웃 `REVEAL_POPUP_EXIT_S` 0.15s→0.1s) — 직전 라운드에서 이미 한 차례 줄였던 값을 다시 한 번 줄인 것이다.

로컬 dev 서버 + Playwright로 두 기보(클린/블런더) 각각의 배율별(1.1/1.2/1.35) 최종 정확도를 실측해 위 결론을 얻었고, 최종 배율(1.2)로 팝업이 더 작고 빠르게 사라지는 것도 확인했다.

**UI — 최선 수 점·"1st" 라벨 제거 + 0.0 평가 점선 강화 + 정확도 그래프 눈금 가독성 개선**
사용자 재피드백 세 가지. ① 직전 라운드에서 넣은 최선 수 기준 평가(연두색 점 + "1st" 라벨)를 요청대로 `EvalGraph`에서 완전히 뺐다(`bestMoveSrc`/`bestMoveY` 계산과 관련 렌더링을 모두 제거 — 죽은 코드로 남기지 않음). ② 평가치 0.0(승률 50%)을 나타내는 정중앙 점선을 더 진하게(`#6B625A`→`T.brass` 계열, `strokeOpacity .65`, 두께 0.5→0.9) 바꾸고, "그래프 위에도 표시"되도록 렌더 순서를 고쳤다 — `EvalGraph`는 이 점선을 흰 영역 채우기(`polygon`)보다 먼저 그리고 있어서 백이 우세해 채우기가 그 높이까지 덮는 구간에서는 선이 가려져 안 보였다. 채우기 다음(위)에 다시 그리는 순서로 바꿔 항상 보이게 했다. 리뷰 진입 애니메이션(`ReviewAccuracyRevealAnim`)의 자체 그래프는 원래 이 점선을 "펜이 지나간 부분"(`tipX>0`)에만 국한해 그렸는데, 이제 검게 비워둔(아직 안 그려진) 구간까지 포함해 전체 폭(0..W)에 항상 그리고, 그 검은 배경 `rect` 및 흰 채우기 둘 다보다 나중에(위에) 그려 두 배경 어디서나 보이게 했다. ③ 백·흑 미니 정확도 그래프(`MiniAccCurve`)의 위·아래 경계값 숫자(예: `60`/`100`)가 너무 작고(5.5~7px) 흐려서(`rgba(...,.45)`) 잘 안 보인다는 지적 — 글자 크기를 7.5~9.5px로, 불투명도를 .45→.85로, 글자 두께도 `fontWeight:800`으로 키웠다.

로컬 dev 서버 + Playwright로 리뷰 진입 애니메이션이 아직 검은 미공개 구간일 때(진행률 0%)도 정중앙 점선이 전체 폭에 걸쳐 뚜렷이 보이는 것, 흰 채우기가 그 선을 지나간 뒤에도 계속 보이는 것, 실제 리뷰 화면의 `EvalGraph`에도 최선 수 점 없이 점선만 뚜렷하게 남아 있는 것, 미니 그래프 눈금 숫자가 커진 것을 확인했다.

**기능 — 정확도 증감 팝업을 정확도 박스에도 표시 + 잔상 속도·스태거 조정 / 확대 기능 제거 / 모바일 그래프 세로 배치 / 정확도 체계 재보정**
사용자 재피드백 네 가지를 반영했다.

① 정확도 증감 팝업(그래프 위에 잠깐 떴다 사라지는 `+n%`/`-n%`)을 그 진영의 정확도 박스(백/흑 숫자) 우하단에도 똑같이 띄운다 — `moveMeta`에 `white` 필드를 추가해 각 팝업이 어느 진영 것인지 구분하고, `ReviewAccuracyRevealAnim`의 `popups` 배열을 그래프 렌더와 정확도 박스 렌더 양쪽에서 `p.white`로 걸러 재사용한다(별도 상태 없이 같은 소스 데이터를 두 곳에 뿌리는 방식이라 항상 그래프의 팝업과 정확도 박스의 팝업이 정확히 같은 순간에 같은 값으로 나타났다 사라진다). "텍스트가 겹친다"는 지적에는 두 가지로 대응했다 — 페이드아웃 시간을 0.3초→0.15초로 줄이고 전체 표시 시간도 1300ms→850ms로 줄여(`REVEAL_POPUP_EXIT_S`/`REVEAL_POPUP_MS`) 잔상이 더 빨리 사라지게 했고, 델타가 큰 수 여러 개가 한 프레임 근처에서 동시에 드러나도 한꺼번에 다 띄우지 않도록 대기 큐(`queueRef`)를 두어 `REVEAL_POPUP_STAGGER_MS`(480ms) 간격으로 하나씩만 순서대로 띄우게(`drainQueue`) 바꿨다.

② 직전 라운드에서 추가한 "역삼각형 마커를 누르고 있으면 그래프가 확대되는" 기능을 요청대로 완전히 제거했다 — `EvalGraph`를 `pressing` 상태·줌 관련 계산(`EVAL_GRAPH_ZOOM_SCALE`, `zoomVbX0`, `sw()`, `displayMarkFrac`)이 생기기 이전 버전으로 되돌리고, 최선 수 점(연두색+"1st" 라벨)만 그대로 유지했다.

③ 모바일에서 백·흑 미니 정확도 그래프가 나란히 좁게 보이던 걸, `narrow`일 때는 세로로 쌓아(`flex-col`) 각각 한 줄 전체 폭을 다 쓰게 하고 `MiniAccCurve`에 새 `big` prop(그래프 높이 40→64px, 격자 숫자·선 굵기도 비례해 키움)을 넘겨 더 크게 보이게 했다. `ReviewAccuracyRevealAnim`이 `narrow`를 받도록 prop을 추가하고(`ReviewPage`가 이미 갖고 있던 `narrow`를 그대로 전달), 정확도 숫자 폰트도 24→30px로 키웠다.

④ "정확도가 너무 높게 나오고 백·흑 차이도 너무 안 난다"는 핵심 피드백 — 실제 원인을 수학적으로 짚었다. 기존 `newCumulativeAccuracy`는 "그 진영이 지금까지 둔 수들의 손실을 먼저 산술평균 낸 뒤, 그 평균 하나만 정확도 변환 곡선(아래로 볼록한 `exp` 감쇠)에 넣는" 순서였다. 이 곡선이 convex(아래로 볼록)라는 사실 때문에 옌센 부등식(Jensen's inequality)에 의해 "손실을 먼저 평균 → 한 번 변환"은 "각 수의 손실을 먼저 변환 → 그 정확도들을 평균"보다 항상 같거나 높게 나온다 — 즉 대국에 무난한 수(손실 0)가 많을수록 가끔 나는 블런더 한두 개가 평균에 옅게 희석돼 최종 정확도에 거의 반영되지 않았다(실측: 이론 수·최선 수가 많고 블런더가 1개뿐인 실전 기보로 재현해 보니 예전 방식으론 백·흑이 82.1/83.2로 거의 안 갈렸다). chess.com이 공개적으로 알려진 대로 쓰는 방식(각 수의 정확도를 먼저 구한 뒤 조화평균)과 같은 순서로 고쳤다 — ⓐ 수 하나하나의(날카로움 보정까지 반영한) 손실을 그 자리에서 바로 `newAccuracyFromAvgLoss`로 정확도 변환하고, ⓑ 그 정확도들을 조화평균(`n / Σ(1/acc_i)`, 0에 가까운 극단적 블런더에서 발산하지 않도록 `acc`에 0.5 하한을 둠)으로 누적한다. 조화평균은 낮은 값(블런더)에 훨씬 민감해(그 항의 역수가 급격히 커짐) 가끔 나는 블런더도 전체 정확도를 눈에 띄게 끌어내리면서도, "n으로 나누는" 구조 자체는 그대로 유지돼(조화평균도 결국 n이 커질수록 각 항의 상대적 비중이 줄어듦) "대국이 길어질수록 수 하나의 영향이 옅어진다"는 원래 설계 의도와 손실이 정확히 0인 수는 어떤 방식으로도 정확도 100을 유지한다는 불변식 둘 다 그대로 지켜졌다. 로컬 dev 서버 + Playwright로 직접 만든(수작업으로 합법성을 확인한, 18수 지나 흑이 나이트를 c3폰에 그대로 헌납하는 실제 블런더가 포함된) 기보를 리뷰해 보니 예전엔 거의 차이가 없던 두 진영 정확도가 85.1/77.2로 뚜렷하게 갈리는 것을, 그리고 블런더가 없는 클린한 기보에서는 여전히 양쪽 다 90중반대의 높은 정확도가 나오는 것(오탐 없음)을 확인했다.

**기능 — 평가치 그래프 확대 보기·최선 수 점 스타일 + 프로필 소개**
사용자 요청 세 가지를 반영. ① 역삼각형 마커를 누르고 있는 동안 그 지점 주변으로 그래프를 확대한다 — 실제 좌표계(`viewBox`)는 그대로 두고, 그래프 내용물을 담은 `<g>` 하나에만 CSS `transform: scale() translate()`(0.18초 `transition`)를 걸어 미는 방식(리뷰 진입 애니메이션 2차 개편 때 쓴 "카메라"와 같은 기법)이라 SVG viewBox 애니메이션 없이도 부드럽게 확대·축소된다. 확대 배율(`EVAL_GRAPH_ZOOM_SCALE=3`)만큼 좁아진 창을 `markX` 중심으로 잡되, 게임 시작·끝 근처에서는 창이 그려질 영역(0..W) 밖으로 나가지 않게 clamp한다("영역 내에서 확대" 요청) — 확대 배율만큼 굵어 보이는 선·점 크기는 `sw()` 헬퍼로 나눠 원래 두께로 되돌린다. 역삼각형 마커 자체는 SVG 밖의 HTML 오버레이라, 확대 중엔 같은 변환을 반영한 위치(`displayMarkFrac`)로 다시 계산해 SVG 내부 마크와 어긋나지 않게 맞춘다. ② 앞서(같은 버전 내 이전 항목) 리뷰 그래프로 옮겨온 최선 수 기준 평가(g) 흐릿한 점에 `T.good`(연두색, `#8FB55E`) 채우기와 "1st"(엔진 1순위 수 기준이라는 뜻) 작은 라벨을 추가했다 — 다른 텍스트 라벨들과 같은 `paintOrder="stroke"` 기법으로 그래프 배경 위에서도 잘 읽히게 했다.

③ "OpenChess 아이디에 간단한 소개를 남길 수 있게 해 달라"는 요청 — 프로필(`profiles.pub`, Supabase JSONB 컬럼이라 스키마 마이그레이션 없이 새 키만 추가하면 됐다)에 `bio`(최대 60자, `PROFILE_BIO_MAX_LEN`) 필드를 추가했다. `ProfileEditor`의 "이름" 입력 바로 아래에 글자 수 카운터와 함께 입력칸을 뒀고, 기존에 `profile`→`pub` 동기화를 담당하던 `publishProfile` 이펙트와 로그인 시 `pub`→`profile` 역방향 로드 양쪽에 `bio`를 추가해 다른 필드(닉네임 등)와 동일한 경로로 저장·복원되게 했다(별도 API 없음). 표시는 "닉네임 바로 밑"(요청대로) — 내 프로필 카드(`MyProfileCard`)·헤더 계정 드롭다운(`HeaderProfileMenu`)·유저 검색 결과 행(`userSearchRow`, 촘촘한 리더보드 `compact` 모드에서는 줄 수가 늘지 않도록 생략)·검색 결과를 눌러 들어가는 프로필 상세(`UserSearchModal`)·친구 프로필 상세(`FriendsModal`) 다섯 곳 모두, 닉네임 줄과 `@아이디` 줄 사이에 `bio`가 있을 때만 한 줄로 표시한다(빈 문자열이면 아무 것도 렌더링하지 않아 기존 계정 레이아웃에는 영향이 없다).

로컬 dev 서버 + Playwright(개발 전용 `window.__debugOpenReview` 임시 훅으로 로그인 없이 리뷰를 직접 열어 검증한 뒤 완전히 제거 — 이전 항목들과 같은 방식)로 마커를 누른 채 유지해 그래프가 확대되는 것, 대국 시작 지점 근처를 눌러도 확대 창이 영역 밖으로 넘치지 않고 왼쪽에 고정되는 것, 연두색 "1st" 점이 정상 위치에 뜨는 것을 확인했다. 프로필 소개는 실제 로그인(Supabase 인증)이 필요해 이 샌드박스에서 종단 간 테스트는 하지 못했고, 데이터 흐름(`profile.bio`→`publishProfile`→`pub.bio`→로드 시 복원)과 다섯 표시 지점의 조건부 렌더링을 코드 리뷰로 재확인했다.

**기능/성능 — 최선 수 흐릿한 점을 리뷰 그래프의 선택 지점으로 이동 + 역삼각형 마커 드래그 시 엔진 라인 먹통 수정**
사용자 요청 두 가지를 함께 반영. ① 리뷰 진입 애니메이션(`ReviewAccuracyRevealAnim`)이 모든 수마다 찍던 최선 수 기준 평가(g) 흐릿한 점을 완전히 뺐다 — 애니메이션이 끝난 뒤 실제로 보게 되는 리뷰 화면의 평가치 그래프(`EvalGraph`)에서, 역삼각형 마커로 지금 선택한 지점(`curPly`)에만 그 점이 뜨도록 옮겼다. `EvalGraph`는 원래 `moves[i].lossWinPct`(그 수의 손실, 독립 정확도 체계에서 이미 계산해 둔 값)를 갖고 있지 않아도 되는 컴포넌트였는데, `curPly>0`이면 `moves[curPly-1]`(지금 마커가 가리키는 위치로 이어진 수)의 `lossWinPct`·`white`를 이용해 `g = evalWin[curPly] + (백이면 +, 흑이면 -) × lossWinPct`를 그 자리에서 계산해, 기존의 실제 평가 마커(진한 원)보다 살짝 흐린 점(`rgba(235,221,196,.6)`, 반지름 2.6)으로 같은 x좌표에 겹쳐 그린다 — 최선 수를 그대로 뒀다면(손실 0) 두 점이 정확히 겹쳐 하나로 보이고, 실수·블런더처럼 손실이 있으면 두 점이 갈라져 "실제로 둔 수가 최선수보다 얼마나 아래(나쁜 쪽)에 있었는지"가 한눈에 보인다. 로컬 dev 서버 + Playwright로 마커를 이론 수 지점(손실 0, 점 하나로 겹침)과 블런더 지점(Kg7, 두 점이 뚜렷이 갈라짐 — 코치 카드도 "Kg7은(는) 실수예요"로 일치) 양쪽에 각각 클릭해 실제로 확인했다.

② "역삼각형 마커를 좌우로 빠르게 움직이면 계산량이 쌓여 엔진 라인이 잠시 먹통이 된다"는 신고 — `EvalGraph`를 드래그하면 `onJump`가 `pointermove`마다(빠른 드래그면 초당 수십 번) `curPly`를 바꾸고, 그 값에서 파생되는 `effSans`/`effFen`이 바뀔 때마다 엔진 라인 패널의 `useEffect`가 매번 새로 엔진 워커에 이전 탐색 중단(`stop`)+새 탐색 시작(`position`+`go`) 명령을 쌓아 보내던 게 원인이었다. 보드·마커·코치 카드처럼 즉시 반응해야 하는 나머지 화면은 그대로 두고, 이 효과 하나만 자기만의 디바운스된 스냅샷(`debouncedEff = { sans, fen }`, 220ms `setTimeout`, 새 값이 들어오면 이전 타이머를 취소하고 다시 잰다)을 보게 바꿨다 — `effSans`/`effFen`/`board` 등 컴포넌트 나머지는 전혀 건드리지 않아 드래그 중 화면(마커 위치, 방금 추가한 g 점, 보드, 코치 카드)은 여전히 매 프레임 즉시 따라오고, 실제로 워커에 요청을 보내는 이 한 곳만 "마커가 한 지점에 220ms 이상 머무른 뒤"에야 시작한다. Playwright로 그래프 위에서 포인터를 누른 채 왕복 80스텝을 빠르게 스윕한 뒤 마우스를 떼 봤는데, 드래그 도중 콘솔 에러 없이 매끄러웠고 뗀 지점에서 곧바로(디바운스 뒤) 정상적으로 엔진 라인 3줄이 채워지는 것을 확인했다.

**성능 — 게임 리뷰 분석 정확도 개선(포지션당 시간 상한 1.5배)**
사용자 요청 — 리뷰 시간을 늘려 분석 정확도를 개선하는 방향으로 진행. `REVIEW_MOVETIME_MS`(포지션당 엔진 탐색 시간 상한)를 1000ms→1500ms로 올렸다. 이 값은 어디까지나 "상한"이라(주석 참고) 후보 수가 적은 대부분의(쉬운) 포지션은 이미 그 전에 depth 20에서 먼저 끝나 이 상한 자체를 건드리지 않는다 — 실제로 이 상한에 걸리는 건 후보 수가 많은 복잡한 중반전처럼 그 시간 안에 depth 20을 못 찍는 몇몇 어려운 포지션뿐이라, 이 상한을 올리면 정확히 그런(정확도가 가장 아쉬웠던) 포지션들만 더 깊이 봐서 정확도를 끌어올리고, 쉬운 포지션은 그대로라 리뷰 전체가 균일하게 느려지지는 않는다. 대신 `analyzeGame`의 전체 시간 예산(`wallBudgetMs = ((N+1)/워커수) × movetime × REVIEW_BUDGET_FACTOR`)이 이 값에 정비례해, 사용자 요청대로 리뷰 전체 소요 시간도 약 1.5배로 늘어난다(포지션당 상한의 배수인 `REVIEW_PER_POS_CAP_MULT`/`FLOOR_MULT`도 함께 비례해 늘어남). `REVIEW_MOVETIME_MS`는 게임 리뷰뿐 아니라 퍼즐 트리 평가·도감 오프닝 노드 평가 등 여러 곳에서 "같은 포지션은 어디서 봐도 같은 값이 나오게" 의도적으로 공유하는 상수라(주석 참고), 게임 리뷰만 따로 떼어 값을 올리지 않고 이 공유 상수 자체를 올려 그 일관성을 그대로 유지했다.

**기능 — 리뷰 페이지 진입 애니메이션 전면 개편**
사용자가 손으로 그린 시퀀스(moveEvalGraph·bestMoveEvalGraph·구간 정확도 그래프)를 그대로 구현했다. 일러스트(`REVIEW_LOADING_PANELS` 세 장을 카드 세 칸에 겹쳐 보여주던 `ReviewLoadingSplit`)를 `ReviewIntroCarousel`로 교체 — 카드 테두리·배경 없이(투명 배경) 한 번에 한 장만 `objectFit:contain`으로 전체를 보여주고, `AnimatePresence`로 4.2초마다 좌→우 슬라이드하며 자연스럽게 넘어간다. 분석(`analyzeGame`)이 100% 끝나면(`resultDone`) 새 `ReviewAccuracyRevealAnim`이 재생된다 — ① `moveEvalGraph`(f, 실제 평가 승률% 곡선)를 SVG `<path>`의 `getTotalLength()`+`stroke-dashoffset` CSS 전환으로 왼쪽부터 그리고, 그 아래 4개의 x좌표 눈금(ply 0/⅓/⅔/전체)을 표시한다. ② f가 다 그려지면 `bestMoveEvalGraph`(g, 최선수 기준 평가)를 점선으로 덧그린다 — g는 새로 계산하지 않고 이미 갖고 있는 데이터에서 정확히 유도한다: `g[k] = f[k] + (그 수를 둔 진영이 백이면 +, 흑이면 -) × 그 수의 손실(lossWinPct)`(흑의 손실은 이 승률% 모델에서 백 관점으로 정확히 그만큼의 이득으로 보이므로 부호만 바꾸면 된다 — 별도 근사가 아니라 독립 정확도 체계의 손실값과 100% 일치하는 수치). ③ 수 구간(ply k-1→k)마다 f·g 사이의 넓이를 사각형(정확히는 사다리꼴에 가까운 사각형) `<polygon>`으로 그려 두고, 정해진 속도(총 3.2초를 수 개수로 나눠 26~220ms 간격, 게임 길이와 무관하게 총 재생 시간이 너무 늘어나거나 순식간에 끝나지 않게 clamp)로 한 구간씩 opacity를 0→1로 공개해 "그 수 구간의 정적분(=손실)을 넓이로 보여준다"는 요청을 그대로 구현했다. ④ 이 스윕이 지나가는 것과 동시에, 그 구간이 백 수/흑 수인지에 따라 `MiniAccCurve`(백·흑 각각의 구간 누적 정확도 h(n) 스파크라인)가 한 점씩 자라나고, 그 아래 숫자(`motion.div layoutId="review-acc-w"`/`"review-acc-b"`)도 함께 갱신된다. ⑤ 스윕이 끝나면 잠깐(0.9초) 멈췄다가 다음 화면(narrow는 `ReviewSummary`, 데스크톱은 코치+보드 레이아웃)으로 넘어가는데, `ReviewAccuracyPill`에도 같은 `layoutId`를 달아 둬 framer-motion이 이 애니메이션의 마지막 숫자에서 그 다음 화면의 정확도 박스로 자리를 이어받는 전환을 자동 재생한다("자연스럽게 이동하여 합쳐지는" 요청 부분).

설계 과정에서 명시적으로 확인·결정한 것들: (1) 이 진입 애니메이션은 narrow(모바일)뿐 아니라 데스크톱에도 동일하게 적용된다 — 예전엔 데스크톱이 `resultDone`을 기다리지 않고 첫 수 채점 즉시 보드를 바로 보여줬지만, 이 애니메이션 자체가 완결된 전체 데이터를 전제로 하므로 이제 데스크톱도 분석이 100% 끝날 때까지 기다린 뒤 이 시퀀스를 한 번 보고 넘어간다(리뷰 정확도 우선 원칙의 연장). (2) 새로고침 이어보기(v0.3.8 앞 항목)와 결합 — `reviewStorageKey`에 저장하는 값에 `introSeen` 플래그를 추가해, 이미 이 애니메이션을 본 적 있는 리뷰(=저장된 위치가 있는 리뷰)를 새로고침해도 다시 재생하지 않고 곧장 이어보기 화면으로 간다. (3) 애니메이션 시퀀스 자체는 이미 계산이 끝난 데이터를 정해진 속도로 "공개"만 할 뿐이라, 분석 결과·정확도 숫자를 전혀 새로 계산하거나 근사하지 않는다(캐시·트레이드오프 없음 원칙 유지).

로컬 dev 서버 + Playwright(headless Chromium)로 실제 학습 탭에서 오프닝을 두어 수 진행한 뒤 리뷰로 들어가 전체 시퀀스(캐러셀 → 그래프 드로잉 → g 덧그리기 → 구간 공개 → ReviewSummary로 전환, 정확도 숫자 일치)가 콘솔 에러 없이 재생되는 것을 확인했다.

**기능 — 리뷰 진입 애니메이션 2차 개편: 분석 도중 실시간 재생 + 확대 추적 카메라**
사용자 피드백 반영. 위 애니메이션은 처음엔 `resultDone`(분석 100% 완료) 이후에만 재생하는, 정해진 길이(총 3.2초를 수 개수로 나눈)의 "리플레이"였다 — 이미 다 계산된 결과를 그럴듯한 속도로 다시 보여주는 방식이었던 셈이다. 사용자 요청대로 이제는 분석이 진행되는 도중에 실시간으로 재생된다 — 타이머를 없애고, `sweepIdx`를 `analyzeGame`이 `onMove`로 흘려보내는 `result.moves.length`(지금까지 실제로 채점된 수 개수)에 직접 연결했다. `moveEvalGraph`(f)·`bestMoveEvalGraph`(g)는 항상 "지금까지 채점된 지점까지만" 그려지고(`evalWin.slice(0, sweepIdx+1)`), 수 구간별 넓이 폴리곤도 그 수가 실제로 채점되는 순간 즉시 나타난다 — 애니메이션의 재생 속도 자체가 실제 엔진 분석 속도와 정확히 같아, 어떤 의미의 근사·재생도 없다(분석이 끝난 뒤에는 `REVEAL_HOLD_MS`만큼 완성된 모습을 잠깐 보여주고 다음 화면으로 넘어간다).

"그래프가 잘 안 보인다"는 지적에는, 전체 좌표계(0..N)는 그대로 두고 그 위에 얹은 `<g>` 하나에 CSS `transform: translate(...) scale(...)`(0.45초 `transition`)만 걸어 지금 채점 중인 지점(`sweepIdx`) 근방 `REVEAL_WINDOW_INTERVALS`(6)개 구간만 화면 가득 확대해서 보여주는 "카메라"로 대응했다 — 별도의 확대 로직이나 좌표 재계산 없이, 이미 있는 좌표계를 CSS 변환 하나로 밀고 당길 뿐이다. 지금 채점 중인 지점이 항상 화면의 62%(`REVEAL_ANCHOR_FRAC`) 지점에 오도록 `translateX`를 계산해, 방금 채점된 구간(왼쪽, 컨텍스트)과 다음에 채점될 자리(오른쪽, 여백)가 함께 보이면서 자연스럽게 오른쪽으로 흘러가는 느낌을 낸다. `scale`만큼 선이 가늘어 보이는 부작용은 `strokeWidth`를 전부 `/scale`로 나눠 보정했다. 확대된 화면만 있으면 전체 진행 상황(지금이 게임의 어디쯤인지)을 놓치기 쉬워, 그 아래에 축소된 전체 f 곡선과 지금 확대해서 보고 있는 구간을 반투명 사각형으로 표시하는 `RevealOverviewStrip`(개요 띠)을 새로 추가하고, x좌표 눈금 4개도 (계속 움직이는 확대 화면 대신) 여기로 옮겼다. "투명 배경을 사용해 달라"는 요청에 맞춰 그래프·미니 정확도 그래프의 카드 배경(`#3B342E` 사각형)도 모두 제거했다 — 이제 그래프가 일러스트 캐러셀과 마찬가지로 페이지 배경 위에 직접 떠 있다. 그래프의 "still working" 역할을 이제 살아있는 그래프 자체가 맡게 돼, 기존에 함께 쓰던 장식용 기물 바운스 애니메이션(`AnalyzingPiecesAnim`)은 중복이라 제거했다(다른 화면에서 참조가 없어 완전히 제거 — 죽은 코드로 남기지 않음).

**기능 — 리뷰 진입 애니메이션 3차 개편: 그래프를 리뷰 페이지의 평가치 변동 그래프(EvalGraph)와 완전히 같은 모양으로**
사용자 피드백 — "리뷰페이지에 있는 평가치 변동 그래프와 똑같은 형태의 그래프를 애니메이션으로 그리길 원한다." 2차 개편에서 만든 확대 추적 카메라·`bestMoveEvalGraph`(g) 점선 오버레이·수 구간 넓이 폴리곤·`RevealOverviewStrip`(개요 띠)은 실제 리뷰 화면의 그래프(`EvalGraph`)와는 다른 모양이었다 — 그래서 이 커스텀 SVG를 전부 걷어내고, `EvalGraph` 컴포넌트를 그대로 재사용하도록 바꿨다. `EvalGraph`는 `evalWin`(백 관점 승률% 배열)·`moves`만 받으면 진한 카드 배경·채워진 영역·등급별 점까지 리뷰 화면과 완전히 동일하게 그린다 — "애니메이션으로 그려 달라"는 부분은 그 값 자체가 실시간으로 자라나는 `result.evalWin`/`result.moves`(analyzeGame이 `onMove`로 흘려보내는 값, `evalWin`은 항상 게임 전체 길이로 채워져 있고 아직 채점 안 된 뒤쪽은 마지막 값으로 평평하게 유지됨, `moves`는 채점되는 대로 하나씩 늘어남)를 그대로 넘기기만 해서 저절로 해결된다 — 매 렌더 `<EvalGraph evalWin={result.evalWin} moves={result.moves} />`를 호출할 뿐, 별도의 좌표 계산이나 타이머는 전혀 없다. `buildRevealData`도 이제 백·흑 구간 누적 정확도 곡선(`wCurve`/`bCurve`, 아래 정확도 박스로 이어지는 미니 스파크라인용) 계산만 남기고 `g` 배열·`RevealOverviewStrip`·확대 카메라 관련 상수(`REVEAL_WINDOW_INTERVALS`/`REVEAL_ANCHOR_FRAC`)는 모두 삭제했다(죽은 코드로 남기지 않음). "투명 배경" 요청은 `EvalGraph` 자체의 카드 배경(진한 `#3B342E`)이 리뷰 화면 그 자체의 모양이라 이번엔 그대로 뒀다 — 일러스트 캐러셀 등 그래프가 아닌 나머지 요소들은 여전히 투명하다.

**기능 — 리뷰 진입 애니메이션 4차 개편: 검은 여백/흰색 채우기·일정 속도·최선수 점·정확도 증감 팝업**
사용자 요청 4가지를 한 번에 반영: ① 아직 채점되지 않은 구간은 값을 추측해 잇지 않고 그냥 검은 여백으로 표시, ② 펜(그려지는 지점)이 지나간 부분만 아래쪽을 흰색으로 채움, ③ 너무 한꺼번에 확 그려지지 않게 일정한 속도로 채워짐, ④ 각 수의 x좌표에 최선 수 평가를 흐릿한 점으로 찍고, 그 점과 실제 그래프 선 사이의 간격이 그 수 하나가 정확도를 얼마나 올렸는지/내렸는지로 수치화돼 초록/빨강 글씨로 잠깐 떴다 사라짐. 3차 개편에서 재사용했던 `EvalGraph`는 "이미 있는 값을 그대로 넘기는" 정적 컴포넌트라(패딩된 뒷부분이 평평한 값으로 즉시 다 보임) 이 요구사항들을 못 담아, 같은 배색·비율(320×92, `EvalGraph`와 동일)만 유지한 채 이 컴포넌트 전용 SVG를 새로 직접 그리는 방식으로 다시 바꿨다.

핵심은 "그려지는 지점"(pen)을 실제 분석 진행(`result.moves.length`)과 분리한 것이다 — `requestAnimationFrame` 루프가 `REVEAL_SPEED_PER_SEC`(4수/초)의 일정한 속도로만 전진하는 `progress` 상태를 관리하고, 이 값은 그 순간 실제로 채점된 지점(`target = min(moves.length, N)`)을 앞지르지 못하게 클램프된다 — 그래서 포지션 여러 개가 한꺼번에 몰려 끝나도 화면은 항상 정해진 속도로만 채워지고 절대 순간이동하듯 확 그려지지 않는다(요청 ③). 매 프레임 `floored=⌊progress⌋`까지는 확정된 실제 평가값을, 그다음 한 칸은 다음 값으로 선형보간해 매끄럽게 움직이는 펜 끝(tip)을 만든다. 렌더링은 `tip.x`를 경계로 완전히 나뉜다 — 그 오른쪽은 값을 전혀 그리지 않고 단색 검정(`#0A0604`) `<rect>` 하나로 채우고(요청 ①), 왼쪽만 기존 `EvalGraph`와 같은 방식(`polygon`으로 흰색 영역 채움 + 회색 선)으로 그린다(요청 ②).

각 수(`moveMeta[i]`)는 `ply`(x좌표)·`gVal`(최선수 기준 평가, 독립 정확도 체계에서 이미 쓰던 `f(ply)+그 수를 둔 진영 부호×손실` 공식 그대로 재사용)·`delta`(그 수 하나가 그 진영의 누적 정확도 곡선 `wCurve`/`bCurve`를 얼마나 움직였는지, `newCumulativeAccuracy` 호출 전후 차이)를 미리 계산해 둔다. 펜이 그 수의 x좌표를 지나가는 순간(`floored`가 그 인덱스를 넘어서는 프레임) `gVal` 위치에 흐릿한 점(`opacity .55`)을 띄우고(요청 ④ 앞부분), 동시에 `delta`를 `+0.3`/`-1.8`처럼 부호와 함께 초록(`T.good`)·빨강(`T.blunder`) `motion.text`로 그 자리에 띄웠다가 1.3초 뒤 `AnimatePresence`로 페이드아웃한다(요청 ④ 뒷부분) — 손실이 0인(최선수를 그대로 둔) 수도 대국이 길어질수록 평균이 희석돼 누적 정확도가 미세하게 올라가므로 옅은 초록 "+0.0"이 뜨는 것이 의도한 동작이다(독립 정확도 체계의 "구간이 길어질수록 한 수의 영향이 자연히 줄어든다" 설계와 정확히 같은 이유). `resultDone`이 되어도 시각적 펜(`progress`)이 아직 `N`까지 못 왔으면(그리는 속도가 분석 속도보다 느린, 짧은 대국에서 흔한 경우) 다음 화면으로 넘어가지 않고 그림이 다 그려지기를 기다린 뒤에야(`resultDone && progress>=N`) 넘어간다 — 분석은 순식간에 끝나도 시청자가 항상 전체 그림을 다 보게 된다. 로컬 dev 서버 + Playwright로 처음(전체 검정)부터 끝(전체 흰색, 정확도 증감 팝업 노출, ReviewSummary 전환)까지 콘솔 에러 없이 재생되는 것을 확인했다.

**버그 수정 — 리뷰 진입 애니메이션 그래프가 "멈춰 있다가 한꺼번에 그려지는" 문제의 진짜 원인**
사용자 재피드백 — 4차 개편(펜 속도를 `REVEAL_SPEED_PER_SEC`로 고정 클램프)을 거친 뒤에도 "여전히 그래프가 한꺼번에 그려지는 문제가 있다"는 신고가 이어졌다. 코드만 읽어서는 펜의 전진 속도 계산 자체는 이미 옳았기 때문에(직접 재검산해도 문제없음), `window.__debugOpenReview`(개발 전용 임시 훅 — `openReview`를 그대로 노출해 로그인·chess.com 연동 없이도 임의의 수순으로 리뷰를 열 수 있게 함, 검증 뒤 완전히 제거)를 심고 로컬 dev 서버 + Playwright로 40수짜리 대국 리뷰를 실제로 열어 1초 간격으로 `분석 중 N%`(`gradedCount/sans.length`) 값을 찍어보니, 실제로 어떤 수는 채점 진행이 8~10초씩 완전히 멈췄다가 한 프레임 사이에 25%→85%처럼 훌쩍 뛰는 게 확인됐다 — 펜의 속도 제한(4차 개편)은 정상 동작했지만, 애초에 "그릴 수 있는 지점"(`target = moves.length`) 자체가 계단식으로 뛰다 보니 화면은 오래 멈춰 있다가 몰아 그리는 것처럼 보였던 것이다.

원인을 좁히기 위해 `analyzeGame`의 `evaluateMulti` 호출마다 실제 소요 시간을 임시로 로깅해 보니, 40포지션 중 1~2개가 정확히 `movetime+4000ms`(하드 워치독 상한)에 걸려 `ok=false`로 실패하고, 재시도까지 또 같은 시간만큼 걸려 실패하는 게 보였다 — 포지션 하나가 최대 약 10초를 통째로 날린 것이다. 채점(`gradeIdx`)은 `gradeBoard`·직전 두 수(`moves[i-1]`/`moves[i-2]`)에 의존해 반드시 순서대로만 진행되므로, 이 포지션 하나가 막혀 있는 동안 다른 워커들은 work-stealing으로 훨씬 앞선 포지션들까지 이미 다 계산해 놓고 대기하고 있었고, 막힌 자리가 풀리는 순간 그동안 밀려 있던 결과가 한꺼번에 통과한 것이다.

처음엔 "MultiPV=3을 요구했는데 그 포지션의 실제 합법수가 3개 미만이면(외통 직전 외길 응수 등) 엔진이 3순위 줄을 영원히 못 내놓아, `pump()`의 소프트 스톱이 movetime을 다 채우고도 계속 기다리다 하드 워치독까지 간다"는 가설을 세우고, 이미 존재하던 `countLegalMoves`(합법수 개수 판정, v0.3.6에서 학습 탭 UI에 쓰던 함수) 헬퍼를 재사용해 `analyzeGame`이 각 포지션의 실제 합법수(`legalCounts[i]`, 원래 FEN을 만들던 순회 한 번에 편승해 함께 계산 — 추가 순회 없음)를 미리 세어 두고, `evaluateMulti`에 `3` 대신 `Math.min(3, legalCounts[i])`를 요구하도록 고쳤다(엔진이 절대 내놓을 수 없는 순위를 기다리지 않게 됨 — 탐색 depth·movetime은 전혀 건드리지 않아 정확도에는 영향 없음). 이 자체는 실전에서 충분히 발생할 수 있는(체크 회피 외길, 유일한 되잡기 등) 상황을 막는 정당한 수정이라 그대로 남겼지만, 다시 측정해 보니 정작 재현되던 멈춤 두 자리는 합법수가 24~37개로 넉넉해 이 가설과는 무관했다.

로그를 다시 보니 두 정지 지점 모두 `withTimeout`이 정확히 상한값(`movetime+4000ms`)에서 끊기며 `ok=false`로 확정되고 있었다 — 즉 엔진(또는 워커 통신)이 그 포지션에서 드물게(약 40포지션에 1~2개꼴, 이 샌드박스 환경 한정일 가능성도 있음) 실제로 멈춰버린 것으로, 정상적으로 끝나는 나머지 포지션들은 movetime 근처(±수십ms)에서 항상 정확히 끝나고 있었다. 이 4000ms 여유값은 "엔진이 완전히 멈췄다"고 최종 확정하기 전 마지막으로 기다려 주는 순수 안전망일 뿐이라, 정상 케이스에는 전혀 영향을 주지 않으면서 이 값만 줄이면 이런 드문 정지의 최대 길이를 직접 줄일 수 있다고 보고, `bootAnalysisWorker`(여러 화면이 공유하는 분석 워커 풀의 엔진 통신 계층)의 하드 워치독에 `hardBuffer`라는 선택적 필드를 새로 열었다(`evaluateMulti(fen, d, multipv, mt, onLines, slot, hardBuffer)` — 생략하면 기존과 똑같이 4000ms, 학습 탭·퍼즐 등 다른 모든 호출부는 아무 인자도 추가하지 않아 전혀 영향받지 않는다). 게임 리뷰 호출부만 `hardBuffer=1500`을 넘겨 이 안전망을 1500ms로 좁혔다(바깥쪽 `withTimeout`의 상한도 안쪽 워치독과 정확히 맞춰, 둘이 어긋나 워커 큐에 유령 작업이 쌓이는 일이 없게 했다). 같은 재현 스크립트로 다시 측정하니 40수짜리 대국의 그래프 완성까지 걸리는 시간이 27~45초에서 18~22초로 줄고, 한 번에 뛰는 최대 폭도 60%p대에서 30%p대로 줄어 훨씬 고르게 채워지는 것을 여러 차례 재현해 확인했다. `REVIEW_PER_POS_CAP_MULT`(포지션 하나가 예산에서 가져갈 수 있는 시간 상한)를 4배→2배로도 낮춰 봤지만 이 정지 패턴에는 눈에 띄는 개선이 없어(진짜 원인이 아니었음을 재확인) 되돌리고 원래의 정확도 우선 값(4배)을 그대로 유지했다. 검증을 마친 뒤 `window.__debugOpenReview` 임시 훅은 완전히 제거했다(로그인 없이 리뷰를 열 수 있는 경로를 프로덕션에 남기지 않기 위해) — 이 훅 제거 자체를 다시 Playwright로 재검증(리뷰 열기 → 닫기 → 재열기)해 콘솔 에러가 없는 것도 확인했다.

**버그 수정 — 리뷰 페이지를 닫았다가 다시 열면 사이트가 먹통이 되던 크래시**
증상 재현: 리뷰를 한 번 끝까지 봐서(정확도 공개 애니메이션까지 재생 완료) `introRevealDone`이 sessionStorage에 `introSeen:true`로 저장된 상태에서, 리뷰를 닫고 같은 대국의 리뷰를 다시 열면 화면이 그대로 멈췄다 — 실제로는 리액트 트리 전체가 죽어 있었다(에러 바운더리가 없어 사용자에게는 그냥 "먹통"으로 보임). 원인: `ReviewPage`가 로딩·정확도 공개 화면으로 보낼지를 예전엔 `if (!resultDone || !result) return (로딩)`과 `if (!introRevealDone) return (공개 애니메이션)` 두 단계 게이트로 나눠 걸었는데, 4차 개편을 거치며 이 둘을 `if (!introRevealDone) return (...)` 하나로 합치면서 `!resultDone || !result` 검사가 통째로 빠졌다. `introRevealDone`은 `useState(() => !!(savedPos && savedPos.introSeen))`로 마운트 시 즉시 sessionStorage 값으로 seed되므로, "이미 본 적 있는 리뷰"를 다시 열면 새 `ReviewPage` 인스턴스가 마운트되자마자(analyzeGame이 아직 시작도 하기 전, `result`가 여전히 `null`인 시점에) `introRevealDone=true`로 시작해 이 게이트를 그대로 통과, `narrow` 분기의 `<ReviewSummary result={null} .../>`가 렌더되며 `result.moves`를 읽다가 `TypeError: Cannot read properties of null`로 죽었다(Playwright로 재현해 정확한 스택트레이스 확보). 분석 자체는(정확도를 위해) 캐시하지 않고 매번 새로 기다려야 하므로, 게이트를 `if (!introRevealDone || !resultDone || !result) return (...)`로 되돌려 데이터 준비 여부를 다시 독립적으로 검사하게 했다 — 다만 "이미 본 적 있는 리뷰는 애니메이션을 다시 재생하지 않는다"는 원래 의도를 지키기 위해, 이 마운트가 애초에 `introSeen=true`로 시작했는지를 스냅샷으로 고정해 두는 `introRevealSeededRef`를 추가하고 이를 `ReviewAccuracyRevealAnim`의 새 `instant` prop으로 넘긴다 — `instant`면 시각적 펜이 끝까지 다 그려지길 기다리지도, `REVEAL_HOLD_MS`만큼 붙잡아 두지도 않고 `resultDone`이 되는 즉시 다음 화면으로 넘어간다(대기 자체는 여전히 하되, 그 대기 화면에서 애니메이션을 억지로 완주시키지는 않음). Playwright로 "리뷰 열기 → 닫기 → 다시 열기"를 반복해 더 이상 크래시가 나지 않는 것을 확인했다.

**UI — 정확도 증감 팝업 다듬기(% 표기, 사소한 변동 숨김) + 미니 정확도 그래프 동적 격자**
사용자 피드백 — "숫자가 너무 많이 겹쳐서 뜬다." 팝업 텍스트에 `%` 단위를 붙이고(`+0.3` → `+0.3%`), 절댓값 0.5%p 미만인 변동은 아예 띄우지 않도록 걸렀다 — 이론 수·무난한 수 대부분이 여기 해당해(손실 0인 수도 희석 효과로 항상 미세한 `+`가 뜸) 실제로 거의 매 수마다 겹쳐 떴던 문제였다. 백·흑 미니 정확도 그래프(`MiniAccCurve`)의 세로축은 기본 60~100 구간으로 확대해 그 구간 안에서의 작은 변화도 잘 보이게 하고, 마지막으로 공개된 값이 60 밑으로 떨어지면 그 순간만 0~100 전체 구간으로 다시 잡아(다시 60 이상으로 오르면 60~100으로 되돌아감) 낮은 정확도도 화면 밖으로 잘리지 않게 했다 — 위·아래 경계값에 점선 격자선과 작은 숫자 라벨(`60`/`100` 또는 `0`/`100`)을 함께 그려 지금 어느 구간으로 보고 있는지 알아볼 수 있게 했다.

**기능 — 게임 리뷰 정확도 체계를 독립 모델로 완전히 교체**
사용자가 손으로 유도한 수식(사진 전달)을 그대로 구현했다 — 기존 chess.com 공개 근사 공식(수 하나의 승률 손실을 exp 감쇠로 변환한 뒤 조화평균)을 완전히 들어내고 새 체계로 바꿨다. 핵심 아이디어 셋: ① f(n)=n번째 수를 둔 뒤 실제 평가(승률%), g(n)=n번째 수를 두기 전 최선수 기준 평가(승률%) — 한 수의 손실은 기존에도 쓰던 `winBefore-winAfter`와 같다. ② h(n)="구간 n까지의 누적 정확도" — 그 진영이 지금까지 둔 수들의 손실을 처음부터 n번째까지 평균 내 정확도로 변환한 값으로, n이 커질수록(대국이 길어질수록) 평균이 희석돼 수 하나가 전체에 미치는 영향이 자연히 줄어든다. ③ "날카로움 보정" — 그 수를 두기 전 포지션의 엔진 상위 1·2·3위 후보 수 평가(승률%)의 표준편차가 작을수록(비슷한 대안이 여럿인 무난한 포지션) 그 수의 손실을 관대하게, 클수록(정답이 사실상 하나뿐인 날카로운 포지션) 엄격하게 취급한다. 사용자와의 확인 과정에서 구체화한 지점들: (1) 후보 수 표준편차는 1·2·3위 전체로 계산하기로 해 `analyzeGame`의 MultiPV를 2→3으로 늘렸다(포지션마다 엔진 호출이 조금 더 걸리지만, "리뷰 정확도는 트레이드오프 대상이 될 수 없다"는 이번 버전의 다른 항목과 같은 원칙으로 감수). (2) 이 체계는 기존 chess.com 근사와 나란히 두지 않고 완전히 교체한다(사용자 확답). (3) 구간(오프닝/미들게임/엔드게임) 정확도는 "그 구간만"이 아니라 "대국 시작부터 그 구간 끝까지"의 누적값으로 정의한다(사용자 확인 — "구간 정확도는 그 구간까지 진행된 수순의 길이를 계속 갱신하며 계산해야 한다") — `reviewPhaseAccuracy`가 더 이상 `fromPly`부터가 아니라 항상 0부터 누적하도록 바뀌었고, 그 결과 "엔드게임 정확도"는 항상 그 진영의 게임 전체 정확도(`whiteAcc`/`blackAcc`)와 같아진다.

구현 중 초안(날카로움 보정을 그 대국 자신의 표준편차·평균 대비 z-score로, 즉 상대 기준으로 계산)에서 실측 테스트로 버그를 하나 잡았다 — 상대 기준으로 하면 "포지션이 전부 날카로웠던 대국"과 "전부 무난했던 대국"의 보정값이 완전히 똑같아지는 결함이 있었다(같은 표본의 평균·표준편차로 자기 자신을 정규화하면 그 표본이 무엇이었든 z-score 평균이 항상 같은 값 근처로 수렴하기 때문). 손실이 없는(0인) 완벽한 수까지 포지션 난이도로 깎이는 부작용도 있었다(정확도 자체에 곱하는 구조라 0에 무엇을 곱해도 0이어야 하는데 그렇지 않았음). 두 문제 모두 근본 원인이 같아 — 보정을 "그 진영이 정확도에" 곱하는 대신 "그 수 하나의 손실에" 곱하도록, 그리고 기준을 그 대국 자신의 분포가 아니라 고정된 절대 기준값(`SHARP_REF_MEAN`/`SHARP_REF_SD`, 정규분포 백분위로 `[NEW_ACC_SHARP_LO, NEW_ACC_SHARP_HI]` 배율에 매핑)으로 바꿔 해결했다 — 손실이 0이면 배율이 무엇이든 여전히 0이라 완벽한 수는 항상 완벽하게 남고, 대국 간 절대 비교도 가능해진다. `NEW_ACC_DECAY`·`SHARP_REF_MEAN`·`SHARP_REF_SD`·`NEW_ACC_SHARP_LO`/`HI`·`NEW_ACC_CALIB` 모두 실측 없이 상식적으로 잡은 튜닝 상수로 남겨뒀다 — 사용자 확인대로, chess.com 체감 수치와의 유사도가 필요하면 이 상수들만 조정하면 된다.

**UI — 라인/퍼즐 클리어 배너(LINE·PUZZLE·CLEAR) 폰트·레이아웃 변경**
`LineClearBanner`·`PuzzleClearBanner`(둘 다 퍼즐 풀이 화면, `App.jsx`)가 쓰던 `GAME_FONT`(Bagel Fat One — 퀘스트 클리어·티어 승급 팝업과 공용)를 이 두 배너 전용의 새 `CLEAR_FONT`(Google Fonts Kotta One)로 분리했다 — 다른 보상 팝업 폰트는 그대로 두고 이 배너만 바꾸는 게 목적이라, `GAME_FONT` 자체를 바꾸지 않고 상수를 새로 추가했다(`index.html`의 Google Fonts 링크에도 `Kotta+One` 추가). "LINE"/"PUZZLE"과 "CLEAR"는 기존엔 `alignItems:"baseline"` 가로 배치였는데, 사용자 요청대로 `flexDirection:"column"`으로 바꿔 위아래로 줄바꿈되게 했다(기존 좌우 슬라이드 인 애니메이션 `lineWordInLeft`/`lineWordInRight`는 그대로 재사용 — 세로로 쌓여도 각 단어가 옆에서 미끄러져 들어오는 느낌은 유지된다). 두 배너 모두 `position:"absolute"`로 페이저(보드/모식도 스와이프 컨테이너, `position:relative; overflow:hidden`) 안에 얹혀 있어서 그 컨테이너의 크기·스크롤 상태에 따라 화면 정중앙이 아니라 그 컨테이너 중앙에 뜨고 있었다 — `position:"fixed"`로 바꿔 뷰포트 자체의 정중앙에 고정되도록 했다(페이저·그 조상 어디에도 transform이 없어 fixed가 뷰포트 기준으로 정확히 동작함을 확인).

**기능/성능 — 이미지 스캔(체스판 사진 → FEN) 인식률 개선**
`api/scan-board.js`(OpenRouter `openrouter/free` 라우터 비전 모델 호출)의 근본 문제를 하나 찾았다 — 모델에게 곧장 압축된 FEN 문자열(빈 칸 개수를 숫자로 뭉친 표기, 예: `8`)을 만들어 달라고 요청하고 있었는데, 이 "몇 칸이 비었는지 정확히 세어서 숫자 하나로 뭉치기"는 LLM이 실제로 취약한 유형의 작업이다(기물 배치 자체는 정확히 "봤"어도, 마지막 압축 단계에서 칸 수를 잘못 세거나 자릿수가 밀려 결과 전체가 깨지는 경우가 실제로 있었을 것으로 보임 — "보는" 문제와 "세는" 문제를 한 요청에 섞어 둔 구조적 결함). 요청 형식을 바꿔 모델에게는 랭크 8→1, 파일 a→h 순서로 64칸을 하나도 뭉치지 않고 전부(기물 FEN 문자 또는 빈 칸은 `""`) 나열만 시키고, 그 64칸 배열을 실제 FEN 문자열로 압축하는 계산(연속 빈 칸 세기)은 서버가 결정적으로 하도록(`ranksToFenBoard`) 옮겼다 — 모델은 자신이 잘하는 "각 칸에 뭐가 있는지 보는" 일만 맡는다. 그 김에 프롬프트도 "랭크 8부터 1까지, 칸을 하나도 빠뜨리지 말고" 식으로 절차를 명시하고, 예시(초기 배치 첫 두 줄)를 프롬프트에 포함해 형식 오류 가능성을 줄였다. 응답의 `ranks`가 8x8 구조가 아니거나 유효하지 않은 문자를 담고 있으면(형식 오류) `null`을 돌려주게 하고, 결과 FEN이 64칸 전부 빈 칸이면(명백한 오독) 실패로 간주해 — 이런 경우 한 번 더 같은 요청을 재시도한다(`openrouter/free`는 호출마다 다른 무료 모델로 배정될 수 있어, 재시도가 실제로 다른 모델을 만나 성공하는 경우가 있다). 부가적으로 `temperature:0`(결정적 응답), `max_tokens` 상향(64칸 배열은 기존 압축 문자열보다 응답이 길어짐), `image_url.detail:"high"`(지원하는 모델에 한해 고해상도 분석 요청)도 함께 반영했다. OpenRouter 자체가 네트워크 정책상 이 개발 환경에서 직접 접근이 막혀 있어(egress 차단) 실제 여러 사진으로 인식률을 직접 비교 측정하지는 못했다 — 위 개선은 전부 "모델이 틀리기 쉬운 작업을 서버의 결정적 계산으로 옮긴다"는, 검증 가능한 구조적 근거에 기반한 것이며, 실제 무료 모델 카탈로그를 확인할 수 없어 `openrouter/free` 자체를 특정 모델로 바꾸는 시도는 하지 않았다(잘못된/사라진 모델 id로 하드코딩하면 기능이 아예 깨질 위험이 있어, 검증 안 된 변경은 피했다).

**UI — 모바일 리뷰 페이지 엔진 라인 확대·왼쪽 정렬**
`EngineLines`/`EngineLineRow`/`EngineLineSkeleton`/`EngineLineBlank`(학습 탭·리뷰 페이지·퍼즐 풀이가 공유하는 컴포넌트)에 `large` prop을 추가해, 이 prop이 켜지면 글자 크기(10→13)·`EvalBadge`(작은 버전 대신 기본 크기, 9→11)·행 패딩·간격을 키우도록 했다. 모바일 리뷰 페이지(`ReviewPage`의 `narrow` 분기) 호출부에만 `large`를 켜고, 다른 호출부(학습 탭, 데스크톱 리뷰, 퍼즐 풀이 모식도)는 기존 크기 그대로 둬 그쪽 레이아웃에 영향이 없게 했다. "왼쪽 정렬" 요청은, 이 블록의 `width`가 예전엔 보드 프레임을 뺀 격자 폭(`boardSize`, 카드 전체 폭보다 42px 작음)이라 `margin:"0 auto"`로 그 좁은 폭만큼 살짝 안쪽으로 밀려 보였던 것이 원인으로 보여, 모바일 호출부의 `width`를 카드 전체 폭(`"100%"`)으로 바꾸고 `large`일 때는 `margin`도 auto가 아니게 해 카드 왼쪽 끝(보드 왼쪽 끝)에 맞춰지도록 했다.

**기능 — 리뷰 페이지 새로고침 시 이어보기**
`ReviewPage`의 `phase`("summary"/"review")·`curPly`가 순수 `useState` 초기값(각각 "summary"·1)이라, 페이지를 새로고침하면 컴포넌트가 완전히 새로 마운트되며 이미 몇 수째 보고 있던 리뷰도 항상 처음(모바일은 요약 화면, 데스크톱은 1수째)부터 다시 시작해야 했다. `reviewGameKey`(chess.com 대국을 선수 닉네임+종료 시각으로 식별하던 기존 함수)와 같은 방식으로, 그 외의 경우(고유 id가 있는 저장된 대국, PGN 붙여넣기, FEN 리뷰)까지 포함해 안정적으로 식별하는 `reviewStorageKey`를 새로 만들고, 이 키로 `phase`·`curPly`만 `sessionStorage`에 저장한다(엔진 재분석 자체는 절대 캐시하지 않고 새로고침 때마다 그대로 다시 돈다 — 분석 결과·등급·코치 코멘트의 정확도를 조금이라도 타협하지 않기 위함, 아래 항목 참고). `useState`의 지연 초기화 함수에서 `sessionStorage`를 동기적으로 읽어 복원하므로 첫 렌더부터 바로 이전 위치가 반영되고, `phase`/`curPly`가 바뀔 때마다(수 이동, 리뷰 시작 등) 같은 키로 계속 갱신 저장한다. 세션(탭)이 끝나면 자동으로 사라지는 `sessionStorage`를 써서, "다시 방문"이 아니라 "새로고침"이라는 요청 범위에 정확히 맞추고 영구 저장소를 어지럽히지 않게 했다.

**UI — 단계별 정확도 말풍선(오프닝/미들게임/엔드게임 아이콘) 모바일 클리핑·폭·폰트**
`ReviewSummary`의 단계 아이콘 말풍선이 `left:50%; transform:translateX(-50%)`로 트리거 아이콘 기준 항상 중앙 정렬이었는데, 이 아이콘들(w/b) 자체가 단계 줄 오른쪽에 나란히 붙어 있어(그 줄이 `justify-content:space-between`이라 아이콘 그룹이 항상 오른쪽에 몰림) 말풍선 폭(내용에 따라 가변)이 모바일 화면 오른쪽 안전 영역을 넘어가 잘려 보였다. 새 `PhaseAccBubble` 컴포넌트로 분리하고, `useLayoutEffect`로 실제 렌더된 위치(`getBoundingClientRect`)를 재서 화면 좌우 10px 안전 여백을 넘는 만큼만 반대(화면 중앙) 방향으로 밀어 넣도록 했다 — 말풍선을 가리키는 꼬리 화살표는 그 이동분만큼 반대로 보정해, 말풍선이 밀려도 항상 트리거 아이콘의 정중앙을 계속 가리킨다. 위아래 패딩을 6px→12px로 늘려 말풍선 자체의 세로 폭을 키웠고, 텍스트 폰트는 기존 `ui-monospace,monospace`(정확도 수치의 다른 표기들과 맞추려 썼던 것으로 보임)에서 사이트 기본 폰트인 `IBM Plex Sans KR`로 바꿨다.

**UX — 리뷰 요약 화면의 "기다리지 않고 바로 리뷰 시작" 버튼 제거**
모바일 리뷰 요약(`phase==="summary"`) 대기 화면에 있던, 전체 분석(정확도%·단계별 하이라이트 집계)이 끝나기 전에 이미 채점된 수부터 곧장 리뷰를 시작할 수 있는 지름길 버튼을 없앴다. 분석이 덜 끝난 상태에서 리뷰를 보면 아직 등급·코치 코멘트가 확정되지 않은 수를 볼 수 있어, 리뷰 정확도를 기다림과 맞바꾸는 셈이 된다 — 사용자 요청대로 이 트레이드오프 자체를 없애, 이제 모바일도 항상 분석 100% 완료(`resultDone`)까지 기다린 뒤에만 리뷰를 시작할 수 있다(데스크톱은 원래도 이런 지름길이 없었다).

### OpenChess v0.3.5 — 2026/8/16

**버그 수정 — 리뷰 공유 딥링크가 실제 티켓 보유 여부와 무관하게 항상 "티켓 부족"으로 막히던 문제**
지시 없이 코드베이스 자체 감사로 발견: v0.3.4에서 게임 리뷰에 고유 주소(`/review/<식별자>`)가 생겼는데, 그 주소로 새로고침·직접 진입하면 실제로는 리뷰 티켓이 있거나 이미 열어봤던 리뷰라도 항상 `NoReviewTicketsModal`이 뜨는 회귀가 있었다. 원인은 마운트 시 진입 주소를 해석하는 `useEffect(..., [])`(App 루트, `/review`·`/(퍼즐번호)-(라인번호)` 딥링크를 되살리는 자리)가 콜백 안에서 `openReview`를 호출하는데, 이 콜백 자체가 이 effect의 dep 배열이 `[]`라 **최초 렌더 시점**의 `openReview` 클로저에 영구히 고정된다는 데 있었다 — `openReview`는 `useCallback([devUnlockAll, reviewUnlocked, reviewTickets])`으로 매 렌더 갱신되는 함수지만, 딥링크 effect는 그 함수가 갱신될 때마다 다시 구독하는 게 아니라 마운트 때 캡처한 단 하나의 버전만 영원히 참조한다. 마운트 직후 시점의 `reviewTickets`/`reviewUnlocked`는 아직 로컬·계정 복원이 끝나기 전의 초기값(`0`, 빈 `Set`)이므로, `openReview` 안의 `if (reviewTickets<=0) { setTicketBlockedOpen(true); return; }` 분기가 실제 값과 무관하게 항상 참이 됐다 — 채팅의 "리뷰 보기"(`onOpenSharedReview`, 클릭 시점의 최신 `openReview`를 씀)는 이 문제가 없고, 순수 URL 직접 진입·새로고침 경로만 영향을 받는다는 점도 이 클로저 캡처 구조로 설명된다. 이 딥링크 해석 effect를 `loaded`(로컬+계정 복원이 모두 끝난 뒤 서게 되는 기존 플래그) 의존성으로 바꿔, `loaded`가 `false→true`로 바뀌는 그 렌더의(=실제 값을 반영한) `openReview`를 쓰도록 고쳤다. 퍼즐 딥링크(`/(퍼즐번호)-(라인번호)`)는 티켓 게이팅이 없어 원래도 문제는 없었지만 같은 effect 안에 있어 함께 `loaded`를 기다리게 됐다 — 열리는 시점이 복원 완료 이후로 약간(보통 1개 네트워크 왕복 이내) 밀리는 정도의 트레이드오프.

**버그 수정 — 퍼즐 생성자가 자기 퍼즐을 편집한 직후 곧바로 재편집을 시도하면 부정확한 오류가 뜨던 문제**
같은 감사에서 발견: `PuzzleSolver`의 `creatorInfo`(생성자 uid·`editedAt`, 1시간 편집 주기 판정에 씀)는 퍼즐을 열 때 서버에서 한 번만 불러오고, 생성자 본인의 저장 경로(`persistEdit`의 `puzzleCreatorSave` 분기)는 성공해도 이 로컬 값을 갱신하지 않았다. 서버는 저장 성공 시 `editedAt`을 갱신해 새 1시간 주기를 시작하지만, 클라이언트의 `creatorCooldownMs`(`creatorInfo.editedAt` 기반 `useMemo`)는 낡은 값을 계속 써 마치 편집 가능한 것처럼 `canEditPuzzle`을 계속 허용했다 — 같은 세션에서 곧바로 다시 편집을 시도하면 서버 RPC가 주기 제한으로 거절하는데도, UI엔 정확한 사유("아직 편집 주기가 안 지났어요") 대신 catch 블록의 범용 문구("저장에 실패했어요. 잠시 후 다시 시도해주세요.")만 떴다. `persistEdit`의 생성자 저장 성공 직후 `setCreatorInfo`로 `editedAt`을 지금 시각으로 로컬 반영해, 다음 시도부터 `creatorCooldownMs`가 곧바로 올바른 값을 갖도록 고쳤다.

**기능 — 게임 리뷰 엔진을 설정 탭에서 고른 분석 엔진으로 통합, Stockfish 16 폐기**
사용자 요청: 게임 리뷰도 설정 탭에서 고른 엔진을 그대로 쓰고, 리뷰 전용으로 쓰던 Stockfish 16은 없앤다. 예전엔 `ReviewPage`·`LegacyManageModal`(유산 만들기의 analyzeGame 호출)이 전용 훅 `useReviewEngine()`으로 항상 고정된 `ENGINE_PROFILES.full`(Stockfish 16, `UCI_LimitStrength`/`UCI_Elo=3270`으로 사람 최상급 수준까지 세기 제한)을 썼는데, 이 훅과 `full` 프로필을 완전히 제거하고 두 컴포넌트가 App 루트의 `useEngine(enginePref)` 인스턴스를 `engine` prop으로 그대로 받게 했다(`LearnTab`·`PuzzleTab`과 동일한 패턴). `ENGINE_PROFILES`에서 `full` 항목과 그 하나뿐이던 `.elo` 필드를 지우면서 `eloSetoptions`(세기 제한 setoption 생성 함수)도 호출부 3곳과 함께 통째로 제거했다 — `UCI_LimitStrength`가 실제로는 엔진이 스스로 약한 척 수를 고르게 하는 옵션이라 분석 정확도에는 오히려 해가 됐던 것도 있어, 이번 통합이 원래 취지(사람 눈높이 리뷰)와 실제 동작 사이의 불일치도 함께 해소한 셈이다.
이 교체로 새로 드러난 문제 두 가지도 같이 고쳤다. 첫째, `useReviewEngine()`은 `status`가 항상 `"ready"`인 가짜 값이었는데, 실제 `useEngine`의 `status`는 `"loading"→"ready"/"off"`로 비동기 전이한다 — `ReviewPage`/`LegacyManageModal`의 분석 시작 `useEffect`가 각각 `deps=[]`·`deps=[step, sans]`로 고정돼 있어 `engine.status`가 바뀌어도 재확인하지 않고, 마운트 시점에 아직 부팅 중이면 그대로 "분석 실패"로 굳어버렸다(리뷰 딥링크 티켓 오탐과 같은 클래스의 클로저 문제) — `engine.status`를 deps에 추가하고, 실제로 분석을 시작했는지 추적하는 ref로 중복 시작만 막는 방식으로 고쳤다. 둘째, 앱 유휴 시간에 리뷰용 엔진 풀을 미리 부팅해 두던 프리페치(`requestIdleCallback`)가 `ENGINE_PROFILES.full`을 하드코딩하고 있어 `enginePref`가 바뀌어도 반영하려면 이 프로필로 바꾸고 `enginePref`를 deps에 추가했다.
`scripts/copy-engine.mjs`에서 Stockfish 16(`stockfish16` npm 별칭, `nn-*.nnue` 신경망 포함) 복사 단계를 지우고 `package.json`의 `stockfish16` devDependency도 제거했다 — `public/engine/`(gitignore 대상, 빌드 산출물)에 남아있던 관련 파일도 로컬에서 정리했다. `getAnalysisPool`의 프로필별 캐시(`analysisPoolCache`)는 그대로 남겨 뒀다 — 이제 프로필이 갈릴 이유 자체가 없어졌지만(학습 탭과 리뷰가 항상 같은 프로필), 여전히 "탭을 오가도 이미 띄운 풀을 재사용한다"는 목적으로 유효하다.

**UI — 설정 탭에 흩어진 개발자 전용 패널을 카드 하나로 통합**
사용자 요청: 개발자 권한 패널이 설정 탭 곳곳에 흩어져 있어 산만하니, 관련 주제끼리 순서를 맞춰 한 블록에 모으고 스크롤로 볼 수 있는 "2번째 페이지"처럼 만들어 개발자 모드를 켜면 자동으로 그 페이지로 스크롤되게 해 달라. `DevResourcePanel`(재화·티어·XP 조정)·`DailyPuzzleDevPanel`(일일 퍼즐 테마·직접 지정)·`PuzzleBatchRegenPanel`(전체 퍼즐 일괄 재생성)·인라인 "공동 개발자 지정" 블록이 `SettingsTab` 여기저기(모드 토글 바로 아래, 프로필 카드와 분석 엔진 카드 사이, 개발진 명단 위)에 개별 카드로 흩어져 있었는데, 이 넷을 설정 탭 맨 아래(FAQ 카드 다음)에 새로 만든 "개발자 도구" 카드 하나로 옮겼다 — 순서는 권한(공동 개발자 지정, `canManageCodev`만) → 재화·티어 테스트(`DevResourcePanel`) → 퍼즐 콘텐츠 관리(`DailyPuzzleDevPanel`, `PuzzleBatchRegenPanel`) 순으로 관련 주제끼리 묶었다. 세 패널 컴포넌트 자체는 손대지 않고, 각자 받던 `card`(배경·테두리·여백을 가진 개별 카드 스타일) prop을 빈 객체(`{}`)로 바꿔 넘겨 자체 카드 테두리 없이 하나의 바깥 카드 안에 내용만 흐르게 하고, 구간 사이에는 얇은 구분선을 뒀다.
"2번째 페이지"는 별도 탭·페이지네이션이 아니라 같은 설정 탭 안에서 스크롤로 이어지는 아래쪽 섹션으로 구현했다(기존 설정 탭 자체가 세로 스크롤 페이지라 자연스럽게 맞아떨어진다). 개발자/공동 개발자 모드 켜짐 여부(`canEdit`)가 꺼짐→켜짐으로 바뀌는 순간만(이전 값을 담은 `prevCanEditRef`로 판별) 이 블록의 ref(`devToolsRef`)로 `scrollIntoView({behavior:"smooth"})`를 호출해, 이미 켜진 채로 새로고침·재접속한 경우(마운트 시점부터 `true`)에는 스크롤하지 않는다 — 방금 스위치를 켠 그 순간에만 자연스럽게 아래로 안내되도록 하기 위함이다.

**기능 — 퍼즐 라인 길이 조정(수 추가)을 SAN 직접 입력 대신 후보 목록에서 고르는 방식으로 변경**
사용자 요청: 퍼즐 라인 길이를 직접 조작할 때 수를 타이핑하게 하지 말고, 평가치·채택률 순으로 정렬된 후보 중에서 고르게 하고, 직접 입력은 후보에 원하는 수가 없을 때 쓰는 마지막 선택지로 남겨 달라. `PuzzleSchematic`의 리프 연장("+") 패널이 예전엔 SAN을 타이핑하는 입력창 하나뿐이었는데, 바로 옆 "형제 갈래 추가"(⑂) 패널이 이미 정확히 이 요청대로(후보 목록 우선 + 맨 아래 직접 입력 폴백) 동작하고 있어 그 패턴을 그대로 재사용했다. 두 기능 모두 내부적으로 `onSuggestSiblings(path)`(→`puzzleCandidatesAt`)를 호출해 "`setup+path`로 도달하는 위치에서 다음 수 후보"를 구하는 동일한 계산이라 — 형제 갈래 추가는 그 결과에서 대상 노드의 기존 자식과 겹치는 수만 걸러내고(`existing` 필터), 리프 연장은 리프 자체가 자식이 없어(`children: []`) 그 필터가 항상 공집합이라 자연히 같은 함수를 그대로 재사용해도 의미가 어긋나지 않는다. `puzzleCandidatesAt`이 반환하는 배열은 이미 엔진 MultiPV 순위(=평가치 내림차순)로 정렬돼 있고 각 후보에 실전 채택률(`adopt`, %)이 함께 붙으므로 별도 정렬 로직은 추가하지 않았다.
`openAdd`를 비동기로 바꿔 후보 조회를 트리거하는 `addCands` state(`null`=로딩 중)를 새로 두고, 후보를 고르면 곧장 추가하는 `pickAdd`(기존 `onAddMove` 재사용, 리프 연장은 SAN 문자열만 저장하고 kind/ev/adopt는 형제 갈래 추가와 마찬가지로 저장하지 않는다 — 배경 보강이 나중에 채운다)를 추가했다. 기존 `submitAdd`(직접 입력)는 그대로 두되 UI에서 후보 목록 아래 "원하는 수가 없으면 직접 입력하세요"로 위치·문구만 옮겼고, 목록에 포커스가 먼저 가도록 입력창의 `autoFocus`는 제거했다.

**UI — 퍼즐 풀이 화면 "다음 라인 풀기" 버튼 제거, 채팅 목록 알림 해제 아이콘 추가**
사용자 요청 두 건. 첫째, 퍼즐 카드(풀이 화면)의 "다음 라인 풀기" 버튼을 없앤다 — v0.3.4에서 이미 라인을 다 풀면(클리어 애니메이션 `celebrate`가 2.4초 재생된 뒤) 자동으로 다음 미해결 라인(`nextTag`)으로 넘어가는 `useEffect`가 들어가 있어서, 이 버튼은 그 자동 전환을 수동으로 앞당기는 중복 동작이었다 — `PuzzleSolver`의 `done && (fullyComplete ? 완주배너 : nextTag != null && 버튼)` 삼항 분기에서 버튼 쪽(`nextTag` 분기)만 들어내고 완주 배너(`fullyComplete`)는 그대로 뒀다. `nextTag`는 자동 전환 effect가 여전히 쓰므로 변수 자체는 남아 있다.
둘째, 채팅 목록에서 알림을 꺼 둔(`muted`) 상대는 이름 옆에 `BellOff`(종 모양 + 사선) 아이콘을 표시한다 — 같은 자리에 이미 있던 고정(`pinned`→`Pin` 아이콘) 표시와 동일한 패턴으로, `ChatsModal`의 대화 행 렌더링에서 `pinned && <Pin/>` 바로 뒤에 `muted && <BellOff/>`를 추가했다. 데이터는 이미 `chat_conv_prefs.muted`로 존재하고 목록 행(`rows`)에도 이미 내려오고 있어(컨텍스트 메뉴의 "알림 끄기"/"알림 받기" 토글이 이 값을 읽고 쓴다), 새 조회 없이 그 값을 행 렌더링에 한 줄 더 반영하는 것으로 끝났다.

**UI/버그 수정 — 유산 재생 유산 수 등장에 극적 딜레이 추가, 착지 후 계속 흔들리던 문제 수정**
사용자 요청: 유산 수가 등장하기 전 약 1초의 정적을 둬 등장을 더 극적으로 만들고, 등장 이후 위아래로 계속 흔들리는 문제를 고친다. `LegacyRevealScreen`의 "before"(유산 수 이전 토큰 타이핑) 단계는 `typedCount >= moveIndex`가 되는 즉시 `setTypingStage("heroSolo")`를 호출해 유산 수가 곧바로 떨어지기 시작했는데, 그 사이 아무 정적이 없어 등장이 밋밋했다 — 새 `HERO_ENTRANCE_DELAY_MS`(1000ms)만큼 `setTimeout`으로 지연시킨 뒤에야 `heroSolo`로 전환하도록 바꿨다.
"흔들리는" 문제의 원인은 유산 수 낙하 스프링(`{type:"spring", stiffness:380, damping:13, mass:1}`)의 감쇠비가 약 0.33으로 너무 낮아, 착지 후에도 여러 차례(체감상 "계속") 위아래로 튀는 진동이 눈에 띄게 이어졌던 것이었다 — 감쇠비 = damping/(2·√(stiffness·mass))로 계산하면 damping 13은 확연히 언더댐프(under-damped) 영역이라 다회 진동이 났다. damping을 22(감쇠비 ≈0.56)로 올려, 착지 직후 한 번만 또렷하게 튀어오르고 곧바로 정착하도록 다듬었다 — 반동 자체를 없애 버리면(감쇠비 1 이상, 임계/과감쇠) "부딪히는" 타격감이 사라지므로, 반동은 한 번 남기되 반복 진동만 없앴다. 참고로 유산 수가 정중앙에서 왼쪽 위 제자리로 옮겨가는 이후 단계("after")의 layoutId FLIP 전환은 이미 damping 26(감쇠비 ≈0.75)으로 처음부터 안정적이었다.

**기능 — FEN 리뷰 자유 탐색·정확한 무승부 판정 (v0.3.4에서 "범위를 넘어선다"고 미뤄 둔 항목)**
사용자 요청: 지난 버전에 FEN 코드 관련해서 만들어 놓고 범위를 넘어간다고 미뤄 둔 작업을 이번에 마저 한다. 코드에서 명시적으로 "범위 밖"이라 적어 둔 지점 3곳을 찾았다 — ① `ReviewPage`의 자유 탐색(보드에 직접 수 두기)이 `interactive={!fenRoot}`로 통째로 꺼져 있던 것("legalDests의 캐슬링 판정이 지금 킹/룩 위치만 보는 기존 단순화, gameEndState가 표준 시작을 전제하는 것 등을 fenRoot까지 정확히 확장하는 건 범위를 크게 넘어선다"), ② 같은 페이지의 실시간 엔진 상위 3줄 패널이 `pvUciToSans`의 표준 시작 전제 때문에 fenRoot 리뷰에서는 통째로 비워져 있던 것("pvUciToSans 자체를 고쳐야 해 범위가 커진다"), ③ `LearnTab`의 스테일메이트·3회 동형 반복 무승부 판정(`gameDrawn`)이 `!fenRoot &&`로 FEN 모드에서 항상 꺼져 있던 것("표준 시작 위치 가정을 쓰는 이 판정 대상이 아니므로 항상 false").
세 곳 모두 근본 원인은 같다 — `gameEndState`(체크메이트/스테일메이트/3회 반복 판정)와 `pvUciToSans`(엔진 PV→SAN 변환)가 항상 표준 시작 위치(`startBoard()`)를 전제하고 있었던 것. 반면 캐슬링 권리를 FEN에서 유래한 값으로 걸러내는 `fenLegalDests`, 임의의 FEN부터 수순을 재생하는 `replayFromFen`은 `LearnTab`의 기존 FEN 모드(v0.3.4)를 위해 이미 만들어져 있었다 — 그 부품들을 그대로 재사용할 수 있는 구조였다.
새 보조 함수 세 개(`boardOfRoot`/`fenOfRoot`/`colorOfRoot`, `sansToFen`/`boardFromSans` 등 표준 전용 함수들의 fenRoot 대응)를 추가하고, `gameEndState(sans, fenRoot)`·`pvUciToSans(prevSans, uciList, maxPlies, fenRoot)`에 선택적 `fenRoot` 인자를 더해(둘 다 인자를 생략하면 기존 표준 시작 동작과 완전히 동일 — 기존 호출부는 전부 무수정으로 그대로 동작) 위 세 항목을 순서대로 되살렸다. `ReviewPage`에서는 `legalTargets`/`tryMove`가 `fenRoot`일 때 `fenLegalDests`를 쓰도록, `interactive={!fenRoot}` 제한을 제거(기본값 `true`)하도록, `drawState`가 `gameEndState(effSans, fenRoot)`를 쓰도록, 엔진 라인 패널의 `baseWhite`(백/흑 관점 부호)가 `colorOfRoot`로 정확히 계산되도록 고쳤다. `LearnTab`은 `drawState`·`goFen`(FEN 모드 전용 수 두기 함수, 원래 무승부 게이팅이 없었다)에 같은 판정을 연결했다.
**의도적으로 하지 않은 것**: 자유 탐색으로 둔 가상의 수에는 최선/우수/블런더 같은 등급이나 코치 코멘트를 붙이지 않는다 — 이 채점(`exploreMove` effect)과 코치 코멘트(`mecFacts`)는 `recaptureFact`·`exchangeOfferFact`·`redeployFact` 등 최소 10개가 넘는 하위 "Fact" 함수가 전부 `boardFromSans`로 표준 시작을 전제하는 구조라, 전부 fenRoot 인식하게 고치는 건 이번 세션에서 검증 없이 안전하게 끝내기엔 범위가 너무 컸다(사용자에게 세 가지 범위 옵션을 제시해 "보드 조작 + 정확한 무승부 판정만"으로 명시적으로 확인받았다). `ReviewPage`의 `exploreMove` effect는 `fenRoot`일 때 그대로 `null`로 유지해(`LearnTab`의 기존 `goFen`이 `stampQ`/`evalMoveKind`를 건너뛰는 것과 동일한 선) 보드·평가치 막대·엔진 상위 줄만 보여주고 등급 배지·코치 카드는 뜨지 않는다.
**부수적으로 발견해 고친 버그**: 위 조사 중 `ReviewPage`의 "Show" 화살표·마지막 수 배지 칸을 계산하는 `arrows`/`lastQ`가 (자유 탐색 여부와 무관하게, **실제로 둔 수를 볼 때도**) `boardFromSans(effSans...)`로 표준 시작을 전제하고 있었다는 걸 확인했다 — `analyzeGame`(등급·코치 코멘트 자체)과는 무관한, 순수 위치 재생이라 MEC 시스템을 건드리지 않고 안전하게 `boardOfRoot`로 고칠 수 있어 함께 수정했다. 반면 `analyzeGame` 자신의 `recaptureFact` 호출(유일한 수 판정에 쓰는 "단순 되잡기 여부")과 `brilliantExplain`/`sacrificedPieceKor` 등 코치 코멘트 계열은 위와 같은 이유로 손대지 않았다 — FEN 리뷰의 실제 played move 등급·코멘트가 이 지점에서는 여전히(v0.3.4 때와 마찬가지로) 완전히 정밀하지 않을 수 있다는 뜻이며, 다음에 이어갈 항목으로 남겨 둔다.

**기능 — 학습 탭 보드 편집기**
사용자 요청: 학습 탭 보드 상단 "분석" 버튼을 다른 화면에서 쓰는 연두색+별 리뷰 버튼(`BestMoveJumpButton`)으로 바꿔 맨 오른쪽으로 옮기고, 그 빈자리에 펜 아이콘 "편집" 버튼을 새로 만든다 — 누르면 모바일은 제시된 스크린샷 그대로(초기화/지우기, 착수 차례 라디오, 캐슬링 체크박스, 보드, 기물 팔레트, FEN 복사/붙여넣기, FEN 입력창, 이미지 스캔, 되돌리기/다시하기, 취소/완료가 위에서 아래로 이어지는 전체 화면 시트), 데스크톱은 보드만 남기고 나머지 UI를 전부 보드 오른쪽에 모은 가로 비율 창으로 뜨는 보드 편집기를 연다.
`BestMoveJumpButton`(20차, 최선 수 바로가기에 쓰던 연두색 원형+별 배지 버튼)이 하드코딩돼 있던 `title`을 선택적 prop으로 바꿔(기본값은 기존 문구 그대로라 기존 세 호출부는 무수정으로 동일하게 동작) 학습 탭에서는 `title="기보 분석(리뷰)"`로 재사용했다. `NotationTools`(기존 FEN/PGN 텍스트 붙여넣기 모달)와 같은 `onLoadFen(parseFenFull 결과)` 계약을 그대로 따르는 새 `BoardEditorModal`을 추가해, 완료 시 그 계약 그대로 `onLoadFen`을 호출해 학습 탭의 기존 FEN 모드 진입 경로에 자연스럽게 합류시켰다(별도의 새 "포지션 진입 경로"를 만들지 않았다).
보드 편집은 실제 대국 규칙(합법수 판정)이 전혀 필요 없는 순수 배치 작업이라, 기존 `Board` 컴포넌트(포인터 드래그로 기물을 옮기는, 규칙에 강하게 결합된 컴포넌트)를 재사용하지 않고 좌표 라벨만 곁들인 순수 클릭 그리드(`EditorBoardGrid`)를 새로 만들었다 — 모바일 터치 환경에서 팔레트→보드 간 네이티브 HTML5 드래그가 동작하지 않는 문제(터치 미지원)를 피하기 위해서이기도 하다. 상호작용은 "팔레트에서 기물/지우개를 고르면 계속 무장된 채로 보드 칸을 찍을 때마다 그 기물을 놓거나 지운다(도장 찍듯 반복 배치 가능)" + "아무 도구도 안 고른 상태에서 보드 위 기존 기물을 클릭하면 집어 들고, 다른 칸을 클릭하면 그 자리로 옮긴다"는 두 모드로 설계했다 — 모바일 터치·데스크톱 마우스 모두 동일하게 동작하고 별도의 커스텀 포인터-드래그 코드가 필요 없다.
상태는 `{list, idx}` 형태의 히스토리 스냅샷 배열 하나로 관리해(board/turn/rights를 따로 두는 대신 스냅샷 하나에 다 묶어) 되돌리기·다시하기·처음으로·마지막으로 네 버튼이 서로 어긋날 여지를 원천 차단했다. FEN 문자열은 앙파상 없이(`-`) `boardToFen(board, 0, castleStr, "-", turn)`로 그때그때 파생시키는 값이며, 입력창에 직접 타이핑/붙여넣기해도(Enter 또는 blur 시 `parseFenFull`로 검증 후 반영, 실패하면 에러 문구만 뜨고 보드는 그대로 유지) 같은 경로로 반영된다.

**기능 — 보드 편집기 드래그 무브, 이미지 스캔(사진→FEN), FEN 모드 평가치 바·엔진 라인 활성화(리뷰는 계속 차단)**
사용자 요청 세 가지를 한 번에 처리했다.
① **드래그 무브**: 위 보드 편집기는 처음엔 순수 클릭(무장→도장 찍기, 집기→옮기기)만 지원했는데, "이 환경에서도" 자연스럽게 드래그로 기물을 옮길 수 있게 해 달라는 요청에 따라 `Board` 컴포넌트가 이미 쓰고 있던 Pointer Events 드래그 패턴(마우스·터치·펜을 하나의 이벤트 체계로 다루는 `pointerdown/move/up/cancel`, `setPointerCapture`, 4px 이동 임계값으로 탭과 드래그를 구분, 드래그가 실제로 있었을 때만 뒤이은 합성 click을 억제하는 `suppressClickRef`)을 `EditorBoardGrid`/`BoardEditorModal`에 그대로 옮겨왔다. 팔레트 기물 버튼과 보드 위 기존 기물 둘 다 드래그 출발점이 될 수 있어 `{source:"board"|"palette", from, piece, x, y}` 형태로 통일해 다뤘고, 도구(팔레트 지우개/기물)가 무장된 동안은 드래그를 아예 시작하지 않아(순수 클릭 도장 찍기와 충돌하지 않도록) 기존 클릭 상호작용을 그대로 보존했다. 보드 위 기물을 보드 밖으로 드롭하면 그 기물을 지운다(체스 보드 편집기의 흔한 관례). 고스트(드래그 중 손가락/커서를 따라다니는 기물 그림)는 그리드 내부에 `position:absolute`로 그리는 기존 `Board`의 방식 대신, 화면 전체 기준 `position:fixed`로 `BoardEditorModal` 최상단에 그렸다 — 팔레트가 그리드 바깥에 있어 좌표계가 다르고, 드래그 도중 보드 경계를 자유로이 넘나들어야 하기 때문에 이 편이 더 단순했다(그리드의 `overflow:hidden`과도 충돌하지 않는다).
② **이미지 스캔**: 새 Vercel 서버리스 함수 `api/scan-board.js`를 추가해 사진 속 체스판 배치를 실제로 읽어 FEN으로 변환한다. Claude(Anthropic Messages API, `claude-opus-5`, 비전 입력 + `output_config.format`의 JSON 스키마 구조화 출력)에게 이미지를 보내 FEN의 보드 배치 부분(첫 필드)만 받아온다 — 캐슬링 권리·차례·앙파상·수 카운트는 사진만으로 알 수 없는 정보라 서버가 추측하지 않고, 클라이언트가 사용자의 편집 화면에 이미 갖고 있던 값(현재 `turn`/`rights`)에 인식된 보드만 얹어 반영한다(`parseFenFull(fen_board + " " + turn + " " + castleRightsStr(rights) + " - 0 1")` → `pushSnap({board})`). 클라이언트는 숨긴 `<input type="file" accept="image/*" capture="environment">`(모바일에서 카메라 촬영도 바로 가능)로 이미지를 받아 `FileReader`로 base64 변환 후 이 함수에 POST하고, 인식 중엔 버튼이 "인식하는 중..."으로 바뀌며 비활성화된다. **배포 시 별도 설정 필요**: Vercel 프로젝트 환경변수에 `ANTHROPIC_API_KEY`가 설정돼 있어야 실제로 동작한다 — 키가 없으면 함수가 500과 안내 문구를 돌려주고(다른 기능엔 영향 없음), 이 세션 환경에는 그 키가 없어 실제 인식 왕복까지는 검증하지 못했다(파일 선택→base64 변환→요청 전송→에러를 UI에 정상 표시하는 클라이언트 경로까지는 Playwright로 확인함). `package.json`에 `@anthropic-ai/sdk`를 새 의존성으로 추가했다(기존 `api/lichess.js`는 순수 `fetch` 프록시였지만, 이번엔 비전+구조화 출력을 다루는 공식 SDK 사용이 적합하다고 판단).
③ **FEN 모드 리뷰 차단(원복) + 평가치 바·엔진 라인 활성화**: v0.3.4에서 열어 뒀던 "FEN 모드에서도 리뷰 페이지 열기"를 다시 막았다 — `LearnTab`의 리뷰 버튼(`BestMoveJumpButton`)이 `fenRoot`가 있으면 `onOpenReview`를 아예 호출하지 않고, 대신 보드 카드 위에 "업데이트를 기대해 주세요!"라고 잠깐(1.8초) 떴다 사라지는 토스트(`framer-motion`의 `AnimatePresence`)만 띄운다. 그 대신 같은 FEN 모드에서 평가치 바와 엔진 상위 줄은 정확히 작동하도록 새로 만들었다 — 기존에는 `useMergedMoves`(학습 탭의 후보 수 패널 훅)가 fenRoot 여부와 무관하게 표준 시작 위치 전용 계산(`sansToFen`·`boardFromSans`·MultiPV 이론 후보 보충)을 이론 수 조회와 실시간 엔진 평가 양쪽에 한데 얽어 두고 있어, 이 훅 자체를 fenRoot 인식하게 고치는 건 위험이 커 보였다(v0.3.4에서 FEN 모드 리뷰를 "범위 밖"으로 미룬 것과 같은 이유). 대신 `ReviewPage`의 엔진 라인 effect(이미 `fenOfRoot`·`colorOfRoot`·`pvUciToSans(...,fenRoot)`로 FEN 인식하게 돼 있던 코드)를 본떠, `LearnTab`에 훨씬 단순한 별도 effect(`fenEval` state)를 새로 추가해 `posEval`·`engineLines`·`curDepth`만 채운다 — 이론 수·마스터 통계 등 "책" 데이터(`moves`, `posGames`, `engineNote`)는 fenRoot일 때 여전히 빈 값으로 중립화된 채로 남는다(애초에 이 위치와 무관한 정보이므로 의도한 동작).
구현 중 실제로 두 가지 버그를 잡았다. 첫째, 처음엔 공용 엔진(`engine`, 단일 워커 FIFO 큐)에 바로 `evaluateMulti`를 요청했는데, 학습 탭의 기본 후보 수 패널이 표준 시작 위치용 `"learn-lines"` 작업(최대 20초짜리 심화 탐색 단계 포함)을 이미 이 큐에 올려 두고 있으면 FEN 모드 평가가 그 뒤에 줄을 서 몇십 초씩 늦게 뜨는 문제가 있었다 — 게임 리뷰 엔진 라인과 동일하게 `getAnalysisPool`(독립 워커 풀)에서 전용 워커를 받아 쓰도록 바꿔 해결했다. 둘째, 이 풀 워커의 `evaluateMulti`가 공용 `engine` 인스턴스와 인자 개수가 다르다는 걸 놓쳤다 — 공용 엔진은 `(fen,depth,multipv,movetime,onProgress,onLines,slot)` 7개를 받지만 풀 워커는 `onProgress` 없이 `(fen,d,multipv,mt,onLines,slot)` 6개뿐이라(`callEvaluateMulti`의 기존 주석에 정확히 이 함정이 적혀 있었다), 7개짜리로 호출하면 `onLines` 자리에 내 `onProgress` 함수가, `slot` 자리에 내 `onLines` 함수가 잘못 들어가 조용히 아무 값도 못 받아왔다 — 6개 시그니처에 맞춰 다시 짜고, `posEval`·`curDepth`도 (별도 진행 콜백 없이) 이 `onLines`가 돌려주는 1순위 줄에서 함께 뽑아내도록 고쳤다. Playwright로 유효한 미드게임 FEN을 붙여넣고 평가치 바(`+0.27`~`+0.30` 등 실제 값)와 엔진 상위 3줄이 정상적으로 채워지는 것, 리뷰 버튼을 눌러도 페이지 이동 없이 토스트만 뜨는 것을 확인했다.

**기능 — 퍼즐 딥링크에 `/puzzle/` 접두사 추가, 리뷰 티켓 시스템 전면 삭제(게임 리뷰 무제한)**
사용자 요청 두 건.
① **링크 형식 변경**: 퍼즐 딥링크가 예전엔 `openchess.kr/(6자리 번호)-(라인 번호)`처럼 접두사 없이 곧장 숫자로 시작해, URL만 봐서는 이게 무슨 링크인지 알 수 없었다 — 이미 명확한 접두사를 쓰고 있던 리뷰 링크(`openchess.kr/review/(식별자)`)와 같은 방식으로 `openchess.kr/puzzle/(6자리 번호)-(라인 번호)`로 바꿨다(번호와 라인을 잇는 하이픈은 사용자 요청으로 그대로 유지, `/puzzle/` 접두사만 추가). `puzzleShareUrl` 생성 함수, 퍼즐 창을 열 때/라인이 바뀔 때 URL을 갱신하는 `pushState`/`replaceState` 호출부(`onPuzzleLineChange`), 뒤로가기 시 팝스테이트 정리 로직(`closeActive`, 전역 `popstate` 리스너), 마운트 시 딥링크를 해석하는 정규식(`/^\/puzzle\/(\d{6})-(\d+)$/`)까지 총 4곳을 일관되게 바꿨다 — 리뷰 링크는 이미 `/review/` 접두사가 있어 형식 변경 대상이 아니었다. `vercel.json`은 이미 API 경로를 제외한 모든 경로를 `index.html`로 돌려보내는 캐치올 리라이트라 별도 수정이 필요 없었다.
② **리뷰 티켓 전면 삭제**: v0.2.9에서 도입된 "게임 리뷰를 하나 열 때마다 리뷰 티켓 1개를 소비"하는 재화 시스템(일일 퀘스트·티어 승급으로 획득)을 코드·이미지 자산까지 전부 걷어냈다 — `openReview`의 소비·부족 검사 분기, `NoReviewTicketsModal`(부족 안내 팝업), `TicketIcon` 컴포넌트, `reviewTickets`/`setReviewTickets`/`ticketBlockedOpen` state와 그 로컬스토리지·Supabase(`user_progress.progress` JSON blob) 저장/복원 필드, 일일 퀘스트 개별 클리어(+1)·전체 클리어(+2)·티어 승급(+10) 세 지급 지점과 그 UI 배지(퀘스트 토스트·전체 클리어 모달·`TierUpOverlay`), `DevResourcePanel`의 티켓 입력란, `StoreTab`/`SettingsTab`으로 내려가던 prop 전부를 제거했다. 원래 티켓 전용 서버 자산(전용 테이블·컬럼·RPC)은 애초에 없었다 — `reviewTickets`는 `user_progress.progress`의 그냥 JSON 필드 하나였을 뿐이라, 이 작업은 순수 클라이언트 코드 변경만으로 끝났고 별도 DB 마이그레이션이 필요 없었다(과거에 저장된 progress blob에 `reviewTickets` 키가 남아 있어도 이제 아무도 읽지 않으므로 무해하게 무시된다). 이제 `openReview`는 무조건 즉시 리뷰를 연다.
`reviewUnlocked`(리뷰를 연 적 있는 대국의 `reviewGameKey` 집합)는 삭제하지 않고 남겨 뒀다 — 티켓 소비 여부 판정에도 쓰였지만, `AccountChessStats`의 "리뷰한 대국만" 필터 체크박스가 이 데이터를 그대로 재사용하고 있어(v0.3.1) 지우면 그 필터 기능 자체가 망가진다. 사용자에게 이 둘을 분리할지 명시적으로 물어 "필터는 유지"로 확답을 받았다 — `openReview`는 이제 리뷰를 열 때마다 조건 없이 `reviewUnlocked`에 그 대국의 키를 계속 기록만 하고(소비·차감 없음), 필터는 그 기록을 그대로 읽는다. `public/review-ticket.png`·`public/review_ticket.png`(orphan 중복 파일) 두 이미지 자산도 함께 삭제했다.

**기능 — 이미지 스캔 백엔드를 Anthropic(유료) → Gemini → OpenRouter(무료, 지역 제한 없음)로 교체**
사용자가 `ANTHROPIC_API_KEY` 발급에 결제(크레딧 충전)가 필요하다는 걸 확인하고 무료 대안을 요청했다. 체스판처럼 8×8 고정 격자에 표준화된 기물 모양만 읽으면 되는 단순한 인식 작업은 비전 모델 간 정확도 차이가 결과에 크게 영향을 주지 않는다고 판단해 1차로 Google Gemini API로 교체했으나, 사용자가 `aistudio.google.com` API 키 발급 화면 자체에서 지역 제한 메시지를 만나 이 경로가 막혔다. 그래서 2차로 OpenRouter(여러 제공사의 모델을 한 계정·한 API로 중개하는 서비스)로 다시 교체했다 — `openrouter/free` 라우터 모델을 쓰면 그 시점에 쓸 수 있는 무료 비전 모델 중 하나로 자동 배정해 준다(특정 무료 모델 하나를 하드코딩하면 그 모델이 나중에 내려갔을 때 통째로 깨지므로, 배정을 라우터에 맡기는 편이 더 안정적이라고 판단). `api/scan-board.js`가 `api/lichess.js`와 같은 순수 `fetch`로 OpenRouter의 OpenAI 호환 `chat/completions` 엔드포인트를 호출하도록 다시 쓰였다(`content`에 텍스트+`image_url`(data URI) 조합, `Authorization: Bearer`, 관례상 `HTTP-Referer`/`X-Title` 헤더). 구조화 출력(JSON 스키마)은 라우터가 매번 다른 모델로 배정하는 특성상 모든 후보 모델이 지원한다는 보장이 없어 쓰지 않고, 프롬프트로 "다른 설명 없이 JSON 객체 하나만" 요청한 뒤 서버의 `extractJson`이 우선 `JSON.parse`를 시도하고 실패하면 응답 텍스트에서 `{...}` 블록만 정규식으로 추출해 다시 파싱하는 관대한 방식으로 처리한다 — 클라이언트에 돌려주는 응답 형태(`{fen_board, confidence}`)와 캐슬링·차례·앙파상을 서버가 추측하지 않는 동작은 세 백엔드 모두 동일하게 유지했다. Vercel 환경변수는 최종적으로 `OPENROUTER_API_KEY`(`openrouter.ai/keys`에서 카드 등록 없이 발급)를 요구한다(키가 없으면 이전과 동일하게 500과 안내 문구만 반환, 다른 기능엔 영향 없음).

### OpenChess v0.3.4 — 2026/8/13

**기능 — 게임 리뷰 페이지에 공유 버튼(인앱 친구·외부 앱)**
사용자 요청: 퍼즐과 마찬가지로 리뷰 페이지에도 모바일·데스크톱 모두 공유 버튼이 필요하다. `ReviewPage`는 두 레이아웃(좁은 화면 전용 vs 데스크톱 2단)이 헤더 하나(`header` 변수)를 공유하는 구조라, 그 헤더 오른쪽에 공유 아이콘 버튼 하나만 추가해 두 레이아웃 모두에 자동으로 적용되게 했다(별도 분기 불필요). 페이지가 열리면 `reviewGameIdentifier(game)`로 이 리뷰의 딥링크 식별자를 독립적으로 한 번 더 계산해 둔다(App 루트가 주소창을 갱신하는 것과 같은 계산이지만, 그쪽 히스토리 갱신 타이밍과 무관하게 공유 시트가 준비되는 대로 곧장 쓸 수 있어야 하므로 별도로 갖는다).
새 `ReviewShareSheet`(퍼즐의 `PuzzleShareSheet`와 같은 구조 — 외부 공유 줄(`ExternalShareRow` 재사용) + 친구 목록 보내기)를 열어, 위쪽은 Web Share API(모바일에서 카카오톡·인스타그램 등 OS 공유 시트) 또는 데스크톱 폴백(링크 복사·카카오스토리·X·페이스북), 아래쪽은 친구를 골라 채팅으로 직접 보낼 수 있다. 채팅 공유는 새 `reviewShareSend` 함수가 `chat_messages`에 `review_id`(리뷰 딥링크 식별자 그 자체) 컬럼을 채운 행을 남기는 방식으로, 퍼즐 공유 카드(`puzzle_no`)·유산 공유 카드(`legacy_slot`)와 완전히 같은 패턴이다(`supabase-setup.sql`에 `chat_messages.review_id` 컬럼 추가). `ChatPanel`은 이 컬럼이 있는 메시지를 리뷰 미리보기 카드(대국 상대 이름 또는 "FEN 포지션 분석"/"PGN 대국 리뷰" 라벨 + "리뷰 보기" 버튼)로 렌더링한다 — 미리보기는 `resolveReviewIdentifier`로 그 자리에서 복원되는 FEN·PGN 리뷰는 즉시, chess.com 리뷰는 서버 캐시(`reviewed_games`, 보낸 사람이 이미 그 대국을 열어 올려 둔 것)를 한 번 더 조회한다. "리뷰 보기"는 새 `onOpenSharedReview`(App 루트)가 같은 방식으로 복원해 기존 `openReview`를 그대로 재사용해 연다.

**성능 — 게임 리뷰가 총 소요 시간은 거의 그대로 두면서 depth 20에 더 안정적으로 도달하도록**
사용자 요청: 리뷰 정확도가 낮다(depth 20을 목표로 하지만 실제로는 자주 못 찍는다) — 전체 리뷰 시간은 기존의 약 1.1~1.2배 안에서 depth 20 도달률을 높여 달라. `analyzeGame`은 예전엔 모든 포지션에 똑같은 고정 `movetime`(포지션당 상한)을 줬는데, 실제로는 조용한 포지션 대부분이 그보다 훨씬 일찍(엔진이 스스로 멈춤) depth 20에 도달해 남는 시간이 그냥 버려지고, 후보 수가 많은 복잡한 중반전 포지션 몇 개만 그 짧은 상한 안에 depth 20을 못 찍어 정확도가 떨어지는 구조였다.
이 게임 전체에 쓸 "벽시계 시간 예산"을 병렬 워커 수를 감안해 미리 잡아 두고(`(포지션 수 ÷ 워커 수) × 기존 movetime × 1.15`), 매 포지션마다 "남은 예산 ÷ 남은 포지션 수"를 그 포지션의 movetime으로 새로 계산해서 쓴다(`nextMovetime`) — 쉬운 포지션이 일찍 끝내고 남긴 여유를 다음 어려운 포지션이 자동으로 이어받는, 실제 체스 엔진들의 시간 관리와 같은 원리다. 한 포지션이 예산을 통째로 집어삼키지 않도록 상한(기본 movetime의 4배)과, 예산이 바닥나도 최소한의 탐색은 보장하는 하한(0.4배)을 둔다. 상한을 처음엔 6배로 잡았다가, 아래 "몇몇 수의 등급·평가치가 미리 계산되지 않는" 문제와 상충한다는 걸 확인하고 4배로 낮췄다(depth 20을 더 채우려는 목적과 head-of-line 정체를 완화하려는 목적이 서로 밀어내는 관계라, 안전한 쪽으로 다소 보수적으로 잡았다).
(알려진 한계) 이 시간 예산 방식은 "쉬운 포지션의 남는 시간을 어려운 포지션에 자동으로 재분배한다"는 원리로 설계했지만, 이 개발 환경에서는 실제 체스 엔진을 오래 돌려 벤치마크할 수 없어(오프라인 샌드박스) 목표한 1.1~1.2배 배율이 실측으로 검증된 수치는 아니다 — 원리상 그렇게 수렴하도록 만들었을 뿐이라는 점을 알려드린다. 실제 사용 중 체감 배율이 크게 벗어나면 `REVIEW_BUDGET_FACTOR`/`REVIEW_PER_POS_CAP_MULT`/`REVIEW_PER_POS_FLOOR_MULT`(세 상수, `analyzeGame` 안) 조정으로 대응할 수 있다.

**버그 수정 — 리뷰의 몇몇 수가 그 수순에 도달해서야 계산이 시작되는 것처럼 보이던 문제**
사용자 재현: 리뷰를 보다 보면 몇몇 수는 수 체계 아이콘·평가치가 미리 채워져 있지 않고, 마치 그 수까지 왔을 때에야 계산이 시작되는 것처럼 늦게 뜬다. `analyzeGame`의 채점(`gradeOne`)은 `gradeBoard`·직전 두 수(`moves[i-1]`/`moves[i-2]`) 의존성 때문에 반드시 왼쪽부터 순서대로만 진행되는데(work-stealing으로 여러 포지션을 병렬 평가하더라도), 병렬로 도는 워커들의 완료 순서는 뒤섞일 수 있어 한 포지션이 유난히 오래 걸리면 이미 평가가 다 끝난 그 뒤 포지션들까지 전부 그 자리에서 대기하는 head-of-line 정체가 근본 원인이었다. 위 성능 항목의 상한(6배→4배)을 낮춰 한 포지션이 정체를 유발할 수 있는 최악의 길이 자체를 줄였고, 별도로 발견한 진짜 버그(타임아웃으로 실패한 포지션이 재시도 없이 "분석 중"에 영원히 멈춰 있던 것)를 한 번 재시도하도록 고쳐 실제로 다시는 안 채워지던 자리를 없앴다. (알려진 한계) 두 조치 모두 정체를 완화하는 조치일 뿐, "느린 포지션 하나가 뒤 포지션들의 표시를 늦춘다"는 순차 채점 구조 자체의 근본적인 재설계(예: 전체를 먼저 빠르게 훑고 어려운 포지션만 나중에 재분석하는 2단계 구조)는 이번 범위에서 하지 않았다 — 남아 있는 지연은 예전보다 훨씬 짧아졌지만 완전히 없어지지는 않을 수 있다.

**버그 수정 — 집중 학습에서 "탁월한 수"가 한참 뒤에야 확정돼 퍼즐 생성이 늦어지던 문제**
사용자 재현: 탁월한 수를 눌러 집중 학습에 들어가면 처음엔 "좋은 수"로 뜨다가 한참 뒤에야 "탁월한 수"로 바뀌고, 그동안 퍼즐 생성(탁월한 수로 확정돼야 시작됨)도 함께 늦어진다. `useFocusAnalysis`의 실시간 재분류 로직은 두 원인이 겹쳐 있었다 — (1) 아직 확정되지 않은 상태의 표시용 기본값이 `"good"`(좋은 수)이라 실제로는 "아직 모름"인데 마치 확정된 등급처럼 보였고, (2) 최선 응수(`best`)와 실제 응수(`after`) 두 위치는 서로 독립적이라 순서를 지킬 이유가 없는데도 공용 엔진(`engine`) 하나의 FIFO 큐에서 순차로 `await`(각각 최대 5초, 합쳐 최대 10초) 처리되고 있었다. 기본값을 `"pending"`(분석 중 — 이미 있던 정식 등급)으로 바꾸고, 세션 내내 재사용되는 병렬 워커 풀(`getAnalysisPool`, 리뷰 페이지의 `evalMoveKind`가 이미 쓰던 것과 같은 패턴)에서 워커 두 개를 받아 `best`·`after`를 동시에 평가하도록 고쳐 최악의 지연을 대략 절반으로 줄였다(풀 부팅에 실패하면 기존처럼 단일 `engine`으로 폴백). 진행 중 갱신(`onProgress` 콜백으로 depth마다 아이콘이 여러 번 바뀌던 것)도 함께 없애, 최종 확정 전까지는 "분석 중"만 보이고 확정된 뒤 한 번에 정확한 등급으로만 바뀐다(중간에 오답인 등급이 잠깐 스쳐 보이는 일 자체가 없어짐).

**기능 — 채팅 명령어 `/review`(-recent·-PGN·-FEN)**
사용자 요청: 채팅에서 `/review` 명령어로 리뷰를 공유할 수 있게 하고, 하위 명령어로 `-recent`(가장 최근 chess.com 대국)·`-PGN <코드>`·`-FEN <코드>`를 지원한다. `-PGN`/`-FEN`은 대소문자 관계없이 인식하고, 뒤에 오는 코드는 전송 전에 유효성부터 검증해 — 유효하지 않으면 전송 자체를 막고 "유효하지 않은 코드"로 안내한다. 기존 `/puzzle`·`/legacy` 명령어와 같은 자리(`ChatPanel`의 `send` 함수, 정규식 매칭)에 추가했다 — 세 정규식(`-recent`/`-pgn <코드>`/`-fen <코드>`, 전부 `/i` 플래그로 대소문자 무관) 중 하나라도 매치하면 명령어로 처리하고, `/review`로 시작하지만 셋 다 안 맞으면(예: `/review -xyz`, 코드 누락) 다른 명령어들과 달리 평범한 텍스트로 흘려보내지 않고 곧바로 "유효하지 않은 코드"로 막는다(요청 문구 그대로).
- `-recent`는 App 루트가 이미 갖고 있는 내 chess.com 최근 대국 목록(`useChessCom`)을 `myChesscomGames` prop으로 `ChatPanel`까지 내려받아, 종료 시각(`endTime`) 기준 최신 대국을 골라 리뷰 게임 객체로 만든다(연동된 계정이 없거나 대국을 하나도 못 찾으면 그 사실을 안내하고 막는다).
- `-PGN <코드>`는 새 `sanSequenceValid(sans, fenRoot)` 헬퍼로 각 SAN이 실제로 그 시점에 둘 수 있는 합법적인 수인지 한 수씩 확인한다(기존 `parsePgnSans`는 토큰 모양만 보고 걸러내 오타·불법수를 그대로 통과시킬 수 있었다) — `[FEN "..."]` 헤더가 있으면 그 위치부터, 없으면 표준 시작 위치부터 검증한다.
- `-FEN <코드>`는 `looksLikeFen`·`parseFenFull`로 실제 파싱 가능한 FEN인지 확인한다.

검증을 통과하면 세 경우 모두 `reviewGameIdentifier`로 딥링크 식별자를 만들고(-recent는 chess.com 숫자 ID를 그대로, -PGN/-FEN은 위 공유 버튼 항목과 같은 암호화·원문 방식), 새 `reviewShareSend`로 채팅에 리뷰 공유 카드를 남긴다(-recent는 `reviewedGameShare`로 서버 캐시에도 함께 올려 상대가 딥링크를 복원할 수 있게 한다). 입력창 위 명령어 자동완성 목록(`CHAT_COMMANDS`)에도 세 하위 명령어를 추가했다.

**UI — 유산 관련 모달 3종이 v0.3.2~v0.3.3의 모바일 전체 화면 패턴에서 빠져 있던 문제**
`ChatsModal`·`ChatUserProfileModal`·`UserSearchModal`(v0.3.2), `FriendsModal`(v0.3.3)은 모바일(`useNarrow(640)`)에서 고정폭 카드 대신 화면을 꽉 채우는 전체 화면으로 전환되는데, 같은 v0.3.3에서 새로 추가된 `LegacyManageModal`(유산 만들기/편집)·`LegacyAllModal`(유산 전체 보기)·`LegacyShareSheet`(유산 공유)는 이 패턴이 빠진 채 `maxWidth: 380~460`의 고정폭 카드로 남아 있었다. 특히 `LegacyManageModal`은 "chess.com 대국에서 선택"을 누르면 필터·최근 대국 목록을 갖춘 `AccountChessStats`를 그 안에 그대로 펼치는데, 이 컴포넌트는 이미 `FriendsModal`·`UserSearchModal`의 전체 화면 프로필 서브뷰 안에서 검증된 채로 쓰이고 있어 좁은 고정폭 카드 안에 있을 이유가 없었다. 세 컴포넌트 모두 `useNarrow(640)`을 추가하고, 기존 전체 화면 모달들이 쓰는 패턴(모바일에서 `width/height: 100%`·`borderRadius: 0`·`border: none`, 헤더는 `flexShrink: 0`으로 고정하고 본문만 `flex: "1 1 auto"` + `overflowY: "auto"`로 스크롤)을 그대로 맞췄다. `LegacyManageModal`·`LegacyAllModal`은 원래 헤더와 본문이 한 덩어리 `padding` 블록이었던 것을, 이 패턴에 맞춰 제목 줄을 별도 `flexShrink: 0` 헤더로 분리했다.

**시스템 — `package.json`의 `version` 필드가 실제 배포 버전과 어긋나 있던 것**
`APP_VERSION`은 `src/App.jsx`의 `CHANGELOG[0].version`에서 파생되고 `AboutPage.jsx`의 `VERSION_HISTORY`도 매 버전 함께 갱신돼 왔지만, `package.json`의 `version`은 v0.3.0 이후로 방치되어 계속 `"0.3.0"`으로 남아 있었다(소스 어디서도 참조되지 않아 빌드·배포 동작에는 영향이 없었다). 실제 배포 버전과 맞춰 `"0.3.4"`로 동기화했다.

**UX — 채팅 메시지 수정/삭제 메뉴를 말풍선 옆 여백으로 재배치**
`ChatPanel`의 꾹 누르기/오른쪽 클릭 메뉴는 v0.3.3까지 말풍선 바로 위/아래로 열렸는데, 메시지 목록의 줄 간격이 6px뿐이라 34px짜리 메뉴가 항상 이웃 메시지와 겹쳤다(v0.3.3에서 넣은 `BUBBLE_CENTER_BIAS` 보정은 겹침을 줄였을 뿐 구조적으로 없애지는 못했다). `safeBubbleAnchor`를 새 `sideBubbleAnchor`로 교체해, 메뉴를 그 메시지의 세로 중앙 높이에 맞춰 말풍선이 없는 쪽 여백(내 메시지는 왼쪽, 상대 메시지는 오른쪽 — 항상 대화창 중앙을 향하는 쪽)에 옆으로 띄우도록 바꿨다. 그 여백엔 애초에 다른 말풍선이 없으므로 메시지 밀도와 무관하게 구조적으로 겹칠 일이 없다. 세 곳(유산 카드·퍼즐 카드·텍스트 메시지) 모두 메뉴 JSX를 말풍선을 감싸는 `transform` 요소(드래그 애니메이션용) 밖으로 옮겨, `position:absolute` 계산이 그 transform에 영향받지 않게 했다. dx/dy는 이 "자연 위치"가 실제로 잘리는 경계(`listRef`, 없으면 window) 밖으로 나갈 때만 안쪽으로 당기는 보정값으로 남긴다.

**버그 수정 — 채팅 메시지 수정/삭제 실패가 조용히 묻히던 문제**
`doDelete`는 삭제 버튼을 누르면 곧장 로컬 목록에서 메시지를 지우고(낙관적 업데이트), 서버 삭제(`chatDeleteMessage`)가 실패했을 때만 `load()`로 되돌리는 구조였다 — 실패해도 에러 표시가 전혀 없어, 메시지가 잠깐 사라졌다가 되돌아오는 것으로만 보이거나(깜빡임) 그냥 아무 일도 없던 것처럼 보였다("삭제가 안 된다"는 신고와 부합). 수정(`chatEditMessage`)도 실패 시 `editingId`만 그대로 남을 뿐 아무 표시가 없었다. 두 경우 모두 서버 응답을 기다린 뒤 성공했을 때만 상태를 반영하도록 바꾸고(삭제는 낙관적 업데이트 제거), 실패하면 기존 `cmdError`(입력창 위 에러 문구, `/puzzle`·`/legacy` 명령 실패에 이미 쓰이던 자리)로 "메시지를 삭제/수정하지 못했어요. 잠시 후 다시 시도해 주세요."를 띄운다. `chat_edit_message` RPC·`chat_messages` delete 정책(`from_uid`만 허용) 자체는 `supabase-setup.sql`에 이미 정확히 정의돼 있어(클라이언트 호출부도 정상 매칭) 원인은 실패 자체가 아니라 실패를 감지할 방법이 없던 것이었다 — 이제 실패하면 실제 원인(권한·네트워크 등)을 알아볼 수 있도록 눈에 보이는 에러가 남는다.

**UI — 채팅에 공유된 유산 카드를 프로필 유산 타일과 동일한 디자인으로 통일**
채팅의 유산 공유 카드는 프로필의 `LegacyStoneTile`(어보니+브래스 라운딩 사각 블록, 우상단 삐져나온 등급 배지)과 별개로, 원형 배지+텍스트 두 줄+"유산 보기" 버튼으로 구성된 전용 카드였다. `LegacyStoneTile`에 `size`(px) prop을 추가해(프로필의 3칸 행 `flex-basis` 대신 고정 폭으로도 쓸 수 있게) 채팅 카드를 이 컴포넌트로 교체했다 — 이제 정확히 프로필과 같은 재질·배지 배치이며, 타일 자체가 클릭하면 재생되는 인터랙티브 요소라 별도 "유산 보기" 버튼도 없앴다(편집·공유 아이콘은 넘기지 않아 보기 전용으로 동작). 배지 아이콘이 블록 밖으로 삐져나오는 디자인이라, 기존에 있던 `overflow:hidden` 카드 래퍼도 함께 제거했다(안 그러면 배지가 잘려 보였을 것).

**기능 — 채팅 목록에 대화 고정·알림 끄기·삭제**
대화(conversation) 전용 테이블 없이 `chat_messages`를 상대별로 묶어 쓰는 기존 구조에, 대화방별 "나만의" 설정을 담을 새 테이블 `chat_conv_prefs`(uid, other_uid, pinned, muted, cleared_before)를 추가했다. 고정/알림 끄기는 본인 소유 행이라 일반 RLS(`auth.uid()=uid`)로 자유롭게 읽고 쓰지만, 대화 삭제("나에게서만 삭제")는 상대방의 워터마크까지 봐야 영구 삭제 여부를 판단할 수 있어(내 RLS로는 상대 행을 못 읽음) `chat_edit_message`와 같은 이유로 `chat_clear_conversation` SECURITY DEFINER RPC로 처리한다 — 내 `cleared_before`를 지금 시각으로 올려 두고, 상대방도 이미 자신의 워터마크를 기록해 뒀다면(=상대도 그 대화를 지운 적이 있다면) 두 워터마크 중 더 이른 시점까지의 메시지를 서버에서 영구적으로 삭제한다. `ChatsModal`은 목록을 불러올 때 `chat_conv_prefs`도 함께 가져와, `cleared_before` 이전 메시지는 목록 계산 자체에서 제외하고(그 대화가 새 메시지가 오기 전까지는 목록에서 사라진다), 고정한 대화는 안정 정렬(stable sort)로 항상 맨 위로 올린다(같은 고정 여부 안에서는 기존 최근 메시지 순서가 그대로 유지됨). `ChatPanel`도 `ChatsModal` 목록을 거치지 않고 곧장 열리는 경로(친구 프로필에서 채팅 시작 등)에서 같은 워터마크가 적용되도록 별도로 `chatConvPrefGet`을 호출한다. 대화 행을 꾹 누르거나(480ms, 메시지 롱프레스와 동일 임계값) 오른쪽 클릭하면 고정/고정 해제·알림 받기/끄기·삭제 메뉴가 뜨고(`safeAreaDx`로 화면 가장자리 잘림 방지), 삭제는 되돌릴 수 없는 동작이라 친구 삭제와 같은 패턴으로 한 번 더 확인받는다. 고정한 대화는 이름 옆에 채워진 핀 아이콘이 뜬다. 전역 채팅 배지(`checkUnreadChat`)와 `ChatPanel`의 안읽음 처리도 알림을 꺼 둔 대화·지운 시점 이전 메시지는 집계에서 제외하도록 함께 고쳤다.

**기능·버그 수정 — 유산 낙하 충격 물리 연출과 그 뒤 타이핑 정렬 버그**
`LegacyRevealScreen`의 "유산 수가 정중앙에 등장 → 주변 수가 무너져 사라짐" 연출(v0.3.3)을 "위에서 떨어져 부딪히는 낙하 충격 → 그 충격이 파동처럼 퍼져 주변 수가 튕겨나가며 흩어짐"으로 다시 만들었다. 유산 수 자신은 `initial={{y:-220}}` → `animate={{y:0}}`를 `type:"spring"`(질량-감쇠 진동 물리, `stiffness:380, damping:13`)으로 재생해 실제로 한두 번 튀어오르는 반동을 남기며 자리를 잡는다(트윈이 아니라 진짜 스프링 시뮬레이션). 주변의 이전 수 토큰은 새 컴포넌트 `LegacyShatterToken`이 두 단계 스프링을 순서대로 재생한다 — (1) 충격에 짧게 눌렸다 튀어오르는 반동(뻣뻣하고 감쇠가 약한 스프링, `stiffness:900, damping:11`), (2) 그 반동을 타고 옆으로 흩어지며 사라지는 궤적(부드럽고 감쇠가 강한 스프링, `stiffness:110, damping:14` + 별도 tween으로 불투명도만 0.35초에 걸쳐 페이드). 각 토큰의 지연(delay)은 배열 인덱스 순서 대신 유산 수(충격 지점)로부터 떨어진 거리(`moveIndex-1-i`)에 비례하도록 고쳤다 — 예전엔 유산 수와 가장 가까운 마지막 토큰이 인덱스가 가장 커서 오히려 가장 늦게 반응해, 파동이 먼 곳에서 가까운 곳으로 거꾸로 퍼지는 것처럼 보였다. 이 물리 연출이 예전보다 오래 걸려(거리 기반 지연 + 2단계 스프링), 다음 단계로 넘어가는 타이머(`HERO_SOLO_MS`)도 1500→1600ms로 늘렸다.

그 다음 유산 수가 왼쪽 위 제자리로 옮겨간(`layoutId` FLIP) 뒤 나머지 수가 이어서 타이핑될 때 위치가 어긋나 보이던 버그의 원인을 찾았다 — 기보 타이핑 영역(`typingRef`)을 항상 맨 아래로 스크롤하는 효과가 `[typedCount, afterTypedCount, typingStage]`가 바뀔 때마다(FLIP이 시작되는 바로 그 틱 포함) 동기적으로 `scrollTop`을 강제로 바꾸고 있었는데, 이 시점이 정확히 framer-motion이 FLIP의 "도착 위치"를 측정하는 시점과 겹쳐, 측정 직후 스크롤이 다시 콘텐츠를 밀어내며 정렬이 어긋나 보였다(실제로 넘칠 일이 거의 없는 순간에도 매번 스크롤을 강제 실행하고 있었다는 것도 원인의 일부). `el.scrollHeight > el.clientHeight`일 때만(실제로 넘칠 때만), 그것도 `requestAnimationFrame`으로 현재 프레임의 레이아웃이 커밋된 뒤에만 스크롤하도록 바꿔 FLIP의 레이아웃 측정과 같은 틱에서 겹치지 않게 했다. 이 김에 FLIP 전환 자체의 트랜지션(`easeInOut` 트윈)도 낙하 물리와 결을 맞춰 스프링(`stiffness:300, damping:26`)으로 바꿨다.

**버그 수정 — 일일 퀘스트 chess.com 링크가 여전히 앱 대신 웹사이트로 열리던 문제**
v0.3.3에서 `target="_blank"`를 제거했는데도(같은 탭 이동으로 바꿔도) 여전히 웹사이트로만 열린다는 신고. `www.chess.com` 도메인은 이 개발 환경의 네트워크 정책상 직접 접근이 차단돼 있어 그 사이트의 "앱으로 열기" 버튼이 실제로 무엇을 하는지 직접 확인할 수는 없었다. 다만 iOS는 사용자가 그 도메인을 "브라우저에서 열기"로 한 번이라도 선택한 적이 있으면(또는 그 경로가 애초에 유니버설 링크 대상에 없으면) 이후로도 계속 웹사이트로만 여는 잘 알려진 제약이 있고, chess.com 자체 사이트에 별도 버튼이 있는 것도 순수 유니버설 링크만으로는 이 상황을 못 벗어나기 때문으로 보인다. 순수 https 링크 하나로 그 버튼과 똑같은 내부 동작을 재현할 수는 없어, 스마트 배너가 쓰는 표준 폴백 패턴을 `ChesscomTextLink`에 추가했다 — 평소처럼 https 링크로 이동을 시도하고, `CHESSCOM_APP_FALLBACK_MS`(1.5초) 안에도 이 페이지를 실제로 떠나지 않으면(`visibilitychange`/`pagehide` 미발생 = OS가 앱으로 가로채지 못한 것) `navigator.userAgent`로 판별한 플랫폼에 맞춰 앱스토어(`apps.apple.com/app/id329218549`)·플레이스토어(`play.google.com/store/apps/details?id=com.chess`)로 대신 보낸다. 앱이 이미 설치돼 있으면 스토어 링크를 열 때 OS가 스토어 대신 앱을 직접 여는 경우가 많고, 설치돼 있지 않더라도 최소 한 번의 탭으로 설치 페이지까지는 확실히 도착한다. 데스크톱(플랫폼 판별 실패)은 폴백 없이 평범한 링크 그대로 둔다.

**버그 수정 — "리뷰한 대국만" 필터가 예전에 리뷰한 대국도 없다고 보여주던 문제**
`reviewGameKey(game)`은 `white.username + black.username + endTime`을 조합해 대국을 식별하는데, `game.white`/`game.black`은 원래 상대 쪽 정보까지 담도록 나중에(19차) 추가된 필드였다 — 그런데 이 필드가 늘어난 시점에 `useChessCom`의 로컬 캐시 버전(`CHESSCOM_CACHE_VERSION`)이 함께 올라가지 않아, 아직 최신 달이 아니라서 다시 안 받아 온(캐시를 그대로 신뢰하는) 옛 대국 데이터는 `white`/`black` 키 자체가 없는 구버전 형태로 `localStorage`에 계속 남아 있을 수 있었다. 이 경우 `reviewGameKey`가 `null`을 반환하고, `openReview`는 `if (key && ...)` 조건 때문에 리뷰를 열어도 `reviewUnlocked`에 전혀 기록하지 않은 채(티켓도 소모하지 않고) 그대로 리뷰 페이지를 띄웠다 — 사용자 입장에서는 "분명히 리뷰했는데" 그 기록이 애초에 한 번도 저장된 적이 없는 상태였던 것이다(반대로 "리뷰한 대국만" 필터에서도 당연히 걸러졌다). `reviewUnlocked`의 저장·복원 배선(로컬스토리지+Supabase `user_progress`, 값이 바뀔 때마다 저장) 자체는 정상이었다 — 저장이 안 된 게 아니라 저장할 키가 애초에 만들어지지 않았던 것. `CHESSCOM_CACHE_VERSION`을 1→2로 올려 옛 캐시를 무효화했다 — 이후 첫 방문에서 모든 달을 새 스키마로 한 번 다시 받아 오므로, 그 뒤로는 모든 대국이 항상 `white`/`black`을 갖게 되어 이 문제 자체가 재발하지 않는다(과거에 이미 유실된 기록 자체는 애초에 저장된 적이 없어 복구할 방법은 없다).

**기능 — 퍼즐 라인 자동 진행**
`PuzzleSolver`는 한 라인을 다 풀면 완료 배너와 함께 "다음 라인 풀기" 수동 버튼만 보여줬다(자동 전환 기능은 원래 없었다 — git blame으로 확인, v0.3.3의 라인 선택 버튼 커밋도 이 부분은 건드리지 않았다). 클리어 애니메이션(`celebrate`)이 스스로 꺼지는 시점(2.4초)에 맞춰, 모든 라인을 다 푼 게 아니라면(`!fullyComplete && nextTag != null`) 자동으로 `gotoLine(nextTag)`를 호출하도록 그 타이머 effect를 확장했다 — `gotoLine` 자체가 `celebrate`를 함께 초기화하므로, 사용자가 그 사이 라인 번호 버튼이나 "다음 라인 풀기"를 직접 눌러 먼저 이동해도 이 타이머가 예약되지 않아(`celebrate`가 이미 null) 중복 전환이 없다. 이 effect는 `nextTag`/`fullyComplete`(둘 다 `totalLines`·`solvedNow`에서 파생) 선언보다 앞에 있던 기존 위치에서는 참조할 수 없어(TDZ), 두 값이 선언된 지점 바로 뒤로 옮겼다.

**버그 수정 — 퍼즐 오답 시 상대 응징 수가 안 뜨던 문제**
오답을 두면 1초 뒤 `engine.evaluate`로 최선 응수를 계산해 보여주는데, 이 호출이 `engine.status === "ready"`일 때만 실행되고 있었다 — 앱을 켜자마자(엔진 워커가 아직 WASM을 부팅하는 중) 곧장 퍼즐부터 푸는 흔한 경로에서는 이 시점에 엔진이 아직 준비되지 않아 조용히 포기하고 오답만 되돌아가, "응징 연출이 아예 안 뜬다"는 신고와 정확히 부합했다. `liveOn`이 켜져 있고 `engine` 인스턴스 자체는 있는데 아직 `status`만 "ready"가 아니라면, 최대 4초까지 150ms 간격으로 짧게 기다렸다가 계산하도록 고쳤다(`liveOn`이 꺼져 있거나 그 시간 안에도 준비되지 않으면 예전처럼 포기하고 오답만 되돌린다).

**UI — 퍼즐 라인 버튼의 해결/진행 표시를 서로 독립된 신호로 분리**
힌트 버튼 아래 라인 번호 원형 버튼은 "지금 풀고 있는 라인"(`isTarget`)이면 채워진 금색 배경으로, 그 외엔 해결 여부에 따라 테두리·글자색만 달리해 숫자를 보여줬다 — `isTarget` 스타일이 다른 모든 상태를 덮어써, 이미 푼 라인을 다시 선택하면(재도전) 방금 그 라인이 풀렸던 라인인지 시각적으로 전혀 알 수 없었다(둘 다 그냥 금색 원에 숫자). 해결 여부는 배경·아이콘(초록 배경 + 체크 아이콘, 숫자 대신)으로, 지금 풀고 있는지는 테두리 색(금색)만으로 표시하도록 두 신호를 분리했다 — 이제 두 상태가 겹쳐도(이미 푼 라인을 지금 다시 풀고 있는 경우) 초록 체크(해결됨)와 금색 테두리(지금 여기)가 동시에 보여 한눈에 구분된다.

**기능 — 퍼즐 생성 라인 종료 조건에 "기물 이득 확정" 규칙 추가**
`genPuzzleTree`는 사용자 수를 둔 뒤 기존엔 오직 `target`(누적 cp 이득) 기준(포지션 우위 테마) 또는 순수 기물 증가(기물 우위 테마)로만 라인 종료를 판단했다. 요청대로 새 독립 조건을 추가했다 — 그 수 이후 기물 이득이 3점 이상(`MATERIAL_LOCK_MIN_GAIN`)이고, 평가치가 절대값으로 2점(200cp, `MATERIAL_LOCK_MIN_CP`) 이상 유리하며, 그 기물 이득이 최소 5수(`MATERIAL_LOCK_PLIES`) 뒤까지도 유지된다면 기존 `target` 기준을 만족하지 못했더라도 곧바로 그 수에서 라인을 끝낸다. "5수 유지" 검증은 새로 엔진을 호출하지 않고, 이 위치 평가 시 이미 받아 둔 멀티PV 결과(`pvs2[0].pv`)를 `pvUciToSans`로 그대로 시뮬레이션해 각 수마다 기물 이득이 3점 밑으로 떨어지지 않는지 확인한다(PV가 5수보다 짧으면 지속을 증명할 수 없다고 보고 이 규칙을 적용하지 않음) — 순간적으로만 유리해 보였다가 몇 수 뒤 되잡기 등으로 다시 균형을 찾는 얕은 전술은 걸러진다. `puzzleType`(포지션/기물 우위)과 무관하게 항상 함께 적용되며, 테마별 조건(`requireMaterialRecovery`/`requireCapture`)은 그대로 함께 검사한다.

**기능 — 개발자 전용 "전체 퍼즐 일괄 재생성" 도구**
사용자 요청: 새 라인 종료 규칙(바로 위 항목)을 기존에 이미 만들어진 모든 퍼즐에도 소급 적용해 달라는 것. `genPuzzleTree`는 브라우저 WASM 엔진(`getAnalysisPool`)에 강하게 결합돼 있어 Node 스크립트로 서버에서 일괄 실행할 방법이 없다(`scripts/` 전체를 확인 — Stockfish를 Node에서 구동하는 코드는 `refresh-data.mjs`의 오프닝 이론 수 평가용뿐이고 퍼즐 트리와는 무관, `genPuzzleTree`를 호출하거나 Supabase 클라이언트를 쓰는 스크립트는 전무). 기존 재생성 기능(`regenerateWithTarget`)도 개발자가 화면에 열어 둔 퍼즐 1개만 재생성해 `CONTENT.puzzleOverrides`(전역 `app_content` 단일 행)에 저장하는 구조라, 퍼즐이 수천 개 규모일 수 있는 크라우드소싱 구조(`puzzles` 테이블에 상한 없음, 각 퍼즐이 `{no, data}` 독립 행)에는 맞지 않는다(모든 결과를 하나의 JSON 블롭에 누적하면 매 저장마다 그 전체를 다시 올려야 해 사실상 못 씀).

새 컴포넌트 `PuzzleBatchRegenPanel`(설정 탭, `canEdit` 게이트 — 개발자/공동 개발자 모드 전용)을 만들었다. 새 헬퍼 `puzzleListAllNos()`가 PostgREST 기본 반환 행 수 제한을 `limit`/`offset` 페이지네이션으로 우회해 존재하는 모든 퍼즐 번호를 모아 오면, `regenerateWithTarget`과 정확히 같은 방식(같은 `puzzleThemeOpts`·"희생" 테마의 `firstSan` 고정 로직)으로 번호 순서대로 하나씩 `genPuzzleTree`를 돌려 결과를 **`puzzles` 테이블에 직접**(override 경유 없이) `sbUpsert`한다 — `getAnalysisPool`은 프로필별로 메모이즈돼 있어 수천 번을 순회로 호출해도 워커가 누적되지 않는다. 재생성한 퍼즐 번호에 예전 `CONTENT.puzzleOverrides` 항목이 남아있으면 함께 지운다(`puzzleTreeOf`가 override를 `p.tree`보다 우선시켜, 안 지우면 방금 새로 쓴 트리가 계속 가려짐). 진행률·현재 처리 중인 번호·실패 목록을 보여주고 언제든 중단할 수 있으며(브라우저 탭을 열어 둔 채로 개발자가 직접 실행해야 함 — 이 저장소 개발 환경은 실제 프로덕션 Supabase 접근 권한이 없어 이 도구를 직접 실행해 볼 수는 없었다), 이 작업은 라인 태그를 전부 새로 발급하므로 기존 유저의 라인별 풀이 기록(`lineSolves`)이 재생성된 퍼즐과 더 이상 안 맞을 수 있다는 위험을 사용자에게 먼저 확인받았다(베타 단계라 감수하기로 함).

**기능 — MEC 규칙에 체크메이트 추가(최상위 규칙) + 유일한 수·탁월한 수 코멘트에 체스 표기 기호 반영**
`mecFacts`(학습 탭·게임 리뷰의 "코치" 코멘트 생성부)에 체크메이트 규칙을 추가하고, 기존 최상위였던 "수비자 제거"(`removeDefenderFact`)보다도 먼저 확인하도록 함수 맨 앞(`facts` 배열 초기화 직후)에 배치했다 — `san`이 `#`로 끝나면(SAN 표기 규칙, `genPuzzleTree` 등 다른 곳에서도 이미 쓰는 판정) 다른 어떤 사실도 계산하지 않고 즉시 반환한다. `kind`(그 수의 등급)에 따라 세 갈래로 고정 문구를 쓴다(수비자 제거와 같은 이유로 무작위 변주 없음) — 탁월한 수(`kind==="brilliant"`, 대개 희생으로 만든 외통)는 실제로 그 수를 둔 기물(`sanSrc`로 조회)을 문장에 넣어 "당신의 (기물)은 상대의 킹과 맞바꾸게 될 거에요!!", 유일한 수(`kind==="only"`)는 "체크메이트를 향한 유일한 길을 찾아냈어요!", 그 외 등급은 "체크메이트! 이 수로 게임이 끝나요."로 고정했다.

별개로, 체크메이트가 아니더라도 유일한 수·탁월한 수로 뽑힌 코멘트는 끝에 그 등급을 상징하는 체스 표기 기호를 붙이도록 `mecFacts` 맨 끝에 후처리를 추가했다 — 유일한 수는 "!" 1개, 탁월한 수는 "!!" 2개(표준 체스 기보 표기에서 "!"=좋은 수/유일수, "!!"=탁월한 수인 관례를 그대로 반영). 실제로 화면에 붙는 건 `facts[0]`뿐이라(호출부 `ReviewCoachCard`가 그렇게 씀) 거기에만 적용하고, 문장이 이미 마침표로 끝나 있으면 그 자리를 대신해(정규식으로 끝의 `.`/`!`/`?`를 지운 뒤 붙임) "...예요.!!"처럼 부호가 겹쳐 보이지 않게 했다. (참고) 사용자가 예시로 든 두 체크메이트 문구는 마침표 개수가 뒤이어 요청한 "유일한 수 1개·탁월한 수 2개" 규칙과 반대로 적혀 있었다 — 체스 기보의 실제 관례(!!=탁월, !=유일)와 뒤 문장의 명시적 개수 규칙 쪽을 따랐다(탁월한 수=!!, 유일한 수=!). 의도와 다르면 알려주면 바로 맞바꾸겠다.

**버그 수정 — 유산 재생에서 무너짐 연출이 유산 수 등장과 시간을 추측해 겹칠 수 있던 문제**
`LegacyRevealScreen`의 "heroSolo" 단계는, 유산 수가 정중앙에 스프링 물리로 낙하해 부딪히는 동안 주변 수(`LegacyShatterToken`)가 그 충격의 파동으로 무너지기 시작하는데, 예전엔 무너짐 시작 시점(`WAVE_BASE_MS`=260ms)이 "유산 수 스프링이 대략 이쯤 바닥을 찍을 것"이라는 추측값이었다 — 기기 성능이나 리렌더 지연에 따라 유산 수가 아직 화면에 다 나타나기도 전에 주변 수가 먼저 무너지기 시작할 수 있었다(사용자 요청: "유산 수의 기보가 확실하게 등장한 뒤 무너짐을 시작할 것"). 유산 수 `motion.span`의 `onAnimationComplete`(framer-motion이 스프링이 실제로 정착했을 때만 호출)로 새 상태 `heroLanded`를 켜고, `LegacyShatterToken`의 `active`를 이 값에 직접 연결했다 — 이제 무너짐은 시간 추측이 아니라 "유산 수가 실제로 다 등장했다"는 사실 자체로만 시작된다. 시작 시점이 그만큼 늦어진 만큼(추측값 260ms보다 늦게, 실제 착지 이후) `WAVE_BASE_MS`는 착지 직후의 짧은 호흡(80ms)으로 줄이고, 전체 단계 길이(`HERO_SOLO_MS`)는 늦어진 시작을 감안해 1600→2000ms로 늘렸다.

**버그 수정 — 도감 오프닝 트리의 수 설명 카드가 여전히 잘리고, 팬/줌 도중 말풍선 모양이 뒤틀리던 문제**
`OpeningSchematic`의 수 설명 카드(`DexMoveBlock`)는 v0.3.3에서 `position:fixed`(뷰포트 좌표계)로 옮겨졌지만, 그 좌표(`left`/`top`/`tailPos`) 자체는 여전히 매 렌더 `pan`/`zoom`을 다시 읽어 앵커(그 수 블록)의 화면상 위치를 실시간으로 계산했다 — 카드가 "뷰포트 기준"이라는 것과 "카드가 앵커를 계속 따라다닌다"는 것은 별개 문제였는데, 후자가 남아 있었던 것이 근본 원인이었다. 드래그·휠 확대 도중 그 앵커 위치가 프레임마다 바뀌면서 카드의 clamp된 좌표도 함께 흔들려, 말풍선 꼬리(`tailPos`)가 매끄럽지 못하게 각도를 바꾸며 뒤틀려 보였고, 클램프가 한 프레임 늦게 새 위치를 따라잡는 순간에는 카드 일부가 화면 밖으로 잠깐씩 잘려 보였다(사용자 재현: "여전히 잘리는 문제").
사용자 요청대로 카드를 "그냥 고정된 좌표에" 생성하도록 고쳤다 — `frozenCard`라는 새 `useMemo`(의존성 배열이 `[openKey]`뿐)가 카드를 연 그 순간의 `pan`/`zoom`으로 `left`/`top`/`tailPos`를 딱 한 번만 계산해 두고, `openKey`가 바뀌기 전까지는 pan/zoom이 이후 아무리 바뀌어도 그 값을 다시 계산하지 않는다(이전엔 렌더 안 IIFE로 매번 다시 계산했다). 그 대신 새 `useEffect`가 `pan`/`zoom`을 감시하다가, 카드가 열려 있는 동안 실제로 그 값이 얼려 둔 시점과 달라지면(=사용자가 스크롤/팬하거나 확대·축소하면) `onToggleOpen`으로 카드를 그냥 닫아버린다 — "스크롤하면 같이 사라지도록 해줘"라는 요청 그대로다. 검색·키보드 이동으로 카메라가 날아가는 도중(`centerOn`/`flyAlongPath`)에는 카드가 아직 닫혀 있어(`selectNode`가 먼저 기존 카드를 닫고 이동이 끝난 뒤에야 새로 연다) 이 감시 로직이 이동 애니메이션 자체를 오작동으로 오인해 곧바로 닫아버리는 일은 없다. 결과적으로 카드는 열려 있는 동안 항상 연 순간 그대로의, 뒤틀리지 않고 화면 안에 온전히 들어오는 고정된 모양으로만 보인다.

**기능 — 학습 탭 "수 설명"에 작성자 본인 편집권 추가**
`move_notes`(학습 탭 수 설명 게시판)는 v0.2.9부터 로그인한 모두가 글을 남길 수 있었지만, 정작 수정·삭제는 작성자 본인도 못 하고 `is_content_editor`(개발자/공동개발자)만 할 수 있었다(`MoveNoteCard`가 `canModerate` prop 하나로만 편집 버튼을 게이트). 사용자 요청대로 `MoveNoteCard`에 `uid` prop을 추가해 `canEditThis = canModerate || n.uid === uid`로 바꿨다 — 자기 글이면 개발자가 아니어도 편집·삭제 버튼이 뜬다. 서버 쪽(`supabase-setup.sql` 15번 섹션)도 함께 맞춰, `move notes update`/`delete` RLS policy의 `using`/`with check` 조건을 `public.is_content_editor(auth.uid())` 단독에서 `auth.uid() = uid or public.is_content_editor(auth.uid())`로 넓혔다 — UI만 바꿔서는 REST를 직접 호출하는 우회를 막을 수 없으므로 실제 방어선은 이 RLS다. 안내 문구("이미 이 수에 설명을 남겼어요 — 수정·삭제는 개발자에게 문의해주세요.")도 "위 카드에서 직접 수정하거나 지울 수 있어요"로 바꿨다.

**기능 — 퍼즐 생성자 표시, 본인 퍼즐 편집권(1시간 주기), 개발자의 재지정**
지금까지 `puzzles` 테이블은 "no"만으로 식별되는 완전한 크라우드소싱 데이터라 어느 유저가 처음 만들었는지 전혀 기록하지 않았다. `creator_uid`/`creator_username`/`creator_edited_at` 세 컬럼을 추가하고(`supabase-setup.sql` 20번 섹션), `data`(퍼즐 본문 jsonb) 자체는 기존처럼 누구나 갱신할 수 있어야 하므로(테마 태그 병합 크라우드소싱 유지) 생성자 식별 정보는 `data` 밖 별도 컬럼으로 분리했다 — 이 컬럼들은 REST 직접 update grant가 아예 없고, 아래 세 SECURITY DEFINER RPC로만 바뀐다(테이블 소유자 권한으로 실행되어 grant를 우회).
- `puzzle_claim_creator(no)` — `puzzleShare`(퍼즐을 서버에 새로 공유하는 기존 함수)가 매번 호출한다. 그 번호에 아직 생성자가 없을 때만(멱등) 지금 공유를 실행한 로그인 유저를 생성자로 선점한다.
- `puzzle_creator_save(no, tree, lines, tagSeq?, target?, puzzleType?)` — 라인 추가/삭제·재생성 저장 창구. 개발자/공동개발자(`is_content_editor`)는 언제든, 그 퍼즐의 생성자 본인은 `creator_edited_at` 기준 1시간이 지난 뒤에만 호출할 수 있다(그 외엔 서버가 `not_puzzle_creator`/`edit_cooldown` 예외로 거부). 개발자가 예전에 이 번호에 만들어 둔 `CONTENT.puzzleOverrides`(app_content, `puzzleTreeOf`가 `data.tree`보다 항상 우선시키는 블롭) 항목이 있으면 함께 지워, 생성자가 여기 저장한 내용이 다음 로드 때 가려지지 않게 한다.
- `puzzle_reassign_creator(no, targetUsername?)` — 개발자/공동개발자 전용. `targetUsername`이 비어 있으면 개발자 계정(DEV_ACCOUNT) 명의로 회수, 있으면 그 아이디의 유저에게 양도한다 — 둘 다 원래 생성자의 그 퍼즐 편집권을 그 즉시 박탈하는 효과다.

클라이언트(`PuzzleSolver`)는 퍼즐을 열 때마다 `puzzleCreatorInfo(no)`로 생성자를 새로 조회해(개발자가 방금 재지정했을 수도, 1시간 주기가 지났을 수도 있으므로 캐시하지 않는다) 풀이 카드에 "제작 · @아이디"로 표시한다. `isMyPuzzle`(생성자 uid === 내 uid)이고 1시간 주기가 지났으면(`creatorCooldownMs <= 0`) `canEditPuzzle`이 켜져, 기존에 개발자(`canEdit`) 전용이던 모식도 편집 도구(라인 +/- , 형제 추가, "이 기준으로 기본 트리 재생성") 전체가 그대로 열린다 — 저장 창구만 편집 주체에 따라 갈린다(`persistEdit`): 개발자는 예전 그대로 `CONTENT.puzzleOverrides`+`bumpContent()`에, 생성자는 `puzzleCreatorSave` RPC로 `puzzles.data`에 직접. 아직 주기가 안 지났으면 도구 대신 "약 N분 뒤에 다시 조정할 수 있어요" 안내만 보인다. 개발자에게는 별도로 "퍼즐 생성자" 패널(현재 생성자 표시 + 아이디 입력 후 양도 / "개발자 명의로 회수" 버튼)을 추가했다.
(참고) 개발자가 이 퍼즐 번호에 남긴 `CONTENT.puzzleOverrides`를 생성자 저장 시점에 지우는 것으로 대부분의 충돌을 막았지만, 정확히 "생성자가 편집하는 바로 그 순간과 겹치는 극히 드문 타이밍"에 개발자가 같은 번호에 새로 override를 만드는 경우까지는 다루지 않는다 — 배타적 락 없이 두 저장 창구가 별개로 존재하는 구조상 남는 이론적 엣지 케이스이며, 실제로 마주칠 가능성은 매우 낮다고 판단해 그대로 두었다. 또한 비로그인 게스트가 퍼즐을 최초로 공유하면(`auth.uid()`가 없어) 생성자가 기록되지 않고, 이후 처음 로그인 상태로 그 퍼즐을 다시 공유하는 유저가 대신 생성자가 된다.

**기능 — 게임 리뷰·퍼즐 풀이 창에 딥링크 가능한 고유 URL**
사용자 요청: 리뷰 페이지는 `openchess.kr/review/(대국 식별자)`, 퍼즐 풀이 창은 `openchess.kr/(퍼즐 번호)-(라인 번호)` 형태의 고유 URL을 갖고, 그 URL을 그대로 열면 실제로 같은 화면이 다시 열려야 한다(단순 표시용이 아니라 진짜 딥링크). 세 대국 소스마다 식별자를 다르게 만든다(`reviewGameIdentifier`):
- **chess.com 대국** — chess.com이 이미 발급한 숫자 게임 ID(대국 URL 끝자리, 예: `.../game/live/12345678` → `12345678`)를 그대로 쓴다. `useChessCom`이 대국을 파싱할 때 이 값을 `extractChesscomGameId(g.url)`로 함께 저장하도록 스키마를 바꿔(`CHESSCOM_CACHE_VERSION` 2→3, 옛 캐시는 자동 무시하고 다시 받아 옴) 게임 리뷰를 여는 네 경로(`onOpenMyGameAnalyze`, 프로필·검색의 `onOpenGameAnalyze` 두 곳) 모두에 실었다. chess.com 공개 API는 "이 ID의 대국 하나"를 바로 조회하는 엔드포인트가 없어(플레이어별 월간 아카이브로만 조회 가능), 딥링크를 만든 사람이 아닌 다른 방문자가 열면 원본을 되찾을 방법이 없다 — 새 테이블 `reviewed_games`(puzzles와 동일한 크라우드소싱 패턴, `supabase-setup.sql` 21번 섹션)에 chess.com 대국을 리뷰로 열 때마다 올려 두고(`reviewedGameShare`, fire-and-forget), 딥링크는 여기서 복원한다(`reviewedGameFetch`) — 아직 아무도 그 ID를 리뷰로 연 적이 없으면 복원할 수 없어 `/learn`으로 조용히 되돌린다.
- **FEN 분석** — 학습 탭 "FEN 모드"에서 아직 한 수도 안 둔 채(그 위치 자체를 분석) 연 리뷰는 그 FEN 코드 자체를(URL 세그먼트라 `encodeURIComponent`는 거치되, 디코딩하면 정확히 원래 문자열) 그대로 식별자로 쓴다. FEN 모드의 "분석" 버튼은 이제까지 완전히 비활성화돼 있었는데("FEN 모드에서는 사용할 수 없어요") 이번에 활성화했다 — 그러려면 표준 시작 위치만 전제하던 `analyzeGame`(게임 리뷰 채점 엔진)이 임의 FEN 시작을 받을 수 있어야 해서, `boardToFen`에 시작 진영(`startColor`) 선택 인자를 추가하고 `analyzeGame`도 `fenRoot`(`parseFenFull`이 돌려주는 `{board,turn,rights,ep}`)를 선택적으로 받아 보드·캐슬링 권리·앙파상·백선/흑선 판정(`plyIsWhite`) 전반을 그 위치 기준으로 계산하도록 고쳤다(기존 호출부는 인자를 안 넘기므로 완전히 하위 호환). `ReviewPage`도 같은 방식(`replayFromFen`, LearnTab의 FEN 모드와 동일한 함수)으로 메인 보드·앙파상·진영을 fenRoot 기준으로 재생한다.
  (알려진 한계, 의도적 축소) FEN 리뷰에서 실제 둔 수의 등급·정확도·평가 그래프·코치 코멘트(핵심 기능)는 fenRoot를 정확히 반영하지만, 자유 탐색(가상의 수를 더 둬 보기)과 실시간 엔진 상위 3줄 패널은 이번 범위에서 껐다 — `legalDests`의 캐슬링 판정이 "지금 킹/룩이 원위치인지"만 보고 그 전에 이미 잃은 권리는 추적하지 않는 기존 단순화, `gameEndState`(3회 반복 판정)·`pvUciToSans`(엔진 라인 패널의 PV→SAN 변환)가 표준 시작을 전제하는 것까지 전부 fenRoot에 맞춰 고치는 건 이 작업 범위를 크게 넘어서, 대신 `interactive={!fenRoot}`로 보드 클릭·드래그 자체를 막아 잘못된 판정이 아예 노출되지 않게 했다.
- **그 외(직접 둔 수순·PGN 붙여넣기·마스터 대국 리뷰, 또는 FEN에서 시작해 수를 더 둔 경우)** — sans를 PGN 텍스트로 합쳐(FEN에서 시작했다면 `[FEN]`/`[SetUp]` 헤더까지 포함해 시작 위치까지 완전히 복원 가능하게) Web Crypto `AES-GCM`으로 암호화한 뒤 그 결과(IV+암호문을 이어붙여 base64url)를 식별자로 쓴다. 키는 클라이언트 코드 안의 고정 문자열을 SHA-256으로 해시해 만든다 — 진짜 비밀을 지키는 용도가 아니라(키 자체가 번들에 있어 복호화 가능) URL을 짧고 안 읽히게 만드는 목적이며, 이 경우는 식별자 자체가 내용을 전부 담고 있어 서버 캐시가 필요 없다. 되돌릴 때(`resolveReviewIdentifier`)는 접두사 없이 구조로만 세 가지를 구분한다 — 순수 숫자면 chess.com ID, 디코딩해서 `looksLikeFen`에 맞으면 FEN, 그 외에는 복호화를 시도한다(base64url은 `/`가 절대 안 나와 FEN의 `/` 8칸 구분 패턴과 구조적으로 겹치지 않는다).

퍼즐 풀이 창은 열려 있는 동안 실제로 보고 있는 라인 번호를 `PuzzleSolver`가 부모에 알려주고(`onLineChange`), 처음 열 때는 `pushState`로 새 히스토리 항목을(뒤로가기로 닫을 수 있도록), 같은 퍼즐 안에서 라인만 바뀌면 `replaceState`로 그 자리만 갱신한다. 딥링크로 `/(퍼즐 번호)-(라인 번호)`를 열면 그 번호를 `puzzleFetch`로 불러와 즉시 열고, `PuzzleSolver`에 새로 추가한 `initialLineNo` prop으로 "미해결 라인부터" 기본값 대신 그 라인으로 바로 이동한다.

브라우저 뒤로가기(`popstate`)·앱을 이 주소로 처음 열거나 새로고침하는 경우 둘 다 지원한다 — 예전엔 `/review`로 직접 들어오면 무조건 `/learn`으로 되돌렸는데, 이제 App 컴포넌트 마지막에 딥링크 해석 전용 effect를 하나 두고(`openReview`/`onOpenPuzzle`이 모두 정의된 뒤라야 재사용할 수 있어 이 자리에 둔다) 식별자를 실제로 복원해 그 리뷰·퍼즐을 연다. 복원에 실패하면(식별자가 깨졌거나, chess.com 대국인데 아직 아무도 캐시에 올린 적 없거나, 퍼즐 번호가 존재하지 않으면) 예전과 같이 조용히 `/learn`으로 되돌린다.

**기능 — 퍼즐 공유 시트에 외부 앱 공유 추가**
사용자 요청: 퍼즐 공유가 인앱 친구 목록뿐 아니라 카카오톡·인스타그램 같은 외부 앱으로도 가능해야 한다. `PuzzleShareSheet`(카드·풀이 화면의 "공유" 버튼이 여는 시트)에 새 `ExternalShareRow`를 추가했다 — 각 앱마다 별도 SDK·API 키를 등록하는 대신 표준 Web Share API(`navigator.share`)에 이 퍼즐의 고유 딥링크(바로 위 기능의 `openchess.kr/(번호)-1` — 항상 1번 라인부터, 공유는 대개 처음 푸는 사람에게 가므로)를 넘긴다. 모바일 브라우저는 이 API 하나로 OS가 그 기기에 설치된 모든 공유 대상 앱(카카오톡·인스타그램 DM·문자 등)을 담은 네이티브 공유 시트를 그대로 보여주므로, 앱마다 따로 연동할 필요 없이 사실상 모든 외부 앱을 지원한다. `navigator.share`가 없는 환경(대부분의 데스크톱 브라우저)에서는 대신 "링크 복사"와, URL만으로 공유 가능한 카카오스토리·X(트위터)·페이스북 바로가기를 보여준다 — 인스타그램은 웹에 "이 링크를 공유받아라"라는 공개 URL 방식 자체가 없어(앱 전용 공유만 지원) 데스크톱 대체 목록에는 넣지 않았다(모바일에서는 Web Share API 경로로 인스타그램 DM 공유가 가능하다).

**기능 — 학습 탭 "수 설명" 개수 한도를 계정 등급에 따라 차등**
사용자 요청: 개발자 계정은 한 수에 무제한으로 설명을 남길 수 있고, 그랜드마스터 티어에 도달한 일반 계정은 2개까지 남길 수 있게 한다(그 외는 기존과 같이 1개). 예전엔 `move_notes` 테이블에 `unique(move_key, uid)` 제약이 걸려 있어 "1인 1수당 1개"가 DB 차원에서 고정돼 있었다 — 이 제약을 걷어내고, 그 자리를 새 SQL 함수 `move_notes_cap()`(계정별 한도를 계산 — `user_progress.progress->>'xp'`가 그랜드마스터 문턱(누적 200,000 — `src/App.jsx`의 `TIER_XP_REQ` 총합과 반드시 함께 맞춰야 하는 숫자)을 넘으면 2, 그 외 1)와 `move_notes_moderate` 트리거의 INSERT 시점 개수 검사로 대신했다(`supabase-setup.sql` 15번 섹션). 처음엔 "개발자 계정"만 무제한으로 좁혀 구현했다가(요청 문구를 문자 그대로 해석), 이어진 사용자 요청으로 공동개발자까지 포함하도록 넓혀 `is_content_editor`(app_content·master_games_dev와 동일한 헬퍼)를 그대로 재사용한다. XP는 이 앱의 티어 체계 전체가 이미 그렇듯 클라이언트가 소유한 값을 그대로 신뢰한다(서버가 직접 XP를 증감시키지 않음 — 7-1번 섹션 참고, 새로운 신뢰 수준을 도입한 것은 아니다).
클라이언트(`MoveExplainBlock`)는 이 한도를 `noteCap` prop으로 받아(App 루트에서 `canEdit`(개발자/공동개발자가 각자 모드를 켜 둔 상태 — 다른 `canEdit`류 권한과 같은 결) 또는 그랜드마스터 여부로 계산해 `LearnTab` → `FocusPanel`까지 그대로 내려보낸다) 이미 남긴 개수가 한도에 닿았을 때만 "이미 남겼어요" 안내로 바꾸고, 그 전까지는 계속 입력창을 보여준다. 실제 강제는 어차피 서버가 최종 결정하므로, 클라이언트 값은 UI를 미리 맞게 보여주는 용도일 뿐이다.

### OpenChess v0.3.3 — 2026/8/12

**성능 — 모바일에서 도감 오프닝 트리를 열면 심하게 버벅이던 문제**
오프닝 트리(`OpeningSchematic`)는 이론 수 최대 4000개·연결선(엣지) 4000개·오프닝 이름 라벨 최대 1500개까지 자라는데, 뷰포트 컬링이 전혀 없어 화면에 안 보이는 것까지 전부 항상 DOM에 그리고 있었다 — 트리가 배경에서 자라는 동안(`useOpeningTreeAuto`의 `bumpVersion`, 220ms 주기로 최대 20초 지속)에는 매 갱신마다 그 전체가 다시 조정(reconcile)됐고, 다 자란 뒤에도 박스섀도우·그라디언트·텍스트 그라디언트(`WebkitBackgroundClip`) 라벨 수천 개짜리 레이어가 항상 DOM에 떠 있어 특히 모바일에서 트리를 열 때마다 렉이 심했다. 방사형 배치(`items`/`edges`/`groups`) 자체는 형제·사촌 간 전역 각도·반지름 계산이 트리 전체를 필요로 하므로 그대로 두고, 실제로 DOM에 그리는 부분만 화면(뷰포트)보다 넉넉하게(뷰포트 크기의 1.4배) 잡은 "그릴 범위" 안으로 제한했다(`cullWindowRef` + `culledItems`/`culledEdges`/`culledGroups`). 팬·줌이 바뀔 때마다 매번 다시 걸러내면 CSS `transform`만으로 부드럽게 팬하던 기존 최적화(v0.0.6)가 무력화돼 오히려 매 프레임 필터링·재조정 비용이 들므로, 그릴 범위를 벗어날 만큼(뷰포트 크기의 0.5배) 실제로 멀리 움직였을 때만, 또는 트리 구조 자체가 자랄 때만(150ms 주기로 드리프트 여부만 저렴하게 확인, `refreshCullWindow`) 다시 계산한다. 연결선은 두 끝 중 하나라도 화면에 그려지는 노드면 남겨, 부모가 범위 밖으로 살짝 벗어나도 자식 쪽 선이 뚝 끊겨 보이지 않게 했다.

**UI — 추천 퍼즐을 데스크톱에서도 한 줄 가로 스크롤로**
"추천 퍼즐" 섹션은 v0.3.2에서 모바일(`narrowPuzzleSearch`, 768px 이하)에만 미해결 목록과 같은 점선 테두리 + 한 줄 가로 스크롤 형태를 적용하고, 데스크톱은 여전히 `grid-template-columns: repeat(auto-fill, 230px)`로 줄바꿈되는 그리드를 쓰고 있었다. 화면 폭과 무관하게 항상 가로 스크롤 한 줄로 보이도록, 그 분기를 없애고 가로 스크롤 레이아웃 하나로 통일했다.

**기능 — 체스보드 드래그 무브 감도 개선**
`Board`의 포인터 기반 드래그 무브(v0.3.0)는 `DRAG_THRESHOLD`(이동량 5px)를 넘어야만 드래그로 인정하고, 손을 뗀 지점이 보드 칸 안에 정확히 들어와야만(`squareFromClient`, `relX`/`relY`가 [0,1) 범위 안) 그 칸으로 인식했다. 빠르게 두는 사용자일수록 손을 뗀 순간의 좌표가 목표 칸을 살짝 지나치거나(특히 가장자리 칸), `pointermove` 샘플링 간격 때문에 마지막 좌표가 칸 경계 바로 밖에서 잡히는 경우가 흔해, 의도한 수인데도 제자리로 취소되곤 했다. `DRAG_THRESHOLD`를 5px→4px로 낮춰(단순 탭은 사실상 0px 이동이라 여전히 뚜렷이 구분됨) 드래그 의도를 더 일찍 인식하고, `squareFromClient`에 보드 가장자리 바깥으로 칸 한 개의 40%(`DROP_TOLERANCE`)만큼 허용 오차를 둬 그 안에서 벗어난 지점은 가장 가까운 칸으로 스냅한다(그보다 훨씬 많이 벗어나면 — 보드 밖 다른 UI를 누른 경우 — 여전히 취소로 처리한다).

**UI — 친구 창을 모바일에서 전체 화면으로**
v0.3.2에서 채팅·프로필·검색 창(`ChatsModal`·`ChatUserProfileModal`·`UserSearchModal`)에 적용했던 모바일 전체 화면 전환(`useNarrow(640)`)이 `FriendsModal`에는 빠져 있었다 — 이번에 같은 패턴으로 맞췄다. 카드 폭·높이·테두리·모서리를 narrow일 때 뷰포트를 꽉 채우도록 바꾸고, 내부 스크롤 영역(프로필 서브뷰, 친구/요청/추가 탭 목록)도 고정 `maxHeight` 대신 남은 공간을 채우는 flex로 바꿨다. `FriendsModal`에 임베드된 미니 채팅(`ChatPanel`)도 이제 `fillNarrow`를 넘겨 모바일에서 전체 높이를 채운다(카드 자체가 이제 전체 화면이라, v0.3.2 당시 이 prop을 넘기지 않았던 이유가 사라졌다).

**UI — 채팅 상세 화면 헤더를 하나로 통합**
`ChatsModal`은 대화를 선택해 들어가도 "채팅"+닫기(X) 래퍼 헤더를 그대로 띄운 채, 그 아래 `ChatPanel`이 다시 ←+"OO님과의 채팅" 자체 헤더를 그려 헤더가 두 겹으로 쌓여 보였다. `ChatPanel`의 헤더를 ←·상대 프로필 사진·아이디(로그인 아이디) 순서로 다시 구성하고(사진이 없으면 이니셜 아바타), `ChatsModal`은 대화가 선택돼 있으면(`chatWith`) 래퍼 헤더 자체를 렌더링하지 않도록 해 헤더 중복을 없앴다 — 대화 목록으로 돌아가려면 `ChatPanel`의 ←, 모달 전체를 닫으려면 그 뒤 목록 화면의 X를 쓴다.

**버그 수정 — 채팅 메시지 수정/삭제 메뉴가 채팅창에 가려지거나 이웃 말풍선과 겹치던 문제**
`safeBubbleAnchor`(v0.2.6, 위/아래로 열 방향과 좌우 안전 오프셋을 계산)는 항상 `window.innerHeight`/`innerWidth`를 기준으로 삼았다. 데스크톱의 메시지 목록(`listRef`)은 고작 320px 높이의 작은 스크롤 박스인데, 그 안 맨 위쪽 메시지는 window 기준으로는 "화면 위쪽 절반"이라 위로 열어도 괜찮아 보였지만, 정작 그 좁은 박스 맨 위라 위로 열면 곧장 박스 밖(채팅창 자체)에 가려졌다 — 실제로 잘리는 경계가 window가 아니라 그 컨테이너였던 것이 근본 원인. `safeAreaDx`/`safeBubbleAnchor`에 `bounds`(기준 컨테이너의 `getBoundingClientRect()`) 매개변수를 추가해, 메시지 메뉴를 열 때는 `listRef`의 경계를 기준으로 방향·오프셋을 계산하도록 고쳤다. 메시지끼리 간격이 좁아(6px) 메뉴가 곧바로 이웃 말풍선과 겹치던 것도, `safeBubbleAnchor`가 열리는 방향으로 남는 여유(컨테이너 경계까지 거리)를 계산해 그 안에서만(최대 14px, `BUBBLE_CENTER_BIAS`) 대화 창 중앙 쪽으로 추가로 띄우는 `dy`를 함께 반환하도록 확장해 완화했다.

**기능 — 퍼즐 모식도 배율을 퍼즐 전용 기준으로 재정의**
퍼즐 모식도(`PuzzleSchematic`)는 도감 오프닝 트리(`OpeningSchematic`)와 줌 상수(`SCHEMATIC_ZOOM_LABEL_BASE` 등)를 그대로 공유하고 있었는데, v0.3.2에서 블록 크기를 약 50% 키운 뒤로는 그 기본 배율("100%")이 다들 과해 매번 50%까지 축소해서 봐야 했다. 오프닝 트리·개발자 트리 에디터에 영향이 가지 않도록 `PUZZLE_ZOOM_LABEL_BASE`(=`SCHEMATIC_ZOOM_LABEL_BASE`의 절반)·`puzzleZoomLabel`·`PUZZLE_ZOOM_STEP`·`snapPuzzleZoom`을 퍼즐 모식도 전용으로 새로 두고, `PuzzleSchematic`의 초기 줌·줌 버튼 3곳만 이 상수로 바꿨다 — raw CSS 배율은 그대로(예전 "50%" 그 값)라 좌표 계산은 손대지 않고, 그 값이 이제 "100%"라고 표시될 뿐이다.

**기능 — 미해금 라인도 끝까지 형태 표시 + 라인 번호**
`PuzzleSchematic`은 아직 두지 않은(미해금) 갈래를 만나면 고스트(내용을 가린 "?" 자리표시자) 하나만 만들고 그 아래는 아예 방문하지 않아, 미해금 라인이 몇 수짜리인지·"라인 N"이 몇 번인지 전혀 알 수 없었다. 트리 순회 함수(`visit`)를 고쳐 revealed 여부와 무관하게 항상 리프까지 재귀하되, 한 번 안 보이는 지점을 지나면 `hidden` 플래그를 자식에게 전파해 그 아래 전부를 ghost로 표시하도록 했다 — 형태(노드·연결선)는 라인 끝까지 그대로 드러나고, 렌더링 단계에서 `it.ghost`일 때만 내용(수·평가치·등급)을 "?"로 가린다. 고스트 아이템도 이제 실제 `node`(과거엔 `null`)를 들고 있어 `it.node.tag`를 그대로 읽을 수 있으므로, 고스트 리프에도 실제 리프와 동일하게 "라인 N"(과 레이팅, 아래 참고)을 보여준다.

**UI — 라인 선택 원형 버튼**
퍼즐 풀이 화면(`PuzzleSolver`)의 힌트 버튼 아래에, 라인이 2개 이상인 퍼즐이면 숫자만 표시한 원형 버튼을 라인 개수만큼 나열했다. 누르면 기존 `gotoLine(tag)`(처음부터·모식도 고스트 클릭이 이미 쓰던 함수)을 그대로 호출해 그 라인을 목표로 즉시 처음부터 풀이를 시작한다 — 이미 푼 라인도 눌러 다시 도전할 수 있다(별 3개짜리 보상은 라인당 최초 1회만 지급되지만, 추천 랭킹용 풀이 이벤트와 아래 레이팅용 풀이 시간은 재도전마다 매번 기록된다).

**UI — 모식도 노드에 평가치 대신 등급 표시**
`PuzzleSchematic`의 각 수 블록은 평가치 숫자(`fmtEvalCp`)와 색깔 있는 등급 배지 아이콘을 함께 보여줬는데, 정확히 어떤 등급인지는 원형 배지의 색·아이콘만으로는 바로 읽히지 않았다. 평가치 숫자 자리를 이미 있던 등급별 한국어 이름 매핑(`QLABEL` — "최고의 수"·"탁월한 수" 등)으로 바꿨다.

**기능 — 퍼즐 레이팅 시스템**
퍼즐(과 그 안의 각 라인)에 100~3000 범위 난이도 레이팅을 매겼다. `puzzleLineBaseRating(setupSans, tree, line)`이 정적 요소만으로 기본 레이팅을 계산한다 — 사용자가 직접 둬야 하는 수의 개수(길이), 각 차례 시작 시점 `tensionFacts`로 센 얽힌 기물 수의 평균(긴장), 그 라인에 포함된 `kind==="only"|"brilliant"` 수의 개수(유일한 수/탁월한 수), 라인을 따라가는 평가치(`evToNumericCp`)의 표준편차(`stdDevOf`, 대리 지표 — genPuzzleTree가 만드는 트리에는 채택되지 않은 후보 수의 평가치가 남지 않아 "최선 수와 다른 후보의 차이"를 문자 그대로 구할 원자료가 없다) 네 요소를 각각 상한을 둔 점수로 환산해 더한다. 퍼즐 전체 레이팅은 `puzzleAverageRating`으로 모든 라인 기본 레이팅의 평균(합÷라인 수)이다.

실제 풀이 시간으로 레이팅을 가감하는 절차도 함께 넣었다 — `PuzzleSolver`가 라인 시도 시작 시각(`solveStartRef`, `gotoLine`·퍼즐 진입 시 갱신)부터 그 라인을 완료(`done`)할 때까지의 ms를 매번(재도전 포함) `puzzle_line_solve_times`(신규 테이블, `puzzle_solve_events`와 동일한 append-only 로그 패턴)에 기록한다. 새 RPC `puzzle_line_avg_solve_ms`가 표본이 20개 이상이면 상하위 10%(`percentile_cont`)를 잘라낸 절사평균을, 그보다 적으면 단순평균을 서버에서 계산해 돌려주고, `applySolveTimeAdjustment`가 그 실제 평균을 레이팅대별 기대 시간(`expectedSolveMsFromRating`, 100점=8초~3000점=120초 선형 기준)과 로그 스케일로 비교해 최대 ±400점 가감한다(표본이 `RATING_MIN_SAMPLES`=5 미만이면 기본 레이팅을 그대로 씀). 소규모 절사평균이 오히려 왜곡되는 것을 막기 위해 표본 20개 미만에서는 절사 없이 단순평균만 쓴다.

표시 위치는 요청대로 세 곳 — 퍼즐 카드(`PuzzleCard`) 좌하단(정적 기본 레이팅만, 네트워크 없이 카드당 즉시 계산), 퍼즐 풀이 카드(`PuzzleSolver`) 우측 여백(닫기 버튼 바로 아래), 그리고 모식도의 "라인 N" 라벨 바로 옆(고스트 리프 포함). 목록에 카드가 아무리 많아도 카드마다 서버 왕복 없이 즉시 계산되도록, 목록/카드 뷰는 정적 기본 레이팅만 쓰고 실제 풀이 시간 가감은 퍼즐 하나만 상세히 보는 `PuzzleSolver`에서만 반영한다.

**기능 — 프로필 유산(Legacy)**
프로필에 "가장 자랑스러운 한 수"를 전시하는 기능을 추가했다. 딱 세 종류만 둔다 — 최선의 유산·유일한 유산·탁월한 유산(`LEGACY_TYPES`, 기존 `QLABEL`의 `best`/`only`/`brilliant` 등급과 정확히 대응). 저장 형태는 `profile.legacies = { best, only, brilliant }`(각각 `{ sans, moveIndex, playCount, savedAt }` | `null`)이고, 기존 `pub` 확장 패턴을 그대로 따라 `publishProfile`이 호출되는 5곳(발행 effect+의존성 배열, `loadAccount` 병합, OAuth 최초 로그인, 로그아웃 초기화, 두 곳의 `myPub` 로컬 구성)에 함께 반영해 다른 유저의 공개 프로필에서도 보이게 했다.

만들기 흐름(`LegacyManageModal`) — PGN 직접 입력(`parsePgnMoves` + `NotationTools`와 동일한 방식의 합법성 재생 검증) 또는 chess.com 대국에서 선택(`useChessCom`이 이미 불러와 둔 `games` 재사용, 새 fetch 없음) 중 하나로 대국을 고르면, `analyzeGame`(`ReviewPage`가 쓰는 것과 동일한 함수)으로 대국 전체를 채점한다. 그 유산이 요구하는 등급(`kind`)으로 채점된 수만 목록에서 고를 수 있게 해, 아무 수나 우겨 넣을 수 없도록 서버가 아니라 실제 엔진 분석으로 검증한다. 마지막으로 그 수부터 몇 수까지 재생할지(`playCount`, 남은 수만큼 상한)를 입력받아 저장한다.

디자인은 전체 PGN에서 고른 그 한 수를 "암석판에 룬 문자를 새긴" 모습으로 보여준다(`LegacyStoneTile`) — 이미지 자산 없이 방사형 그라디언트(돌 표면)와 반복 그라디언트(균열 결) 레이어, 그리고 등급 색(`QCOLOR`)으로 발광하는 `text-shadow`만으로 구현했다. 프로필의 "푼 퍼즐" 바로 위(`PublicProfileStats`)에 최대 3칸을 나열하고, 내 프로필에서는 빈 칸에 추가 버튼·채워진 칸에 편집 버튼이 함께 뜬다.

누르면 `LegacyRevealScreen`이 뜬다 — 먼저 그 암석판이 확대되며 룬 문자가 밝기·`text-shadow` 강도를 오가며 빛나고(`charge` 단계, ~0.95초), 이어서 전체 화면 "빛의 체스보드"로 전환한다(`board` 단계). 이 보드는 기존 나무 질감 `Board` 대신 새로 만든 `LegacyLightBoard`로, 반투명 격자선과 `PieceGlyph`에 `drop-shadow` 발광 필터를 씌운 기물로 "허공에 뜬 빛"을 표현하고, 방금 둔 수의 도착 칸에는 원형 발광 하이라이트(`sanSrc`로 계산)를 씌운다. 지정된 수부터 시작해 1.15초 간격으로 한 수씩(`setTimeout` 재귀 스케줄링, 반복 `setInterval` 아님) `playCount`만큼 자동 재생하고, 끝나면 "다시 보기" 버튼이 뜬다.

**기능 — 유저 검색 리더보드에 모든 계정을 최대 100위까지**
`leaderboard_top(p_limit)` SQL 함수가 `where coalesce((pub->>'xp')::bigint, 0) > 0` 조건으로 경험치가 0인 계정을 아예 순위 밖으로 걸러내고 있었다 — 신규 가입 직후처럼 아직 아무 활동이 없는 계정은 검색해도 리더보드에 전혀 보이지 않았다. 이 필터를 없애 `profiles` 테이블의 모든 계정이 XP 내림차순(동점이면 아이디 오름차순)으로 순위에 오르도록 고쳤다. 클라이언트 쪽 `UserSearchModal`의 기본 추천 화면이 부르는 `leaderboardTop(8)` 호출도 `leaderboardTop(100)`으로 올려, 검색 전 기본 화면에서 최대 100위까지 볼 수 있게 했다(1~3위는 기존처럼 메달 이미지, 4위부터는 숫자로 표시 — `userSearchRow`의 `rank` 분기는 이미 임의 순위를 지원해 추가 변경이 필요 없었다).

**UX — 오프닝 트리 빈 공간 스크롤 가속 되돌리기**
v0.3.2에서 트리 반지름이 커진 만큼 화면에 블록이 하나도 안 보이는 빈 공간에서는 드래그·휠 감도를 기본(1.5배)보다 더 올렸었는데(`SCHEMATIC_DRAG_MULT_EMPTY`/`SCHEMATIC_WHEEL_MULT_EMPTY`, `anyItemVisible` 판정으로 매 이동마다 전환), 막상 써 보니 블록이 있다/없다 경계를 넘나들 때 감도가 튀는 느낌이 오히려 어색하다는 피드백을 받았다. 빈 공간 전용 배율과 그 판정 로직을 완전히 제거하고, `onPointerMove`·휠 핸들러 모두 항상 기본 배율(`SCHEMATIC_DRAG_MULT`/`SCHEMATIC_WHEEL_MULT`, 1.5배)만 쓰도록 되돌렸다.

**디자인 — 유산 블록을 사이트 팔레트의 라운딩 사각 블록으로, 새겨지는 수는 Merriweather 폰트로**
`LegacyStoneTile`·`LegacyRevealScreen`의 `charge` 단계에 한 차례 원형 석판 이미지 자산(`public/legacy-stone.png`, 사용자 제공 텍스처를 PIL로 알파만 남겨 정리)을 넣었다가, "기존 사이트에서 쓰는 금색·갈색을 조합한 라운딩 사각형"으로 다시 바꿔 달라는 요청에 따라 이미지 자산을 완전히 제거했다. 대신 사이트 전반의 어보니·브래스 팔레트(`T.ebony`/`T.ebony2`/`T.ebony3`, `T.brass`/`T.brassHi`)만으로 두 컴포넌트를 다시 만들었다 — 대각선 그라디언트(`linear-gradient(155deg, T.ebony3→T.ebony2→T.ebony)`) 몸통에 `T.brass` 1.5px 테두리, 좌상단 금빛 사선 광택(`rgba(236,203,134,.16)`), 상단 코너의 은은한 금색 글로우를 얹었다(모두 순수 CSS, 이미지 자산 없음). 모서리도 원형 대신 `borderRadius: 14`(타일)·`20`(확대 연출)의 라운딩 사각형으로 통일했다. 새겨지는 수 표기(`legacyMoveLabel`) 글꼴은 `ui-monospace`에서 새 상수 `LEGACY_FONT`("Merriweather")로 바꿨다 — `index.html`의 Google Fonts `<link>`에 `Merriweather:ital,wght@0,400;0,700;0,900;1,400`을 추가했다.

이어서 "추가 전(빈 칸)과 추가 후(채워진 칸)의 블록 크기가 달라 보인다"는 지적을 받아, 버튼 자체의 스타일(`LEGACY_BLOCK_BTN_STYLE`)과 장식 레이어(`LegacyBlockDecor` — 사선 광택 + 코너 글로우)를 두 컴포넌트가 공유하는 모듈 상수/컴포넌트로 뽑아냈다 — `LegacyStoneTile`·`LegacyEmptySlot` 모두 정확히 같은 배경·테두리·모서리·패딩을 쓰게 되어 채워지기 전후로 블록 크기·재질이 완전히 같다. `LegacyEmptySlot`은 더 이상 점선 테두리 + Gem 아이콘 + "OO 추가" 텍스트가 아니라, 같은 금색·갈색 블록 가운데에 "+"만 보여준다. `LegacyStoneTile`도 블록 안에 등급 라벨 텍스트("최선의 유산" 등)를 더 이상 쓰지 않고, 그 자리에 실제 새겨진 수(`legacyMoveLabel`)만 크게 보여준 뒤, 등급은 블록 우하단의 작은 원형 배지(`badgeIcon(typeInfo.kind)`, 다른 화면과 동일한 아이콘·등급색)로만 표시한다.

**기능 — 유산 재생 애니메이션에 PGN 타이핑 예고편·영사기 연출·수 체계 아이콘·명암 격자 추가**
`LegacyRevealScreen`의 중간 단계를 다시 설계했다. (1) 처음에는 실제로 수를 두는 예고편(보드 위에서 빠르게 재생)이었는데, "수를 직접 두지 말고 PGN 텍스트만 타이핑하듯 보여 달라"는 요청에 따라 `charge`와 `board` 사이 단계를 `preview`(보드 애니메이션)에서 `typing`(텍스트 전용)으로 바꿨다 — 전체 기보를 `moveNumber(i)+sans[i]` 토큰으로 만들어 한 수당 45ms 간격으로 한 토큰씩 이어 붙이며(`typedCount` 상태), 어보니+브래스 카드 안에 Merriweather 폰트로 스트리밍되고 끝에는 깜빡이는 커서(▍)가 붙는다. 체스보드는 이 단계에서 아예 렌더링하지 않는다 — 다 타이핑되면 지정된 시작 수로 `appliedCount`를 맞추고 `board` 단계로 넘어가 기존처럼 1.15초 간격으로 느리게 재생한다. (2) `board` 단계에 새 `LegacyProjectorBlock`(유산 타일과 같은 재질의 작은 라운딩 블록)을 보드 바로 아래에 두고, 그 블록에서 보드 밑면까지 사다리꼴로 좁아지는 금빛 광선(`clip-path` trapezoid, 은은하게 밝기가 맥동)을 그려 "이 블록에서 빛이 뿜어져 나와 보드를 비추는" 영사기 연출을 만들었다. 이에 맞춰 `LegacyLightBoard`의 전체 빛깔도 기존 파란 톤에서 사이트 전반의 금색·갈색(브래스) 톤(칸 배경·기물 발광·보드 외곽 drop-shadow 전부)으로 바꿨다. (3) `LegacyEntry`에 `kinds`(전체 수순과 같은 길이의 등급 배열, `LegacyManageModal`이 저장 시 이미 갖고 있던 `analyzeGame` 결과에서 그대로 떼어옴 — 재생 시 엔진을 다시 돌리지 않는다) 필드를 추가하고, `LegacyLightBoard`가 `haloKind` prop으로 방금 둔 수의 도착 칸 위에 실제 체스보드(`Board`)와 정확히 같은 위치·크기·흰 테두리(칸 우상단, `top:-cell*.18, right:-cell*.18`)로 `badgeIcon(kind)` 배지를 띄운다. (4) `LegacyLightBoard`의 칸 배경을 `(r+c)%2`로 구분해 밝은/어두운 칸을 색이 아니라 불투명도 차이(=빛의 밝기 차이)로 표현하는 기존 동작은 그대로 유지했다.

**디자인 — 팝업 제목 글꼴을 Bagel Fat One으로**
"굵고 각진 한글 디스플레이 폰트"로 쓰던 `GAME_FONT` 상수는 퀘스트 클리어·칭호 획득·티어 승급 팝업의 큰 제목(정확히 이 3곳에만 쓰여 그 자체로 "팝업 Title 텍스트"의 전체 목록이었다) 세 곳에서만 쓰이고 있었다. 이 상수의 폰트만 `'Gasoek One'`에서 `'Bagel Fat One'`로 교체했다(`index.html`의 Google Fonts `<link>`에 `Bagel+Fat+One` 추가) — 사용하는 3곳 모두 코드 변경 없이 자동으로 새 폰트를 받는다.

**UX — 유산 만들기에서 chess.com 대국 선택을 별도 화면 없이 인라인으로**
`LegacyManageModal`이 "chess.com 대국에서 선택"을 누르면 자체적으로 만든 단순 목록(상대·결과만 보이는 버튼 목록)으로 전체 화면을 바꿔치기하던 것을, 프로필 카드가 이미 쓰던 `AccountChessStats`(레이팅·필터·전적·최근 대국·오프닝별 승률까지 갖춘 완전한 컴포넌트)를 그 버튼 바로 아래에 그대로 펼치는 방식으로 바꿨다. `AccountChessStats`에 `onSelectGame` prop을 새로 받게 해, 이 prop이 있을 때만 "최근 대국" 각 줄의 검색(🔍)·리뷰 버튼 두 개 자리에 "선택" 버튼 하나만 렌더링하도록 분기했다(넘기지 않으면 기존 프로필·유저 검색 화면은 완전히 그대로). `LegacyManageModal`은 이제 `chesscomGames` 배열 대신 `chesscom` 훅 객체 전체와 `username`을 받아 그대로 넘겨준다. 이 김에 모달 제목도 "최선의 유산" 같은 문장형 대신 사용자가 요청한 "유산 • 최선" 형식으로 바꿨다(`LEGACY_TYPES`에 `short` 필드 추가).

**UX — 집중 학습을 도감 탭에서 열면, 닫을 때 도감 탭의 모식도 위치를 그대로 보존**
도감 탭(`CollectionTab`)은 `{tab === "dex" && <CollectionTab .../>}`처럼 조건부로만 마운트돼 있어, 그 안의 `OpeningSchematic`이 들고 있는 팬·줌·펼쳐 둔 수 카드(`openKey`) 같은 화면 상태는 전부 컴포넌트 로컬 상태였다 — 도감 탭 모식도의 수 카드에서 "오프닝 이름"을 눌러 집중 학습으로 이동하면(`onOpenOpening`이 `setTab("learn")`을 부름) `CollectionTab`이 통째로 언마운트되어 이 상태가 모두 사라지고, 집중 학습을 닫아도 도감 탭으로 돌아가지조차 않았다(학습 탭에 그대로 남음). 새 상태 `focusReturnTab`을 추가해 `onOpenOpening`이 도감 탭에서 불렸을 때만 `"dex"`로 표시해 두고, 이 값이 있는 동안은 도감 탭 렌더 조건을 `(tab === "dex" || focusReturnTab === "dex")`로 넓혀 `CollectionTab`을 계속 마운트한 채 `display:none`으로 화면에서만 숨긴다(트리 자체 데이터 `useOpeningTreeAuto`는 이미 v0.3.x에서 App로 끌어올려져 있었지만, 화면 상태는 그대로 CollectionTab 로컬이었다). 집중 학습이 닫히는(`learnFocus`가 null이 되는) 순간을 지켜보는 effect가 `focusReturnTab === "dex"`이면 (navNonce는 건드리지 않고) 탭만 `"dex"`로 되돌린다 — `navNonce`를 올리는 기존 `switchTab`(수동 탭 전환, 이 예약을 취소한다)을 거치면 `CollectionTab`의 `key`가 바뀌어 애써 숨겨 둔 인스턴스가 다시 마운트되므로 반드시 피해야 한다. 실제로 도감 탭에서 "1.e4 → King's Pawn Game" 카드를 열고 트리를 드래그해 옮긴 뒤 집중 학습에 들어갔다 닫아 보면, 팬 위치는 물론 열려 있던 수 카드까지 떠나기 전 그대로 남아 있다(로딩 지연 없이 즉시).

**버그 수정 — 도감 오프닝 트리 수 카드가 화면 밖(하단 고정 내비게이션 위)으로 삐져나오던 문제**
사용자가 보내온 실기기 화면 녹화로 재현을 확인했다. v0.3.2에서 수 설명 카드(`DexMoveBlock`)의 위치를 모식도 박스(`boxRef`, `overflow:hidden`, `height:640`) 자신의 로컬 좌표계 안으로 clamp하는 수정을 넣었는데도, 모바일 실기기에서는 카드가 계속 박스 밖 — 특히 화면 하단 고정 내비게이션(`nav`, `position:fixed`, 66px) 위로 — 삐져나와 보였다. 카드를 `position:absolute`(박스 기준 로컬 좌표)에서 `position:fixed`(뷰포트 기준 좌표)로 바꾸고, clamp 기준도 박스 자신의 크기가 아니라 실제 화면 크기(`window.innerWidth`/`innerHeight`에서 하단 내비게이션 높이만큼 여유를 뺀 값)로 잡도록 다시 만들었다 — 박스가 화면에 얼마나 걸쳐 있는지와 무관하게, 카드는 항상 뷰포트 안에서만 그려진다. `rect.left`/`rect.top`(박스의 현재 화면상 위치)을 더해 팬/줌 로컬 좌표를 뷰포트 좌표로 변환하고, 세로/가로 두 분기(`vertical`) 모두 같은 방식으로 통일했다.

**성능 — 모식도 미리 그리기 여유를 뷰포트의 1.4배 → 2배로**
`OpeningSchematic`의 뷰포트 컬링(v0.3.3 초반 성능 개선)은 화면보다 `CULL_REFRESH_PAD`(뷰포트 크기의 배수)만큼 넉넉하게 미리 그려 두는데, 빠르게 드래그(플릭)하면 이 여유 범위 경계가 화면에 보이기 전에 블록이 걸러져 나타나는 게 눈에 띈다는 피드백을 받았다. 1.4배 → 2배로 늘려 더 빠른 스크롤에서도 새 블록이 화면에 들어오기 전에 이미 그려져 있도록 안정성을 높였다.

**UX — 체스보드 드래그 무브를 칸 전체로, 터치 스크롤과 충돌하지 않도록**
`Board`의 포인터 드래그(v0.3.0)는 지금까지 기물 도형 크기(`cell*0.74`)만큼의 작은 래핑 div에만 `onPointerDown` 등 드래그 핸들러와 `touchAction:"none"`을 걸어 뒀다 — 손가락이 그 좁은 영역을 정확히 짚지 못하면(같은 칸 안이라도, 특히 좁은 화면·작은 폰 기물에서 흔함) 드래그가 아예 시작되지 않았고("드래그 무브가 잘 안 된다"), 그 지점은 `touchAction`이 없어 브라우저가 페이지 스크롤로 먼저 채가 버렸다("보드 근처에서 드래그하면 스크롤이 함께 움직인다"). 두 문제 모두 근본 원인이 같아, 드래그 시작 판정과 `touchAction:"none"`을 기물 도형이 아니라 그 **칸 전체**(그리고 보드 그리드 전체)로 넓혔다 — 칸 안 어디를 짚어도 내 기물이면 드래그가 시작되고, 보드 위에서는 손가락이 움직이는 순간 페이지가 스크롤로 새지 않고 곧장 드래그로만 이어진다. 이 경쟁이 사라지면서, 드래그 중 화면이 스크롤과 다투며 기물이 손가락을 따라오지 못하고 늦게 쫓아가던 느낌("스크롤할 때 커서를 따라오는 속도가 느리다")도 함께 해소된다 — 고스트 기물 자체는 원래도 `pointermove` 좌표를 그대로 쓰는(트랜지션 없는) 1:1 추적이었어서, 지연의 실제 원인은 네이티브 스크롤과의 제스처 경합이었다.

**기능 — 일일 퀘스트 오프닝 문구에 진영 표시 + chess.com 하이퍼링크**
오프닝 플레이 퀘스트 문구("OO로 chess.com에서 게임 1회 플레이하기")가 그 오프닝을 실제로 누가 두는지(백 오프닝인지 흑 응수인지)를 알려주지 않았다. 새 헬퍼 `questOpeningSide(name)`가 그 오프닝의 조건 PGN 수순(`questOpeningMovesText`)을 `parsePgnMoves`로 토큰화해, 마지막 수의 홀짝(1-based 개수가 홀수면 백, 짝수면 흑)으로 진영을 판정한다 — 문구를 "chess.com에서 <백|흑>으로 <오프닝 명칭> 1회 플레이하기" 형식으로 바꿨다(예: "1.d4 d5 2.c4 c6"로 정의되는 슬라브 디펜스는 마지막 수가 흑의 c6이므로 "흑으로"). 새 컴포넌트 `ChesscomTextLink`가 문구 맨 앞의 "chess.com" 부분에 밑줄과 실제 `https://www.chess.com` 링크를 건다(일반 https 링크라 모바일에 chess.com 앱이 설치돼 있으면 OS 유니버설/앱 링크로 자동으로 앱이 열린다 — 커스텀 URL 스킴 불필요). 이 링크는 퀘스트 행 전체의 클릭(오프닝 집중 학습 이동)과 겹치므로 `stopPropagation`으로 분리했다. `DailyQuestCard`·`DailyQuestClearedModal` 두 곳 모두 `questLabelNode(q)`(JSX를 반환)로 바꿔 반영했고, 2.6초 후 자동으로 사라지는 개별 클리어 토스트는 그대로 plain text(`questSlotLabel`)를 쓴다.

**UI·버그 수정 — 도감 모식도 위 안내 문구를 해금률로, 개발자 추가 이론 수 표시 안 되던 문제**
모식도 위에 있던 안내 문구("마디를 클릭하면 상세 정보가...")를 지우고, 그 자리에 "도감 해금률"(소수점 둘째 자리까지, 예: `12.34% (413/3,347)`)을 표시한다. `OpeningSchematic`이 레이아웃을 계산할 때마다(트리가 자라는 동안 계속) 지금까지 펼쳐진 노드(`items`) 중 `unlocked`인 것의 비율을 새 `onUnlockStats` 콜백으로 `CollectionTab`에 올려보내 `unlockStats` state로 받는다. 이 김에 별도 버그도 고쳤다 — 개발자가 `SchematicEditor`("이론 수 체계 편집")로 새로 추가한 이론 수(`CONTENT.treeAdds`, `addsFor`)가 `DexMoveBlock`·`useMergedMoves` 등 다른 화면에는 이미 병합돼 보였지만, 정작 모식도 자신의 트리 순회(`visit`)는 `treeData.get(key)`(리체스 탐색기에서 자동으로 채운 데이터)만 보고 있어 `addsFor`를 전혀 병합하지 않았다 — 리체스에 아예 없는(개발자가 순수하게 새로 만든) 수는 노드 자체가 생성되지 않아 모식도에 나타날 수가 없었다. `visit` 진입부에서 `treeData`의 `rawMoves`에 `addsFor(key)`를 병합하도록 고치고(다른 화면들과 같은 패턴), 개발자가 저장한 직후(`bumpContent`) 바로 반영되도록 `contentVer`를 새 prop으로 받아 레이아웃 `useMemo`의 의존성에도 추가했다.

**버그 수정 — 모바일에서 chess.com 링크가 앱 대신 항상 웹사이트로 열리던 문제**
`ChesscomTextLink`가 `target="_blank"`(새 창/탭)로 `https://www.chess.com`을 열고 있었는데, 대부분의 모바일 브라우저는 새 창/탭 내비게이션에서는 유니버설/앱 링크 가로채기 자체를 건너뛰고 항상 웹사이트로 연다 — 링크 URL이나 OS 설정과 무관하게 재현되는, `target="_blank"` 자체가 원인인 문제였다. `target`(과 그에 딸린 `rel="noopener noreferrer"`)을 제거해 같은 탭에서 이동하도록 바꿨다 — 이제 chess.com 앱이 설치돼 있으면 OS가 그 이동을 가로채 앱으로 바로 연다.

**UI — 유산 타일 크기 고정, 등급 아이콘 확대·재배치, 수정 버튼 위치 변경**
`LegacyStoneTile`/`LegacyEmptySlot`이 `flex:1`을 써서, 등록된 유산이 1~2개뿐인 프로필(특히 남의 읽기 전용 프로필)에서는 타일이 그 개수만큼 늘어나 3개 다 채웠을 때보다 훨씬 크게 보였다. `LEGACY_TILE_FLEX = "0 0 calc((100% - 16px) / 3)"`로 고정해, 항상 "3칸 중 1칸" 폭만 차지하도록 했다(개수와 무관하게 타일 크기 동일). 등급(최선/유일/탁월) 아이콘은 버튼 안쪽(`overflow:hidden`이라 잘림)의 작은 원형 배지 대신, 버튼 바깥의 형제 `<span>`으로 우상단(`top:-9,right:-9`)에 배치해 블록 밖으로 살짝 삐져나오게 하고 크기도 12px→26px로 키웠다 — 뒤에 있던 같은 색 원형 배경은 없애 아이콘 이미지만 드롭섀도우와 함께 떠 보인다. 수정(✎) 버튼은 우상단(등급 아이콘과 겹치던 자리)에서 우하단으로 옮겼다.

**버그 수정 — 알림 카드·수 아이콘 설명 말풍선이 모바일에서 여전히 잘리던 문제**
도감 카드(`DexMoveBlock`)에 적용했던 것과 같은 원인이 `NotificationBell`의 알림 드롭다운과 `CircleBadge`의 설명 말풍선에도 남아 있었다 — 둘 다 `position:absolute`로 가까운 조상의 로컬 좌표계 안에서만 clamp하고 있어, 그 조상이 `overflow:hidden`인 다른 박스에 걸쳐 있으면 화면(뷰포트) 기준으로는 여전히 잘려 보였다. 두 컴포넌트 모두 열릴 때 `getBoundingClientRect()`로 뷰포트 좌표를 직접 계산해 `position:fixed`로 렌더링하도록 바꾸고, `window.innerWidth`/`innerHeight`에서 하단 고정 내비게이션(66px + 여유)을 뺀 범위 안으로 clamp했다.

**기능 — 학습 탭에 FEN 붙여넣기 전용 "FEN 모드" 추가, 기존 FEN 미리보기 폐기**
지금까지 FEN을 붙여넣으면 그 포지션을 읽기 전용으로 미리 보여주는 모달만 있었다 — 실제로 그 포지션부터 이어서 둘 수는 없었다. 이 미리보기 코드를 전부 없애고, FEN을 불러오면 그 포지션을 루트로 자유롭게 두는 "FEN 모드"로 전환하도록 새로 만들었다. 앱 전역에서 쓰는 공유 리플레이 캐시(`_replayCache`/`replaySans`/`boardFromSans`)는 표준 시작 포지션 + 전체 캐슬링 권리를 항상 전제하므로 손대지 않고, `LearnTab` 안에 병렬 구현을 뒀다 — `parseFenFull`(FEN 문자열 → 기물 배치·차례·캐슬링 권리·앙파상 파싱)과 `replayFromFen`(그 루트에서부터 지금까지의 수순 재생), `fenLegalDests`(기존 `legalDests`는 캐슬링 "권리" 자체는 검사하지 않고 킹·룩이 홈 스퀘어에 물리적으로 있는지만 보므로, FEN이 지정한 `{K,Q,k,q}` 권리로 캐슬링 목적지를 한 번 더 걸러냄)를 새로 추가했다. 수 번호가 항상 백의 첫 수를 "1."로 가정하던 `moveNumber`/`sansToPgnText`/`SequenceBar`를 시작 진영을 받는 `startColor` 인자로 일반화해, 흑이 먼저 두는 FEN에서는 그 첫 수부터 정확히 "1..."로 표시되고(기존 표준 포지션 호출부는 인자를 생략하면 그대로 동작). 오프닝북·라이브 엔진을 합치는 `useMergedMoves`는 `sans` 배열 내용만으로 캐시를 조회해, FEN 모드의 빈 `sans`(`[]`)를 표준 시작 포지션의 빈 경로와 구분하지 못하고 엉뚱한 정석 데이터·평가치를 그대로 보여주는 버그가 실제 테스트로 드러나 — FEN 모드일 때는 이 훅의 결과를 전부 중립 값으로 덮어써 해결했다. "분석"·"학습"(집중 학습 진입) 버튼은 FEN 모드에서 숨기거나 비활성화했다.

**UI — 퍼즐 탭 "일일 퍼즐" 라벨을 "추천 퍼즐"과 통일**
캐러셀 위 "일일 퍼즐" 라벨이 "추천 퍼즐"보다 한 단계 작은 글자 크기(11px)를 쓰고 있었다 — 12.5px로 맞추고, 앞의 `Sparkles` 아이콘은 "📅" 이모티콘으로 바꿨다.

**기능 — 그랜드마스터 티어 시각 효과 확장(무지개 그라데이션 테두리·게이지·별)**
`TIER_COLORS.grandmaster`가 이미 갖고 있던 홀로그램 색(보라 `#B983FF`·민트 `#6EE7C8`·핑크 `#FF8FD1`, `tierGradientCss("grandmaster")`)을 재사용해 그랜드마스터 전용 효과를 늘렸다. 새 헬퍼 `gmPhotoRingStyle`(배경 두 겹 클리핑 — border-box엔 그라데이션, content-box엔 투명 — 트릭으로 사진 위에 자연스러운 링을 그린다)을 내 프로필 편집·프로필 카드·헤더 프로필 메뉴·채팅/친구/검색의 각 프로필 사진 표시부에 적용해, 그랜드마스터의 프로필 사진에는 무지개 테두리가 둘린다. 티어 로고를 감싸는 흰 십각형(`TierLogoDisc`)도 그랜드마스터일 때는 SVG `<linearGradient>`(useId로 인스턴스별 고유 id 부여)를 만들어 테두리 획(stroke)에 같은 그라데이션을 입히고 두께도 2→3으로 살짝 키웠다. 공용 XP 게이지 컴포넌트 `TierStatPill`(프로필 카드·검색 리더보드 등에서 재사용)과 `TierProgressStrip`(퍼즐 탭 상단)의 진행바 채우기 색을 그랜드마스터 티어일 때 같은 그라데이션으로 바꾸고, 그 옆에 프레스티지 별 개수(`info.gmStars`)를 "★N" 텍스트에 같은 그라데이션을 입혀(`WebkitBackgroundClip:"text"`) 함께 보여준다. (채팅 말풍선에도 같은 효과를 적용했다가, 사용자 피드백으로 채팅 쪽은 이번 버전에서 제외했다.)

**UX — 유산 만들기 chess.com 대국 선택 화면을 "최근 대국" 목록만 보이도록 축소**
`LegacyManageModal`이 재사용하는 `AccountChessStats`가 프로필·전적 통계 화면을 그대로 갖고 있어, 대국 하나를 고르는 용도로는 정보가 과했다(프로필 헤더·전체 기간 전적·레이팅 그래프·가장 많이 둔 오프닝·오프닝별 승률까지 전부 표시). `onSelectGame`(이 prop은 오직 유산 대국 선택 화면에서만 넘어온다)이 있을 때는 `recentOnly` 분기로 그 블록들을 모두 건너뛰고 "최근 대국" 목록(과 그 위 시간/색 필터, 페이지 넘김)만 남도록 잘라냈다 — 다른 호출부(프로필·유저 검색 등)는 그대로 전체 화면을 유지한다.

**조사 — chess.com 하이퍼링크가 모바일에서 여전히 앱으로 안 열리는 문제**
이전 수정(`target="_blank"` 제거)이 배포된 뒤에도 앱으로 연결되지 않는다는 재보고가 있어, Playwright로 실제 클릭 시 무슨 일이 일어나는지 직접 검증했다 — 렌더링된 마크업은 `<a href="https://www.chess.com">chess.com</a>`(target·rel 없음, 다른 JS 인터셉트도 없음)이고, 실제로 클릭하면 새 탭이 아니라 현재 탭에서 진짜 최상위(top-level) 내비게이션이 시도된다(`framenavigated` 이벤트로 확인, 샌드박스가 chess.com 자체 접근을 막고 있어 그 다음 단계만 실패). 즉 웹사이트 코드 쪽에서 할 수 있는 조치는 이미 다 되어 있는 상태 — 실제로 앱이 열리는지는 이제 전적으로 chess.com이 자신의 iOS Universal Links(`apple-app-site-association`)·Android App Links(`assetlinks.json`)에 루트 도메인(`/`)을 등록해 뒀는지, 그리고 테스트 환경(카카오톡 등 인앱 브라우저는 이 핸드오프 자체를 항상 차단함)에 달려 있다 — 두 파일 모두 이 세션의 네트워크 정책상 chess.com에 직접 접근할 수 없어 확인하지 못했다. 사용자가 안드로이드 크롬·Vercel preview 환경에서 재현됨을 알려와, 안드로이드 App Links는 기기에 저장된 "이 도메인은 항상 Chrome으로 열기" 선택이 있으면 사이트 쪽 코드와 무관하게 계속 브라우저로만 열린다는 점을 안내했다(설정 → 앱 → 지원되는 링크 열기에서 확인 가능).

**버그 수정 — 퍼즐 탭 티어 진행 스트립에서 그랜드마스터 별 개수가 티어 명칭과 겹쳐 보이던 문제**
`TierProgressStrip`에 추가했던 "★N" 프레스티지 표시를 티어 명칭 텍스트 옆(같은 줄)에 뒀더니 "그랜드마스터"라는 긴 라벨과 좁은 `minWidth:96` 칼럼 안에서 서로 겹쳐 보였다. 별 표시를 명칭 줄에서 게이지 바 줄로 옮기고, 게이지 바를 `flex:1`로 늘린 뒤 그 오른쪽에 배치해 겹침 없이 항상 게이지 바 우측에 나오도록 고쳤다(`TierStatPill`은 애초에 이 구조였어서 그대로 둠).

**버그 수정 — 도감 오프닝 트리 기본 화면에서 중심 회로 칩이 뷰포트 정중앙에 오지 않던 문제**
나침반 중심 회로 칩(`left: centerX - CHIP_SIZE/2`)은 local 좌표 `(centerX, centerY)` 자체가 칩의 중심이 되도록 그려지는데, 초기 화면·"화면 가운데로 되돌리기" 버튼의 팬 계산식은 특정 노드(블록 좌상단 좌표 `it.x/it.y`)를 중앙에 맞출 때 쓰는 것과 같은 `+ boxW/2, + boxH/2` 보정을 그대로 복사해 와, 칩의 실제 중심보다 블록 반 칸만큼 오른쪽 아래로 치우친 점을 뷰포트 정중앙에 맞추고 있었다 — 그만큼 칩이 화면 중앙에서 위·왼쪽으로 어긋나 보인 원인. 그 보정을 빼고 `centerRef.current.x/y`를 그대로 뷰포트 중앙에 맞추도록 고쳐, 두 경로(자동 재중앙 정렬 interval·되돌리기 버튼) 모두 칩이 정확히 화면 정중앙에 오도록 했다.

**UI — 유산 재생 화면 재설계**
사용자 스케치 피드백을 반영해 여러 부분을 다시 다듬었다. (1) 기보 타이핑 단계의 갈색 카드 배경과 "대국 훑어보기…" 문구를 없애 기보 텍스트가 우주 배경 위에 그대로 떠 보이게 했다. (2) 체스보드 단계의 "탁월한 유산" 같은 유산 등급 라벨과 하단 SAN 텍스트, "다시 보기" 버튼을 모두 없앴다. (3) 대신 하단 영사기 블록(`LegacyProjectorBlock`)을 버튼으로 바꿔, 언제든 누르면 `appliedCount`가 시작 지점으로 되돌아가 처음부터 다시 재생된다. (4) 체스보드 크기를 `useBoardSize(360)`→`useBoardSize(440)`으로 키웠다. (5) SAN 텍스트가 있던 자리에 그 수의 수 체계 아이콘(`badgeIcon(haloKind, 28)`)을 대신 표시한다(보드 위 칸에 뜨는 작은 배지와 별개로, 아래에 더 크고 분명하게). (6) 다음 수로 넘어가는 간격을 1150ms→1450ms로 살짝 늘렸다. (7) 화면 전체 배경(`radial-gradient`)이 고정된 남색 대신 `QCOLOR[haloKind]`(현재 두어진 수의 등급 색, 수를 두기 전엔 유산 등급 색)를 반영해 매 수마다 은은하게 색이 바뀌도록(900ms 크로스페이드) 했다 — 체스보드 halo 링 색(`glowColor`)도 같은 값으로 통일해 배지 색과 어긋나지 않게 했다.

**버그 수정·UI — 유산 재생 화면 후속 조정(수 체계 아이콘 미표시, 배경·타이핑 속도·기물 링)**
직전 재설계 후 실제로 확인해 보니 세 가지가 더 필요했다. (1) 수 체계 아이콘이 안 뜨는 근본 원인 — `entry.kinds`는 구버전에 저장된 유산이거나 분석 결과에서 해당 ply를 못 찾으면 없거나 `"pending"`으로 남아 있는데, 이 경우 `haloKind`가 계속 `null`이라 아이콘이 전혀 그려지지 않았다. 다만 유산을 저장할 때 이미 "지정한 수(`moveIndex`)는 반드시 그 유산 등급(`typeInfo.kind`)으로 채점된 수"라는 게 `LegacyManageModal`의 선택 목록(`qualifying = result.moves.filter(m => m.kind === typeInfo.kind)`)에서 검증돼 있으므로, `kinds`가 없거나 못 믿을 때도 지정한 수 차례에는 `typeInfo.kind`로 확실하게 표시하도록 폴백을 추가했다(그 외의 수는 실제 등급을 모르니 생략). (2) 배경을 "매 수 등급 색을 반영"하던 것에서 다시 고정된 검은색(`#000`)으로 되돌렸다(사용자가 색 배경 대신 검은 배경을 요청). (3) 기보 타이핑 속도를 45ms→80ms로 더 늦췄다. (4) 기물 칸을 감싸던 등급색 사각 테두리(halo 링)를 없애고, 우상단 원형 수 체계 배지만 남겼다.

**버그 수정 — 도감 오프닝 트리 중심 회로 칩 정렬이 모바일에서는 오히려 더 어긋나던 문제**
직전 수정(`+ boxW/2, + boxH/2` 보정 제거)은 박스 자신의 전체 높이(고정 640px)를 기준으로 한가운데를 잡는 계산이라, 데스크톱처럼 640px가 화면에 다 들어오는 환경에서는 정확히 맞았지만, 모바일은 화면 자체가 좁아 박스 하단이 뷰포트 아래로(그리고 하단 고정 내비게이션 뒤로) 잘려 나가는 경우가 흔하다 — 이때 "박스 전체 높이의 절반"은 실제로 화면에 보이는 범위의 절반보다 훨씬 아래쪽에 있어, 칩이 화면상으로는 오히려 아래로 처져 보였다(실측: iPhone 13 뷰포트에서 칩 중심이 뷰포트 상단에서 380px 지점이 아니라 460px대까지 처짐). 새 헬퍼 `visibleBoxCenter(rect)`가 박스의 `getBoundingClientRect()`를 뷰포트(가로는 `window.innerWidth`, 세로는 `window.innerHeight`에서 하단 고정 내비게이션 66px를 뺀 값)와 교차시켜 "실제로 보이는 범위"만의 중심을 구하도록 하고, 자동 재중앙 정렬 interval과 "화면 가운데로 되돌리기" 버튼 모두 이 값을 기준으로 팬을 계산하도록 바꿨다. iPhone 13 뷰포트로 재검증한 결과 칩이 정확히 그 가시 범위 중앙(오차 1px 이내)에 온다.

**기능 — 유산 재생 화면에 나온 모든 수의 등급 아이콘 표시, PGN 직접 입력 시 진영 선택**
그동안 등급 아이콘은 방금 둔 마지막 한 수(`haloKind`)만 보여주고, 그다음 수로 넘어가면 사라졌다 — `playCount`가 1보다 크면 이전 수의 아이콘이 화면에서 없어지는 셈이었다. `LegacyRevealScreen`에 `kindAt(i)` 헬퍼를 두고 `startCount`부터 `appliedCount`까지 이미 재생된 모든 수의 등급을 매번 다시 모아(`playedKinds`, `useMemo`), 보드 아래에 순서대로 나란히 쌓이도록 바꿨다(각 아이콘은 자기 등급 색으로 은은하게 발광). 또한 `LegacyManageModal`의 "PGN 직접 입력" 경로는 대국 데이터에 진영 정보가 전혀 없어(chess.com 선택은 `g.color`로 자동 판단) 재생 화면에서 어느 쪽을 아래에 둘지 알 수 없었다 — 붙여넣기 단계에 백/흑 토글을 추가해 사용자가 직접 고르게 하고, 그 값을 `entry.side`로 저장한다. `LegacyLightBoard`가 실제 `Board` 컴포넌트와 같은 방식(행·열 모두 뒤집기 + halo 좌표 변환)으로 `flip` prop을 받도록 확장해, `side === "b"`면 재생 화면에서 항상 흑이 아래로 오도록 뒤집는다(구버전 항목은 `side`가 없어 기존처럼 백 기준 그대로 표시).

**기능 — 유산 등록에 "지정한 수 이전 N수"도 함께 선택, 기보 타이핑에 지정한 수 강조 연출, 체스보드 등장을 빛으로 그리는 연출, Playfair Display 폰트 전면 교체**
네 가지를 한 번에 손봤다.
1) `LegacyManageModal`의 "count" 단계에 `beforeCount` 입력을 추가했다(0~`moveIndex`, 기본 0) — 기존 `playCount`(지정한 수부터 앞으로 몇 수)와 별개로, 지정한 수보다 몇 수 전부터 재생을 시작할지 고를 수 있다. `LegacyRevealScreen`의 `startCount`가 `moveIndex`에서 `Math.max(0, moveIndex - beforeCount)`로 바뀌었고, 등급 아이콘 트레일(`playedKinds`)도 이 확장된 범위를 그대로 반영한다.
2) 기보 타이핑 애니메이션에서 유산으로 지정한 수(`moveIndex`)가 나타나기 직전·직후 각각 2초씩 크게 멈추고(기존 토큰 간격은 80ms 그대로), 그 순간 화면이 어두워지며(반투명 검은 오버레이) 타이핑 컨테이너 전체가 확대되고(scale 1→1.14), 지정한 수의 텍스트만 그 유산 등급 색(`QCOLOR[typeInfo.kind]`)으로 크고 진하게 등장하며(`legacyHeroPop` 키프레임), 그와 동시에 이미 나와 있던 다른 토큰들이 지정한 수로부터의 거리에 비례해 순차적으로 한 번 흔들린다(`legacyWaveShake` 키프레임, 파동처럼 퍼져나가는 느낌). 각 토큰을 하나의 문자열로 합쳐 그리던 것을 개별 `<span>`으로 바꿔야 토큰별 스타일·애니메이션이 가능해, 그렇게 구조를 바꿨다(흔들림은 `waveNonce`가 바뀔 때 그 시점까지의 토큰들만 다시 마운트시켜 키프레임을 정확히 한 번만 재생시키는 방식).
3) `board` 단계로 전환될 때, 체스보드의 64칸이 한꺼번에 나타나지 않고 영사기 블록이 있는 하단 중앙에서 빛이 퍼져나가듯 칸마다 조금씩 늦게(칸-중심 거리 × 45ms) 나타나도록 `LegacyLightBoard`의 각 칸을 `motion.div`로 바꿨다 — `LegacyLightBoard`는 수가 진행돼도 다시 마운트되지 않으므로(칸 컴포넌트 자체는 유지되고 `board`/`halo` prop만 갱신됨) 이 등장 연출은 보드가 처음 나타날 때 딱 한 번만 재생된다.
4) 기보·자주 두는 수 등 사이트 전반에서 써 온 `SEQ_FONT`(Playfair Display)를 유산 재생 화면의 기보에 쓰던 `LEGACY_FONT`(Merriweather)와 통일했다 — `SEQ_FONT`의 값 자체를 Merriweather로 바꿔, 참조하는 모든 화면(약 10곳)에 자동으로 반영되게 했다. 더 이상 쓰이지 않는 Playfair Display Google Fonts 임포트도 `index.html`에서 제거했다.

**기능 — 체스보드에도 지정한 수 강조 연출, 등급 아이콘 트레일 정중앙 고정+금테, 앞뒤 수 1~7 선택 박스, 그랜드마스터 유산 보너스 칸**
1) 체스보드 단계(`board`)에서도 유산으로 지정한 수가 실제로 두어지는 순간(`appliedCount - 1 === moveIndex`) 타이핑 단계와 같은 느낌으로 화면 전체가 확대되고(scale 1→1.1) 어두워지며(반투명 검은 오버레이), 보드 위 모든 기물이 그 수가 놓인 칸에서부터 퍼져나가듯(칸-거리 비례 지연) 한 번 흔들린다(`legacyWaveShake` 재사용) — `LegacyLightBoard`가 새 `heroNonce` prop을 받아, 그 값이 바뀔 때만 기물 래퍼(`<span key={"piece-"+heroNonce}>`)를 다시 마운트시켜 흔들림을 정확히 한 번만 재생하고, 보드 최초 등장 시의 칸 순차 등장 연출과는 서로 간섭하지 않는다(칸 자체의 key는 그대로 `r_c`로 고정).
2) 보드 아래 등급 아이콘 목록을 flex 한 줄에서 3열 CSS 그리드(`1fr auto 1fr`)로 바꿔, 유산으로 지정한 수의 아이콘은 항상 가운데 열에 고정되고(앞의 수들은 오른쪽 정렬로 왼쪽 열에, 뒤의 수들은 왼쪽 정렬로 오른쪽 열에 쌓여 자연스럽게 가운데를 향해 모인다) 개수가 비대칭이어도(예: 앞 1수·뒤 7수) 항상 정중앙을 유지한다. 지정한 수의 아이콘에만 금색(`T.brassHi`) 테두리와 은은한 발광(`box-shadow`)을 둘러 다른 아이콘과 구분한다.
3) 유산 등록 "count" 단계의 숫자 직접 입력란(전/후 모두)을 `<select>` 드롭다운으로 바꾸고, 고를 수 있는 값을 1~7수로 제한했다(`beforeOptions`/`playOptions`, 각각 `maxBeforeCount`/`maxPlayCount`와 7 중 작은 값까지). 지정한 수보다 앞선 수가 아예 없으면(0수) 선택 박스 대신 안내 문구만 보여준다.
4) 그랜드마스터 티어에 도달하면 유산 종류(최선·유일·탁월)마다 칸을 하나씩 더(총 6칸) 쓸 수 있게 했다. 기존 저장 키(`best`/`only`/`brilliant`)는 그대로 두고, 보너스 칸은 `"2"`를 붙인 완전히 독립된 키(`best2`/`only2`/`brilliant2`)로 저장해 기존 유산 데이터의 마이그레이션이 필요 없다. `LegacyStoneRow`가 프로필 주인의 xp로 그랜드마스터 여부를 판단해 슬롯 목록을 만들고(`isGM`이면 종류당 슬롯 2개), `LegacyManageModal`은 이제 `typeInfo`(등급 판정용, 항상 기반 종류)와 별도로 `slotKey`(저장용 실제 키)를 받는다. 6칸까지 늘어날 수 있어 유산 줄 컨테이너에 `flexWrap: "wrap"`을 추가했다.

**UI·버그 수정 — 유산 카드 종류별 행 정렬·더보기, 재생 연출 후속 보정(과잉 흔들림·아이콘 레이아웃 밀림)**
1) 유산 타일을 종류(최선/유일/탁월)별로 항상 같은 행에 묶어 보여주도록 `LegacyGrid` 공용 렌더러를 새로 뽑았다 — 그랜드마스터 보너스 칸이 있을 때 자연스러운 flex-wrap 순서로는 다른 종류끼리 한 행에 섞이던 문제(예: best,best2,only가 1행)를 없앴다. "유산" 라벨과 같은 줄에 "더보기 ›" 버튼을 추가했고, 누르면 `LegacyAllModal`이 열려 등록된 모든 유산을 종류별 행으로, 각 유산 아래 등록 날짜(`entry.savedAt`)와 함께 더 크게 볼 수 있다(프로필 주인 본인이면 이 화면에서도 그대로 편집·추가 가능).
2) 기보 타이핑에서 유산 수 이후 토큰이 잘못 흔들리던 버그를 고쳤다 — 이전엔 `waveNonce`가 한 번 켜지면 그 뒤에 새로 타이핑되는 모든 토큰도 첫 마운트 시 `legacyWaveShake`가 걸려(마운트 자체가 애니메이션을 재생시킴) 유산 수 "이후" 토큰까지 흔들렸다. `waveNonce`/키 재마운트 방식을 걷어내고, `typedCount > moveIndex && i < moveIndex`(유산 수가 등장한 뒤 + 그보다 앞선 토큰)로만 애니메이션 이름을 켜는 단순 조건으로 바꿨다 — 이후 토큰은 조건이 항상 거짓이라 아예 흔들리지 않는다. 체스보드의 기물 흔들림도 같은 원리의 버그(`heroNonce`가 한 번 오르면 이후 새로 나타나는 기물도 첫 마운트에 흔들림)가 있어, `heroNonce`+키 재마운트 대신 `heroActive`(그 순간만 참) 불리언을 그대로 CSS `animation-name`에 연결하는 방식으로 바꿨다 — `animation-name`이 "none"→키프레임으로 바뀌는 그 순간에만 브라우저가 애니메이션을 새로 시작하므로 이전/이후 수는 전혀 흔들리지 않는다.
3) 체스보드 단계에서 유산 수를 둘 때의 확대 배율을 1.1→1.4로, 기물 흔들림도 더 큰 진폭의 전용 키프레임(`legacyPieceShake` — 이동폭·회전각·스케일 펄스 모두 `legacyWaveShake`보다 크게)으로 바꿨다.
4) 보드 아래 등급 아이콘 목록이 매 수마다 밀리던 문제(3열 그리드+flex-wrap 조합이 줄바꿈 시 다음 줄 정렬을 깨뜨리는 부작용도 있었다) — `startCount~endCount` 전체 구간(아직 두지 않은 수 포함)의 슬롯을 재생 시작 전부터 전부 고정 렌더링해 두고(`allSlots`), 아직 안 둔 수는 `visibility:hidden`으로 자리만 차지하게 했다. 3열 그리드 대신 단순한 `flex-wrap + justify-content:center` 한 줄로 바꿔, 줄바꿈이 일어나도 각 줄이 스스로 중앙 정렬되어 깨지지 않는다. 유산으로 지정한 수의 자리는 여전히 금색 테두리로 구분된다.

**UI·버그 수정 — 유산 카드를 열 단위로, 타이핑·체스보드 단계 정중앙 확대 알고리즘, 아이콘 금테 왜곡 수정, 지워진 유산 이력 조회**
1) 유산 타일을 종류(최선/유일/탁월)별 "행"으로 묶던 `LegacyGrid`를 종류별 "열"(3열, 그랜드마스터 보너스 칸은 그 열 안에서 세로로 쌓임)로 다시 바꿨다 — 각 열은 `LEGACY_TILE_FLEX`로 폭 1/3을 고정하고, 그 안의 `LegacyStoneTile`/`LegacyEmptySlot`은 (원래 가로 3칸 행 기준으로 설계돼 세로 컨테이너 안에서는 자신의 `flex:LEGACY_TILE_FLEX`가 main axis를 오작동시키므로) 일반 블록 `<div>`로 한 번 더 감싸 무력화했다.
2) 기보 타이핑 단계에서 유산으로 지정한 수가 항상 화면 정중앙에 오도록, 그때그때 확대 애니메이션으로 흉내 내는 대신 애초에 "항상 중심"이 되도록 레이아웃 자체를 3분할(고정 폭 없는 좌측 칸=`flex:"1 1 0", justify-content:flex-end`에 지정 수 이전 토큰들을 오른쪽 정렬로, 중앙의 `flex-shrink:0` 칸에 지정 수 하나만, 우측 칸=`flex:"1 1 0", justify-content:flex-start`에 이후 토큰들을 왼쪽 정렬로)로 다시 짰다 — 앞뒤 토큰이 1~7개로 몇 개든 지정 수는 항상 컨테이너의 기하학적 중심에 남아, 컨테이너 전체가 확대(scale)돼도 그 확대의 중심이 자동으로 지정 수가 된다.
3) 체스보드 단계에서 유산 수가 두어지는 순간의 확대를 고정 배율(1.4배) 대신, 실제 뷰포트 크기를 기준으로 보드가 벗어나지 않는 한도 안에서 계산한 동적 최대 배율(`Math.min(뷰포트 폭의 94%, 뷰포트 높이의 80%) ÷ 보드 크기`)로 바꿨다. 이와 함께 확대가 시작되는 순간 보드 자신의 `getBoundingClientRect()`를 재서 뷰포트 정중앙까지의 이동량(dx, dy)을 계산해 두고, framer-motion으로 보드만(아이콘 트레일·영사기 광선 등 나머지는 그대로) `x/y/scale`을 애니메이션해 뷰포트 중앙으로 잠시 옮겼다가, 그 수가 지나가면 다시 원래 위치·배율(0,0,1)로 되돌아온다.
4) 아이콘 트레일의 금색 테두리가 지정 수 칸에만 실제로 그려지고 나머지 칸은 `border:none`이었던 탓에, 테두리 두께만큼 칸마다 렌더링 크기가 달라져 `flex-wrap` 정렬이 어긋나 보이던 버그를 고쳤다 — 모든 칸에 항상 같은 두께(2px)의 테두리를 두되(지정 수가 아니면 투명) `box-sizing:border-box`로 테두리를 안쪽에 그려, 모든 칸이 항상 정확히 34×34 크기를 유지하도록 했다.
5) 지워지거나 새 유산으로 덮어써서 더 이상 프로필에 보이지 않는 옛 유산도 다시 볼 수 있는 이력 기능을 추가했다. 새 `profile.legacyHistory` 배열(`{ slotKey, entry, replacedAt }[]`)에 슬롯이 교체·삭제될 때마다 그 직전 값을 추가(기존 항목은 절대 지우거나 덮어쓰지 않음)하고, 기존 `legacies`와 완전히 같은 5개 동기화 지점(발행 effect+의존성 배열, 계정 불러오기 병합, OAuth 최초 로그인, 두 곳의 `myPub` 로컬 구성)에 함께 실어 다른 사람의 공개 프로필에서도 이력을 볼 수 있게 했다. "더보기" 화면(`LegacyAllModal`)에 현재 슬롯 열 아래로 "지난 유산" 구간을 새로 두어, 교체·삭제된 순서의 역순(최신이 위)으로 등급 배지·수·교체된 날짜를 나열하고, 누르면 지금 슬롯에 있는 유산과 똑같이 `LegacyRevealScreen`으로 재생해 볼 수 있다(편집·복원은 지원하지 않음 — 순수 열람 전용).

**버그 수정 — 도감 오프닝 트리를 한 화면에 다 담기게, 유산 재생에서 확대된 체스보드가 아이콘 트레일과 겹쳐 보이던 진짜 원인 해결**
1) 도감 모식도 박스가 고정 640px 높이였던 탓에, 위쪽 탭 버튼줄·해금률 문구를 더한 전체 높이가 화면(뷰포트)보다 커지는 기기에서는 박스 일부를 보려면 페이지 자체를 스크롤해야 했다 — `OpeningSchematic`에 `panelH` 상태를 추가해, 박스 자신의 `getBoundingClientRect().top`(그 위 형제 요소들의 높이로만 정해지므로 박스 자신의 높이와 무관해 되먹임 없이 한 번에 계산됨)과 하단 고정 내비게이션(66px)을 뺀 실제 남는 뷰포트 높이로 박스 높이를 동적으로 맞췄다 — 이제 화면 크기와 무관하게 모식도 영역 전체가 스크롤 없이 한 화면에 들어온다.
2) 나침반 중심 회로 칩을 정렬할 때 썼던 `visibleBoxCenter`(박스가 뷰포트보다 커서 잘릴 때를 대비해 "실제로 화면에 보이는 범위"만의 중심을 구하던 보정)를 없애고, 박스 자신의 정중앙(`rect.width/2, rect.height/2`)에 그대로 맞추도록 되돌렸다 — 1번 수정으로 박스가 이제 항상 뷰포트 안에 통째로 들어오므로, 그런 보정 없이도 박스 중심이 곧 화면에 보이는 중심과 일치한다(자동 재중앙 정렬 interval·"화면 가운데로 되돌리기" 버튼 두 곳 모두 반영, 실제로는 요청대로 더 단순한 계산으로 되돌린 것).
3) "유산 수 아이콘에 금테가 제대로 씌워지지 않고 좌표가 왜곡된다"는 반복 신고를 실제 화면으로 재현해 근본 원인을 찾았다 — 지난 라운드에서 손봤던 아이콘 한 칸 한 칸의 테두리 두께 자체는 이미 정상이었고, 진짜 원인은 유산 수를 둘 때 체스보드가 확대되는 연출이었다. 확대된 보드는 `z-index:2`를 가진 채 자신의 레이아웃 박스보다 몇 배(최대 2배 이상)나 크게 그려지는데, 그 바로 아래 있는 등급 아이콘 트레일·금빛 광선·영사기 블록은 `z-index`가 없어(auto) 같은 쌓임 맥락에서 항상 확대된 보드 뒤로 깔려 서로 겹쳐 보였다 — 금테 두른 아이콘이 커진 보드 칸들 사이에 파묻혀 "테두리가 제대로 안 씌워지고 좌표가 뒤틀린" 것처럼 보인 것. 확대가 켜져 있는 동안(`boardHeroActive`)은 아이콘 트레일·광선·영사기 블록 전체를 투명하게 페이드아웃(`pointer-events`도 함께 꺼 숨은 상태에서 클릭 안 되게)해, 애초에 겹칠 대상 자체가 없도록 해결했다. 실제 재현·검증은 Playwright로 진행했다(로그인 없이도 볼 수 있도록 임시 디버그 진입점을 만들어 스크린샷으로 확대 전/중/후 상태를 모두 확인한 뒤 제거).
4) "유산 수 뒤에 더 이상 재생할 수가 없으면 체스보드가 원래 크기로 안 돌아온다"는 버그도 같은 조사 중 재현·확인했다 — `appliedCount - 1 === moveIndex`라는 파생값을 그대로 확대 여부로 썼던 탓에, 유산 수가 재생 구간의 마지막 수라 그 뒤로 `appliedCount`가 더 이상 바뀌지 않으면 이 값이 영원히 참으로 남아 있었다. 확대 여부를 스스로 만료되는 타이머를 가진 상태값(`boardHeroActive`)으로 바꿔, 뒤이은 수가 있든 없든 일정 시간(`HERO_TOTAL_MS`) 뒤에는 항상 원래 크기로 돌아오도록 했다.
5) "유산 수 애니메이션에서 먼저 체스보드를 확대한 뒤 그 수가 두어지도록 해 달라"는 요청에 맞춰, 그 수 차례가 되면 곧장 두지 않고 먼저 확대 창을 연 채(`HERO_PRE_MS`=0.9초) 대기했다가 그 뒤에야 실제로 수가 적용되도록 순서를 바꿨다(기존엔 수가 이미 적용된 뒤에야 확대가 시작돼 "확대→착수" 순서가 아니라 동시에 일어나는 느낌이었다).
6) 영사기가 쏘는 빛처럼, 빛의 체스보드 각 칸의 밝기가 조금씩·계속(무한 반복) 미세하게 흔들리는 연출(`legacyBoardFlicker` 키프레임, `filter:brightness()`)을 추가했다 — 모든 칸이 똑같이 맞춰 숨쉬면 인위적으로 보이므로, 칸 좌표(r,c)로부터 결정적으로 뽑아낸 주기·시작 지연으로 칸마다 어긋나게 반짝인다.

**UI — 프로필 "자주 두는 첫 수"를 카드 폭에 비례하게, 채팅 메시지 메뉴에 오른쪽 클릭 지원**
`FirstMovesDisplay`(백/흑 두 박스)는 90px·최대 200px의 고정 픽셀 폭을 써서, 유산 타일처럼 카드 폭에 비례(%)해 커지는 다른 요소와 달리 컴퓨터(넓은 화면)의 프로필 카드 안에서는 작은 섬처럼 떠 보였다 — 두 박스를 `flex:"1 1 0"`(백)·`flex:"2 1 0"`(흑) 비율로 바꿔, 카드 폭이 넓어지는 만큼(모바일이든 컴퓨터든) 이 블록도 함께 커지도록 했다(백 박스는 `maxWidth:170`으로 과도하게 넓어지지는 않게 함). 채팅 메시지의 수정/삭제(또는 전달/삭제) 메뉴는 지금까지 꾹 누르기(long-press, 480ms)로만 열렸는데, 마우스로는 꾹 누르기가 직관적이지 않아 컴퓨터에서는 사실상 열기 어려웠다 — 텍스트·퍼즐 공유 카드 두 종류 말풍선 모두에 `onContextMenu`(오른쪽 클릭, 브라우저 기본 메뉴는 막음)를 추가해 같은 `openMsgMenu`(중앙 쪽으로 치우치며 이웃 말풍선과 안 겹치도록 이미 계산해 두는 `safeBubbleAnchor`)를 그대로 재사용한다 — 꾹 누르기로 열든 오른쪽 클릭으로 열든 항상 똑같이 안전한 위치에 뜬다.

**버그 수정 — 채팅 퍼즐 "전달" 실패 시 아무 표시가 없던 문제**
`PuzzleShareSheet.send()`는 `puzzleShareSend`가 실패(`false`)해도 버튼이 그냥 "보내기"로 되돌아갈 뿐 아무 안내가 없어, 사용자에게는 "눌러도 전달이 안 된다"로 보였다 — 실패 시 짧은 오류 문구를 보여주도록 고쳤다. 마우스 사용자가 지금까지 꾹 누르기를 몰라 메뉴 자체를 열지 못했을 가능성도 함께 감안해, 위 오른쪽 클릭 지원이 퍼즐 공유 카드에도 똑같이 적용된다.

**기능 — 유산 공유(프로필 아이콘 재배치 + 채팅 카드 + "/legacy N" 명령어)**
1) 유산 타일의 편집(✎) 아이콘을 우하단에서 좌하단으로 옮기고, 우하단에 새 공유(종이비행기) 아이콘을 추가했다(`LegacyStoneTile`). 두 아이콘 모두 내 프로필(관리 권한이 있는 화면)에서만 뜬다.
2) 공유 아이콘을 누르면 퍼즐 공유(`PuzzleShareSheet`)와 같은 패턴의 친구 선택 시트(`LegacyShareSheet`)가 뜬다 — 친구를 고르면 `chat_messages`에 새 컬럼 `legacy_slot`(그 슬롯 키, 예: `best`/`best2`)만 기록한 행을 남긴다(`legacyShareSend`). 유산은 퍼즐과 달리 번호별 전역 저장소가 없으므로 원본 데이터를 복제해 두지 않고, 받는 쪽 화면이 보낸 사람(from_uid=유산 주인)의 "지금" 공개 프로필에서 그 슬롯을 그대로 읽어와 보여준다 — 나중에 그 칸을 바꾸면 이미 보낸 공유 카드도 최신 내용을 보여준다(스냅샷이 아닌 라이브 참조).
3) 채팅창에 `legacy_slot`이 설정된 메시지가 보이면 퍼즐 카드와 같은 톤(어보니+브래스)의 유산 미리보기 카드(등급 배지+새겨진 수)를 렌더링하고, "유산 보기"를 누르면 그대로 `LegacyRevealScreen` 재생 화면이 뜬다. 내가 보낸 유산 카드는 꾹 누르기/오른쪽 클릭으로 삭제할 수 있다(퍼즐과 달리 "전달"은 지원하지 않음).
4) 채팅 명령어 `/legacy N`(N=1~6, `LEGACY_SLOT_ORDER = [best, only, brilliant, best2, only2, brilliant2]`)을 추가해 타이핑만으로도 내 유산을 보낼 수 있게 했다 — 전송 전 반드시 검증한다: N이 4~6인데 그랜드마스터 티어가 아니면 "그랜드마스터 티어에 도달해야 추가 유산을 설정할 수 있어요"를, 그 슬롯이 비어 있으면 "등록되어 있지 않습니다"를 보여주고 전송을 막는다. 1~6 범위를 벗어난 숫자는 애초에 명령어 정규식이 매치하지 않아(기존 `/puzzle`과 같은 방식) 그냥 평범한 텍스트로 전송된다 — 별도 처리를 하지 않아도 자연히 "명령어로 작동하지 않는다."
5) 이 기능은 `chat_messages` 테이블에 새 컬럼 `legacy_slot`(text, nullable)이 필요하다 — 기존 `puzzle_no` 컬럼과 같은 방식으로 Supabase 쪽에 직접 추가해야 한다(이 세션은 실제 DB 접근 권한이 없어 스키마를 직접 만들 수 없었다).

**기능 — 유산 재생 기보 타이핑을 "정중앙 등장 → 충격에 무너짐 → 이동 → 이어 타이핑" 3단계 연출로 재설계**
지정한 수(유산 수)가 나타나기 직전·직후에 2초씩 크게 멈추는 정도였던 예전 연출을, 사용자 스케치 요청대로 뚜렷한 단계로 다시 짰다 — `typingStage` 상태값(`"before" → "heroSolo" → "after"`)으로 명시적인 단계 전환을 관리한다. 1) **before**: 유산 수 이전 토큰이 한 수씩(80ms 간격) 타이핑된다(기존과 동일). 2) **heroSolo**: 다 타이핑되고 나면 유산 수가 화면 정중앙에 큰 글씨(`fontSize:34`)로 먼저 팝업되어 발광하며 등장하고(`position:fixed`로 뷰포트 전체를 덮는 오버레이 안에 배치), 그 등장 직후(0.22초 지연을 둬 "그 충격에" 무너지는 인과관계처럼 보이게 함) 이전 토큰들이 살짝씩 다른 낙하 거리·회전(각 토큰의 인덱스에서 결정적으로 뽑아 매번 같은 느낌을 유지)으로 아래로 무너져 내리며 사라진다. 유산 수는 `HERO_SOLO_MS`=1.5초 동안 정중앙에 머문다. 3) **after**: 유산 수가 왼쪽 위(본문 흐름 속 제자리)로 옮겨가고, 그 뒤를 이어 나머지 토큰이 한 수씩 타이핑된다. 정중앙(2단계)과 본문 흐름(3단계)의 유산 수 스팬은 같은 `layoutId="legacyHeroToken"`을 공유해, framer-motion의 공유 레이아웃 전환(FLIP)이 두 인스턴스 사이의 위치·크기 차이를 자동으로 매끄럽게 보간해 "정중앙에서 왼쪽 위로 날아가 앉는" 움직임을 만들어 낸다(수동으로 좌표를 재는 이전 방식들과 달리, 시작·끝 두 스타일만 선언하면 됨). 실제로 Playwright로 각 단계 전환 순간을 스크린샷으로 재현·검증하는 과정에서, 유산 수가 자리로 옮겨가는 FLIP 전환(0.6초)이 채 끝나기도 전에 바로 다음 토큰이 타이핑되기 시작해 서로 겹쳐 보이는 문제를 발견해, 첫 "이후" 토큰만은 유산 수가 완전히 착지할 시간(`HERO_LAND_MS`=0.65초)을 기다렸다가 나타나도록 고쳤다. (직후 사용자 피드백으로 "먼저 무너진 뒤 등장" 순서를 "먼저 등장한 충격으로 무너짐" 순서로 다시 바꿨다 — 별도의 "collapse" 단계를 없애고 붕괴 애니메이션 자체를 heroSolo 단계 진입 시점에 지연 트리거로 옮겼다.)

**버그 수정 — 유산 수 아이콘 금테가 아이콘 중심과 어긋나 보이던 진짜 원인**
지난 두 차례 손봤는데도(테두리 두께 통일, 확대된 보드와의 겹침 제거) 여전히 안 맞는다는 신고가 있어, 이번엔 Playwright로 실제 DOM 좌표를 직접 재서(`getBoundingClientRect()`) 테두리 원과 그 안 아이콘 이미지의 중심 좌표를 비교했다 — 가로는 정확히 일치했지만 세로로 2px 어긋나 있었다. 원인은 아이콘(`<img>`)을 감싸던 안쪽 `<span style={{filter:"drop-shadow(...)"}}>`이 일반 `inline` 요소였던 것 — 줄바꿈 기준선(baseline) 아래로 보이지 않는 여백(descender 공간)이 비대칭으로 붙어, 바깥 `flex` 컨테이너의 `align-items:center`가 그 안 보이는 여백까지 포함해 중앙 정렬하면서 정작 눈에 보이는 아이콘만 위로 치우쳐 보였다. 이 안쪽 span도 `display:"inline-flex"`로 바꿔 보이지 않는 기준선 여백 자체를 없애자, 실측 좌표 차이가 정확히 0으로 사라졌다.

### OpenChess v0.3.2 — 2026/8/12

**버그 수정 — 아군 폰이 지키는 칸으로 가는 수가 잘못 "탁월한 수"로 판정되던 문제**
탁월한 수(brilliant) 판정을 도맡는 단일 함수 `isSacrifice`의 "직접 희생" 갈래는 이동한 기물이 정적 교환 평가(SEE, `seeSquare`)상 순손실 ≤ -1이 나는 칸으로 가면 희생으로 인정하는데, 이 임계값(-1)은 원래 "비숍/나이트를 폰 두 개와 맞바꾸는" 교환 희생 패턴을 잡아내기 위한 것이었다. 그런데 도착 칸이 아군 폰에게 보호받고 있어(상대가 동가 이상 기물로 잡아도 즉시 그 폰이 되잡는 자리) net이 딱 -1로 떨어지는 경우도 같은 조건을 만족해 버렸다 — seeSquare는 이미 그 폰의 되잡기까지 재귀적으로 반영해 net을 계산하므로, 이 상황에서 실제로 벌어지는 일은 "동가 이상 상대 기물과 맞바꾼 뒤(순손익 0) 그 되잡은 폰마저 결국 내주는" 것뿐인데, 최종적으로 잃는 기물이 폰 한 개뿐이라 진짜 기물 손해(net ≤ -2)에 못 미친다. 즉 폰 하나를 내주는 자연스러운 흐름이 "탁월한 수"로 과대 평가되고 있었다. 새 헬퍼 `pawnDefendsSquare(board, r, c, color)`(핀 등으로 실제로는 되잡을 수 없는 폰은 제외하고, 그 칸을 지키는 아군 폰이 있는지 확인)를 추가해, net이 정확히 -1이면서 도착 칸이 아군 폰의 보호를 받는 경우는 탁월한 수에서 제외하도록 `isSacrifice`에 예외를 추가했다(net ≤ -2로 더 크게 밀리면 폰 이상의 진짜 손해이므로 그대로 희생 인정). `isSacrifice`는 학습 탭·리뷰 전체 분석·리뷰 자유 탐색·집중 학습·퍼즐 생성이 모두 공유하는 단일 함수라 이 수정은 다섯 곳 전부에 함께 적용된다. 이 변경을 포함해 `isSacrifice`에 지금까지 누적된 모든 판정 규칙(직접 희생 임계값, 예정된 손실의 능동적 이용, 안전한 대안 부재 확인, 방치 희생·포크 트레이드오프·진짜 반격·지연 발동 위협 판정, 콤보 중복 방지, 언더프로모션 완화, 열세 상황 제외, 손실 상한 등)을 `public/MEC Reference` 문서에 새로 정리해 두었다.

**UI — 집중 학습 화면 평가치·등급 글자색을 더 진한 크림색으로**
v0.3.1에서 "배경과 대비가 가장 뚜렷하다"는 이유로 이 텍스트를 T.ivoryHi(#FAF2E2, 거의 흰색)로 통일했는데, 막상 써보니 흰색에 너무 가까워 보인다는 피드백을 받았다. `FocusMode` 헤더의 평가치·등급 텍스트(수 이름 옆) 두 곳을 좀 더 채도 있는 크림색인 T.ivory(#EBDDC4)로 낮췄다 — 여전히 어두운 배경과 대비는 충분하되 순백에 더 가까워 보이지 않게 했다.

**UI — 모바일에서 추천 퍼즐을 점선 영역 + 한 줄 가로 스크롤로**
`PuzzleTab`의 "🔥 추천 퍼즐" 섹션이 항상 `grid-template-columns: repeat(auto-fill, minmax(148px,1fr))` 그리드로 줄바꿈되며 표시됐는데, 좁은 화면에서는 카드가 세로로 여러 줄 늘어져 다른 섹션과의 구분이 흐릿했다. 이미 미해결 목록(`openByOpening`)에서 쓰던 "점선 테두리 상자 + 가로 스크롤 한 줄" 패턴을 그대로 가져와, `narrowPuzzleSearch`(기존 768px 브레이크포인트 훅 재사용)가 true일 때만 추천 퍼즐도 같은 형태(고정폭 148px 카드, `overflow-x:auto`)로 렌더링한다. 데스크톱은 기존 그리드 레이아웃 그대로 유지.

**버그 수정 — 퍼즐 풀이 화면 평가치 막대가 정답을 두어도 계속 바뀌어 보이던 문제**
`PuzzleSolver`의 평가치 막대(`EvalBar`)는 v0.2.7에서 "퍼즐 창을 열면 막대가 실제로 살아 움직이는 걸 보여준다"는 의도로, 매 포지션마다 엔진을 `depth 18·movetime 900ms`로 다시 돌려 `onProgress` 콜백으로 들어오는 중간 depth 값들을 55ms 간격으로 순차 재생(`liveEval`/`liveDepth`)한 뒤에야 정적인 `curNode.ev`로 멈추는 구조였다. 의도는 "탐색 애니메이션"이었지만 실제로는 최선의 수를 둬도 숫자가 한동안 계속 바뀌는 것처럼 보여 "내 수가 계속 재평가되고 있나?"라는 혼란을 준다는 지적을 받았다. `ReviewPage`(`analyzeGame`)가 게임 전체를 한 번만 평가해 정적 배열(`evalDisp`)로 고정해 두는 방식과 동일하게 맞췄다 — `curNode.ev`(생성 시 미리 계산해 둔 값)가 있으면 그 값을 즉시·고정적으로 쓰고, 트리 루트나 구버전 노드처럼 `curNode.ev`가 없을 때만 `REVIEW_DEPTH`(20)·`REVIEW_MOVETIME_MS`(1000)로 **한 번만** 평가해 `settledEval`에 고정한다. 중간 depth를 순차 재생하던 `liveEval`/`liveDepth` state와 55ms 재생 루프를 통째로 제거했다.

**UI — 채팅·프로필·검색 창을 모바일에서 전체 화면으로**
`ChatsModal`(채팅)·`ChatUserProfileModal`(프로필)·`UserSearchModal`(검색) 세 모달이 항상 `padding:"60px 16px"` 오버레이 안에 `maxWidth:420`짜리 카드로 떠, 모바일에서도 화면 대부분이 어두운 배경으로 남고 실제 콘텐츠는 좁은 카드 안에 갇혀 있었다. 세 컴포넌트 모두 `useNarrow(640)`으로 좁은 화면을 감지해, 그럴 때는 오버레이 padding을 0으로, 카드는 `width/height:100%`·`borderRadius:0`·테두리 없음으로 바꿔 뷰포트를 꽉 채우는 전체 화면으로 전환한다. 데스크톱은 기존 카드 레이아웃 그대로. `ChatPanel`(채팅 내용)도 새 `fillNarrow` prop을 받으면 메시지 목록을 고정 320px 대신 `flex:1`로 늘려 남은 세로 공간을 전부 채운다 — `ChatsModal`은 narrow일 때 이 prop을 넘기지만, `FriendsModal`에 임베드된 미니 채팅 뷰는 그 카드 자체가 전체 화면이 아니라서 prop을 넘기지 않고 기존 고정 높이를 그대로 쓴다(전체 화면 모드로 전환하려면 부모도 flex 높이 체인을 갖춰야 하므로, 그렇지 않은 호출부까지 임의로 늘리면 오히려 0 높이로 찌그러질 수 있다).

**버그 수정 — @멘션으로 연 프로필 창이 닫아도 자동으로 다시 열리던 문제**
채팅 메시지 말풍선은 커스텀 드래그(당겨서 시각 확인)·롱프레스(수정/삭제 메뉴)를 구현하려고 `onTouchStart/onTouchMove/onTouchEnd`와 `onMouseDown/onMouseMove/onMouseUp`을 같은 요소에 함께 바인딩하고 있었다. 모바일에서 `touchend`를 `preventDefault()`하지 않으면, 브라우저가 호환성을 위해 같은 지점에 합성(ghost) `mouseup`·`click` 이벤트를 최대 수백ms 뒤에 한 번 더 발생시킨다 — `@아이디` 멘션이 담긴 메시지를 탭해 프로필 창을 연 뒤 그 지연 시간 안에 창을 닫으면, 방금 탭했던 메시지 요소에 남아 있던 그 합성 `mouseup`이 뒤늦게 도착해 `onUp`이 다시 실행되고, `wasTap`이 그대로 참이 되어 `setViewProfile(mention)`이 다시 호출되며 프로필 창이 저절로 재오픈됐다. 실제 `touchend` 핸들러에서 `e.cancelable`일 때 `e.preventDefault()`를 호출해, 뒤이은 합성 마우스 이벤트 자체가 만들어지지 않게 했다.

**버그 수정 — 채팅창에서 상대 프로필 사진이 표시되지 않던 문제**
`ChatPanel`은 상대 아바타를 보여줄 `otherPhoto` prop을 받지만, `FriendsModal`에서 채팅을 시작하는 두 진입점(친구 프로필 서브뷰의 채팅 버튼, 친구 목록 줄의 채팅 아이콘) 모두 `setChatWith({ uid, username })`만 호출하고 `photo`를 전혀 넘기지 않았다 — 그 결과 `FriendsModal`을 거쳐 시작한 채팅은 항상 이니셜 아바타로만 보였다(반면 `ChatsModal`에서 시작한 채팅은 `chatWith.photo`를 제대로 넘겨 정상 표시됐다). 두 진입점 모두 이미 화면에 갖고 있던 프로필 정보(`p.photo`, `profiles[u].pub.photo`)를 `setChatWith` 호출에 함께 담고, `FriendsModal`의 `<ChatPanel>` 렌더에도 빠져 있던 `otherPhoto={chatWith.photo}`를 추가했다.

**UI — 일일 퍼즐 캐러셀을 다른 퍼즐 카드와 같은 크림색 디자인으로**
`DailyPuzzleCarouselItem`은 다른 퍼즐 카드(`PuzzleCard`)와 달리 어두운 ebony 그라데이션(`linear-gradient(180deg,#3A2516,#241509)`) 배경을 쓰고 있어 사이트 전체의 퍼즐 카드 톤과 겉돌았다. 카드 배경·보드 프레임 배경·텍스트 색을 전부 `PuzzleCard`가 쓰는 크림 팔레트(`T.ivoryHi`→`#E2D2B2` 그라데이션, 해결 시 연두색 그라데이션, 테두리 `#CDB98E`/`#A9C589`, 그림자 `0 3px 0 ...`)로 맞췄다.

**기능 — 퍼즐 풀이 화면에 "일일 퍼즐로 선정된 기록" 표시**
`resolveDailyPuzzle`이 만드는 일일 퍼즐 객체는 원래부터 `isDaily: true`·`date: "YYYY-MM-DD"` 필드를 갖고 있고, `puzzleShare`가 이 객체를 통째로(다른 일반 퍼즐과 마찬가지로) `puzzles` 테이블의 `data` JSONB 컬럼에 그대로 저장한다 — 즉 같은 id로 이 퍼즐을 다시 열면(검색·공유 링크·기록 등 어떤 경로로든) `puzzle.isDaily`/`puzzle.date`가 항상 그대로 남아 있다. 처음엔 `PuzzleSolver` 헤더 좌상단에 클릭해야 여는 책갈피 배지 + 말풍선으로 구현했는데, 이후 "굳이 눌러야 보이게 할 필요 없다"는 피드백으로 카드 자체 위에 늘 보이는 띠(배경·테두리는 기존 배지와 같은 톤)로 바꿔 `puzzle.isDaily && puzzle.date`면 "YYYY/MM/DD 일일 퍼즐"이 항상 바로 보이도록 했다.

**UI — 클릭·롱프레스로 여는 말풍선이 화면 가장자리에서 잘리지 않도록 방향 보정**
기존 `safeAreaDx`(수 체계 설명 말풍선·채팅 롱프레스 메뉴·알림 카드 공용)는 가로 오프셋만 보정하고, 세로 방향(위/아래 중 어느 쪽으로 열지)은 항상 고정이었다 — 화면 위쪽 가까이의 기준 요소에서 열리는 말풍선이 여전히 위쪽으로 잘릴 수 있었다. 새 `safeBubbleAnchor(anchorRect, popupW, align, margin)`가 가로 오프셋(`safeAreaDx` 재사용)과 함께, 기준 요소가 화면 위쪽 절반에 있으면 아래로(`openDown:true`), 아래쪽 절반에 있으면 위로 여는 방향을 함께 계산해 — 말풍선이 항상 화면 중앙 쪽을 향해 펼쳐지도록 했다. `CircleBadge`(수 체계 설명)와 채팅 롱프레스 메뉴 두 곳에 적용했다(말풍선 꼬리도 열린 방향에 맞춰 반대쪽 모서리로 이동).

**기능/버그 수정 — 도감 오프닝 트리 방사형 레이아웃 전면 재설계**
"칭호 탭 도감 오프닝 트리를 개편하자"는 요청으로 시작해 여러 차례의 피드백을 거쳐 최종적으로 자리 잡은 구조다. 처음엔 4방향(e4/d4/c4/Nf3) 나침반형으로 만들었다가, "부모·자녀 간격은 좋은데 겹침이 너무 많다"는 피드백에 대표 두 수(e4/d4)만 남기고 각각 위/아래 절반(180°)을 통째로 쓰도록 단순화했다(`ROOT_ORDER`·`DIR_OF_ROOT` 축소, `SECTOR_HALF`를 90°→180°로). 반지름(부모·자녀 거리) 계산은 몇 단계를 거쳤다 — 처음엔 각 서브트리가 리프 개수 비율로 각도 구간을 재귀 분할받고 그 구간에서 최소 안전 거리(`SAFE_GAP`)를 만족하는 반지름을 노드별로 역산했는데, 형제 수가 여러 번 갈라지는 단계를 거칠수록 리프 하나의 각도 몫이 매 단계 비율로 곱해져 기하급수적으로 작아져(깊고 갈라짐 많은 라인에서 흔함) 반지름이 수십만~수백만 px까지 폭발하는 근본 문제가 있었다(실측: 최솟값 -360만). 최종적으로는 같은 depth(ply)의 모든 노드를 "사촌까지 통틀어" 한 번에 세어 그 개수로 180°를 균등 분할하고(사촌 사이 여백을 없애고 그만큼을 형제 사이 간격에 씀), 반지름도 depth 단위로(그 depth에서 가장 좁은 노드 기준) 한 번만 계산해 그 depth 전체가 공유하도록 바꿨다. 반지름 증가폭(step)에는 상한(`MAX_RADIAL_STEP`)을 둬 극단적인 경우에도 폭발하지 않게 했고, "ply가 깊어질수록 오히려 간격이 줄어든다"는 피드백에 맞춰 step이 한 번 커지면 그 뒤로는 절대 작아지지 않도록 단조 비감소로 고정했다. 최소 보장 거리(`SAFE_GAP`, 화면 배율 보정 포함)는 요청에 따라 50→70px로, 오프닝 이름이 표시되는 얕은 구간(depth≤3)은 120px로 더 넉넉하게 뒀다. 부모·자녀 간 최소 반지름 증가폭이 `SAFE_GAP`보다 작았던 부수 버그(부모-자식이 거의 포개져 보임), `childrenOf`(부모 key 문자열로 색인된 Map)를 그대로 부모 "객체"처럼 구조분해해 쓰다가 라벨 조상 탐색에서 `undefined.slice` 크래시로 이어진 버그도 함께 잡았다.

**기능 — 오프닝 트리 전체 노드에 오프닝 이름 라벨 표시 + 라벨 충돌 회피**
기존엔 13개 대표 오프닝(`TITLE_OPENINGS`)에 진입하는 뿌리 노드에만 이름 라벨이 붙었는데, "간격도 넉넉해 보이니 트리의 모든 수에 이름을 표시하자"는 요청으로 확장했다. 문자 그대로 모든 노드(3300여 개)에 라벨을 붙이면 이름이 안 바뀌고 그대로 이어지는 구간마다 중복 라벨이 쏟아지고, 그 전부를 서로 안 겹치게 배치하는 충돌 회피 루프가 O(라벨 수²)라 클릭 반응이 7초 넘게 걸릴 만큼 느려졌다 — 부모의 이름을 `effectiveNameOf`로 재귀 메모이즈해 조상까지 한 번에 알아내고, 그 값이 부모와 "달라지는" 지점(새 오프닝/바리에이션 전환점)에서만 라벨을 붙이도록 바꿔 라벨 수를 3347→1495개로 줄였다. 이 수 자신에게 ECO 이름이 없으면(리체스 비동기 데이터가 아직 안 왔거나 애초에 없는 경우가 흔함) `openingNameOf`(로컬 정적 스냅샷 즉시 조회)로 보완한다. 라벨은 그리기 전에 글자 수 기반으로 대략적인 사각형을 계산해, 블록과 겹치면 위 대신 아래에 놓고, 이미 배치된 다른 라벨과 겹치면 세로로 한 줄씩 밀어 겹치지 않는 자리를 찾는다. 그래도 라벨이 밀집하는 자리(같은 depth·같은 팔에서 라벨 붙은 노드가 바로 인접)에는 반지름에 작은 지터(depth 3 이상만, 최대 ±100px, 각도·`SAFE_GAP` 계산 로직은 건드리지 않고 최종 화면 좌표에만 반영)를 줘 노드 자체를 살짝 어긋나게 한다. 텍스트는 `maxWidth`·`ellipsis`를 없애 항상 풀네임을 다 보여준다.

**버그 수정 — 오프닝 트리 세부 카드에 "Main Line"만 뜨던 문제**
흑의 6번째 수처럼 그 수 자신에게 ECO 이름이 안 붙는 깊은 라인을 열면 무조건 "Main Line"만 떴다 — 오프닝 이름은 매 수마다 새로 붙는 게 아니라 마지막 이름 붙은 조상에서 그대로 이어지는(sticky) 성격인데, `DexMoveBlock`이 조상 이름 없이 그 수 자신의 이름만 봤던 게 원인이다. 이미 게임 헤더 등에서 쓰던 조상 탐색 함수 `openingNameOf`를 재사용해 라벨 계산에 추가했다.

**버그 수정 — 오프닝 트리 드래그 중 갑자기 다른 좌표로 튀던 문제**
나침반 팔 사이 빈 부채꼴을 대각선 드래그로 스쳐 지나가는 프레임마다 `clampSchematicPan`이 "화면에 블록이 하나도 안 보인다"고 오판해 가장 가까운 블록으로 하드 스냅했다 — 방사형 레이아웃에서는 전체 콘텐츠의 바운딩 박스 기준 연속적인 클램프로 교체했다(격자형 퍼즐 모식도는 기존 개별 아이템 검사 방식 유지, `clampSchematicPan`이 배열이면 기존 방식, `{minX,maxX,minY,maxY}` 객체면 새 방식을 쓰도록 분기).

**기능 — 오프닝 트리 드래그/휠 스크롤 감도**
트리 반지름이 커지면서 화면 하나로 훑기엔 캔버스가 훨씬 넓어져, 기본 스크롤 감도를 1.5배로 올리고 화면에 블록이 하나도 없는 빈 공간에서는 더 빠르게(처음엔 3배로 시도했다가 "경계를 넘을 때 속도 변화가 너무 급격하다"는 피드백에 2배로 조정) 이동하도록 했다.

**기능/UX — 오프닝 트리 선택 경로·회로 서지가 거리 비례로 흐르는 애니메이션**
수 블록을 클릭하면 중심 회로 칩부터 그 수까지 이어지는 선이 파란색으로 강조되는데, 기존엔 즉시 색이 바뀌었고 회로 칩→e4/d4 사이 트레이스 구간은 아예 반응하지 않았다. 목표 노드까지의 거리를 기준 속도로 나눈 값(0.3~1초로 clamp)을 총 애니메이션 길이로 삼고, 경로 위 각 지점을 "그 지점까지 거리 / 목표까지 거리" 비율만큼만 지연시켜(CSS `transition-delay`) 중심에서 바깥으로 흐르듯 순차 전환되도록 했다 — 칩→e4/d4 트레이스 구간도 선택된 팔이면 같은 규칙으로 함께 켜진다. 회로 칩을 눌렀을 때의 전체 서지 효과도 기존엔 depth(정수 단계 수)에 비례한 지연을 썼는데, depth별 반지름 증가폭이 서로 달라 방향마다 체감 속도가 달랐다 — 실제 반지름(r)을 고정 속도로 나누는 방식으로 바꿔 어느 방향이든 물리적으로 같은 속도로 퍼져나가게 했다.

**UI — 퍼즐 카드 그리드 폭 통일**
"추천 퍼즐" 섹션은 카드 폭을 고정(`width:230`)해 쓰는데, 다른 퍼즐 그리드들은 CSS Grid의 `minmax(230px, 1fr)`라 화면이 넓을 때 남는 공간만큼 카드가 230px보다 늘어나 두 곳의 카드 비율이 달라 보였다 — 모든 그리드를 고정폭(`repeat(auto-fill, 230px)`, `1fr` 제거)으로 통일해 카드가 항상 같은 크기로 렌더링되게 했다(퍼즐 카드 자체는 모든 화면이 공유하는 단일 `PuzzleCard` 컴포넌트라 폭만 맞추면 비율도 함께 통일된다).

**버그 수정 — 채팅 목록의 안읽음 배지가 대화를 읽어도 남아 있던 문제**
`ChatsModal`의 목록 `unread` 수는 모달을 처음 열 때 딱 한 번만 계산돼, 대화를 열어 실제로 다 읽어도(`ChatPanel`이 서버에 읽음 처리) 목록으로 돌아왔을 때 그 값이 그대로 남아 있었다 — 대화창에서 뒤로가기로 나갈 때 목록을 다시 불러오도록(`closeChatWith`가 `loadRows`를 재호출) 고쳤다.

**UI — 헤더 친구·채팅·알림 배지가 옆 버튼에 가려 잘려 보이던 문제**
검색·친구·채팅 세그먼트가 서로 인접한 버튼들이라, 배지(`position:absolute`, 음수 오프셋으로 버튼 밖에 튀어나옴)가 z-index 없이 기본 페인트 순서를 따르다 보니 뒤에 그려지는 옆 버튼 배경에 가려 잘려 보였다 — 친구·채팅·알림 배지 세 곳에 `zIndex:5`를 줘 항상 옆 버튼 위에 그려지도록 했다.

**UI — 무승부 표시를 기보 표시 창 끝의 ½-½ 기호로 통합**
스테일메이트·3회 동형 반복으로 게임이 끝나면 별도의 "무승부 — 스테일메이트/3회 동형 반복" 알림 박스를 따로 띄웠는데, 기보 표시 창(`SequenceBar`·`ReviewMoveStrip`·`ReviewMoveTable`, 모식도·복기 화면 전부)에 `drawn` prop을 추가해 그 박스를 없애고 대신 기보 목록 맨 끝에 표준 기보 표기(½-½)만 덧붙이도록 통일했다.

### OpenChess v0.3.1 — 2026/8/10

**버그 수정 — 마스터 대국이 항상 정확히 5판만 표시되던 문제**
집중학습 "이 수가 두어진 마스터 대국" 목록이 어떤 포지션이든 딱 5판만 뜨고 더 볼 방법이 없다는 제보로 원인을 조사했다. `public/master-games.json`(구매한 외부 대국 데이터베이스 정적 데이터, `scripts/build-master-games.mjs`)을 만들 때 노드(오프닝 트리 포지션)당 대표 게임을 `TOP_K=5`개로 하드캡하고 있었는데, 실제로 노드 3,985개 중 92%(3,667개)가 정확히 5개씩 채워져 있었다 — Lichess 마스터 DB는 얕은 오프닝 수순 밖에서는 게임을 거의 안 내려주므로, 조금만 깊이 들어간 포지션에서는 이 정적 데이터가 사실상 유일한 소스가 되고, 그 상한이 곧 화면에 보이는 전부가 됐던 것. 상한을 아예 없애는 방안(`MAX_PER_NODE=0`)도 시도했지만, 실제 그 데이터베이스 전체(480만 게임)로 돌려보니 "e4"·"d4" 같은 얕은 노드가 유효 게임 대부분(수백만 건)을 그대로 흡수해 `games` 배열이 감당 안 되는 크기로 불어났고 `JSON.stringify`가 `RangeError: Invalid string length`(V8 문자열 최대 길이 초과)로 실패했다 — "노드 수 × 노드당 개수"로 크기를 제한한다는 스크립트의 원래 설계 전제 자체가 무제한에서는 깨지는 것을 확인하고, 상한을 `MAX_PER_NODE=50`(레이팅 합 기준 상위 50개, 필요하면 환경변수로 조절)으로 되돌렸다. `insert()`는 상한이 없을 때(`MAX_PER_NODE=0`, 소량 테스트용)는 매 삽입마다 정렬하지 않고 push만 하다가 `rl.on("close")`에서 노드당 한 번씩만 정렬하도록 바꿔 성능을 지켰다. 화면 쪽은 `AccountChessStats`(구 `FocusPanel`)의 `MASTER_PAGE_SIZE`를 5 → 20으로 올렸다 — `masterGames`는 `fetchAllMasterGames`가 Lichess 마스터 DB·개발자 추가분·그 정적 데이터를 이미 한 번에 다 불러와 둔 배열이라, 페이지를 넘겨도 추가 네트워크 요청 없이 그대로 슬라이스만 바뀐다. 재생성한 결과 유니크 게임이 8,475개 → 66,786개로, 노드 3,985개 중 2,889개(약 72%)가 상한인 50까지 꽉 찼다. 이 샌드박스는 그 사이트 접속이 막혀 있어 파이프라인 재실행은 개발자가 로컬에서 직접 진행했는데, 그 과정에서 `fetch-external-games.mjs`가 요구하는 `source-urls.json`(다운로드 URL 목록)이 저장소에 없다는 것도 함께 드러나, 색인 페이지(`files.html`)를 긁어 자동 생성하는 `scripts/list-external-game-urls.mjs`를 새로 추가했다(초기엔 페이지 경로를 `players.html`/`events.html`/`openings.html`로 잘못 추측해 전부 404였는데, 개별 페이지 404가 전체를 막지 않도록 고치고 `SOURCE_PAGES` 환경변수로 실제 경로를 덮어쓸 수 있게 했다).

**기능 — chess.com 통계에 "리뷰한 대국만" 필터**
`AccountChessStats`(내 프로필·설정 탭의 chess.com 연동 블록)의 시간 규정·색 필터 옆에 체크박스를 추가했다. 리뷰 티켓 시스템이 이미 갖고 있던 `reviewUnlocked`(리뷰 티켓으로 한 번이라도 열어 본 대국의 키 집합)와 `reviewGameKey(game)`(white/black username + endTime로 대국을 식별하는 기존 함수)를 그대로 재사용해 새 테이블·API 없이 구현했다. 체크박스를 켜면 최근 대국 목록뿐 아니라 전적 요약·가장 많이 둔 오프닝·오프닝별 승률까지 전부 그 필터가 적용된 대국만으로 다시 계산된다(단순 목록 필터가 아니라 통계 전체가 "리뷰한 대국 기준"으로 바뀜). `reviewUnlocked`를 넘기지 않는 화면(다른 사람 프로필의 chess.com 통계)에서는 체크박스 자체를 숨겨, 내 리뷰 기록이 남의 프로필에 노출되지 않게 했다 — 지금은 "내 프로필" 카드와 설정 탭의 chess.com 연동 블록, 두 곳에만 연결돼 있다.

**데이터 — 마스터 대국 노드별 상한 방향 재조정 + 정적 파일 방식 유지**
위 `MAX_PER_NODE=50`(모든 노드에 동일 상한) 방식을 다시 손봤다 — 얕은 포지션("e4" 등)일수록 후보 게임이 압도적으로 많아 다양한 갈래를 보여줄 가치가 크고, 깊은(=드문) 포지션은 매칭 게임 수 자체가 적어 작은 상한으로도 사실상 다 담긴다는 점에서, 모든 노드에 같은 상한을 두는 게 오히려 거꾸로였다. `capForPly(ply)`로 `CAP_SHALLOW=500`(ply≤4)·`CAP_MID=150`(ply 5~8)·`CAP_DEEP=50`(ply>8) 3단계 상한을 두도록 바꾸고, 이 값으로 재생성한 결과 유니크 게임이 66,786개 → 128,598개로 늘었다(상한 500까지 찬 노드 126개, 150까지 찬 노드 817개, 50까지 찬 노드 1,815개). 이 참에 마스터 대국을 Supabase에 전체(480만 게임) 업로드해 실시간 검색 가능하게 만드는 방안도 검토했으나(마스터 이름으로 무제한 검색), 무료 티어 용량·복잡도 대비 실익이 크지 않다고 판단해 기존 정적 파일(`public/master-games.json`) 방식을 그대로 유지하기로 했다.

**UI/버그 수정 — 마스터 통계 정렬·페이지 버튼 무반응 수정 + 선수 이름 검색**
`FocusBoxPager`(마스터 대국 정렬·페이지 버튼)가 데스크톱 드래그 스크롤용 `onPointerDown`을 버튼 위에서도 그대로 캡처해, 정렬·페이지 버튼을 눌러도 반응이 없던 문제를 고쳤다 — `e.target.closest('button, a, input, select, textarea, [role="button"]')`로 걸러 그 위에서는 드래그 초기화를 건너뛴다. "이 수가 두어진 마스터 대국"을 "마스터 통계"로 이름을 바꾸고 목록 위쪽 여백을 늘렸으며, 선수 이름 검색창을 추가했다 — 입력하면 지금까지 불러온 대국에 등장하는 선수 이름을 실시간 자동완성으로 보여준다. `normalizePlayerKey(name)`(마침표 제거·소문자화·"성, 이름" 표기를 "성|이름 이니셜"로 축약)로 "Carlsen, Magnus"·"Carlsen,M."·"Carlsen, M." 같은 표기 차이를 같은 사람으로 묶어, 자동완성 목록에 중복 이름이 뜨지 않게 했다.

**UI — 일일 퍼즐 캐러셀 카드 간격 균등화**
캐러셀 카드가 `transform:scale`로 크기를 달리하며(활성 1 / 인접 0.8 / 그 외 0.68) 고정된 슬롯 간격 위에 배치돼, 카드 폭이 제각각이라 실제 보이는 간격이 카드마다 달라 보였다. 카드 폭을 `DAILY_CARD_W`(206px, 보드 190px + 16px)로 고정하고 `scale` 대신 `opacity`(1 / 0.72 / 0.5)만으로 "활성" 여부를 표시하도록 바꿔, 모든 카드 사이 간격이 슬롯 폭(`DAILY_SLOT_W` = 카드 폭 + 28px)만큼 항상 똑같이 보이게 했다.

**기능 — 퍼즐 검색을 서버 전체 데이터로 확장 + 날짜 8자리 직접 입력**
"번호로 풀기" 검색창의 추천어가 예전엔 그 계정에서 한 번이라도 열어본 퍼즐(로컬 배열)에서만 나왔다 — 한 번도 안 열어본 퍼즐은 번호를 다 알아도 추천에 뜨지 않았다. `search_puzzles_prefix(p_prefix, p_limit)` RPC(`no::text like p_prefix||'%'`)를 추가해 Supabase 전체 퍼즐에서 직접 찾도록 바꾸고(250ms 디바운스), 백엔드가 없을 때만 기존 로컬 목록으로 폴백한다. 날짜 검색은 "/" 구분자 없이 `YYYYMMDD` 8자리를 그대로 입력하면 되도록 바꿨다(퍼즐 번호는 항상 6자리라 자릿수만으로 번호/날짜를 구분). 모바일은 입력창 아래 작은 드롭다운(포커스 중일 때만)을, 데스크톱은 그 자리에 실제 `PuzzleCard` 그리드를 포커스 여부와 무관하게 계속 갱신해서 보여준다.

**UI — 일일 퍼즐 알림 팝업(`DailyPuzzleNoticeModal`) 리디자인**
모바일·데스크톱 모두 팝업을 더 키우고 레이아웃을 다듬었다. 모바일은 체스보드를 상단에 별도의 큰 정사각형 구획으로 올려 위아래로 크기를 키우고, `ResizeObserver`로 실제 렌더된 컨테이너 폭을 재서 `AnimatedMove`의 고정 픽셀 `size` prop에 그대로 넘겨(보드가 컨테이너 CSS 폭과 별개로 고정 크기였던 문제) 여백 없이 박스를 꽉 채우도록 했다. 데스크톱은 좌(보드)·우(텍스트) 두 칼럼 비율은 그대로 두고 전체 배율만 키웠고, 우측 칼럼 안 퍼즐 이름과 "풀기" 버튼 사이에 기보(`SequenceBar` 재사용, 한 줄 스크롤)와 코치 말풍선(`MascotBubble` 재사용)을 끼워 넣었다. 코치 말풍선은 설명이 길어질 것을 대비해 `stacked` 옵션을 추가했다 — 마스코트를 88px에서 40px로 줄여 라벨과 나란히 좌상단에 배치하고, 설명 본문은 그 아래 다음 줄부터 전체 폭으로 펼친다. "아직 일일 퀘스트를 다 마치지 못했어요" 안내 문구는 제거했다.

**성능 — 일일 퍼즐 캐러셀 로딩 속도**
`resolveDailyPuzzle`은 로컬 엔진으로 `genPuzzleTree`를 실제로 돌리는 무거운 계산인데, 날짜 시드로 결과가 완전히 결정적이라 전 세계 모든 유저에게 항상 같은 결과가 나온다 — 그런데도 이 계산을 매번 각자의 브라우저에서 새로 돌렸다. 캐러셀에 최근 7일치가 한꺼번에 나열되다 보니 열 때마다 며칠치 계산이 몰려 느렸다. `daily_puzzle_cache` 테이블(date 기본키)에 계산 결과를 올려 두고, 계산 전에 먼저 그 테이블을 확인해 이미 있으면 로컬 엔진을 아예 돌리지 않고 서버 결과만 가져오도록 바꿨다(`puzzles` 테이블과 같은 크라우드소싱 캐시 패턴).

**UI/성능 — 집중 학습 헤더 재배치 + 퍼즐 생성 엔진 호출 절반으로**
평가치·등급 텍스트를 우측 별도 칼럼 대신 수 이름 바로 옆으로 옮기고, 둘 다 같은 색(처음엔 검정을 섞어 더 진하게 했다가 오히려 잘 안 보인다는 피드백에 배경과 대비가 가장 뚜렷한 크림색 `T.ivoryHi`로 재조정)·같은 폰트(사이트 기본 IBM Plex Sans KR — 예전엔 평가치만 monospace라 서로 달라 보였다)로 통일했다. 비게 된 우측 자리엔 상단 툴바에 있던 "퍼즐 풀기" 버튼을 옮겼다. `genPuzzleTree`의 `expand()`가 사용자 수를 둔 뒤 위치를 종료 판정용 단일PV로 한 번, 곧이어 재귀 호출에서 후보 수집용 멀티PV로 또 한 번, 같은 depth·movetime으로 같은 포지션을 두 번 엔진에 묻고 있던 걸 발견해 하나로 합쳤다(시작 포지션 평가도 동일하게 적용) — 사용자 수 노드마다 엔진 호출이 사실상 절반으로 줄었다.

**성능 — 퍼즐 생성·검색 속도 추가 개선**
`requestPuzzleGen`이 로컬 엔진으로 퍼즐을 생성하기 전에 먼저 서버 `puzzles` 테이블을 확인하도록 바꿨다 — 같은 위치(id)의 퍼즐은 항상 결정적으로 같은 결과가 나오므로, 이미 다른 유저(또는 이전 세션의 나)가 만들어 공유해 둔 퍼즐이 있으면 그대로 쓰고 엔진 계산 자체를 건너뛴다(일일 퍼즐 캐러셀과 같은 크라우드소싱 캐시 재사용). `search_puzzles_prefix`가 쓰는 `no::text like 'prefix%'` 조건에 맞는 인덱스가 없어 `puzzles` 테이블이 커질수록(계속 쌓이는 구조) 검색할 때마다 전체 테이블 순차 스캔이 되던 것도, `text_pattern_ops` 표현식 인덱스(`idx_puzzles_no_text_pattern`)를 추가해 해결했다.

**버그 수정 — 퍼즐 모식도 블록 겹침 (근본 원인)**
퍼즐 분기 트리 모식도(`PuzzleSchematic`)에서 블록이 서로 겹쳐 보이는 문제의 원인을 찾았다 — 리프(아직 안 둔 갈래)가 실제 내부 노드로 바뀌면 그 자리 번호(y좌표)를 freeList로 반납해 다음 새 리프가 재사용하는 구조였는데, 이 반납·재사용이 같은 렌더의 같은 DFS 패스 안에서 겹치면 서로 다른 두 블록이 같은 y에 놓여 겹쳐 보였다. 이미 같은 문제 없이 잘 동작하던 오프닝 모식도(`OpeningSchematic`)와 똑같은 방식으로 맞췄다 — 자리 번호는 한 번 배정되면 절대 반납·재활용하지 않고, 리프였다가 내부 노드가 된 키는 리프였을 때 쓰던 자리를 그대로 물려받아 쓴다(자식 평균으로 다시 계산하지 않으므로 위치가 튀지도 않는다).

### OpenChess v0.3.0 — 2026/8/9

**기능 — 외부 대국 데이터베이스 도입: 이론 수 체계 자동 보강 + 마스터 대국 목록에 실제 GM 대국 통합**
구매한 외부 대국 데이터베이스에서 받은 자료(선수 250명·이벤트 982개·오프닝 세부 변형 233개, zip은 압축 해제·순수 pgn은 그대로)를 로컬 파이프라인 스크립트 3종(대량 다운로드 → 이론 라인 추출 → 마스터 게임 매칭)으로 정제했다. Openings 카테고리는 파일명 자체가 세부 변형 이름(예: `RuyLopezMarshall.zip`, `SicilianNajdorf6Bg5.zip`)이라, 각 파일 안 대국들이 공통으로 따라가는 수순을 다수결로 뽑아(1위·2위 동률이거나 남은 대국이 너무 적어지면 그 지점에서 중단) 대표 라인으로 삼고 파일명을 사람이 읽을 이름으로 변환(카멜케이스·수순 번호 구분 정규식)해, 233개 라인을 이론 수 체계(`CONTENT.treeAdds`/`CONTENT.names`, `seedContent()`의 `THEORY_BOOK` 마커)에 자동 등록했다 — 기존 이름은 덮어쓰지 않는다. 나머지 전체 데이터(선수·이벤트·오프닝 통틀어 480만여 게임)는 기존 Lichess 기반 오프닝 트리(`src/data/openings.json`, 4,211개 노드)에 정확히 매칭되는 게임만 추려 `public/master-games.json`으로 압축했다 — 같은 고레이팅 게임이 자기 자신의 수순을 따라가는 여러 노드에서 동시에 top-K(기본 5)에 들 때 게임 본문이 중복 저장되는 걸 막기 위해 `{games:[...], index:{수순:[게임 인덱스,...]}}` 정규화 구조로 설계했고, 어떤 버킷에도 안 남은 게임은 최종적으로 pruning한다(결과: 유니크 게임 8,475개, 3,985개 포지션에 매칭). `fetchAllMasterGames`가 이 정적 데이터를 Lichess 마스터 DB·개발자 추가분(`master_games_dev`)과 출처 구분 없이 항상 합쳐서 반환하도록 변경했고(전용 로더가 최초 1회만 지연 로드해 초기 번들에는 안 들어감), `fetchAnyMasterGamePgn`에 별도 id 분기를 추가해 이 소스의 대국도 열람·리뷰할 수 있게 했다. 원본 검증 중 데이터 품질 문제를 발견했다 — 일부 Events 파일의 헤더가 손상돼 레이팅이 204269 같은 값으로 들어오거나 컴퓨터 엔진끼리의 대국(Elo 3900대)이 섞여 있어, 그대로 두면 레이팅 합산 정렬에서 항상 1위를 차지해 실제 GM 대국들을 밀어내는 문제였다 — 화이트·블랙 레이팅 중 하나라도 2900을 넘으면 게임 자체를 제외하는 필터를 추가했다. 기존 `master_games_dev`(Supabase) 경로로 대량 삽입하는 방안도 검토했으나, `fetchDevMasterGames`가 테이블에서 최대 500행만 가져온 뒤 클라이언트에서 필터링하는 구조라 대부분(최근 연도 500개 제외)이 조회조차 안 되는 걸 확인하고 정적 파일 방식으로 전환했다.

**기능 — 체스보드 모바일 터치 드래그 지원**
기존 기물 이동은 HTML5 네이티브 드래그(`draggable`/`dragstart`/`drop`)를 써서, 터치 환경에서는 애초에 그 이벤트들이 발생하지 않아 모바일은 탭-선택 방식만 가능했다. `Board` 컴포넌트에 마우스·터치·펜을 모두 아우르는 Pointer Events 기반 드래그를 새로 구현해 네이티브 `draggable`을 대체했다 — 기존 `onPieceDrag`/`onDrop` 콜백 계약은 그대로 재사용해 호출부(각 화면)는 손대지 않았다. 5px 임계값 전에는 탭으로 간주해 `onDrop`을 부르지 않고(탭-선택 동작 보존), 드래그 중에는 원래 칸의 기물을 옅게 하고 손가락/커서를 따라다니는 고스트 기물을 그린다(터치에서 손가락에 가리지 않도록 위로 offset). `touchAction:"none"`으로 터치 스크롤이 드래그 제스처를 가로채지 못하게 했고, 드래그 뒤에 이어지는 합성 `click` 이벤트를 한 번 무시해 방금 옮긴 기물이 재선택된 것처럼 보이는 문제도 막았다. Playwright로 드래그 이동과 기존 탭-탭 이동 둘 다 정상 동작함을 확인했다.

**기능 — MEC "수비자 제거" 전술 추가 + 퍼즐 기물/포지션 우위 종류 구분**
코치 코멘트(MEC)에 최우선 규칙으로 "수비자 제거"(Remove the Defender)를 추가했다 — 이 수로 잡은 상대 기물이 다른 상대 기물을 지키고 있었고 그게 다음 수에 공짜로 잡히게 되면, 고정 문구로 대신하고 눌러보면 ①원래 포지션에서 공짜로 잡힐 기물의 공격자·수비자 화살표 표시 → ②수비자를 제거하는 실제 수로 전환 → ③사라진 수비자 화살표 페이드아웃, 3단계 애니메이션이 재생된다. 퍼즐 생성기(`genPuzzleTree`)에 `puzzleType`(포지션 우위/기물 우위) 옵션을 추가했다 — 기존엔 cp 이득(target) 하나만 종료 기준이라, "수비자 제거"처럼 최선 수만 이어지는 명확한 전술 퍼즐이 그 목표를 못 채워 억지로 안 끝나는 경우가 있었다. 기물 우위 모드는 cp 기준 없이 기물 점수가 실제로 유리해지는 즉시 종료한다. 개발자 모드 퍼즐 편집 UI에 종류 선택 버튼을 추가했고, 목표 cp 입력은 포지션 우위일 때만 노출한다.

**버그 수정 — 퍼즐 생성 시 상대의 진짜 최선 응수가 사라지던 근본 원인 수정 + 개발자용 형제 갈래 추가 도구**
`genPuzzleTree`의 "전개하는 수는 라인을 안 잇는다"(`isDevelopingMove`) 필터가 `depth>=1`이면 상대 응수 차례에도 그대로 걸렸다 — 상대의 최선(또는 유일한) 응수가 하필 마이너 기물을 홈스퀘어에서 움직이는 수(체크 대응 직후 첫 전개 등 흔한 상황)면 그 응수 자체가 통째로 필터링되고, 남은 후보 중 최상위가 무조건 채택되는 구조라 진짜 최선 응수 대신 훨씬 나쁜 수만 유일한 갈래로 남았다. 이 필터를 사용자 차례에만 적용하도록 수정했다. 앞으로도 자동 생성이 비슷하게 놓칠 경우에 대비해, 개발자 모드 모식도의 사용자 수 노드마다 "형제 갈래 추가"(⑂) 버튼을 추가했다 — 누르면 `puzzleCandidatesAt`이 그 자리에서 실제로 있었던 후보 목록(필터로 걸러졌던 것 포함, kind·평가치·채택률 그대로)을 다시 보여주고, 골라서 바로 추가하거나 목록에 없는 수를 직접 입력할 수 있다. 기존 라인은 건드리지 않고 그 자리에 형제 자식만 덧붙인다.

**버그 수정 — 퍼즐 종료 조건을 시작 포지션 대비 실제 이득 기준으로 변경 + 강제 메이트 수순 완결**
`genPuzzleTree`의 라인 종료 판정을 세 가지로 개선했다. (1) 절대 cp(target) 대신 "퍼즐 시작 포지션 대비 실제로 얻어낸 cp 이득"(`cpGain = userCp - startCp`)을 기준으로 삼아, 시작부터 유리했던 포지션이 한 수 만에 끝나버리거나 반대로 균형 포지션에서 서서히 조여가는 수순이 상한(`maxPlies`/`maxNodes`)에 막혀버리던 문제를 함께 해결했다. (2) "실질적인 이득" 조건에 기물점수 변동(`matGain > 0`)을 추가해, 기물 변동 없이 포지션만 미세하게 좋아지는 수순은 아직 끝난 것으로 치지 않는다. (3) 상대가 강제 메이트당하는 수순을 찾으면 그 자리에서 곧장 끝내지 않고(`forceMate` 플래그), 실제 체크메이트(#)가 나오는 수까지 `maxPlies` 상한을 무시하고 계속 확장한다 — `maxNodes`(전체 노드 예산)는 폭주 방지 안전망으로 그대로 유지했다.

**성능 — 퍼즐 트리 생성(genPuzzleTree) 병렬화**
예전엔 트리의 모든 노드(서로 독립적인 형제 가지 포함)를 단일 엔진(FIFO 큐 하나)으로 순서대로 평가해, 라인이 깊어질수록(`maxPlies`) 생성 시간이 거의 선형으로 늘어났다. 게임 리뷰(`analyzeGame`)와 같은 전용 워커 풀(`getAnalysisPool`)을 빌려 써서, 형제 가지의 노드 평가를 여러 워커에 나눠 동시에 돌리도록 바꿨다 — depth·movetime(정확도)은 전혀 건드리지 않고 병렬도만큼 순수하게 생성 시간만 줄인다.

**성능 — 게임 리뷰 결과를 수 단위로 스트리밍**
`analyzeGame`이 모든 포지션 평가가 끝난 뒤 한 번에 채점하던 것을, 포지션이 하나씩 끝날 때마다(work-stealing 풀이라 순서 없이 끝남) 채점 가능한 수를 즉시 채점해 `onMove`로 흘려보내도록 바꿨다. 엔진 탐색량(depth 20·movetime 1000ms)은 손대지 않고 화면이 결과를 "기다리는" 방식만 바꿨다 — `ReviewPage`는 첫 수가 채점되자마자 리뷰 화면을 띄우고 나머지는 분석되는 대로 채워진다. 정확도%·단계별 하이라이트처럼 전체 통계가 필요한 모바일 요약 화면은 그대로 100% 완료를 기다리되, "기다리지 않고 바로 리뷰 시작" 지름길을 추가했다. 평가 그래프는 배열 길이를 게임 전체 길이로 고정하고 미완료 구간을 마지막 값으로 채워, 수가 채점될 때마다 그래프 축이 흔들리며 다시 스케일되는 것을 막았다.

**기능 — 학습 탭 엔진/이론 수 블록이 depth 변동에 따라 순위 이동 애니메이션으로 반응**
엔진 라인 상위 3줄(`EngineLineRow`)의 `key`를 슬롯 번호 대신 그 줄의 첫 수(수의 정체성)로 바꾸고 `motion.div layout`을 적용해, depth가 깊어지며 MultiPV 순위가 바뀔 때마다 자리가 뚝뚝 끊겨 바뀌던 것 대신 같은 수는 같은 컴포넌트로 유지되며 옛 위치에서 새 위치로 부드럽게 이동(FLIP)하도록 했다. 이어서 학습 탭 메인 리스트(`MoveTile`, 이론/비이론 수 블록 카드)도 이론 수를 정렬 대상에서 제외하고 고정 순서로 두던 것을 없애고, 이론 수도 비이론 수와 동일하게 평가치/채택률(rank)로 정렬해 depth가 바뀔 때마다 함께 새 순위로 이동하도록 했다(이론 수 묶음이 비이론 수보다 항상 먼저 온다는 순서는 유지).

**UI — 학습 탭 "수 설명" 캐러셀을 해설 블록에 통합**
집중학습의 수 설명(`move_notes`) 카드를 보드 옆 별도 블록으로 두지 않고 기존 "해설" 박스 자리에 통합했다. 설명이 없을 때만 기존 개발자 정적 해설을 폴백으로 보여준다. 가로 스와이프 캐러셀 대신 세로 축으로 5초마다 자동 스크롤되며, 카드 좌상단에 작성자 "@아이디"를 표시하고 직접 위아래로도 스크롤할 수 있다.

**UI — OC 나이트 코인·리뷰 티켓 아이콘 크기 확대**
두 아이콘이 대부분 11~22px로 그려져 실제 이미지(`oc-coin.png`, `review-ticket.png`)의 디테일이 잘 안 보였다 — 상점 잔액 칩(18→26, 22→30)을 포함해 사이트 전역 18곳의 `CoinIcon`·`TicketIcon` 크기를 맥락에 맞게 15~20% 정도씩 키웠다.

**UI — 본문 텍스트 색상·굵기 통일**
거의 검게 보이던 기본 글자색(`T.ink`, `#2A1A0E`)을 사이트 곳곳에서 이미 쓰던 브라우니 갈색(`#5A3A22`, 기존 `T.cocoa` 폴백값)으로 바꿔 크림색 블록 위에서 더 잘 어울리게 했다. 해설·설명 문단·목록 등 본문 텍스트에는 `fontWeight`를 추가해 더 두껍게 보이도록 했다.

**시스템 — 공동 개발자 권한 범위 확장**
`canEdit`·`devUnlockAll`은 이미 공동 개발자(`codevOn`)를 포함하고 있었지만, chess.com 계정 변경 30일 쿨다운 우회·개발자 자원 조정 패널(`DevResourcePanel`) 노출·리뷰 티켓 시스템 우회(`openReview`) 세 곳이 `isDev && devOn`으로만 걸려 있어 공동 개발자는 여전히 막혀 있었다 — 공동 개발자 지정/해제(`canManageCodev`)만 정식 개발자 전용으로 남기고 나머지는 모두 확장했다.

### OpenChess v0.2.9 — 2026/8/4

**기능 — 게임 리뷰 평가치 막대가 실시간 엔진 라인 1순위 평가를 따라 계속 움직이도록 변경**
사용자 요청 — 리뷰 페이지의 평가치 막대(`EvalBar`)가 실시간 엔진 추천 수 줄(`engineLines`) 1순위의 평가 변화를 따라 계속 함께 바뀌도록 해 달라는 요청. 기존 `activeEvalDisp`는 `exploring`(자유 탐색 중)일 때만 `engineLines[0].ev`를 쓰고, 실제 기보 수를 볼 때는 페이지 진입 시 `analyzeGame`이 미리 한 번 계산해 둔 정적값(`result.evalDisp[curPly]`)에 그대로 고정돼 있었다 — 정작 그 아래 엔진 라인 effect(`evaluateMulti`의 `onLines` 스트리밍 콜백)는 `exploring` 여부와 무관하게 이미 모든 포지션에서 depth가 깊어질 때마다 `engineLines`를 계속 갱신하고 있었으므로, 실제 기보 수를 볼 때는 그 살아있는 값을 그냥 버리고 있었던 셈이다. `activeEvalDisp`를 `engineLines.length ? engineLines[0].ev : (exploring ? null : evalDisp[curPly])`로 바꿔, `engineLines`가 채워져 있는 한(포지션이 바뀔 때 곧장 `[]`로 비워지므로 항상 "지금 포지션" 결과만 반영됨) 항상 그 값을 우선하고, 아직 첫 스트리밍이 도착하기 전(포지션 진입 직후 짧은 순간)에만 기존 정적값으로 임시 표시한다. 학습 탭(`useMergedMoves`)의 `barEval`과 같은 방향이지만, 학습 탭은 `linesPending`(검색 완료 여부) 동안엔 별도의 단일PV 진행 콜백(`onEvalProgress`→`posEval`)으로 임시값을 채우는 구조라 그 게이트를 그대로 가져오면 이 페이지에서는 검색이 끝날 때까지 스트리밍을 아예 무시하게 돼(리뷰 페이지엔 그 별도 채널이 없음) 의도와 반대로 동작했다 — `!linesPending` 게이트 없이 `engineLines.length`만으로 판단하도록 고쳤다. Playwright로 실제 브라우저에 접속해(임시 디버그 훅으로 `openReview` 호출) 새 미들게임 포지션에 진입한 직후 ~1초 movetime 동안 평가치를 프레임 단위로 로깅한 결과, `cp: 0 → 17 → 8 → 8 → 8 → 13 → 13 → 15`로 depth가 깊어짐에 따라 막대 값이 실시간으로 여러 번 갱신되는 것을 확인했다(수정 전에는 이 구간 내내 진입 시점의 정적값 하나로 고정돼 있었음).

**기능 — 일일 퀘스트 전체 클리어를 명시적인 팝업으로 알리고, 접속하지 않은 사이 클리어됐어도 최초 접속 시 띄움**
사용자 요청 — 일일 퀘스트를 다 클리어했을 때 좀 더 명시적인 팝업 알림을 띄워 달라는 것과, 사이트에 접속하지 않던 사이(예: chess.com에서만 대국을 둬서 활동 퀘스트가 채워진 경우) 클리어됐다면 다음 최초 접속 시 그 사실을 띄워 달라는 요청. 기존엔 4개 활동 퀘스트+완료 보상을 모두 받으면(`bonusClaimed`) 다른 코인 획득과 구분되지 않는 1.8초짜리 작은 토스트(`setToast({type:"coins",amount:50})`) 하나만 뜨고 끝이라 놓치기 쉬웠고, 그마저도 접속 안 한 사이 chess.com 연동만으로 조용히 클리어된 경우(그 판정 effect 자체가 앱이 열려 있어야만 도는 구조라, 다음 접속 시 이 effect가 한 번에 다 통과해버리면 사용자가 뭐가 언제 클리어됐는지 알아챌 기회가 아예 없었다)는 그 토스트조차 다음 접속 순간 스쳐 지나가듯 떴다 사라졌다. "보상을 지급했는지"(`bonusClaimed`)와 "그 사실을 사용자에게 실제로 알렸는지"를 분리해야 한다고 보고, `dailyQuest`에 새 `clearAnnounced` 플래그를 추가했다(`genDailyQuest` 초기값 `false`, 날짜가 바뀌어 퀘스트가 재생성될 때 함께 초기화됨). 보너스 지급 effect에서는 토스트 호출을 제거하고 지급만 남기고, 별도 effect를 하나 더 둬 `bonusClaimed && !clearAnnounced`이면(이번 세션에 방금 클리어했든, 이전에 이미 클리어됐지만 아직 못 봤든 상관없이) 새로 만든 `DailyQuestClearedModal`(마스코트 KOKOA celebrate 일러스트, "오늘의 퀘스트 클리어!" 타이틀, +20 XP·+50 코인 보상 요약, 확인 버튼 — 기존 `DailyPuzzleNoticeModal`/`AnnouncementModal`과 같은 카드 스타일)을 열고 그 즉시 `clearAnnounced: true`로 마킹해 다음 접속부터는 다시 뜨지 않게 한다. Playwright로 두 시나리오를 검증했다: (1) 세션 중 마지막 퀘스트를 클리어하는 순간 팝업이 뜨고, 확인 후 새로고침해도 다시 뜨지 않음(`clearAnnounced`가 로컬 저장소에 함께 영속화됨). (2) localStorage를 직접 조작해 "이미 `bonusClaimed:true`지만 `clearAnnounced:false`인 상태"(접속 안 한 사이 클리어된 상황을 재현)를 만든 뒤 완전히 새로 페이지를 열었더니, 아무 조작 없이도 팝업이 자동으로 떴고 그 다음 재접속부터는 다시 뜨지 않는 것을 확인했다.

**UI — 일일 퀘스트 클리어 팝업 디자인 고급화 + 클리어한 퀘스트 목록·기보·대국 정보 표시**
사용자 피드백 두 번에 걸친 개선. (1) 처음 만든 팝업이 "너무 대충 만든 티가 난다"는 지적 — 밋밋한 흰 카드 하나에 텍스트만 나열돼 있던 것을, 사이트 전역 배경과 같은 톤의 짙은 라디얼 그러데이션 배너(다른 화면들이 쓰는 `radial-gradient(130% 120% at 50% -10%, #34230F 0%, #150C06 65%)`와 같은 계열) 위에 마스코트를 금빛 원판(다른 곳의 `GOLD_DISC`와 같은 톤: `radial-gradient(70% 70% at 32% 28%,#F3DFAE,#C49A50 68%,#8A6C2F 100%)`)에 얹은 2단 카드로 재구성했다. 배너에는 기존 `xpStarPop` 키프레임(팝인 후 떠오르며 사라짐)을 재사용해 위치·크기·지연만 다르게 흩뿌린 반짝임 5개를 추가했는데, `animation-fill-mode: forwards`를 안 주면 애니메이션이 끝난 뒤 인라인 style의 기본 opacity로 되돌아가 다시 반짝 나타났다 사라지는 것처럼 깜빡였다 — longhand 속성으로 명시해 고쳤다. 보상 표시는 밋밋한 텍스트 대신 브랜드 컬러(브라스) 알약형 배지로, 등장·퇴장은 즉시 나타나던 것에서 framer-motion 스프링 팝인/아웃(`AnimatePresence`로 부모 쪽 조건부 렌더링도 함께 감싸야 exit 애니메이션이 실제로 재생됨)으로 바꿨다. (2) 이어서 "무슨 퀘스트를 깼는지도 보여주고, 기보도 넣고, 프로필 chess.com 통계처럼 그 퀘스트를 클리어한 대국 정보도 보여 달라"는 추가 요청 — `DailyQuestClearedModal`에 `dailyQuest`·`chesscom`·`onOpenGameAnalyze`를 props로 받게 하고, 5개 퀘스트 슬롯(새 퍼즐 N회·일일 퍼즐·chess.com 활동 퀘스트 3개) 전부를 체크리스트로 나열하는 `QuestClearRow`를 추가했다. 오프닝 퀘스트는 이미 있던 `questOpeningMovesText`(도감·`DailyQuestCard`에서 쓰던 것과 같은 함수)로 그 오프닝의 기보(예: "1.e4 e5 2.Nf3 Nc6 3.Bc4")를 그대로 보여준다. chess.com 활동 퀘스트(오프닝/5회 플레이/3회 승리)는 새 `questMatchingGames(q, chesscom)` 헬퍼가 그 퀘스트를 실제로 클리어한 조건과 정확히 같은 기준(오늘 날짜의 대국 중 오프닝이 일치하는지, 혹은 승리한 것인지)으로 chess.com 대국을 걸러내고, 그 대국을 프로필의 `AccountChessStats` "최근 대국" 행과 같은 요소(진영 색 막대·승패·타임컨트롤·날짜·상대 닉네임과 레이팅·분석 이동 버튼)를 좁은 팝업 폭에 맞게 축소한 `QuestClearGameRow`로 보여준다 — 분석 버튼을 누르면 `onOpenGameAnalyze`로 그 대국이 그대로 게임 리뷰 페이지로 넘어간다. 처음엔 이 체크리스트 영역에 `maxHeight:240 + overflowY:auto`로 자체 스크롤 상자를 뒀는데, 바깥 배경(모달 전체를 감싸는 backdrop)도 이미 `overflowY:auto`로 스크롤을 맡고 있어 이중 스크롤 상자가 되면서 그 240px를 넘는 세 번째 chess.com 퀘스트 행이 안내 없이 통째로 안 보이는 버그가 있었다 — 안쪽 스크롤 상자를 없애고 바깥 배경 스크롤 하나로 통일했다. Playwright에서 `page.route`로 chess.com API(`/games/archives`, `/games/YYYY/MM`)를 가짜 대국 5판(Italian Game 오프닝 포함)으로 모킹하고, 임시 디버그 훅으로 `profile.chesscom`을 연동한 뒤 오늘의 퀘스트를 그 대국들로 다 클리어한 상태를 재현해 검증했다 — 5개 퀘스트 전부가 목록에 뜨고, 오프닝 퀘스트엔 정확한 기보가, 활동 퀘스트 두 개엔 실제로 매칭된 대국 정보가 표시되며, 분석 버튼을 누르면 게임 리뷰 페이지로 정상 이동해 그 대국 분석이 시작되는 것을 확인했다. 좁은 뷰포트(700px 높이)에서도 바깥 스크롤로 확인 버튼까지 끝까지 도달 가능함을 확인했다.

**UI — 일일 퀘스트 클리어 팝업을 "게임 보상 화면"처럼 더 화려하게**
사용자 요청 — 좋은데 디자인을 조금 더 게임처럼 만들어 달라는 것. 게임의 보상/레벨업 화면에서 흔히 쓰이는 문법을 몇 가지 겹쳐 넣었다. (1) 원판 뒤에 `repeating-conic-gradient`로 만든 햇살(sunburst) 광선을 두고 새 `questRaySpin` 키프레임으로 16초 주기로 천천히 회전시킨다 — `to{}`만 정의하고 `from`은 생략해, 인라인 style에 이미 있던 정적 `transform:translate(-50%,-50%)`을 애니메이션 시작점으로 그대로 이어받게 했다(값을 두 곳에 중복 정의하지 않기 위함). (2) 마스코트를 얹은 금빛 원판에 `questGlowPulse`로 숨쉬듯 부풀었다 가라앉는 발광 테두리를 무한 반복시킨다 — 기존 정적 `boxShadow`를 이 키프레임의 0%/100% 상태에 그대로 옮겨 넣어, 애니메이션이 도는 동안에도 원래 원판의 입체감(안쪽 하이라이트·그림자)은 그대로 유지된다. (3) 기존 별 반짝임 위에 색색 컨페티 조각(`QUEST_CLEAR_CONFETTI`, 작은 사각형 7개)을 `questConfettiFall`로 위에서 떨어뜨리며 회전·페이드시켰다. (4) 제목을 잉크색 평문 대신 금빛 세로 그러데이션을 `WebkitBackgroundClip:"text"`로 텍스트에 입히고 아래 그림자를 깔아 "각인된 금속 글자" 느낌을 내고, 양옆에 가는 금선+다이아몬드를 둬 현판처럼 감쌌다. (5) 보상 배지(+XP·+코인)는 카드가 자리 잡은 뒤 `questBadgePop`(오버슈트가 있는 `cubic-bezier(.34,1.56,.64,1)`)으로 하나씩 튕기며 나타나고, 그 안의 숫자는 새 `AnimatedCountUp` 컴포넌트(`requestAnimationFrame`으로 ease-out 세제곱 곡선을 그리며 0에서 목표치까지 약 0.55초 만에 도달)로 카운트업된다. (6) 클리어한 퀘스트 목록의 각 줄도 `framer-motion`으로 위에서부터 순서대로 슬라이드+페이드 인(줄마다 0.06초씩 지연)되도록 해, 다섯 줄이 한꺼번에 나타나지 않고 차례로 "펼쳐지는" 느낌을 준다. (7) 확인 버튼에는 다른 화면(그랜드마스터 보드 등)이 이미 쓰고 있던 금속 광택 스윕 클래스(`gm-board-shine`)를 그대로 얹어, 이 팝업만의 새 이펙트를 따로 만들지 않고도 사이트 전역에서 "특별한" 요소에 쓰는 것과 같은 시각 언어로 은은한 하이라이트가 주기적으로 스친다. Playwright로 애니메이션 도중 여러 프레임을 연속 캡처해 확인했다 — 진입 직후 프레임에서 컨페티가 낙하 중이고 광선이 회전한 상태이며 보상 숫자가 목표치 이전 값(예: "+17 XP", "+43")으로 카운트업 중인 것, 활동 퀘스트 줄이 아직 스태거 지연 중이라 옅게 보이는 것을 확인했고, 정착된 프레임에서는 모든 배지·목록·버튼이 최종 상태로 온전히 자리 잡은 것을 확인했다.

**기능 — 일일 퀘스트 5개 중 하나만 클리어해도 어떤 퀘스트인지 알려주는 팝업 추가**
사용자 요청 — 전체 5개를 다 클리어했을 때 뜨는 팝업(`DailyQuestClearedModal`) 말고, 퀘스트 하나를 클리어할 때도 팝업을 만들어 달라는 것. 기존엔 개별 퀘스트 클리어(`claimQuestCoins`, 퍼즐 목표 달성·일일 퍼즐 풀이·chess.com 활동 퀘스트 3개 공용)가 전부 화면 중앙에 뜨는 범용 "coins" 토스트(별 아이콘 팝업 + "+10 OC 나이트 코인" 텍스트, 티어 승급 보너스와 동일한 컴포넌트를 공유)로만 표시돼, 정작 어떤 퀘스트를 깼는지는 알 수 없었다. `questSlotLabel(key, dq)` 헬퍼를 새로 만들어(다섯 슬롯 키 "puzzle"/"dailypuzzle"/"cc_0"~"cc_2"를 사람이 읽는 라벨로 — `DailyQuestClearedModal`의 체크리스트가 이미 인라인으로 하던 것과 같은 로직이라 그쪽도 이 함수를 쓰도록 정리해 라벨 문구가 두 화면에서 갈리지 않게 했다), `claimQuestCoins`가 지급 시점에 그 슬롯의 라벨을 함께 담아 새 `"questClear"` 타입 토스트를 띄우게 했다. 렌더는 기존 title/share_reward 토스트와 같은 상단 배너 자리(`top:70, 가운데 정렬`)에 체크 배지(전체 클리어 팝업과 같은 `questBadgePop` 튕김 애니메이션)·퀘스트 라벨(길면 말줄임)·코인 보상을 한 줄로 담고, 확인 버튼에 썼던 `gm-board-shine` 금속 광택 스윕도 그대로 얹었다. 하루에 최대 5번 뜰 수 있는 만큼 전체 클리어 팝업과 달리 확인 클릭을 요구하지 않고 2.6초 후 자동으로 사라진다. Playwright로 임시 디버그 훅을 통해 `claimQuestCoins("puzzle", 10)`·`claimQuestCoins("cc_0", 10)`을 직접 호출해 검증했다 — 진입 애니메이션(`lockpop`, 0.4초짜리 스케일 팝) 도중에는 인라인으로 걸어 둔 가운데 정렬용 `transform:translateX(-50%)`이 애니메이션이 완전히 덮어써 잠깐 우측으로 치우쳐 보인다는 것도 스크린샷 비교로 함께 확인했다(0.4초 뒤 애니메이션이 끝나면 인라인 transform이 다시 적용돼 정상적으로 가운데 정렬된다 — title/share_reward 등 같은 wrapper를 쓰는 기존 토스트들에도 원래 있던 특성이라 이번에 새로 생긴 문제는 아니다). 0.7초 후 안착된 화면에서 "새 퍼즐 2회 풀기"·"chess.com에서 게임 3회 승리" 두 라벨 모두 정확히 표시되는 것을 확인했다.

**UI — 티어 승급 연출도 "게임 보상 화면"처럼 카드형 팝업으로 재구성**
사용자 요청 — 일일 퀘스트 팝업과 비슷하게 티어 승급 연출도 팝업 창으로 만들되, 기존 애니메이션(이전 티어 기물이 흔들리다 왼쪽으로 퇴장하고 새 기물이 오른쪽에서 등장)과 반투명 검은 배경(`rgba(6,3,1,.75)`)은 그대로 유지하면서 팝업 창 자체와 추가 요소에만 디자인·애니메이션을 더해 달라는 것. 기존 `TierUpOverlay`는 반투명 배경 위에 텍스트+200×200 이미지가 맨몸으로 떠 있는 구조였다 — 그 위에 라운드 코너·테두리·그림자가 있는 카드(퀘스트 팝업과 같은 `radial-gradient(130% 120% at 50% -10%,#3A2610 0%,#150C06 70%)` 배경)를 씌우고, `phase` state로 제어되는 기존 흔들림→퇴장→등장 `AnimatePresence`/`motion.img` 블록은 타이밍·트랜지션 값 전부 한 글자도 바꾸지 않고 그 카드 안으로 그대로 옮겼다. 추가 장식은 전부 "도달한 티어 고유 색"으로 물들였다 — 이미 배지·진행바에 쓰이던 `TIER_COLORS`(예: 다이아몬드는 `{lo:"#0090C8",hi:"#4FE8FF"}`, 그랜드마스터는 3색 `stops`)를 그대로 재사용해 새 헬퍼 `tierGlowHex`(햇살·발광 펄스용 대표색 하나)·`tierGradientCss`(제목 텍스트용 그러데이션, 그랜드마스터는 3색 다 살림)를 만들었다. 퀘스트 팝업의 반짝임·컨페티(`QUEST_CLEAR_SPARKLES`/`QUEST_CLEAR_CONFETTI`, `xpStarPop`/`questConfettiFall` 키프레임)는 그대로 재사용해 "이 앱의 축하 연출"이라는 공통 언어를 유지하면서, 햇살(`questRaySpin` 재사용, 색만 다르게)과 새로 추가한 `tierGlowPulse`(숨쉬듯 부풀었다 가라앉는 원형 발광, boxShadow가 아니라 radial-gradient 배경으로 만들어 색을 자유롭게 바꿀 수 있게 함)만 티어 색으로 새로 만들었다. 제목 "티어 승급!"은 퀘스트 팝업과 같은 `WebkitBackgroundClip:"text"` 각인 금속 글자 기법을 그대로 쓰되 그러데이션만 `tierGradientCss(toTierKey)`로 바꿨고, 기물 아래에 도달한 티어 이름(`TIERS`에서 조회, 예: "다이아몬드 티어 도달!")을 새로 추가했다. 예전엔 승급 보상(+N 코인)을 이 오버레이(zIndex 200)와 별개인 "coins" 토스트(zIndex 65)로 띄웠는데, 같은 순간 뜨는 오버레이에 화면 전체가 가려져 사실상 보이지 않는 상태였다(발견은 이번에 카드를 뜯어보다가 우연히 확인함) — 토스트 호출을 걷어내고 `tierUpAnim` state에 `reward`를 함께 실어 카드 안에 퀘스트 팝업과 같은 카운트업 배지(`AnimatedCountUp`+`questBadgePop`)로 직접 보여주도록 고쳐, 승급마다 눈에 보이지 않던 보상이 이번에 함께 보이게 됐다. 배경(바깥 div, `onClick={onDone}`)에는 `stopPropagation`을 걸지 않아 "화면 아무 곳이나 누르면 바로 닫힌다"는 기존 동작을 카드 위를 눌러도 그대로 유지했다. Playwright로 임시 디버그 훅(`setTotalXp`)을 통해 다이아몬드 티어(누적 50,000 XP)로 바로 점프시켜 승급을 재현했다 — 흔들림 단계에서 이전 티어(아이언 폰)가 흔들리는 동안에도 사이언 톤 햇살·컨페티·발광이 함께 보이는 것, 등장 단계에서 새 기물(다이아몬드 퀸)이 자리 잡은 뒤 "다이아몬드 티어 도달!"과 "+200 OC 나이트 코인" 배지가 튕기며 나타나는 것을 프레임별 스크린샷으로 확인했다.

**UI — 게임 보상 팝업 제목에 사용자 지정 한글 디스플레이 폰트(Gasoek One) 적용**
사용자가 티어 승급 팝업 디자인이 "너무 별로"라며, 우선 자신이 골라 준 구글 폰트(Gasoek One)부터 적용해 달라고 요청했다. `index.html`의 기존 Google Fonts `<link>`(Black Ops One·Noto Sans KR·Noto Sans·Playfair Display를 한 요청에 묶어 불러오던 것)에 `family=Gasoek+One`만 추가했다 — 이 폰트는 획이 굵고 각진 단일 굵기(레귤러 하나만 존재) 디스플레이 서체라 문단 본문에 쓰면 가독성이 떨어지므로, 새 상수 `GAME_FONT = "'Gasoek One', 'Noto Sans KR', sans-serif"`를 만들어 "게임 보상 화면" 성격의 큰 제목 두 곳(`DailyQuestClearedModal`의 "오늘의 퀘스트 클리어!", `TierUpOverlay`의 "티어 승급!")에만 적용했다. 두 제목 모두 기존에 `fontWeight:900`으로 브라우저가 없는 굵기를 인조로 흉내 내던(faux-bold) 상태였는데, Gasoek One은 그 자체로 이미 두꺼운 굵기 하나뿐인 폰트라 `fontWeight:400`(레귤러)으로 낮춰 폰트 자체의 원래 형태가 그대로 나오게 했다. Playwright로 실제 웹폰트가 로드된 뒤 두 팝업을 다시 캡처해, 각지고 개성 있는 한글 글자체가 금빛/티어색 그러데이션 위에 그대로 얹히는 것을 확인했다. 본문·목록·설명 텍스트는 그대로 Noto Sans KR을 쓴다 — 나머지 디자인 전반은 사용자가 이 폰트를 먼저 확인한 뒤 다음 방향을 정하기로 했다.

**UI — 티어 승급 팝업 컨페티 제거 + 사이트 전체 기본 폰트를 IBM Plex Sans KR로 교체**
사용자 피드백 두 가지. (1) 티어 승급 팝업(`TierUpOverlay`)의 색색 컨페티가 그 카드의 짙은 단색 배경(`radial-gradient(...#3A2610 0%,#150C06 70%)`)과 안 어울린다는 지적 — 일일 퀘스트 클리어 팝업의 컨페티는 그대로 두고, `TierUpOverlay`에서만 `QUEST_CLEAR_CONFETTI.map(...)` 렌더 블록을 제거했다(햇살·발광 펄스·반짝임은 유지). (2) 이번엔 특정 팝업이 아니라 "웹사이트 전반과 지금까지 만든 창들"에 새 구글 폰트(IBM Plex Sans KR)를 적용해 달라는 요청 — `index.css`의 `html, body` 셀렉터 font-family 선언 딱 한 곳만 `"IBM Plex Sans KR", "Noto Sans KR", "Noto Sans", ...`로 바꿨다. 이 한 줄이 전역 기본값이라 별도로 폰트를 지정하지 않은 모든 화면(사이트 전체 본문은 물론, 이번 세션에 만든 팝업들의 본문·목록·설명 텍스트도 전부 포함)에 자동으로 적용된다 — 팝업마다 따로 손댈 필요가 없었다. `index.html`의 기존 Google Fonts 결합 요청에 `family=IBM+Plex+Sans+KR:wght@400;500;600;700`을 추가했다(Google Fonts 카탈로그의 IBM Plex Sans KR은 정적 굵기만 있고 400~700까지만 실제 글리프가 있어, 사이트 곳곳의 `fontWeight:800/900` 굵은 텍스트는 브라우저가 700 위에 인조 굵기를 살짝 얹어 표시된다 — 완벽친 않지만 아예 폴백 폰트로 빠지는 것보다는 낫다). 이전 세션에 별도로 지정해 둔 브랜드 워드마크(Black Ops One)·게임 보상 팝업 제목(Gasoek One)·기보(Playfair Display)·티어 승급 제목(도달한 티어색 그러데이션)은 각자 컴포넌트에서 명시적으로 font-family를 지정해 두었으므로 이번 전역 교체의 영향을 받지 않고 그대로 유지된다. Playwright로 공지 모달·티어 승급 팝업을 다시 캡처해 (a) 컨페티가 더 이상 뜨지 않는 것, (b) 사이트 전역 본문 텍스트가 IBM Plex Sans KR로 바뀐 것을 확인했다.

**UI — 티어 승급 팝업에 순차적으로 터지는 "폭죽" 추가(그랜드마스터는 다색으로)**
사용자 요청 — 방금 뺀 컨페티 대신, 기물 교체 애니메이션이 다 끝난 뒤에 카드 곳곳에서 하나씩 순서대로 터지는 진짜 폭죽 연출을 만들고, 크기도 다양하게, 그랜드마스터 승급은 색을 더 여러 개 뽑아 화려하게 해 달라는 것. 위에서 떨어지는 컨페티(`questConfettiFall`)와 달리, 진짜 "펑" 터지는 느낌을 내려면 한 점에서 사방으로 흩어지는 파티클이 필요해 새 키프레임 `tierFirework`를 만들었다 — CSS 커스텀 프로퍼티(`--dx`/`--dy`)를 파티클마다 다르게 줘서 하나의 키프레임 정의를 공유하면서도 각자 다른 방향·거리로 튀어나가게 했다(각도·거리·굵기는 `TIER_FIREWORK_PARTICLES` 8개 배열에 미리 정해 둠 — "크기도 조금씩 다양하게"는 이 배열의 `size`를 3~6px로 흩어 반영). 폭죽이 터지는 6개 지점(`TIER_FIREWORK_SPOTS`, 카드 좌상단·우상단·좌측·우측·좌하단·우하단)은 각자 다른 `animationDelay`(0/320/640/960/1280/1600ms)를 가져 순서대로 터진다. "기물 교체가 다 끝난 뒤에"라는 요청을 지키기 위해, 흔들림→퇴장→등장 phase 전환과는 별개로 새 `fireworksOn` state를 두고 등장 슬라이드(phase가 "enter"로 바뀌는 1050ms + 슬라이드 자체 0.5초)가 완전히 끝난 1650ms에야 켜지도록 타이머를 하나 더 추가했다 — 그 전까지는 폭죽 관련 엘리먼트 자체를 렌더링하지 않는다. 순차 폭죽이 다 터지려면 시간이 더 필요해져(마지막 지점이 1650+1600=3250ms에 시작, 파티클 애니메이션 0.7초 재생), 팝업이 자동으로 닫히는 시점도 2700ms→4000ms로 늦췄다(스킵하고 싶으면 여전히 화면 아무 곳이나 눌러 바로 닫을 수 있다). 색상은 새 헬퍼 `fireworkColorsFor(tierKey)`가 담당 — 일반 티어는 그 티어 고유색(`tierGlowHex`) 위주에 흰 스파크를 섞어 대비만 주고, 그랜드마스터는 요청한 "스포이드로 여러 개 따서"에 맞춰 이미 있던 3색 홀로그램 그러데이션(`TIER_COLORS.grandmaster.stops`)에 금색·흰색 스파크까지 더해 5가지 색을 파티클마다 순환시킨다. Playwright로 그랜드마스터 승급(누적 200,000 XP로 디버그 훅 점프)을 재현해, 등장 슬라이드가 끝난 1.4초 시점까지는 폭죽이 전혀 안 뜨는 것, 1.75초 시점에 첫 폭죽이 보라·금색·민트·흰색이 섞인 채로 터지는 것, 그 이후 다른 지점에서도 순서대로 다색 폭죽이 이어지는 것을 프레임별로 확인했다.

**UI — 티어 승급 카드를 밝은(MILKU 톤) 배경으로 바꾸고, 뒤에서 돌아가던 부채꼴 햇살 제거**
사용자 요청 — 승급 카드를 밝은 색(MILKU 마스코트 톤)으로 바꾸고, 뒤에서 도는 "풍차 같은" 요소를 빼 달라는 것. "풍차"는 `repeating-conic-gradient` + `questRaySpin`으로 만들어 뒀던 햇살 광선(회전하는 부채꼴 패턴)을 가리키는 것으로 판단해 그 div를 통째로 지웠다. 카드 배경은 기존 짙은 갈색 라디얼 그러데이션(`#3A2610→#150C06`)에서 다른 밝은 팝업들과 같은 톤(`#FFFDF7→#F1E6D0`, `T.paper`/`T.ivoryHi` 계열)으로 바꾸고, 테두리·그림자도 `DailyPuzzleNoticeModal` 등 다른 밝은 카드와 같은 값(`#DCCBA8` 테두리, `0 20px 50px -10px rgba(0,0,0,.6)` 그림자)으로 맞췄다. 어두운 배경을 전제로 골랐던 글자·장식 색(부제 `T.ivoryHi`, 힌트 텍스트 `T.ivory`)도 밝은 카드에서 읽히도록 `T.ink`/`T.inkSoft`로 바꾸고, `tierGlowHex`가 옅은 `.hi`색(특히 실버는 거의 흰색) 대신 더 짙고 채도 높은 `.lo`색을 돌려주도록 바꿔 반짝임·발광·폭죽 색 전부가 밝은 카드에서도 묻히지 않게 했다(흰 폭죽 스파크도 같은 이유로 짙은 잉크색으로 교체). 그런데 이렇게 바꾸고 실제로 띄워 보니 새 버그가 났다 — 티어 기물 이미지 자체가 원래 어두운 배경 전제로 그려진 흰색/밝은 선화라(그랜드마스터만 홀로그램 다색이라 그나마 보임), 카드를 밝게 바꾸자 기물이 카드 배경에 묻혀 사실상 안 보이게 됐다. 카드 전체를 밝게 유지하되, 일일 퀘스트 팝업이 마스코트를 위한 어두운 배너를 따로 뒀던 것과 같은 발상으로, 기물 전용의 짙은 원형 "무대"(사이트 전역 배경과 같은 톤의 라디얼 그러데이션)를 기물 이미지 뒤에만 깔아 해결했다 — 흔들림→퇴장→등장 애니메이션과 그 안의 발광 펄스는 이 무대 안으로 그대로 옮겼을 뿐 로직은 건드리지 않았다. Playwright로 다이아몬드·그랜드마스터 두 승급을 다시 캡처해, 밝은 카드에서도 기물이 무대 위에 또렷하게 보이고 풍차 광선은 더 이상 없는 것을 확인했다.

**기능 — 일일 퀘스트·티어 승급 팝업이 자동/배경 클릭으로 닫히지 않고 버튼을 눌러야만 닫히도록 변경**
사용자 요청 — 두 팝업 다 자동으로 닫히지 말고 X 버튼을 눌러야 닫히게 해 달라는 것. `DailyQuestClearedModal`은 원래 자동 닫힘 타이머는 없었지만, 배경(backdrop) 전체에 `onClick={onClose}`가 걸려 있어 카드 바깥 아무 곳이나 눌러도(예: 목록을 읽다가 실수로 여백을 탭해도) 닫혔다 — 이 onClick을 없앴다(안쪽 카드의 `stopPropagation`도 더는 필요 없어져 같이 정리). `TierUpOverlay`는 사정이 더 컸다 — 4초 뒤 자동으로 닫히는 타이머(`onDone` setTimeout)가 있었고, 애초에 전용 닫기 버튼 자체가 없어서 배경 전체가 유일한 닫기 수단(`onClick={onDone}`)이었다. 그 타이머와 배경 onClick을 모두 없애고, 카드 우측 상단에 다른 밝은 팝업들과 같은 스타일(`#0002` 원형 버튼)의 X 버튼을 새로 추가했다 — 이제 카드가 자기 시간에 맞춰 자동으로 사라지지 않으니, 순차 폭죽이 다 터지는 데 걸리는 시간(마지막 폭죽이 1650+1600=3250ms에 시작)을 신경 써서 타이머를 조정할 필요도 없어졌다. 사라진 "화면을 누르면 바로 닫혀요" 안내 문구 자리에는 일일 퀘스트 팝업과 같은 스타일의 "확인" 버튼을 새로 넣어, X 버튼과 함께 명확한 닫기 수단 두 개를 제공한다. Playwright로 검증했다 — 티어 승급 팝업은 배경 모서리를 클릭해도, 예전 자동 닫힘 시점(4초)을 훌쩍 넘긴 5초가 지나도 계속 떠 있다가 X 버튼을 눌러야만 닫히는 것을, 일일 퀘스트 클리어 팝업도 배경 클릭엔 반응하지 않고 "확인" 버튼을 눌러야만 닫히는 것을 확인했다.

**기능 — 칭호 획득도 일일 퀘스트·티어 승급과 같은 "보상 화면" 팝업으로 재구성**
사용자 요청 — 칭호 획득도 팝업창으로 만들어 달라는 것. 기존 칭호 획득 알림은 상단에 잠깐 떴다 6초 뒤 자동으로 사라지는 작은 토스트(`setToast({type:"title", id})`, 어두운 배너 안에 마스코트 아이콘 + 칭호 배지 하나만)였다 — 같은 "달성"의 성격을 가진 다른 두 팝업(`DailyQuestClearedModal`, `TierUpOverlay`)에 비해 확연히 작고 눈에 덜 띄었다. 새 컴포넌트 `TitleEarnedModal`을 만들어 `DailyQuestClearedModal`과 완전히 같은 시각 언어(짙은 라디얼 그러데이션 배너 위 금빛 원판 마스코트, `questRaySpin` 햇살, `QUEST_CLEAR_CONFETTI` 컨페티, `QUEST_CLEAR_SPARKLES` 반짝임, `questGlowPulse` 발광 펄스, `GAME_FONT`로 각인한 금속 제목)를 그대로 재사용하고, 본문에는 기존 `TitleBadge`(오프닝명·등급·해금 이미지 또는 SVG 배지 — 이미 있는 컴포넌트라 클릭하면 그 자리에서 바로 장착도 된다)를 카드 폭에 맞게 크게 얹었다. 다른 두 팝업과 마찬가지로 자동으로 닫히지 않고 배경을 눌러도 닫히지 않으며, 우측 상단 X 버튼이나 "확인" 버튼을 눌러야만 닫힌다 — 앱 상태로는 새 `titleEarnedPopup`(획득한 칭호 id 또는 null) 하나만 두고, 기존에 토스트를 껐던 6초 타이머와 "장착하면 토스트를 끈다"는 별도 로직은 필요 없어져 함께 정리했다(알림 기록에 남기는 `notifyCreate` 호출은 그대로 유지). Playwright로 임시 디버그 훅을 통해 이미지가 있는 등급(그랜드마스터, 홀로그램 배지)과 이미지가 없어 SVG 배지로 그려지는 등급(비기너, 초록 다이아몬드 배지) 둘 다 카드 안에 잘 들어차는 것을 확인했고, X 버튼·확인 버튼을 눌러야만 닫히고 배경을 눌러거나 5초 넘게 기다려도 닫히지 않는 것, 배지를 누르면 그 자리에서 바로 장착되며(장착 조건 안내 오버레이도 그대로 동작) 안내 문구가 "지금 장착 중인 칭호예요"로 바뀌는 것을 확인했다.

**성능 — 학습 탭 엔진 분석에 세 번째 심화 단계("third movetime") 추가 + depth 인위적 상한 제거**
사용자 요청 — "third movetime을 추가하고, 상한 없이 depth가 무한정 증가하도록 해줘"라는 것. 기존엔 첫 700ms 확정 → 5초 심화("extra movetime")까지 딱 두 단계였고, 두 단계 모두 `evaluateMulti`에 `depth=20`을 명시적으로 넘기고 있었다 — `evaluateMulti`가 UCI에 실제로 보내는 명령은 `"go depth " + depth`(movetime은 `pump()`의 소프트 스톱 타이머로만 별도 처리, 위 v0.2.8 근본 원인 수정 참고)라서, 이 20이라는 값 자체가 진짜 하드 캡이었다 — 5초짜리 심화 탐색도 시간이 남아도 depth 20에 닿는 순간 그대로 멈췄다(대부분의 평범한 포지션은 실제로 5초가 다 되기 전에 depth 20에 먼저 닿아 있었다). 두 가지를 함께 고쳤다. (1) 새 모듈 상수 `LEARN_MAX_DEPTH = 99`를 만들어 세 `evaluateMulti` 호출(700ms·5000ms·아래 새로 추가한 20000ms) 모두 `depth` 인자를 여기로 통일했다 — UCI 프로토콜상 `go depth`엔 숫자가 필요하지만, 스톡피시 WASM이 movetime 몇십 초 안에 실제로 닿을 수 있는 depth(대체로 20~30대)보다 훨씬 큰 값이라 사실상 도달 불가능한 상한이 되고, 결과적으로 각 단계를 실제로 멈추는 건 depth가 아니라 movetime(시간)만 남게 된다. (2) 5초 심화 탐색이 끝난 뒤, 사용자가 그 포지션에 그대로 머물러 있으면 같은 slot("learn-lines")으로 세 번째 탐색을 20초 더 이어서 돈다 — 두 번째 단계와 마찬가지로 스톡피시 치환 테이블(Hash)이 그대로 남아 있어 처음부터 다시 하는 게 아니라 5초 지점에서 실질적으로 이어가며, `posCacheRef`에 `thirdKey`를 따로 둬 한 포지션당 정확히 한 번만 시작되게 가드했다. 이 세 번째 탐색도 다른 두 단계와 완전히 같은 supersede 규칙을 따른다 — 사용자가 다른 수·포지션으로 넘어가면(같은 slot에 새 요청이 들어오면) 자동으로 중단된다. Playwright로 실제 시작 포지션에 진입해 "?" 도움말 말풍선의 "N수 후까지 탐색 중.." 표시를 2초 간격으로 31초간 폴링해 확인했다 — 예전에는 5초 안팎에 depth 20에 도달한 뒤 그 값에 완전히 고정됐어야 하는데, 이번엔 4.8초 시점에 15수, 14초 시점에 21수(처음으로 옛 상한을 넘김), 31초 시점에는 26수까지 막힘없이 계속 늘어나는 것을 확인했다(자연스럽게 깊이가 깊어질수록 증가 속도는 느려지지만, 더 이상 어떤 값에서도 멈추지 않는다).

**성능 — 게임 리뷰의 자유 탐색 엔진 라인·평가치 막대·수 체계 아이콘에도 third movetime·depth 무상한 적용(실제 대국 채점은 기존 기준 유지)**
사용자 요청 — "리뷰 기능에서는 초기 정확도와 평가치를 판정할 때 기존 기준을 사용하고, 자유 탐색에서 표시하는 수 체계 아이콘, 엔진 라인 및 평가치 막대는 똑같이 third movetime까지 적용해서 변동 사항이 있으면 실시간으로 반영하도록" 해 달라는 것. 게임 리뷰 페이지에는 성격이 다른 두 종류의 엔진 평가가 있다 — (1) `analyzeGame`(대국 전체를 한 번 훑어 정확도%·각 수의 등급을 확정하는 "채점", `REVIEW_DEPTH=20`·`REVIEW_MOVETIME_MS=1000` 고정)과, (2) 지금 보고 있는 포지션(실제 기보 수든 자유 탐색 중이든)의 엔진 상위 3줄·평가치 막대를 실시간으로 보여주는 별도 effect, 그리고 자유 탐색으로 기보에 없는 수를 뒀을 때 그 수 하나를 채점하는 또 다른 effect. 사용자의 요청대로 (1)은 전혀 건드리지 않고 그대로 뒀다 — 정확도%는 안정적으로 재현 가능한 값이어야 하므로, 백그라운드 심화 탐색으로 계속 바뀌면 오히려 이상하다. (2)와 (3)에는 학습 탭에 방금 추가한 것과 똑같은 3단계 점진 심화(첫 확정 → +5초 → +20초, depth 상한을 새 공용 상수 `MAX_SEARCH_DEPTH=99`로 통일해 사실상 무제한)를 그대로 적용했다 — 학습 탭 전용이던 상수 이름을 `LEARN_MAX_DEPTH`에서 `MAX_SEARCH_DEPTH`로 바꿔 두 기능이 공유하게 했다. 엔진 라인/평가치 막대 effect는 첫 단계(기존 `REVIEW_MOVETIME_MS`)가 끝나면 `linesPending`을 바로 끄고(화면은 곧장 반응), 이어서 5초·20초 두 단계를 배경에서 더 돌며 결과가 나올 때마다 `engineLines`를 갱신한다 — 평가치 막대는 이미 지난 세션에 `engineLines[0].ev`를 실시간으로 따라가게 바뀌어 있었으므로 별도 수정 없이 자동으로 더 깊어진 값을 반영한다. 자유 탐색 채점 effect는 기존 30줄 가까운 등급 판정 로직을 `grade(movetime)` 내부 함수로 추출해, `REVIEW_MOVETIME_MS` → `5000` → `20000` 세 번 그대로 재사용하며 호출한다 — 더 깊이 본 결과로 등급이 달라지면(예: "우수"였던 게 실은 "최선"이었던 경우) `setExploreMove`가 다시 호출돼 아이콘이 실시간으로 갱신된다. Playwright로 임시 디버그 훅(`__debugOpenReview`로 리뷰를 직접 열고, `__debugPushExplore`로 자유 탐색 수를 주입, `onLines` 콜백에 임시로 raw depth를 훅에 넘겨 폴링)을 통해 검증했다 — (a) 실제 기보 마지막 수를 보고 있는 상태(자유 탐색 아님)에서도 depth가 4초 18 → 8초 22(옛 상한 돌파) → 32초 25까지 멈추지 않고 늘어나는 것을 확인했고, (b) 자유 탐색 수를 하나 두자 등급 판정이 movetime `[1000, 5000, 20000]` 순서로 정확히 세 번 실행되는 것을 확인했다.

**기능 — 게임 리뷰에 "리뷰 티켓" 소비 재화를 도입(한 번 연 대국은 이후 영구 무료)**
사용자 요청 — 대국 하나를 리뷰로 여는 데 리뷰 티켓을 쓰게 하고, 한 번 쓴 대국은 이후 티켓 없이 영구히 다시 볼 수 있게 해 달라는 것. 새 영구 저장 상태 `reviewTickets`(보유 개수)와 `reviewUnlocked`(이미 티켓을 쓴 대국의 키 집합)를 추가했다. 대국을 구분할 키가 필요해 새 헬퍼 `reviewGameKey(game)`을 만들었다 — 현재 리뷰로 열리는 모든 대국은 chess.com에서 온 것뿐이고 항상 양쪽 닉네임(`white.username`/`black.username`)과 종료 시각(`endTime`)을 함께 저장해 두므로 이 셋을 합친 문자열을 키로 쓴다(같은 두 사람이 같은 초 단위 시각에 대국을 두 번 끝내는 경우는 사실상 없어 충분히 안정적이다). 이 정보가 없는 대국(현재는 없지만 나중에 다른 소스가 생길 경우를 대비)은 키를 `null`로 돌려주고, 그런 경우는 구분할 방법이 없으므로 항상 무료로 취급한다. 실제 게이팅은 모든 "게임 리뷰 열기" 경로가 공유하는 단일 진입점 `openReview`에 넣었다 — 이미 `reviewUnlocked`에 있는 대국이면 그냥 열고, 아니면 티켓이 있을 때만 1개를 소비하며 그 키를 `reviewUnlocked`에 추가하고, 티켓이 없으면 리뷰를 열지 않고 새 `NoReviewTicketsModal`(다른 세 축하 팝업과 같은 밝은 카드 스타일, X·확인으로만 닫힘 — 보유 티켓 수와 "티켓 얻는 방법" 안내를 보여줌)을 띄운다. 개발자 모드(`isDev && devOn`)에서는 티켓 시스템 자체를 완전히 건너뛴다(devUnlockAll이 다른 잠금들을 건너뛰는 것과 같은 결) — 소비도, `reviewUnlocked` 기록도 하지 않아 매번 자유롭게 테스트할 수 있다. 두 영구 저장 경로(로컬 `store`, 서버 `progressSave`/계정 로드)와 로그아웃 초기화에도 새 필드 두 개를 빠짐없이 추가했다. `CoinIcon`과 완전히 같은 패턴(전용 이미지가 없거나 로드 실패 시 이모지로 폴백)의 `TicketIcon`을 만들어 `/review-ticket.png`를 가리키게 해 뒀다 — 나중에 그 파일만 채워 넣으면 코드 변경 없이 실제 아이콘으로 바뀐다. Playwright로 임시 디버그 훅을 통해 검증했다: 티켓 0개로 새 대국을 열면 차단 모달이 뜨고 리뷰는 안 열리는 것, 티켓 1개를 준 뒤 같은 대국을 열면 티켓이 0개로 줄고 리뷰가 열리는 것, 그 상태(티켓 0개)에서 같은 대국을 다시 열면 이번엔 막힘없이 무료로 열리는 것(reviewUnlocked에 남아 있으므로), 개발자 모드에서는 티켓 0개에 한 번도 안 연 새 대국을 열어도 그냥 열리고 티켓·기록 둘 다 변하지 않는 것을 각각 확인했다.

**기능 — 리뷰 티켓 획득 경로(일일 퀘스트·티어 승급) 연결 + 재화 표시**
사용자 요청 — 일일 퀘스트 단일 클리어마다 티켓 1개, 5개 모두 클리어 시 2개 추가(하루 총 7개), 티어 승급마다 10개를 지급하고, 리뷰 티켓을 OC 나이트 코인과 함께 재화로 표시해 달라는 것. 개별 퀘스트 클리어를 담당하는 `claimQuestCoins`(퍼즐 목표·일일 퍼즐·chess.com 활동 퀘스트 3개가 공유하는 함수, 하루 5번 호출됨)에 `setReviewTickets((t) => t + 1)`을 더했고, 그 보상을 보여주는 개별 퀘스트 토스트에도 코인 배지 옆에 "+1🎫" 배지를 나란히 추가했다. 5개 전부 클리어 보너스 effect(기존 +50 코인)에 `+2` 티켓을 더하고, 그 결과를 보여주는 `DailyQuestClearedModal`의 보상 배지 줄에도 티켓 배지를 하나 더 얹었다(XP·코인 배지와 같은 `questBadgePop` 튕김 애니메이션, 지연 시간만 더 늦춰 셋이 순서대로 나타나게 함). 티어 승급 effect(기존 `tierUpReward` 코인)에는 티어 무관 고정값 `+10` 티켓을 추가하고, `TierUpOverlay`에 새 `ticketReward` prop을 더해 코인 배지 옆에 티켓 배지를 나란히 보여준다. 재화 표시는 상점 탭 상단의 기존 코인 칩(유일한 "보유 재화" 표시 지점이었다) 옆에 같은 스타일의 티켓 칩을 추가하는 것으로 마무리했다. Playwright로 개발자 패널을 통해 XP를 아이언→브론즈, 브론즈→다이아몬드로 두 차례 실제 승급시켜 각각 승급 팝업에 코인·티켓 배지가 정확한 수치로 함께 뜨는 것을 확인했고, 상점 탭에서 두 재화 칩이 나란히 올바른 값(1,234 / 7)을 보여주는 것도 확인했다.

**기능 — 개발자 전용 재화·티어·XP 통합 설정 패널**
사용자 요청 — 개발자 권한으로 모든 재화(코인·티켓)와 티어·XP 값을 한 블록에서 숫자로 직접 설정하되, XP와 티어는 서로 연동되게 해 달라는 것. 기존 `DevXpPanel`(XP 증감량만 입력 가능, 티어 시작점으로 점프하는 버튼들)을 `DevResourcePanel`로 완전히 대체했다. OC 코인·리뷰 티켓은 각각 숫자 입력창 + "적용" 버튼으로 절대값을 그대로 `setOcCoins`/`setReviewTickets`에 넣는다. XP도 절대값 입력창을 새로 추가했다(기존엔 증감량만 가능했다). 티어·구간은 두 상호 연동되는 방향으로 구현했다 — ① XP를 입력해 적용하면 `totalXp`가 바뀌고, `tierFromXp(totalXp)`로 계산되는 티어·구간 표시와 아래 티어/구간 선택기가 `useEffect`로 그 값에 맞춰 자동 갱신된다. ② 반대로 티어(7개)와 구간(그랜드마스터가 아니면 I~V, 그랜드마스터면 ★ 프레스티지 숫자)을 고르고 "티어 적용"을 누르면, 새 헬퍼 `xpForTierDivision(tierKey, division, gmStars)`(`tierFromXp`의 정확한 역함수 — 티어 이전 누적 요구치를 더하고 구간 인덱스만큼 그 티어 내 XP를 더함)로 그 지점의 누적 XP를 역산해 `setTotalXp`에 넣는다. 두 방향 다 결과적으로 같은 `totalXp` 하나로 수렴하므로 XP↔티어를 오가며 여러 번 조작해도 항상 일치한다. `SettingsTab`에 `ocCoins`/`setOcCoins`/`reviewTickets`/`setReviewTickets` prop을 새로 뚫어 최상위 App 컴포넌트의 상태를 그대로 전달한다. Playwright로 검증했다 — 티어=다이아몬드·구간=I를 고르고 "티어 적용"을 누르자 누적 XP가 정확히 90,000(아이언~골드 누적 요구치 50,000 + 다이아몬드 자체 요구치 50,000의 80%)으로 바뀌고 요약 줄도 "다이아몬드 I · 누적 90,000 XP"로 즉시 갱신되는 것을 확인했다(이 큰 XP 점프가 실제 티어 승급 이벤트를 연쇄적으로 발동시켜 코인·티켓 보상까지 정확히 지급되는 부수 효과도 함께 확인했다).

**UI — 이미지 4장 반영: 리뷰 티켓 아이콘 교체 + 리뷰 로딩 3분할 애니메이션**
사용자가 GitHub 웹 업로드로 `main` 브랜치에 직접 올려 준 이미지 4장(`review_ticket.png`, `ilust_5/6/7.png`)이 채팅 첨부로는 이번에도 전달되지 않아, `git fetch origin`으로 전체 브랜치 목록을 다시 훑어 `main`에만 있는 새 커밋("Add files via upload")을 찾아내고 그 커밋에서 해당 4개 파일만 `git checkout <커밋> -- <경로>`로 현재 작업 브랜치에 가져왔다. (1) 리뷰 티켓 아이콘 — 원본은 검정 배경 위에 놓인 별 그림 초록 티켓(1536×1024, 알파 채널 없음)이라, Pillow로 배경의 거의 검정에 가까운 픽셀만 부드러운 경계로 투명 처리하고(순수 검정 근처만, 티켓 자체의 초록·크림·흰색은 전혀 건드리지 않음) 실제 그림 영역만 여백을 조금 남기고 잘라낸 뒤, 아이콘으로 쓰기에 맞는 크기(400×203)로 줄여 `TicketIcon`이 이미 가리키고 있던 `public/review-ticket.png`(지난 커밋에서 하이픈 이름으로 미리 만들어 둔 경로)에 그대로 덮어썼다 — 코드는 전혀 안 건드려도 됐다. (2) 나머지 3장(MILKU·KOKOA가 체스보드를 사이에 두고 있는 삽화, 2560×1440)은 사이트가 이미 쓰고 있던 `ilust-N-web.webp` 관례(원본 PNG는 그대로 저장소에 보관하고, 실제 화면엔 1300px 폭으로 줄인 WebP만 쓰는 방식 — 기존 `ilust-1~4`와 동일)를 그대로 따라 `ilust-5/6/7-web.webp`(각 100~120KB)로 새로 만들었다 — 원본 그대로(장당 6~7MB, 3장 합쳐 20MB) 썼다면 리뷰를 열 때마다 그만큼을 내려받아야 했다. 사용자 요청대로 "7이 가장 위, 5가 가장 아래"에 오도록 새 컴포넌트 `ReviewLoadingSplit`을 만들어 게임 리뷰의 로딩 화면(`analyzeGame`이 아직 안 끝나 `!result`인 상태 — 예전엔 "생각 중" 마스코트 아이콘 하나만 있었다) 맨 위에 얹었다. 세로로 쌓은 3개 패널(각 92px 높이, 12px 라운드, `RV.border`와 같은 톤의 테두리)에 순서대로 ilust-7·ilust-6·ilust-5를 `object-fit:cover`로 채우되, 그림마다 원래 인물·보드 구도가 얇은 띠 안에서도 잘리지 않도록 `object-position`을 따로 맞췄다(7은 인물+티켓이 화면 중하단에 있어 38%, 6은 인물 얼굴이 상단에 몰려 있어 22%, 5는 눈높이 구도라 46%). framer-motion으로 위 패널부터 0.14초 간격으로 튕기듯 등장하고, 자리를 잡은 뒤에는 패널마다 다른 지연으로 `gm-board-shine`(이 사이트의 "특별한" 화면들이 이미 공유하는 금속 광택 스윕 클래스)을 얹어, 분석이 몇 초 넘게 걸려도 화면이 멈춘 것처럼 보이지 않게 했다. Playwright로 리뷰를 실제로 열어 등장 애니메이션 도중(0.25초 시점)과 정착 후(진행률 33% 시점) 두 프레임을 캡처해 세 패널이 요청한 순서(7→6→5)로 정확히 쌓여 있고 각 그림의 핵심 요소(인물 얼굴·체스보드·티켓)가 잘리지 않고 보이는 것을 확인했고, 상점 탭에서 티켓 칩의 `<img>`가 더 이상 이모지 폴백이 아니라 실제 `review-ticket.png`(naturalWidth 400)를 불러오는 것도 확인했다.

**UI — 리뷰 티켓 아이콘 크기 확대**
사용자가 리뷰 티켓 이미지가 잘 안 보인다고 지적 — 상점 탭 재화 칩(18→22px), 개발자 패널의 요약 줄(12→15px)·입력창 아이콘(16→20px), 상점 부족 안내 모달의 큰 원형 아이콘(32→40px), 일일 퀘스트 전체클리어 배지(14→18px)·토스트(16→20px), 티어 승급 배지(15→19px) 등 `TicketIcon`이 쓰이는 7곳 전부를 조금씩 키웠다.

**기능 — 학습 탭 "수 설명"을 로그인한 모든 유저가 남길 수 있는 커뮤니티 게시판으로 전환 + 서버 측 검열**
사용자 요청 — 기존엔 개발자/공동개발자만 남길 수 있던 "수 설명"(FocusPanel의 "해설" 카드, `CONTENT.explains[mkKey]`에 단일 텍스트로 저장)을 로그인한 모든 유저가 쓸 수 있게 열되, 카드 하나에 다 들어갈 만큼 글자 수를 제한하고, 여러 개면 스크롤·자동 넘김 캐러셀로 보여주고, 개발자 권한으로는 편집·삭제가 가능하며, 확실한 검열 시스템을 갖추라는 것. 기존 저장 위치를 그대로 열 수는 없었다 — `CONTENT.explains`는 `app_content` 테이블에 실려 있고 그 테이블은 RLS로 `is_content_editor`(개발자/공동개발자)만 쓸 수 있게 서버에서 잠겨 있어(UI만 바꿔도 일반 유저의 쓰기는 서버가 그대로 거부한다), 게다가 값이 수 하나당 문자열 하나뿐이라 "여러 개"를 표현할 수도 없었다. 그래서 이 기능만 완전히 분리된 새 테이블 `move_notes`(수마다 여러 행, `unique(move_key, uid)`로 1인당 같은 수엔 1개만)로 새로 만들었다 — `supabase-setup.sql` 15번 섹션. RLS는 읽기는 누구나, 등록은 `auth.uid()=uid`인 로그인 유저 본인만, 수정·삭제는 `is_content_editor`(개발자/공동개발자)만 가능하도록 짰다(작성자 본인도 스스로 고치거나 지울 수 없다 — "게시판"이라 최종 편집권은 항상 운영진에게 남기는 설계). 글자 수 제한은 100자로 정해 클라이언트 `textarea maxLength`와 서버 `check (char_length(body) between 1 and 100)` 두 군데에서 강제한다. 검열은 2단으로 짰다 — ① 클라이언트: 숫자·영문·한글만 남기고 나머지(공백·구두점·이모지)를 지운 뒤 흔한 욕설·혐오 표현 목록과 부분 문자열로 대조하는 `containsBannedWord`(등록 전에 막아 즉시 피드백). ② 서버: 같은 판정 방식을 그대로 옮긴 Postgres 트리거 `public.move_notes_moderate`(before insert or update)가 다시 검사해 REST를 직접 호출해도 서버가 등록 자체를 거부한다 — 이게 실질적인 강제력이고, 클라이언트 필터는 UX용 1차 방어선일 뿐이다. 처음엔 정규식을 "지울 문자 블랙리스트"로 짰다가, 이모지로 단어를 쪼개 피하는 경우("시🌟발")를 놓친다는 걸 깨닫고 "남길 문자 화이트리스트"(숫자·영문·한글만 허용)로 바꿨다 — 이 방식은 그 사이에 무엇을 끼워 넣어도 결국 지워져 원래 글자만 남는다. 이 두 목록을 뚫는 표현은 개발자 권한(`canModerate`, 카드마다 뜨는 삭제 버튼)으로 사후에 지운다.

캐러셀 UI(`MoveNotesCarousel`)는 가로 스크롤-스냅 컨테이너 위에 카드를 한 장씩 배치하고, 카드가 2개 이상이면 4.5초마다 자동으로 다음 카드로 넘어가되(사용자가 방금 손으로 스와이프·화살표 조작을 했으면 6초간 자동 넘김을 쉼) 점 인디케이터로 원하는 카드로 바로 이동할 수도 있게 했다 — "스크롤 가능 + 자동 애니메이션 전환"을 모두 만족시킨다. 구현 중 실제로 버그를 하나 잡았다 — `el.scrollTo(...)`로 프로그램상 스크롤을 시작하면 그 애니메이션이 진행되는 동안에도 `scroll` 이벤트가 여러 번 연속으로 발생하는데, 매번 즉시 `scrollLeft`로 인덱스를 되계산해 `setIdx`를 부르면 아직 절반만 이동한 중간 지점에서 "이전 인덱스"로 되돌아가 버리고, 그 인덱스 변화가 다시 `scrollTo`를 옛 위치로 불러 애니메이션이 끝나기도 전에 취소되는 피드백 루프가 생겼다(다음/이전 화살표를 눌러도 카드가 안 넘어가는 것처럼 보였다). 스크롤이 실제로 멈춘 뒤(120ms 동안 추가 이벤트가 없을 때)에만 최종 위치로 인덱스를 동기화하도록 디바운스해 해결했다 — Playwright로 `scrollLeft`를 직접 확인해 고치기 전/후 차이를 검증했다.

Playwright로 `move_notes` REST 호출을 가로채 가짜 데이터를 응답하는 방식(백엔드 없이 프론트 로직만 검증)으로 확인했다 — 게스트에겐 로그인 안내가, 로그인 유저에겐 등록 폼이 뜨는 것, 화살표로 카드 사이를 정확히 넘나드는 것(scrollLeft로 실제 위치까지 확인), 개발자 모드에서만 편집·삭제 버튼이 보이고 삭제가 실제로 카드를 지우는 것(DELETE 요청 발생 확인), 금칙어("시발")를 넣으면 POST 없이 바로 에러 메시지가 뜨고 정상 문구는 등록되는 것, 등록 직후 "이미 이 수에 설명을 남겼어요" 안내로 폼이 교체되는 것까지 전부 확인했다. **다만 이번 세션에는 실제 Supabase 프로젝트에 접근 권한이 없어 `supabase-setup.sql`의 새 섹션(15번, move_notes 테이블·RLS·검열 트리거)을 실제 DB에 반영하지 못했다 — 이 기능이 실제로 동작하려면 그 SQL을 Supabase 대시보드의 SQL Editor에 붙여넣고 실행해야 한다.**

### OpenChess v0.2.8 — 2026/8/2

**버그 수정(진짜 근본 원인) — 엔진 추천 수 줄이 특정 depth에서 갑자기 뚝 짧아지던 문제**
사용자가 화면 녹화로 다시 제보 — "n수 후까지 탐색 중" depth 표시 수정 이후에도 특정 depth(영상에서는 19)에 도달하는 순간 3순위 줄이 "7.Ba4 b5 8.Bb3 cxd5 9.Qe2 e4 10.Qxb5+ Bd7 11...."(13플라이 이상)에서 곧바로 "7.Ba4 b5"(2플라이)로 뚝 줄어드는 것이 프레임 단위 비교로 확인됐다 — 같은 첫 수("Ba4")를 유지한 채로 벌어진 진짜 회귀였다(단순히 다른 후보 수로 교체된 것이 아님). `TypedMoveLine`의 "가장 긴 텍스트 유지" 안전장치가 `useRef`(컴포넌트 인스턴스에 묶임)로 구현돼 있었던 게 원인 — `EngineLineRow`는 `key={슬롯 번호}`로 자리를 재사용해 순위가 바뀌어도 리마운트를 피하도록 설계돼 있었지만, 이 특정 후보 수가 MultiPV 순위 변동으로 화면 상위 3줄에서 아주 잠깐 밀려났다가 곧바로 같은 슬롯으로 되돌아오면, 그 사이 그 슬롯을 다른 후보가 차지했던 순간이 있었으므로 React가 이 컴포넌트를 완전히 새로 마운트하고 `useRef`를 초기화한다 — 결과적으로 "가장 긴 텍스트 기억"이 통째로 사라져, 같은 수인데도 처음 본 것처럼 다시 짧게 나타났다. 이 기억을 컴포넌트 인스턴스가 아니라 React 트리 바깥의 모듈 레벨 캐시(`engineLineMaxTextCache`, 포지션이 바뀌면 통째로 비움, 후보 수별로 `Map`에 저장)로 옮겨 마운트·언마운트와 무관하게 유지되도록 했다. Playwright로 9초간 3개 줄 전체를 150ms 간격으로 샘플링해 같은 첫 수에 대해 표시된 수 번호 개수가 한 번도 줄어들지 않는 것을 확인했다(수정 전에는 프레임 비교로 실제 회귀가 재현됨).
사용자가 방금 추가한 5초 심화 탐색이 화면의 depth 표시(평가치 바 "?" 도움말)에 실제로 반영되는지 확인해 달라고 요청했다. 직접 확인해보니 실제로 문제가 있었다 — Worker의 원시 UCI 출력을 가로채는 진단 로그를 심어보니 엔진 자체는 실제로 depth 20까지 정상적으로 도달하는데, 화면의 depth 표시는 8 같은 훨씬 낮은 값에 멈춘 채 움직이지 않았다. 원인은 `onEvalProgress`(depth 진행 콜백)가 이 특정 effect "실행 한 번"에만 묶인 `cancelled` 플래그로 막혀 있었던 것 — 바로 아래 `cache.multiPromise`/`extraKey` 가드는 "같은 포지션이면 진행 중인 엔진 요청을 여러 번의 effect 재실행에 걸쳐 그대로 이어받아 재사용"하도록 설계돼 있어(중복 요청 방지가 목적) 실제 엔진 작업은 `moves.length`가 점진적으로 채워지며 자주 일어나는 effect 재실행을 가로질러 계속 살아있는데, 그 작업을 맨 처음 만든 특정 실행이 재실행으로 정리(cleanup)되며 `cancelled=true`가 되는 순간 그 콜백은 그 뒤로 영원히 조용히 아무 일도 안 하게 됐다(엔진 라인 자체는 `streamLines`가 별도로 `posCacheRef.current.key !== key`라는 다른 기준으로 가드돼 있어 계속 갱신되므로, 라인 내용은 바뀌는데 depth 숫자만 멈춘 것처럼 보이는 불일치가 생겼다). 이 버그는 이전부터 존재했지만 짧은 700ms 안에서는 effect가 재실행되기 전에 요청이 먼저 끝나 잘 드러나지 않았고, 5초짜리 심화 탐색을 추가하면서 그 사이 재실행이 일어날 확률이 크게 늘어 비로소 눈에 띄었다. `onEvalProgress`도 `streamLines`와 동일하게 `posCacheRef.current.key !== key`(어느 effect 실행이 이 콜백을 만들었든, 지금도 같은 포지션인지)로 가드하도록 고쳤다. Playwright로 depth 도움말 툴팁을 열어 t=0.3s "18수", t=3.3s "20수"로 실제 목표 depth까지 정상적으로 올라가는 것을 확인했다(고치기 전에는 t=0.3s/3.3s/7.3s 내내 "8수"에 고정돼 있었다).

**기능 — 학습 탭: 빠른 1차 확정 후 백그라운드에서 5초간 depth 20까지 추가 심화 탐색**
사용자 요청 — MultiPV를 7에서 5로 낮추고(순위가 늘수록 노드당 비용이 커져 목표 depth 도달이 어려워짐), 기존 movetime(700ms)으로 엔진 라인·평가치·수 블록(수 체계 아이콘)을 먼저 전부 빠르게 확정한 뒤, 그 자리에서 곧바로 같은 포지션에 대해 "extra movetime" 5초짜리 두 번째 `evaluateMulti` 요청을 추가로 보낸다. 스톡피시는 "go" 명령 사이에도 치환 테이블(Hash, 방금 올린 성능 개선과 시너지)이 그대로 남아 있어, 이 두 번째 탐색은 실질적으로 "멈췄던 depth부터 이어서" 진행되는 것과 비슷하게 동작한다(얕은 depth는 대부분 캐시 적중으로 순식간에 재통과). 두 요청 모두 같은 slot(`"learn-lines"`)을 써서, 사용자가 다른 수·포지션으로 넘어가면 아직 안 끝난 5초 심화 탐색이 기존 `evaluateMulti`의 supersede 메커니즘으로 자동 중단된다 — 같은 포지션에서 후보 수 목록이 채워지며 이 effect가 여러 번 재실행돼도(`moves.length`가 deps에 있음) 캐시(`posCacheRef`)에 시작 여부를 남겨 심화 탐색이 포지션당 정확히 한 번만 시작된다. 심화 탐색 결과는 엔진 라인·평가치 바뿐 아니라, 그 결과의 1순위 수와 일치하는 후보 수 블록의 `live` 평가치도 함께 갱신한다. Playwright로 실제 브라우저에서 로드 직후와 7초 후 스크린샷을 비교해, 엔진 라인 2·3순위 수순이 실제로 더 깊고 다른 내용으로 갱신되는 것을 확인했다.

**성능 — 짧은 movetime(700ms) 안에서 더 깊이 탐색하도록 엔진 Hash(치환 테이블) 크기 상향**
사용자 질문("700ms 안에 더 깊이 탐색할 수 있는 방법은 없어?")에 대한 답으로, 실제로 안전하게 적용 가능한 개선을 찾아 반영했다. 스톡피시 WASM 빌드는 UCI `setoption`으로 Hash 크기를 지정하지 않으면 보통 16MB의 아주 작은 치환 테이블(Transposition Table)로 시작한다 — 같은 국면을 반복 탐색(트랜스포지션)할 때 이전 계산 결과를 재사용하지 못하는 비중이 커져, movetime처럼 짧은 시간 상한 안에서는 도달하는 depth 자체가 눈에 띄게 줄어든다. `useEngine`(학습 탭·퍼즐 실시간 평가에 쓰는 싱글턴 워커 1개)에는 64MB를, `bootAnalysisWorker`(게임 리뷰·후보 수 배치 분석에 쓰는 기기 코어 수만큼의 워커 풀)에는 워커당 32MB를(풀 전체 메모리 사용량이 코어 수만큼 곱해지므로 더 보수적으로) 부팅 시 `setoption name Hash value N`으로 설정했다. 검토했지만 채택하지 않은 다른 방법들: (1) MultiPV 줄 수를 더 줄이면 depth는 늘겠지만 이미 후보 수 채점에 필요한 최소치(7)까지 낮춰 둔 상태라 더 줄이면 다른 기능이 깨진다, (2) 스레드 수를 늘리는 건 `crossOriginIsolated`(SharedArrayBuffer)가 켜진 호스팅 환경에서만 의미가 있어 코드 변경만으로는 보장할 수 없는 배포 인프라 설정이다, (3) movetime 자체를 늘리는 건 사용자가 명시한 "700ms 안에서"라는 제약과 반대 방향이라 제외했다.

**기능 — 일일 퍼즐 캐러셀 카드를 정사각형 + 큰 보드 중심으로 재설계**
사용자 요청으로 카드에서 "일일 퍼즐" 라벨·오프닝명 텍스트를 모두 없애고, 정사각형 카드 안에 체스보드만(기존 68px → 190px, 약 2.8배) 크게 보여준 뒤 그 아래에 작게 "N명이 풀었습니다"만 남겼다. 활성 카드가 자기 슬롯(`DAILY_SLOT_W`, 스크롤 스냅 계산 기준폭)보다 커도 스크롤 폭 계산에 영향을 주지 않는 기존 `position:absolute` 기법은 그대로 유지하고, 카드가 커진 만큼 슬롯의 예약 높이만 112px→250px로 늘렸다. Playwright로 렌더링을 확인해 카드 실제 크기가 224×231(거의 정사각형)로 나오는 것을 검증했다(정확한 190px 정사각형이 아니라 위 날짜 라벨 높이가 포함된 값).

**버그 수정(근본) — 엔진 추천 수 줄 "타이핑" 애니메이션을 완전히 제거**
사용자가 "ply 15까지 표시돼야 하는데 도중에 타이핑이 끊긴다"며 스크롤이 아닌 진짜 완성을 요구했다. `TypedMoveLine`의 55ms 간격 타이핑 애니메이션은 이 세션 내내 여러 형태로 재발한 버그(줄어듦·멈춤·초기화 경쟁)의 공통 근원이었다 — interval 기반 상태 전이가 많을수록 놓치는 예외 경로도 늘어난다는 판단으로, 애니메이션 자체(`useState`+`useEffect`+`setInterval`)를 걷어내고 지금 확보된 PV를 지연 없이 즉시 전부 렌더링하도록 바꿨다. 같은 줄(같은 posKey+첫 수)이 depth 심화로 다시 보고될 때 PV가 일시적으로 짧아지는 정상적인 탐색 변동에 대비한 "가장 긴 텍스트 유지" 안전장치만 남겼다. 참고로 `pvUciToSans`의 `maxPlies=15`는 상한일 뿐 보장이 아니다 — 실제 표시되는 수순 길이는 movetime(700ms) 안에 엔진이 도달한 depth에 따라 달라지며, 짧은 PV(6~10수) 자체는 정상이다.

**기능 — 게임 리뷰 자유 탐색: 실제 기보 수는 재분석 없이 즉시 원래 리뷰로 이동, 이론 수 즉시 인식**
사용자 요청 두 가지. (1) `ReviewPage`의 `playFree`가 자유 탐색 수를 두면 무조건 `exploreSans`에 쌓고 아래 `exploreMove` effect가 매번 독립 엔진 풀로 실시간 재분석을 돌렸는데, 아직 실제 기보 위치(`exploreSans` 비어 있음)에서 둔 수가 그 지점의 실제 기보 수(`sans[curPly]`)와 정확히 같다면 이미 게임 리뷰 시작 시 `analyzeGame`으로 다 계산해 둔 `result.moves`가 있으므로 다시 분석할 이유가 없다 — 이 경우 곧장 `jump(curPly+1)`로 원래 리뷰 지점으로 이동해 불필요한 재계산과 그 사이의 "분석 중" 깜빡임을 없앴다. (2) `exploreMove` 채점 effect에는 `analyzeGame`·학습 탭 후보 블록에 이미 있는 이론 수(`isBookMoveAt`) 확인이 빠져 있어, 자유 탐색 중 이론 수를 둬도 매번 실시간 재검색을 거쳐 평가치 기반 등급(최선·우수 등)으로 표시됐다 — 같은 `isBookMoveAt` 확인을 추가해, 이론 수는 실시간 분석 없이 곧바로 "이론" 등급으로 확정한다.

**되돌림 + 진짜 원인 수정 — 엔진 추천 수 줄 가로 스크롤을 줄바꿈 대신 유지하되, 자체 구현 드래그를 네이티브 터치 스크롤로 교체**
직전 시도(줄바꿈)를 사용자가 "누구 맘대로 두 줄로 바꾸래, 스크롤 복원하고 원래대로 되돌려"라고 명시적으로 되돌려 달라고 요청해 원래 한 줄+가로 스크롤 디자인으로 복귀했다. 다만 사용자는 여전히 "나와야 할 수순까지 다 안 나오고 정보가 누락된다"고 확신했으므로, Playwright로 `el.innerText`(화면에 실제로 보이는 부분이 아니라 DOM에 실재하는 전체 텍스트)와 `scrollWidth`/`clientWidth`를 직접 찍어 확인했다 — 예를 들어 한 줄의 실제 전체 텍스트는 `"1.e4 e6 2.d4 d5 3.Nc3 dxe4 4.Nxe4 Nf6 5.Nxf6+ Qxf6"`로 온전했고(`scrollWidth 219 > clientWidth 177`), `scrollLeft`를 끝까지 밀면 스크린샷에서도 뒷부분이 그대로 나타났다 — 즉 데이터 자체는 처음부터 누락된 적이 없었다. 그렇다면 사용자가 실제로 손가락으로 밀었을 때 반응이 없어서 "정보가 없다"고 느꼈을 가능성이 가장 크다고 보고, `EngineLineRow`의 자체 구현 pointer 드래그 스크롤(`setPointerCapture`+수동 `scrollLeft` 대입)을 걷어내고 브라우저 네이티브 터치 스크롤(`overflowX:auto`+`WebkitOverflowScrolling:touch`)에만 맡기도록 바꿨다 — 자체 구현 드래그가 일부 모바일 브라우저(특히 인앱 웹뷰)의 네이티브 터치 스크롤과 충돌해 밀어도 반응이 없었을 가능성이 높은 원인으로 지목된다. 페이드 인디케이터와 그 폴백 recompute는 유지한다.

**버그 수정 — 엔진 추천 수 줄이 박스 오른쪽 끝에서 뚝 잘려 "끊긴 것처럼" 보이던 문제**
사용자가 위 `TypedMoveLine`/`bestSansRef` 수정 이후에도 여전히 줄 끝이 잘려 보인다며 스크린샷을 다시 제보했다. 픽셀 단위로 확대해 확인한 결과, 두 스크린샷 모두 텍스트가 "6" 같은 토큰 중간에서 박스 테두리에 바로 붙어 끝났고 — `EngineLineRow`의 기보 텍스트 영역(`outerRef`)은 애초에 `overflowX: "auto"`인 가로 스크롤 박스로 설계돼 있어(전체 PV가 박스 폭보다 길면 자연스러운 동작), 원래도 드래그하면 나머지가 이어져 나와야 정상이었다. 다만 `pvUciToSans`가 변환 실패 시 빈 SAN을 절대 만들지 않고 그 자리에서 `break`하는 것도 재확인해, "빈 수 번호만 찍히는" 시나리오는 코드상 애초에 불가능함을 확인했다 — 즉 데이터 자체는 끊기지 않았고, 문제는 순수히 시각적 신호 부족이었다. 오른쪽 끝 "더 보기" 페이드가 폭 18px·최종 불투명도 0.9로 너무 옅어 어두운 배경에서 거의 안 보였고, 그 페이드를 갱신하는 `ResizeObserver`도 일부 인앱 브라우저(카카오톡·인스타그램 내장 웹뷰 등)에서 콜백이 누락될 수 있어 아예 안 뜬 채로 남을 수 있었다. 페이드를 폭 26px·완전 불투명까지 진하게 넓히고, `ResizeObserver`와 별개로 200ms 주기 폴백 recompute를 추가해 어떤 브라우저에서도 "더 있음" 신호가 항상 정확히 갱신되도록 이중화했다.

**버그 수정(진짜 근본 원인) — 학습 탭 엔진 추천 수 줄이 눈앞에서 갑자기 짧아지던 문제**
사용자가 화면 녹화로 다시 제보 — 이전에 고친 "3줄이 아니라 2줄만 뜨는" 문제와는 다른 증상으로, 이미 여러 수까지 타이핑돼 보이던 PV 줄이 같은 포지션에서 갑자기 더 짧은 수순으로 되돌아가 보였다. `TypedMoveLine`이 `sans.slice(0, Math.min(shown, sans.length))`로 렌더링하는데, 엔진이 depth를 더 깊이 파며 같은 순위(같은 첫 수)의 PV를 다시 보고할 때 그 PV가 이전 보고보다 항상 더 길다는 보장이 없다(반복 심화 탐색의 정상적인 특성 — 실제로 더 짧아질 수 있다). `sans`가 실제로 짧아지면 화면도 그대로 줄어들어, 이미 타이핑된 글자가 사용자 눈앞에서 지워지는 것처럼 보였다. 첫 수(`sans[0]`)가 이전과 같은 한(=같은 줄이 갱신된 것) 지금까지 본 것 중 가장 긴 수순을 계속 렌더링하고, 첫 수 자체가 바뀌면(=다른 줄로 교체됨) 그 즉시 새 수순으로 전환하는 `bestSansRef`를 추가했다.

**버그 수정 — 퍼즐 창의 X 버튼을 누르면 사이트 전체가 먹통이 되던 문제**
사용자 제보 — 퍼즐 풀이 화면에서 X 버튼을 누르면 그 이후로 어떤 버튼도 반응하지 않았다. 로컬 스토리지에 테스트용 퍼즐 데이터를 직접 심어 실제로 재현한 결과, React가 콘솔에 "Rendered more hooks than during the previous render"를 던지며 트리 전체가 깨지는 것을 확인했다 — 이 앱에는 ErrorBoundary가 없어 이 에러가 화면 전체를 그대로 멈춰버린 화면으로 만들었다. 원인은 `PuzzleTab`의 `if (active) return <PuzzleSolver .../>` 조기 반환 "아래"에 `numSuggestions`(번호 자동완성 목록)를 계산하는 `useMemo` 훅이 남아 있었던 것 — 퍼즐을 열어 둔 동안(active 있음)에는 이 훅이 아예 호출되지 않다가, X를 눌러 닫으면(active만 null) 그제서야 호출되면서 렌더마다 호출되는 훅의 개수가 달라지는 Rules of Hooks 위반이 발생했다. 이 훅을 조기 반환보다 앞으로 옮겨 항상 같은 순서로 호출되게 고쳤다. 재현 스크립트로 수정 전/후를 비교해, 수정 전에는 X를 누르면 즉시 저 에러가 뜨며 이후 모든 클릭(다른 탭 이동 포함)이 응답하지 않았고 수정 후에는 정상적으로 목록 화면으로 돌아가고 다른 탭도 계속 클릭 가능한 것을 확인했다.

**기능 — 퍼즐 풀이 화면에서 코치(마스코트) 말풍선 숨기기/보이기 토글**
사용자 요청으로 보드 페이지의 마스코트 말풍선 옆에 눈 모양 아이콘 버튼을 추가했다 — 누르면 말풍선이 사라지고 아이콘만 남고, 다시 누르면 돌아온다.

**기능 — 단순 되잡기의 "유일한 수" 제외 규칙을 학습 탭까지 확장**
직전 버전에서 게임 리뷰(`analyzeGame`·리뷰 자유 탐색)에만 적용했던 규칙("되잡을 수 있는 내 기물이 그 자리 하나뿐인 수는 유일한 수로 승격하지 않는다")을 학습 탭의 후보 수 등급 판정(`assignTiers`, 오프닝 트리 카드·도감 상세 블록에서 모두 쓰임)에도 동일하게 적용해 달라는 요청. `assignTiers`는 그동안 후보 수 목록과 보드 스냅샷만 받고 실제 둔 수의 이력(`sans`)은 받지 않아 `recaptureFact`(상대의 직전 수를 봐야 함)를 호출할 수 없었다 — 새 `sans` 인자를 추가하고, 세 호출부(오프닝 트리 후보 카드, 도감 상세 블록, 학습 탭 메인 트리) 모두 이미 스코프에 있던 실제 수순 배열(`sans`/`path`)을 그대로 넘기도록 고쳤다.

**버그 수정 — 퍼즐 풀이 화면 평가치 막대가 "0.00"에 멈추던 문제**
사용자가 화면 녹화로 제보 — 퍼즐을 열면 막대가 "+2.99"처럼 실제 값을 잠깐 보여주다가 이후 계속 "0.00"으로 고정됐다. `PuzzleSolver`의 `EvalBar`는 `liveDepth != null`(실시간 검색이 진행 중)일 때만 방금 검색한 `liveEval`을 보여주고, 검색이 끝나면 항상 생성 시 미리 계산해 둔 정적값 `curNode.ev`로 돌아가도록 설계돼 있었다(v0.2.6에서 "매번 평가할 때마다 값이 미묘하게 흔들리는" 문제를 없애기 위한 의도적 결정). 문제는 `curNode.ev`가 트리 루트(퍼즐을 열자마자 보이는 첫 포지션)나 개발자가 손으로 만든/구버전 퍼즐 노드에서는 애초에 계산돼 있지 않은 경우가 흔했다는 것 — 이런 노드에서는 검색이 끝나는 순간 없는 값으로 돌아가며 `EvalBar`가 `null`을 `0.00`으로 잘못 표시한 채 그 퍼즐 내내 고정됐다(실시간 검색 자체는 계속 정상 동작했지만, 그 결과를 버리고 없는 정적값을 우선했을 뿐이다). 이런 누락을 채우는 "meta 보강" effect가 이미 있었지만 모식도(`PuzzleSchematic`) 전용으로만 연결돼 있어 메인 막대는 그 값을 전혀 몰랐다. 실시간 검색 effect에 새 `settledEval` 상태를 추가해 이 포지션에서 마지막으로 실제 계산된 값을 별도로 남겨 두고, `curNode.ev ?? settledEval`로 우선순위를 매겼다 — 정식 생성 퍼즐은 여전히 정적값을 그대로 쓰므로 v0.2.6의 수정은 그대로 유지되고, 정적값이 없을 때만 방금 검색한 실제 값이 표시된다.

**기능 — 퍼즐 풀이 화면 코치에도 게임 리뷰와 동일한 MEC(mecFacts) 설명 적용**
퍼즐 코치 말풍선은 그동안 `mecFacts`를 전혀 호출하지 않고, `summarizePosition`(걸린 기물·기물 점수차만 보는 훨씬 단순한 휴리스틱)과 하드코딩된 문구("직전 수를 살펴보는 중이에요…" 등)만 썼다. `mecFacts(sansBeforeMove, san, color, kind, bestSan, beforeCp, threatOut)`를 그대로 쓰려면 `kind`뿐 아니라 `bestSan`·`beforeCp`도 필요한데, 기존 `classifyMoveKind`는 내부적으로 이 값들을 계산하고도 `kind` 문자열 하나만 버렸다. `classifyMoveKind`의 로직을 `classifyMoveKindDetailed`로 옮기고 `{kind, bestSan, beforeCp}`를 모두 반환하게 한 뒤, `classifyMoveKind`는 이를 감싸 `kind`만 꺼내 쓰는 얇은 래퍼로 남겨 기존 세 호출부(모식도 아이콘·메타 보강 등)의 계약은 그대로 유지했다. 새 `mecBubble` effect가 이미 아이콘 판정에 쓰이던 것과 정확히 같은 방식(`moveIcon` effect와 동일한 분기 — 응수 중이면 그 수, 이미 둔 수가 있으면 마지막 수, 아직 안 뒀으면 상대 실수 수)으로 "지금 설명할 수"를 골라 `classifyMoveKindDetailed`를 호출하고, 결과를 `mecFacts`에 그대로 넘겨 `facts[0]`을 코치 말풍선으로 쓴다(`mecFacts` 자체를 계산하지 못하면 기존 `summarizePosition`으로 자연스럽게 폴백).

**버그 수정 — 일일 퍼즐 캐러셀 카드가 스크롤 중 모양이 바뀌는 게 "이상하다"는 제보**
사용자가 화면 녹화로 제보 — 직전 버전에서 활성 카드만 넓은 카드, 비활성 카드는 텅 빈 작은 박스로 서로 다른 모양을 썼는데, 스크롤해 넘어갈 때 카드 자체의 모양이 통째로 바뀌며 옆 카드와 어색하게 겹쳐 보였다. 모든 카드(활성·비활성 모두)가 항상 같은 넓은 카드(보드+"일일 퍼즐"+오프닝명+풀이수) 구조를 쓰고, 비활성 카드는 기존 v0.2.7 방식 그대로 `transform: scale()`로 작고 흐리게 줄어들기만 하도록 되돌렸다 — 카드 구조 자체가 스크롤 중 절대 바뀌지 않아 자연스럽게 커지고 작아지는 느낌만 남는다.

**기능 — 캐러셀 스와이프 범위를 최근 7일로 제한, "번호로 풀기"에서 날짜(YYYY/MM/DD) 검색 및 추천 목록 추가**
사용자 요청으로 `useDailyPuzzleDates`가 만드는 날짜 목록에 `DAILY_CAROUSEL_MAX_DAYS = 7` 상한을 추가했다. 그 대신 "번호로 풀기" 입력창이 숫자만이 아니라 `/`도 받아들이도록 확장해("번호 또는 날짜로 풀기"), `YYYY/MM/DD` 형식을 감지하면 캐러셀과 똑같은 `resolveDailyPuzzleCached(dateStr, engine)`로 그 날짜의 일일 퍼즐을 직접 찾아 연다. 숫자를 입력하는 동안에는(날짜 모드가 아닐 때) 로컬에 알려진 퍼즐(`puzzles`·`archivedPuzzles`) 중 그 번호로 시작하는 퍼즐을 번호(`#123456`)와 이름을 함께 붙여 최대 6개까지 드롭다운으로 추천한다(항목의 `onMouseDown`에서 `preventDefault`해, 클릭 전에 input의 `onBlur`가 목록을 먼저 숨겨버리지 않게 했다).

**기능 — 일일 퍼즐 캐러셀·일일 퀘스트 알림 창을 사용자 스케치대로 재구성**
사용자가 손그림 스케치 두 장(캐러셀 카드 레이아웃, 다이어리 모달 레이아웃)을 올려 그대로 재현해 달라고 요청했다. 캐러셀 카드(`DailyPuzzleCarouselItem`)는 기존엔 날짜 라벨이 미니보드 "아래"에 있고 오프닝명·풀이수가 세로로 쌓이는 구조였는데, 스케치는 날짜를 카드 "위"에 얹힌 헤더로 분리하고 선택된 카드만 미니보드+텍스트를 가로로 나란히 담는 넓은 카드로 그렸다. 이 구조로 다시 짰다 — 활성 카드는 `position:absolute`로 문서 흐름에서 빼서 자기 슬롯(스크롤 스냅 계산이 의존하는 고정폭 96px)보다 넓게(208px) 그려져도 스크롤 폭 계산에 영향을 주지 않게 했고, 비활성 카드는 날짜만 얹힌 작은 빈 테두리 박스로 축소했다. 이 작업 중 진짜 버그 하나를 발견해 함께 고쳤다: 컨테이너 폭을 재는 `useLayoutEffect`가 빈 deps라 딱 한 번만 실행되는데, `dates`가 비동기로 채워지기 전 첫 렌더에서는 스크롤러 자체가 DOM에 없어 측정이 아예 스킵되고, 이후 스크롤러가 실제로 나타나도 다시 측정되지 않아 가운데 정렬용 여백(spacer)이 항상 0으로 남아 있었다 — 기존 디자인은 카드가 슬롯 폭 안에 머물러 티가 안 났지만, 새로 넓어진 활성 카드는 이 여백이 0이면 스크롤러 왼쪽 끝에 잘린 채 렌더링됐다. `dates.length`를 deps에 추가해 스크롤러가 처음 마운트되는 시점에 다시 측정하도록 고쳤다. `DailyPuzzleNoticeModal`(일일 퀘스트 다이어리 모달)의 왼쪽 페이지도, 스케치의 "정사각형 격자 = 미니 체스보드" 의도에 맞춰 장식용 빈 4×4 격자 대신 캐러셀과 동일한 `AnimatedMove` 실제 퍼즐 포지션 미리보기로 교체했다. 브라우저로 캐러셀 레이아웃(날짜 헤더 위치, 카드 폭·중앙 정렬, 보드+텍스트 가로 배치)을 확인했고, 이 세션 테스트 환경에서 일일 퍼즐 엔진 계산이 끝까지 완료되지 않아(무관한 환경 제약으로 보임) 실제 체스 기물이 채워진 최종 상태와 알림 모달 자체는 라이브 확인하지 못해 코드 검토로 갈음했다.

**버그 수정(진짜 근본 원인 3) — 엔진 추천 수 줄이 3개가 아니라 2개만 뜨던 문제**
사용자가 실제 모바일 화면 녹화 영상으로 다시 제보했다 — 영상을 보니 지난 두 번의 "근본 원인" 수정(분석 풀 공유, 앙파상 SAN 처리)과는 다른 증상이었다: 라인이 짧게 잘리는 게 아니라, 3순위 줄 자체가 통째로 없이(로딩 표시도 없이) 2줄만 뜬 채 여러 초 동안 그대로 멈춰 있었다. 원인을 추적한 결과 `evaluateMulti`가 `"go depth D movetime M"`으로 movetime을 엔진에 직접 넘기고 있었는데, MultiPV 모드에서 엔진은 요청한 낮은 순위(3순위 등) 결과가 movetime 안에 단 한 번도 나오기 전에 스스로 멈춰버릴 수 있다 — 그러면 `job.lines`에 그 순위의 키 자체가 없는 채로 `bestmove`가 오고, 최종 배열엔 애초에 그 줄이 존재하지 않게 된다(느린 기기일수록 자주 발생). `bootAnalysisWorker`(게임 리뷰용 풀)와 `useEngine`(학습 탭용 훅) 양쪽 모두, MultiPV 요청은 이제 `"go depth D"`만 보내고(movetime을 엔진에 넘기지 않음) `pump()`에 새 soft-stop 타이머를 추가했다 — 원래 movetime이 지나도 요청한 순위 수만큼 `job.lines`가 채워지지 않았으면 150ms 간격으로 계속 재확인하며 기다리다, 다 채워지는 즉시 `stop`을 보낸다(이미 다 채워진 흔한 경우는 movetime 시점에 바로 멈추므로 체감 속도 변화 없음). 혹시 정말 느린 극단적 상황에도 기존 워치독(`mt+4000`)이 최종 안전망으로 남는다. 모바일 뷰포트(412×915)로 41수 실전 대국을 리뷰하며 20수 연속 빠르게 넘겨 검증했다 — 이전엔 여러 포지션에서 반복적으로 2줄만 뜨던 것이, 이번엔 매번 3줄이 채워지거나(대부분) 짧게 "..." 로딩 표시 후 바로 채워졌다(잠깐 지연되는 경우는 있어도 통째로 사라지는 경우는 재현되지 않음).

**기능 — 기물 간 "연결"·"중첩" 개념 추가, 애니메이션에 서로를 향한 수비자 화살표 표시**
사용자 요청으로 새 개념을 추가했다. 새 `connectionFact`가 이 수로 룩 2개 또는 나이트 2개가 서로의 캡처 가능 칸에 들어가(=서로를 지켜주는 관계가 됨) 그 연결이 이 수 이전엔 없었는데 새로 생겼는지 확인한다(이미 오래전부터 연결돼 있던 상태를 매 수마다 반복해서 짚지 않기 위함). 룩이 같은 파일(세로)로 연결됐고 그 파일이 오픈·세미오픈 파일이면(새 `fileIsOpenOrSemi` 헬퍼, `openFileFact`와 같은 기준) "연결" 대신 "중첩"으로 구분한다. 클릭하면 두 기물 사이를 수비자(초록) 화살표로 양방향 순서대로 보여주는 `playConnectAnimation`을 추가했다(R7과 같은 순차 등장 패턴 재사용). 순수 로직만 떼어내 재현 테스트했다 — 나이트를 각각 다른 자리에서 전개해 서로 지키는 자리로 합류시키자 새 연결로 정확히 감지됐고, 룩 두 개를 같은 오픈 파일에 배치한 보드에서 `stacked` 플래그가 올바르게 켜지는 것을 확인했다. 실제 게임에서의 시각 확인은 이번엔 SAN 표기 제약(중의성 해소 표기 파싱 이슈)으로 하지 못해, 로직 검증으로 갈음했다.

**버그 수정 — "위협 대처"에 애니메이션 연결, 과보호·위협 대처의 공격자/수비자 색이 뒤바뀌어 있던 문제**
사용자 요청으로 R8의 "위협 대처"(이미 걸려 있던 기물을 지키는 수)에도 R7·과보호와 같은 클릭 애니메이션을 연결했다. 그 과정에서 기존 과보호 애니메이션의 색이 실제로는 뒤바뀌어 있었다는 것도 함께 발견해 고쳤다 — `threatSquareDetail(board, sq, color)`은 sq가 상대 기물의 칸일 때(R7 위협)만 "내(color) 쪽=공격자(빨강), 상대=수비자(초록)"가 되도록 설계돼 있는데, sq가 "내" 기물의 칸인 과보호·위협 대처에 그대로 `color`를 넘기면 이 관계가 뒤집혀 진짜 위협하는 상대 기물이 초록(수비자)으로, 나를 지키는 내 기물이 빨강(공격자)으로 표시되고 있었다(과보호는 대부분 실제 위협이 없어 눈에 띄지 않았을 뿐). 두 곳 모두 `enemy`를 넘기도록 고쳐 상대는 항상 빨강, 나는 항상 초록으로 표시되게 했다 — 위협 대처를 실제 재현해 상대 비숍·나이트가 빨간 공격자 화살표로, 내 폰이 초록 수비자 화살표로 순서대로 나타나는 것을 브라우저에서 확인했다.

**기능 — 애니메이션이 있는 MEC 문장은 실제 용어 하나만 밑줄+클릭 가능하게 변경**
지금까지는 "위협"·"과보호"·"예방 수" 사실이면 문장 전체에 밑줄을 긋고 문장 아무 데나 클릭하면 애니메이션이 재생됐다. 사용자 요청으로, 사용자가 직접 정의한 용어(위협/위협 대처/과보호/예방 수/연결/중첩)만 문장 안에서 찾아 그 부분에만 밑줄을 긋고 그 단어를 눌러야만 애니메이션이 재생되도록 바꿨다. 새 `MecKeywordLine` 컴포넌트가 `mecFacts`가 함께 채워 주는 `threatOut.keyword`(예: `"위협 대처"`)를 문장 텍스트에서 찾아 그 부분만 별도 `<span>`으로 감싸 클릭 핸들러를 건다. 기존 문구 중 일부 변형(예: 위협의 "노려요", "걸렸어요")은 실제로 그 키워드를 포함하지 않아, 모든 변형이 항상 정확한 키워드를 포함하도록 문구도 함께 다듬었다("위협 대처"는 기존 문구에 그 단어 자체가 아예 없어서 새로 추가). 브라우저로 실제 재현해 "먼저 d4를 선점하는 <u>예방 수</u>예요.", "미리 지키는 기물을 늘렸어요, <u>과보호</u>예요." 처럼 해당 단어만 밑줄 처리되는 것을 확인했다.

**기능 — 단순 되잡기를 "유일한 수"에서 제외하고, 전용 설명("기물 상황을 유지했다")을 붙임**
스크린샷 제보 — 상대 나이트가 내 폰을 잡은 직후 폰으로 그 나이트를 되잡는 수(`exd5`)에 "유일한 수예요"라는 놀라움의 배지와 "설명하긴 어렵지만, 엔진에 의하면 좋은 수예요"라는 무성의한 문구가 붙었다. 되잡을 수 있는 내 기물이 그 자리 하나뿐인 수는 기물 점수를 맞추는 게 너무 직관적이라 "유일한 수"로 분류할 이유가 없다는 지적이었다. 새 `recaptureFact`(상대 직전 수가 어떤 칸에서 캡처였고 이번 수도 같은 칸을 잡는 캡처인지 확인, 새 `countLegalCapturesOnSquare`로 그 칸을 합법적으로 잡을 수 있는 내 기물 수를 셈)를 두 곳(정식 게임 리뷰 `analyzeGame`, 리뷰 자유 탐색)의 "유일한 수" 승격 조건에 추가해, 후보가 1개뿐인 되잡기는 승격에서 제외했다(등급 자체는 최선의 수로 남는다). `mecFacts`에도 같은 헬퍼를 연결해 "상대 (기물)을 되잡아서 기물 상황을 유지했어요"라는 전용 설명을 붙였다 — 스캔디나비안(`1.e4 d5 2.exd5 Qxd5`)으로 재현해 "퀸을 적절한 칸인 d5에 전개했어요. 상대 폰을 되잡아서 기물 상황을 유지했어요."가 정확히 나오는 것을 확인했다.

**기능 — 룩-마이너 교환을 "교환 희생"으로 구분해 설명**
룩으로 상대의 지켜진 나이트·비숍을 그냥 잡아 곧바로 되잡히거나, 반대로 내 룩이 상대 나이트·비숍의 공격을 받는데 구하지 않고 다른 급한 일을 해 그 교환이 이뤄지도록 두는 수(둘 다 이미 `isSacrifice`가 참으로 확인한 탁월한 수에서만 호출되는 `brilliantSubtype`이 대상) 요청으로, 이 두 경우를 기존 "direct"(자기 기물 직접 희생)·"neglect"(다른 기물 방치) 갈래와 별도로 `exchangeSac` 타입으로 분리했다. `brilliantExplain`이 이 타입일 때 "룩과 비숍을 교환하는 건 쉽지 않은 선택이지만, 지금 상황에선 탁월한 선택이에요"처럼 기물 조합에 맞춰 조사를 붙여 설명한다.

**기능 — 체크메이트 강제 수순에서는 기물 위협 MEC 대신 메이트 카운트다운 표시**
`activeEvalDisp.mate`가 null이 아니면(엔진이 이 위치를 강제 메이트로 판단) `mecFacts`를 아예 호출하지 않고, 대신 `mecPick`으로 로테이션되는 전용 문구("체크메이트로 몰아가는 강제 수순이에요, 메이트까지 N수 남았어요" 등)를 반환하도록 `ReviewPage`의 `mecNotes` useMemo에 `inMateSequence` 분기를 추가했다. 폴즈메이트(1.f3 e5 2.g4 Qh4#)로 검증 — g4(M1 진입)에서 "메이트까지 1수 남았어요", Qh4#(메이트 성사) 자체에서는 "0수 뒤에 체크메이트예요"로 정확히 전환되는 것을 브라우저에서 확인했다. 처음엔 "외통"이라는 단어를 썼는데, 사용자 요청으로 더 직관적인 "체크메이트"로 바꿨다.

**버그 수정 — 리뷰 기보 스트립 드래그 스크롤이 뻑뻑하게 느껴지던 문제**
`ReviewMoveStrip` 컨테이너의 CSS에 `scrollBehavior: "smooth"`가 걸려 있어, 포인터 드래그 중 매 프레임 갱신하는 raw `scrollLeft` 대입까지 브라우저가 매번 이징(easing)을 걸어 애니메이션으로 처리하고 있었다 — 그 결과 드래그가 손가락/마우스 이동에 1:1로 반응하지 못하고 정해진 스텝만큼 뚝뚝 끊겨 움직이는 것처럼 느껴졌다. 컨테이너 스타일에서 `scrollBehavior: "smooth"`를 제거해 드래그 중에는 즉시 반응하도록 하고, 현재 수가 바뀔 때 화면 가운데로 자동 스크롤하는 별도의 `scrollTo({behavior:"smooth"})` 호출은 그대로 남겨 그 경우에만 부드러운 애니메이션이 유지되도록 했다.

**기능 — 과보호 MEC도 R7 위협과 동일한 방식으로 공격자 화살표를 순차 애니메이션으로 표시**
`overprotectFact`가 반환하는 대상 칸(`sq`)을 `mecFacts`의 과보호 분기에서 기존 R7 위협용 `threatSquareDetail(board, sq, color)`에 그대로 넘겨 `threatOut.detail`을 채우도록 했다 — 새 애니메이션 로직을 따로 만들지 않고 `ReviewCoachCard`의 `threatDetail`/`onThreatClick`(`playThreatAnimation`) 경로를 그대로 재사용해, 과보호 문장도 밑줄이 생기고 클릭하면 그 기물을 실제로 지키고 있는 공격자들이 화살표로 순서대로 나타났다 사라지게 했다.

**기능 — 예방 수(R4 직접/R9 간접) 시각화: 금색 테두리 + 공격자/수비자 화살표 페이드 전환 애니메이션**
`directPreventionFact`/`indirectPreventionFact`가 이제 대상 칸뿐 아니라 상대 공격 기물의 출발 칸(`attackerFrom`)과 내가 그 진입을 막은 경로(`myFrom`→`myTo`, 직접 예방이면 옮긴 기물 자체의 경로, 간접 예방이면 새로 컨트롤을 얻은 기물의 경로)까지 반환한다(공유 헬퍼 `enemyCanReach`를 `{type, from}` 형태로 확장). `mecFacts`가 이 값을 `threatOut.prevent`에 채우면, `ReviewCoachCard`의 새 `preventDetail`/`onPreventClick`(`playPreventAnimation`) 경로로 지키는 칸에 금색 그라데이션 테두리(`Board`의 새 `haloSquares` prop)를 씌우고, 상대 진입 경로를 빨간 공격자 화살표로 먼저 보여준 뒤 0.7초 후 그 화살표에 `fading` 플래그(CSS `opacity` 전환, 거의 투명하게)를 얹으면서 동시에 내 대응을 초록 수비자 화살표로 겹쳐 그려 "수비자가 나타나며 공격자가 날아가 사라지는" 듯한 전환 연출을 만들었다. 직접 예방(비숍이 직접 g4를 막아섬)과 간접 예방(폰이 g4의 컨트롤만 늘림) 두 경우 모두 브라우저에서 실제 대국으로 재현해 금색 테두리·화살표·페이드 전환이 의도대로 나타나는 것을 확인했다.

**버그 수정 — 상대 기물을 잡는 수에도 "재전개(재배치)" 설명이 잘못 붙던 문제**
`redeployFact`가 같은 나이트·비숍을 3번 이상 움직인 좋은 수라면 무조건 재전개/재배치로 판정했는데, 그 수가 캡처(예: 세 번째로 움직인 나이트가 상대 기물을 잡는 수)여도 예외 없이 적용돼 "가장 값진 기물로 상대 기물을 잡았다"는 게 진짜 이유인 수에까지 "더 나은 자리로 재전개했다"는 엉뚱한 설명이 붙었다. `info.isCap`일 때는 제외하도록 조건을 추가했다 — 캡처는 자리를 찾아가는 게 아니라 기물을 얻는 게 목적이므로 애초에 재전개 범주가 아니다.

**버그 수정(진짜 근본 원인 2) — 엔진 추천 수 줄이 특정 수순에서 조용히 끊기던 문제**
사용자가 실제로 화면이 끊긴 스크린샷을 다시 제보했다. 지난 세션에 고친 분석 풀 공유 문제(리뷰-학습/퍼즐 탭이 같은 엔진 풀을 나눠 쓰던 것)를 dwell(한 포지션 24초 체류)·rapid-nav(다음/이전 30회 연타) 두 시나리오로 재검증했더니 둘 다 재현되지 않아, 그 수정 자체는 여전히 유효했다 — 그런데 스크린샷은 실제로 한 줄만 훨씬 짧게 끊겨 있었다. 코드를 더 파보니 완전히 다른 두 번째 근본 원인을 찾았다: `pvUciToSans`(엔진 PV의 UCI 수 목록을 SAN으로 순서대로 변환)가 앙파상 캡처를 전혀 고려하지 않았다. `uciToSan`이 앙파상 여부(`ep`)를 받지 않다 보니, PV 중간에 앙파상 캡처가 있으면 "x" 없는 잘못된 SAN(예: "exd6" 대신 "d6")이 만들어지고, 그 SAN으로는 `sanSrc`가 그 폰을 찾지 못해 `applySan`이 조용히 아무 일도 하지 않는다 — 그러면 캡처한 폰이 실제로는 도착 칸에 없는 상태로 다음 반복이 진행되고, 그다음 PV 수가 (엔진 관점에서는 존재하는) 그 폰을 움직이려 하면 `uciToSan`이 도착 칸에서 아무 기물도 찾지 못해 `null`을 반환하며 그 지점부터 라인 전체가 조용히 끊겼다. 각 반복마다 `epTarget`/`epTargetFromMoveInfo`로 앙파상 타깃을 갱신해 `uciToSan`에 넘기도록 고쳤고, 부수적으로 UCI 승진 접미사(5번째 글자)도 그동안 무시되고 있던 것을 파싱해 언더프로모션도 올바르게 반영되게 했다. 앙파상 캡처가 낀 가상의 PV(`e5d6`(앙파상) → `d6d7` → `d7d8q`)로 순수 로직만 떼어내 재현 테스트했더니, 수정 전 코드는 2수 만에 끊겼고 수정 후 코드는 5수 전부를 올바른 표기(`exd6`, `d7+`, `dxd8=Q+` 등)로 만들어냈다 — 진짜 근본 원인이었음을 확인했다.

**버그 수정 — 과보호 대상이 미전개 기물(퀸 등)로 잘못 뽑히던 문제**
스크린샷 제보 — Nc3(나이트를 c3로 전개)를 두면 "퀸이 위협받진 않지만 과보호하는 수예요"라고 나왔는데, 실제로 Nc3가 새로 지키는 건 e4 폰이었다(나이트가 c3에서 d1도 지오메트리상 "컨트롤"하고 있어 퀸도 후보에 올랐을 뿐). `overprotectFact`가 후보를 상대 공격자 수·기물 가치로만 정렬해, 둘 다 공격자 0으로 동률이면 기물 가치가 높다는 이유만으로 아직 한 번도 안 움직인 퀸이 폰을 제치고 뽑혔다 — 누가 봐도 실제로 의미 있는 대상은 그 자리에서 진짜로 지켜지는 폰 쪽이었다. `pieceMoveState`로 각 후보가 전개됐는지(시작 칸을 떠난 적 있는지) 확인해, 전개되지 않은 기물은 정렬에서 가장 낮은 우선순위로 미루도록 정렬 기준에 `developed` 플래그를 맨 앞에 추가했다.

**기능 — 모바일 리뷰 기보 스트립을 스와이프로 빠르게 넘길 수 있도록 전면 개편**
`ReviewMoveStrip`(모바일 리뷰 화면의 가로 기보 줄)이 예전엔 현재 수 앞뒤로 딱 5수만 보여주고 좌우 화살표 버튼으로 한 수씩만 넘길 수 있었다 — 실제로 스크롤할 콘텐츠 자체가 거의 없어 "스와이프로 빠르게 넘기기"가 불가능했다. `SequenceBar`·엔진 라인 줄과 같은 패턴으로 전체 수순을 한 줄에 렌더링하고, 포인터 드래그 스크롤(데스크톱 마우스용)까지 추가했다(터치는 `overflow-x:auto`+`WebkitOverflowScrolling:touch` 네이티브 스크롤이 이미 자연스럽게 동작). 현재 수가 바뀌면 그 수가 항상 화면 가운데로 부드럽게(`scrollBehavior:smooth`) 자동 스크롤된다. 모바일 뷰포트(412×915, 터치 활성화)로 실제 드래그 제스처를 재현해 `scrollLeft`가 실제로 이동하고 뒤쪽 수순이 화면에 나타나는 것까지 확인했다.

**버그 수정(진짜 근본 원인 1) — 엔진 라인이 끝까지 타이핑되지 않던 문제의 진짜 근본 원인(리뷰 페이지와 학습/퍼즐 탭이 같은 분석 풀을 나눠 쓰고 있었음)**
사용자가 이 문제를 8번째로 신고했다. 이전까지의 수정들은 전부 "리뷰 페이지 안에서" 새로 추가한 기능(punishLine·onlyRefutation·brilliantExplain 등)이 slot 없이 엔진 큐를 막던 사례를 하나씩 잡는 방식이었는데, 그때마다 다른 사례가 또 나타났다 — 근본 구조를 안 보고 증상만 쫓았기 때문이다. 이번엔 구조 자체를 다시 봤다. **진짜 원인 1**: `openReview`는 리뷰를 연 탭을 언마운트하지 않는다(주석에 "onOpenGame과 달리 setTab을 호출하지 않음"이라고 명시돼 있음 — 뒤로가기 시 원래 화면으로 돌아가기 위한 의도적 설계). 즉 리뷰를 학습 탭에서 열면 `LearnTab`이, 퍼즐 탭에서 열면 `PuzzleTab`(→`PuzzleSolver`)이 그 아래에 계속 마운트된 채로 `liveOn`이 켜져 있으면 실시간 엔진 평가를 계속 돌린다. **진짜 원인 2**: 그 백그라운드 평가와 리뷰 페이지의 엔진 라인 스트리밍이 "완전히 독립된 풀"이라고 여러 커밋의 주석에 적혀 있었지만 사실이 아니었다 — `getAnalysisPool(profile, urls)`은 `profile`을 키로 캐시되는 **앱 전체에서 하나뿐인** 풀이라, 학습 탭·퍼즐 탭·리뷰 페이지가 같은 엔진 프로필을 쓰면 전부 같은 풀 인스턴스를 나눠 쓴다("독립"은 useEngine의 단일 공유 워커와는 별개라는 뜻이었을 뿐, 탭 사이의 독립은 아니었다). 게다가 리뷰 페이지 내부에서도 6개 기능(review-lines·exploreMove 채점 2개·brilliantNote·onlyRefutation·punishLine)이 각자 다른 slot을 쓰면서도 전부 `pool[0]`(또는 `pool[1]`)만 재사용해, 그 fallback 인덱스 하나에 요청이 몰렸다. 두 가지를 함께 고쳤다: (1) `reviewGame`이 열려 있는 동안 `LearnTab`·`PuzzleTab`에 넘기는 `liveOn`을 `liveOn && !reviewGame`으로 강제 차단해, 리뷰가 열리면 배경 탭의 엔진 사용이 완전히 멈추고 풀 전체를 리뷰에 양보하게 했다. (2) 새 `poolWorker(pool, idx, engine)` 헬퍼로 리뷰 페이지의 6개 기능에 서로 다른 워커 인덱스를 배정하고, idx 0(review-lines, 실시간 라인 스트리밍)은 다른 어떤 기능과도 절대 겹치지 않는 전용 자리로 고정했다. 실제 41수 대국을 브라우저에서 분석까지 마친 뒤 Next를 20회 연속 빠르게 눌러 검증했다 — 엔진 라인 3줄이 매번 끝까지(11수 이상) 타이핑됐다.

**기능 — 리뷰 페이지 자유 탐색에도 MEC이 기보 수와 동일하게 적용됨을 확인**
`mecFacts` 호출부가 이미 `activeMove`(기보 수든 자유 탐색 수든 공통으로 쓰는 값)만 참조하도록 되어 있어 별도 코드 변경은 필요 없었지만, 실제로 그런지 브라우저에서 직접 검증했다 — 8.Qf3 대신 8.Qe2를 자유롭게 둬 보니 "퀸을 e2로 옮겨 전개했어요. 상대 비숍을 향한 잠재 위협을 만드는 수예요."처럼 R6(전개)·R13(잠재 위협)이 실제로 둔 기보 수와 완전히 동일한 방식으로 적용됐다.

**기능 — 걸린 기물 안내 문구를 좌표 중심으로, 탁월한 수의 "기물 회수 가능성" 텍스트 판단을 화살표 시각화로 교체**
사용자 요청으로 두 가지를 바꿨다. 기물 긴장 사실(`MEC_PHRASES.mine`, 이 진영의 걸린 기물)의 문구를 "(기물)이 지금 안전하게 잡힐 수 있는 위치에 있어요."에서 좌표를 명시하는 "(좌표)에 있는 (기물)을 잃게 돼요."로 바꿨다(`tensionFacts`가 이미 갖고 있던 칸 좌표를 `FILES[c]+(8-r)`로 붙임). 탁월한 수(희생) 설명에서 엔진 PV 몇 수만 보고 "이후 이 수순을 두면 기물 점수를 되찾을 수 있다"고 판단하던 `brilliantRecoupLine`은 근거가 얕다는 지적에 따라 완전히 삭제했다 — `brilliantExplain`은 이제 그 판단 없이 곧장 "장기적인 포지션 이득으로 압박한다"는 일반적인 문장으로 넘어갔는데, 뒤이어 이 fallback 문장 자체도 "빼 달라"는 요청을 받아 완전히 없앴다 — 이제 무승부를 노린 경우(`brilliantDrawSeekLine`)가 아니면 탁월한 수 설명은 유형 설명 한 문장으로 끝난다. 대신 새 `hangingPieceArrows(board, color)`가 그 진영의 기물 중 지금 상대에게 안전하게 잡힐 수 있는 것 전부를 찾아, 각 기물마다 그걸 잡을 수 있는 모든 상대 기물에서 화살표를 그린다 — 탁월한 수(언더프로모션 제외) 위치에서 기존에는 방금 옮긴 기물 하나에 대한 위협만 화살표로 보여줬는데, 이제 그 수로 인해 방치된 다른 기물까지 전부 빨간 화살표로 한눈에 보여준다.

**기능 — R4/R9 예방 수에 좌표 명시, R8 과보호 대상 우선순위 정렬**
사용자 요청으로 두 가지를 보강했다. `directPreventionFact`/`indirectPreventionFact`가 이미 들고 있던 대상 칸(`sq`)을 `preventDirect`/`preventIndirect` 문구에도 좌표(`FILES[c]+(8-r)`)로 명시하도록 `MEC_PHRASES`에 `sq` 인자를 추가했다("상대가 이 칸에" → "상대가 d4에"). `overprotectFact`는 예전엔 보드를 훑다가 처음 찾은 후보 하나만 돌려줬는데, 새 `countAttackers(board, r, c, attackerColor)`로 각 후보의 상대 공격자 수를 세어 공격자 수 내림차순(같으면 기물 가치 내림차순)으로 정렬한 뒤 1순위 후보를 돌려주도록 바꿨다.

**기능 — MEC Reference(사용자가 실제 대국 16수를 분석해 정리한 규칙 문서) 반영, 새 개념 15종 추가**
사용자가 직접 한 대국(1.e4 e5 2.Nf3 Nf6 3.Nxe5 d6 4.Nd3 Nxe4 5.Nc3 Nf6 6.Nf4 d5 7.d4 Be7 8.Qf3 c6 9.Bd3 O-O 10.h3 Na6 11.Bxa6 bxa6 12.Nd3 a5 13.O-O Ba6 14.Re1 Re8 15.Bg5 Qb6 16.Ne5)의 16수를 한 수씩 검토하며 `public/MEC Reference` 문서에 정리해 준 새 규칙들을 코드화했다. 우선순위 충돌 시 참조할 수 있도록 R1~R15로 번호를 매겼다(R7 제외, 아래 참고).
- **R1 교환 요청 / R2 요청 수락·거절**: 새 `exchangeOfferFact`가 이 수(폰·킹 제외)를 상대의 같은 가치 기물이 공격할 수 있는 칸으로 옮겼는지 확인해, 그 칸이 아군에 보호받고 있으면(대등한 맞교환) "위협"이 아니라 "교환 요청"으로, 보호받지 못하면(탁월한 수 제외) "공짜로 내준다"로 표현한다. 새 `exchangeOfferResponseFact`는 상대의 직전 수가 이런 "교환 요청"이었을 때, 이번 수가 그 기물을 잡으면 "수락", 다른 칸으로 물리면 "거절"로 판정한다. 이 재설계에 맞춰 `evasionFact`(기존 회피/반격 판정)는 최소가치 공격자가 나와 같은 가치일 때(=대등한 교환 요청 상황)는 더 이상 "위협을 피한다"로 오판하지 않도록 제외했다.
- **R3·R11 재전개/재배치**: 새 `redeployFact`가 같은 나이트·비숍을 3번 이상(2번까지는 흔한 정상 전개로 무시) 움직인 좋은 수에, 마이너 피스 전개+캐슬링이 아직 안 끝난 오프닝 단계면 "재전개", 그 이후면 "재배치"로 용어를 구분해 붙인다. R2(교환 요청 거절)와 같은 수에 함께 적용되면 하나의 글로 잇는다(예: "상대의 교환 요청을 거절했어요. 나이트를 최적의 위치로 재전개하는 수예요.").
- **R4·R9 예방 수(직접/간접)**: 새 `directPreventionFact`는 이 수가(캡처가 아닌) 빈 칸을 선점했는데 이 수가 없었다면 상대가 폰이나 기물로 그 칸에 와서 내 기물을 새로 위협할 수 있었는지, 새 `indirectPreventionFact`는 폰의 비캡처 전진이 새로 컨트롤하게 된 대각선 칸에 같은 논리를 적용한다. 두 헬퍼 `enemyCanReach`(상대가 그 칸에 도달할 수 있는 기물 종류)·`wouldThreatenIfEnemyReaches`(그 기물이 거기 있었다면 안전하며 내 기물을 SEE상 위협하는지)를 공유한다.
- **R5 다음 수 캐슬링 예고**: 새 `nextMoveCastleFact`가 캐슬링 권리는 있지만 경로가 막혀 있던 상태에서 이 수로 경로가 완전히 비어 다음 수에 캐슬링이 가능해졌는지 `legalDests`로 직접 확인해 짚어준다.
- **R6 전개 표현**: 새 `developmentNote`가 폰·킹을 제외한 기물의 첫 이동에 "(적절한 칸에) 전개했다" 문장을 만들어, mecFacts가 최종적으로 고른 문장 앞에 붙인다(하나의 글로 이어지도록).
- **R8 위협 대처 / 과보호**: 기존 `controlFacts.defends`(이미 걸려 있던 기물을 지키는 것)는 그대로 "위협 대처"로 쓰고, 새 `overprotectFact`가 걸리지 않은 기물에 미리 방어자를 붙이는 경우를 "과보호"로 구분한다.
- **R10 교환론**: 기존 `exchangeFact`(동가 교환의 유·불리 프레이밍)의 실제 등급 대조 로직을, 비숍쌍 유지·상대 폰 구조 약화라는 두 근거가 상충할 때 "등급이 곧 어느 쪽이 우세했는지의 증거"로 해석하도록 주석으로 명시했다(판정 로직 자체는 기존 등급 대조 그대로 재사용).
- **R12 캐슬링 전용 평가**: 캐슬링이 좋은 수면 고정된 긍정 문구로 다른 사실을 전부 대체하고(퀸사이드면 a열 폰 안내를 이어 붙임), 나쁜 수인데 위 파이프라인이 아무 원인도 못 찾았으면 "엔진에 의하면" 문구로 대체한다.
- **R13 잠재 위협**: 새 `latentThreatFact`가 SEE상 순이득 있는 "진짜 위협"은 아니어도 상대 기물에 새로 공격자를 하나 더 늘렸는지 확인한다. 비숍·룩·퀸이면 첫 번째로 막힌 기물 뒤에 놓인 배터리 기물까지(한 겹만) 대상에 포함한다.
- **R14 오픈 파일**: 새 `openFileFact`가 룩이 이동한 파일에 내 폰이 없으면(상대 폰도 없으면 오픈, 있으면 세미오픈) 짚어준다.
- **R15 폰 희생**: 새 `pawnSacrificeFact`가 이 수를 두기 전 걸려 있던 내 폰을 이 수가 지키지도 잡지도 옮기지도 않았고(좋은 수일 때만) 그 폰이 이 수 이후에도 여전히 걸려 있으면 "폰 희생"으로 명시한다.
- 좋은 수인데 위 어떤 사실도 못 찾으면(R2·R3가 요구한 "엔진에 의하면" 취지를 일반화) `noObviousReason` 문구로 대체한다.

**기능 — R7: "위협" 코멘트 밑줄 표시 + 클릭 시 공격자→수비자 화살표 애니메이션**
텍스트 사실 판정과 성격이 달라 지난 커밋에서는 미뤄 뒀던 R7을 마저 구현했다. `reviewCoachCopy`가 반환하던 `body`(등급별 기본 문구 + 블런더 응징/유일한 수 대안 등)와 mecFacts의 마지막 사실 한 줄을 분리해 `mecLine`으로 따로 돌려주도록 바꾸고, `ReviewCoachCard`가 이 `mecLine`을 별도의 `<p>`로 렌더링한다 — mecFacts의 대표 사실이 "위협"(`ctrl.threats`)이었을 때만(새 out-parameter `threatOut`을 통해 `mecFacts`가 `{ targetSq, attackers, defenders }`를 채워 줌) 이 줄에 밑줄과 클릭 커서를 준다. 새 `threatSquareDetail(board, sq, color)`가 그 칸을 둘러싼 내 공격자 전부와 상대 수비자 전부를 모으고, 클릭하면 `playThreatAnimation`이 공격자 화살표부터(빨강, danger와 같은 스타일) 하나씩 450ms 간격으로 그리고 이어서 수비자 화살표(초록, `T.good`)를 하나씩 그린 뒤, 마지막 화살표가 뜨고 1초 후 전부 지운다(진행 중 다른 수로 넘어가면 예약된 타이머를 전부 취소하고 즉시 지움). 8.Qf3(퀸을 f3로 전개하며 d5 폰을 위협)로 실제 화면에서 검증했다 — 밑줄 줄 클릭 시 Qf3·Nc3(공격자, 빨강)와 Qd8(수비자, 초록) 화살표가 순서대로 나타났다가 정상적으로 사라졌다.

**기능 — MEC에 나이트·비숍의 비효율적인 전개(시작 칸 복귀·상대 폰에 쫓긴 우회) 추가**
직전 커밋에서 폰 템포 판정 전용으로만 쓰고 삭제했던 `pieceMoveState`(기물별 이동 이력)를, 나이트·비숍의 전개 낭비도 잡아내기 위해 되살렸다(id별 "처음 시작 칸을 떠난 수의 인덱스"까지 함께 기억하도록 `firstMovePly`를 추가). 새 `pieceDetourFact`가 이미 한 번 이상 움직인 나이트·비숍이 이번 수로 (1) 정확히 자기 시작 칸으로 되돌아가면 "시작 칸으로 그대로 돌아갔다"고, (2) 상대 폰이 지금 그 기물이 있던 칸을 공격해 쫓아냈고(그 공격이 실제 상대 폰인지 확인) 그 도착 칸이 시작 칸을 처음 떠나던 순간의 보드를 다시 만들어 확인했을 때 사실 한 수만에 갈 수 있는 자리였다면 "상대 폰 한 수에 템포를 내주며 두 수를 들여 도착했다"고 짚어준다. 두 판정 모두 그 수의 Stockfish 등급이 이미 좋으면(`MEC_GOOD_KINDS`) 그럴 만한 이유가 있었다고 보고 건너뛴다.

**버그 수정 — 템포 낭비 지적을 "낭비를 저지른 그 수"에만, 실질적 이득이 있으면 아예 안 붙게 재설계**
직전 수정에서 `firstMoveDist`로 "첫 이동이 두 칸이었으면 낭비가 아니다"는 고쳤지만, 여전히 낭비를 저지른 수(첫 전진) 자체가 아니라 그 폰이 다시 움직이는 한참 뒤의 무관한 후속 수마다 반복해서 "그때 두 칸 갈 수 있었다"고 지적하는 구조였고, 그 첫 전진이 상대 기물을 쫓아내는 등 실질적인 이득이 있었어도(사용자 예시 — 한 칸만 전진해 상대 기물을 쫓아내며 포지션을 개선한 수) 무조건 낭비로 몰았다. `tempoWasteFact`를 다시 설계해, (1) "홈 랭크에서 한 칸만 나가는 그 수 자체"에만 판정하고(더 이상 이후 수순을 돌아보지 않음 — `pieceMoveState`가 이 용도로는 더 이상 필요 없어져 삭제), (2) 그 전진이 `controlFacts`로 확인했을 때 상대 기물에 새 위협을 전혀 만들지 않았을 때만, (3) 그 수 자체의 Stockfish 등급(`kind`)이 이미 나쁠 때만(`MEC_GOOD_KINDS`에 속하지 않을 때만) 붙도록 세 조건을 모두 걸었다.

**버그 수정 — MEC 세 가지 오판정(기물 교환 유불리·완성된 교환을 위협으로 오인·이미 두 칸 뛴 폰의 템포 낭비 오탐)**
사용자가 스크린샷 3장으로 지적한 세 가지 문제를 각각 원인부터 고쳤다. (1) `exchangeFact`가 "이기고 있다/지고 있다"를 순수 `materialDiff`(기물 점수차)로만 판정해, 포지션이 확실히 유리해도(+0.74) 기물 점수 자체가 팽팽하면 "지고 있을 때는 교환을 피하는 게 좋다"는 엉뚱한 문구가 나왔다 — 이 수를 두기 직전 실제 엔진 평가(mover 관점 cp)를 `analyzeGame`·자유 탐색 양쪽의 수 객체에 `beforeCp`로 함께 담아 `exchangeFact`에 넘기고, 평가가 없을 때만(예외 상황) materialDiff로 대체하도록 고쳤다. (2) 상대 기물을 동가 이상으로 잡는 수(예: 나이트를 잡는 비숍)를 두면, 그 도착 칸이 곧 되잡히는 것은 원래 그 교환이 완성되는 정상적인 흐름인데도 `tensionFacts.mine`이 "이 기물을 지킬 수를 찾아야 한다"고 마치 새로 위태로워진 것처럼 경고했다 — `mecFacts`에서 이 수 자체가 만든 동가 이상 교환의 도착 칸은 `tensionFacts.mine`에서 제외해, 대신 `exchangeFact`가 그 교환의 유불리를 설명하도록 우선순위를 넘겼다. (3) `tempoWasteFact`("두 칸 갈 수 있었는데 나눠서 전진했다")가 같은 폰의 두 번째 이동이기만 하면 무조건 발동해, 첫 이동이 이미 a2-a4처럼 두 칸이었던 폰의 그다음 이동(a4-a5)에도 잘못 붙었다 — `pieceMoveState`가 각 기물의 "첫 이동 거리"(`firstMoveDist`)까지 기억하게 해, 첫 이동이 실제로 한 칸이었을 때만(즉 정말 두 칸 갈 기회를 놓쳤을 때만) 이 사실이 붙도록 했다.

**기능 — 탁월한 수 코치 설명을 유형별로 세분화하고 엔진 PV 근거를 덧붙임**
기존엔 탁월한 수(brilliant) 판정 내부에 "직접 희생"(이동한 기물 자신이 순손실)·"방치 희생"(다른 기물을 걸어 둔 채 더 급한 일을 함)·언더프로모션 세 갈래가 이미 있었지만, 코치 코멘트는 어느 쪽이든 "OOO에 대한 위협을 무시하고 희생하는 탁월한 수예요"라는 한 가지 문구+`brilliantIdeaNote`(새 엔진 호출 없이 브릴리언트 화살표의 "idea" 화살표만 문장으로 바꾼 것)로 뭉뚱그려 설명하고 있었다. 새 `brilliantSubtype`이 `isSacrifice`와 같은 조건식을 재사용해 세 갈래를 실제로 구분하고, `brilliantExplain`(엔진 필요, 비동기)이 그 유형을 능동적인 문장으로 밝힌 뒤(예전의 "위협 때문에 더 급한 일을 처리했다" 같은 수동적 표현 대신, 방치 희생은 "위협을 무시하고 대신 더 큰 이득을 얻어내는 수"로 명시) 엔진 PV로 왜 탁월한지 근거를 덧붙인다. 새 `brilliantRecoupLine`이 이 수를 둔 뒤 엔진의 자연스러운 응수 흐름(`genPunishLine`과 같은 원리)을 몇 수 따라가며 mover 관점 기물 점수차가 다시 좋아지는 지점을 찾으면 그 구체적인 수순을 그대로 설명하고, 찾지 못하면 "당장 기물 점수는 못 되찾아도 포지션 이득으로 압박해 나중에 상대의 기물 희생을 강제할 수 있다"는 문장으로 대신한다. 이 수를 두기 전 평가(mover 관점 cp)가 이미 마이너스였다면(지는 상황) 새 `brilliantDrawSeekLine`으로 같은 응수 흐름 안에 스테일메이트·3회 동형 반복으로 실제 무승부가 되는 지점이 있는지 먼저 확인해, 있으면 "정상적으로 두면 계속 밀릴 뿐이라 이 수순으로 무승부를 강제해야 했다"를 우선 설명한다. 언더프로모션은 새 `underpromoReason`이 퀸으로 승진했을 때의 가상 국면을 실제로 재구성해 스테일메이트가 되는지, 또는 퀸 자리는 바로 잡히는데 실제로 고른 기물 자리는 안전한지 두 원인만 확인해 밝히고, 둘 다 아니면 원인을 특정하지 않는다. 마지막으로 실제 둔 수가 엔진의 1순위 수와 달랐다면 "최선의 수는 아니지만 찾기 힘든 감각적이고 탁월한 수"라는 문구를 덧붙인다. 모든 문장은 논리적인 전후 구조로 이어 붙인 하나의 글이며, 기호는 마침표·쉼표만 쓴다. 기존 `sacrificedPieceKor`(어떤 기물을 희생했는지)·`brilliantIdeaNote`는 이 새 설명으로 대체됐고, `sacrificedPieceKor`는 위협 화살표 표시용으로만 남았다.

**기능 — MEC에 기물 교환의 전략적 타당성(유·불리 상황 대조) 추가**
새 `exchangeFact(sansBeforeMove, san, color, kind)`가 동가 교환(N-N·B-B·R-R·Q-Q 등 — 폰끼리 교환은 이미 `pawnTradeFact`가 다루므로 제외)일 때, 이 수를 두기 전 `materialDiff`로 유·불리를 가르고 실제 Stockfish 등급과 대조한다. 유리한 상황의 교환은 원래 좋은 선택(단순화 전략)이므로 등급이 좋으면 "유리한 상황에서의 기물 교환은 게임을 쉽고 안정적으로 풀어가는 방법이에요"를, 등급이 나쁘면 반대로 "목적 없는 교환"이었다는 프레이밍을 쓴다. 불리한 상황의 교환은 원칙적으로 피해야 하므로, 등급이 최선 이상(brilliant/best/only)이면 "피하는 게 좋지만 이 교환은 불가피한 최선이었다"를, 우수·좋음이면 "피하는 게 좋지만 이번엔 괜찮은 선택이었다"를, 등급이 나쁘면 "피하는 게 좋은데 이 교환은 아쉬운 선택이었다 — 기물 교환은 목적을 갖고 해야 한다"를 붙인다. 재료 우위가 정확히 0(팽팽)일 때는 유·불리 프레이밍이 성립하지 않으므로 판정을 건너뛴다.

**기능 — MEC(Move Evaluation Codification): FEN·PGN 기반 수 설명 체계를 코치 코멘트에 적용**
사용자와 여러 차례 설계를 주고받으며 세 번 다시 세운 기능이라, 최종 형태만 기록한다(중간 단계는 아래 개발자 기록 참고). 처음엔 사용자가 정리해 준 오프닝 원칙 20개를 "위반 판정" 방식으로 코드화했는데, 실전에서 문제가 드러났다 — 예를 들어 퀸이 나이트에게 그냥 잡히는 블런더에 "공격받지도 않는 룩을 조심해 폰 뒤에 숨어라"는, 실제 손실 원인과 무관한 원칙을 갖다 붙이는 식이었다. 그래서 "판정" 대신 "이 수가 실제로 만드는 구체적 사실"만 코드화하는 쪽으로 전면 재설계했다 — 옳고 그름을 매기지 않고 사실을 우선순위대로 나열해, 해석이 자연스럽게 드러나게 한다. `mecFacts(sansBeforeMove, san, color)`가 네 갈래를 우선순위 순으로 합친다: (1) FEN 기반 — 기물 긴장(`tensionFacts`, 양쪽에서 지금 SEE상 순손실이 나는 방치 기물 전체 — 대부분의 블런더는 이 사실 하나로 이미 정확히 설명된다), (2) FEN 기반 — 이 수가 새로 만든 위협·방어(`controlFacts`, 신설 — 이동한 기물이 도착 칸에서 실제로 SEE상 이득이 있게 공격 가능한 상대 기물은 "위협", 이 수 전엔 SEE상 뺏길 수 있었는데 지금은 아닌 아군 기물은 "방어"로만 잡는다 — 단순히 기하학적으로 닿는다고 다 넣지 않는다), (3) PGN 기반 — 템포 낭비(`tempoWasteFact`, 신설 — 같은 폰이 파일이 바뀐 적 없이 두 번째로 전진하면 처음에 두 칸 갈 수 있었다는 뜻), (4) PGN 기반 — 캐슬링/앙파상은 판정 없이 정보로만(`castleEpFacts` 재사용, 권리를 잃었을 때·퀸사이드 캐슬링일 때만 언급). 여러 수에 걸친 기물 재배치 계획(엔진 PV 참고)은 매 수 엔진을 새로 돌려야 해 비용이 커서 코치 카드에 자동으로 붙이지 않고, 온디맨드 기능으로 남겨 뒀다(아직 미구현).

`/review`의 `reviewCoachCopy`/`ReviewCoachCard`에 연결했다 — 정석 수(book)만 제외하고 모든 등급에 `mecFacts`의 최우선 사실 하나를 붙인다. 세 "극단" 등급은 별도 근거를 추가로 붙인다: **블런더**는 응징 수순(`punishLine`, `genPunishLine` 재사용), **탁월한 수**는 "무엇을 희생했는지"(`sacrificedPieceKor`)에 이어 새 `brilliantIdeaNote`(새 엔진 호출 없이 기존 `brilliantArrows`의 "idea" 화살표를 문장으로 바꿔 "그래서 무엇을 얻는지"까지 설명), **유일한 수**는 새 `onlyMoveRefutation`(그 포지션을 MultiPV-2로 재평가해 2순위 후보를 찾고 `genPunishLine`으로 그 후보의 응징 수순까지 보여줌). 이 기능이 "안 되는 것 같다"는 지적을 받아 원인을 다시 보니, `genPunishLine`이 movetime 상한 없이 `go depth 14`만 보내 워커 큐의 기본 워치독(15000ms)에 걸릴 때까지 최악 수십 초씩 걸릴 수 있었다 — `genPunishLine`에 movetime 인자를 추가해 900ms 상한을 걸어 체감 응답 속도를 확보했다. 퍼즐 풀이 화면의 코치 말풍선(`summarizePosition`)에도 `tensionFacts`를 연결해, 막연한 "기물 점수가 팽팽해요" 대신 "상대 나이트가 걸려 있어요 — 잡을 기회를 찾아보세요" 같은 구체적인 힌트를 우선 보여주도록 했다.

**버그 수정 — 방금 위협한 상대 기물을 "원래부터 있던 기회"처럼 말하던 문제**
백이 나이트를 위협하는 수(e5 등)를 두면, `tensionFacts.theirs`(지금 SEE상 안전하게 잡을 수 있는 상대 기물 전체)가 방금 위협받게 된 그 나이트도 함께 잡아내 "상대 나이트가 걸려 있어요 — 잡을 기회예요"라고 말했는데, 상대 차례가 지나야 실제로 잡을 수 있는데도 마치 지금 당장 잡을 수 있는 기존 약점을 발견한 것처럼 들려 어색하다는 지적을 받았다. `mecFacts`에서 `tensionFacts.theirs`의 최우선 항목이 `controlFacts.threats`(이 수 자체가 새로 위협한 기물)와 같은 칸이면 "잡을 기회예요" 문구 대신 `controlFacts`의 "이 수는 상대 OOO를 노려요"(이 수가 방금 만든 위협이라는 뉘앙스)를 쓰도록 우선순위를 바꿨다. 이 수와 무관하게 이미 걸려 있던(다른 칸의) 기물만 "잡을 기회예요" 문구를 그대로 쓴다.

**버그 수정 — MEC 문구의 조사(을/를·이/가·은/는) 오류, 폰 교환/긴장 사실 추가**
MEC 문구가 `PIECE_KOR` 값(킹·퀸·룩·비숍·폰은 받침 있음, 나이트만 받침 없음)에 조사를 하드코딩해 붙이고 있어("...를 노려요" 등), 받침 있는 기물 이름에는 "룩를 노려요"처럼 문법이 깨졌다. 유니코드 완성형 한글의 마지막 글자 코드로 받침 유무를 판별하는 `hasBatchim`과 `josaIGa`/`josaEulReul`/`josaEunNeun`/`josaGwaWa` 헬퍼를 추가해, `MEC_PHRASES`·`brilliantIdeaNote`의 기물 이름 결합을 전부 이 헬퍼로 바꿨다. 별개로 두 가지 폰 전용 사실을 추가했다 — **폰 교환**(`pawnTradeFact`): 이 수가 폰으로 폰을 잡는 수(앙파상 포함)면, 잡은 뒤 내 폰이 중앙에 더 가까워졌는지(파일 거리 기준)로 "OOO폰과 OOO폰을 교환하는 것은 좋은/좋지 않은 선택이에요"를 만든다(원칙 16 "폰 교환은 중앙 쪽으로"를 SEE 없이 파일 거리만으로 코드화). **폰 긴장**(`pawnTensionFact`): 폰을 잡지 않는 전진이 상대 폰과 서로 대각선으로 마주보게(둘 다 서로를 잡을 수 있는 상태) 만들면 "서로 폰을 노리고 있어요"를 붙인다(폰의 대각선 캡처는 마주보면 항상 상호적이라 한쪽만 확인). `mecFacts` 우선순위에서 회피/반격 다음, 일반 위협/방어보다 앞자리에 넣었다.

**기능 — MEC에 회피·반격 개념 추가, 문장 다양화**
사용자 요청으로 새 `evasionFact(sansBeforeMove, san, color)`를 추가했다 — 가치가 높은(폰 제외) 내 기물이 이 수를 두기 전부터 SEE상 걸려 있었는데, 이 수가 바로 그 기물을 움직여 더 이상 안전하게 잡히지 않는 칸으로 옮겼다면 "회피"로, 그 도착 칸에서 상대의 가치가 같거나 더 높은 기물을 SEE상 이득 있게 새로 노린다면 한 단계 더 적극적인 "반격"으로 판정한다(둘 다 SEE 계산 재사용, 새 엔진 호출 없음). `mecFacts`의 우선순위에 기물 긴장 바로 다음(회피 판정이 실질적으로 tensionFacts.mine이 사라진 이유를 설명해 주므로) 자리로 넣었다. 별개로, 같은 사실을 매번 똑같은 문장으로만 말하면 기계적으로 들린다는 지적에 따라 `MEC_PHRASES`로 유형별 표현을 2~3개씩 두고 `mecPick(variants, seed)`이 이 수의 ply로 순환시키도록(`REVIEW_COACH_COPY`가 등급별 코멘트를 ply로 순환시키는 것과 같은 방식) 모든 MEC 문구를 다시 썼다.

**기능 — MEC에 "공짜 기물을 잡지 않고도 좋은 수" 개념 추가**
방치된(공짜로 잡을 수 있는) 상대 기물이 있는데 이 수가 그걸 잡지 않았어도, Stockfish 등급이 나쁘지 않다면(`MEC_GOOD_KINDS` — brilliant/best/only/excellent/good/book) "잡지 않았지만 여전히 좋은 수"임을 새 `declinedCaptureFact`가 짚어준다. 실제 둔 수가 엔진 최선수와 다를 때만 채워지는 `activeMove.best`(analyzeGame이 이미 계산해 둔 값, 새 엔진 호출 없음)가 바로 그 방치된 기물을 잡는 수라면, "잡는 게 최선이었지만"까지 구체적으로 밝힌다(`wasBestCapture`). `mecFacts`가 이제 `kind`·`bestSan`도 받아, 기존 `preexistingTheirs`("잡을 기회예요" — 등급이 나쁠 때, 즉 정말 놓친 경우에만 쓰이도록 우선순위를 양보)보다 먼저 검사한다.

**버그 수정(최종) — 엔진 라인이 여전히 끝까지 타이핑되지 않던 진짜 원인(useEngine/getAnalysisPool 공유 워커의 큐 경쟁)**
v0.2.6~v0.2.7에서 여러 차례 고쳤다고 기록했는데도 재발했다. 원인을 다시 추적한 결과, 이번엔 순전히 이번 버전에서 새로 추가한 기능(`punishLine`·`onlyMoveRefutation`)이 만든 새로운 회귀였다 — 두 기능 모두 `getAnalysisPool`의 `pool[0]`(엔진 라인 스트리밍이 slot `"review-lines"`로 쓰는 바로 그 워커)에 `slot` 없이 요청을 넣고 있었다. `slot` 없는 요청은 그 뒤에 아무리 포지션이 바뀌어도 아무도 superseded(중단)시키지 않아, 유저가 수를 빠르게 넘기면 이미 쓸모없어진 옛 `genPunishLine`/`onlyMoveRefutation` 요청들이 같은 워커의 큐에 계속 쌓이며 진짜 중요한 `"review-lines"` 스트리밍을 뒤로 밀어냈다 — 화면에는 "엔진 라인이 도중에 멈춘다"로 보였지만 실제로는 그 라인의 데이터 자체가 늦게 도착한 것이었다(TypedMoveLine 자체는 받은 데이터까지는 항상 끝까지 타이핑함). `genPunishLine`에 `slot` 인자를 추가해 `punishmentFacts`는 `"review-punish"`, `onlyMoveRefutation`(내부의 `evaluateMulti` 첫 호출과 뒤이은 `genPunishLine` 호출 모두)은 `"review-only"`로 각각 고유 slot을 지정했다 — 이제 포지션이 바뀌면 즉시 이전 요청이 중단되고 큐를 계속 붙잡지 않는다. 부수적으로, `useEngine` 훅 인스턴스와 `getAnalysisPool` 워커 래퍼가 `evaluateMulti`의 인자 개수(전자는 `onProgress` 포함 7개, 후자는 없이 6개)가 서로 다르다는 걸 모르고 그대로 넘기면 slot 문자열이 `onLines` 자리에 들어가 나중에 함수처럼 호출되며 크래시할 수 있었다 — `status` 필드 유무로 두 종류를 구분해 각자 맞는 인자 개수로 호출하는 `callEvaluateMulti` 헬퍼를 새로 만들어 이 위험을 없앴다.

**기능 — 대국 기록이 없는 기간에도 레이팅 변동 그래프가 최근 레이팅을 이어서 보여주도록**
`RatingHistoryChart`는 고른 기간(`period`, 1주/1달/6개월/1년) 안에 대국이 2판 미만이면(선을 그릴 수 없음) "이 기간엔 대국이 부족해요"만 보여주고 그래프 자체를 비웠다 — 최근에 그 시간 규정으로 두지 않은 유저는 기간을 좁힐 때마다 그래프가 통째로 사라져 보였다. `inPeriod`가 부족해도 전체 기록(`allPoints`)에 대국이 하나라도 있으면, 그 마지막 대국 당시 레이팅(=현재 레이팅)으로 기간의 시작(`cutoff`)과 끝(현재 시각) 두 점을 만들어 평평한 선으로 이어 보여준다(`flatFallback`). "전체"(래피드·블리츠·불릿 동시 표시) 모드도 시간 규정별로 동일하게 적용해, 그 시간 규정을 최근에 안 뒀더라도 마지막 값에서 멈춰 있는 것으로 보이게 한다. 실제 대국 데이터가 아니라 값을 유지만 하는 선이라는 걸 구분할 수 있도록 점선(`strokeDasharray`)과 낮춘 불투명도로 그리고, 판수 표기 옆에 "· 최근 레이팅 유지"를 덧붙였다(상승/하락 화살표는 표시하지 않음).

**버그 수정 — "전체" 레이팅 그래프에서 대국이 적은 시간 규정이 수직선 하나처럼 보이던 문제**
`isAll`(시간 규정 "전체") 모드는 x좌표를 실제 시간(`endTime`)으로 잡아 세 시간 규정 선을 같은 축 위에 겹쳐 그리는데, 특정 시간 규정의 대국이 딱 2판뿐이고 그 두 판이 실제로 짧은 시간 안에 몰려 있으면(예: 연달아 둔 블리츠 2판) 전체 기간 폭(예: 1주) 안에서 그 둘 사이 간격이 거의 0에 가까워 화면에는 사실상 수직선 한 줄만 보이고 좌우로 이어지는 선이 전혀 없었다 — 사용자가 "그래프가 그냥 한 줄처럼 보인다"고 지적한 원인. 새 `extendToPeriodEdges(points, cutoff, nowT)`가 각 시리즈의 실제 데이터 앞뒤에 그 시점의 레이팅을 그대로 유지한 수평 구간을 기간의 시작(`cutoff`)·끝(현재 시각)까지 이어 붙여, 대국이 있는 좁은 구간만 오르내리고 나머지는 평평하게 기간 전체 폭을 채우도록 했다(위 `flatFallback`—대국이 아예 없는 시간 규정—과 같은 아이디어를 대국이 있지만 적은 경우에도 확장 적용). 범례의 "첫 레이팅→마지막 레이팅" 표시는 실제 데이터의 값을 그대로 복제해 이어 붙이므로 영향받지 않는다.

**버그 수정 — 퍼즐 창을 열면 평가치 막대가 곧바로 눈에 띄게 움직이도록**
v0.2.7에서 퍼즐 창을 열 때마다 짧게 실제 엔진으로 재평가해 평가치 막대가 살아 움직이는 것처럼 보이게 했는데, 실제로는 거의 안 움직이는 것처럼 보인다는 지적이 있었다. 원인은 퍼즐 포지션 대부분이 좁고 전술적이라 스톡피시가 목표 depth(18)까지 사실상 한 프레임 안에 끝내 버린다는 것 — `onProgress`는 depth마다 호출되지만 화면에 그려지기도 전에 다음 depth 값으로 덮여, 눈에는 "정적 값 → (깜빡할 새도 없이) 최종 값"으로만 보였다. `PuzzleSolver`의 라이브 평가 effect가 들어오는 depth 갱신을 받는 즉시 반영하지 않고 작은 큐에 모아 뒀다가 최소 55ms 간격으로 하나씩 재생하도록 바꿔, 탐색이 실제로는 순식간에 끝나도 depth·평가치가 눈에 보이게 차례로 올라가는 애니메이션이 되도록 했다.

**버그 수정 — 일일 퍼즐 캐러셀 데스크톱 클릭이 여전히 안 되던 진짜 원인(4)**
v0.2.7에서 세 차례(smooth 스크롤 제거, 클릭 임계값 확대, wasDragRef 죽은 코드 수정, setPointerCapture 마우스 전용화) 손봤는데도 데스크톱에서 카드 클릭이 간헐적으로 씹힌다는 신고가 이어졌다. Playwright로 직접 재현해 확인한 진짜 원인은 pointerdown 시점에 곧장 걸던 `setPointerCapture` 자체였다 — 캡처를 건 조상 요소(스크롤 컨테이너)가 그 뒤의 `click`까지 자신을 타깃으로 재지정해 버려, 실제로 누른 자손 `<button>`(카드)의 `onClick`이 아예 호출되지 않았다(순수 클릭이라 전혀 끌지 않았어도 동일하게 발생 — 최소 재현 케이스로 확인). 캡처를 pointerdown 시점이 아니라 실제로 드래그 임계값(8px)을 넘어 "이동"이 확정된 순간에만 걸도록 미뤄, 진짜 드래그에서는 여전히 스크롤이 정상 동작하되 순수 클릭에서는 캡처가 아예 걸리지 않아 브라우저가 클릭을 원래 타깃(카드)에 정상적으로 전달하게 했다.

**성능 — 일일 퍼즐 캐러셀 프리뷰 로딩 시간 단축**
`resolveDailyPuzzle`이 매번 순차로 기다리던 개발자 오버라이드 조회(`fetchDailyPuzzleOverride`)와 테마 배정 조회(`loadDailyThemeRows`)를 서로 무관한 요청임을 확인하고 `Promise.all`로 동시에 쏴 왕복 하나만큼 줄였다. 또한 `DailyPuzzleCarousel`이 활성 항목과 그 양옆(`activeIdx-1/±1`)을 계산할 때 배열 순서상 `activeIdx-1`을 `activeIdx`보다 먼저 큐에 넣고 있었는데, 엔진 워커가 하나뿐이라 요청이 FIFO로 처리되는 탓에 정작 화면 가운데 크게 보이는(가장 먼저 눈에 띄어야 할) 프리뷰가 옆 칸 계산이 끝날 때까지 뒤로 밀렸다 — `activeIdx`를 항상 맨 먼저 큐에 넣도록 순서를 바꿨다.

### OpenChess v0.2.7 — 2026/7/29

**기능/UI — 오늘의 퍼즐 화면 전면 개편(캐러셀)**
사용자가 첨부한 스케치(가운데 날짜 카드가 크고, 좌우 날짜 카드는 작고 어둡게 물러나는 형태)를 바탕으로, 기존의 단일 카드(`TodayPuzzleCard`)+접이식 날짜 목록(`PastDailyPuzzles`) 조합을 하나의 가로 스크롤 캐러셀(`DailyPuzzleCarousel`/`DailyPuzzleCarouselItem`)로 완전히 교체했다. 날짜 범위는 사용자 요청대로 "전체 기간"으로 — `daily_puzzle_themes`(오프닝 테마를 2주 단위로 배정하는 관리 테이블)의 가장 이른 `starts_on`부터 오늘까지 전체를 담되(테마가 아직 하나도 없으면 오늘 하루만), 오늘이 배열 맨 앞(인덱스 0)에 오도록 최신순으로 만든다(`useDailyPuzzleDates`). CSS `scroll-snap-type:x mandatory` + `scroll-snap-align:center`로 스냅시키고, 좌우에 `containerWidth/2 - SLOT_W/2` 만큼의 spacer div를 둬 첫/마지막 항목도 뷰포트 중앙에 스냅될 수 있게 했다(`ResizeObserver`로 컨테이너 폭을 재고 `useLayoutEffect`로 측정해 초기 깜빡임을 최소화). `onScroll`에서 `Math.round(scrollLeft/SLOT_W)`로 활성 인덱스를 구하고, 활성 항목은 `transform:scale(1)`·불투명, 바로 옆(distance 1)은 0.8배·반투명, 그 밖은 0.68배·더 흐리게 렌더한다. 카드를 누르면 활성 항목이면 바로 열리고, 비활성 항목이면 `scrollTo({left:i*SLOT_W, behavior:"smooth"})`로 그 항목을 가운데로 스냅시킨다("스크롤=선택, 클릭(활성 상태)=플레이" 동작).

**성능 — 캐러셀 항목의 지연 계산**
`resolveDailyPuzzle`은 네트워크(Supabase)+실제 엔진 트리 생성(`genPuzzleTree`)이 필요한 무거운 비동기 함수라, 캐러셀에 보이는 모든 날짜를 한꺼번에 계산하면 안 된다는 게 기존 `PastDailyPuzzles` 주석에도 명시돼 있던 제약이었다. 사용자에게 "가운데(선택된) 것만 즉시 계산, 나머지는 스크롤해서 가까워지면 계산"으로 확인받아, 활성 인덱스가 바뀔 때마다 `[activeIdx-1, activeIdx, activeIdx+1]` 범위만 `resolveDailyPuzzleCached`로 계산하도록 했다. 여러 캐러셀 항목(및 알림 팝업의 "오늘의 퍼즐")이 같은 날짜를 중복 계산하지 않도록, 계산 결과(Promise)를 날짜별 모듈 캐시(`dailyPuzzleResolveCache`)에 담아 재사용한다 — 단, 엔진이 아직 준비되지 않아 계산을 시도조차 못 한 경우는 캐시하지 않고 다음 요청 때 다시 시도해, "엔진이 늦게 준비되는 것"과 "그 날짜엔 정말 데이터가 없는 것"을 구분한다. 아직 계산되지 않은(또는 계산 중인) 항목은 기존 힌트 애니메이션에 쓰던 `hintSquarePulse` 키프레임을 재사용한 흐릿한 사각형 placeholder로 표시한다.

**기능 — 캐러셀에 풀이 인원 수 상시 표시**
사용자 요청("일일 퍼즐 UI에는 그날 푼 사람 수를 표시해 달라")에 따라, 이미 존재하던 전역 풀이수 인프라(`puzzles.solves` 컬럼, `puzzle_solve` RPC, `puzzleNo(id)` 해시 기반 매핑, `solveCountText`)를 그대로 재사용해 캐러셀의 모든 카드(활성/비활성 무관, 계산 완료된 것만)에 "OOO명이 풀었습니다!"를 작은 글씨로 표시한다. 새 Supabase 테이블/RPC는 추가하지 않았다 — 일일 퍼즐도 결국 `id`를 가진 일반 퍼즐 객체라 기존 카운터가 그대로 작동한다. 다른 퍼즐 카드의 `solveCountFor`(`PuzzleTab`)와 동일하게, 아직 서버 집계가 반영되기 전이라도 내가 방금 푼 퍼즐은 최소 1명으로 보정한다.

**버그 수정/삭제 — 폐기된 큐레이션 오늘의 퍼즐 완전 제거**
`DAILY_PUZZLES`(Fool's Mate·Scholar's Mate·Légal's Mate·Blackburne Shilling Gambit 4개짜리 하드코딩 배열)와 그걸 날짜 시드로 뽑던 `curatedDailyPuzzleFor`를 완전히 삭제했다. 이 폴백은 v0.2.4에서 리체스 퍼즐 DB 기반 시스템(`resolveDailyPuzzle`)으로 넘어가면서 "엔진 부팅 전/테마 미배정 등 예외 상황에 화면이 비어 보이지 않게" 남겨둔 것이었는데, 실제로는 한 번도 사용자에게 정식으로 배정된 적 없는 임의의 퍼즐이 "오늘의 퍼즐"이라는 이름으로 뜨는 게 부적절하다는 사용자 지적에 따라 제거를 결정했다. `resolveDailyPuzzle`의 4개 폴백 분기(`!theme`, `!pool.length`, `!engine.ready`, `!gen.lines.length`)와 `useDailyPuzzle`의 1개 분기, 총 5개 호출부를 전부 `null` 반환으로 바꿨다 — 호출부(`PuzzleTab`의 캐러셀, 상단 `todayPuzzle`을 쓰는 `openDailyPuzzle`·일일 퀘스트 완료 감지 effect·`DailyPuzzleNoticeModal`)는 이미 대부분 `dailyPuzzle &&` 형태로 안전했지만, `openDailyPuzzle`(무조건 `setPuzzleActive(todayPuzzle)` 호출)과 `DailyPuzzleNoticeModal` 렌더 조건(`puzzleNoticeOpen &&`만 검사)에는 `null` 가드가 없어 두 곳에 `todayPuzzle &&` 조건을 추가했다. `isStaleDaily`(어제 이전 오늘의 퍼즐 복원 방지 가드, v0.2.3)는 `curatedDailyPuzzleFor`와 무관하게 `pz.isDaily`/`pz.date` 필드만 보므로 그대로 유지된다.

**UX — 오늘의 퍼즐 이름을 일반 퍼즐과 통일**
사용자 요청("현재 사용되는 일일 퍼즐의 명칭을 별도로 두지 말고, 그냥 다른 퍼즐들과 동일하게 표시")에 따라 `resolveDailyPuzzle`이 만드는 퍼즐 객체의 `name` 필드를 `opening + " — 오늘의 퍼즐"`(전용 접미사)에서 일반 퍼즐과 동일한 `puzzleName("punish", setupSans, mistakeSan)`(`"<오프닝 이름>, <수 이름>"` 형식)으로 바꿨다. 이 `name`은 퍼즐 풀이 화면(`PuzzleSolver`)에서 그대로 렌더되므로, 오늘의 퍼즐을 열어도 다른 퍼즐과 구분되지 않는 이름이 보인다. 다만 캐러셀 카드 자체의 "오늘의 퍼즐" 섹션 라벨과 `DailyPuzzleNoticeModal`(알림 팝업)의 `puzzle.opening` 표기는 그대로 유지했다 — 사용자의 요청은 퍼즐의 `name`(정체성 표기)에 한정된 것으로 판단했다.

**시스템 — 퍼즐 데이터·유저 풀이 기록 초기화 SQL**
`public.puzzles`·`puzzle_likes`·`puzzle_reposts`·`puzzle_solvers`·`puzzle_solve_events` 5개 테이블을 전부 `truncate`하고(테이블 구조·RLS·함수는 그대로 유지), `public.user_progress.progress`(jsonb) 안의 `solved`(푼 퍼즐 id 목록)·`lineSolves`(퍼즐별 풀어낸 라인 태그) 두 필드만 `jsonb_build_object`로 덮어써 초기화하는 스크립트를 사용자에게 채팅으로 전달했다(사용자 요청에 따라 저장소 파일로는 남기지 않음). XP·코인·칭호·퀘스트 진행도·chat_messages(퍼즐 공유 카드 포함)·친구 관계 등 풀이 기록과 무관한 나머지 진행 상황은 건드리지 않는다 — Supabase 대시보드 SQL Editor에서 개발자가 직접 실행해야 반영되는 일회성 스크립트.

**UI/UX — 캐러셀 마우스 드래그 스크롤 지원, 밝기 조정**
데스크톱에서 `overflow-x:auto`의 브라우저 기본 스크롤만으로는 마우스로 좌우 스크롤이 거의 안 된다는 지적(엔진 라인·기보 줄과 동일한 기존 이슈, `DRAG_SCROLL_MULT`)에 따라, `DailyPuzzleCarousel`에도 같은 포인터 이벤트 기반 드래그 스크롤 패턴을 추가했다. `onPointerDown`/`onPointerMove`로 직접 `scrollLeft`를 옮기고, `onPointerUp`/`onPointerCancel`에서 드래그였다면(`moved`) 가장 가까운 칸으로 `scrollToIndex`를 호출해 스냅을 보정한다(CSS `scroll-snap`은 네이티브 스크롤에만 적용되고 JS로 직접 옮긴 `scrollLeft`에는 관여하지 않으므로). `onClickCapture`로 드래그 직후의 의도치 않은 클릭(카드 열림/재스크롤)도 막는다. 별개로, 비활성 카드의 `opacity`가 어두운 카드 배경 위에서 너무 낮아(거리 1칸: .5, 그 밖: .28) 거의 안 보인다는 지적에 따라 값을 각각 .78/.58로 올렸다.

**버그 수정 — 퍼즐 모식도에서 마지막 수가 잘려 보이던 문제**
`PuzzleSchematic`에서 리프(라인의 마지막 수) 노드는 오른쪽에 "라인 N"·해결 체크 배지(개발자 모드에서는 추가·삭제 버튼까지)가 같은 줄에 나란히 붙는데, 그 바깥 wrapper `<div>`의 폭이 `boxW`(104px, 개발자 모드 210px)로 고정돼 있고 정작 SAN 라벨을 담은 버튼이 `flex:1`+`minWidth:0`으로 그 폭 안에 욱여넣어지는 구조였다 — 배지가 넓을수록 버튼이 더 심하게 눌려, 정작 라인이 실제로 끝나는(=정보가 가장 중요한) 마지막 수의 라벨이 가장 심하게 말줄임(ellipsis)으로 잘렸다. wrapper를 `width:"max-content", minWidth:boxW`로, 버튼은 `flexShrink:0, minWidth:boxW`(flex:1 제거)로 바꿔 배지가 버튼을 짓누르지 않고 자연스럽게 옆으로 이어지도록 했다. 전체 캔버스 폭(`width` useMemo) 오른쪽 여백도 40→120px로 늘려, 넓어진 리프 노드가 캔버스 오른쪽 경계에서 잘리지 않게 했다.

**기능 — 퍼즐 창을 열면 평가치 막대가 실시간으로 움직이도록**
`PuzzleSolver`의 평가치 막대(`EvalBar`)는 v0.2.6부터 매번 재평가할 때의 미묘한 값 흔들림을 없애려 생성 시 미리 계산해 둔 `curNode.ev`(모든 유저 공통값)만 정적으로 표시하고 있었는데, 그 결과 창을 열어도 막대가 전혀 "살아 움직이지" 않아 죽어 있는 것처럼 보인다는 지적을 받았다. `curSans`(현재 포지션)가 바뀔 때마다 실제 `engine`으로 짧게(depth 18, movetime 900ms) 다시 평가하되(`engine.evaluate` + `onProgress` 콜백 — ReviewPage 엔진 라인과 같은 단일PV 진행 갱신 방식, `posEvalToWhite`로 백 관점 변환), 그 탐색이 진행되는 동안(`liveDepth != null`)만 `EvalBar`에 실시간 값과 depth(3-dot "탐색 중" 인디케이터)를 넘겨 막대가 눈에 보이게 움직이도록 했다. 탐색이 끝나면(또는 다음 포지션으로 넘어가면) `liveDepth`를 다시 `null`로 되돌려 곧바로 정적인 `curNode.ev` 표시로 돌아간다 — v0.2.6에서 고친 "매번 다시 평가해 값이 미묘하게 흔들리는" 문제는 재발하지 않으면서(최종 정지 값은 항상 `curNode.ev`), 창을 열자마자 잠깐 실제로 탐색하는 모습만 보여준다.

**버그 수정 — 존재하지 않는 퍼즐 번호를 채팅 명령어로 보낼 수 있던 문제**
채팅의 `/puzzle 000000`(또는 `/puzzle -num 000000`) 명령어가 번호 형식만 확인하고 실제로 그 번호의 퍼즐이 존재하는지는 검증하지 않은 채 `puzzleShareSend`를 그대로 호출했다 — 없는 번호를 보내면 받는 쪽 채팅에 내용이 비어 있거나 깨진 퍼즐 공유 카드만 남았다. `ChatPanel.send()`의 명령어 분기에서 `puzzleShareSend` 호출 전에 `puzzleFetch(no)`로 서버에 그 번호의 퍼즐 데이터가 실제로 있는지 먼저 확인하도록 바꿨다(이미 화면에 로드된 적 있는 번호면 기존 `puzzlePreviews` 캐시를 재사용해 중복 조회를 피한다). 데이터가 없으면 전송을 막고 새로 추가한 `cmdError` state로 "#000000 번호의 퍼즐을 찾을 수 없어 보낼 수 없어요."를 입력창 위 명령어 안내 블록 아래에 보여주며, 텍스트를 다시 입력하면 에러가 사라진다.

**기능 — 오늘의 퍼즐도 puzzles 테이블에 저장해 번호 공유·풀기 지원**
사용자 요청("일일 퍼즐도 puzzles 테이블에 똑같이 고유번호로 저장해서 공유, 숫자로 풀기 기능을 활용할 수 있게 해줘, 다른 퍼즐들과 구분을 아예 하지 말 것")에 따라, 일반 퍼즐이 처음 만들어질 때 `onSavePuzzle`이 `puzzleShare(pz)`로 서버 `puzzles` 테이블에 업로드하는 것과 동일한 경로를 오늘의 퍼즐에도 그대로 태웠다. 별도 분기 없이 `resolveDailyPuzzleCached`(캐러셀·`useDailyPuzzle` 둘 다 이 함수를 통해서만 오늘의 퍼즐을 계산함) 안에서 `resolveDailyPuzzle`이 성공적으로 계산을 끝낸 직후 그 결과 객체를 그대로 `puzzleShare`에 넘긴다 — 일반 퍼즐과 완전히 같은 `puzzleNo(id)` 해시·같은 업로드 함수(같은 upsert 병합 로직 포함)를 쓰므로 채팅 `/puzzle 000000` 공유, 퍼즐 탭 "번호로 풀기" 입력창이 오늘의 퍼즐도 다른 퍼즐과 구분 없이 그대로 찾아낸다. 같은 날짜는 모든 유저에게 결정적으로 같은 id·데이터로 계산되므로, 여러 유저가 각자 화면에서 오늘의 퍼즐을 열어 중복으로 업로드해도 upsert가 그대로 병합해 문제없다.

**버그 수정 — 일일 퍼즐 캐러셀에서 카드를 눌러도 반응이 없던 문제**
바로 앞서 추가한 마우스 드래그 스크롤 구현에 두 가지 원인이 겹쳐 있었다. (1) 컨테이너에 `scrollBehavior:"smooth"`가 걸려 있어, 드래그 중 `el.scrollLeft` 대입마다(손 떨림 수준의 1px만 움직여도) 애니메이션이 새로 시작돼 손을 떼는 순간까지도 카드가 커서 아래에서 계속 미끄러졌다 — pointerdown·pointerup 시점의 실제 타깃이 어긋나 브라우저가 `click`을 아예 합성하지 않는 경우가 있었다. `scrollBehavior:"smooth"`는 컨테이너 CSS에서 빼고, `scrollToIndex`의 명시적 `scrollTo({behavior:"smooth"})` 호출에만 남겨, 드래그 중 `scrollLeft` 대입은 항상 즉시 반영되도록 했다. 또한 클릭 임계값(3px)이 실제 손 떨림보다 작아 진짜 클릭도 드래그로 오판하기 쉬웠던 것을 8px로 늘리고, 임계값을 넘기 전까지는 `scrollLeft` 자체를 건드리지 않도록 했다(그전엔 임계값 판정과 무관하게 매 픽셀 스크롤이 적용됐음). (2) 더 근본적인 버그로, "드래그였으면 뒤이은 클릭을 막는다"는 로직이 `pointerup` 핸들러(`endDrag`) 안에서 `dragRef.current`를 곧장 `null`로 비우고 있었는데, `click` 이벤트는 항상 `pointerup`보다 나중에 발생하므로 `onClickCapture`가 실행되는 시점엔 이미 `dragRef.current`가 `null`이라 "방금 드래그였는지" 판정이 구조적으로 항상 거짓이었다(=클릭 차단이 원래부터 죽은 코드) — 다행히 원인 (1) 때문에 실제 드래그 뒤엔 애초에 click 자체가 잘 안 일어나 이 버그가 가려져 있었지만, (1)을 고치고 나면 진짜 드래그 뒤에도 스퓨리어스 클릭이 새어나갈 뻔했다. `pointerup`이 별도 ref(`wasDragRef`)에 "방금 드래그였는지" 결과를 남겨 두고, `onClickCapture`가 그 값을 읽어 소비(한 번 쓰고 리셋)하도록 바꿔 클릭 차단 로직이 실제로 의도대로 동작하게 했다.

**버그 수정 — 퍼즐 힌트 단계별 애니메이션이 서로 겹쳐 보이던 문제**
`hintTo`(1단계, 도착 칸)·`hintFrom`(2단계, 기물 흔들림)·`hintPathSq`(3단계, 경로 반짝임) 세 prop이 각각 `hintLevel >= 1`/`>= 2`/`>= 3` 조건으로 넘겨지고 있어, 3단계에 도달하면 세 애니메이션이 전부 동시에 켜진 채 화면에 겹쳐 보였다. 각 단계가 정확히 그 단계에서만(그리고 3단계는 명시적으로 기물 흔들림+경로 반짝임의 조합으로) 켜지도록 조건을 `=== 1`/`=== 2 || === 3`/`=== 3`로 바꿔, 1단계는 도착 칸만, 2단계는 기물 흔들림만, 3단계는 기물 흔들림과 경로 반짝임만 보이도록(단독 도착 칸 표시는 3단계에서 끔 — 경로의 마지막 칸이 도착 칸을 대신 비춤) 정리했다. 별개로 힌트 칸을 채우던 `HINT_GOLD_GRADIENT`가 `radial-gradient(circle at 50% 50%, ...)`라 칸 모서리가 15% 농도까지 옅어져 정사각형 칸 안에 둥근 빛 덩어리 하나만 뜬 것처럼(원 모양) 보였다는 지적에 따라, 칸 전체를 고르게 채우는 진한 대각선 그라데이션으로 바꾸고 외곽 발광 `box-shadow`(`HINT_GOLD_GLOW`)를 더해 훨씬 강하고 또렷한 금빛으로 보이도록 했다.

**버그 수정 — 엔진 라인이 여전히 끝까지 작성되지 않던 문제(진짜 원인, 2)**
v0.2.6(`af34f69`)에서 "엔진 추천 수 줄이 빠른 수 진행 중 섞여 멈추던 진짜 원인(큐 경합)"을 고쳤다고 기록했지만, 사용자가 이후에도 재현된다고 보고했다. 원인을 다시 조사한 결과 그 수정은 `useEngine`의 자체 큐(`supersede`, 782행)에만 적용됐고, 구조가 거의 동일한 **또 다른 엔진 큐**인 `bootAnalysisWorker`(2349행, `getAnalysisPool`/`/review`의 엔진 라인이 쓰는 독립 워커 풀)의 `supersede`(2362행)는 고치기 전의 버그 있는 버전 그대로 남아 있었다 — 같은 slot의 새 요청이 들어오면 이전 탐색에 "stop"을 보내자마자 `queue.shift(); running = false;`로 곧장 자리를 비워, 진짜 `bestmove`가 도착하기도 전에 다음 요청의 "position"/"go"를 같은 워커에 보내 버렸다. 그 사이 뒤늦게 오는 이전 탐색의 `info` 줄이 `handleLine`에서 "지금 큐 맨 앞"(=새 요청)의 MultiPV 버퍼에 그대로 섞여, 전혀 다른 포지션의 PV가 새 줄에 합쳐지고 `pvUciToSans`가 그 지점에서 실패해 조용히 끊긴 `sans` 배열을 반환했다 — 그 결과 `TypedMoveLine`의 타이핑 애니메이션은 (손상된) 목표 문자열까지는 정확히 다 타이핑하고 멈추므로, 사용자에게는 "엔진 라인이 끝까지 안 써진다"로 보였다. `useEngine.supersede`와 정확히 같은 수정(자리를 곧장 비우지 않고 큐에 남겨 둬, 진짜 `bestmove`가 와야 정상적인 bestmove 분기가 shift·pump하게 함)을 이식했다. 처음엔 `useEngine`에 있는 것처럼 `swallowBest++`도 함께 옮겼는데, `useEngine` 쪽은 이 자리를 shift하지 않으므로 그 실제 bestmove가 정상 분기로 자연스럽게 소비돼 애초에 `swallowBest`를 건드리지 않는다는 걸 재확인하고 제거했다 — 만약 그대로 뒀다면 빠르게 두 번 이상 슈퍼시드될 때(예: 세 포지션을 연달아 빠르게 넘길 때) `swallowBest`가 실제 필요한 값보다 더 커져, 진짜 도착한 `bestmove`를 도리어 무시하며 큐가 `running=true`인 채로 영영 멈추는 새로운 회귀를 만들 뻔했다. 영향 범위는 `getAnalysisPool`을 `slot`과 함께 쓰는 유일한 호출부인 `/review`의 엔진 라인(`"review-lines"`) — 빠르게 수를 넘기거나 자유 탐색으로 포지션을 자주 바꿀 때 재현됐다.

**UI — 일일 퍼즐 알림 창 재디자인**
사용자가 그려 준 스케치(마스코트가 위에서 내려다보는 펼친 달력 다이어리 — 왼쪽 페이지는 날짜+달력 모식 격자, 오른쪽 페이지는 오늘의 오프닝·수·풀이수·풀기 버튼)를 그대로 `DailyPuzzleNoticeModal`에 옮겼다. 기존의 보드 미리보기(`AnimatedMove`) 중심 레이아웃을 걷어내고, 왼쪽엔 `todayStr()` 기반 날짜 라벨 + 순수 장식용 4×4 CSS 그리드(달력 모식), 오른쪽엔 "일일 퍼즐" 라벨(종 아이콘)·`puzzle.name`·`solveCountText`(신규로 `solveCount` prop 추가 — 호출부에서 `puzzleNo(todayPuzzle.id)`로 기존 `solveCounts` 맵을 조회해 다른 퍼즐 카드와 동일한 "최소 1명 보정" 규칙까지 그대로 적용)·풀기 버튼을 배치했다. 두 "페이지" 사이는 점선 세로 구분선으로 나눴고, 마스코트(`kokoa`/`wink`)는 카드 왼쪽 위에 절반 겹쳐 떠 있도록 `position:absolute`로 올렸다. 사용자 요청대로 풀기 버튼은 기존 금색 그라데이션(`linear-gradient(180deg,T.brass,#A8842F)`)을 그대로 유지했다. "오늘 하루 다시 보지 않기" 체크박스와 퀘스트 미완료 안내 문구는 카드 하단에 그대로 남겼다.

**버그 수정 — 일일 퍼즐 캐러셀 클릭이 여전히 안 되던 문제(진짜 원인, 3)**
앞서 두 차례(scrollBehavior:smooth 제거, 클릭 임계값 확대, wasDragRef로 죽은 코드 수정) 손봤는데도 사용자가 재현했다. 남은 원인은 `setPointerCapture` 자체였다 — 이 드래그 스크롤은 애초에 "컴퓨터 환경에서 마우스로"라는 요청에서 시작됐는데, 구현이 포인터 타입을 구분하지 않고 터치에도 똑같이 걸려 있었다. 일부 모바일 브라우저는 조상 요소가 포인터를 캡처한 상태로 pointerup을 맞으면 그 직후의 click 합성 자체를 건너뛰는 경우가 있어(이동이 전혀 없는 순수한 탭에서도), 실제 사용 환경(모바일)에서는 클릭이 원천적으로 죽어 있었던 것으로 보인다. `onPointerDown`/`onPointerMove`에 `e.pointerType !== "mouse"` 가드를 추가해 커스텀 드래그·포인터 캡처를 마우스 전용으로 좁히고, 컨테이너의 `touchAction: "pan-y"`(가로는 커스텀 드래그가 처리한다는 전제로 막아 뒀던 값)도 지정하지 않도록(기본값 `auto`) 되돌려 터치는 브라우저 기본 스크롤+click을 그대로 쓰도록 했다(스냅은 `scroll-snap-type`이 계속 담당). 원래 요청 범위(데스크톱 마우스)와도 정확히 일치하는 수정이다.

### OpenChess v0.2.6 — 2026/7/28

**버그 수정 — 게임 리뷰(/review) 페이지 흰 화면**
사용자가 리뷰 페이지에 들어가면 흰 화면만 나온다고 제보해 재현·조사함. 원인은 `ReviewPage`의 `playFree`(`useCallback`) 의존성 배열이 `activeMove`·`engineLines`를 참조하는데, 이 두 값이 실제로는 `playFree`보다 한참 아래(각각 `exploreMove` 채점 effect 옆, 엔진 라인 effect 옆)에서 `const`로 선언되고 있었다는 것 — JS의 TDZ(temporal dead zone) 규칙상 `const`/`let` 선언 이전에 그 식별자를 참조하면(같은 스코프 안이면 실행 순서상 "아직 실행 전"이어도) `ReferenceError`가 발생한다. `useCallback`의 두 번째 인자(의존성 배열)는 함수 몸체와 달리 렌더 시점에 즉시 평가되는 일반 표현식이라, `ReviewPage`가 마운트되어 게임 데이터가 있기만 하면 렌더할 때마다 예외 없이 `Cannot access 'activeMove'/'engineLines' before initialization`이 던져졌다 — 에러 바운더리가 없어 리액트 트리 전체가 언마운트되며 흰 화면으로 보였다. 이 버그는 이번 v0.2.5 세션이 아니라 그보다 앞선 `b0cd969`(자유 탐색 최선수 오판정 수정) 커밋에서 들어간 것으로, 게임 리뷰를 여는 모든 경로(검색·친구·설정 탭의 chess.com 프로필, 집중 학습의 마스터 대국 등)에서 항상 재현되는 문제였다. `exploreMove`(state)·`activeMove`·`engineLines`(state) 세 선언을 각각 처음 참조되는 지점(`playFree` 정의 이전)으로 끌어올려 고쳤다. 더미 대국 데이터를 주입해 로컬에서 재현한 뒤, 수정 전엔 확실히 크래시하고 수정 후엔 리뷰 요약 화면·보드·코치 카드·엔진 라인까지 정상 렌더되는 것을 Playwright로 확인했다.

**버그 수정 — 탁월한 수(brilliant) 판정 추가 보정**
사용자가 제공한 3개 실제 대국을 기준으로 `isSacrifice` 계열 판정을 추가로 다듬었다. (1) 24.Re1처럼 룩을 상대 대형 뒤 배후로 들여보내 메이트 그물을 짜는 패턴이 탁월로 잡히지 않던 원인을 분석해, 새 `hasSaferSquare` 헬퍼로 "지금 방치된 것처럼 보이는 기물이 사실은 더 안전한 칸으로 피할 수 있어 위협이 허상인" 경우를 가려내 같은 패턴의 수들도 탁월로 인정되게 했다. (2) 13...Qxc3처럼 잡히는 기물(비숍)이 사실은 안전한 경우 — 그 기물을 잡으면 룩이 퀸을 스큐어로 노리게 되는 등 — 을 탁월로 오판하지 않도록, 기물을 잡은 뒤 정말로 다음 수에 즉시 그 점수를 돌려받지 못하는지까지 확인하는 검증을 강화했다. (3) 16...Nf3+처럼 먼저 기물을 희생한 뒤 그 대가로 점수를 회복해 가는 콤비네이션 수순에서, 첫 희생 수 이후 연달아 나오는 "점수 회복용" 후속 수들까지 탁월로 중복 표시되지 않도록 새 `ownPriorMoveWasSacrifice` 헬퍼로 직전 자신의 수가 이미 희생 수였는지를 추적해 `classifyMoveKind`·`analyzeGame`과 3곳의 인라인 중복 판정 블록 전부에 가드를 추가했다.

**버그 수정 — 집중 학습 미니보드 캐슬링 애니메이션**
`AnimatedMove`가 캐슬링 수를 재생할 때 `sanSrc()`가 킹의 좌표만 반환해 킹만 슬라이드하고 룩은 제자리에 얼어붙어 있던 문제를 고쳤다. 캐슬링 여부(`geo.castle`)에 따라 룩의 시작/도착 좌표(`rookFr`/`rookTo`)를 함께 계산해 킹과 룩이 동시에 각자의 목적지로 슬라이드하도록 했다.

**버그 수정 — 엔진 라인 타이핑 미완료·자동 스크롤 방향**
`TypedMoveLine`이 라인마다 `setTimeout`을 의존성 배열과 함께 재귀 호출하는 방식이라 리렌더 타이밍에 따라 중간에 멈춰버리는 경우가 있었다 — 포지션 키(`posKey`)당 단일 `setInterval`로 재작성해 타이핑이 끝까지 안정적으로 진행되게 했다. 또한 `EngineLineRow`가 라인이 길어질 때마다 스크롤을 마지막 수(오른쪽 끝)로 자동으로 밀어붙이던 `followTail`/`followRef` 로직을 완전히 제거해, 다음 수가 항상 왼쪽에 붙어 보이도록 정리했다.

**버그 수정 — 엔진 라인이 여전히 중간에 끊기던 진짜 원인(useEngine 큐 경합)**
위 수정 이후에도 사용자가 빠르게 수를 두거나 포지션을 넘기면 특정 줄의 수순이 특정 지점에서 영구히 멈춰 보이는 현상이 재발했다. `chromium`을 헤드리스로 띄워 실제 재현해 보니, 멈춘 줄의 표시 텍스트가 지금 포지션과 무관한 수순(예: 1.e4 e5로 이미 넘어갔는데 1...c5로 시작하는 PV)이었다 — `chess.js`로 합법성을 검사해 보니 실제로 이전 포지션의 PV 데이터가 새 포지션의 MultiPV 결과에 섞여 들어가 있었다(`pvUciToSans`가 색이 어긋난 UCI 수를 만나 SAN 변환에 실패하고 그 지점에서 조용히 멈춘 것). 근본 원인은 `useEngine`의 `supersede`(같은 slot의 새 요청이 오면 실행 중이던 이전 요청을 "stop"으로 중단시키는 함수)가 엔진의 실제 `bestmove` 응답을 기다리지 않고 곧장 큐에서 그 작업을 빼고 `running`을 `false`로 되돌려, 바로 이어지는 다음 요청의 `position`/`go`를 같은 엔진 워커에 즉시 보냈다는 점이었다 — 그 사이 도착하는 이전 탐색의 뒤늦은 `info` 줄들이 `handleLine`에서 "지금 큐 맨 앞"(=이미 새로 들어온 다음 요청)의 결과 버퍼로 잘못 합쳐졌다. `supersede`가 중단시킨 작업을 곧장 빼지 않고 그대로 큐에 남겨 두어, 실제 `bestmove`가 도착해 자연스럽게 큐가 흘러갈 때까지는 다음 요청을 엔진에 보내지 않도록 고쳤다 — 그동안 들어오는 뒤늦은 `info` 줄은 이미 `settle`된 자리로만 흘러가(그 자리의 콜백은 자신의 포지션 키 검사에서 걸러져 무시됨) 다음 요청의 결과와 섞이지 않는다. 헤드리스 브라우저로 초를 다투는 빠르기로 수십 수를 연달아 두며 재현·검증했다.

**UX — 드래그 스크롤 감도 상향**
도감(오프닝 모식도), 현재 대국 기보, 엔진 라인에서 포인터 드래그로 가로 스크롤할 때 체감 이동량이 실제 손 이동량보다 작게 느껴지던 것을, 공용 상수 `DRAG_SCROLL_MULT = 2.2`를 도입해 `EngineLineRow`·`SequenceBar`·`OpeningSchematic` 세 곳 모두에 동일하게 적용했다.

**UI — 프로필 카드·집중 학습 폰트·레이아웃 통일**
"자주 두는 수" 정보에 Playfair Display 폰트를 적용했다(`TopOpeningsAnimated`, 기존 `TopWhiteMovesAnimated`에서 이름도 변경). `MyProfileCard`의 OpenChess 자체 정보와 chess.com 통계 사이에 점선 구분선과 "Chess.com 통계" 아이콘+텍스트 헤더를 새로 넣었다. 집중 학습(`FocusPanel`)의 chess.com 통계 카드 중 최근 대국 목록 UI를 프로필 카드와 동일한 스타일로 통일하고, 집중 학습에서 보여주는 모든 수 표기에도 Playfair Display를 적용했다.

**기능 — 프로필 오프닝·전적 표기 개선**
가장 많이 둔 오프닝을 수순 표기(예: "1.e4 e5") 대신 오프닝 이름으로 보여주고, 그 아래에 흑 레퍼토리 기준 오프닝도 함께 보여주도록 `TopOpeningsAnimated`를 확장했다. 승/패/무 표기를 승리/패배/무승부로 풀어 쓰고, 레이팅 변동을 결과에 바로 붙여 "승리(+9)"처럼 한 줄로 보여준다. 오프닝 이름이 바뀔 때마다 카드 폭이 흔들리던 문제를 폭 고정으로 해결했고, 대국 날짜는 시간 규정 옆 괄호 안으로 옮겼다. "그 외 변형" 라벨은 "Side Line"으로 이름을 바꿨다.

**버그 수정 — 모바일 말풍선·카드 화면 잘림**
수 아이콘 설명 팝오버가 화면 가장자리 근처 수에서는 화면 밖으로 잘려 나가던 문제를, 화면 중앙까지의 거리를 기준으로 안전한 방향을 판정하는 공용 함수 `safeAreaDx`를 새로 만들어 해결했다(`CircleBadge`). 기존의 작은 툴팁 방식 코드는 완전히 제거하고, 같은 원리를 채팅 롱프레스 삭제 메뉴와 알림 카드(`NotificationBell`)에도 동일하게 적용했다.

**기능 — 퍼즐 화면 대규모 개편**
해결한 퍼즐도 오프닝 테마별 점선 상자로 묶어 보여주도록 `PuzzleTab`의 그룹핑을 `groupByTopOpening` 재사용으로 통일했다. 별(`LineStars`) 크기·간격을 줄이고, 좋아요·재게시·공유 순서로 소셜 아이콘을 재배치하며 크기를 키웠다. "라인 a/b 해결" 표기를 "a/b"로 축약했다. 코치 말풍선은 대기 상태에서 새 `summarizePosition` 함수로 현재 포지션을 요약해 보여준다. PGN은 별도의 스크롤 가능한 한 줄짜리 박스(`PuzzlePgnBox`)로 분리했고, 보드 아래 텍스트 프롬프트 자리는 평가치 바(`EvalBar`)로 대체했다. 퍼즐 모식도(`PuzzleSchematic`)에서 "라인 n" 라벨과 체크마크를 마지막 수 블록 오른쪽으로 옮겼다. `genPuzzleTree`/`puzzleCandidatesAt`의 평가 depth/movetime을 게임 리뷰와 동일한 `REVIEW_DEPTH`/`REVIEW_MOVETIME_MS`로 맞춰 라인 생성 시점과 재평가 시점의 평가치가 어긋나던 문제를 해결했다. 힌트 체계를 3단계 시각 힌트(칸 반짝임 → 기물 흔들림 → 이동 경로 애니메이션)로 완전히 새로 짰다 — 나이트는 고정된 L자 분해로 경로를 표시하는 `hintPathSquares` 헬퍼를 새로 만들고, `Board` 컴포넌트에 `hintTo`/`hintFrom`/`hintPathSq` prop을 추가했으며, 새 CSS 키프레임(`hintSquarePulse`/`hintSquarePop`)을 도입하고 힌트 버튼 UI도 단계에 맞춰 갱신했다.

**기능 — 채팅 기능 확장**
연속으로 온 상대 메시지 묶음의 첫 번째 말풍선 왼쪽에 상대 프로필 사진을 보여주고(클릭 시 프로필로 이동), 메시지 안의 "@사용자명" 멘션은 굵게 강조되며 클릭하면 해당 프로필로 바로 이동한다(`MENTION_RX`·`renderMentionText`·`ChatUserProfileModal`). 입력 중 표시 말풍선은 기존 3점 바운스 애니메이션을 유지한 채 투명도 50%로 낮췄다. 입력창이 "/"로 시작하면 슬래시 명령어 자동완성 블록이 뜨는데, 이번 버전엔 `/puzzle -num 000000` 또는 `/puzzle 000000` 형식으로 해당 번호의 퍼즐을 공유하는 명령어 하나만 담았다.

**기능 — chess.com 통계 흑/백 분리 및 레이팅 변동 그래프**
시간 규정(전체/래피드/블리츠/불릿) 필터에 더해 흑/백 색 필터를 추가했다 — 시간 규정 선택 박스를 조금 줄이고 같은 줄 오른쪽에 색 필터를 나란히 배치했다. "전체 기간 전적"과 "최근 대국" 사이에 지금 걸린 필터가 적용된 대국들을 시간순으로 이은 기간별 레이팅 변동 선 그래프(`RatingHistoryChart`, 첫 레이팅→마지막 레이팅과 최저/최고를 함께 표시)를 새로 추가했다.

**UI — "자주 두는 첫 수" 레이아웃 개편, 통계 헤더 문구 단순화, 레이팅 그래프 보강**
사용자가 그린 스케치를 따라 프로필의 "자주 두는 첫 수"(`FirstMovesDisplay`)를 세로 목록에서 "백"·"흑" 두 박스를 나란히 두는 레이아웃으로 바꿨다 — 왼쪽 박스엔 자주 두는 첫 수(백)를 큰 글씨로, 오른쪽 박스엔 백의 각 첫 수(`e4`/`d4`/`c4`/`Nf3`, 프로필 편집기 입력 순서 그대로)에 대한 흑의 응수를 "1.e4 e5" 형태의 한 줄씩으로 묶어 보여준다. 프로필 카드의 "Chess.com 통계" 헤더 문구를 로고 아이콘이 이미 브랜드를 나타내므로 "통계"로 줄였다. `RatingHistoryChart`에 x축·y축 선과 3단(최저/중간/최고) 그리드+눈금 라벨, 선 아래 반투명 영역 채우기(선 색과 동일 색, opacity 0.16)를 추가하고, 그래프 위에 최근 1주/1달/6개월/1년 중 고를 수 있는 기간 필터(기존 시간 규정·흑백 필터와는 별개 축)를 얹었다 — 고른 기간에 대국이 2판 미만이면 그래프 대신 안내 문구를 보여준다.

**버그 수정 — 레이팅 그래프 시간 규정 혼합, 엔진 라인 MultiPV 순위 변동 시 재타이핑**
사용자가 보낸 스크린 레코딩으로 두 가지를 더 발견·수정했다. (1) `RatingHistoryChart`가 위 시간 규정 필터가 "전체"일 때 래피드·블리츠·불릿 대국을 시간순으로 그냥 한 선에 뒤섞어 그리고 있었다 — 세 시간 규정은 레이팅 체계 자체가 달라(값 크기가 서로 다름) 뒤섞은 선은 실제 실력 변화가 아니라 그날 어떤 시간 규정을 뒀는지에 따라 요동치는 사실상 무의미한 그래프였다. `timeFilter` prop을 새로 받아, "전체"일 때는 세 시간 규정(대국 2판 이상 있는 것만)을 각자 다른 색 선으로 겹쳐 그리고 범례를 달며, 특정 시간 규정으로 좁혀져 있을 때는 기존처럼 그 하나만 영역 채우기와 함께 보여준다. (2) 엔진 라인이 여전히 "끝까지 안 써진다"는 재보고를 헤드리스 브라우저로 재현한 결과, 같은 포지션에 머물러 있어도(포지션 자체는 안 바뀜) MultiPV 순위가 흔들릴 때마다(탐색 초반 흔함) `EngineLineRow`/`TypedMoveLine`을 그 줄의 첫 수로 키를 잡고 있어 순위가 바뀔 때마다 그 자리 전체가 언마운트·재마운트되며 타이핑이 처음(빈칸)부터 다시 시작되길 반복하는 게 진짜 원인이었다(직전에 고친 `useEngine` 큐 경합과는 별개의, 새로 발견한 문제). 키를 그 줄의 내용이 아니라 화면상 자리(슬롯 번호, 포지션이 바뀌지 않는 한 항상 같음)로 바꿔, 순위가 바뀌어 다른 수순이 들어와도 같은 컴포넌트 인스턴스가 유지되며 타이핑 진행 상태가 리셋되지 않도록 했다. 사용자가 보낸 영상과 똑같은 포지션(1.e4 c5 2.Nf3 Nc6)에서 순위가 여러 번 뒤바뀌는 동안 헤드리스 브라우저로 재현·검증함.

**버그 수정/UI — 레이팅 그래프 색 필터 무관화, 오프닝 표시 개편, 자주 두는 첫 수 흑 박스 2열화**
레이팅은 흑/백 어느 색으로 뒀든 하나로 합산 적용되므로 색 필터와 무관해야 하는데, `RatingHistoryChart`가 `AccountChessStats`의 색 필터가 적용된 `games`를 그대로 받아 색을 바꾸면 그래프도 달라지는 버그가 있었다 — 시간 규정만 적용하고 색 필터는 뺀 `gamesForRating`을 별도로 계산해 그래프에는 항상 이것만 넘기도록 분리했다. `TopOpeningsAnimated`(가장 많이 둔 오프닝)에 예전 버전(`TopWhiteMovesAnimated`)에 있던 "백 n수"/"흑 n수" 배지를 되살려 지금 몇 번째로 많이 둔 오프닝을 보여주는 중인지 표시하고, 흑 쪽의 기존 "흑 오프닝 레파토리" 텍스트 라벨은 이 배지로 완전히 대체했다. 오프닝 이름 글자 크기를 16→13px로 줄여 긴 이름(예: "Sicilian Defense: Najdorf Variation, English Attack")도 2줄 안에서 잘리지 않고 온전히 들어가게 했다. `FirstMovesDisplay`의 흑 박스도 개편했다 — 백의 각 첫 수(e4/d4/c4/Nf3)에 대한 흑의 응수를 한 줄에 하나씩 세로로 4줄 늘어놓던 것을, 한 줄에 두 항목씩 2x2 그리드로 배치해 2줄로 줄이고 박스 자체의 폭도 함께 줄였다.

**버그 수정 — 다음 수 블록과 현재 수 블록의 수 키워드(TOP LEVEL 등) 불일치**
다음 수 블록(`MoveTile`)은 `useMergedMoves`가 만든, 마스터/Lichess 채택률이 채워진 "지금 포지션의 다음 수" 객체로 `deriveKeywords`를 불러 키워드를 표시하는데, 그 수를 실제로 두면 현재 수 블록은 한 칸 전 포지션을 `snapNode`(채택률 필드가 없는 정적 북 데이터)로 다시 조회해 같은 함수를 불러 전혀 다른 입력으로 다른 결과를 냈다. 이미 있던 `pinnedKindRef`(블록에 뜬 등급을 pin해 두는) 패턴을 그대로 따라 `pinnedKwRef`를 추가 — 블록에 실제로 떴던 키워드 배열(순서까지)을 "포지션 key + 수"로 pin해 두고, 현재 수 블록은 그 값을 그대로 재사용해 두 곳이 절대 어긋나지 않는다.

**기능 — 집중학습 chess.com 통계 필터, 검색/리뷰 버튼 크기 통일**
집중학습(`FocusPanel`)의 "이 수가 두어진 내 대국" 목록에도 프로필 카드와 동일한 시간 규정(전체/래피드/블리츠/불릿)·진영(전체/백/흑) 선택 박스를 추가했다. 이 목록의 "대국 보기" 버튼이 28px로 옆의 리뷰 버튼(`BestMoveJumpButton`, 30px)과 미묘하게 크기가 달랐던 것을, 프로필 카드·마스터 대국 목록과 같은 30px로 통일했다.

**기능 — 모바일 집중학습 미니보드 하단 2페이지 구성**
집중학습은 전체화면 오버레이(`position:fixed,inset:0`)라, "다음 수" 블록 목록(전체/마스터·평가치순/채택률순 선택 포함)이 그 뒤에 완전히 가려 실제로는 볼 방법이 없었다(특히 모바일에서 두드러짐 — 오버레이 자체가 화면 전체라 더 스크롤할 여지가 없다). 이 블록 목록 JSX를 `nextMovesContent`로 뽑아 `FocusPanel`에 prop으로 넘기고, 미니보드 하단(chess.com 통계+마스터 대국 / 다음 수 블록)을 새 `FocusBoxPager` 컴포넌트로 2페이지에 나눠 담았다 — 페이지 전환은 드래그 또는 ‹/› 버튼으로만 되고(휠·트랙패드 스크롤에는 반응하지 않는 transform 기반 캐러셀), 각 페이지는 `ResizeObserver`로 실측한 자기 콘텐츠 높이만큼만 차지한다(기본 flex의 `align-items:stretch`로 두 페이지 중 더 긴 쪽에 맞춰 짧은 페이지 아래로 빈 공간이 크게 남던 문제를 이렇게 고쳤다).

**기능 — 레이팅 변동 그래프 대규모 개편(크기·드래그 크로스헤어·색상·가산혼합·그리드·범례)**
그래프 크기를 키우고(320×120 → 360×210), 리뷰 페이지 `EvalGraph`와 같은 방식의 드래그 크로스헤어를 추가했다 — 그래프를 누른 채 좌우로 끌면(포인터 캡처) 점선 세로선 + 역삼각형 마커 + 말풍선으로 그 지점의 날짜·레이팅을 보여준다. "전체"(다중 시간 규정) 모드는 x좌표가 시간 값이라 시리즈마다 그 시간에 가장 가까운 자기 지점을 따로 찾아 말풍선에 함께 나열한다. 드래그가 SVG 축 라벨(`<text>`) 위를 지나면 브라우저 기본 텍스트 선택이 함께 시작돼 크로스헤어 대신 파란 선택 영역이 생기며 드래그 자체가 씹히던 버그를 `preventDefault`+`user-select:none`으로 고쳤다. 시간 규정별 색을 빛의 삼원색(RGB)에 가깝게 고정했다(래피드 `#FF3B30` 빨강·블리츠 `#34C759` 초록·불릿 `#0A84FF` 파랑) — 특정 시간 규정 하나만 볼 때도 이제 평가치 등락 색 대신 이 색으로 고정해 항상 같은 색으로 알아볼 수 있다(등락 방향은 대신 ▲/▼ 기호로 표시). "전체" 모드에서도 세 시리즈 모두 선 아래 반투명 영역을 채우고, `isolation:isolate`로 격리한 `<g>` 안에서 `mix-blend-mode:screen`(가산혼합)으로 겹쳐 — 두 색이 겹치면 그 둘을 섞은 밝은 색(빨강+초록=노랑 등), 세 색이 다 겹치면 흰색에 가깝게 빛난다. 처음엔 area opacity를 0.55로 낮게 둬 겹치는 자리가 선명한 흰빛 대신 탁한 카키색으로만 보였는데, screen 블렌드 자체가 이미 밝게 겹치는 효과를 내므로(어둡게 만들지 않음) opacity를 0.88로 올려야 노랑/시안/마젠타~흰색까지 또렷하게 겹친다는 걸 확인해 고쳤다. y축 그리드를 3단(최저/중간/최고)에서 5단으로, x축에도 세로 기준선 4개를 새로 추가해 더 촘촘하게 만들었다. 범례는 그래프 위에서 아래로 옮겼다. 배경도 EvalGraph와 같은 어두운 갈색(`#211A13`)으로 바꿔 가산혼합 색이 또렷하게 보이도록 했다.

**버그 수정 — TopOpeningsAnimated의 "n수" 배지가 실제 오프닝 깊이와 어긋나던 문제**
"백 1수에 Italian Game 표시" 같은 오류의 원인은, 배지에 쓰인 "n수"가 실은 오프닝 이름의 move-depth가 아니라 "게임 전체에서 이 오프닝이 몇 번째로 자주 나왔는지"의 순위였기 때문이다(예전 코드는 대국별 최종 오프닝 이름을 집계해 상위 6개를 뽑고, 지금 화면에 보이는 항목의 인덱스를 그대로 "n수"라고 표시). 실제로 Italian Game은 백의 3수(Bc4)에서야 성립하는데 무관한 순위값이 "1수"로 찍힌 것이다. 이제는 각 n(1~6)에 대응하는 실제 ply(백은 2n-1, 흑은 2n)에서 `snapNode(g.moves.slice(0, ply))`로 그 깊이에 실제로 도달한 오프닝만 집계해 가장 많이 둔 것을 고르도록 전면 재작성했다 — 임베드된 오프닝 북 트리에서 직접 값을 뽑아 ply=1..7로 검증해, Italian Game이 ply=5(백의 3수)에서 처음 나타남을 확인했다. "백 n수" 배지도 오프닝 이름 바로 위 별도 줄로 옮기고, 백/흑 배지 모두 금색 그라데이션 박스로 통일했다.

**기능/버그 수정 — chess.com 통계 localStorage 캐싱 및 대형 계정 시간 규정 데이터 누락 근본 원인 해결**
로그인한 사용자가 새로고침하거나 다른 페이지로 이동했다 돌아올 때마다 chess.com의 전체 대국 기록을 처음부터 다시 불러오던 것을(대국 수가 아주 많은 계정은 이 과정이 매우 오래 걸림) `useChessCom`을 localStorage(`occ_chesscom_v1_<username>`) 캐싱 구조로 재작성해 해결했다 — 캐시가 있으면 마운트 즉시 그 값을 화면에 보여주고, 백그라운드에서는 아직 캐시에 없는 새 달(月)과 "가장 최근에 저장했던 달"(그 사이 새로 끝난 실시간 대국이 쌓였을 수 있으므로)만 다시 불러와 얹는다 — 이미 완결된 과거 달은 재요청하지 않는다. 이 과정에서 "chess.com 통계가 너무 큰 계정은 특정 시간 규정의 대국이 아예 표시되지 않는다"는 문제의 근본 원인도 함께 찾아 고쳤다: 예전 구현은 모든 달을 다 받아야만 캐시를 저장하는 all-or-nothing 방식이라, 대국이 많아 완주하지 못하고 화면을 벗어나면(또는 사용자가 다른 페이지로 이동하면) 진행 상황이 통째로 사라지고 다음 방문 때 처음부터 다시 시작했다 — 오래된 특정 시간 규정 대국이 몰려 있는 달들이 영영 로드되지 못한 채 반복 리셋된 것이다. 이제 달 하나를 받을 때마다 즉시 캐시를 저장하도록 바꿔, 중단되더라도 그동안의 진행 상황이 남아 재방문마다 점점 완전한 데이터로 수렴한다. 네트워크 API를 모킹한 Playwright 테스트로 "새로고침 후 오래된 달은 재요청하지 않고 최신 달만 재요청해 새 대국을 반영하는지"를 검증하는 과정에서, 최신 달을 판별하는 기준이 캐시에 저장된 `fetchedMonths` 배열의 마지막 요소(달을 최신순으로 먼저 받아 저장하다 보니 실제로는 가장 오래된 달이 마지막에 들어감)였던 버그를 추가로 발견해, chess.com 아카이브 목록 자체(항상 오래된→최신 순으로 옴)에서 마지막 항목을 최신 달로 삼도록 고쳤다.

**UI — 레이팅 그래프 영역 투명도·범례 크기·드래그 크로스헤어 말풍선 위치 재조정**
"전체" 모드의 가산혼합 영역 채우기 opacity를 0.88에서 0.7로 낮춰 배경 그리드가 살짝 비치는 반투명한 느낌을 살리면서도 겹치는 구간의 밝은 혼색은 여전히 또렷하게 유지했다. 범례의 글자 크기(10→8.5px)·점 크기(8→6px)·간격을 줄이고 줄바꿈을 막아, 래피드/블리츠/불릿 세 항목이 항상 한 줄에 들어가도록 했다. 드래그 크로스헤어 말풍선이 역삼각형 마커·점선을 가리던 문제는, 크로스헤어가 그래프 오른쪽 절반에 있으면 말풍선을 좌하단에, 왼쪽 절반에 있으면 우하단에 붙여 옆으로 비켜서게 하는 `tooltipPos` 헬퍼로 해결했다("전체"·단일 시간 규정 모드 모두 적용). 또한 포인터를 떼면(`onPointerUp`/`onPointerCancel`) `dragFrac`을 `null`로 되돌려, 크로스헤어 UI(마커·점선·점·말풍선) 전체가 화면에서 손을 떼는 즉시 사라지도록 했다.

**성능/버그 수정 — 대국이 아주 많은 계정에서 특정 시간 규정 그래프가 계속 비어 보이는 문제 추가 개선**
사용자가 보낸 스크린 레코딩으로, 26,000판이 넘는 대형 계정에서 "전체" 레이팅 그래프에 불릿 선만 그려지지 않는 현상을 재확인했다. `useChessCom`이 달(月)을 한 번에 하나씩만 순서대로 기다려 받다 보니, 달 개수가 아주 많은 계정은 최근 몇 달치를 받는 데도 여러 초가 걸렸다 — 그 사이엔 실제로는 더 최근 달에 있는 특정 시간 규정 대국을 아직 못 받아, 그래프엔 "이 기간엔 대국이 없다"고 (일시적으로) 표시됐다. 최신 달부터 여러 달을 한 번에(배치, `BATCH_SIZE=10`) 병렬로 요청하도록 바꿔, 같은 시간에 훨씬 더 많은 달을 받아 온다 — 기본 필터 기간(6개월~1년)은 대개 한 배치 안에 다 들어가, 페이지를 열자마자 최근 데이터가 거의 곧바로 채워진다. 또한 훅이 아직 배치 로딩 중인지(`stillFetching`)를 반환하도록 확장해, `RatingHistoryChart`가 로딩이 끝나기 전엔 "대국이 부족해요" 대신 "불러오는 중이에요" 안내를 보여주도록 해 — 아직 못 받아온 데이터를 영구히 없는 것으로 오인하지 않게 했다.

**버그 수정 — "백 n수"·"흑 n수" 배지가 서로 다른 n을 동시에 보여주던 문제**
"가장 많이 둔 오프닝"의 백 박스와 흑 박스가 각각 독립된 `setInterval` 타이머로 자기 목록(백은 최대 6개, 흑은 그보다 적을 수 있음)만 순환해, 화면에 "백 1수"와 "흑 5수"처럼 서로 다른 n이 동시에 표시되는 경우가 있었다(사용자 요청: n이 항상 일치하길 원함). `TopOpeningsAnimated`를 `computeTopOpenings`(순수 집계 함수) + `OpeningBox`(단순 표시 컴포넌트) + `TopOpeningsPair`(단일 타이머로 두 박스를 함께 구동)로 나눠, 백·흑 둘 다에 오프닝이 존재하는 n만 모은 공유 목록을 단일 인덱스로 순환하도록 재작성했다 — 이제 두 박스는 항상 같은 n을 함께 보여준다(한쪽에만 있는 n은 공유 목록에서 자연히 빠진다).

**UI — 퍼즐 탭 해결 완료 목록을 미해결과 동일한 오프닝별 한 줄 스크롤로 통일**
해결 완료 퍼즐 목록이 오프닝별 점선 상자 안에서 그리드(줄바꿈)로 나열돼, 오프닝 하나가 대국 수만큼 여러 줄을 차지하며 미해결 목록(가로 스크롤 한 줄)과 시각적으로 어긋났다 — 미해결 목록과 똑같이 `display:flex, overflowX:auto`로 바꿔 오프닝마다 한 줄로만 표시하고 옆으로 스크롤해서 보도록 통일했다.

**UI — 퍼즐 힌트 애니메이션 개편(기물 회전 흔들림·칸 그라데이션·3단계 토글)**
사용자 요청으로 3단계 시각 힌트의 표현 방식을 다듬었다. (1) 움직여야 할 기물이 옆으로 미끄러지듯(`translateX`) 흔들려 부자연스러웠던 것을, 받침(아래쪽)을 축으로 각도만 바뀌는 새 `hintPieceWobble` 회전 애니메이션으로 바꿔 마치 기물 윗부분을 손으로 잡고 흔드는 것처럼 보이게 했다(`transform-origin: 50% 90%`). (2) 두어야 할 칸(`hintTo`)과 행마법 경로 칸(`hintPathSq`)을 가느다란 윤곽선(inset box-shadow)·작은 원형 점 대신, 칸 전체를 금색 방사형 그라데이션(`HINT_GOLD_GRADIENT`)으로 덧씌우도록 바꿨다 — 경로 칸은 새 `hintPathProgress`(0~1, 목적지에 가까울수록 1에 가까움) prop으로 오버레이의 opacity를 조절해, 경로를 따라갈수록 색이 점점 진해지며 목적지로 이어지는 느낌을 준다. (3) 힌트 버튼이 3단계(꽉 채운 힌트)에 닿으면 `disabled`로 막혀 힌트를 다시 숨길 방법이 없었다 — 이제 3단계에서 버튼을 한 번 더 누르면(토글) 0단계(힌트 없음)로 되돌아간다.

### OpenChess v0.2.5 — 2026/7/24

**버그 수정 — 엔진 라인·기보 데스크톱 드래그 스크롤**
`EngineLines`의 각 줄(수순 텍스트)이 화면 폭보다 길면 `overflow-x:auto`로만 스크롤을 처리하고 있었는데, 이는 `WebkitOverflowScrolling:touch`가 붙어 터치 기기에서는 자연스럽게 관성 스크롤이 되지만 데스크톱 마우스로는 얇은 스크롤바를 정확히 붙잡거나 shift+휠을 써야만 스크롤이 가능해 사실상 마우스로는 스크롤이 거의 안 됐다 — 뒷부분 수순이 항상 가려진 채 "끊긴" 것처럼 보이는 원인이었다. `EngineLines`를 줄마다 독립된 `EngineLineRow` 컴포넌트로 쪼개고, 포인터 이벤트(`pointerdown`/`pointermove`/`pointerup`, `setPointerCapture`)로 직접 `scrollLeft`를 옮기는 드래그 스크롤을 구현해 마우스·터치·펜 어디서나 동일하게 동작하게 했다. 기존엔 줄 전체에 `onClick`을 걸고 `pointerdown` 시점의 clientX와 `click` 시점의 clientX 차이(`dx<6`)로 드래그와 클릭을 구분했는데, 이 방식은 실제 스크롤 동작 자체가 없어 사용자가 스크롤을 시도하며 끌다 손을 뗀 것도 종종 짧은 드래그로 오인해 클릭(수 두기)으로 처리되곤 했다 — 이제 실제로 픽셀 단위로 끌었는지(`moved` 플래그, 3px 임계값)를 기준으로 스크롤과 클릭을 구분해, 스크롤 의도로 끈 경우엔 절대 수가 두어지지 않고 제자리 클릭에서만 그 줄의 첫 수를 둔다. 같은 패턴을 학습 탭 상단 `SequenceBar`(기보)에도 적용했는데, 여기는 줄 전체가 아니라 수마다 개별 `span`에 `onJump` 클릭이 걸려 있어 캡처 단계(`onClickCapture`)에서 드래그 여부를 먼저 판정해 실제로 끌었으면 그 클릭을 막는 방식으로 구현했다(버블링 전에 가로채므로 어떤 수를 클릭했든 안전).

**버그 수정 — 엔진 라인 타이핑 도중 자동 추적**
위 드래그 스크롤 자체는 됐지만, 타이핑 효과(`TypedMoveLine`)로 수순이 시야보다 길게 자라나도 `scrollLeft`가 그대로 0에 머물러 있어 새로 타이핑되는 뒷부분이 화면 밖에서 계속 이어지기만 할 뿐 사용자 눈에는 여전히 특정 수에서 멈춘 것처럼 보였다 — 사용자가 직접 끌어야만 그 뒤를 볼 수 있었던 게 실제 원인이었다. `ResizeObserver`로 줄의 실제 콘텐츠 폭이 자랄 때마다, 채팅창 자동 스크롤과 같은 패턴으로 지금 보이는 오른쪽 끝이 이미 콘텐츠 끝(atEnd)이었다면 새로 타이핑되는 글자를 계속 따라가며 자동으로 스크롤하도록(`followRef`) 했다. 사용자가 직접 드래그해 앞부분을 살펴보는 중에는 그 위치를 존중해 자동 추적을 멈추고(드래그 종료 시 스크롤 위치가 다시 끝이면 추적 재개), 줄 정체성(포지션+순위)이 바뀌면 처음부터 추적을 재개한다. 아직 안 보이는 수순이 남아 있으면 줄 오른쪽 끝에 옅은 그라데이션 페이드도 표시해 "더 있음"을 시각적으로 알려준다.

**기능 — 티어 여정 지도 배경 이미지 교체**
개발자가 GitHub 웹 UI로 티어 배경(`tier-bg-*`) 원본을 화질이 더 좋은 새 파일로 직접 업로드(webp 삭제 후 png 추가, `main`에 직접 반영된 것을 이 브랜치에 머지)함에 따라, `TIER_BG_IMAGE`의 확장자 참조를 `.webp`에서 `.png`로 맞춰 갱신했다. 예전에 지워졌던 `tier-bg-iron.webp` 자리에도 `tier-bg-iron.png`가 새로 채워져, 아이언 구간 배경도 다시 정상적으로 표시된다.

### OpenChess v0.2.4 — 2026/7/24

**기능 — 게임 리뷰/분석 엔진 분리**
설정 탭의 단일 "분석 엔진" 선택지를 학습 탭·퍼즐 탭·사이트 전반에 쓰이는 "분석"과 게임 리뷰 전용으로 나눴다. 게임 리뷰는 사용자 선택과 무관하게 항상 `ENGINE_PROFILES.full`(Stockfish 16)로 고정하고, `UCI_LimitStrength`/`UCI_Elo=3270`을 새로 보내 사람 최상급 수준으로 세기를 제한한다(`eloSetoptions`, `useEngine`의 boot 시퀀스와 `bootAnalysisWorker` 둘 다에 적용). 분석 엔진 선택지는 Stockfish 18 Lite(기본, 기기 종류와 무관하게 고정)와 17.1 두 가지로 좁히고, 각 엔진의 텍스트 설명(`desc` 필드)을 전부 제거했다. 분석 엔진의 스레드 수는 코어 수 비례(`cores-1`) 대신 2로 고정. 게임 리뷰 핵심 분석(`analyzeGame`, 엔진 라인 패널, 수 판정)은 depth 20·moveTime 상한 1초로, 학습 탭 "엔진 상위 수" 패널은 depth 20·MultiPV 7(movetime 700ms는 체감 속도 유지를 위해 그대로)로 조정했다.

**성능 — 게임 리뷰 로딩 지연 제거**
`ReviewPage`가 안 쓰이던 `useEngine("full")` 상시 연결 대신, 풀 부팅이 통째로 실패할 때만 지연 부팅되는 폴백(`useReviewEngine`)을 쓰도록 바꿔 리뷰당 중복 wasm 컴파일을 제거했다. `getAnalysisPool`의 캐시를 프로필 하나만 유지하던 단일 슬롯(`analysisPoolCache = {profile, promise}`)에서 프로필별 `Map`으로 바꿔, 학습 탭(lite/full17)과 게임 리뷰(full) 풀이 서로 다른 프로필이라는 이유만으로 상대 풀을 매번 통째로 정리하고 재부팅하며 서로를 밀어내던(thrashing) 버그를 발견해 함께 고쳤다. 앱 유휴 시간(`requestIdleCallback`)에 게임 리뷰용 Stockfish 16 풀을 미리 부팅해 두는 프리페치를 추가하고, `vercel.json`에 `/engine/*` 정적 파일 장기 캐시 헤더(`public, max-age=604800, stale-while-revalidate=604800`)를 달아 재방문 시 재검증 왕복을 없앴다.

**기능/성능 — 집중 학습 depth 20 및 엔진 큐 취소**
집중 학습(`useFocusAnalysis`)의 두 평가 경로(수 체계 아이콘, 실수 분석)를 depth 20·moveTime 5초 상한으로 올렸다. 아이콘 판정은 이제 depth가 깊어질 때마다(`onProgress`) 다시 매겨져 최대 5초 동안 여러 번 바뀔 수 있다(단발성 결과 대신 점진적 스트리밍). 실수 분석은 라인마다 공통 시작 포지션을 반복 평가하던 중복 호출도 제거했다. 별도로, `useEngine`·`bootAnalysisWorker`의 작업 큐에 slot 기반 취소(`supersede`)를 도입했다 — 같은 slot의 새 요청이 오면 이전 요청을 즉시 큐에서 빼거나(대기 중) "stop"으로 중단시킨다(실행 중). 이전에는 `cancelled` 플래그가 결과 반영만 막을 뿐 엔진의 실제 작업 큐는 그대로 둬, 긴 기보에서 수를 빠르게 연타해 넘기면 이미 지나간 포지션의 계산이 큐를 막아 다음 요청을 지연시켰다 — 학습 탭 엔진 라인 패널·집중 학습·게임 리뷰의 엔진 라인/수 판정에 slot을 적용해 해결했다. slot을 넘기지 않는 기존 호출(`analyzeGame` 등 배치 분석)은 그대로 FIFO로 전부 처리된다.

**버그 수정 — 게임 리뷰 평가치 일관성**
`analyzeGame`이 포지션마다 병렬 워커로 독립적으로 다시 검색하다 보니, 최선 수를 그대로 뒀을 때(손실 0으로 채점)도 다음 포지션의 재검색 값이 이번 포지션 최선수 평가치의 정확한 부호 반전과 조금씩 어긋나(같은 depth·movetime 목표라도 실제 도달 depth가 매번 조금씩 달라질 수 있음) 그래프·코치 카드 평가치가 흔들려 보였다. 최선 수를 따라간 구간은 다음 포지션의 cp를 이번 포지션 값에서 부호만 뒤집어 이어받도록 해 일관된 값이 나오게 했다(다음 수 채점에 쓰는 최선수·2순위는 독립 검색 결과를 그대로 유지, `graphCp` 계산을 채점 루프 뒤로 옮겨 보정된 값을 반영). 리뷰 자유 탐색에서도 같은 클래스의 버그가 있었다 — 화면에 보여준 추천 수(Show 화살표·엔진 라인 1순위)를 그대로 뒀는데도 재검색이 다른 1순위를 내놓아 "우수한 수"로 오판정되던 것을, `playFree`에서 "지금 보여준 추천과 일치하는 수를 뒀는지"를 `forcedBestRef`로 기록해 뒀다가 채점 시 신뢰하는 방식으로 고쳤다.

**버그 수정 — 엔진 라인 타이핑 멈춤**
`EngineLines`의 각 줄이 배열 인덱스(`i`)로 key/`posKey`를 잡고 있어, MultiPV 순위가 바뀌면(탐색 depth가 깊어지며 흔함) 다른 수순이 같은 자리에 들어와도 `TypedMoveLine`이 재사용돼 이전 수순만큼 이미 진행된 `shown` 값을 그대로 물려받았다 — 새 수순이 그보다 짧으면 그 자리에서 타이핑이 멈춘 것처럼 보였다. key를 수순의 정체성(첫 수)으로 바꿔 순위가 바뀌어도 각 수순이 독립적으로 진행 상태를 유지하게 했다.

**UI — 학습 탭 수 블록 재정렬 애니메이션**
`useMergedMoves`의 `tiled` 정렬이 실시간 평가 스트리밍으로 순위가 바뀌어도, 렌더링이 plain `.map()`(key만 안정적)이라 React가 DOM을 순간이동시킬 뿐이었다. 이미 있던 `FadeIn`(`motion.div layout`) 컴포넌트로 `MoveTile`을 감싸 FLIP 애니메이션이 붙게 했다.

**버그 수정 — "?" 탐색 depth 표시**
평가치 바 옆 "?" 도움말의 depth 숫자(`curDepth`)가 메인 검색(depth 20 목표)과 개별 후보 수 평가(depth 15 고정, 즉각 반응 유지 목적으로 그대로 둠) 두 진행률을 같은 값으로 합쳐 표시하고 있었다. 후보 수가 많은 포지션일수록 depth 15 검색들이 자주 완료되며 그 값을 계속 15로 밀어붙여, 메인 검색의 실제 진행 상황이 15에서 멈춘 것처럼 가려 보였다 — 후보 수 개별 평가는 표시용 depth에 더 이상 반영하지 않도록 분리했다.

**기능 — 리체스 퍼즐 DB 기반 오늘의 퍼즐**
기존 "오늘의 퍼즐"은 하드코딩된 4개짜리 배열에서 날짜를 시드로 결정적으로 뽑는 완전 클라이언트 사이드 기능이었다. 리체스 퍼즐 데이터베이스(`database.lichess.org`, 라이브 API가 아니라 CSV 통짜 덤프)를 오프닝 테마별로 활용하도록 새로 만들었다 — `scripts/build-daily-puzzles.mjs`(신규 devDependency `chess.js`)가 CSV를 오프닝 태그로 필터링하고, 각 후보의 원본 대국 PGN을 리체스 게임 export API에서 받아 시작 위치부터의 SAN 수순(`setupSans`)으로 복원해 `public/daily-puzzles/<태그>.json`을 만든다(스트리밍 라인 리더로 읽어 1~2GB급 CSV도 메모리 문제 없이 처리; `scripts/peek-puzzle-tags.mjs`로 실제 태그 표기를 사전 확인 가능). 리체스 퍼즐 자체는 정답이 한 줄뿐이지만, 런타임에 실제 엔진으로 `genPuzzleTree`를 돌려 상대의 다른 응수까지 포함한 여러 라인을 만들고 그중 한 줄을 다시 날짜 시드로 뽑아 `XXXXXX-N`(N=그 줄의 순번) 형태의 id를 부여한다(`resolveDailyPuzzle`). 신규 Supabase 테이블 `daily_puzzle_themes`(2주 오프닝 테마 로테이션, 개발자가 그때그때 지정)와 `daily_puzzles_dev`(미래 날짜 퍼즐을 PGN으로 직접 지정) — 둘 다 `master_games_dev`와 동일한 `is_content_editor` RLS 이중 검증 패턴. `useDailyPuzzle` 훅이 비동기 계산이 끝나기 전엔 기존 큐레이션 폴백(`curatedDailyPuzzleFor`)을 보여줘 화면이 비지 않게 한다. id 형식이 `daily_YYYY-MM-DD`에서 `XXXXXX-N`으로 바뀌어, "어제 이전의 오늘의 퍼즐" 복원 방지 로직을 id 접두사 대신 퍼즐 객체의 `isDaily`/`date` 필드로 판정하도록 함께 수정했다(구버전 저장값은 접두사 검사로 폴백). 설정 탭에 개발자 전용 관리 패널(`DailyPuzzleDevPanel`)과 퍼즐 탭에 "지난 오늘의 퍼즐" 목록(`PastDailyPuzzles`, 순수 함수+결정적 시드라 과거 날짜도 그대로 재현 가능)을 추가했다. XP는 기존 라인 솔브 로직을 그대로 재사용한다.

### OpenChess v0.2.3 — 2026/7/24

**버그 수정 — 탁월한 수(brilliant) 판정**
`isSacrifice`가 4가지 오판정을 내고 있던 것을 실제 대국 4개(사용자 제공 PGN)로 재현·수정함. (1) 내 기물이 방치돼 공격받고 있어도, 그 방치된 기물과 가치가 같거나 더 높은 상대 기물을 안전하게(다음 수에 안 잡히게) 반격 위협하면 탁월로 치지 않도록 `attacksPricierIndependent`를 새로 추가함 — 다만 그 반격 기물이 "도망가면서 동시에" 방치된 내 기물을 잡을 수 있는 경우(예: 9.d5가 나이트 e5의 보호를 없애는 동시에 그 옆 퀸도 위협해, 퀸이 도망가며 그대로 나이트를 챙길 수 있는 경우)는 진짜 반격이 아니므로 예외로 남겨 탁월 판정을 유지함. (2) 원래 `beforeHang>=2 && afterHang>=2`를 동시에 요구해, "이 수 자체가 자신의 보호자를 옮겨서 처음으로" 다른 기물이 방치되는 패턴(beforeHang=0)을 전혀 잡아내지 못했다 — `afterHang>=2`만으로 게이트를 완화함. (3) 포크에 걸린 기물을 옮겨 위험을 줄이기만 했을 뿐인데(방치된 기물의 손실이 원래 포크 손실보다 작음, 22.Qe3 예시) 우연히 무관한 기물이 방치돼 있었다는 이유로 탁월로 오판되던 것을, `afterHang < movedThreatLoss`면 제외하도록 수정함. (4) 이동한 기물이 이미 예정된 손실(movedThreatLoss)을 그대로 실현하는 수는 원래 탁월에서 제외되는데, 그 수가 체크를 동반하는 경우(36.Nh6+처럼 상대 응수를 강제하며 콤비네이션의 발판이 되는 수)는 수동적 손실 수용이 아니므로 예외로 둠. `isSacrifice`는 8개 호출부가 공유하는 단일 함수라 이 수정이 학습 탭·리뷰 페이지·집중 학습·퍼즐 전부에 함께 적용됨.

**성능 — PGN 재생 O(n²) 제거**
`boardFromSans`/`sansToFen`이 호출될 때마다 시작 포지션부터 sans 전체를 처음부터 재생해, 학습 탭·리뷰 페이지에서 긴 기보를 두거나 포지션을 빠르게 넘길수록(=진행된 수만큼) 호출 하나하나의 비용이 커지고 이게 누적돼 뒤쪽 수순으로 갈수록 체감 속도가 심각하게 느려졌다. 두 함수가 공유하는 모듈 스코프의 "마지막으로 재생한 경로" 캐시(`replaySans`)를 두고, 새 호출의 sans와 캐시된 경로의 공통 접두사까지는 이미 계산해 둔 보드·캐슬링 권리·앙파상 상태를 재사용, 갈라지는 지점부터만 증분으로 적용하도록 바꿈 — 실제 진행(한 수씩 이동, 같은 위치에서 여러 후보 수 평가)은 대부분 직전 호출과 긴 공통 접두사를 공유하므로 실질 비용이 거의 O(1)에 가까워짐. 이 두 함수를 쓰는 20여 개 호출부(엔진 평가·후보 수 계산 등) 전부가 코드 변경 없이 함께 빨라짐. board는 항상 새로 만들어지므로(applySan이 원본을 mutate하지 않음) 캐시가 스냅샷을 들고 있어도 안전함을 board mutation 여부 전수 확인으로 검증함.

**버그 수정 — 리뷰 페이지 평가치 변동**
`/review` 자유 탐색 중 포지션이 바뀌어도 `engineLines`가 이전 값을 그대로 유지한 채(새 계산이 끝날 때까지) 그대로 두고 있어, `activeEvalDisp`가 새 계산이 끝나기 전까지 이전 포지션의 평가치를 보여주다 계산이 끝나는 순간 새 값으로 튀는 것이 "최선의 수를 뒀는데 평가치가 변동한다"는 증상으로 보였다. 학습 탭(`useMergedMoves`)은 같은 상황에서 `barEval` 계산부가 `linesPending` 중엔 `engineLines`가 아니라 별도의 빠른 단일PV 진행 콜백(`posEval`)을 쓰도록 분리해 두어 이 문제가 없었는데, 리뷰 페이지엔 그 분리가 없었다 — 새 계산을 시작하는 즉시 `engineLines`를 비워, 화면에는 항상 지금 포지션의 값(또는 계산 중)만 보이게 함.

**버그 수정 — 채점 기준 통일**
리뷰 페이지의 자유 탐색 판정(exploreMove)이 depth 14·movetime 700ms를 쓰고 있어 학습 탭(`evalMoveKind`, depth 13·`MOVETIME_MS` 260ms)과 같은 위치·같은 수에 다른 등급이 나올 수 있었다 — `MOVETIME_MS`를 모듈 스코프 상수로 승격해 depth·movetime을 통일함. 언더프로모션(=/=Q 아닌 승진)을 탁월로 완화하는 규칙이 학습 탭에는 있는데 리뷰 자유 탐색과 정식 게임 리뷰(`analyzeGame`)에는 빠져 있던 것도 양쪽에 동일하게 추가함.

**기능 — 스테일메이트·3회 동형 반복 무승부**
`gameEndState(sans)`를 새로 추가해 스테일메이트·체크메이트·3회 동형 반복(국면 반복 비교는 기물 배치+다음 차례만 보는 단순화된 키를 사용) 여부를 판정함. 체크메이트는 `legalDests`가 이미 자연히 더 이상의 수를 막지만, 3회 동형 반복은 규칙상 여전히 "합법적으로 둘 수 있는" 수가 남아 있어 게이팅이 없으면 계속 둘 수 있었다 — 학습 탭 `go()`, 리뷰 페이지 `playFree()` 양쪽에 이 판정을 넣어 스테일메이트·3회 반복 국면에서는 더 이상 수를 둘 수 없게 하고 보드 하단에 원인을 표시함. chess.com 최근 대국·친구 프로필 목록의 "무" 표시에도 `drawKindLabel`로 실제 기보를 재생해 스테일메이트/3회 동형 반복이면 그 이름을, 아니면 "합의 무승부"를 괄호로 덧붙임.

**기능/UI**
티어 승급 시(`tierUpAnim` 트리거 시점) 새로 도달한 티어 인덱스 × 50개의 나이트 OC 코인을 지급하고 기존 코인 토스트로 알림. 티어 여정 지도의 닫기 버튼을 스크롤되는 콘텐츠 밖으로 빼 `position:fixed`로 우상단에 고정. 학습 탭 메인 보드에서 잡힌 기물·기물 점수차 표시를 제거(`BoardWithMaterial`에 `hideMaterial` prop 추가, 리뷰 페이지는 그대로 유지). 집중 학습 미니보드(`AnimatedMove`)에서 금색(idea)·빨간색(danger) 화살표가 같은 두 칸을 잇는 경우, 그 그룹을 찾아 중심선 기준 대칭으로 수직 평행이동시켜 겹치지 않게 함. 일일 퍼즐 — `puzzleActive`가 새로고침 시 localStorage에서 그대로 복원되는데, 어제 오늘의 퍼즐(id: `daily_YYYY-MM-DD`)을 열어본 채로 자정을 넘기면 그 stale한 값이 계속 복원돼 퍼즐 탭 첫 화면이 전날 퍼즐로 보이던 버그 — 복원 시 그 id가 오늘 날짜가 아닌 `daily_`로 시작하면 복원하지 않도록 수정.

**기능 — 마스터 대국 데이터 소스**
마스터 대국을 다른 외부 대국 데이터베이스로 대체할 수 있는지 검토함 — 검토한 곳은 공개 API가 없어(문서화된 개발자 접근 경로 부재) 자동 연동이 불가능함을 확인함. 대신 개발자 권한(`is_content_editor`, 기존 `app_content` RLS와 동일 헬퍼 재사용)으로 마스터 대국을 직접 등록하는 기능을 추가함 — `master_games_dev` 테이블(RLS: 읽기는 전체 공개, 쓰기는 개발자/공동개발자만)을 신설하고, 집중 학습의 "이 수가 두어진 마스터 대국" 옆에 개발자 전용 `+` 버튼으로 단일 대국 등록(백/흑 GM 이름+레이팅, 연도, PGN — 결과는 PGN의 결과 토큰 또는 마지막 수의 `#`로 자동 판정) 또는 여러 대국이 이어 붙은 PGN(외부에서 받은 파일) 대량 가져오기(`splitPgnGames`+`parsePgnGameForImport`가 `[Event`를 기준으로 대국을 쪼개고 표준 헤더 태그로 자동 채움, `addDevMasterGamesBulk`가 200개씩 묶어 PostgREST 배열 insert)를 지원함. 등록된 대국은 `fetchAllMasterGames`가 Lichess 마스터 DB 결과와 합쳐 반환해 출처 구분 없이 같은 목록에 표시됨. `master_games_dev` 테이블·정책은 저장소의 `supabase-setup.sql`에만 추가돼 있고, 실제 프로젝트 DB에는 별도로 SQL을 실행해야 반영됨.

### OpenChess v0.2.2 — 2026/7/23

**성능**
학습 탭의 엔진 라인(엔진 상위 3줄)이 확정되기까지의 벽시계 시간을 크게 줄임. `useMergedMoves`에서 평가치 바·엔진 상위 3줄·후보 수 보충을 하나로 통합한 `evaluateMulti(depth 16, MultiPV-10)` 호출의 `movetime` 상한을 3000ms에서 700ms로 낮춤 — 엔진 라인은 이미 `streamLines`로 depth가 깊어질 때마다 실시간으로 흘러 나오므로 사용자가 체감하는 "라인 계산 시간"은 이 상한이 결정한다. 평범한 학습·오프닝 포지션은 이 시간 안에 목표 depth에 이미 도달해 표시가 그대로이고, 유독 오래 걸리던 복잡한 포지션만 0.5~0.7초 안에서 (그 시점의 충분히 깊은 라인으로) 확정돼 대기감이 사라짐. 개별 후보 수(`MoveTile`) 평가는 아래에서 여전히 독립된 depth 15 검색으로 채워지므로 수 블록 정확도에는 영향이 없다.

**버그 수정**
학습 탭에서 "다음 수 블록"(`assignTiers`로 매긴 후보 수 등급)에 표시된 수 체계 아이콘과, 그 수를 실제로 뒀을 때 체스판 도착칸·"현재 수 블록"(`curKind`)에 뜨는 아이콘이 서로 다르게 보이던 문제를 해결함. 원인은 두 곳이 서로 다른 분류 알고리즘을 쓴 것 — 후보 블록은 형제 수들의 평가치를 기준으로 판정하는 `assignTiers`(`only`/최선 argmax 로직 포함)를, 실제 둔 수는 마지막 수 재평가 `useEffect`가 별개 알고리즘인 `evalMoveKind`(새 depth 검색으로 판정하고 `decided` 완화·언더프로모션→탁월·`only` 미생성 등 규칙이 다름)로 매번 다시 계산해 `stampQ`가 세팅한 후보 등급을 덮어썼다. 블록에 실제로 떠 있던 각 수의 확정 등급을 `pinnedKindRef`(`"이 위치 key|수"` → 등급)에 pin해 두고(후보 목록이 갱신될 때마다, 그리고 블록에서 수를 고르는 `stampQ` 시점에도 즉시 기록), 마지막 수 재평가 effect가 그 수의 pin이 있으면 `evalMoveKind` 재계산 대신 pin된 등급을 그대로 쓰도록 함 — 클릭·드래그·엔진 라인 클릭·앞으로/되돌리기 어느 경로로 그 수에 도달하든 다음 수 블록과 정확히 같은 아이콘으로 보인다(보드 badge와 `curKind`는 둘 다 `lastQ`에서 파생되므로 한 곳만 맞추면 함께 일치).

**기능 / UX (UI·UX 최적화 1차)**
- 퀘스트: 오늘의 퀘스트 목록 두 번째 항목을 항상 `dailypuzzle`("오늘의 퍼즐 풀기")로 고정. 오늘의 퍼즐(`todayPuzzle`)이 `solved`에 들어오면 `claimQuestCoins("dailypuzzle")`로 1회 완료·지급되도록 하고, 완료 카운트(/5)·전체완료 보너스(`allDone`)·3시간 알림(`dailyQuestCleared`)·클리어 애니메이션 키 목록에 모두 반영.
- 도감(UI): 오프닝 모식도의 수 블록 설명 카드(`DexMoveBlock`)에 삼각형 말풍선 꼬리를 붙여 어느 블록에서 나왔는지 시각적으로 잇고, 블록을 가리지 않도록 세로 모식도=아래·가로 모식도=오른쪽에 배치. 모바일(세로 모식도, `vertical`)에서는 `transformOrigin`을 꼬리에 둔 채 `scale(0.65)`로 축소해 축소해도 꼬리가 블록에 붙어 있게 함.
- 도감(UX): 나침반 정중앙 회로 칩을 클릭 가능하게 하고, 클릭 시 전기 효과음(`/sfx/...circuit-bent-stylophone...`, `SFX_SRC.electric`)을 재생하며 칩 과충전(`dex-chip-surge`) + 모든 트레이스·연결선(`dex-surge-line`)·블록(`dex-surge-node`)에 `depth` 비례 delay로 전류가 바깥으로 퍼지는 서지 애니메이션을 1회 재생(`DexEdgesLayer`/`DexNodesLayer`에 `electric` prop 추가).
- 보드(UX): 그랜드마스터 보드 스킨일 때만 `gm-board-shine` 오버레이로 주기적 광택 sweep 추가(다른 스킨·`prefers-reduced-motion` 무영향).
- 설정(UI): 사운드 카드의 배경음악·효과음 설명 `<p>` 문구 제거.
- 프로필(UI): `TierStatPill`을 박스 없는 흰 십각형 로고 + 오른쪽 금색 XP 게이지로 재설계(size/gaugeWidth로 프로필·리더보드 공용). "퍼즐 N개 해결" 배지 제거. 최근 대국 행은 "⬜백/⬛흑" 텍스트 대신 좌측 진영 색 막대, 상대 레이팅을 아이디에 공백 없이 붙임. 검색/리뷰(`BestMoveJumpButton`) 버튼 30×30 통일. `ListPager`에 `jump`(기본 5페이지=25판) 단위 `«/»` 버튼 추가. chess.com 통계에 전체/래피드/블리츠/불릿 시간 규정 탭 추가 — 게임 객체에 `rules` 필드를 더해 일일·체스960을 집계에서 제외하고 overall/openingStats/최근 대국을 필터된 목록으로 재계산. `useChessCom.analyze`에 `excludeBullet` 옵션을 더해 집중 학습 모드의 수별 통계·오프닝 실수에서 불릿 대국 제외. 친구 프로필의 채팅 버튼을 액션 영역이 아니라 닉네임/아이디 헤더 우측으로 이동.
- 프로필(UX): "가장 많이 둔 오프닝"을 백 첫 수 기반 오프닝 이름 하나로 고정 표시하던 것을, 백 1~6번째 수 인덱스별 최다 수를 2.2초 간격으로 번갈아 애니메이션(`TopWhiteMovesAnimated`, framer-motion)하는 방식으로 교체(내가 백으로 둔 대국 우선, 없으면 전체).
- 검색 리더보드(UI): 자기 자신을 필터로 빼지 않고 금색 윤곽선 + "나" 라벨로 강조 표시. "티어 리더보드" 제목 텍스트 제거. `userSearchRow`에 `rank`/`isMe`/`compact` 옵션 추가 — 프로필 사진 왼쪽에 순위(상위 3명은 전용 메달 이미지 `/rank-{1,2,3}.png`, 초록 배경을 PIL 크로마키로 투명 처리해 생성)를 표시하고, 티어는 축소한 십각형+게이지, 블록 두께는 약 65%(compact)로.
- 퍼즐 탭(UI): `PuzzleCard`에서 다른 사람의 풀이 정보를 좋아요·공유 버튼과 분리해 별도 줄에 잘리지 않게 표시. 미해결 퍼즐을 `firstNamedOpening` 기준 최상위 오프닝별로 묶어 점선 영역 + 가로 스크롤 행으로 배치. 수 체계(테마) 칩을 줄바꿈 없이 한 줄에 들어가도록 축소(좁은 화면만 가로 스크롤).
- 소개 페이지(UX/성능): `DecoBoard`의 수 이동 애니메이션이 `top/left`(레이아웃)를 무한 애니메이션해 "유명한 오프닝들" 갤러리처럼 보드가 여러 개일 때 매 프레임 리플로우로 버벅이던 것을, 시작 칸 고정 + `transform`(x/y translate) 애니메이션으로 바꿔 해결.

**기능 / UX / 버그 수정 (UI·UX 최적화 2차 — 스크린샷 피드백 반영)**
- 검색 리더보드 마감 손질: 순위·아바타·이름·티어가 블록마다 다른 x좌표에 있던 문제 — 순위 칸을 고정폭 슬롯으로, 블록을 전체 폭(`width:100%`)으로 통일. 티어 옆 금색 게이지 바 제거(`TierStatPill`에 `gauge` 옵션 추가). 순위 메달이 1위(왕관 포함이라 본체가 작아 보임) 대신 2위가 가장 커 보이고 3위는 너무 작아 보이던 문제 — 표시 높이를 1위>2위>3위로 뚜렷이 차등(48/39/35px)해 육각형 크기 균형을 다시 맞춤.
- 도감 회로 클릭 전기 효과음 재생 시간을 1초 → 0.7초로 재조정.
- 학습 탭: 엔진 라인이 실시간 스트리밍되며 멀티PV 슬롯이 1→2→3개로 순차 채워질 때 `belowEval` 아래 체스보드가 위아래로 들썩이던 문제 — 실제 줄 수와 무관하게 항상 3줄 높이를 차지하도록 모자란 슬롯을 스켈레톤으로 채움.
- 도감: 오프닝 트리 안에서 수 블록을 직접 클릭한 경우(이미 화면에 보이는 자리)는 카메라 이동(pan/zoom) 애니메이션 없이 즉시 설명 카드만 열고 닫도록 `selectNode`에 `instant` 옵션 추가. 검색 결과 선택·키보드(WASD/방향키) 이동은 기존처럼 애니메이션 유지.
- 소개 페이지(성능): `/about`에 `DecoBoard`가 15개 동시 마운트돼 있는데, 화면 밖에 있어도 무한 반복 애니메이션(둥실임 + 수 이동)을 계속 돌리고 있어 스크롤 중 매 프레임 그만큼의 transform 애니메이션이 동시에 합성돼 버벅였다 — `useInView`로 실제 화면에 보이는 보드만 애니메이션하고 화면 밖으로 나가면 정지하도록 수정(top/left→transform 수정만으로는 해결되지 않던 근본 원인).
- 프로필: 연동된 chess.com 계정이 있으면 "자주 두는 첫 수"의 비어 있는 칸만 실제 대국 통계로 자동 채움 — 백은 백으로 둔 대국의 첫 수, 흑은 백의 첫 수(e4/d4/c4/Nf3)별로 그 대국들에서 가장 많이 둔 응수. 사용자가 이미 입력했거나 지운 칸은 덮어쓰지 않음.
- 리뷰 페이지: 탁월한 수(언더프로모션 제외)가 두어진 위치에서 Show 여부와 무관하게, 내 기물을 상대의 어떤 기물이 공격할 수 있는지 항상 붉은색(`T.danger`) 경고 화살표로 표시(`brilliantArrows`의 `danger` 결과 재사용). 집중 학습 모드의 같은 경고 화살표 색도 `T.blunder`(벽돌빛 갈색)에서 `T.danger`로 통일.
- 학습 탭 제안 화살표: 이론 수뿐 아니라 모든 후보 수로 범위를 넓히고, 선택된 정렬 기준(평가치순/채택률순)으로 상위 3수만 표시. 예전엔 두께·투명도가 "평가치순"을 선택해도 항상 채택률(adopt) 기준으로만 계산되던 버그도 함께 수정(`weight` 필드로 일반화).
- 리뷰 코치 카드: 탁월한 수 설명에 실제로 희생한 기물을 명시(`sacrificedPieceKor` — 방금 이동한 기물 자신이 공격받으면 그 기물을, 아니면 가장 크게 걸린 다른 기물을 찾음). 평가치 박스를 반투명 흰 배경+아이보리 텍스트에서 `EvalBadge`(유리한 쪽에 따라 흰색/검은색 반전)로 통일.


### OpenChess v0.2.1 — 2026/7/23

**기능**
게임 리뷰 보드를 학습 탭 메인 보드처럼 자유롭게 둘 수 있게 함. 실제 기보에 없는 수를 두면 우측 기보 목록에는 반영하지 않고 `<`/`>` 버튼만으로 그 수순을 오가며, 자유 탐색으로 둔 수에도 실제 기보 수와 동일하게 평가치·수 체계 아이콘("계산 중" 포함)·최선수 화살표·코치 카드가 전부 반영되도록 `exploreMove` 분류 이펙트를 추가함.

Analysis 탭·모바일 요약 화면의 `ReviewKindTable`에 `showAll`을 적용해 두어지지 않은 등급도 0개로 항상 표시하고, 오프닝·엔드게임 단계도 항상 노출함. 이론 수만 있어 하이라이트할 비이론 수가 없는 단계(대개 오프닝)는 그 단계·진영의 부분 정확도(`reviewPhaseAccuracy`)를 새 `gradeFromAccuracy` 매핑으로 등급 역산해 아이콘을 채움 — 예전엔 이런 단계의 아이콘 자체가 비어 있었음.

수 체계 아이콘 전반(리뷰뿐 아니라 학습 탭 `MoveTile`·현재 수 블록, 퍼즐 헤더·블록)에 클릭형 설명 말풍선(`CircleBadge`의 `descOnClick`)을 추가함 — 새 `QDESC` 맵에 등급별 조건/설명(나무위키 체스닷컴 문서 참고)을 담아 아이콘 밑에 보여줌. 단계 아이콘 클릭은 별도 동작으로, 좌측 라벨은 그대로 두고 부분 정확도만 별도 말풍선으로 띄움.

chess.com 프로필 사진을 리뷰 보드 위아래에 표시(연동 정보가 없으면 흑/백 반전 파비콘 나이트로 폴백), GM 대국에도 chess.com처럼 초록색 즉시 리뷰 버튼을 추가하고 GM 실명·대국 당시 레이팅을 반영함.

평가치 그래프에 점으로 표시되는 수 체계를 탁월/유일/최선/부정확/실수/블런더/이론으로 통일(이론은 마지막 수만, 탁월/유일/실수/블런더는 항상 표시, 최선/부정확은 등급별 최소 3수 간격)하고, 그래프를 클릭·드래그하면 점선과 함께 움직이는 역삼각형 마커로 임의 시점의 평가치를 조회할 수 있게 함. 세로 평가치 막대를 모든 화면에서 보드 좌측에 배치(백이 아래, 4·5랭크 경계가 0.0, 랭크 1당 1점, ±4점을 넘으면 단색 통일)하고, 엔진 상위 3줄을 `/review`(모바일은 보드 하단, 데스크톱은 그래프 하단)와 학습 탭 양쪽에 실시간 스트리밍 + 타이핑 애니메이션으로 표시함.

Details·Openings 탭은 삭제하고 오프닝 정보만 그래프 위로 옮김. 뒤로가기가 무조건 학습 탭으로 돌아가지 않고, 리뷰를 연 실제 경로(검색 모달·설정 탭 내 프로필·마스터 대국)로 복귀하도록 `openReview`/`closeReview`를 수정함.

**UX**
평가치 표시에서 부호를 없애고 막대 위/아래 위치로만 유불리를 구분하도록 바꾸고(글자 크기도 줄여 막대 안에 항상 들어가게 함), 코치 카드 마스코트를 랜덤 대신 백 수엔 MILKU·흑 수엔 KOKOA로 고정함. 모바일 Analysis는 1회성 창으로 남기되 닫기 버튼은 제거하고, 리뷰 화면에서 첫 번째 뒤로가기는 Analysis 창으로, 두 번째 뒤로가기에서만 `/review` 페이지 자체를 닫도록 단계화함. Analysis의 등급별 개수 숫자를 누르면 그 등급이 처음(이론 수는 예외로 마지막) 두어진 위치로 리뷰 화면이 곧장 이동함. 코치 카드 하단 컨트롤에서 실질적으로 쓰이지 않던 Retry 버튼을 제거하고(Show/Next만 남김), Show 버튼 아이콘을 눈 모양 대신 최선 수를 상징하는 별로 바꿈.

**버그 수정**
평가치 그래프가 데스크톱 환경에서 가로로 찌그러져 보이던 문제(고정 `height` + `preserveAspectRatio="none"`) — CSS `aspectRatio` 기반으로 바꿔 해결함. `/review`의 엔진 라인이 학습 탭과 같은 워커 큐를 공유해 응답이 막히던 문제 — `getAnalysisPool` 전용 워커 풀로 분리해 해결함. 체크메이트로 끝난 대국의 종료 국면 평가치가 "1000.00"처럼 뜨거나 "+0.00"으로 무승부처럼 보이던 문제 — 실제 승패(`mate`/`win` 형태)로 덮어써 "M"·"1-0" 등이 올바르게 표시되도록 수정함. 멀티PV 스트리밍과 최종 확정 결과 양쪽에서 서로 다른 슬롯이 잠깐 같은 첫 수를 담아 완전히 중복된 엔진 라인이 나타났다 사라지던 깜빡임 — 첫 수 기준 중복 제거(`dedupeEngineLines`)로 해결함.

### OpenChess v0.2.0 — 2026/7/22

**기능**
게임 리뷰를 학습 탭 안에 끼워 넣던 즉석 분석 모달(`AnalysisModal`) 대신, chess.com의 "Game Review"를 참고한 전용 전체화면 페이지(`/review`)로 완전히 분리함. `ReviewPage`가 좁은 화면(모바일)은 세로 한 열(요약 화면 `ReviewSummary` → 수순별 코치 리뷰 `ReviewCoachCard`), 넓은 화면(데스크톱)은 보드+평가 바 좌측·Review/Analysis/Details/Openings 탭 우측의 2단 레이아웃으로 각각 chess.com 모바일 앱·데스크톱 웹 구조를 따름. 학습 탭 "분석" 버튼과 프로필·집중 학습의 "게임 리뷰" 버튼 모두 이 페이지로 통합했고, 더 이상 쓰이지 않는 `AnalysisModal`·`analyzeOpen` 상태는 완전히 삭제함(`ANALYSIS_KIND_ROWS`만 `ReviewKindTable`이 계속 재사용).

`useChessCom`이 대국마다 로그인 유저의 진영(`side`)만 남기고 상대 쪽 정보는 버리고 있었다 — chess.com 원본 API 응답(`g.white`/`g.black`)엔 원래 양쪽 다 있으므로, 이제 양쪽의 실제 닉네임·그 대국 당시 레이팅을 그대로 저장함. 프로필의 "최근 대국" 목록에 상대 닉네임·레이팅을 추가하고, `/review`의 요약 화면·Analysis·Details 탭에서도 `reviewPlayerInfo(game, side)` 헬퍼로 실제 이름을 우선 쓰되(없으면 "나"/"상대", 그마저 없는 임의 수순 분석은 "백"/"흑")로 단계적으로 대체함.

`Board`를 감싸는 `BoardWithMaterial` 래퍼를 새로 만들어(Board 자체의 prop 계약은 그대로 둠), 시작 기물 수와 현재 보드에 남은 기물 수를 비교하는 `capturedInfo(board)`로 학습 탭 메인 보드와 `/review`(모바일·데스크톱 둘 다)에 잡힌 상대 기물 아이콘과 기물 점수 차이를 표시함 — 점수가 동등하면 숫자는 아예 안 뜨고, 유리한 쪽에서만 `+n`으로 표시됨.

일일 퍼즐 기능 추가 — 매일 KST 자정 갱신되는 큐레이션 퍼즐(Fool's Mate·Scholar's Mate·Légal's Mate·Blackburne Shilling Gambit, 4종 모두 자체 체스 로직으로 합법성 검증 후 등록)을 `seedRand("daily:"+dateStr)`로 결정적으로 하나씩 순환시킴(`dailyPuzzleFor`). 퍼즐 탭 상단에 `TodayPuzzleCard`를 추가하고, 일일 최초 접속 시 1회, 오늘의 퀘스트를 아직 못 끝냈다면 3시간마다 `DailyPuzzleNoticeModal`(오늘 하루 다시 보지 않기 체크박스 포함)을 띄움.

업데이트 공지 모달을 "버전당 최초 접속 시 1회만" 뜨도록 고침 — 예전엔 "다시 보지 않기" 체크박스를 직접 체크해야만 확정됐는데, 이제 닫는 방법과 무관하게 `onClose`에서 곧장 `dismissedAnnounceVersion`을 확정한다. 같은 내용(버전·날짜·항목)은 설정 탭의 개발진 박스 안에 "개발자 기록"으로 상시 열람 가능하게 남기고, `CHANGELOG` 각 항목에 `dev: [...]`(참여 개발자 아이디) 필드를 추가함.

분석 엔진 선택지에 Stockfish 17.1(정식 대형 신경망, 세 엔진 중 최고 성능)을 추가함 — 같은 npm 패키지를 여러 버전으로 동시에 설치할 수 없어 `stockfish171`(`npm:stockfish@17.1.0`) 별칭으로 받고, `scripts/copy-engine.mjs`가 npm 배포 용량 제한 때문에 여러 조각(`-part-N.wasm`)으로 쪼개진 신경망 파일들과, `self.enginePartsCount`를 먼저 정의한 뒤 실제 엔진 스크립트를 `importScripts`하는 부트스트랩 스크립트(`boot-single.js`/`boot-mt.js`)를 생성하도록 확장함. 로더 스크립트가 조각 파일명을 "자신을 실행 중인 워커의 location"에서 유추하는데, 부트스트랩 파일의 실제 경로로는 엉뚱한 이름을 찾다가 실패했다 — 워커 생성 URL에 `#진짜파일명.wasm` 해시를 붙여(로더가 `self.location.hash`를 우선 사용) 정확한 조각을 찾도록 수정함.

**성능**
실시간 수 아이콘(`evalMoveKind`)과 게임 리뷰(`analyzeGame`) 둘 다 필요할 때마다 워커를 새로 띄우고 끝나면 버렸다 — 매번 신경망을 처음부터 다시 컴파일해야 해서, 특히 신경망이 큰 프로필(17.1)일수록 부팅 비용이 목표 응답 시간보다 커지기 쉬웠다. 프로필별로 딱 한 번만 띄워 세션 내내 재사용하는 공용 워커 풀(`getAnalysisPool`)을 도입하고, 엔진을 바꾸면(다른 profile 요청 시) 이전 풀만 정리하도록 함. `evalMoveKind`는 "두기 전"/"둔 뒤" 포지션이 서로 독립적인데도 순차로 평가하던 것을 풀에서 워커 두 개를 받아 완전히 병렬로 처리하도록 바꾸고, depth(13)에 movetime 260ms 안전 상한(이 depth에서 사실상 항상 여유 있게 끝남 — 정확도 손실이 아니라 워치독 성격의 상한)을 추가함. 워커 풀 크기는 프로필별 고정 상한(예: lite=4) 대신 기기 코어 수(`navigator.hardwareConcurrency`)만큼 그대로 쓰도록 바꿈(발열·CPU 점유율은 감수, 안전을 위한 극단적 상한 32만 유지).

학습 탭 후보 수 블록(`useMergedMoves`) 생성이 같은 포지션 하나를 depth16 단일PV(평가치 바) → depth15 멀티PV-3(엔진 상위 3줄) → depth13 멀티PV-10(후보 수 보충) 순서로 메인 엔진에 세 번 연달아 물어보고 있었다 — 셋 다 "같은 포지션의 상위 수순"이라는 같은 정보를 서로 다른 depth·개수로 중복 탐색하던 것이다. Stockfish의 MultiPV는 한 번의 탐색으로 원하는 개수만큼의 최상위 수를 동시에 주므로, 한 번의 MultiPV-10 탐색(depth 16, movetime 3000ms)으로 통합함 — 이를 위해 `evaluateMulti`에도 단일PV 탐색처럼 depth가 깊어질 때마다 진행 상황을 보고하는 `onProgress` 콜백(1순위 줄 기준)을 새로 추가함. depth·정확도는 그대로 두고 중복 탐색만 없애 벽시계 시간을 최대 3분의 1 가까이로 줄였고, 겹치는 수의 평가치가 서로 다른 탐색에서 미세하게 갈려 블록과 엔진 라인 표시가 어긋나던 문제도 부가적으로 해소됨. 계산 중에는 `linesPending` 플래그를 재사용해 "MILKU/KOKOA가 수를 계산하고 있어요" 마스코트 안내를 표시함.

**버그 수정**
평가치 막대의 탐색 도움말(?) 아이콘이 흰색이라, 백이 크게 우세해 막대가 거의 다 흰색이 되면 배경에 묻혀 안 보이던 문제 — 테마 중립색(브라스 골드)으로 변경함.

프로필에 연동한 chess.com 아이디의 대소문자 표기가 실제와 다르게 보이던 문제 — chess.com API가 소문자로 정규화한 필드를 그대로 썼던 것이 원인이며, `url` 필드에서 실제 표시 대소문자를 추출하도록 근본 수정함.

방치 희생(neglect sacrifice) 기반 "탁월한 수" 판정이 캐슬링·폰 이동에서 전혀 작동하지 않던 구조적 버그를 수정함.

모바일에서 "문의하기"를 눌러도 메일 양식에 제목·본문이 채워지지 않거나 메일 창 자체가 안 뜨던 문제 — Gmail 웹 작성 화면 URL을 새 탭으로 열던 방식 대신 표준 `mailto:` 링크로 교체해 운영체제 기본 메일 앱이 항상 제목·본문까지 채운 채 열리도록 함.

일일 퍼즐 알림 팝업과 업데이트 공지 모달이 동시에 뜰 때, 나중에 마운트된 알림 모달의 전체화면 배경(z-index가 같아 뒤 모달을 완전히 가림)이 공지 모달의 닫기 버튼을 가려 못 누르게 만들던 문제 — `announceOpen` React state(다른 effect가 갱신해야 반영되는 값이라 같은 커밋 안에서 지연이 생김) 대신, 그 값을 결정하는 원본 조건(`dismissedAnnounceVersion !== APP_VERSION`)을 직접 계산해 지연 없이 순서를 보장하도록 수정함.

Stockfish 17.1 워커가 분할된 신경망 조각(wasm part) 이름을 찾지 못해 매번 "연결 실패"로 이어지던 문제(위 "기능" 항목 참고)를 부트스트랩 워커 URL의 해시 트릭으로 해결함.

### OpenChess v0.1.5 — 2026/7/18

**기능**
`/about` 소개 페이지의 체스보드(`DecoBoard`)에 원목 질감(칸마다 결이 보이는 두 겹 그러데이션)과 실제 사이트처럼 파일(a~h)·랭크(1~8) 좌표 표기, 네 모서리가 살짝 어두워지는 비네트를 추가함. 기존에 왕·폰 몇 개만 배치한 추상적 예시 대신, 폴스 메이트·스칼라스 메이트·레갈의 함정·프라이드 리버 어택·불멸의 게임(안더센 vs 키제리츠키, 1851)·오페라 게임(모피 vs 브런즈윅 공작·이수아르 백작, 1858) 6개의 실제 유명 대국·오프닝 함정을 데코 보드 소재로 사용함 — 스크래치패드에 별도 python-chess 스크립트로 각 대국의 SAN 기보 전체를 한 수씩 합법성 검증까지 마친 뒤(6개 전부 완전히 합법이었고 5개는 실제로 체크메이트로 종료) 그 최종 FEN과 마지막 수를 그대로 옮겨 적어, 좌표를 손으로 옮기다 생기는 오차 없이 정확히 재현함(FEN → 보드 배치 변환은 `piecesFromFEN` 유틸이 코드에서 직접 계산).

chess.com 아이콘(`/chess.com_Icon.png`)을 "학습" 기능 카드의 이름표 옆 배지로 추가하고, chess.com 연동 기능(대국 자동 동기화·게임 리뷰 정확도·레이팅 변화 그래프·오프닝별 승률 통계)을 소개하는 별도 섹션(`ChessComSection`)을 신설함 — 실제로 App.jsx에 이미 구현돼 있는 기능만 소개함.

`/about` 페이지의 `VERSION_HISTORY` 배열에서 v0.1.2·v0.1.3 두 버전이 통째로 누락돼 있던 것을 발견해 복구함(README·App.jsx CHANGELOG에는 있었지만 이 배열에만 없었음). 재발 방지를 위해 이 README의 개발 워크플로우 규칙에 "버전 기록 갱신 대상은 README·CHANGELOG·`AboutPage.jsx`의 `VERSION_HISTORY` 세 곳"이라는 항목을 명시적으로 추가함.

집중 학습(FocusMode)에서 퍼즐이 자동 생성되는 동안, 진행률 숫자만 보여주던 것을 실제로 만들어지는 과정을 미리 보여주는 애니메이션으로 바꿈. `genPuzzleTree`의 `onProgress` 콜백이 진행률(p)뿐 아니라 그 순간 막 탐색해 늘어난 수순(path)도 함께 실어 보내도록 하고(`bumpNode(path)`), `puzzleGenProgress` 상태를 `{p, path}` 형태로 바꿔 `FocusPanel`이 `AnimatedMove`로 그 path의 마지막 수를 그때그때 슬라이드시켜 보여준다 — 새 수를 찾을 때마다 미니 보드가 그 수만큼 자라난다.

티어 여정 지도에서 그랜드마스터(마지막·끝없는 티어)가 다른 정거장과 같은 지그재그 자리에 묻혀 "여기가 마지막"이라는 느낌이 없었다 — 그랜드마스터 직전 간격만 크게 벌리고(`GM_EXTRA_GAP`), 그 연결선을 지그재그 대각선 대신 중앙(50%)으로 꺾여 들어가는 긴 꺾은선(엘보)으로 바꾸고, 그랜드마스터 자신도 중앙에 배치함. 위쪽 여백(`GM_TOP_PAD`)도 넉넉히 둬 오로라 배경이 잘리지 않고 길게 보이도록 함.

**UI**
`/about` 페이지의 마스코트 이미지를 전부 App.jsx가 학습 탭 등 실제 앱 UI에서 이미 쓰고 있는 `MASCOT_ART`(플랫 벡터 일러스트, 표정별 12종)로 교체함 — 기존에 쓰던 `public/emoji/*.png`는 이모티콘 선택 창 전용 스케치풍 스티커라 소개 페이지 톤과 맞지 않는다는 피드백을 받아, App.jsx에 인라인 base64로 박혀 있던 이미지를 `public/mascot/*.webp` 12개 파일로 그대로 추출해(무거운 초기화가 없는 `/about` 정적 컴포넌트에서도 가볍게 쓸 수 있도록) `<Mascot char expr size>` 컴포넌트로 단순화함. (중간에 시도했던 손으로 좌표를 짜 넣는 SVG 마스코트 자체 제작안은 스타일이 기존 캐릭터와 맞지 않아 전량 폐기함.)

티어 여정 지도의 티어별 배경이 경계에서 뚝뚝 끊어져 보이던 것을 자연스럽게 이어지도록 함 — 예전엔 밴드마다 자기 영역 안에서만 위아래로 페이드돼, 경계에서 두 배경이 섞이는 대신 둘 다 같은 어두운 배경으로 각자 사라졌다 나타났다. 이제 각 밴드를 이웃 티어 영역까지 `BG_FADE`(130px)만큼 겹쳐 그려, 겹친 구간에서 두 배경이 실제로 서로 섞이며 이어지도록 함(맨 처음 아이언의 아래쪽·맨 끝 그랜드마스터의 위쪽은 겹칠 이웃이 없어 확장하지 않음).

티어 여정 지도 헤더의 "티어 여정"·"퍼즐을 풀어 경험치를 쌓으면 다음 티어로 승급해요" 안내 문구를 없애고, 그 자리에 이정표(Milestone) 아이콘만 남김 — 아래 지도 자체가 이미 지금까지의 여정을 보여주므로 문구 없이도 뜻이 통함.

**UX**
사이트 본편(App.jsx) 전체에서 motion이 티어 여정 지도·승급 연출에만 쓰이고 나머지(퍼즐 카드·퀘스트 카드·상점 카드·친구/검색 목록·알림·채팅 목록)는 전부 즉시 나타나는 정적 div였던 것을 개선함. 카드 컴포넌트 내부는 건드리지 않고 리스트를 그리는 `.map()` 호출부만 새 공용 래퍼 `FadeIn`(`/about`의 `Reveal`과 같은 easing)으로 감싸는 방식을 택해, 좋아요·풀이 상태 같은 기존 로직을 그대로 두면서 인덱스만큼 스태거된 페이드+상승 등장 애니메이션을 얇게 씌움 — 퍼즐 탭 추천/미해결/해결 완료 그리드, 프로필 "푼 퍼즐" 미리보기·"더 보기" 모달, 퀘스트 탭 챕터 목록·메인/일일 퀘스트 카드, 상점 탭 보드·기물 스킨 목록, 유저 검색 결과·친구 추천·리더보드, 친구 목록·요청 목록, 알림 드롭다운, 채팅 목록에 적용함. 목록에서 항목이 사라지는 경우(퍼즐 필터링·친구 요청 수락/거절·알림 삭제)는 `AnimatePresence`로 감싸 갑자기 사라지는 대신 축소되며 부드럽게 퇴장하도록 했고, 알림 드롭다운·채팅 목록 모달·"더 보기" 모달 자체의 열림/닫힘에도 페이드+스케일 전환을 추가함. 채팅 메시지 버블(드래그로 시각 표시하는 기존 제스처가 얽혀 있어 회귀 위험이 큼, 이후 별도 항목으로 손을 댐)과 도감 탭 오프닝 트리(팬·줌 로직이 과거 여러 번 깨졌던 이력)는 이번 범위에서 의도적으로 제외함.

**버그 수정**
그랜드마스터 티어 배지가 오로라 사진으로 잘못 표시되던 문제를 완전히 해결함. `git log --follow`로 `public/grandmaster.png`의 이력을 추적해 원인을 특정함 — 사용자가 GitHub 웹 업로드로 올바른 기물 아이콘을 두 차례(피스만 있는 1.3MB 초안 → 기물+"GM" 워드마크 44KB 최종본) 올렸는데, 이후 같은 파일명으로 오로라 사진이 세 번째로 업로드되며 덮어써졌음. v0.1.3의 커밋(`cfa53d7`)이 "복구했다"고 기록돼 있었지만 실제로는 해당 파일이 손대지지 않아 회귀가 남아 있었던 것도 함께 확인함. 이번엔 파일명 충돌 자체를 없애기 위해 사용자가 두 이미지를 `gm-piece.png`(기물만)·`gm.png`(기물+워드마크)라는 새 이름으로 재업로드했고, `App.jsx`의 `TIER_IMAGE.grandmaster`와 `AboutPage.jsx`의 `TierStrip`이 참조하던 `/grandmaster.png`를 전부 `/gm-piece.png`로 교체함(헤더 배지·여정 지도·퍼즐 탭 스트립·`TierUpOverlay` 승급 연출이 모두 이 값 하나를 공유하므로 한 곳만 고치면 전체가 해결됨 — `tierPieceSrc`로 확인). 워드마크가 포함된 `gm.png`는 작은 배지(`TierBadgeShape`, 76px 십각형)에 넣기엔 텍스트가 안 읽힐 만큼 작아져, 대신 `/about` 버전 기록의 이번 v0.1.4 하이라이트(`ImageHighlight`, 새로 추가한 `VersionHighlight`의 `img` 종류)에서 이 수정 자체를 소개하는 큰 이미지로 사용함.

"여정 지도가 모바일에서 왜곡된다"는 신고를 받아 원인을 추적한 끝에 발견한 근본 버그 — framer-motion은 `scale`/`rotate`처럼 transform 계열 모션 값을 다루는 순간 그 엘리먼트의 `transform` CSS를 자기가 계산한 값으로 통째로 덮어써, 정거장 원(`TierJourneyPath`)에 정적으로 준 `translateX(-50%)` 중앙 정렬이 등장 애니메이션이 끝나자마자 조용히 사라지고 있었다. 데스크톱에서는 여백이 넉넉해 티가 안 났지만, 78% 자리 정거장이 중앙 정렬 없이 그대로 오른쪽으로 반 칸(48px)어치 더 밀려나 좁은 모바일 화면(320px 등)에서는 뷰포트 밖으로 실제로 잘려나갔다(Playwright로 `getBoundingClientRect`를 직접 재 확인해 검증). 정적 중앙 정렬은 애니메이션이 없는 바깥 `div`가 전담하고, scale/rotate 애니메이션은 그 안쪽(inset:0)의 별도 `motion.div`로 분리해 해결함.

사이트 전체 카드·목록에 애니메이션을 넣는 과정에서 두 가지 회귀를 발견해 함께 고침 — (1) `AnimatePresence`의 `popLayout` 모드는 퇴장 애니메이션 동안 레이아웃을 측정하려 직계 자식에 ref를 직접 꽂는데, 공용 래퍼 `FadeIn`이 일반 함수 컴포넌트라 그 ref를 못 받아 "Function components cannot be given refs" 경고가 났다 — `React.forwardRef`로 감싸 해결. (2) 학습 탭 수 블록 목록에서 `key`를 수(san) 문자열만으로 줬더니, `popLayout`이 이전 위치(퇴장 중)와 새 위치(등장 중)의 목록을 잠깐 함께 그리는 동안 같은 수("e4" 등)가 서로 다른 위치에서 우연히 겹쳐 "중복 key" 경고가 났다 — 지금 포지션까지 합친 키로 바꿔 해결. 같은 이유로, "수 추천"/"현재 수" 두 블록이 나란히 같은 위치 문자열을 key로 쓰다 겹친 것도 함께 발견해 구분함. (이후 사용자 요청으로 학습 탭 수 블록·"수 추천"/"현재 수" 카드·집중 학습 진입 애니메이션과 스무스 스크롤은 전부 되돌리고, 퍼즐 생성 실시간 미리보기만 남김.)

**기능 — 채팅 개선**
`ChatPanel`에 실시간 타이핑 표시를 추가함 — DB에 쓰지 않는 Supabase Realtime broadcast 채널(`typing:<uid1>:<uid2>`, 두 uid를 정렬해 대화마다 고정된 채널명을 씀)을 대화 상대와 공유해, 입력창에 글자를 칠 때마다(1.5초 스로틀) `{type:"broadcast", event:"typing"}` 신호만 주고받는다. `broadcast:{self:false}`로 열어 내가 보낸 신호가 나에게 되돌아오지 않으므로, 이 채널에서 받는 이벤트는 항상 상대방 것으로 취급해도 된다. 신호를 받으면 2.5초 동안 3-dot 바운스 모션(`motion.span`, `y:[0,-4,0]` 반복, delay로 위상차)과 "입력 중" 텍스트를 상대 메시지 자리에 말풍선처럼 보여주고, 신호가 끊기면(추가 타이핑 없이 2.5초 경과) 자동으로 사라진다. 채팅창을 열고 닫을 때(컴포넌트 마운트/언마운트)마다 채널을 새로 구독/해제하므로, 이전 세션에서 발견됐던 "같은 table+filter 채널을 여러 컴포넌트가 동시에 구독해 무한 재연결"류 버그와는 구조적으로 무관함(postgres_changes가 아니라 순수 broadcast이고, 인스턴스당 한 번만 구독).

같은 컴포넌트에 메시지 수정·삭제를 추가함. 꾹 누르기(long-press, 480ms 타이머)는 기존 홀드-드래그(시각 표시) 제스처와 `onDown`/`onMove`/`onUp` 핸들러를 그대로 공유하되, 이동량이 6px를 넘으면(=드래그 의도로 판단) 타이머를 취소해 서로 간섭하지 않게 함. 내가 보낸 텍스트 메시지에서만 메뉴가 열리고, 본문(body)이 있을 때만 "수정" 버튼을 보여준다(이모티콘 메시지는 수정 대상에서 제외). 수정을 누르면 입력창에 원래 텍스트가 채워지고(`editingId` 상태), 다시 전송하면 새 `chat_edit_message` RPC(`supabase-setup.sql`)를 호출해 본문과 `edited=true`를 함께 갱신한다 — "chat update own" RLS 정책은 수신자(to_uid)의 읽음 처리 전용이라 발신자의 본문 수정에는 쓸 수 없어, 소유권(`from_uid = auth.uid()`)을 직접 검증하는 SECURITY DEFINER 함수로 별도 처리함. 수정된 메시지는 읽음 표시(옆에 붙는 방식)와 다르게, 말풍선 "위쪽"에 별도 줄로 "수정됨"이 표시된다. 삭제는 `puzzle_likes`/`puzzle_reposts`와 동일한 패턴 — 새 `"chat delete own"` RLS 정책(`using (auth.uid() = from_uid)`)과 `grant delete`를 추가해, `notifyDelete`처럼 REST DELETE를 직접 호출하되 이번엔 정책이 실제로 뒷받침한다.

퍼즐 공유 카드에도 같은 홀드-드래그(당기면 보낸 시각 표시)와 꾹 누르기 메뉴를 확장함. 다만 메뉴 구성은 텍스트 메시지와 달라 "수정" 대신 "전달"(`Send` 아이콘) 버튼을 두고, 그 옆에 내가 보낸 카드에서만 "삭제" 버튼을 보여준다. "전달"은 이미 있던 `PuzzleShareSheet`(퍼즐 탭에서 최초 공유할 때 쓰는 친구 선택 시트)를 그대로 재사용 — `puzzlePreviews`에 이미 캐싱돼 있는 퍼즐 데이터를 그 시트의 `puzzle` prop으로 그대로 넘기기만 하면 되므로 새 UI 없이 구현됨.

**버그 수정 (추가)**
퍼즐 풀이 화면·퍼즐 카드의 "공유" 버튼 텍스트가 좁은 flex 행에서 "공"/"유"로 두 줄 줄바꿈되던 문제를 고침(`whiteSpace:"nowrap"`, `flexShrink:0` 누락).

계정을 바꾸거나 로그아웃해도 직전 계정(혹은 게스트)의 퍼즐·프로필 데이터가 화면에 그대로 남아있던 문제를 고침 — `onAuth` 콜백이 각 필드를 "새 계정에 값이 있을 때만 덮어쓰기"(`if (pr.X) setX(...)`)로 처리해, 새 계정에 해당 필드가 비어 있으면 직전 상태가 그대로 남는 구조적 버그였다. 로그아웃(`logout()`)의 "없으면 기본값" 리셋 로직과 동일하게, `unlocked`/`puzzles`/`solved`/`likedPuzzles`/`repostedPuzzles`/`lineSolves`/`xp`/`coins`/`devBonusGranted`/`deletedPuzzles`/`archivedPuzzles`/`titles`/`currentTitle`/`ownedSkins`/`boardSkin`/`pieceSkin`/`dailyQuest`/`mainQuest`/`recentOpenings`를 전부 무조건 확정하도록 고침. 프로필(닉네임·chess.com 아이디·사진 등)도 직전 상태와 병합(`(p) => ({...p, ...})`)하던 것을 이 계정의 실제 값으로 완전히 교체하도록 바꿈. `logout()`에도 빠져 있던 `devBonusGranted`·프로필 세부 필드 초기화를 추가함.

**기능 — 배경음악·효과음**
사이트에 앤티크한 체스 분위기와 어울리는 잔잔한 배경음악을 추가함. 사용자가 직접 확보해 전달한 드뷔시 "달빛"(Clair de Lune, Suite bergamasque 3악장 — 작곡 자체는 퍼블릭 도메인) mp3를 `public/bgm/clair-de-lune.mp3`로 받아, 앱 최상단(모든 탭이 공유하는 루트 컴포넌트)에 `<audio loop preload="none">` 하나만 상시 마운트해 탭을 이동해도 재생이 끊기지 않게 함. 브라우저의 자동재생 정책상 사용자 제스처(클릭) 없이는 소리 있는 재생이 항상 막히므로 무리하게 자동재생을 시도하지 않고, 재생 상태(`bgmOn`)는 `<audio>`의 `play`/`pause` 이벤트를 그대로 구독해 실제 재생 여부와 항상 일치하도록 함. 켜기/끄기 선택은 `localStorage`(`occ_bgm_on`)에 기기별로 저장해, 다음 방문 때 자동재생을 한 번 더 시도함(이전에 재생을 허용받은 origin은 브라우저가 자동재생을 다시 허용해 줄 수 있음 — 실패해도 조용히 무시).

같은 맥락으로 짧은 효과음 두 종을 추가함 — 사용자가 전달한 버튼 클릭음(`public/sfx/click.mp3`)과 나무 재질 "탁" 소리(`public/sfx/move.mp3`), 그리고 기물이 실제로 잡힐 때 재생할 "퍽" 소리(`public/sfx/capture.mp3`, wooden thud)를 별도로 받아 구분함. 클릭음은 개별 버튼·카드마다 onClick을 일일이 손대는 대신, 사이트 전체가 이미 일관되게 쓰고 있는 `.press` 클래스(`index.css`)를 document 레벨 캡처 단계 클릭 리스너로 감지해 한 곳에서만 재생함(캡처 단계를 쓴 이유는 일부 하위 요소가 버블링 중 `stopPropagation`을 호출해도 그보다 먼저 실행돼 놓치지 않기 위함). 이동음은 LearnTab의 `go(san)`(수 블록 클릭·드래그·엔진 라인 클릭·프로모션 완료가 전부 이 함수 하나로 모임)와 PuzzleSolver의 `tryUserMove(from,to)` 두 central 함수에서 SAN 문자열에 "x"(포획, 앙파상 포함)가 있는지만으로 클릭/포획 소리를 갈라 재생함(`playMoveSfx`). 세 사운드 모두 `playSfx(name)`이 매번 `new Audio(src)`를 새로 만들어 재생해, 연속 클릭·연속 착수 시 이전 소리와 자연스럽게 겹쳐 들리게 함.

(v0.1.4 UI 정리) 사운드 관련 켜기/끄기·음량 조절 UI는 처음엔 헤더 아이콘 버튼으로 뒀다가, 사용자 요청에 따라 전부 제거하고 설정 탭의 새 "사운드" 카드 하나로 모음 — 배경음악·효과음 각각 토글 스위치(기존 개발자 모드 토글과 동일한 알약형 스위치 스타일 재사용)와 `<input type="range">` 음량 슬라이더(`accentColor: T.brass`로 테마 색 적용)를 둠. 음량은 `localStorage`(`occ_bgm_vol`/`occ_sfx_vol`)에 저장되고, 배경음악은 `<audio>` 엘리먼트에 직접 반영, 효과음은 재생 시점마다 저장된 값을 그대로 읽어 적용함.

### OpenChess v0.1.4 — 2026/7/18

**기능**
`/about` 소개 페이지에 유명 오프닝 갤러리(`FamousOpeningsSection`)를 추가함 — 이탈리안 게임·루이 로페즈·시실리안 디펜스·그룬펠드 디펜스·프렌치 디펜스·카로칸·퀸즈 갬빗·킹스 갬빗 8개의 정석 수순을 FEN으로 손수 검증해 옮겨 적고, 기존 `DecoBoard`(v0.1.5의 유명 대국 데코 보드와 동일 컴포넌트)로 재현함.

그랜드마스터 티어를 소개하는 `GrandmasterCard`를 신설함 — `App.jsx`의 `TIER_XP_REQ`에서 그대로 가져온 정확한 달성 조건(누적 200,000 XP, 이후 100,000 XP마다 프레스티지 별)과, 실제로 구현한 세 가지 혜택(오로라 배지·프레스티지 별 / 코인으로 못 사는 전용 보드·기물 스킨 자동 해금 / 친구·검색·리더보드 강조 표시)을 설명함. 아직 만들지 않은 기능은 적지 않는다는 원칙에 따라, 처음엔 없던 코인 보상 배율 조정 등은 의도적으로 제외함.

사용자가 `public/`에 새로 올린 그랜드마스터 전용 체스보드(`gm-chessboard.png`)·기물 12종(백/흑 각 6종)을 실제 스킨으로 연동함 — `BOARD_SKINS`/`PIECE_SKINS`에 `tierLocked: "grandmaster"` 필드를 추가하고, 그랜드마스터 티어 도달 시 `ownedSkins`에 `"board:grandmaster"`·`"piece:grandmaster"`를 자동으로 추가하는 `useEffect`로 코인 없이 해금되게 함. 원본(1920px 정사각 캔버스, 여백 포함)은 알파 바운딩 박스로 잘라내고 WebP/JPG로 재인코딩해 용량을 크게 줄임(기물 21~61KB, 보드 216KB — 원본은 최대 3.6MB). 킹·퀸 원본 이미지가 받침·몸통 없이 왕관만 있는 상태로 온 것은 사용자 확인 후 그대로 반영함. `/about`의 `GrandmasterCard`에도 실제 상점과 동일한 보드·기물 미리보기(`GrandmasterSkinPreview`)를 추가함.

그랜드마스터 강조 표시를 세 곳에 추가함 — `FriendRow`·`userSearchRow`(유저 검색·티어 리더보드 공용)가 `pub.xp`로 계산한 티어가 그랜드마스터면 골드 톤 테두리(`boxShadow`)와 `Crown` 아이콘을 붙인다.

프로필의 "오프닝별 승률" 트리(`OpeningWinrateRow`)에 나무위키 문서식 갈래별 펼치기/접기를 추가함 — 각 노드가 독립적인 `open` 상태를 가지며, 최상위 갈래(depth 0)는 기본으로 펼치고 그 아래 하위 갈래는 기본으로 접어 깊이 중첩된 계정에서 목록이 처음부터 한없이 길어지던 문제를 줄임. 오프닝 이름을 누르면 도감으로 이동하는 기존 버튼과 겹치지 않도록 토글은 별도 버튼으로 분리함.

퍼즐 이름 규칙(`puzzleName`)을 테마별 문구("~에서 탁월한 수 찾기" 등) 대신 항상 `"<오프닝 이름>, <수 이름>"`(예: "Italian Game, 4.Ng5") 형식으로 통일함. 퍼즐 카드 위쪽 라벨은 세부 갈래까지 다 붙은 이름(`lastNamedOpening`) 대신 최상위 갈래만(`firstNamedOpening`, 같은 순회를 첫 히트에서 바로 반환하도록 뒤집은 버전) 보여주도록 분리함.

**UI**
MILKU·KOKOA 일러스트 4장(`ilust-1~4`)을 `/about` 페이지 곳곳에 배치함 — `ilust-1`은 화면 맨 위 전체 폭 배너로 옮기고 한 번 페이드인 후 무한 반복되는 켄 번즈 확대·이동을, 나머지 3장은 `FloatImg`로 감싸 `DecoBoard`·`PhoneFrame`과 같은 방식의 은은한 상하 반복 모션을 추가함. 상단의 "< 소개 · 1/13 >" 페이지 안내 바를 없애고, 화면 좌우 중단에 뷰포트 기준으로 고정된 원형 화살표 버튼(`position:"fixed"`)으로 페이지를 넘기도록 바꿈.

헤더 로고 아래 버전 텍스트의 `marginTop`을 음수로 줘 로고에 더 붙이고, `<span>`을 `<a href="/about">`로 바꿔 누르면 소개 페이지로 이동하게 함.

**버그 수정**
티어 여정 지도(`TierJourneyPath`)의 연결선·정거장 등장 애니메이션이 `viewport={{once:false}}`라 스크롤마다 재생되는데, 가느다란 대각선처럼 바운딩 박스가 얇은 도형은 노출 비율이 스크롤 몇 픽셀 차이에도 크게 출렁여 스크롤이 60% 노출 경계에 걸친 좌표에서 멈추면 그 떨림만으로 애니메이션이 무한히 깜빡였다. `whileInView`를 걷어내고 `onViewportEnter`/`onViewportLeave` 신호를 130ms 디바운스하는 `useSettledInView` 훅으로 그 상태를 걸러 `animate`에 반영하도록 바꿔, "매 통과마다 재생"은 유지하면서 깜빡임만 제거함(`TierPathLine`·`TierStationReveal`로 분리).

퍼즐 탭 그리드(`grid-template-columns: repeat(auto-fill, minmax(148px,1fr))`)는 같은 행의 카드끼리 가장 큰 카드에 맞춰 함께 늘어나는데, 이름이 길면 그 행 전체가 예외적으로 커져 행 높이가 들쭉날쭉해 보였다. 이름 영역 높이를 2줄로 고정(`overflow:hidden`)하고, 실제로 안 들어가는 이름만 글자 크기를 0.5px씩 줄여 맞추는 `FitPuzzleName`으로 교체해 카드 높이가 이름 길이와 무관하게 항상 균일하도록 함. 같은 이유로 카드 위 오프닝 라벨과 "n명이 풀었습니다" 줄도 줄바꿈 대신 한 줄 + 말줄임으로 고정함.

### OpenChess v0.1.3 — 2026/7/17

**기능**
학습 탭 메인 보드의 평가치 바와 체스보드 사이에 엔진 상위 3줄(MultiPV-3, depth 15)을 표시함(`engine.evaluateMulti` 호출 추가, `pvUciToSans`/`pvContinuationText` 헬퍼로 UCI 수열을 이미 둔 수는 뺀 "지금 포지션부터의 다음 수"만 SAN으로 이어붙임). 각 줄이 보드 폭보다 길면 그 줄만 좌우로 스크롤되고, 줄을 클릭(스크롤 드래그와 구분하기 위해 pointerdown/up 좌표 차이를 확인)하면 그 줄의 첫 수가 보드에 그대로 두어짐. 평가치 바는 소수점 둘째 자리까지 표기하고, 백 유리 시 좌측(흰 구간)에 검은 텍스트, 흑 유리 시 우측(검은 구간)에 흰 텍스트로 자신이 놓인 구간과 항상 대비되도록 배치를 전환함(탐색 인디케이터도 겹치지 않게 반대편으로 이동). 흰색은 엔진 라인 평가치 배지(EvalBadge)와 동일한 순백(#FFFFFF)으로 통일.

상단 기보(SequenceBar)를 줄바꿈 대신 가로 스크롤로 바꾸고, 수가 늘어 화면에 다 안 담기면 자동으로 오른쪽(최신 수)으로 스크롤함(`scrollIntoView` 대신 이 바 자신의 `scrollLeft`만 직접 계산해, 조상 스크롤 컨테이너까지 함께 밀리지 않도록 함). 데스크톱에서 A/D 키 또는 </> 키가 보드 아래 이전/다음 버튼과 동일하게 동작하도록 키보드 단축키를 추가함(focus 모드·입력 필드·수식키 조합은 제외).

헤더 우상단 프로필 버튼을 눌러 여는 프로필 드롭다운 카드에도 설정 탭 "내 프로필"과 동일한 메인 퀘스트 진척도·푼 퍼즐 목록을 표시함.

`/about` 소개 페이지의 스크롤 애니메이션을 대폭 강화함 — 기존 일회성(`viewport:{once:true}`) 애니메이션을 반복 재생(`once:false`)으로 바꾸고, 체스판·기물 이미지를 활용한 데코보드(DecoBoard)와 실제 앱 화면 스크린샷을 페이지 전반에 추가함. 버전 업데이트 기록 페이지(2페이지 이후)도 텍스트를 좌우로 번갈아 배치하는 지그재그 레이아웃으로 재구성하고 체스보드·스크린샷을 강화함.

티어 여정 지도(TierJourneyMap)를 여러 방면으로 개선함 — 이미 지나온 구간의 연결선을 점선에서 노란색 실선(#F5C542)으로 바꾸고, 스크롤로 그 구간이 화면에 들어올 때마다(`whileInView`+`once:false`) 선이 그어지는 애니메이션이 반복 재생되도록 함. 이미 지나온 티어에는 초록 체크 배지를 반복 펄스 애니메이션과 함께 표시. 각 정거장도 스크롤로 화면에 들어올 때마다 작게·기울어진 채로 시작해 제자리로 펼쳐지는 애니메이션이 반복 재생됨. 잠금 아이콘은 제거하고, 티어 이미지는 상태(잠김/완료/현재)와 무관하게 항상 원본 색 그대로 표시함(기존 `muted` 흑백·채도 처리 제거). 아이언~그랜드마스터 7개 티어 각각의 느낌에 맞는 세로로 긴 배경 이미지를 추가함(WebP로 압축, 원본 색감 그대로 사용) — `TIER_STATIONS`를 티어별로 그룹핑해 그 구간의 세로 픽셀 범위를 구하고, 그 범위에 해당 티어 배경을 깔아 스크롤해서 지날 때만 자연스럽게 보이도록 함. 배경은 천천히 확대·이동하는 카메라 무브(켄 번즈)를 반복 재생하고, 인접한 티어 배경끼리는 위아래 CSS mask로 옅게 페이드시켜 자연스럽게 이어지도록 함. 헤더 티어 로고는 검색 버튼과의 여백을 늘리고 크기를 키움.

**UI**
퍼즐 탭 공유 버튼(퍼즐 카드·풀이 화면 모두)의 좌우 패딩을 늘리고, 배경을 옅은 브라스 틴트에서 어두운 색(`T.ebony2`)으로 바꿔 시인성을 높임.

헤더 티어 배지에서 XP 진행바·텍스트를 없애고 로고만 남김 — 자세한 진행도는 눌러서 여는 여정 지도에서 이미 볼 수 있어 헤더는 배지만으로 충분히 간결하게 유지.

기보 바·엔진 라인·공지 모달 등 스크롤 가능한 모든 영역에서 브라우저 기본 회색 스크롤바를 전역 CSS로 숨김(스크롤 동작 자체는 그대로 유지).

엔진 라인 각 줄의 평가치 배지 padding·최소폭·글자 크기와 줄 사이 여백을 훨씬 압축해 UI 밀도를 높임.

**버그 수정**
프로필의 "푼 퍼즐" 블록 UI가 왜곡(텍스트가 한 글자씩 줄바꿈)되던 문제를 수정함 — 카드 크기·비율을 퍼즐 탭 카드와 동일하게 맞추고, 표시 상한을 5개로 늘려 가로 스크롤로 볼 수 있게 함.

퍼즐 모식도·도감 오프닝 트리 모식도에서 콘텐츠가 없는 위치가 아닌데도 팬(드래그)이 멈추는 버그를 수정함 — 원인은 v0.1.2에서 추가한 "빈 공간 진입 방지" 로직(`schematicItemVisible`)이 블록의 완전 포함 여부만 판정해, 화면에 일부만 걸쳐 있어도 "안 보임"으로 잘못 판단했던 것 — 완전 포함 대신 AABB(경계 상자) 교차 판정으로 바꿔 해결.

퍼즐 풀이 중 컴퓨터의 응징 수 아이콘이 애니메이션 재생 도중 다른 아이콘으로 바뀌어 보이던(깜빡이는) 문제를 수정함 — `kind` 분류를 `setReply` 호출 이전에 미리 계산하도록 effect를 재구성. 오답을 두면 컴퓨터의 응징 수에 "최선의 수" 배지를 표시하고, 원위치로 되돌아가기까지의 대기 시간을 1000ms에서 1700ms로 늘려 응징 수를 충분히 볼 수 있게 함.

학습 탭의 수 블록(MoveTile)과 엔진 라인(EngineLines)이 같은 수를 서로 다른 검색(수 블록은 개별 빠른 검색 depth 15/movetime 700ms, 엔진 라인은 MultiPV-3 depth 15/movetime 3000ms)으로 평가해 값이 미세하게 갈리며 같은 수인데 서로 다르게 표시되던 문제를 수정함 — 겹치는 수는 항상 엔진 라인의 값을 그대로 물려받도록 동기화(집중 학습 화면도 같은 `live` 필드를 공유하므로 함께 해결돼, 결과적으로 학습 탭 전체가 하나의 엔진 결과로 통일됨). 같은 원리로 메인 평가치 바와 엔진 라인 1번째(최선의 수) 줄의 평가치도 항상 정확히 일치하도록 함.

실제 모바일(안드로이드 Chrome) 기기에서 학습 탭 UI 전체가 화면 폭보다 넓게 강제되어 보드 우측·기보 좌측이 잘려 보이는 심각한 왜곡 버그를 수정함. 원인은 세 곳에 겹쳐 있었다 — (1) `EngineLines`의 nowrap 텍스트 div에 `min-width:0`이 없어, flex 자식 기본값(`min-width:auto`) 때문에 `overflow-x:auto`가 사실상 무력화(스크롤되는 대신 그냥 커짐), (2) `SequenceBar`의 `scrollIntoView`가 이 바 자신이 아니라 조상 스크롤 컨테이너까지 함께 스크롤시키던 부작용, (3) `LearnTab` 카드를 감싸는 CSS Grid 아이템(`grid lg:grid-cols-2`의 직계 자식)에 `min-width:0`이 없어, grid 트랙 자체가 콘텐츠의 min-content 폭만큼 강제로 넓어지던 것(가장 근본적인 원인). 세 곳 모두 `minWidth:0`을 추가하고 `SequenceBar`는 로컬 `scrollLeft` 계산으로 전환해 해결 — 실제 버그 재현 스크린샷과 동일한 수순·뷰포트(393×851)로 재현 테스트해 `#root`의 오버플로가 122px에서 0px로 해소됨을 확인함.

일일 퀘스트(오프닝 플레이)의 클리어 조건 기보가, 큐레이션된 39개 오프닝 풀(`QUEST_OPENING_POOL`)에 없는 더 구체적인 하위 오프닝(집중학습·퍼즐 경로에서 5수 이상 진행해 `recentOpenings` 후보로 들어온 이름)이 뽑혔을 때 통째로 안 보이던 버그를 수정함 — `QUEST_OPENING_MOVES`에 없으면 오프닝 트리에서 `findOpeningPathByFuzzyName`으로 그 이름의 실제 경로를 찾아 대신 표기하도록 함(퀘스트 카드 리렌더가 1초 주기 갱신 카운트다운에 걸려 있어, 이 조회는 퀘스트 목록이 실제로 바뀔 때만 `useMemo`로 캐싱).

오프닝 플레이 퀘스트 문구 "{오프닝}로 chess.com에서 1국 플레이"를 "{오프닝}로 chess.com에서 게임 1회 플레이하기"로 다듬음.

### OpenChess v0.1.2 — 2026/7/16

**기능**
퍼즐에서 잘못된 수를 두면 곧장 원위치로 되돌리던 것을, 그 수를 뒀을 때 상대(컴퓨터)가 어떻게 응징하는지 엔진 최선 응수를 한 번 보여준 뒤(가능한 경우만) 응수→오답 순으로 두 단계 슬라이드 애니메이션과 함께 되돌리도록 함(PuzzleSolver의 `wrong`/`wrongReply`/`revertStage` 상태 머신). 엔진을 못 쓰는 상황(liveOn 꺼짐 등)은 기존처럼 오답만 바로 되돌림.

퍼즐 모식도·도감 오프닝 트리 모식도 모두 드래그·휠·확대축소로 블록이 하나도 없는 빈 공간까지 한없이 팬할 수 있던 것을 막음. 도감 트리는 나침반형(N/S/E/W) 레이아웃이라 팔 사이 네 귀퉁이가 항상 비어 있어, 단순 사각 바운딩 박스로 한계를 두면 그 빈 귀퉁이까지 허용해 버리는 문제가 있었음(직접 테스트하다 발견) — 사각 박스 근사 대신 블록 목록(items) 자체를 기준으로 "화면에 실제로 걸쳐 있는 블록이 있는지"를 직접 검사하고, 하나도 없으면 화면 중심에 가장 가까운 블록으로 스냅하는 `clampSchematicPan` 공용 함수를 추가해 두 모식도에 동일하게 적용함.

일일 퀘스트 4개(새 퍼즐 풀기·chess.com 활동 3종) 각각의 클리어 보상을 +10 XP에서 +10 OC 나이트 코인으로 변경(전체 완료 시 지급되는 50코인 보너스는 그대로 유지). `claimQuestXp`를 `claimQuestCoins`로 바꾸고 토스트도 coins 타입으로 통일.

사이트를 소개하는 별도 페이지(`/about`, `src/AboutPage.jsx`)를 신설함. `main.jsx`에서 경로를 보고 App(엔진 워커·Supabase 클라이언트 등 무거운 초기화 포함) 대신 이 가벼운 정적 컴포넌트를 렌더링하도록 분기(vercel.json의 SPA rewrite 덕분에 `/about` 직접 접속도 정상 동작). 퍼즐 풀이 화면(보드↔모식도)과 동일한 드래그 페이지 넘김 패턴으로 구성했고, 1페이지는 핵심 기능 소개(마스코트 삽화+말풍선, 좌우로 번갈아 배치되는 기능 카드, 실제 7단계 티어 이미지 쇼케이스), 2페이지부터는 CHANGELOG를 latest-first로 재구성한 `VERSION_HISTORY`(App.jsx의 CHANGELOG와는 별도 데이터 — 모달은 한 줄 요약, 이 페이지는 기능/UI/UX/성능/버그 수정/보안 카테고리로 나눠 조금 더 풀어 쓴 설명)를 보여줌. 버전마다 마스코트가 소개/마무리하는 말풍선과, 대표 기능을 이미지·애니메이션(티어 승급 연출 재현 데모, 티어 스트립, 아이콘 펄스 등)으로 보여주는 하이라이트를 추가하고 framer-motion으로 전 페이지에 진입/스크롤 애니메이션을 적용함. 공지 모달(`AnnouncementModal`)에 "업데이트 내역 자세히 보기" 링크(`/about?page=2`, 새 탭)를 추가해 연결함.

**버그 수정**
일일 퀘스트에서 "오프닝로 chess.com에서 1국 플레이"처럼 오프닝 이름이 비어 표시되던 버그를 수정함. 원인은 `questLabel`이 `q.opening`이 falsy일 때 "오프닝"으로 대체 표시하는데, `genDailyQuest`로 새로 만든 데이터는 항상 유효한 값을 넣지만 quests 배열 자체는 있으면서 그 안 opening 항목 값이 비거나 손상된 저장 데이터(18차 이전 구버전 `featured` 문자열 배열 구조는 `!dailyQuest.quests`로 이미 걸러졌지만, 그보다 미묘한 손상까지는 못 걸렀음)는 재생성 조건을 통과하지 못해 계속 남아 있었던 것 — 날짜 변경 시 재생성 조건에 quests 배열 각 항목의 유효성 검사(`dailyQuestQuestsValid`)를 추가해 다음 로드 때 자동 복구되도록 함.

퍼즐 진입/재시작 시 재생되는 컴퓨터의 첫 수(mistakeSan, 직전 실수 수)는 이미 지나간 수인데도 "계산 중" 점 애니메이션을 보여주거나 `isSacrifice` 추측값을 먼저 보여줬다 실제 엔진 판정으로 갈아끼우는 연출이 있어, 수 체계 아이콘이 중간에 눈에 띄게 바뀌어 보이던 버그를 수정함. 이 경우(`isSetupMistake`)는 추측·pending 없이 최종 판정이 나온 뒤 한 번만 아이콘을 표시하도록 `moveIcon` 계산 effect를 분리함.

**UI**
티어 로고 원(`TierLogoDisc`)의 배경을 기존 아이보리 그러데이션에서 사이트 전역 브라스 광택 톤(`T.brass`/`T.brassHi`)의 골드 그러데이션으로 바꾸고, 헤더 배지·퍼즐 탭 스트립·프로필 필·여정 지도 전반에서 로고·원 크기를 여러 차례에 걸쳐 축소함(특히 퍼즐 탭은 높이를 우선적으로 줄임). 퍼즐 탭 티어 스트립에서 다음 두 단계를 미리 보여주던 작은 배지·연결선(`NextCheckpointBadge`/`TierConnector`/`upcomingCheckpoints`)은 여정 지도와 중복이라 제거함.

헤더 티어 배지의 XP 진행바·텍스트 배치를 로고 오른쪽에서 아래쪽으로 바꾸고, 그 안에서도 텍스트를 진행바 아래가 아니라 오른쪽에 나란히 배치함. 헤더 로고(`OpenChessLogo.png`) 아래에 `APP_VERSION`을 파생 표시하는 작은 금색 텍스트를 추가하고, 로고("OpenChess" 글자) 오른쪽 끝에 맞춰 오른쪽 정렬함.

### OpenChess v0.1.1 — 2026/7/16

**기능**
퍼즐 카드·풀이 화면의 공유 아이콘을 "공유 수 표시"(비인터랙티브)와 "공유하기 액션"(별도 버튼)으로 분리함. 기존엔 종이비행기 아이콘 하나가 숫자 표시와 클릭 액션을 겸해, 숫자만 보려던 사용자가 실수로 공유 시트를 열게 되는 혼동이 있었음.

퍼즐이 두 개 이상의 테마 태그를 동시에 가질 때(예: "기물 희생하기"+"실수 응징하기") 카드 배경 패턴을 두 테마 색으로 절반씩 나눠 함께 보여주도록 함(PuzzleThemePattern이 themes 배열을 받아 각 테마 고유 패턴을 절반 크기로 좌우에 배치).

티어 UI를 디자이너가 제작한 실제 티어별 이미지로 전면 재설계함. 기존에 "아이언 V"처럼 텍스트로 표기하던 자리를 전부 이미지(로마 숫자가 이미지 안에 이미 그려짐)로 대체하고, 헤더 배지·퍼즐 탭 상단 스트립·여정 지도 전반에 적용함. 각 티어 이미지는 원본 세로 비율이 서로 달라(로마 숫자가 이미지 하단에서 일정한 여백을 두고 그려짐) 티어별 원본 높이(TIER_IMG_NATIVE_H) 기준 균일 배율을 적용해 어느 티어든 숫자 크기가 시각적으로 어긋나지 않도록 함. 로고 뒤 원형 배경(TierLogoDisc)은 여러 차례 사용자 피드백을 반영해 정중앙 정렬, 크기, 색(사이트 톤에 맞춘 아이보리 그라데이션+브라스 테두리)을 조정했고, 최종적으로 로고가 원 안에 꽉 차되 잘리지 않는 최소 크기(그랜드마스터처럼 세로로 가장 긴 이미지 기준 계산)로 맞춤.

티어가 승급하면 전체 화면 애니메이션(TierUpOverlay)이 재생되도록 함 — 반투명 검정 배경으로 화면 전체를 어둡게 하고, 기존 티어 기물이 좌우로 흔들리다 왼쪽으로 퇴장하며, 오른쪽에서 새 티어 기물이 슬라이드 인. 2.7초 후 자동으로 닫히고, 화면 아무 곳이나 탭해도 즉시 닫힘. 기존에 작은 토스트로만 알리던 승급 연출을 대체함.

**버그 수정**
퍼즐 좋아요/리포스트를 취소해도 카운트가 줄지 않던 버그를 해결함. 원인은 puzzle_like_toggle/puzzle_repost_toggle RPC의 RETURNS TABLE 출력 변수(likes/reposts)가 UPDATE 문의 동명 컬럼과 이름이 겹쳐 "column reference is ambiguous" 런타임 에러가 나던 것 — public.puzzles.likes/reposts로 완전히 한정해 해결.

chat_messages 테이블에 puzzle_no/share_reward 컬럼을 추가하는 마이그레이션이 create table if not exists 블록 안에만 있어, 이미 테이블이 있는 기존 프로젝트에는 실제로 반영되지 않던 문제를 별도 alter table로 수정.

채팅 버튼을 누르면 사이트가 먹통이 되던 버그를 해결함. 원인은 useRealtimeTable의 Supabase Realtime 채널 이름이 table+filter만으로 정해져, 헤더의 안읽은 채팅 배지 구독과 채팅창 자체의 구독이 동일한 채널 토픽을 만들어 재연결 루프가 발생하며 CPU를 과점유하던 것 — 컴포넌트 인스턴스별 고유 id를 채널 이름에 추가해 해결.

크라우드소싱 퍼즐 업로드 시 한 세션이 먼저 올린 테마 태그를 다른 세션의 업로드가 덮어써 사라지던 버그를 해결함(업로드 전 서버의 기존 태그를 가져와 합집합으로 병합한 뒤 업서트). 아울러 v0.1.0 이전 레거시 id 체계로 저장돼 있던 특정 퍼즐(#685586)이 테마 unify 이후 코드가 생성하는 id와 어긋나 실제로는 같은 포지션인데 별개 레코드로 남아 있던 것을 재매핑함.

안드로이드 크롬에서 홈 화면·바로가기 추가 시 파비콘 대신 "O" 글자 아이콘이 뜨던 문제를 해결함(웹 매니페스트·apple-touch-icon·정사각 아이콘 세트 추가).

도감 탭 오프닝 트리에서 대표 오프닝 이름표 위치를 v0.1.0 이전 방식(점선 영역 고정 위치)으로 되돌리고, 대신 그 오프닝의 최상위 수가 이름표 쪽으로 오도록 모식도 배치를 조정함. 검색으로 특정 수까지 이동하는 애니메이션 도중에는 수 설명 카드를 숨기고 도착 후에만 보이도록 함.

티어 여정 지도에서 잠긴 구간의 원 배경 전체에 불투명도를 낮췄더니 뒤에 지나가는 점선 연결선이 비쳐 보이던 버그를 해결함(원 배경은 항상 불투명하게 유지하고, "아직 안 옴" 표시는 기물 이미지 쪽에만 적용).

### OpenChess v0.1.0 — 2026/7/16

**기능**
퍼즐 탭·퍼즐 풀이 화면에 종이비행기 아이콘의 공유 기능을 추가함. 누르면 친구 목록이 뜨는 공유 시트가 열리고, 친구를 고르면 그 친구와의 대화창에 인스타그램 릴스 공유처럼 퍼즐 미리보기 카드(미니 보드 + "퍼즐 풀러 가기" 버튼)가 남음(chat_messages에 puzzle_no를 실은 메시지 하나로 구현, 기존 "서로 친구여야 채팅 가능" RLS를 그대로 탐). 버튼을 누르면 그 번호의 퍼즐을 서버에서 가져와 곧장 퍼즐 탭으로 이동시키고, 어떤 공유를 통해 들어왔는지를 세션 동안 기억해 둔다.

공유받은 퍼즐을 그렇게 들어와서 풀면, 얻은 XP의 10%(반올림, 최소 1)가 공유해 준 친구에게 실시간으로 전달되도록 함. XP는 계정마다 클라이언트가 소유·저장하는 값이라(user_progress의 jsonb 블롭을 매번 전체 덮어쓰는 구조) 서버가 남의 XP를 직접 늘릴 수 없어, puzzle_share_reward RPC는 공유자에게 갈 chat_messages 시스템 메시지(보상 금액 포함)만 하나 남기고, 공유자 본인의 클라이언트가 이미 구독 중이던 채팅 실시간 갱신에서 그 메시지를 발견해 자기 XP에 더하고 화면 중앙에 애니메이션으로 보여준 뒤 읽음 처리함(오프라인이었다면 다음 접속 때 안읽은 채팅을 훑으며 소급 적용). 어떤 공유 메시지를 통해 왔는지를 서버에서 검증해 임의 id로 보상을 위조할 수 없게 막음.

퍼즐 리포스트 기능을 추가함(좋아요와 동일한 토글 방식). 리포스트한 퍼즐은 풀이수·좋아요와 무관하게 내 추천 퍼즐 후보 풀에 항상 섞여 들어가는데, 기존에 있던 "후보 풀에서 새로고침마다 6개만 결정적으로 다시 뽑아 보여주는" 메커니즘을 그대로 재사용해, 별도의 확률 로직 없이도 추천 퍼즐에 간헐적으로 나타났다 안 나타났다 하게 함.

퍼즐 카드·풀이 화면에 리포스트 수·공유 수를 좋아요 수 옆에 나란히 표시함.

설정 탭 "내 프로필"에서만 보이던 메인 퀘스트 진척도·푼 퍼즐 목록을, 유저 검색·친구 프로필(공개 프로필)에서도 볼 수 있게 함. profiles.pub jsonb에 이미 xp·solvedCount 등을 공개해 오던 것과 같은 방식으로 solvedNos(전역 공유 puzzles 테이블의 퍼즐 번호 배열 — 데이터 자체는 이미 공개돼 있으므로 번호만 실어도 카드 렌더에 충분)와 mainQuestSummary(챕터/문항 진행 요약 — 전체 챕터·문항 수는 CONTENT 기준이라 개인정보가 아니고 진행분만 개인)를 추가로 공개함. 공개 프로필에서 "푼 퍼즐" 카드를 누르면 그 자리에서 퍼즐 탭으로 이동해 직접 풀어볼 수 있음.

"푼 퍼즐" 목록은 내 프로필·공개 프로필 모두 앞쪽 3개만 미리 보여주고, 4개 이상이면 "더 보기" 버튼으로 전체를 스크롤 가능한 별도 창에서 확인하도록 함(SolvedPuzzlesBlock, 두 화면이 공용으로 씀). 공개 프로필 쪽은 처음엔 미리보기 3개 분량만 조회하고, "더 보기"를 실제로 눌렀을 때만 나머지 전체를 마저 불러온다 — 프로필을 열 때마다 전체를 조회하지 않도록 하는 절제. (버그 수정) 이 작업 전에는 공개 프로필의 "푼 퍼즐"에 60개 상한을 두었는데, solved가 Set이라 먼저 푼 순서대로 쌓이는 걸 그대로 slice(0,60)해 정작 가장 오래전에 푼 것들만 보이고 있었다(제목의 전체 개수와 실제 보이는 카드 수가 안 맞아도 안내조차 없었음) — 이번에 "더 보기"로 상한 자체를 없애면서 함께 해소됨. (버그 수정) "더 보기" 모달 안의 퍼즐 블록을 눌러도, renderCard가 호출부(내 프로필/공개 프로필)마다 자기 onClick을 직접 만들어 붙이던 구조라 onOpenPuzzle은 실행돼 뒤에서 탭·활성 퍼즐은 바뀌는데 모달 자체는 안 닫혀 화면이 그대로인 것처럼 보이는 문제가 있었다 — renderCard(p, onClick)로 바꿔 클릭 동작을 SolvedPuzzlesBlock이 직접 만들어 넘기게 하고, "더 보기" 모달 안에서는 그 onClick이 먼저 모달을 닫은 뒤 퍼즐을 열도록 해, 어디서 누르든 항상 그 퍼즐로 리디렉션되도록 함. (버그 수정) 공개 프로필의 "푼 퍼즐" 카드가 어떤 퍼즐을 보여줄지는 프로필 주인의 solvedNos로 정하는 게 맞지만, 카드 자체의 해결 표시(초록 배경·별 개수)는 PuzzleCard가 앱 다른 모든 곳에서 그렇듯 "지금 보는 사람" 기준이어야 하는데 isSolved를 항상 true로 고정해 둬, 프로필 주인은 다 풀었어도 정작 보는 사람은 안 풀었거나 일부 라인만 푼 퍼즐까지 전부 별 3개로 보였다 — 보는 사람 자신의 solved/lineSolves를 받아 각 카드에 반영하도록 해, 목록(누가 풀었는지)과 카드 표시(내가 얼마나 풀었는지)를 분리함.

**UI**
도감 탭 오프닝 트리에서, 점선으로 묶인 칭호(대표 오프닝) 영역의 이름 라벨이 그 영역의 바운딩 박스 모서리에 고정돼 있던 것을 그 오프닝의 최상위 수(실제로 그 영역이 시작되는 수) 바로 위로 옮김. 나침반 레이아웃은 팔의 방향(N/S/E/W)마다 "깊이"가 화면의 어느 쪽으로 뻗는지가 달라, 특히 1.e4 쪽(Italian Game·Ruy Lopez·Sicilian Defense 등 13개 대표 오프닝 중 8개가 여기 속함)은 뿌리가 영역의 위쪽이 아니라 아래쪽 끝에 있어, 라벨이 실제로는 그 오프닝과 무관한 가장 깊은 변화 근처에 붙어 있는 것처럼 보였다. 이미 계산해 두고 쓰지 않던 그룹별 rootX/rootY 좌표로 라벨을 앵커해, 나침반 방향과 무관하게 항상 "이름 텍스트 바로 밑에 최상위 수"가 오도록 함.

퍼즐 테마 3종의 카드 배경 패턴을 실제 수 체계 배지와 같은 색·기호 언어로 다시 디자인함. 기물 희생하기는 탁월한 수(brilliant) 배지와 같은 민트색 + "!!" 모티프(굵은 스템+사각 점 두 벌)를 그대로 가져옴. 우위 점하기는 부정확한 수(inaccuracy)와 같은 노란색으로, "?!"를 채움 없이 끊어진 점선 윤곽선과 주변 부스러기 획으로 갉아 먹힌 듯한 부식감을 내 "짓밟고 우위를 다진다"는 느낌을 표현함. 실수 응징하기는 블런더의 빨강→실수의 주황 그라데이션 위에 "?"·"??"를 올리고 그 위로 어긋난 균열선을 그어, 카드 배경색과 무관하게(색을 지우는 대신 어두운 균열선만 얹는 방식) 항상 "형체는 알아볼 수 있지만 심하게 파괴된" 인상을 주도록 함.

**버그 수정**
집중 학습에서 퍼즐이 자동 생성되는 방식 때문에, 실수(또는 부정확한 수)를 응징·전환하는 바로 그 수가 동시에 "탁월한 수"로도 분류되면 사실상 같은 포지션(setupSans+mistakeSan)에서 실수 응징하기(또는 우위 점하기) 퍼즐과 기물 희생하기 퍼즐이 각각 따로 생성돼, 겉보기엔 다른 퍼즐인데 내용은 거의 같은 것이 두 개 저장되던 문제를 해결함. 원인은 퍼즐 id에 테마 접두어("sac|"/"adv|")를 붙여 테마별로 별도 id 공간을 강제로 나눠 놓은 것 — 실제로는 실수 응징/우위 점하기는 "그 수 자신의 위치"가, 기물 희생하기는 "그 수 직전 위치"가 정체성인데 둘이 우연히 같은 포지션을 가리키는 경우가 흔했다. 이제 퍼즐 id는 테마와 무관하게 포지션+수로만 정해지고, `theme` 단일 값 대신 `themes` 배열로 바뀌어 한 포지션이 여러 테마에 해당하면 퍼즐 하나에 태그가 함께 붙는다(예: "실수 응징하기 · 기물 희생하기"). 필터 칩·카드·풀이 화면 모두 이 다중 태그를 반영하고, 이미 트리가 생성된 퍼즐에 새 테마가 뒤늦게 해당되면 재생성 없이 태그만 얹는다.

### OpenChess v0.0.6 — 2026/7/15

**성능**
도감 탭 오프닝 트리가 chess.com 대국이 많은 계정에서 심하게 렉이 걸리던 문제를 해결함. 트리 노드마다(최대 4000개) 잠금 여부를 매기려고 매번 전체 대국 배열을 훑는 O(노드 수 × 대국 수) 연산을 하고 있었던 것이 원인이었다 — 대국이 바뀔 때 한 번만 모든 수순 접두어를 Set에 담아 두고, 노드마다 O(1) 조회로 바꿈. 이와 별개로 chess.com 데이터를 감싸는 훅이 무관한 이유로 부모가 리렌더될 때마다 새 객체를 반환해 트리 전체가 불필요하게 다시 계산되던 것도 고침. 팬(드래그)·줌 중에도 노드 좌표 자체는 안 바뀌는데 스타일 계산이 매 프레임 다시 일어나던 부분은 memo 컴포넌트로 분리해 드래그 성능도 별도로 개선함.

도감 오프닝 트리(OpeningSchematic)를 조립하는 useMemo가 배경 로딩 중 약 220ms마다 전체를 다시 계산하는데, 그때마다 내부 노드마다 boardFromSans(path)로 루트부터 그 깊이만큼 수순을 처음부터 재생해 채택 등급을 매기고 있었음(O(노드 수 × 평균 깊이)). 트리를 훑는 재귀 함수가 어차피 부모→자식 순서의 DFS이므로, 부모가 이미 만든 보드를 그대로 물려받아 이번 수 하나만 한 번 더 적용하도록 바꿔 O(노드 수)로 줄임 — 대국이 많은 계정일수록, 트리가 깊고 넓을수록 체감 효과가 큼.

메인 체스보드에서 수를 둘 때마다 화면에 보이는 모든 후보 수(캡 없이 최대 수십 개)의 실시간 엔진 평가를 워커 하나로 한 번에 하나씩 순서대로 처리하고 있었음(수당 최대 700ms 상한이 있어도 총 대기 시간이 후보 수 개수에 정비례). 게임 리뷰(analyzeGame)가 이미 쓰던 "독립 워커 풀 + work-stealing" 패턴을 그대로 재사용해, 워커 여러 개(엔진 프로필별 2~4개)가 같은 목록에서 하나씩 꺼내 동시에 평가하도록 바꿈 — depth·movetime(정확도)은 손대지 않았으므로 정확도 손실 없이 벽시계 시간만 풀 크기만큼 줄어듦. 이 풀은 게임 리뷰와 달리 사용자가 수를 둘 때마다 반복 재사용되므로, 프로필이 바뀌지 않는 한 한 번 띄운 풀을 계속 재사용하도록 컴포넌트 생명주기에 걸쳐 캐싱함.

**기능**
도감 탭 오프닝 검색을 개선함 — 이전엔 상위 8개까지만 보여주고 순서도 사실상 임의였는데, 이제 검색어가 포함된 오프닝을 전부 보여주고 수순이 짧은(더 상위·유명한) 오프닝이 위로 오도록 정렬함. 검색 결과를 선택했을 때의 이동 애니메이션도 중심에서 목표까지 직선으로 가로지르던 것을, 나침반 중심에서 그 오프닝까지 실제로 이어지는 트리 선(ㄱ자 커넥터)을 그대로 따라가도록 바꿈.

레벨/XP 시스템을 전면 재편함. 기존 피보나치 곡선은 너무 가팔라(레벨 10에 누적 23,100 XP, 레벨 20엔 286만 XP) 사실상 레벨 10~13을 넘기지 못했는데, chess.com 퍼즐 티어나 리그 오브 레전드 같은 랭크 게임처럼 아이언·브론즈·실버·골드·다이아몬드·마스터·그랜드마스터 7단계 티어로 바꾸고, 각 티어를 다시 5단계 구간(1이 가장 높음)으로 나눔. 기물 테마(폰~킹 실루엣)는 그대로 유지하되 등급마다 고유한 색을 입힘. 헤더 티어 배지를 누르면 전체 여정 지도(구간마다 원 하나, 위로 갈수록 높은 등급)가 열리고, 퍼즐 탭 상단에도 지금 구간과 다음 두 단계를 미리 보여주는 스트립을 추가함.

유저 검색창을 열었을 때(아직 아무것도 입력하지 않은 기본 화면) 비어 있던 것을 "알 수도 있는 사람"(내 친구의 친구, 같이 아는 친구 수 순)과 "티어 리더보드"(XP 상위 플레이어) 두 갈래로 채움. 친구의 친구는 다른 사람의 friend_edges를 읽어야 하는데 RLS가 본인 관련 행만 읽도록 막아 둬 클라이언트에서 조합할 수 없어 서버 RPC(friend_suggestions)로 계산했고, 리더보드는 XP가 profiles.pub jsonb 안의 값이라 PostgREST 문자열 정렬로는 자릿수가 다른 숫자가 잘못 정렬될 수 있어 숫자로 캐스팅해 정렬하는 RPC(leaderboard_top)를 추가함.

개발자 모드(설정 탭)에 티어/경험치 테스트 패널을 추가함 — 임의 XP 증감량을 즉시 적용하거나 7개 티어 중 아무 곳으로나 곧장 점프할 수 있어, 실제로 퍼즐을 몇 주씩 풀지 않아도 승급 연출·티어별 색상·여정 지도를 바로 확인할 수 있음.

티어 여정·배지에 쓰던 직접 그린 로우폴리 SVG 기물 아이콘을 디자이너가 제작한 실제 티어 이미지(아이언 폰~그랜드마스터 홀로그램 킹)로 교체하고, 각 이미지의 실제 색을 뽑아 배지 테두리·글로우·진행바 색도 함께 맞춤(특히 마스터·그랜드마스터·다이아몬드가 기존 팔레트와 크게 달라짐). 세부 티어(1~5) 표기를 아라비아 숫자 대신 로마 숫자(I~V)로 바꿈 — 텍스트 라벨과 체크포인트 배지 양쪽 모두. 그랜드마스터 이후의 ★프레스티지 카운터는 1~5를 벗어나는 별도 무한 카운터라 그대로 아라비아 숫자로 둠.

하단 탭바의 퍼즐 탭 아이콘을 Google Fonts Material Symbols의 "extension"(퍼즐 조각 모양)으로 교체함. 외부 웹폰트 CDN 의존을 피하기 위해 퀘스트 탭 아이콘과 동일한 방식으로 공식 SVG 패스 데이터를 번들에 포함해 렌더링함.

**버그 수정**
오프닝·퍼즐 모식도에서 확대/축소 버튼·핀치가 pan은 그대로 두고 zoom만 바꿔, 화면 좌상단(콘텐츠 원점)을 기준으로 배율이 바뀌면서 팬으로 멀리 옮겨온 화면에서는 트리 전체가 화면 밖으로 사라진 것처럼 보이던 문제를 해결함(지점 기준 확대 공식 적용). 오프닝 트리·퍼즐 모식도의 기본 확대 배율도 다들 실제로 편하게 보던 배율(기존 75%)을 새 기준 100%로 재정의함.

설정 탭 내 프로필의 "오프닝별 승률" 목록에서, 영문 오프닝 이름과 통계를 한 줄에 양 끝 정렬로 배치하던 방식이 중첩이 깊어질수록(들여쓰기 누적) 이름 칸 너비가 몇 십 px까지 줄어들어 단어 하나하나가 줄바꿈되며 통계 배지와 겹쳐 보이던 문제를 해결함 — 깊이와 무관하게 항상 안전하도록 이름과 통계를 별도 줄로 분리함.

접속 시 뜨는 업데이트 공지 모달이 v0.0.5 이후로 새 버전 기록을 전혀 보여주지 못하던 문제를 해결함 — 코드 안의 버전 목록이 v0.0.4에서 갱신되지 않은 채 남아 있었다. 재발 방지로 APP_VERSION을 별도 상수로 관리하지 않고 CHANGELOG 배열의 첫 항목에서 그대로 파생하도록 바꿈 — 새 버전을 낼 때 CHANGELOG 배열에 항목 하나만 추가하면 되고, 버전 번호를 두 곳에 맞출 필요가 없어짐.

en passant 판정(epTarget)이 "직전 수의 도착 칸이 4/5랭크인 폰 수"이면 무조건 더블 푸시로 간주해, 폰이 한 칸씩 두 번(예: e2-e3, 그다음 턴에 e3-e4) 나눠 전진해도 앙파상을 허용하던 문제를 해결함 — 직전 수 바로 이전 보드에서 실제 출발 칸을 구해 진짜로 두 칸을 건너뛰었는지 검증하도록 함. 이와 별개로 boardToFen/sansToFen이 캐슬링 권리·앙파상 필드를 실제 수순과 무관하게 항상 "KQkq -"로 고정해 내보내던 문제도 해결함 — 킹/룩이 한 번 움직였다 제자리로 돌아와도 캐슬링 권리는 회복되지 않는데 엔진에는 여전히 합법이라고 알려 불법 캐슬링을 추천/수용할 수 있었고, 실제 앙파상 기회는 늘 "없음"으로 알려 엔진이 놓쳤음. 매 수의 실제 이동 정보(킹/룩 이동, 룩 시작 칸 포획, 진짜 더블 푸시 여부)로 캐슬링 권리·앙파상을 누적 계산해 반영하도록 함(기존 O(n²) 회피 최적화는 그대로 유지).

도감 탭에서 특정 수를 클릭한 직후 1~2초 동안 화면이 좌우로 심하게 흔들리고, 그 와중에 드래그하면 트리 전체가 사라지던(꽤 오래전부터 보고된) 문제를 근본 수정함. 원인은 pan/zoom 상태를 세 곳(centerOn이 시작하는 최대 900ms짜리 비행 애니메이션의 requestAnimationFrame 루프, selectedPath 재중앙 정렬용 150ms setInterval, 드래그/휠/확대버튼 같은 수동 조작)이 서로의 존재를 모른 채 동시에 덮어쓰고 있던 것 — 애니메이션이 매 프레임 부드럽게 보간하는 pan을 150ms interval이 "이미 다 도착한 것처럼" 곧장 스냅해버려 두 값이 150ms마다 서로를 덮어쓰며 튕겼고(흔들림의 정체), 그 상태에서 드래그를 시작해도 애니메이션이 멈추지 않아 다음 프레임이 드래그 결과를 계속 덮어써 화면이 엉뚱한 곳으로 튀었음(사라짐의 정체). 드래그·휠·확대 버튼 등 수동 조작이 시작되는 모든 지점에서 비행 애니메이션을 확실히 취소하고, 150ms interval은 애니메이션이 도는 동안 건너뛰도록 해 셋이 절대 동시에 pan/zoom을 다투지 않게 함.

도감 오프닝 검색 결과에서 두 가지를 고침 — (1) 부모 노드의 이름을 그대로 물려받아 같은 오프닝 이름이 서로 다른 수순 길이에 걸쳐 완전히 똑같은 문자열로 중복 등장하던 것을, 같은 이름 중 수순이 가장 짧은 노드 하나만 남기도록 함. (2) 검색어가 오프닝 이름 맨 앞에 오는 결과와 이름 중간 어딘가에 오는 결과가 뒤섞여 나와 찾으려던 오프닝이 관련 없어 보이는 결과들 사이에 묻히던 것을, 맨 앞에 오는 결과를 모두 먼저 보여준 뒤 중간에 오는 결과를 그 아래에 보여주도록 정렬을 바꿈.

학습 탭의 실시간 수 등급 판정(assignTiers)에 classifyMoveKind·analyzeGame과 동일한 "이미 승부가 기운 위치에서의 자포자기 희생은 탁월한 수로 안 쳐준다"는 완화 규칙이 빠져 있어, 같은 포지션이 게임 리뷰·퍼즐 채점과 다르게 학습 탭에서만 "탁월한 수"로 잘못 표시되던 문제를 해결함(퍼즐 생성 로직에도 같은 누락이 있어 함께 고침 — 이제 네 곳 모두 동일한 기준을 씀).

오프닝 이름 퍼지 매칭(findOpeningPathByFuzzyName, 일일 퀘스트에서 사용)이 "트리 이름이 검색어를 포함"과 "검색어가 트리 이름을 포함"을 한 조건으로 묶어 BFS 첫 매치를 반환해, 세부 바리에이션을 찾아도 그 접두어인 훨씬 상위(일반) 오프닝이 먼저 걸려 반환되던 문제를 해결함(퀘스트 오프닝 풀의 약 1/3에 영향). 트리 쪽이 더 구체적인 매치를 먼저 전부 찾고, 없을 때만 반대 방향으로 한 번 더 찾도록 두 단계로 분리함.

공지 모달 자동 표시 effect가 최초 로드 시점에만 판단해, 로그인(onAuth) 이후 서버에 저장된 dismissedAnnounceVersion 값이 바뀌어도 재평가되지 않던 문제를 해결함. 로그인 시 이 값을 "있으면만" 덮어써 계정이 한 번도 안 닫았으면 게스트 시절 값이 남던 문제도 함께 고침. 로그아웃해도 friendUids/puzzleSolvers/solverNames(친구 해결자 표시용 데이터)가 안 비워져 게스트 화면에 방금 로그아웃한 계정 친구들의 "N명이 풀었습니다" 배지가 남아 있던 문제, dismissedAnnounceVersion이 로그아웃 시 초기화되지 않아 게스트 로컬 저장소에 방금 로그아웃한 계정의 값이 새어 들어가던 문제도 함께 해결함.

알림 벨의 읽음/수락·거절/삭제가 전부 서버 요청 실패를 삼키고 낙관적 UI 업데이트를 되돌리지 않던 문제를 해결함 — 관련 서버 함수들이 성공 여부를 돌려주도록 바꾸고, 실패 시 로컬 오버레이를 지운 뒤 서버 상태와 다시 동기화하도록 함.

도감 오프닝 트리의 형제 수 상한(cap) 체크가 배치 내 모든 새 후보에 그 시점의 형제 수를 한 번만 읽어 적용해, 같은 배치에 비이론 수가 여럿 도착하면 상한을 넘겨 추가될 수 있던 문제를 해결함(장식적 문제, 데이터 손상은 아니었음).

학습 탭 실시간 워커 풀(위 성능 항목)에 생명주기 버그 3건도 함께 고침 — 엔진 프로필을 평가 도중 바꾸면 종료된 워커의 평가 Promise가 영영 안 끝나 그 후보 수의 평가가 멈춰 있던 문제, 워커 풀 부팅이 완전히 실패했을 때 수를 둘 때마다 매번 처음부터 재부팅을 시도해(URL당 최대 4초) 반복적으로 멈추던 문제, 컴포넌트가 언마운트된 뒤 뒤늦게 부팅된 워커가 정리되지 않아 Worker 스레드가 누수되던 문제.

**UI 개선**
내 프로필 카드에서 메인 퀘스트 진척도·푼 퍼즐 정보가 chess.com 대국 기록(분량이 길어 화면을 많이 차지함)보다 아래에 있어 한참 스크롤해야 보이던 것을, 퀘스트·퍼즐 정보를 먼저 보여주고 chess.com 데이터를 그 아래로 내리도록 순서를 바꿈.

티어 여정 지도에서 아직 안 온(잠긴) 구간이 전부 똑같은 회색으로 칠해져 각 티어 고유 색(브론즈 구릿빛, 골드 금빛, 다이아몬드 청록…)이 하나도 안 보이던 것을, 잠긴 상태에서도 그 티어 색을 낮은 채도·밝기로 남기도록 바꿔 스크롤할수록 앞으로 만날 색이 은은하게 미리 보이게 함. 퍼즐 탭 상단 티어 스트립의 구불구불한 지그재그 연결선도 좁은 가로 스트립 안에서는 오히려 어수선해 보인다는 이유로 직선으로 바꿈.

### OpenChess v0.0.5 — 2026/7/14

**보안**
퍼즐(`puzzles`) 테이블의 서버 쓰기 권한이 로그인 여부와 무관하게 사실상 전체 열려 있어, REST를 직접 호출하면 임의 퍼즐의 풀이수·좋아요 수를 원하는 값으로 덮어쓸 수 있었던 문제를 해결함. 두 카운터는 이제 원자적 증가를 보장하는 서버 함수(RPC)로만 바뀌도록 컬럼 단위 권한으로 좁혔고, 누구나(비로그인 포함) 새로 만난 퍼즐을 공유하는 기존 크라우드소싱 방식은 그대로 유지함.

로그인만 하면 다른 유저에게 임의 종류의 알림(칭호 획득, 레벨 업 등)을 위조해 보낼 수 있었던 문제를 해결함 — 알림 종류별로 실제 그 알림이 발생할 수 있는 상황(칭호·레벨 알림은 본인에게만, 친구 요청·수락 알림은 실제 친구 관계가 막 생겼을 때만)인지 서버에서 검증하도록 함.

채팅이 친구 관계와 무관하게 임의 유저에게 전송 가능했던 문제를 해결함 — 지금까지는 채팅 버튼이 친구 목록에만 노출될 뿐 서버가 이를 강제하지 않아, REST를 직접 호출하면 친구가 아닌 사람에게도 메시지를 보낼 수 있었다. 이제 서로 친구(수락됨) 상태일 때만 메시지가 저장되도록 서버에서도 검증함.

**성능**
알림(30초)·친구 요청 배지(30초)·채팅창(3초)·안 읽은 채팅 배지(15초)가 각자 독립적으로 반복 조회하던 방식을 실시간(Realtime) 구독으로 전환함 — 상대방의 행동이 즉시 반영되면서도 평소에는 불필요한 요청이 나가지 않음. 연결이 조용히 끊기는 경우를 대비해 1~2분 간격의 느슨한 안전망 재조회만 남겨둠.

페이지 배경의 기하학적 장식(선·도형)이 이 폴링들을 포함해 화면이 다시 그려질 때마다(자신은 내용이 전혀 바뀌지 않는데도) 매번 다시 계산되던 것을 고쳐, 최초 한 번만 그리도록 함.

### OpenChess v0.0.4 — 2026/7/13

**성능**
"게임 리뷰"(전체 기보 분석) 기능이 여전히 비정상적으로 느려지던 문제를 해결함. v0.0.2에서 분석 전용 병렬 워커 풀을 도입했지만, 그 풀의 첫 자리는 "유휴 상태일 것"이라는 가정 아래 앱 전역에서 공유하는 메인 엔진을 그대로 재사용하고 있었다 — 그런데 집중 학습의 실수 분석처럼 게임 리뷰와 무관하게 같은 엔진 큐에 계속 평가를 밀어넣는 경로가 있어, 이미 보드에 긴 기보나 많은 후보 수가 떠 있던 상태에서 새 게임을 리뷰하면 그 배경 작업 뒤에서 워커 한 자리가 계속 대기해 분석 전체가 느려지고 있었다. 이제 게임 리뷰는 메인 엔진 큐를 전혀 재사용하지 않고 완전히 독립된 전용 워커들로만 풀을 구성해, 다른 화면에서 무슨 작업이 진행 중이든 영향받지 않도록 함.

체스보드에서 수를 두거나 위치를 옮길 때마다 학습 탭 실시간 엔진 평가(평가치 바·수 추천 목록)가 체감상 느려진 문제도 함께 개선함. 이 실시간 평가는 movetime 없이 depth만 주고 있어, 복잡한 포지션에서는 목표 depth에 도달할 때까지 무제한으로 오래 걸릴 수 있었다 — 게임 리뷰·퍼즐 생성처럼 movetime 상한을 둬 depth·시간 중 먼저 도달하는 쪽에서 멈추게 했다. 또한 이 실시간 평가는 후보 수 목록이 채워질 때마다(비이론 수를 엔진으로 보충하며) 자기 자신이 다시 실행되는 구조인데, 재실행될 때마다 이미 평가해 둔 현재 포지션·후보 수까지 처음부터 다시 엔진에 물어보고 있었다(같은 포지션에 대한 완전히 중복된 질의). 포지션이 바뀌지 않는 한 이미 진행 중이거나 끝난 평가 결과를 재사용하도록 캐싱해, 같은 수를 둘러싸고 반복되던 중복 질의를 없앴다(측정상 특정 위치에서 완료까지 걸리는 엔진 요청 수가 약 절반으로, 총 소요 시간도 그만큼 줄어듦).

**버그 수정**
설정 탭의 chess.com 연동 오프닝 승률 트리에서, 모바일처럼 화면이 좁고 들여쓰기가 깊게 중첩된 오프닝일수록 이름이 최대 2줄로 제한된 뒤 말줄임(…)으로 잘려 전체 이름을 알아볼 수 없던 문제를 해결함. title 속성으로 전체 이름을 보여주려던 예전 대안은 모바일 터치 환경에서는 아예 작동하지 않았다 — 줄 수 제한을 없애 이름이 몇 줄이 되든 항상 끝까지 그대로 보이도록 함.

퍼즐(실수 응징하기·우위 점하기·기물 희생하기) 최초 생성이 유저의 "퍼즐 풀기" 클릭이 열어 둔 집중 학습 화면의 effect에 묶여 있어, 생성이 끝나기 전에 화면을 벗어나면(다른 탭으로 이동 등, 특히 로딩이 느린 모바일에서 자주 발생) 이미 다 돌아간 엔진 계산 결과가 그냥 버려지고 다음에 다시 처음부터 생성해야 했던 문제를 해결함. 생성 요청과 진행 상태를 앱 최상단(항상 마운트돼 있어 탭을 옮겨도 사라지지 않는 곳)으로 옮겨, 화면을 벗어나도 생성이 끝까지 이어지고 결과가 저장되도록 함. 아직 생성이 끝나지 않아 곧바로 퍼즐 탭으로 이동할 수 없는 경우에는 예전처럼 아무 반응 없는 버튼 대신 "퍼즐을 생성하는 중입니다" 문구와 진행 게이지를 보여주고, 완료되면 정상적인 "퍼즐 풀기" 버튼으로 바뀜.

퍼즐 모식도에서 실전에 자주 두어지지만 통과할 수 없는 "유혹 수"를 일부러 막힌 가지로 끼워 넣던 기능을 없앰 — 사용자가 그 가지를 실제로 시도해 보면 어디로도 이어지지 않는 리프가 그대로 모식도에 남아 "풀 수 없는 라인이 표시된다"는 혼란만 남겼다. 이제 통과 가능한 수만 분기에 남고, 이미 만들어진 퍼즐에 남아 있던 유혹 수 가지도 렌더링 단계에서 함께 걸러냄.

퍼즐 풀이 화면에서 컴퓨터가 둔 첫 수(직전 실수 수)의 수 체계 아이콘이 진입 시 잠깐(직전 수 재생 애니메이션 동안)만 보이다 애니메이션이 끝나자마자 사라지던 문제를 해결함 — 사용자가 첫 수를 두기 전까지는 계속 그 아이콘이 유지되도록 함.

퍼즐 라인을 몇 수 두어 보다가(아직 풀지 못한 채) "처음부터" 버튼을 누르면, 그 라인에서 이미 드러났던 수들이 모식도에서 도로 가려져(고스트로 되돌아가) 마치 진행 상황이 통째로 사라진 것처럼 보이던 문제를 해결함. 보드 진행 상태와 별개로 "이 세션에서 한 번이라도 실제로 두어 본 수"를 계속 누적해 두고, 모식도에 무엇을 보여줄지는 이 누적 기록(+실제로 해결한 라인) 기준으로만 판단하도록 해, 재시작해도 보드만 처음으로 돌아갈 뿐 이미 들여다본 모식도 내용은 그대로 남도록 함.

(참고) 퍼즐 탭 모식도의 기본 확대 배율은 이미 100%로 되어 있음을 확인함 — 코드를 점검한 결과 75% 등 다른 기본값으로 바뀐 지점을 찾지 못했다.

**기능**
접속 시 업데이트 소식을 알려주는 공지 창을 추가함. README 버전 기록을 바탕으로 개발 용어 없이 사용자 입장에서 쉬운 말로 다시 정리해 보여주고, 모든 버전의 기록을 한 창에서 스크롤로 볼 수 있음. "다시 보지 않기"를 체크한 채 닫으면 그 버전 번호가 계정(로그인 시 서버, 아니면 기기 로컬)에 저장되어 다음 접속부터 안 뜨다가, 새 버전이 나오면 다시 뜸. 체크하지 않고 닫으면 다음 접속 때 다시 보임.

사이트 전체 모식도(도감 오프닝 트리·퍼즐 모식도·개발자 트리 에디터)의 확대/축소 배율을 25%p 단위(25/50/75/100/125/150/175/200%)로만 고정함 — 버튼 클릭뿐 아니라 두 손가락 핀치나 마우스 휠 같은 연속적인 제스처로 나온 값도 항상 가장 가까운 단계로 스냅됨.

도감 탭에서 오프닝을 검색해 선택하면 예전엔 그 위치로 화면이 즉시 순간이동했는데, 지금은 원래 보고 있던 위치에서 목표 위치까지 파란 선을 그어 보여주고 그 선을 따라 매우 빠르게(0.22초) 화면이 이동하는 것처럼 보이는 애니메이션으로 바뀜.

**버그 수정**
도감 탭의 오프닝 트리가 배경에서 계속 더 깊이 채워지는데, 이 작업이 도감 탭 화면 자체에 묶여 있어 다른 탭에 갔다가 돌아오기만 해도(흔한 사용 패턴) 지금까지 쌓인 진행이 통째로 사라지고 처음부터 다시 시작되던 문제를 해결함 — 그래서 늘 얕은 수(약 6수)에서 멈춘 것처럼 보였다. 이 작업을 앱 최상단(항상 마운트돼 있는 곳)으로 옮겨, 어느 탭에 있든 계속 더 깊이(실제 이론 데이터가 있는 곳까지, 확인상 10수 이상) 채워지도록 함.

**기능**
퍼즐에 좋아요 기능을 추가함. 퍼즐 풀이 화면과 퍼즐 목록의 각 카드에 하트 아이콘과 좋아요 수가 함께 표시되며, 하트를 누르면 채워지고(좋아요) 다시 누르면 비워짐(취소). 좋아요 수는 같은 퍼즐을 푼 모든 유저에게 공통으로 합산되어 보이고, 로그인하지 않은 상태에서 하트를 누르면 로그인 창이 뜸.

**UI 개선**
메인 퀘스트 카드가 모바일에서 지나치게 길어 일일 퀘스트까지 한눈에 보이지 않던 문제를 해결함. 챕터를 좌우로 넘겨보던 방식은 넘기기 위해 모든 챕터가 한 줄에 나란히 있어야 했고, 그 여파로 지금 보는 챕터가 짧아도 항상 가장 긴 챕터의 높이에 맞춰 늘어나 있었다 — 이제 챕터를 아코디언(접기/펼치기) 방식으로 세로 나열해, 기본으로는 지금 진행 중인 챕터만 펼쳐지고 나머지(완료했거나 아직 잠긴 챕터)는 제목 줄만 보이도록 접혀 있어 전체 높이가 훨씬 짧아짐. 완료한 챕터는 제목 줄을 눌러 언제든 다시 펼쳐볼 수 있음. 또한 CHAPTER 2처럼 한 챕터 안에 하위 챕터가 여러 개(1.e4·1.d4·1.c4·1.Nf3 응수)인 경우, 예전엔 순서 없이 아무 응수나 먼저 풀 수 있었는데 이제 챕터끼리와 마찬가지로 앞 번호 응수부터 순서대로 클리어해야 다음 응수가 열림.

설정 탭의 내 프로필 카드에 메인 퀘스트 진척도(완료한 챕터 수·맞힌 문제 수, 진행률 막대)와 지금까지 푼 퍼즐 목록(카드 형태로 오프닝별 정렬, 좋아요 표시 포함)을 추가함. 푼 퍼즐 카드를 누르면 그 퍼즐 풀이 화면으로 바로 이동해 다시 볼 수 있음.

### OpenChess v0.0.3 — 2026/7/13

**기능**
도감 탭의 오프닝 수 트리를 나침반형(1.e4/1.d4/1.c4/1.Nf3이 각각 동서남북 네 방향으로 뻗어나가는) 레이아웃으로 전면 재설계함. 트리 정중앙에는 회로 칩 장식을 두어 네 갈래가 여기서 뻗어나간다는 느낌을 주고, 칭호 탭에 있는 13개 대표 오프닝(Italian Game, Ruy Lopez, Scotch Game, Sicilian Defense, Caro-Kann Defense, French Defense, Vienna Game, King's Gambit, Benoni Defense, Catalan Opening, Grünfeld Defense, London System, Queen's Gambit)만 점선 테두리로 하나의 영역으로 묶어 표시함. 영역 안의 더 구체적인 하위 바리에이션은 새 영역을 만들지 않고 그대로 상위 오프닝 영역에 포함되며, 영역 이름은 바깥 여백에 화살표와 장식체 글씨로 표기해 블록과 겹치지 않게 함.

**안정성**
트리가 배경에서 계속 자라는(비동기 로딩) 동안 이미 그려진 블록들이 계속 흔들리거나 위치가 튀던 문제를 근본적으로 해결함 — 형제 순서·leaf 좌표·나침반 팔의 기준점·자식이 있는(internal) 노드의 위치를 한 번 계산되면 다시 바뀌지 않도록 모두 캐싱하고, 위치가 바뀔 때 걸려 있던 짧은 슬라이드 애니메이션을 제거해(반복되는 미세 재조정이 애니메이션과 겹치며 화면이 계속 미끄러지는 것처럼 보였음) 순간 이동하도록 바꿈. 트리 로딩 초반 잠깐(약 3.2초) 재배치가 몰리는 구간은 화면을 가려 사용자가 그 과정을 보지 않게 하고, 화면을 드래그·확대할 때 렉이 걸리던 문제는 우선순위 계산용 Map을 pan/zoom이 바뀔 때마다 다시 만들던 것을 고쳐 해결함. 칭호 점선 영역이 배경 로딩으로 계속 커지다 일정 크기를 넘으면 사라지던 문제도 크기 상한을 없애 해결함.

**UI**
수 블록을 클릭하면 열리는 수 설명 카드가 모식도의 확대/축소 트랜스폼 안에 함께 있어, 축소하면 카드가 화면 밖으로 잘리고 확대하면 카드도 같이 커지던 문제를 해결함 — 카드를 트랜스폼 바깥(화면 좌표계)으로 분리해, 카드 자신의 크기는 항상 모식도 창 크기에 비례한 값으로 고정하고 앵커(수 블록)의 화면상 위치만 pan/zoom을 반영해 따라가도록 함.

### OpenChess v0.0.2 — 2026/7/12

**성능**
특정 대국의 "게임 리뷰"(전체 기보 분석) 기능이 기보가 길어질수록 끝없이 느려지다 멈춘 것처럼 보이던 문제를 해결함. 원인은 워커 하나가 모든 포지션을 순서대로 평가하던 구조라 총 분석 시간이 포지션 수에 정비례해 늘어난 것으로, depth·탐색 시간은 그대로 둔 채(정확도 손실 없이) 같은 엔진 프로필의 워커를 여러 개(Stockfish 18 Lite 4개, Stockfish 16 Full 2개) 띄워 포지션들을 나눠 동시에 계산하도록 바꿈. 이어서 교차 출처 격리(Cross-Origin-Opener/Embedder Policy) 헤더를 배포 설정(Vercel)과 로컬 개발 서버에 추가해 SharedArrayBuffer 기반 멀티스레드 Stockfish를 활성화, 실시간(집중 학습 등) 평가에 쓰는 메인 엔진이 활성 포지션 하나를 여러 CPU 코어로 동시에 탐색하도록 함(교차 출처 격리가 안 되는 구형 브라우저 등에서는 자동으로 기존 단일 스레드 빌드로 폴백). COEP는 require-corp 대신 credentialless를 적용해, CORP 헤더가 없는 외부 이미지(구글 프로필 사진 등) 로딩이 깨지는 부작용을 피함.

### OpenChess v0.0.1 — 2026/7/11

**UI**
모바일에서 상단 UI가 잘리는 문제를 해결하고 상단 UI를 최적화함. 학습 탭과 퍼즐 탭을 제외한 탭에서는 마스코트 이미지를 모두 제거해 UI를 정리하고, 로그인 창에는 마스코트 대신 OpenChess 로고를 표시함. 프로필과 집중 학습 모드에 나오는 모든 대국에 타임 컨트롤(래피드/블리츠/불릿)을 표기하고, 레이팅 증감폭은 게임 결과 뒤 소괄호로 표기함. 마스터 대국이 표시되지 않던 문제를 해결하고(불러오는 중에는 "마스터 대국 기록 탐색 중..."으로 표기), 학습 탭 메인 체스보드 크기를 키우고, 수 체계 아이콘이 계산되기 전에는 3-dot 타이핑 인디케이터 스타일의 바운스 애니메이션을 보여주도록 함. 평가치에 따라 제안 화살표의 두께·투명도가 달라지던 UI가 적용되지 않던 문제를 고치고, 기본 보드 스킨 테두리에 금색 광택 효과를 적용해 사이트 전체 미니 체스보드에도 반영, 기물을 하단 블록 기준으로 정렬하도록 좌표 정렬 문제를 해결함. 도감 탭에서는 수 블록 주변에 오프닝 이름을 표시하고 채택률 대신 책 아이콘을 좌상단에 배치, 퍼즐 탭에서는 퍼즐 블록이 하단 탭에 가리던 문제와 모바일에서 추천 퍼즐이 안 뜨던 문제를 해결하고 모식도 크기를 75%에서 100%로, 2페이지에도 미니 체스보드를 추가해 블록 클릭 시 애니메이션 재생이 되도록 함. 상점 탭은 보유 코인 UI를 축소해 우상단에 배치하고 기물 스킨은 "흑 나이트" 한 기물만, 보드 스킨은 8×8 이미지로 보여주도록 하고, 설정 탭은 내 프로필에 칭호를 이름 위에, 레이팅 변동·정확도를 집중 학습 모드와 통일해 표기함. 친구 기능은 채팅 버튼을 아이콘으로 바꾸고, 친구 삭제 버튼은 프로필 클릭 후 우상단에만 노출되도록 함.

**UX**
집중 학습 모드에서 자주 둔 다음 수를 클릭하면 그 수의 집중 학습 모드로 바로 이동하고, 다른 수의 집중 학습 모드를 거쳐 나갈 때는 체스보드에 해당 수순이 표시되도록 개선함. 도감 탭의 오프닝 수 트리는 첫 위치를 1.e4로 정렬하고 하위 라인을 계속 생성하도록 하며, 수 설명 창이 X 버튼으로 안 닫히던 문제와 오프닝 이름 클릭 시 집중 학습 모드로 이동하지 않던 문제를 해결함. 검색 기능은 버튼을 누르지 않아도 입력값에 따라 실시간으로 프로필을 띄우도록 하고, 프로필 클릭 시 하단 정보 확인이 막히던 스크롤 문제를 해결함. chess.com 연동 시 데이터가 많은 계정의 로딩이 느리던 문제를 보완하기 위해 정보를 순차적으로 불러오는 즉시 표기하고, 최근 대국 기록도 최신순으로 불러오도록 함.

**시스템**
framer-motion 의존성을 추가해 로그인/회원가입 모달에 배경 페이드 및 스프링 인 패널 애니메이션을 적용했고, Claude Code용 ui-ux-pro-max 디자인 스킬을 벤더링해 이후 작업에서 디자인 스타일·색상·타이포그래피·UX 가이드라인을 참고할 수 있게 함.

### OpenChess v0.0.0 (Beta) — 2026/7/10

**기능**
기초적인 웹사이트의 구조와 틀은 모두 갖추었고, 웹사이트의 핵심 기능인 학습 탭과 퍼즐 탭은 거의 완성 단계에 도달, 퀘스트 탭과 도감 탭은 전반적인 수정이 필요하고, 상점 탭 및 설정 탭은 미완성 상태, 그 외 검색 기능, 친구 기능, 채팅 기능, 알림 기능 역시 조금 발전이 필요함. 향후 베타테스팅을 고려하여 개발자 기능의 편의성을 조금 개선할 것.

**UI**
학습 탭을 제외하면 심각한 상태, 여전히 필요없는 텍스트가 너무 많고, 비율이 왜곡되거나 레이아웃이 부자연스러운 부분이 꽤 있으며, 모바일 비율에서는 심각한 불편함을 느낄 정도로 레이아웃이 컷오프되는 현상도 발생함.

**UX**
UI에 비해 비교적 양호한 편이기는 하나 여전히 만족스러울 정도로 부드럽고 깔끔한 느낌은 아님, 특히 웹사이트 전반에 사용되는 체스보드의 착수 UX와 기물 움직임 애니메이션을 가장 먼저 다듬어두면 앞으로 유용할 것으로 보임.

**시스템**
개발 언어는 JavaScript, 전반적인 개발 과정은 Claude Code의 Sonnet 5.0, Opus 4.8, Fable 5로 진행됨, 디자인은 Flaticon, Google Fonts를 활용하였고, 일부 이미지는 직접 디자인함. openchess.kr 도메인과 함께 각 탭을 하나의 경로(/learn, /book, /puzzle, /quest, /store, /setting)로 지정하여 구분하였고, 1차적인 취약점 검사를 통해 일반적인 보안 기준을 충족함. Github Repository에서 개발한 PR을 Merge하면 Vercel에서 자동으로 deploy하는 방식, Supabase를 통해 서버 데이터를 저장하고, OpenChess 자체 계정과 Google OAuth 서비스를 통한 Google 계정을 연동할 수 있게 함.
