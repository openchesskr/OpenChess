# 버그 기록 (Bug Log)

OpenChess 개발 과정에서 발견된 모든 버그를 기록한다. 규칙은 `CLAUDE.md`의 "버그 관리 규칙"을 따른다.

- **P1 · 치명**: 즉시 수정 + 재발 방지 안전장치 필수
- **P2 · 주요**: 기록 + 보고, 승인 후 수정
- **P3 · 경미**: 기록 + 보고

## 현황

| ID | 등급 | 제목 | 상태 | 발견일 |
|---|---|---|---|---|
| BUG-021 | P3 | 나이트 레이스 서버 거리 계산이 6수에서 멈춰 par 7 라운드가 실시간 대전에선 안 나옴 | 수정 완료 | 2026-09-26 |
| BUG-020 | P1 | 나이트 레이스 위협 칸 판정이 실제 체스와 달라 "답이 없어 보이는" 라운드가 나옴 | 수정 완료 | 2026-09-26 |
| BUG-019 | P2 | 채팅 목록이 전체 대화 중 최근 메시지 200개로만 만들어져 오래된 대화방·안 읽은 수가 빠짐 | 발견(수정 결정 필요) | 2026-09-26 |
| BUG-018 | P1 | 메시지 300개가 넘는 대화에서 최신 메시지가 안 보이고, 보낸 메시지가 보내자마자 사라짐 | 수정 완료 | 2026-09-26 |
| BUG-017 | P3 | 숫자로 끝나는 단어에 조사가 항상 받침 없는 쪽("#10가")으로 붙음 | 수정 완료 | 2026-09-26 |
| BUG-016 | P2 | 집중 분석 "내 최근 대국"의 레이팅 증감에 변형 체스(Chess960 등) 대국이 섞여 틀린 값이 나옴 | 수정 완료 | 2026-09-26 |
| BUG-015 | P1 | 개발자 모드로 추가한 이론 수가 도감에서 이론 수로 표시되지 않음 | 수정 완료 | 2026-09-26 |
| BUG-014 | P3 | 도감 트리 형제 순서가 데이터 도착 순서에 따라 달라짐 | 수정 완료 | 2026-09-25 |
| BUG-013 | P2 | 도감 탭을 열 때마다 약 2초 멈춤 + 3.2초 강제 대기, 로딩 중 레이아웃 재계산으로 반복 멈춤 | 수정 완료 | 2026-09-25 |
| BUG-012 | P2 | 도감 트리 블록·라벨 겹침과 연결선의 블록 관통 | 수정 완료 | 2026-09-25 |
| BUG-011 | P2 | 도감 트리 수 카드가 화면 밖으로 잘림(데스크톱 위쪽, 모바일 왼쪽) | 수정 완료 | 2026-09-25 |
| BUG-010 | P2 | 기기의 "애니메이션 줄이기"가 켜져 있으면 플레이 탭 버튼 보드 등 사이트 애니메이션이 멈춤 | 수정 완료 | 2026-09-25 |
| BUG-009 | P2 | 기기의 "애니메이션 줄이기"가 켜져 있으면 수 등급 이펙트가 전혀 뜨지 않음 | 수정 완료 | 2026-09-25 |
| BUG-008 | P3 | 수 등급 이펙트 이름표 글자가 알약 가운데에 오지 않음 | 수정 완료 | 2026-09-25 |
| BUG-007 | P3 | 설정 탭 문구에 "국면" 용어가 남아 있음 | 수정 완료 | 2026-09-25 |
| BUG-006 | P2 | 보드 수 등급 이펙트가 끝나도 칸에 겹겹이 남음(형제 key 중복) | 수정 완료(배포 전) | 2026-09-25 |
| BUG-005 | P3 | 무한 체크메이트 버튼 장면이 메이트가 아닌데 메이트로 표시됨 | 수정 완료 | 2026-09-25 |
| BUG-004 | P2 | 무한 체크메이트 게임이 시작 직후 동작하지 않음(카운트다운이 포지션 로딩보다 먼저 끝남) | 수정 완료 | 2026-09-24 |
| BUG-003 | P2 | 러시아워 봇 대전에서 봇이 못 푸는 라운드는 결과가 최대 2분 늦게 뜸 | 수정 완료 | 2026-09-24 |
| BUG-002 | P2 | AnimatedMove·FocusPanel의 조건부 훅 호출 | 수정 완료 | 2026-09-24 |
| BUG-001 | P1 | 미니게임 랜덤 매칭을 누르면 대기열 대신 곧장 패배 | 수정 완료 | 2026-09-24 |

---

## 상세 기록

### BUG-021 · [P3] 나이트 레이스 서버 거리 계산이 6수에서 멈춤
- **상태**: 수정 완료 (v0.5.7)
- **발견일**: 2026-09-26 (BUG-020 서버 쪽 수정 중 발견)
- **위치**: `supabase-setup.sql` `knight_distance`
- **증상**: BFS가 `dist < 6`에서 멈춰 7수 이상이면 "경로 없음"(null). 4·5라운드 규격(par 6~7)의 par 7 라운드가 실시간 대전에선 절대 만들어지지 않았다(봇·혼자 플레이는 클라이언트 생성이라 영향 없음). 라운드 종료 타이브레이커의 남은 거리도 7수 이상이면 99로 뭉개졌다.
- **수정 내용**: 상한을 64로(BFS는 방문 배열로 스스로 끝남)
- **검증**: 로컬 PostgreSQL 16에서 5라운드 생성 시 par 7 라운드가 나오는 것 확인

### BUG-020 · [P1] 나이트 레이스 위협 칸 판정이 실제 체스와 달라 "답이 없어 보이는" 라운드가 나옴
- **상태**: 수정 완료 (v0.5.7)
- **발견일**: 2026-09-26 (사용자 제보 — 스크린샷: 목표 e5, 흑 룩 b4·f4, 흑 비숍 d2, 백 나이트 h2)
- **등급 근거**: 게임 규칙 판정 오류(P1 기준)
- **위치**: `src/App.jsx` `knightDangerFor`·`knightAttackedSquares`(→ `src/lib/knightRace.js`로 이동), `supabase-setup.sql` `knight_danger`·`knight_attacked_squares`·`_knight_gen_round`
- **증상**: 나이트가 갈 수 있는 f1·f3·g4가 모두 룩 f4의 공격 칸이라 "답이 없는" 포지션으로 보였다. 게임은 f1을 안전 칸으로 취급해 f1을 거치는 경로로 라운드를 만들었다(규칙상으론 풀렸지만 체스와 달라 아무도 그 길을 믿을 수 없음). 또 룩·비숍의 공격선이 다른 기물을 뚫고 보드 끝까지 이어져, 막혀 있어야 할 칸도 위협 칸이었다.
- **근본 원인**: ① 위협 칸 계산이 "목표 칸 기준 점대칭 반사점이 보드 안인 칸"만 셌다(실시간 대전의 두 진영 위협 칸을 대칭으로 맞추려던 필터) — f1의 반사점 d9가 보드 밖이라 빠짐 ② 공격선이 기물에 막히지 않음
- **수정 내용**: 위협 칸을 실제 체스와 같게 — 모든 공격 칸을 세고, 공격선은 남아 있는 기물(색 무관)에 막힌다(막은 칸은 공격 = 보호). 대칭 공정성은 이미 있던 "흑 쪽 par도 같을 때만 채택" 검사로 보장. par는 "잡지 않고도 확실히 가는 길"(위협 칸 + 모든 기물 칸을 벽으로) 기준으로 재서, 생성된 라운드엔 규칙대로 반드시 통하는 경로가 있다(기물을 잡아 더 빨리 가는 길은 보너스). 봇도 같은 기준으로 걷는다. 규칙을 `src/lib/knightRace.js`로 모아 혼자·봇·실시간 화면과 검사 스크립트가 한 코드를 쓰고, 서버 SQL을 같은 규칙으로 맞췄다. 함께 5라운드를 사용자 요청대로 "반드시 상대 퀸이 나오는 매우 어려운 라운드"(기물 5쌍 중 1쌍 퀸, par 6~8, 기물 없을 때보다 2수 이상 돌아가기, 최단으로 가는 첫 수가 하나뿐)로 바꿨다.
- **재발 방지 안전장치**: `scripts/check-knight-rounds.mjs`(prebuild, `npm run check:knight-rounds`) — 위협 칸을 독립 구현과 비교(막힘·퀸 포함), 제보 포지션 회귀(f1 위협·답 없음), 공격선 막힘/기물 잡은 뒤 선 열림, 시드 고정 라운드 240개(혼자·실시간 × 1~5라운드)가 전부 규칙대로 풀리는지(기물 잡기까지 고려한 상태 BFS), 5라운드 퀸 필수
- **검증**: 검사 스크립트가 옛 규칙에서 5건 실패 → 새 규칙에서 통과. 로컬 PostgreSQL 16에 서버 함수를 올려 라운드 60개(1~5라운드 × 12)를 만들어 JS 규칙으로 전부 대조 — 위협 칸 일치, par 일치, 전부 풀림, 5라운드 전부 퀸. 5라운드 서버 생성 평균 0.7초(라운드 시작 3초 카운트다운 안). `npm run build` 통과. **배포 시 `supabase-setup.sql` 재실행 필요**(실시간 대전 규칙)

### BUG-019 · [P2] 채팅 목록이 최근 메시지 200개로만 만들어짐
- **상태**: 발견 (수정 결정 필요 — 채팅 강화 작업에서 대화방 요약 RPC로 함께 해결 제안)
- **발견일**: 2026-09-26 (채팅 기능 강화 검토 중 발견)
- **위치**: `src/App.jsx` `chatFetchAll`(`order=created_at.desc&limit=200`), `ChatsModal` 목록·안 읽은 수 계산
- **증상**: 나와 관련된 전체 메시지 중 최근 200개로만 대화방 목록을 만든다. 한두 명과 대화를 많이 하면, 그보다 오래 전에 마지막 메시지가 오간 대화방이 목록에서 사라지고, 방별 안 읽은 수도 200개 안에 든 것만 센다.
- **근본 원인**: "대화방별 마지막 메시지 + 안 읽은 수"를 서버에서 묶지 않고, 메시지 원본 몇 개로 클라이언트가 추정함
- **수정 방향(제안)**: 대화 상대별 마지막 메시지·안 읽은 수를 돌려주는 SECURITY DEFINER RPC(예: `chat_rooms`)로 교체

### BUG-018 · [P1] 메시지 300개가 넘는 대화에서 최신 메시지가 안 보이고, 보낸 메시지가 사라짐
- **상태**: 수정 완료 (v0.5.7)
- **발견일**: 2026-09-26 (채팅 기능 강화 검토 중 코드에서 발견)
- **등급 근거**: 사용자에게는 메시지 손실로 보이고, 해당 대화에서 채팅을 사실상 쓸 수 없음 — 애매해 한 단계 높게 P1
- **위치**: `src/App.jsx` `chatFetch`, `ChatPanel`의 `load`
- **증상**: 한 대화의 메시지가 300개를 넘으면 그 뒤 메시지가 화면에 안 나온다. 새 메시지를 받거나 보낼 때마다 대화를 다시 불러오므로, 보낸 메시지가 보내자마자 사라지고, 300개 밖의 안 읽은 메시지는 읽음 처리되지 않아 안 읽은 배지가 계속 남는다.
- **재현 방법**: 1. 두 계정이 메시지를 300개 넘게 주고받는다 2. 대화를 다시 연다 → 가장 오래된 300개만 보임 3. 새 메시지를 보낸다 → 잠깐 뒤 사라짐
- **근본 원인**: `order=created_at.asc&limit=300` — 오름차순에 limit을 걸어 "가장 오래된" 300개를 받음
- **수정 내용**: 최신 300개를 `order=created_at.desc,id.desc`로 받아 화면 순서로 뒤집음. 함께, 긴 대화를 처음 열 때 안 읽은 메시지마다 PATCH를 동시에 최대 300개 보내던 `chatMarkRead`를 `id=in.(...)` 100개 단위 요청으로 묶음
- **재발 방지 안전장치**: `scripts/check-latest-rows.mjs`(prebuild, `npm run check:latest-rows`) — src의 PostgREST 조회에서 "asc + limit" 조합을 막는다(offset 페이지네이션 전체 스캔과 `asc-limit-ok` 주석을 단 의도된 곳만 허용)
- **검증**: 검사 스크립트가 수정 전 코드에서 실패(`chatFetch`) → 수정 후 통과. `npm run build` 통과. 실제 Supabase로 300개 넘는 대화를 만들어 보는 확인은 이 세션 환경에 DB 접근이 없어 하지 못함 — 배포 후 긴 대화 하나를 열어 최신 메시지가 보이는지 확인 필요

### BUG-017 · [P3] 숫자로 끝나는 단어에 조사가 항상 받침 없는 쪽으로 붙음
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-26 (알림 문구에 퍼즐 번호를 넣다가 발견)
- **위치**: `src/App.jsx` `hasBatchim`
- **증상**: `josaIGa("퍼즐 #10")`이 "퍼즐 #10가"가 됨(읽으면 "십이"라 "#10이"가 맞음)
- **근본 원인**: 한글 완성형 범위 밖 글자는 전부 "받침 없음"으로 처리
- **수정 내용**: 숫자로 끝나면 읽는 소리로 판단(0 영·1 일·3 삼·6 육·7 칠·8 팔은 받침 있음)
- **검증**: 알림 문구 "내가 만든 퍼즐 #10이 / #12가 오늘의 퍼즐로 선정됐어요!"

### BUG-016 · [P2] 집중 분석 "내 최근 대국"의 레이팅 증감에 변형 체스 대국이 섞임
- **상태**: 수정 완료 (v0.5.6, 사용자 승인)
- **발견일**: 2026-09-26 (대국 요약 알림의 레이팅 증감을 만들다가 발견)
- **위치**: `src/App.jsx` FocusPanel `ratingChanges = computeRatingChanges(chesscom.games)`, `src/lib/chesscom.js` `computeRatingChanges`
- **증상**: 같은 시간 규정(예: 블리츠)으로 Chess960 같은 변형 체스도 두는 사용자는, 표준 대국의 "(+8)" 같은 증감이 변형 대국 레이팅과 비교돼 크게 틀린 값으로 보일 수 있음. 프로필 "최근 대국"은 목록을 표준 체스로 먼저 걸러서 괜찮음
- **재현 방법**: 1. 블리츠 표준 대국과 블리츠 Chess960 대국을 번갈아 둔 계정 연동 2. 도감/분석에서 그 수순의 집중 분석 → 내 최근 대국의 레이팅 증감 확인
- **근본 원인**: `computeRatingChanges`가 timeClass로만 묶고 rules(변형 여부)를 보지 않음(chess.com은 변형마다 레이팅 풀이 따로)
- **수정 내용**: `computeRatingChanges`가 표준 체스(rules "chess") 대국끼리만 비교. 대국 요약 알림(`ratingDeltaOf`)도 같은 규칙
- **재발 방지 안전장치**: `scripts/check-cc-game-toast.mjs`(prebuild)에 변형 대국이 섞인 목록의 증감 검사 추가
- **검증**: 수정 전 검사 실패(−493, 변형 대국과 비교) → 수정 후 통과(+3)

### BUG-015 · [P1] 개발자 모드로 추가한 이론 수가 도감에서 이론 수로 표시되지 않음
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-26 (사용자 제보 — 오래전부터 반복된 문제)
- **등급 근거**: 이론 수 판정(수 등급) 오류 — 판정 오류는 P1 기준, 애매하면 한 단계 높게
- **위치**: `src/App.jsx` `assignTiers`·`forceKindFor`, 도감 트리 로더(`useOpeningTreeAuto`)·배치(`computeDexLayout`)·데이터 입히기, 수 카드(`DexMoveBlock`), 분석 탭 후보 병합
- **증상**: 분석 탭 집중 분석의 "이론 수로 등록" 등으로 추가한 이론 수가 도감 트리에는 나타나지만 블록 아이콘·색과 수 카드에서 이론이 아닌 수(계산 중)로 보임. 강제 등급을 체크 기호 없이 저장한 수(예: "Bb5" ↔ 실제 "Bb5+")도 같은 증상.
- **재현 방법**: 1. 개발자 콘텐츠에 treeAdds `{"e4 e5 Nf3": [{san:"Qe7", theory:true}]}`(forceKind 없이) 2. 도감에서 2...Qe7로 이동 3. 블록·카드에 이론 아이콘이 없음(수정 전 브라우저 재현: badge=none, card=none)
- **근본 원인**: 이론 여부를 정하는 기준이 여러 곳에 흩어져 서로 달랐다. ① 트리에 넣을지는 `isBookMoveAt`(개발자 추가 이론 포함)으로 정하는데, 아이콘을 정하는 `assignTiers`는 스냅샷 book 플래그와 강제 등급만 봤다 → 트리에는 있는데 등급은 비이론. ② 개발자 추가 수를 후보 목록에 넣는 코드 5곳이 제각각이었고, 그중 트리 로더·수 카드는 `{ san, dev: true }`로 이론 여부 없이 넣었다. ③ `forceKindFor`는 표기가 글자 그대로 같을 때만 찾아, 체크·메이트 기호 차이로 강제 등급이 조용히 무시됐다. ④ 도감 트리 로더는 앱 시작 때 한 번만 돌아, 서버의 개발자 콘텐츠가 늦게 도착하거나 수를 새로 추가해도 다시 펼치지 않았다.
- **수정 내용**: 이론 판정을 `isBookMoveAt` 하나로 통일(`assignTiers`도 사용). 개발자 추가 수 병합은 `devAddEntry`/`mergeDevAdds` 한 곳으로. `forceKindFor`는 표기 그대로 → 기호 뗀 것 → +/# 붙인 것 순으로 조회. 트리 로더는 contentVer가 바뀌면 다시 펼치고, 트리 구조는 스냅샷 + 현재 개발자 추가 수로 바로 만든다(늦게 오는 treeData에 의존하지 않음).
- **안전장치**: `scripts/check-theory-merge.mjs`(prebuild). 개발자 추가 수를 `devAddEntry` 없이 끼워 넣는 코드, `assignTiers`가 `isBookMoveAt`을 쓰지 않는 것, `forceKindFor`가 기호를 떼고 다시 찾지 않는 것을 막는다. 수정 전 코드에 돌려 8곳 모두 검출 확인.
- **검증**: 브라우저에서 개발자 콘텐츠를 앱 시작 뒤에 불러오는 상태로 확인 — (A) 이론 등록만 한 수, (B) 도감 편집으로 추가(강제 등급 포함), (C) 강제 등급 표기에 체크 기호가 없는 수 모두 블록·카드가 "이론적인 수". 이론에서 삭제(unbook)한 수는 트리·검색에서 빠짐. `npm run build` 통과.

### BUG-014 · [P3] 도감 트리 형제 순서가 데이터 도착 순서에 따라 달라짐
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-25 (도감 트리 개편 중 코드 검토)
- **위치**: `src/App.jsx` OpeningSchematic(개편 전) 순서 캐시
- **증상**: "채택률이 높은 수가 가운데" 규칙이 형제 순서를 처음 본 순간의 채택률로 고정했는데, 구조는 채택률 조회보다 먼저 만들어져 대개 모든 채택률이 0인 상태로 고정됐다. 조회가 먼저 끝난 갈래만 채택률 순이 돼, 세션마다 순서가 달라질 수 있었다.
- **수정 내용**: 형제 순서를 스냅샷(SNAP)에 기록된 순서로 정하고, 가운데부터 좌우로 번갈아 배치한다. 늦게 도착하는 데이터와 무관하게 항상 같은 순서가 된다.
- **검증**: 같은 구조로 여러 번 배치해도 좌표가 같음(레이아웃 재계산 1회, 이후 캐시 적중)

### BUG-013 · [P2] 도감 탭을 열 때마다 약 2초 멈춤 + 3.2초 강제 대기, 로딩 중 레이아웃 재계산으로 반복 멈춤
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-25 (사용자 요청 "더 빠르게 트리를 그리고"에 따른 측정)
- **위치**: `src/App.jsx` OpeningSchematic, useOpeningTreeAuto
- **증상**: 도감 탭을 누르고 트리가 보이기까지 약 7초(개발 서버 기준). 채택률이 도착할 때마다(220ms 간격) 트리 전체 배치를 다시 계산해, 15초 동안 28번, 한 번에 최대 0.86초씩 화면이 멈춤. 가만히 있어도 150ms마다 다시 그림.
- **근본 원인**: ① 흔들림을 가리려고 트리를 열 때마다 3.2초 동안 "불러오는 중…"만 보여줌 ② 첫 렌더에 그릴 범위(컬링)가 없어 블록 3,300여 개·라벨 1,500개를 한꺼번에 DOM으로 만듦 ③ 좌표 계산이 채택률·이름 같은 부가 데이터와 한 useMemo에 묶여 매번 전체 재계산, 라벨 배치는 O(라벨×블록) ④ 구조 확장 큐가 매번 큐 전체를 훑어 O(노드²) ⑤ 중심 맞추기·선택 고정이 150ms마다 같은 값으로 setPan
- **수정 내용**: 좌표 계산을 구조만의 함수로 분리(`src/lib/dexTreeLayout.js`)하고 모듈 캐시와 앱 유휴 시간 미리 계산을 더함. 부가 데이터는 가벼운 O(노드) 패스로만 입힘. 첫 렌더부터 컬링. 3.2초 대기 제거. 구조 큐는 FIFO. 중심 맞추기는 크기가 바뀔 때만, 선택 고정은 값이 달라질 때만 반영.
- **안전장치**: `scripts/check-dex-tree-layout.mjs`에 배치 계산 시간 예산(1.5초) 검사 포함(prebuild)
- **검증**: 개발 서버에서 탭을 연 뒤 트리 표시까지 약 7초 → 0.46초. 레이아웃 재계산은 로딩 중 28회 → 1회(캐시).

### BUG-012 · [P2] 도감 트리 블록·라벨 겹침과 연결선의 블록 관통
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-25 (사용자 요청 "겹침 없이"에 따른 측정)
- **위치**: `src/App.jsx` OpeningSchematic 배치 계산(개편 전)
- **증상**: 블록끼리 겹침 5곳, 라벨이 블록을 덮는 곳 33곳, 연결선이 다른 블록을 가로지르는 곳 192곳. 부모·자녀 평균 거리 3,157px로, 자식이 부모와 멀리 떨어져 흩어짐.
- **근본 원인**: ① 최소 간격(93)이 블록 폭(98)보다 좁아, 링이 가로로 흐르는 팔 가운데에서 이웃 블록이 실제로 겹침 ② 라벨 지터(최대 ±133)가 링 간격을 넘어 옆 링 블록과 부딪힘 ③ 라벨을 블록 아래로 옮긴 뒤에는 블록과의 충돌을 다시 확인하지 않음 ④ 한 링의 노드를 반원 전체에 균등하게 흩어, 자식이 부모에게서 멀어지고 곧은 연결선이 이웃 블록을 가로지름
- **수정 내용**: 새 배치(`src/lib/dexTreeLayout.js`). 자식을 부모 각도 바로 밑에 모으고 붐빌 때만 밀어냄(순서 유지 + 최소 간격 조건의 1차원 최적 배치, PAV). 간격은 블록 모양에 맞춰 방향별로 계산. 링 간격은 앞으로의 필요를 내다보고 정함(규칙 "깊어질수록 줄지 않음" 유지). 지터는 여유 안에서만. 계산 뒤 공간 격자로 전체 겹침을 검사해 없앰. 라벨은 블록·라벨을 모두 피하는 자리를 찾음. 연결선은 링 사이 빈 띠를 도는 회로 배선형.
- **안전장치**: `scripts/check-dex-tree-layout.mjs`(prebuild). 실제 스냅샷으로 배치해 블록·라벨 겹침, 선 관통, 링 간격 규칙을 검사하고, 어기면 빌드 실패. 간격 상수를 일부러 망가뜨려 실패(겹침 309곳 검출)하는 것까지 확인.
- **검증**: 브라우저 측정 결과 블록 겹침 0, 라벨 겹침 0, 선 관통 0, 부모·자녀 평균 거리 3,157px → 1,915px, 트리 최대 반경 25.7k → 21.1k

### BUG-011 · [P2] 도감 트리 수 카드가 화면 밖으로 잘림(데스크톱 위쪽, 모바일 왼쪽)
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-25 (도감 트리 개편 중 확인, 개편 전 코드에서도 동일하게 재현)
- **위치**: `src/App.jsx` OpeningSchematic `frozenCard`
- **증상**: 수 블록을 누르면 뜨는 2배 크기 카드가 데스크톱에선 위쪽 약 300px, 모바일에선 왼쪽 약 50px이 화면 밖으로 잘림
- **근본 원인**: 카드 높이를 460px(×2)로 어림(실제 약 290px)해 위치를 화면 맨 위(8px)로 붙였다. 또 transform-origin(꼬리 자리)을 기준으로 2배 키운다는 점을 좌표에 반영하지 않아, 키우는 순간 카드가 기준점 바깥으로 밀려났다.
- **수정 내용**: 카드를 그린 직후(그리기 전) 실제 크기를 재서, 꼬리가 블록을 가리키면서 카드 전체가 화면 안에 들어오게 위치·꼬리 자리·배율을 다시 계산한다(모자라면 배율 축소, 내용이 바뀌면 ResizeObserver로 재계산). 확대 버튼보다 위에 그린다.
- **검증**: 1280×900, 1280×700, 390×844에서 카드 사각형이 모두 화면 안(예: 데스크톱 위 64 · 아래 578 · 오른쪽 1272 ≤ 1280)

### BUG-010 · [P2] 기기의 "애니메이션 줄이기"가 켜져 있으면 플레이 탭 버튼 보드 등 사이트 애니메이션이 멈춤
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-25 (사용자 제보 "애니메이션은 왜 재생 안 되는지")
- **위치**: `src/App.jsx` — 일반 대국 버튼 마스터 대국 재생(`MgMasterReplay`), 미니게임 버튼 보드(`MgKnightRun`·`MgScenePlayer`·조준경 CSS `.mg-anim`), 좌표 인지 게임 조준경·결과(`.cc-anim`), 나이트 레이스 목표 칸 맥동(`kgPulse`), 도감 현재 줄·서지 효과, 그랜드마스터 보드 광택
- **증상**: 설정과 무관하게 버튼 속 보드가 첫 장면에서 멈춰 있고, 미니게임 안의 짧은 연출도 생략·정지됨
- **재현 방법**: Playwright `reducedMotion: "reduce"`로 플레이 탭을 열고 2.5초 간격으로 두 번 캡처 → 수정 전 두 화면이 똑같음(정지), 설정을 끄면 달라짐
- **근본 원인**: BUG-009와 같다 — 여러 컴포넌트가 제각각 `prefers-reduced-motion`(JS `matchMedia`·CSS `@media`)으로 애니메이션을 껐다. 사용자 기기에 이 설정이 켜져 있어(과거 매칭 대기 궤도 아이콘 제보 때도 확인됨) 같은 종류의 "애니메이션이 안 나온다" 버그가 반복됐다.
- **수정 내용**: 520px 조정과 함께 src/의 기기 설정 검사를 전부 제거(`mgReducedMotion` 함수 삭제, CSS `@media (prefers-reduced-motion)` 규칙 5곳 삭제). 애니메이션 on/off는 설정 탭 "시각 효과"만 정한다는 원칙으로 통일.
- **안전장치**: `scripts/check-reduced-motion.mjs` — src/ 코드(주석 제외)에 `prefers-reduced-motion`·`useReducedMotion`·`reducedMotion:`이 들어오면 빌드 실패. `prebuild`에 연결, `npm run check:reduced-motion`으로 단독 실행. 수정 전 코드에 돌려 5곳을 잡는 것 확인.
- **검증**: 위 재현 절차에서 수정 후 두 화면이 달라짐(재생됨) 확인, `npm run build` 통과

### BUG-009 · [P2] 기기의 "애니메이션 줄이기"가 켜져 있으면 수 등급 이펙트가 전혀 뜨지 않음
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-25 (사용자 제보 "이펙트가 안 뜨는데?")
- **위치**: `src/App.jsx` Board 수 등급 이펙트 트리거, 무한 체크메이트 게임 `gradeMove`
- **증상**: 설정 탭 "시각 효과 → 수 등급 이펙트"가 켜져 있어도 분석·학습·퍼즐·리뷰·무한 체크메이트 어디서도 이펙트가 뜨지 않고 평소 배지만 보임
- **재현 방법**: 1. Windows "애니메이션 효과" 끄기 / iOS "동작 줄이기" / Android "애니메이션 삭제"를 켠다(브라우저에선 `prefers-reduced-motion: reduce`) 2. 분석 탭에서 FEN `6k1/5ppp/8/8/8/8/5PPP/R5K1 w - - 0 1` 입력 후 Ra8# 3. 이펙트 없이 배지만 뜸
- **근본 원인**: 이펙트 트리거가 `mgReducedMotion()`이면 이펙트를 통째로 건너뛰었다. 기기 설정은 이 사이트를 위해 켠 게 아닌 경우가 많고(성능·배터리 때문에 끄는 경우가 흔함), 화면엔 아무 안내가 없어 설정 토글과 실제 동작이 어긋났다.
- **수정 내용**: 이펙트를 켜고 끄는 건 설정 탭 토글만 정한다. 기기의 동작 줄이기는 가장 큰 움직임인 기물 미끄러짐·꼬리만 생략한다. 무한 체크메이트 게임의 채점(`gradeMove`)도 같은 조건을 뺐다.
- **검증**: Playwright로 `reducedMotion: "reduce"`를 켠 상태에서 위 재현 절차 → 수정 전 배지만 / 수정 후 칸 색·★·"최선의 수" 이름표가 뜨는 것 확인

### BUG-008 · [P3] 수 등급 이펙트 이름표 글자가 알약 가운데에 오지 않음
- **상태**: 수정 완료 (v0.5.6)
- **발견일**: 2026-09-25 (사용자 제보)
- **위치**: `src/App.jsx` `MoveClassFx` 이름표 알약
- **증상**: "탁월합니다"·"유일한 수"·"최선의 수" 글자가 알약 안에서 왼쪽으로 치우쳐 보임
- **근본 원인**: 글자를 `left: padX`로 왼쪽에 붙이고, 알약 폭은 캔버스로 잰 글자 폭(×0.98)으로 계산했다. 잰 폭과 실제 렌더 폭(글꼴 대체·자간)이 다르면 그 차이가 전부 오른쪽 여백으로 몰린다.
- **수정 내용**: 글자 칸을 알약 전체(`inset: 0`)로 넓히고 `justifyContent: center`로 가운데 정렬. 폭 측정이 어긋나도 좌우 여백이 똑같이 나뉜다.
- **검증**: 테스트 페이지에서 세 등급 이펙트를 렌더해 스크린샷으로 좌우 여백이 같은지 확인

### BUG-007 · [P3] 설정 탭 문구에 "국면" 용어가 남아 있음
- **상태**: 수정 완료
- **발견일**: 2026-09-25
- **위치**: `src/App.jsx` SettingsTab "포지션 변동성 보정" 설명
- **증상**: "날카로운 국면의 실수를…" — README 용어 규칙("국면" 대신 "포지션") 위반
- **근본 원인**: 규칙 도입 전에 쓴 문구
- **수정 내용**: "날카로운 포지션의 실수를…"로 교체 (4f1d249)
- **검증**: 설정 탭 화면에서 문구 확인

### BUG-006 · [P2] 보드 수 등급 이펙트가 끝나도 칸에 겹겹이 남음
- **상태**: 수정 완료(배포 전, v0.5.5 개발 중 발견)
- **발견일**: 2026-09-25
- **위치**: `src/App.jsx` Board — `MoveClassFx`·`MoveFxSlide`
- **증상**: 분석 탭에서 최선의 수를 여러 번 두면 끝난 이펙트(옅은 칸 색·배지·그림자)가 칸마다 여러 겹 남음
- **재현 방법**: 1. 분석 탭에서 이펙트가 나는 수를 여러 번 둔다 2. DOM에 같은 칸의 이펙트 레이어가 수십 개 쌓임
- **근본 원인**: 같은 칸 안의 형제 요소 두 개(`MoveClassFx`, `MoveFxSlide`)에 같은 `key`(이펙트 id)를 줘서, React가 언마운트할 노드를 잘못 짚어 DOM이 남음
- **수정 내용**: key를 `"fx"+id` / `"slide"+id`로 분리 (27a1777)
- **검증**: Playwright로 7수 연속 둔 뒤 `z-index:7` 이펙트 레이어 수 0 확인

### BUG-005 · [P3] 무한 체크메이트 버튼 장면이 메이트가 아닌데 메이트로 표시됨
- **상태**: 수정 완료
- **발견일**: 2026-09-25 (사용자 제보)
- **위치**: `scripts/build-hub-scenes.mjs` → `src/data/hubScenes.json`
- **증상**: 플레이 탭 미니게임 버튼 속 보드에서 체크메이트가 아닌 포지션에 "#" 표시 후 다음 장면으로 넘어감
- **근본 원인**: 장면은 원래 포지션 기준으로 메이트가 검증돼 있었지만, 버튼에는 4×7 창 안의 기물만 그려서 창 밖 기물이 막아 주던 도주 칸이 보이지 않음
- **수정 내용**: 창 안 기물만 남긴 포지션에서 같은 수순을 다시 두어 체크메이트로 끝나는 장면만 남김(보이지 않는 공격 측 킹은 수비 킹과 4칸 이상 떨어져 있을 때만 허용), 68 → 31개 (454bc61)
- **재발 방지 안전장치**: 검증이 생성 스크립트(`visibleMateHolds`) 안에 있어 장면을 다시 만들 때마다 자동 적용
- **검증**: 스크립트 재실행 결과 1수 23개·2수(3수순) 8개, 러시아워 장면은 변화 없음

### BUG-004 · [P2] 무한 체크메이트 게임이 시작 직후 동작하지 않음
- **상태**: 수정 완료
- **발견일**: 2026-09-24 (사용자 제보)
- **위치**: `src/App.jsx` 무한 체크메이트(공격 모드) 혼자·봇·대전 화면
- **증상**: 3초 카운트다운이 끝나도 포지션이 뜨지 않아 게임이 안 되는 것처럼 보임
- **근본 원인**: 카운트다운 시작 시각을 포지션 풀 로딩과 무관하게 잡아, 로딩이 늦으면 카운트다운이 먼저 끝남(295개 포지션 데이터 자체는 전부 정상)
- **수정 내용**: 포지션 풀을 다 불러온 뒤에 카운트다운 시작 (806f15e)
- **검증**: 혼자·봇 플레이로 카운트다운 뒤 첫 포지션이 바로 뜨는 것 확인

### BUG-003 · [P2] 러시아워 봇 대전에서 봇이 못 푸는 라운드는 결과가 최대 2분 늦게 뜸
- **상태**: 수정 완료
- **발견일**: 2026-09-24
- **위치**: `src/App.jsx` 러시아워 봇 대전
- **증상**: 내가 먼저 풀어도 라운드 제한시간(2분)이 다 될 때까지 결과가 안 뜸
- **근본 원인**: 봇의 결과는 라운드 시작 때 이미 정해져 있는데도 봇 보고 타이머가 끝날 때까지 판정을 미룸
- **수정 내용**: 봇 계획으로 승패가 더 바뀔 수 없으면 즉시 판정 (ff6d857)
- **검증**: 봇이 못 푸는 라운드에서 내가 풀자마자 결과 표시 확인

### BUG-002 · [P2] AnimatedMove·FocusPanel의 조건부 훅 호출
- **상태**: 수정 완료
- **발견일**: 2026-09-24
- **위치**: `src/App.jsx` AnimatedMove, FocusPanel
- **증상**: 수 해석 여부나 패널 상태가 바뀌는 순간 "훅 개수가 달라졌다"는 React 오류로 화면이 깨질 수 있음
- **근본 원인**: 조기 반환 뒤에 `useMemo` 등 훅이 있어 렌더마다 훅 호출 수가 달라짐
- **수정 내용**: AnimatedMove는 훅을 일반 계산으로 바꾸고, FocusPanel은 조기 반환을 바깥 껍데기 컴포넌트(FocusPanel → FocusPanelBody)로 분리 (ff6d857)
- **검증**: 빌드·린트(react-hooks/rules-of-hooks) 통과

### BUG-001 · [P1] 미니게임 랜덤 매칭을 누르면 대기열 대신 곧장 패배
- **상태**: 수정 완료
- **발견일**: 2026-09-24 (사용자 제보)
- **위치**: `src/App.jsx` `useMinigameMatch` — `pvp_queue_join` 호출
- **증상**: 대기열에 상대가 없을 때 매칭 버튼을 누르면 대기 화면 없이 "패배"가 뜸(100% 재현)
- **재현 방법**: 1. 대기열에 아무도 없는 상태에서 2. 미니게임 "대전 상대 찾기"를 누른다
- **근본 원인**: `pvp_queue_join`(returns public.pvp_games)이 상대가 없어 SQL NULL을 돌려주면, PostgREST는 이를 "모든 필드가 null인 객체"로 직렬화한다. `if (g)`가 이를 매칭된 대전으로 오인해 status가 null인 빈 대전을 열었고, 대전 화면이 곧장 끝난 대전(패배)으로 판정. 체스 PvP에서 v0.4.x에 고친 같은 버그가 v0.5.3 미니게임 매칭 훅을 새로 만들며 재발
- **수정 내용**: id가 있고 active인 대전만 받고, 다른 게임 종류의 진행 중 대전이면 열지 않고 안내 (93eb697)
- **재발 방지 안전장치**: ① `sbRpcRow()`(`src/lib/supabaseClient.js`) — 모든 값이 null인 객체를 null로 바꾸는 행 반환 RPC 전용 호출 함수, 체스·미니게임 두 호출부 모두 전환 ② `scripts/check-rpc-null-rows.mjs` — `supabase-setup.sql`에서 한 행을 돌려주면서 `return null`이 있는 함수를 자동으로 찾아, `src/`에서 그 함수를 `sbRpc`로 직접 부르면 실패. `prebuild`로 등록돼 `npm run build`(Vercel 배포 포함) 때마다 실행
- **검증**: 수정 후 빈 대기열에서 대기 화면 유지 확인. 검사 스크립트는 호출 하나를 `sbRpc`로 되돌리면 exit 1로 빌드를 막고, 되돌리면 통과함을 확인

<!--
새 버그는 아래 템플릿을 복사해 맨 위에 추가한다. ID는 BUG-001부터 순서대로 매긴다.

### BUG-000 · [P1] 제목
- **상태**: 발견 / 수정 중 / 수정 완료 / 보류
- **발견일**: YYYY-MM-DD
- **위치**: `path/to/file.js:123`
- **증상**: 사용자 입장에서 무엇이 잘못되는가
- **재현 방법**: 1. ... 2. ...
- **근본 원인**: 왜 발생했는가
- **수정 내용**: 무엇을 어떻게 고쳤는가 (커밋 해시)
- **재발 방지 안전장치**: 테스트/검증/구조 변경 (P1 필수)
- **검증**: 수정 전 재현 → 수정 후 통과 확인 방법
-->
