# Supabase → Project Settings → API 에서 복사
# 이 파일을 .env 로 복사해 값을 채우면 백엔드가 활성화됩니다(미설정 시 자동으로 브라우저 localStorage 사용).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
